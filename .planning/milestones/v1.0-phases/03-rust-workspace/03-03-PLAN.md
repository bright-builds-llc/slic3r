---
phase: 03-rust-workspace
plan: "03"
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - packages/slic3r-rust/README.md
  - docs/port/README.md
  - docs/port/checklist.md
  - docs/port/package-map.md
autonomous: true
requirements:
  - RUST-01
  - RUST-02
must_haves:
  truths:
    - Contributors can discover the Rust package structure and Bazel commands from repo-owned docs.
    - The migration checklist shows the Rust workspace and verification milestones as complete once the code lands.
    - The control-plane docs describe the Rust package as a Bright Builds-compliant workspace, not a placeholder skeleton.
  artifacts:
    - path: packages/slic3r-rust/README.md
      provides: Package-local contributor guide for the Rust workspace
      contains: "//packages/slic3r-rust:verify"
    - path: docs/port/checklist.md
      provides: Updated phase checklist reflecting completed Rust workspace items
      contains: "Rust toolchain integration exists in Bazel"
    - path: docs/port/package-map.md
      provides: Updated package role text for the Rust package
      contains: "Bright Builds"
  key_links:
    - from: packages/slic3r-rust/README.md
      to: packages/slic3r-rust/BUILD.bazel
      via: documented Bazel labels
      pattern: "//packages/slic3r-rust:verify"
    - from: docs/port/checklist.md
      to: MODULE.bazel
      via: checklist item for Rust toolchain integration
      pattern: "Rust toolchain integration exists in Bazel"
---

<objective>
Align the Rust package and migration docs with the real Phase 3 workspace and verification surfaces.

Purpose: Make the Rust package discoverable and clearly documented as the new Bazel-backed implementation surface without overstating parity progress.
Output: A package README plus updated `docs/port/` guidance for the Rust workspace and verify commands.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-rust-workspace/03-CONTEXT.md
@.planning/phases/03-rust-workspace/03-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@../coding-and-architecture-requirements/standards/core/verification.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/02-legacy-oracle/02-03-SUMMARY.md
@docs/port/README.md
@docs/port/checklist.md
@docs/port/package-map.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add package-local Rust workspace guidance</name>
  <files>packages/slic3r-rust/README.md</files>
  <action>Create a package README that explains the role of `packages/slic3r-rust`, the internal `crates/slic3r_core` layout, the Bazel build label `//packages:rust_build`, the package verification label `//packages/slic3r-rust:verify`, and the expected write-mode formatter path via `bazel run @rules_rust//:rustfmt`. Keep the README phase-scoped and factual: do not promise CLI parity or invent Phase 5 module boundaries that do not exist yet.</action>
  <verify>
    <automated>rg -n "//packages:rust_build|//packages/slic3r-rust:verify|@rules_rust//:rustfmt" packages/slic3r-rust/README.md</automated>
  </verify>
  <done>The Rust package has a discoverable README that explains its current structure and Bazel-native workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Update the migration control plane for the Rust workspace</name>
  <files>docs/port/README.md, docs/port/checklist.md, docs/port/package-map.md</files>
  <action>Update the Phase 3 docs to reflect that the Rust package now has a pinned Bazel toolchain, a real internal crate, and Bazel-native format/lint/test verification per RUST-01 and RUST-02. Mark the Rust workspace checklist items complete, describe the package as a Bright Builds-compliant workspace instead of a skeleton, and document Bazelisk as the expected local Bazel launcher on macOS. Leave the parity-status vocabulary and parity matrix semantics alone because user-facing parity surfaces are still legacy-only in this phase.</action>
  <verify>
    <automated>rg -n "Bright Builds|Bazelisk|Rust toolchain integration exists in Bazel|Rust format/lint/test targets exist in Bazel|slic3r_core" docs/port/README.md docs/port/checklist.md docs/port/package-map.md</automated>
  </verify>
  <done>The `docs/port/` control plane reflects the real Phase 3 Rust workspace and verification state without overstating contract parity progress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Implemented Rust surfaces → contributor-facing docs | Docs must match the actual Bazel labels and package layout |
| Migration docs → future phase planning | Inaccurate docs could cause later plans to assume parity or package structure that does not exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | R | `docs/port/*` | mitigate | Update the control-plane docs in the same phase with exact Bazel labels and completed checklist items |
| T-03-03-02 | T | Rust package documentation | mitigate | Explicitly document that Phase 3 changes workspace/tooling status only and leave parity surfaces marked legacy-only |
</threat_model>

<verification>
Run `rg -n "//packages:rust_build|//packages/slic3r-rust:verify|@rules_rust//:rustfmt" packages/slic3r-rust/README.md`.
Run `rg -n "Bright Builds|Bazelisk|Rust toolchain integration exists in Bazel|Rust format/lint/test targets exist in Bazel|slic3r_core" docs/port/README.md docs/port/checklist.md docs/port/package-map.md`.
</verification>

<success_criteria>
Contributors can discover the Rust workspace structure and Bazel-native commands from repo-owned docs, and the Phase 3 control-plane docs reflect the completed workspace and verification surfaces accurately.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rust-workspace/03-rust-workspace-03-SUMMARY.md`
</output>
