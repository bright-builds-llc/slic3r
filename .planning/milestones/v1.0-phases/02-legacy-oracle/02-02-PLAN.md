---
phase: 02-legacy-oracle
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/legacy-slic3r/BUILD.bazel
  - tools/bazel/legacy/test_legacy_oracle.sh
  - tools/bazel/legacy/test_legacy_smoke.sh
  - tools/bazel/legacy/check_legacy_prereqs.sh
autonomous: true
requirements:
  - MONO-03
  - LEGA-02
user_setup: []
must_haves:
  truths:
    - Maintainer can run a trusted legacy oracle test surface through Bazel on macOS.
    - The first-class oracle test set is narrow enough to be reliable but meaningful enough to preserve the retained implementation as a reference.
  artifacts:
    - At least one Bazel-exposed legacy smoke or test wrapper exists.
    - A package-level aggregate target or suite exists for the trusted oracle set.
    - The test wrapper(s) reuse the documented prerequisite gate where needed.
  key_links:
    - The trusted oracle test target is reachable from the Bazel surface created in Plan 01.
    - The test target executes the chosen legacy oracle path instead of a disconnected placeholder.
---

<objective>
Expose the first trusted legacy oracle test set through Bazel on macOS.

Purpose: make the retained legacy package testable enough to serve as a credible oracle before later parity phases depend on it.
Output: trusted oracle smoke/test wrappers and stable Bazel labels for the retained legacy verification surface.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-legacy-oracle/02-CONTEXT.md
@.planning/phases/02-legacy-oracle/02-RESEARCH.md

# Prior plan outputs:
@.planning/phases/02-legacy-oracle/02-01-SUMMARY.md

[Relevant source files:]
@packages/legacy-slic3r/Build.PL
@packages/legacy-slic3r/src/CMakeLists.txt
@packages/legacy-slic3r/t/
@packages/legacy-slic3r/src/test/
@packages/legacy-slic3r/BUILD.bazel
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement trusted legacy oracle smoke and test wrappers</name>
  <files>tools/bazel/legacy/test_legacy_smoke.sh, tools/bazel/legacy/test_legacy_oracle.sh, packages/legacy-slic3r/BUILD.bazel</files>
  <action>Choose the first trusted legacy oracle set for macOS based on the Phase 2 research. Prioritize the most reliable and meaningful retained test/smoke paths rather than historical completeness. If the full Perl or native suite is too brittle for Phase 2, expose a narrower smoke/core target and make that choice explicit in the wrapper behavior and later docs. Reuse the prerequisite gate from Plan 01 instead of duplicating environment checks.</action>
  <verify>Run the trusted oracle Bazel target on macOS and capture whether it passes or fails for a documented prerequisite reason.</verify>
  <done>A real Bazel target executes the chosen trusted oracle test surface for the retained legacy package.</done>
</task>

<task type="auto">
  <name>Task 2: Publish stable Bazel labels for the oracle test set</name>
  <files>packages/legacy-slic3r/BUILD.bazel, packages/BUILD.bazel, BUILD.bazel</files>
  <action>Add stable labels for the trusted oracle set and any aggregate target needed for discoverability. Keep the surface explicit: this phase should make it obvious which tests are first-class oracle checks, not imply that every historical test path is now equally supported. Do not add GUI test coverage or release packaging checks here.</action>
  <verify>Use the stable Bazel label from the root or package surface and confirm it reaches the trusted oracle wrapper rather than a placeholder.</verify>
  <done>The trusted oracle test surface is discoverable and invokable from stable Bazel labels.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] A Bazel target runs the trusted legacy oracle set on macOS
- [ ] The chosen oracle set reflects reliability and reference value, not historical breadth
- [ ] GUI and release packaging test paths remain out of scope
</verification>

<success_criteria>

- All tasks completed
- The retained legacy package is testable as an oracle through Bazel
- The trusted oracle set is explicit and intentionally scoped
- Later parity phases can rely on this test surface as a reference point
</success_criteria>

<output>
After completion, create `.planning/phases/02-legacy-oracle/02-02-SUMMARY.md`
</output>
