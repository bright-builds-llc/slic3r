---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .bazelversion
  - MODULE.bazel
  - BUILD.bazel
  - .bazelrc
  - packages/BUILD.bazel
  - packages/slic3r-rust/Cargo.toml
  - packages/launcher/BUILD.bazel
  - packages/parity/BUILD.bazel
  - packages/parity-fixtures/BUILD.bazel
autonomous: true
requirements:
  - MONO-01
  - MONO-02
user_setup: []
must_haves:
  truths:
    - Maintainer can invoke a repo-wide Bazel build and test entrypoint on macOS for the new scaffold.
    - Contributors can see the locked `packages/` skeleton and infer the role of each package from the repo structure.
  artifacts:
    - Root Bazel files exist and define the canonical monorepo entrypoint.
    - The locked package directories exist under `packages/`.
    - "`packages/slic3r-rust` contains the initial Cargo workspace root."
  key_links:
    - Root Bazel targets reference the package skeleton instead of leaving it disconnected.
    - The Rust package shape matches the locked "one top-level package, internal workspace" decision.
---

<objective>
Create the Bazel root and locked package scaffolding for the Slic3r monorepo foundation.

Purpose: establish one canonical macOS-first repo entrypoint before legacy wrapping or docs work proceeds.
Output: root Bazel files, locked `packages/` skeleton, and the initial Rust workspace root.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

[Relevant source files:]
@README.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Bazel root and canonical entrypoint files</name>
  <files>.bazelversion, MODULE.bazel, BUILD.bazel, .bazelrc</files>
  <action>Create the macOS-first Bazel foundation using the phase research recommendations: pin Bazel through `.bazelversion`, use a Bzlmod-first `MODULE.bazel`, and make the repository root the canonical build/test surface. Add only the minimum root-level targets and configuration needed for `bazel build //...` and `bazel test //...` to resolve against the scaffold. Do not attempt to wrap every legacy script here; Phase 1 only needs a real top-level interface, not full legacy coverage.</action>
  <verify>Run the pinned Bazel entrypoint on macOS so the repo scaffold resolves cleanly, for example `bazelisk build //...` and `bazelisk test //...` with only the Phase 1 scaffold targets in scope.</verify>
  <done>Root Bazel files exist, use the pinned Bazelisk/Bzlmod path, and provide a discoverable repo-wide entrypoint without relying on ad hoc root commands.</done>
</task>

<task type="auto">
  <name>Task 2: Create the locked package skeleton and Rust workspace root</name>
  <files>packages/BUILD.bazel, packages/slic3r-rust/Cargo.toml, packages/launcher/BUILD.bazel, packages/parity/BUILD.bazel, packages/parity-fixtures/BUILD.bazel</files>
  <action>Create exactly the locked Phase 1 package skeleton: `packages/legacy-slic3r`, `packages/slic3r-rust`, `packages/launcher`, `packages/parity`, and `packages/parity-fixtures`. Keep root-owned assets at the repo root. Initialize `packages/slic3r-rust` as one top-level Rust package with an internal Cargo workspace root and placeholder crate layout hooks, not multiple top-level Rust packages. Add only the minimal Bazel placeholder targets needed so the package roles are visible and the repo-wide scaffold resolves.</action>
  <verify>Confirm the package skeleton exists with the expected paths, and re-run the root Bazel build/test entrypoint to prove the skeleton is wired into the repo surface.</verify>
  <done>The locked package set exists exactly once under `packages/`, the Rust side starts as a single top-level package with an internal workspace root, and contributors can infer package roles from the scaffold.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bazelisk build //...` resolves against the scaffold on macOS
- [ ] `bazelisk test //...` resolves against the scaffold on macOS
- [ ] `find packages -maxdepth 2 -type d | sort` shows the locked package skeleton
</verification>

<success_criteria>

- All tasks completed
- Root Bazel files establish the canonical entrypoint
- Locked `packages/` skeleton exists and is discoverable
- No extra top-level Rust packages or root-to-package ownership drift introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
