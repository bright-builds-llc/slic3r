---
phase: 52-executable-structural-g-code-evidence
plan: "02"
type: execute
wave: 2
depends_on:
  - "52-01"
files_modified:
  - packages/parity/BUILD.bazel
  - packages/parity/compare_prusaslicer_gcode_output.sh
  - packages/parity/compare_prusaslicer_gcode_output_test.sh
autonomous: true
requirements:
  - GCEV-01
  - GCEV-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 52-2026-06-18T01-09-43
generated_at: 2026-06-18T01:27:32.342Z
must_haves:
  truths:
    - "Maintainer can keep running `bazel run //packages/parity:prusaslicer_gcode_output_parity` as the public evidence command."
    - "The public command validates both `expected-gcode-summary.tsv` and `expected-gcode-structural-summary.tsv` through the Rust summary binary."
    - "The public command prints structural path, row count, source identity, fixture identity, command counts, ordered markers, movement/extrusion indicators, and temperature/tool-change counts."
    - "The public command fails closed when `command_count_g1` is mutated from `4` to `3`."
  artifacts:
    - path: "packages/parity/BUILD.bazel"
      provides: "Structural sidecar data wiring for public command and failure test"
      contains: "prusa_gcode_output_expected_gcode_structural_summary"
    - path: "packages/parity/compare_prusaslicer_gcode_output.sh"
      provides: "Public Bash comparator validating summary and structural artifacts through Rust"
      contains: "--structural"
    - path: "packages/parity/compare_prusaslicer_gcode_output_test.sh"
      provides: "Command-level structural mutation guard"
      contains: "command_count_g1"
  key_links:
    - from: "packages/parity/BUILD.bazel"
      to: "//packages/parity-fixtures:prusa_gcode_output_expected_gcode_structural_summary"
      via: "sh_binary and sh_test data plus args"
      pattern: "prusa_gcode_output_expected_gcode_structural_summary"
    - from: "packages/parity/compare_prusaslicer_gcode_output.sh"
      to: "packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary"
      via: "`--structural` invocation"
      pattern: "--structural"
    - from: "packages/parity/compare_prusaslicer_gcode_output_test.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv"
      via: "temp-copy mutation of `command_count_g1`"
      pattern: "command_count_g1"
---

<objective>
Extend the public Prusa G-code parity command from summary-only evidence to executable structural evidence.

Purpose: GCEV-01 and GCEV-02 require the public command to validate the structural sidecar through Rust and prove command-level failure on a meaningful structural drift.
Output: updated Bazel target data/args, an extended fail-closed comparator, and one focused structural mutation guard.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
@.planning/phases/52-executable-structural-g-code-evidence/52-RESEARCH.md
@.planning/phases/52-executable-structural-g-code-evidence/52-01-SUMMARY.md
@packages/parity/BUILD.bazel
@packages/parity/compare_prusaslicer_gcode_output.sh
@packages/parity/compare_prusaslicer_gcode_output_test.sh
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv

<interfaces>
Public command target to preserve per D-01:
```text
//packages/parity:prusaslicer_gcode_output_parity
```

Comparator final argument contract:
```text
compare_prusaslicer_gcode_output.sh \
  summary_binary \
  rust-summary-input.tsv \
  expected-gcode-summary.tsv \
  rust-structural-input.tsv \
  expected-gcode-structural-summary.tsv \
  fixture-provenance.tsv
```

Required public success-output lines:
```text
ok: fork.prusaslicer.gcode-output structural parity passed
source_ref: prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
fixture: packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode
expected: packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
structural_expected: packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
summary_rows: 5
structural_rows: 16
command_counts: total=4 g1=4
ordered_markers: G1 F99999.123 | G1 F1 | G1 F203.2 | G1 F203.201
movement_extrusion: movement_axis_present=false extrusion_axis_present=false
temperature_tool_change_markers: temperature_marker_count=0 tool_change_marker_count=0
```
</interfaces>
</context>

<decision_coverage>
- D-01: keep `//packages/parity:prusaslicer_gcode_output_parity` as the public entry point.
- D-02: validate both summary and structural artifacts through Rust.
- D-03: print maintainer-readable structural facts in command output.
- D-04: validate checked-in evidence only; do not generate or compare fresh runtime G-code.
- D-05, D-06, D-07: add exactly one command-level structural drift mutation guard for `command_count_g1`.
- D-12: include public command, failure test, Rust tests, and fixture/scope verifiers in verification.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Wire structural sidecar into the public comparator target</name>
  <files>packages/parity/BUILD.bazel, packages/parity/compare_prusaslicer_gcode_output.sh</files>
  <read_first>
    packages/parity/BUILD.bazel
    packages/parity/compare_prusaslicer_gcode_output.sh
    packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
    packages/parity-fixtures/BUILD.bazel
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
    .planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
  </read_first>
  <action>
    In `packages/parity/BUILD.bazel`, add `//packages/parity-fixtures:prusa_gcode_output_expected_gcode_structural_summary` to both `prusaslicer_gcode_output_parity` and `prusaslicer_gcode_output_parity_failure_test` data. Change the public target args to pass six values in this exact order: summary binary, expected summary as Rust summary input, expected summary as expected artifact, expected structural summary as Rust structural input, expected structural summary as expected structural artifact, fixture provenance. In `compare_prusaslicer_gcode_output.sh`, require exactly six arguments and validate the new `expected-gcode-structural-summary.tsv` path with `assert_file`. Keep the existing summary validation unchanged, then run `"${summary_binary}" --structural "${rust_structural_input}"` and `"${summary_binary}" --structural "${expected_structural_artifact}"`, diff those generated structural summaries, assert the D-03 fields, and print the exact success lines listed in this plan.
  </action>
  <acceptance_criteria>
    `rg -n "prusa_gcode_output_expected_gcode_structural_summary" packages/parity/BUILD.bazel` finds the structural data in the public command and failure test blocks.
    `rg -n -- "--structural|expected-gcode-structural-summary.tsv|structural_rows: 16|command_counts: total=4 g1=4|temperature_tool_change_markers" packages/parity/compare_prusaslicer_gcode_output.sh` finds the public structural behavior.
    `bazel run //packages/parity:prusaslicer_gcode_output_parity` prints `ok: fork.prusaslicer.gcode-output structural parity passed`, `structural_expected: packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv`, `structural_rows: 16`, and `command_counts: total=4 g1=4`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n packages/parity/compare_prusaslicer_gcode_output.sh</automated>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
  </verify>
  <done>The preserved public target validates both checked-in summaries through Rust and exposes structural facts in maintainer-readable output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add command-level structural drift mutation guard</name>
  <files>packages/parity/compare_prusaslicer_gcode_output_test.sh</files>
  <read_first>
    packages/parity/compare_prusaslicer_gcode_output_test.sh
    packages/parity/compare_prusaslicer_gcode_output.sh
    packages/parity/BUILD.bazel
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
    packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
  </read_first>
  <behavior>
    - Existing `line_4` summary mutation still fails and reports `expected-gcode-summary.tsv`, `line_4`, and a unified diff.
    - New structural mutation changes only `command_count_g1` from `4` to `3`, fails the public comparator, and reports `expected-gcode-structural-summary.tsv` plus `command_count_g1`.
  </behavior>
  <action>
    Add `expected_gcode_structural_summary="${fixture_dir}/expected-gcode-structural-summary.tsv"` and assert it exists. Update `run_comparator` to accept both mutated summary and mutated structural artifact paths, then pass all six comparator arguments. Add `mutate_command_count_g1_value()` using Bash-3.2-compatible `awk` with `FS = OFS = "\t"` and the predicate `$3 == "command_count_g1" && $5 == "4" { $5 = "3"; changed++ }`. Keep the existing `line_4` test, then add a second temp-copy mutation test that mutates only the structural file, expects non-zero comparator exit, and asserts stderr contains `expected-gcode-structural-summary.tsv` and `command_count_g1`. Do not duplicate Phase 50 fixture-verifier or Phase 51 Rust parser rejection matrices.
  </action>
  <acceptance_criteria>
    `rg -n "expected_gcode_structural_summary|mutate_command_count_g1|command_count_g1|expected-gcode-structural-summary.tsv" packages/parity/compare_prusaslicer_gcode_output_test.sh` finds all structural mutation guard strings.
    `bazel test --cache_test_results=no //packages/parity:prusaslicer_gcode_output_parity_failure_test` passes.
    The failure test still includes `assert_contains "${expected_gcode_summary}" $'line_4\tG1 F203.201'`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n packages/parity/compare_prusaslicer_gcode_output_test.sh</automated>
    <automated>bazel test --cache_test_results=no //packages/parity:prusaslicer_gcode_output_parity_failure_test</automated>
    <automated>bazel test --cache_test_results=no //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check</automated>
  </verify>
  <done>The parity package has one focused command-level structural mutation guard for `command_count_g1` drift, with diagnostics naming the structural artifact and field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazel runfiles to Bash comparator | Public command receives checked-in artifacts and a Rust binary through Bazel data wiring. |
| Bash comparator to Rust parser | Shell orchestration invokes the Rust parser binary and must not hide parse failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02-01 | Tampering | `expected-gcode-structural-summary.tsv` public evidence | mitigate | Run `prusa_gcode_output_summary --structural` on both Rust input and expected artifact, diff generated structural summaries, and assert exact structural facts. |
| T-52-02-02 | Repudiation | comparator diagnostics | mitigate | Add structural mismatch labels from column 3 and require diagnostics containing `expected-gcode-structural-summary.tsv` and `command_count_g1`. |
| T-52-02-03 | Elevation of privilege | comparator execution path | mitigate | Keep comparator limited to checked-in runfiles and the Rust summary binary; do not add PrusaSlicer execution, upstream fetch, generated G-code creation, upload, release, or sync commands. |
</threat_model>

<verification>
Run both task verification sets, then run `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture`, `bazel run //packages/prusa-gcode-output-scope:verify`, and `git diff --check`.
</verification>

<success_criteria>
- `//packages/parity:prusaslicer_gcode_output_parity` remains the public command.
- The command output includes the structural sidecar path, row count 16, command counts, ordered markers, movement/extrusion indicators, and temperature/tool-change marker counts.
- `//packages/parity:prusaslicer_gcode_output_parity_failure_test` fails closed on both the existing `line_4` mutation and the new `command_count_g1` structural mutation.
</success_criteria>

<output>
After completion, create `.planning/phases/52-executable-structural-g-code-evidence/52-02-SUMMARY.md`.
</output>
