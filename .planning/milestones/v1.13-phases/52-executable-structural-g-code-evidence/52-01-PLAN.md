---
phase: 52-executable-structural-g-code-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
autonomous: true
requirements:
  - GCEV-01
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 52-2026-06-18T01-09-43
generated_at: 2026-06-18T01:27:32.342Z
must_haves:
  truths:
    - "Maintainer-facing Rust helper can validate the structural sidecar through `parse_prusa_gcode_output_structural_summary`."
    - "The existing `prusa_gcode_output_summary` binary keeps one-argument summary behavior and adds explicit `--structural <expected-gcode-structural-summary.tsv>` behavior."
    - "Structural helper output exposes path, row count, source identity, fixture identity, command counts, ordered markers, movement/extrusion indicators, and temperature/tool-change marker counts."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      provides: "Rust structural summary-lines helper over the Phase 51 typed parser"
      contains: "prusa_gcode_output_structural_summary_lines"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs"
      provides: "Public re-export for the structural summary-lines helper"
      contains: "prusa_gcode_output_structural_summary_lines"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs"
      provides: "Compatibility-preserving CLI structural mode"
      contains: "--structural"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs"
      provides: "Focused Rust coverage for structural summary lines"
      contains: "summarizes_expected_gcode_structural_summary_lines"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      via: "`prusa_gcode_output_structural_summary_lines` import"
      pattern: "prusa_gcode_output_structural_summary_lines"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv"
      via: "existing `EXPECTED_GCODE_STRUCTURAL_SUMMARY` fixture include"
      pattern: "EXPECTED_GCODE_STRUCTURAL_SUMMARY"
---

<objective>
Add the Rust structural summary adapter that Phase 52's public Bazel command will call.

Purpose: GCEV-01 requires the public evidence command to validate `expected-gcode-summary.tsv` and `expected-gcode-structural-summary.tsv` through Rust-owned parser boundaries, not through shell-only TSV checks.
Output: a tested `prusa_gcode_output_structural_summary_lines` helper and a compatibility-preserving `--structural` mode in the existing Rust summary binary.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
@.planning/phases/52-executable-structural-g-code-evidence/52-RESEARCH.md
@.planning/phases/51-rust-structural-g-code-summary-boundary/51-01-SUMMARY.md
@.planning/phases/51-rust-structural-g-code-summary-boundary/51-02-SUMMARY.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs

<interfaces>
From `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs`:
```rust
pub fn parse_prusa_gcode_output_structural_summary(
    input: &str,
) -> PrusaGcodeOutputStructuralParseResult;

pub const fn prusa_gcode_output_structural_readiness() -> PrusaGcodeOutputStructuralReadiness;

impl PrusaGcodeOutputStructuralSummary {
    pub fn rows(&self) -> &[PrusaGcodeOutputStructuralSummaryRow];
    pub fn facts(&self) -> PrusaGcodeOutputStructuralFacts;
}
```

Required structural values from `expected-gcode-structural-summary.tsv`:
```text
structural_row_count	16
source_ref	prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
fixture_id	gcodewriter-set-speed.gcode
fixture_path	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode
command_count_total	4
command_count_g1	4
section_count_total	1
ordered_marker_1	G1 F99999.123
ordered_marker_2	G1 F1
ordered_marker_3	G1 F203.2
ordered_marker_4	G1 F203.201
movement_axis_present	false
extrusion_axis_present	false
temperature_marker_count	0
tool_change_marker_count	0
```
</interfaces>
</context>

<decision_coverage>
- D-02: structural TSV validation must flow through Rust-owned parser boundaries.
- D-03: Rust structural output must expose all maintainer-visible structural facts needed by the public command.
- D-04: the adapter reads checked-in expected artifacts only; it does not generate fresh G-code or compare runtime output.
- D-13: Rust changes require the pinned Rust pre-commit sequence from `packages/slic3r-rust`.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add structural summary lines over the typed parser</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs, packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs</files>
  <read_first>
    packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
    packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
    packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
    .planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `prusa_gcode_output_structural_summary_lines(EXPECTED_GCODE_STRUCTURAL_SUMMARY)` returns a vector containing `structural_summary_path`, `structural_row_count`, source/fixture identity fields, command counts, four ordered marker lines, movement/extrusion booleans, and temperature/tool-change counts.
    - Test 2: The helper rejects the same malformed structural TSV inputs as `parse_prusa_gcode_output_structural_summary`, including a `command_count_g1` value changed from `4` to `3`.
  </behavior>
  <action>
    Add `pub fn prusa_gcode_output_structural_summary_lines(input: &str) -> Result<Vec<String>, PrusaGcodeOutputStructuralParseError>` in `prusa_gcode_output.rs`. It must call `parse_prusa_gcode_output_structural_summary(input)?`, call `prusa_gcode_output_structural_readiness()`, read `summary.rows().len()` and `summary.facts()`, and return exactly these tab-delimited keys: `structural_summary_path`, `structural_row_count`, `source_ref`, `inventory_source_paths`, `fixture_source_literal`, `fixture_id`, `fixture_path`, `command_count_total`, `command_count_g1`, `section_count_total`, `ordered_marker_1`, `ordered_marker_2`, `ordered_marker_3`, `ordered_marker_4`, `movement_axis_present`, `extrusion_axis_present`, `temperature_marker_count`, `tool_change_marker_count`. Re-export the helper from `lib.rs`. Add a focused test named `summarizes_expected_gcode_structural_summary_lines` that asserts the exact values listed in this plan.
  </action>
  <acceptance_criteria>
    `rg -n "pub fn prusa_gcode_output_structural_summary_lines" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` finds the helper.
    `rg -n "prusa_gcode_output_structural_summary_lines" packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs` finds the re-export and test.
    `rg -n "structural_row_count|command_count_g1|ordered_marker_4|temperature_marker_count|tool_change_marker_count" packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs` finds exact assertions.
  </acceptance_criteria>
  <verify>
    <automated>cargo +1.94.1 test -p slic3r_flavors --test prusa_gcode_output --manifest-path packages/slic3r-rust/Cargo.toml</automated>
    <automated>bazel test --cache_test_results=no //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test</automated>
  </verify>
  <done>The Rust helper parses the Phase 50 structural sidecar through the Phase 51 parser and emits all D-03 structural facts as stable tab-delimited summary lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit structural mode to the existing Rust binary</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs</files>
  <read_first>
    packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
    packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
    packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
  </read_first>
  <behavior>
    - Existing one-path invocation still prints summary-only lines for `expected-gcode-summary.tsv`.
    - New invocation `prusa_gcode_output_summary --structural expected-gcode-structural-summary.tsv` prints the structural lines from Task 1.
    - Invalid arguments fail with a usage message naming both `expected-gcode-summary.tsv` and `--structural expected-gcode-structural-summary.tsv`.
  </behavior>
  <action>
    Update `prusa_gcode_output_summary.rs` to match `env::args_os()` as `[_, fixture_path]` for existing summary mode and `[_, mode, fixture_path] if mode == "--structural"` for structural mode. Keep filesystem reads in this thin binary only. Summary mode must call `prusa_gcode_output_summary_lines`; structural mode must call `prusa_gcode_output_structural_summary_lines`. Use the error text `error: expected expected-gcode-summary.tsv or --structural expected-gcode-structural-summary.tsv` for invalid arguments. Do not add Git, network, process spawning, PrusaSlicer runtime execution, G-code generation, or upstream source import behavior.
  </action>
  <acceptance_criteria>
    `rg -n -- "--structural|expected-gcode-structural-summary.tsv|prusa_gcode_output_structural_summary_lines" packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs` finds all three strings.
    `bazel run //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary -- $(pwd)/packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv` prints `row_count	5`.
    `bazel run //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary -- --structural $(pwd)/packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv` prints `structural_row_count	16` and `command_count_g1	4`.
  </acceptance_criteria>
  <verify>
    <automated>bazel run //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary -- --structural "$(pwd)/packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv"</automated>
    <automated>bazel test --cache_test_results=no //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check</automated>
    <automated>cd packages/slic3r-rust &amp;&amp; cargo +1.94.1 fmt --all &amp;&amp; cargo +1.94.1 clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo +1.94.1 build --all-targets --all-features &amp;&amp; cargo +1.94.1 test --all-features</automated>
  </verify>
  <done>The existing Rust binary remains summary-compatible and provides an explicit structural parser mode for Plan 52-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args to Rust file reads | Local command arguments select checked-in TSV files to parse. |
| TSV text to Rust domain types | Raw TSV content becomes typed `PrusaGcodeOutputStructuralSummary` and `PrusaGcodeOutputStructuralFacts`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01-01 | Tampering | `prusa_gcode_output_structural_summary_lines` | mitigate | Parse with `parse_prusa_gcode_output_structural_summary` before projecting facts; add exact tests for `command_count_g1`, row count, markers, source identity, and fixture identity. |
| T-52-01-02 | Repudiation | `prusa_gcode_output_summary` CLI errors | mitigate | Invalid args and parse failures must name the path/mode clearly so a Rust parse failure cannot look like a successful structural evidence run. |
| T-52-01-03 | Elevation of privilege | Rust CLI adapter | mitigate | Keep adapter limited to caller-supplied file reads and forbid Git, network, process execution, PrusaSlicer runtime execution, upstream imports, uploads, releases, and sync behavior. |
</threat_model>

<verification>
Run the automated commands in each task. Because Rust files change, also run the full pinned Rust sequence from `packages/slic3r-rust`. Finish with `git diff --check`.
</verification>

<success_criteria>
- `prusa_gcode_output_structural_summary_lines` exists, is re-exported, and has focused Rust tests.
- `prusa_gcode_output_summary --structural` prints `structural_row_count	16` and `command_count_g1	4`.
- Existing summary mode still prints `row_count	5`.
- No runtime/upstream/G-code-generation behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/52-executable-structural-g-code-evidence/52-01-SUMMARY.md`.
</output>
