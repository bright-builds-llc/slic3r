---
phase: 52-executable-structural-g-code-evidence
plan: "03"
type: execute
wave: 3
depends_on:
  - "52-02"
files_modified:
  - packages/parity/status.tsv
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
autonomous: true
requirements:
  - GCEV-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 52-2026-06-18T01-09-43
generated_at: 2026-06-18T01:27:32.342Z
must_haves:
  truths:
    - "Maintainer can inspect `packages/parity/status.tsv` and see `fork.prusaslicer.gcode-output` described as narrow structural evidence."
    - "The broad `generated-outputs` status row remains exactly one `in progress` row."
    - "The fixture verifier requires the exact structural status row naming the Phase 49 closed structural scope contract, Phase 50 structural fixture summary, Phase 51 Rust parser/readiness boundary, and Phase 52 public parity command."
  artifacts:
    - path: "packages/parity/status.tsv"
      provides: "Exact narrow structural status publication"
      contains: "narrow structural Prusa G-code evidence slice"
    - path: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      provides: "Fixture verifier status-row enforcement updated to structural wording"
      contains: "GCODE_OUTPUT_STATUS_ROW"
    - path: "packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh"
      provides: "Fixture verifier mutation coverage updated for structural status wording"
      contains: "wrong_gcode_output_parity"
  key_links:
    - from: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      to: "packages/parity/status.tsv"
      via: "exact `GCODE_OUTPUT_STATUS_ROW` match"
      pattern: "GCODE_OUTPUT_STATUS_ROW"
    - from: "packages/parity/status.tsv"
      to: "//packages/parity:prusaslicer_gcode_output_parity"
      via: "evidence column"
      pattern: "fork\\.prusaslicer\\.gcode-output\\tverified\\t//packages/parity:prusaslicer_gcode_output_parity"
---

<objective>
Publish the narrow Prusa G-code status row as structural evidence and keep fixture verification exact.

Purpose: GCEV-03 requires status visibility for the structural evidence slice without promoting broad `generated-outputs`.
Output: an exact structural `fork.prusaslicer.gcode-output` status row and fixture verifier/test enforcement for that row.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
@.planning/phases/52-executable-structural-g-code-evidence/52-RESEARCH.md
@.planning/phases/52-executable-structural-g-code-evidence/52-02-SUMMARY.md
@packages/parity/status.tsv
@packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
@packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh

<interfaces>
Exact status row to publish:
```text
fork.prusaslicer.gcode-output	verified	//packages/parity:prusaslicer_gcode_output_parity	Shared fixture comparison proves the narrow structural Prusa G-code evidence slice backed by the Phase 49 closed structural scope contract, Phase 50 structural fixture summary, Phase 51 Rust structural parser/readiness boundary, and Phase 52 public parity command only; byte-for-byte G-code parity, full generated-output parity, toolpath geometry, extrusion, timing, support generation, wall seam behavior, arc fitting, STEP import, full 3MF import/export, printer-runtime behavior, firmware or printability, GUI export or viewer behavior, binary G-code, thumbnails, post-processing, host upload, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, and sync automation remain deferred
```

Broad status row invariant:
```text
generated-outputs	in progress	docs/port/cli-slice.md	Scoped export plus repair/split artifact naming are fixture-verified; geometry and output-content parity are deferred
```
</interfaces>
</context>

<decision_coverage>
- D-08: update `fork.prusaslicer.gcode-output` from summary-only wording to narrow structural evidence wording while preserving status token and command.
- D-09: keep broad `generated-outputs` exactly `in progress`.
- D-11: keep all deferred generated-output, runtime, release, network/device, non-Prusa fork, upstream import, and sync surfaces explicit.
- D-12: rerun the public command plus fixture verifier after status publication.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Publish exact structural status wording</name>
  <files>packages/parity/status.tsv</files>
  <read_first>
    packages/parity/status.tsv
    .planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Replace only the notes text of the existing `fork.prusaslicer.gcode-output` row with the exact structural status row from this plan. Preserve the first three fields exactly as `fork.prusaslicer.gcode-output`, `verified`, and `//packages/parity:prusaslicer_gcode_output_parity`. Do not change the `generated-outputs` row; it must remain `generated-outputs	in progress	docs/port/cli-slice.md	Scoped export plus repair/split artifact naming are fixture-verified; geometry and output-content parity are deferred`. Do not add a second `fork.prusaslicer.gcode-output` row and do not introduce any non-Prusa fork status row.
  </action>
  <acceptance_criteria>
    `awk -F '\t' '$1=="fork.prusaslicer.gcode-output" && $2=="verified" && $3=="//packages/parity:prusaslicer_gcode_output_parity" && index($4, "narrow structural Prusa G-code evidence slice") && index($4, "Phase 49 closed structural scope contract") && index($4, "Phase 50 structural fixture summary") && index($4, "Phase 51 Rust structural parser/readiness boundary") && index($4, "Phase 52 public parity command") { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    `awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    `rg -n "summary-only Prusa G-code evidence slice" packages/parity/status.tsv` finds no matches.
  </acceptance_criteria>
  <verify>
    <automated>bazel run //packages/parity:status</automated>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
  </verify>
  <done>The status source publishes only the narrow structural fork row and leaves broad generated-output status unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile fixture verifier status enforcement</name>
  <files>packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh, packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</files>
  <read_first>
    packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
    packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
    packages/parity/status.tsv
    packages/parity/BUILD.bazel
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
  </read_first>
  <action>
    Update `GCODE_OUTPUT_STATUS_ROW` in `verify_prusa_gcode_output_fixture.sh` to match the exact structural row from this plan. Preserve the existing split-literal pattern for `host upload` so `reject_verifier_behavior_terms` does not self-match. Update the wrong-evidence mutation in `verify_prusa_gcode_output_fixture_test.sh` to use the same structural notes text with evidence column `//packages/parity:wrong_gcode_output_parity`. Keep duplicate-row and missing-row mutation coverage intact. Do not remove the existing structural sidecar checks for header, field counts, exact rows, provenance alignment, forbidden metadata, or `command_count_g1`.
  </action>
  <acceptance_criteria>
    `rg -n "narrow structural Prusa G-code evidence slice|Phase 49 closed structural scope contract|Phase 50 structural fixture summary|Phase 51 Rust structural parser/readiness boundary|Phase 52 public parity command" packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` finds all required phrases.
    `rg -n "Phase 49 closed structural scope contract" packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` finds the required Phase 49 contract wording.
    `rg -n "narrow summary-only Prusa G-code evidence slice" packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` finds no matches.
    `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture` prints `ok: Prusa G-code output fixture verification passed`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
    <automated>bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test</automated>
  </verify>
  <done>The fixture verifier enforces the structural status publication while retaining all Phase 50 structural sidecar guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checked-in status TSV to public status command | Human-readable parity status is derived from `packages/parity/status.tsv`. |
| Fixture verifier to status/docs trust | Fixture verification treats the status row as a publication contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03-01 | Repudiation | `packages/parity/status.tsv` | mitigate | Require exactly one `fork.prusaslicer.gcode-output` row with structural wording and the preserved public command. |
| T-52-03-02 | Information disclosure | status wording | mitigate | Keep broad generated-output/runtime/release/network/non-Prusa/upstream/sync deferrals explicit in the exact row. |
| T-52-03-03 | Tampering | fixture verifier status guard | mitigate | Update `GCODE_OUTPUT_STATUS_ROW` and mutation tests so stale summary-only or wrong-evidence rows fail closed. |
</threat_model>

<verification>
Run task verification, then run `awk` guards for exactly one `generated-outputs` in-progress row and exactly one `fork.prusaslicer.gcode-output` row. Finish with `git diff --check`.
</verification>

<success_criteria>
- `packages/parity/status.tsv` contains exactly one structural `fork.prusaslicer.gcode-output` row.
- That row names the Phase 49 closed structural scope contract, Phase 50 structural fixture summary, Phase 51 Rust parser/readiness boundary, and Phase 52 public parity command.
- `generated-outputs` remains exactly one `in progress` row.
- Fixture verifier and mutation tests pass with the structural status wording.
</success_criteria>

<output>
After completion, create `.planning/phases/52-executable-structural-g-code-evidence/52-03-SUMMARY.md`.
</output>
