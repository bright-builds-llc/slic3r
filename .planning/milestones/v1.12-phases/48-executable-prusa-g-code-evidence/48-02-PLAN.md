---
phase: 48-executable-prusa-g-code-evidence
plan: "02"
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - packages/parity/status.tsv
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
autonomous: true
requirements: [PGEV-02, PGEV-03]
requirements_addressed: [PGEV-02, PGEV-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 48-2026-06-14T18-49-25
generated_at: 2026-06-14T19:19:13Z
must_haves:
  truths:
    - "Maintainer can inspect exactly one `fork.prusaslicer.gcode-output` row in `packages/parity/status.tsv`."
    - "The broad `generated-outputs` row remains `in progress` after the fork-specific row becomes verified."
    - "The existing scope and fixture verifiers require the exact Phase 48 status row and parity target instead of rejecting them."
  artifacts:
    - path: "packages/parity/status.tsv"
      provides: "Exact verified `fork.prusaslicer.gcode-output` status row"
      contains: "fork.prusaslicer.gcode-output\tverified\t//packages/parity:prusaslicer_gcode_output_parity"
    - path: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      provides: "Publication-aware fixture verifier"
      contains: "verify_status_published"
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      provides: "Publication-aware scope verifier"
      contains: "verify_status_published"
  key_links:
    - from: "packages/parity/status.tsv"
      to: "//packages/parity:prusaslicer_gcode_output_parity"
      via: "status evidence field"
      pattern: "fork\\.prusaslicer\\.gcode-output\\tverified\\t//packages/parity:prusaslicer_gcode_output_parity"
    - from: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      to: "packages/parity/status.tsv"
      via: "exact status row publication check"
      pattern: "fork\\.prusaslicer\\.gcode-output"
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      to: "packages/parity/BUILD.bazel"
      via: "published target check"
      pattern: "prusaslicer_gcode_output_parity"
---

<objective>
Publish the exact Phase 48 status row and reconcile prior fail-closed guards so
they require the newly owned command and row.

Purpose: PGEV-03 requires status publication that names only the narrow Prusa
G-code evidence slice while keeping broad generated-output work in progress.
PGEV-02 requires existing verifiers to remain fail-closed after publication.

Output: One `fork.prusaslicer.gcode-output` status row and publication-aware
scope/fixture verifiers with mutation tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-executable-prusa-g-code-evidence/48-CONTEXT.md
@.planning/phases/48-executable-prusa-g-code-evidence/48-RESEARCH.md
@.planning/phases/48-executable-prusa-g-code-evidence/48-01-SUMMARY.md
@.planning/phases/47-rust-prusa-g-code-summary-boundary/47-VERIFICATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/parity/status.tsv
@packages/parity/BUILD.bazel
@packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
@packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
@packages/parity-fixtures/verify_prusa_project_file_fixture.sh

<interfaces>
Exact status row to add:

```text
fork.prusaslicer.gcode-output	verified	//packages/parity:prusaslicer_gcode_output_parity	Shared fixture comparison proves the narrow summary-only Prusa G-code evidence slice backed by the Phase 46 fixture and Phase 47 Rust summary boundary only; byte-for-byte G-code parity, full generated-output parity, toolpath geometry, extrusion, timing, support generation, wall seam behavior, arc fitting, STEP import, full 3MF import/export, printer-runtime behavior, firmware or printability, GUI export or viewer behavior, binary G-code, thumbnails, post-processing, host upload, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, and sync automation remain deferred
```

The broad row must remain:

```text
generated-outputs	in progress	docs/port/cli-slice.md	Scoped export plus repair/split artifact naming are fixture-verified; geometry and output-content parity are deferred
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
| --- | --- | --- | --- | --- |
| D-07 | 48-02 | 2 | Full | Converts obsolete Phase 48 absence guards into exact publication checks. |
| D-08 | 48-02 | 1 | Full | Adds exactly one checked-in `fork.prusaslicer.gcode-output` row. |
| D-09 | 48-02 | 1 | Full | Row status/evidence/notes use the locked values and narrow summary-only wording. |
| D-10 | 48-02 | 1 | Full | Keeps `generated-outputs` as `in progress`. |
| D-14 | 48-02 | 1, 2 | Partial | Runs parity, scope, and fixture checks here; Plan 48-03 runs the full phase suite. |
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Publish the exact G-code status row</name>
  <files>packages/parity/status.tsv</files>
  <read_first>
    - `packages/parity/status.tsv`
    - `packages/parity/README.md`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-CONTEXT.md`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-01-SUMMARY.md`
  </read_first>
  <action>
    First run `bazel run //packages/parity:prusaslicer_gcode_output_parity` and confirm it exits 0 per D-08.

    Then append exactly one new tab-delimited row to `packages/parity/status.tsv`:

```text
fork.prusaslicer.gcode-output	verified	//packages/parity:prusaslicer_gcode_output_parity	Shared fixture comparison proves the narrow summary-only Prusa G-code evidence slice backed by the Phase 46 fixture and Phase 47 Rust summary boundary only; byte-for-byte G-code parity, full generated-output parity, toolpath geometry, extrusion, timing, support generation, wall seam behavior, arc fitting, STEP import, full 3MF import/export, printer-runtime behavior, firmware or printability, GUI export or viewer behavior, binary G-code, thumbnails, post-processing, host upload, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, and sync automation remain deferred
```

    Do not edit the `generated-outputs` row; it must remain `in progress` per D-10. Do not add any other status row.
  </action>
  <verify>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>awk -F '\t' '$1=="fork.prusaslicer.gcode-output" && $2=="verified" && $3=="//packages/parity:prusaslicer_gcode_output_parity" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv</automated>
    <automated>awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F '\t' '$1=="fork.prusaslicer.gcode-output" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` passes.
    - `rg -n '^fork\.prusaslicer\.gcode-output\tverified\t//packages/parity:prusaslicer_gcode_output_parity\tShared fixture comparison proves the narrow summary-only Prusa G-code evidence slice backed by the Phase 46 fixture and Phase 47 Rust summary boundary only;' packages/parity/status.tsv` succeeds.
    - `awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv` passes.
    - The new row contains `byte-for-byte G-code parity`, `full generated-output parity`, `printer-runtime behavior`, `Bambu Studio`, `OrcaSlicer`, `upstream source imports`, and `sync automation remain deferred`.
  </acceptance_criteria>
  <done>The exact fork-specific G-code row is verified in `status.tsv`, and broad `generated-outputs` is still in progress per D-08 through D-10.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile fixture and scope verifiers with Phase 48 publication</name>
  <files>packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh, packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh, packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh, packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</files>
  <read_first>
    - `packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh`
    - `packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh`
    - `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh`
    - `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh`
    - `packages/parity-fixtures/verify_prusa_project_file_fixture.sh`
    - `packages/parity/status.tsv`
    - `packages/parity/BUILD.bazel`
  </read_first>
  <action>
    In both `verify_prusa_gcode_output_fixture.sh` and `verify_prusa_gcode_output_scope.sh`, replace the Phase 48 absence checks with exact publication checks per D-07.

    Add readonly `GCODE_OUTPUT_STATUS_ROW` equal to the exact row from Task 1. Add a `verify_status_published` function that:
    - requires that exact line in `packages/parity/status.tsv`
    - counts rows whose first field is `fork.prusaslicer.gcode-output`
    - errors unless the count is exactly `1`

    Replace `reject_parity_target` with `verify_parity_target_published`, requiring `packages/parity/BUILD.bazel` to contain a target named `prusaslicer_gcode_output_parity` using the same regex currently used by `reject_parity_target`.

    Preserve all existing fixture integrity, source, expected-summary, deferred-scope, forbidden-behavior, and overclaiming checks. Do not loosen broad deferral checks.

    In both test scripts:
    - update valid temp fixtures so they include the exact status row and a real `prusaslicer_gcode_output_parity` target snippet
    - replace `test_premature_status_row_fails` with tests for missing row, wrong evidence, and duplicate row
    - replace `test_premature_parity_target_fails` with a missing-target failure test
    - keep the Phase 47 Rust boundary allowed test
    - keep overclaim and forbidden-behavior tests unchanged except for setup updates
  </action>
  <verify>
    <automated>bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test</automated>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
    <automated>bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'verify_status_published|GCODE_OUTPUT_STATUS_ROW|verify_parity_target_published' packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` succeeds.
    - `rg -n 'reject_status_row|forbidden status row exists|forbidden parity target exists' packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` finds no matches.
    - `rg -n 'missing.*status|wrong.*evidence|duplicate.*status|missing.*parity' packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` succeeds.
    - `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture` prints `ok: Prusa G-code output fixture verification passed`.
    - `bazel run //packages/prusa-gcode-output-scope:verify` prints `ok: Prusa G-code output scope verification passed`.
  </acceptance_criteria>
  <done>Earlier G-code scope and fixture verifiers now fail closed on missing, duplicate, or wrong Phase 48 publication while preserving all non-overclaiming and fixture integrity checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Status TSV -> maintainer | `packages/parity/status.tsv` is a checked-in public progress signal. |
| BUILD/status files -> verifiers | Scope and fixture verifiers interpret local package metadata to decide whether publication is valid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-48-02-01 | Spoofing | `packages/parity/status.tsv` | mitigate | Require exactly one row with status `verified`, evidence `//packages/parity:prusaslicer_gcode_output_parity`, and notes limited to the summary-only slice. |
| T-48-02-02 | Tampering | G-code fixture and scope verifiers | mitigate | Replace absence guards with exact-row and exact-target publication checks plus missing/wrong/duplicate mutation tests. |
| T-48-02-03 | Information Disclosure | Status notes | mitigate | Keep broad deferred-scope list in the row so maintainers do not infer byte-for-byte, runtime, printer, geometry, GUI, release, network, or sync support. |
| T-48-02-04 | Repudiation | Publication audit trail | mitigate | Verifiers inspect checked-in `status.tsv` and `BUILD.bazel`, and tests prove invalid publication states fail with deterministic errors. |
</threat_model>

<verification>
Run:

```bash
bazel run //packages/parity:prusaslicer_gcode_output_parity
bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture
bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test
bazel run //packages/prusa-gcode-output-scope:verify
bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test
git diff --check
```
</verification>

<success_criteria>
PGEV-02 and PGEV-03 are satisfied for status/verifier publication when the
exact fork-specific status row exists once, `generated-outputs` remains `in
progress`, and both prior verifiers require the Phase 48 command and row.
</success_criteria>

<output>
After completion, create `.planning/phases/48-executable-prusa-g-code-evidence/48-02-SUMMARY.md` with `requirements-completed: [PGEV-02, PGEV-03]`.
</output>
