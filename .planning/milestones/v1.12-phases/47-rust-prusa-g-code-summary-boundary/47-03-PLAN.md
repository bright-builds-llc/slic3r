---
phase: 47-rust-prusa-g-code-summary-boundary
plan: "03"
type: execute
wave: 3
depends_on: ["47-01", "47-02"]
files_modified:
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
autonomous: true
requirements: [PGSUM-01, PGSUM-02, PGSUM-03]
requirements_addressed: [PGSUM-01, PGSUM-02, PGSUM-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 47-2026-06-14T15-07-52
generated_at: 2026-06-14T15:23:57Z
must_haves:
  truths:
    - "Phase 45 and Phase 46 verifiers allow the reviewed Phase 47 Rust summary boundary."
    - "Phase 45 and Phase 46 verifiers still reject `//packages/parity:prusaslicer_gcode_output_parity` and `fork.prusaslicer.gcode-output` status publication."
    - "The complete Rust/Bazel verification surface covers the new parser test, registry test, crate clippy, crate rustfmt, and package aggregate verify suite."
    - "Final absence checks prove no Phase 48 parity command, no status row, and no optional G-code summary binary were added in Phase 47."
  artifacts:
    - path: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      provides: "Fixture verifier reconciled to permit Phase 47 Rust summary boundary while blocking Phase 48 publication"
      contains: "reject_parity_target"
    - path: "packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh"
      provides: "Mutation tests proving Phase 47 Rust boundary is allowed and Phase 48 status/parity still fails"
      contains: "test_phase47_rust_summary_boundary_is_allowed"
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      provides: "Scope verifier reconciled to permit Phase 47 Rust summary boundary while blocking Phase 48 publication"
      contains: "reject_status_row"
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh"
      provides: "Scope mutation tests proving Phase 47 Rust boundary is allowed and Phase 48 status/parity still fails"
      contains: "test_phase47_rust_summary_boundary_is_allowed"
  key_links:
    - from: "packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      via: "temp checkout allowance test"
      pattern: "prusa_gcode_output"
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs"
      via: "temp checkout allowance test"
      pattern: "pub mod prusa_gcode_output"
    - from: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      to: "packages/parity/status.tsv"
      via: "`reject_status_row \"fork.prusaslicer.gcode-output\"`"
      pattern: "fork\\.prusaslicer\\.gcode-output"
---

<objective>
Reconcile the existing Phase 45 and Phase 46 verifiers with the intentional
Phase 47 Rust summary boundary, then run the final Rust/Bazel validation
surface.

Purpose: Phase 46 deliberately rejected Rust summary markers before Phase 47.
After Plans 47-01 and 47-02, those guards must permit exactly the reviewed
pure Rust boundary while keeping Phase 48 executable parity and status
publication unavailable.

Output: Updated verifier scripts/tests plus final validation evidence. Do not
commit or push; the parent wrapper owns final git mutation after clean phase
verification.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-rust-prusa-g-code-summary-boundary/47-CONTEXT.md
@.planning/phases/47-rust-prusa-g-code-summary-boundary/47-RESEARCH.md
@.planning/phases/47-rust-prusa-g-code-summary-boundary/47-01-SUMMARY.md
@.planning/phases/47-rust-prusa-g-code-summary-boundary/47-02-SUMMARY.md
@.planning/phases/46-prusa-g-code-fixture-surface/46-02-SUMMARY.md
@.planning/phases/46-prusa-g-code-fixture-surface/46-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
@packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
@packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
@packages/slic3r-rust/BUILD.bazel
@packages/parity/status.tsv
@packages/parity/BUILD.bazel

Bright Builds sources informing this plan: local `AGENTS.md`,
`AGENTS.bright-builds.md`, `standards-overrides.md`, and pinned canonical
architecture, code-shape, verification, testing, and Rust standards at
`05f8d7a6c9c2e157ec4f922a05273e72dab97676`.

<interfaces>
Phase 46 verifiers currently reject these Rust markers:

```bash
"slic3r_flavors::prusa_gcode_output"
"pub mod prusa_gcode_output"
"prusa_gcode_output_summary"
"parse_prusa_gcode_output_summary"
```

After Phase 47, the reviewed parser/module markers are required:

```rust
pub mod prusa_gcode_output;
pub fn parse_prusa_gcode_output_summary(input: &str) -> PrusaGcodeOutputParseResult;
pub fn prusa_gcode_output_summary_lines(input: &str) -> Result<Vec<String>, PrusaGcodeOutputParseError>;
```

These Phase 48 surfaces must remain rejected:

```text
//packages/parity:prusaslicer_gcode_output_parity
fork.prusaslicer.gcode-output
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
| --- | --- | --- | --- | --- |
| D-01 | 47-01 | 1, 2 | Full | Parser module exists before verifier reconciliation. |
| D-02 | 47-03 | 2 | Full | Final verification confirms no side-effect imports or behavior in the Rust module. |
| D-03 | 47-03 | 2 | Full | Final verification confirms public exports and no broad public names. |
| D-04 | 47-02 | 2 | Full | Registry metadata tests assert exact traceability. |
| D-05 | 47-02 | 1, 2 | Full | Registry row exists before final verification. |
| D-06 | 47-01 | 1, 2 | Full | Parser header validation covered before final checks. |
| D-07 | 47-01 | 1, 2 | Full | Parser accepted/rejected row cases covered before final checks. |
| D-08 | 47-01 | 2 | Full | Typed values covered before final checks. |
| D-09 | 47-01 | 1, 2 | Full | Summary output covered before final checks. |
| D-10 | 47-01 | 1 | Full | Parser tests are part of final verification. |
| D-11 | 47-02 | 2 | Full | Registry tests are part of final verification. |
| D-12 | 47-03 | 2 | Full | Final grep/test checks preserve non-overclaiming names and Phase 48 boundary. |
| D-13 | 47-03 | 1, 2 | Full | Bazel fixture/scope/Rust targets plus package aggregate verification run cleanly. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile fixture and scope verifiers with Phase 47 Rust boundary</name>
  <files>packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh, packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh, packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh, packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</files>
  <read_first>
    - `packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh`
    - `packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh`
    - `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh`
    - `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh`
    - `.planning/phases/46-prusa-g-code-fixture-surface/46-02-SUMMARY.md`
    - `.planning/phases/47-rust-prusa-g-code-summary-boundary/47-CONTEXT.md` decisions D-02, D-12, and D-13
  </read_first>
  <behavior>
    - Verifiers pass when a checkout contains `packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs` with `pub mod prusa_gcode_output;`.
    - Verifiers pass when a checkout contains `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` with `parse_prusa_gcode_output_summary` as pure Rust source.
    - Verifiers still fail when `packages/parity/status.tsv` contains a `fork.prusaslicer.gcode-output` row.
    - Verifiers still fail when `packages/parity/BUILD.bazel` declares a `prusaslicer_gcode_output_parity` target.
  </behavior>
  <action>
    In both `verify_prusa_gcode_output_fixture.sh` and
    `verify_prusa_gcode_output_scope.sh`, remove the Phase 47 Rust-marker
    rejection path:

    - Delete or stop calling `reject_rust_implementation_markers`.
    - Remove error text that treats `slic3r_flavors::prusa_gcode_output`, `pub mod prusa_gcode_output`, `prusa_gcode_output_summary`, or `parse_prusa_gcode_output_summary` as forbidden.
    - Keep `reject_status_row "fork.prusaslicer.gcode-output" ...`.
    - Keep `reject_parity_target`.
    - Keep fixture, provenance, expected-summary, README, status, parity-target, and forbidden verifier-behavior checks unchanged.

    In `packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh`:

    - Remove `test_premature_slic3r_flavors_marker_fails`.
    - Remove `test_premature_rust_marker_fails`.
    - Add `test_phase47_rust_summary_boundary_is_allowed`.
    - The new test must call `write_valid_fixture_copy "${dir}"`, create `packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs` containing `pub mod prusa_gcode_output;`, create `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` containing `pub fn parse_prusa_gcode_output_summary(input: &str) -> usize { input.len() }`, run the verifier, and assert stdout contains `^ok: Prusa G-code output fixture verification passed$`.
    - Keep `test_premature_status_row_fails` and `test_premature_parity_target_fails` unchanged.

    In `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh`:

    - Remove `test_premature_slic3r_flavors_marker_fails`.
    - Remove `test_premature_rust_marker_fails`.
    - Add `test_phase47_rust_summary_boundary_is_allowed`.
    - The new test must call `write_valid_fixture "${dir}"`, create the same temp `lib.rs` and `prusa_gcode_output.rs` files described above, run the verifier, and assert stdout contains `^ok: Prusa G-code output scope verification passed$`.
    - Keep `test_premature_status_row_fails` and `test_premature_parity_target_fails` unchanged.

    Do not weaken any checks for expected summary rows, fixture bytes, Phase 45
    scope values, Phase 46 fixture namespace values, status row absence, or
    parity target absence.
  </action>
  <acceptance_criteria>
    - `! rg -n 'reject_rust_implementation_markers|forbidden Prusa G-code summary marker|forbidden Phase 45 Rust G-code summary marker' packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` succeeds.
    - `rg -n 'test_phase47_rust_summary_boundary_is_allowed' packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` succeeds.
    - `! rg -n 'test_premature_slic3r_flavors_marker_fails|test_premature_rust_marker_fails' packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` succeeds.
    - `rg -n 'reject_status_row "fork\.prusaslicer\.gcode-output"|reject_parity_target' packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` succeeds.
    - `bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` passes.
    - `bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` passes.
    - `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture` passes in the real checkout after Plan 47-01 created the Rust module.
    - `bazel run //packages/prusa-gcode-output-scope:verify` passes in the real checkout after Plan 47-01 created the Rust module.
  </acceptance_criteria>
  <verify>
    <automated>bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
    <automated>bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test</automated>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
    <automated>bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
    <automated>shfmt -d packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>shellcheck packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
  </verify>
  <done>Fixture and scope verifiers allow the Phase 47 Rust summary module and still fail closed on Phase 48 parity target or status-row publication.</done>
</task>

<task type="auto">
  <name>Task 2: Run final Rust, Bazel, and publication-boundary verification</name>
  <files>No file modifications; verifies Phase 47 outputs across Rust, Bazel, fixture, scope, status, and parity-target surfaces.</files>
  <read_first>
    - `.planning/phases/47-rust-prusa-g-code-summary-boundary/47-01-SUMMARY.md`
    - `.planning/phases/47-rust-prusa-g-code-summary-boundary/47-02-SUMMARY.md`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel`
    - `packages/slic3r-rust/BUILD.bazel`
    - `packages/parity/status.tsv`
    - `packages/parity/BUILD.bazel`
  </read_first>
  <action>
    Run the final verification commands exactly as listed in `<verify>`.

    If any command fails, fix only the file(s) owned by Plans 47-01 through
    47-03 that caused the failure, then rerun the failed command and all later
    commands in the list.

    Do not add `packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs`.
    Do not add a `prusa_gcode_output_summary` Bazel binary.
    Do not add `//packages/parity:prusaslicer_gcode_output_parity`.
    Do not add any row beginning `fork.prusaslicer.gcode-output` to
    `packages/parity/status.tsv`.
    Do not edit schema or ORM files.
    Do not commit or push.
  </action>
  <acceptance_criteria>
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test` passes.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test` passes.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:clippy` passes.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check` passes.
    - `bazel test //packages/slic3r-rust:verify` passes and includes `//packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test`.
    - `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture` passes.
    - `bazel run //packages/prusa-gcode-output-scope:verify` passes.
    - `! bazel query //packages/parity:prusaslicer_gcode_output_parity` succeeds.
    - `! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv` succeeds.
    - `! test -e packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs` succeeds.
    - `! rg -n 'std::fs|std::env|std::process|std::net|Command|read_to_string|File::|Tcp|http|curl|git ' packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` succeeds.
    - `rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check` passes.
    - `rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings` passes.
    - `rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features` passes.
    - `rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features` passes.
  </acceptance_criteria>
  <verify>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:clippy</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check</automated>
    <automated>bazel test //packages/slic3r-rust:verify</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test</automated>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
    <automated>bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
    <automated>rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check</automated>
    <automated>rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings</automated>
    <automated>rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features</automated>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features</automated>
    <automated>! bazel query //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv</automated>
    <automated>! test -e packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>All parser, registry, fixture, scope, Rust formatting/lint/build/test, status absence, parity-target absence, and schema-exclusion checks pass with Phase 48 surfaces still unavailable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| older verifiers -> Phase 47 checkout | Phase 45/46 absence guards must distinguish reviewed Rust parser readiness from Phase 48 publication. |
| fixture/scope verifiers -> status/parity packages | The verifiers must still fail if status or executable parity surfaces appear early. |
| final verification -> release claim discipline | Passing Rust tests must not be confused with public verified G-code parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-47-03-01 | Tampering | `verify_prusa_gcode_output_fixture.sh` and `verify_prusa_gcode_output_scope.sh` | mitigate | Remove only the obsolete Rust-marker rejection; keep fixture, TSV, docs, status-row, parity-target, and forbidden behavior checks intact. |
| T-47-03-02 | Spoofing | temp verifier tests | mitigate | Add positive tests that create only a temp Phase 47 Rust module and prove verifiers allow that exact boundary. |
| T-47-03-03 | Tampering | Phase 48 parity target | mitigate | Preserve `reject_parity_target` and mutation tests for `prusaslicer_gcode_output_parity`. |
| T-47-03-04 | Repudiation | Phase 48 status row | mitigate | Preserve `reject_status_row "fork.prusaslicer.gcode-output"` and final absence grep against `packages/parity/status.tsv`. |
| T-47-03-05 | Elevation of Privilege | parser side effects | mitigate | Final grep rejects filesystem, env, process, network, command, file, HTTP, curl, and Git references in `src/prusa_gcode_output.rs`. |
</threat_model>

<verification>
Run:

```bash
bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test
bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test
bazel test //packages/slic3r-rust/crates/slic3r_flavors:clippy
bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check
bazel test //packages/slic3r-rust:verify
bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture
bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test
bazel run //packages/prusa-gcode-output-scope:verify
bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features
! bazel query //packages/parity:prusaslicer_gcode_output_parity
! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv
! test -e packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
git diff --check
```
</verification>

<success_criteria>
All PGSUM requirements are verified end to end: the Rust parser boundary,
metadata traceability, registry row, negative tests, verifier reconciliation,
and final absence checks pass while Phase 48 parity command and status
publication remain unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/47-rust-prusa-g-code-summary-boundary/47-03-SUMMARY.md`
</output>
