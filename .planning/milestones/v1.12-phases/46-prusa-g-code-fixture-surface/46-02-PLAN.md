---
phase: 46-prusa-g-code-fixture-surface
plan: "02"
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - packages/parity-fixtures/BUILD.bazel
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
autonomous: true
requirements: [PGFIX-02]
requirements_addressed: [PGFIX-02]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 46-2026-06-13T16-58-19
generated_at: 2026-06-13T17:19:25Z
must_haves:
  truths:
    - "Maintainer can run `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture` and see the fixture surface pass only when bytes, provenance, summary rows, docs text, and absence boundaries are correct."
    - "Maintainer can run mutation tests that prove byte, SHA-256, ASCII/LF, provenance, expected-summary, update-route, exclusion, and overclaiming drift fail closed."
    - "Maintainer can rerun the Phase 45 scope verifier after Phase 46 fixture files exist."
    - "Maintainer can confirm Phase 47 Rust implementation, Phase 48 parity target, and `fork.prusaslicer.gcode-output` status row remain absent."
  artifacts:
    - path: "packages/parity-fixtures/BUILD.bazel"
      provides: "Bazel exports, aliases, bundle, verifier binary, and verifier test for the G-code fixture"
      contains: "verify_prusa_gcode_output_fixture"
    - path: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      provides: "Fail-closed G-code fixture verifier"
      contains: "set -euo pipefail"
    - path: "packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh"
      provides: "Mutation tests for fixture verifier failure modes"
      contains: "test_wrong_gcode_checksum_fails"
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      provides: "Scope verifier reconciled with Phase 46 fixture artifacts"
      contains: "reject_later_phase_artifacts"
  key_links:
    - from: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode"
      via: "byte and SHA-256 checks"
      pattern: "dc1bb725fb2d81b986356bcdd0b160877dce48b086b3cf71867abc0ecf4467cb"
    - from: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      to: "packages/parity/status.tsv"
      via: "absence check for status row"
      pattern: "fork\\.prusaslicer\\.gcode-output"
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output"
      via: "allowed Phase 46 namespace"
      pattern: "prusaslicer\\.gcode-output"
---

<objective>
Add executable verification for the Phase 46 Prusa G-code fixture surface and
reconcile the Phase 45 scope verifier with the newly valid fixture artifacts.

Purpose: PGFIX-02 requires a repo-owned fixture verifier that fails closed on
fixture provenance, checksums, line-ending policy, expected-summary shape,
update rules, exclusions, and non-overclaiming scope text.

Output: Bazel-visible G-code fixture bundle, Bash verifier, mutation tests, and
Phase 45 scope verifier/tests updated to allow Phase 46 files while still
rejecting Phase 47/48 artifacts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-prusa-g-code-fixture-surface/46-CONTEXT.md
@.planning/phases/46-prusa-g-code-fixture-surface/46-RESEARCH.md
@.planning/phases/45-prusa-g-code-output-scope-gate/45-VERIFICATION.md
@.planning/phases/46-prusa-g-code-fixture-surface/46-01-SUMMARY.md
@.planning/milestones/v1.11-phases/42-prusa-project-file-fixture-surface/42-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/parity-fixtures/BUILD.bazel
@packages/parity-fixtures/verify_prusa_project_file_fixture.sh
@packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
@packages/parity/status.tsv
@packages/parity/BUILD.bazel

Bright Builds sources informing this plan: local `AGENTS.md`,
`AGENTS.bright-builds.md`, `standards-overrides.md`, and the pinned
architecture, code-shape, verification, and testing standards cited in
`46-RESEARCH.md` at commit `05f8d7a6c9c2e157ec4f922a05273e72dab97676`.

<interfaces>
Expected G-code summary header:

```text
source_ref	fixture_path	metadata_key	metadata_value	marker_key	marker_value	notes
```

Bazel target names to create:

```text
//packages/parity-fixtures:prusa_gcode_output_bundle
//packages/parity-fixtures:verify_prusa_gcode_output_fixture
//packages/parity-fixtures:verify_prusa_gcode_output_fixture_test
```

Later-phase artifacts that must remain absent in Phase 46:

```text
slic3r_flavors::prusa_gcode_output
//packages/parity:prusaslicer_gcode_output_parity
fork.prusaslicer.gcode-output
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
| --- | --- | --- | --- | --- |
| D-11 | 46-02 | 1 | Full | Adds fixture exports, aliases/filegroup, `verify_prusa_gcode_output_fixture`, and `verify_prusa_gcode_output_fixture_test`. |
| D-12 | 46-02 | 2 | Full | Implements Bash exact text checks plus targeted TSV header/row checks, matching existing fixture verifier style. |
| D-13 | 46-02 | 2 | Full | Verifier fails on fixture bytes, byte count, SHA-256, ASCII/LF, provenance, expected-summary shape, update route, traceability, exclusions, and boundary text drift. |
| D-14 | 46-02 | 2, 3 | Full | Guards absence of `slic3r_flavors::prusa_gcode_output`, `//packages/parity:prusaslicer_gcode_output_parity`, and `fork.prusaslicer.gcode-output`. |
| D-15 | 46-02 | 2 | Full | Verifier remains local and hermetic; no Git, network, live generation, upstream import, host upload, printer-runtime, plugin, or credential behavior. |
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Export G-code fixture bundle through Bazel</name>
  <files>packages/parity-fixtures/BUILD.bazel</files>
  <read_first>
    - `packages/parity-fixtures/BUILD.bazel`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/.gitattributes`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv`
    - `packages/parity-fixtures/verify_prusa_project_file_fixture.sh`
    - `.planning/phases/46-prusa-g-code-fixture-surface/46-CONTEXT.md` decision D-11
  </read_first>
  <action>
    Extend `packages/parity-fixtures/BUILD.bazel` following the existing
    `prusa_project_file_*` shape.

    Add these five files to the existing top-level `exports_files([...])` list:

```python
"forks/prusaslicer/prusaslicer.gcode-output/.gitattributes",
"forks/prusaslicer/prusaslicer.gcode-output/README.md",
"forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv",
"forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv",
"forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode",
```

    Add these aliases:

```python
alias(
    name = "prusa_gcode_output_readme",
    actual = "forks/prusaslicer/prusaslicer.gcode-output/README.md",
)

alias(
    name = "prusa_gcode_output_provenance",
    actual = "forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv",
)

alias(
    name = "prusa_gcode_output_expected_gcode_summary",
    actual = "forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv",
)

alias(
    name = "prusa_gcode_output_gcodewriter_set_speed",
    actual = "forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode",
)
```

    Add this filegroup:

```python
filegroup(
    name = "prusa_gcode_output_bundle",
    srcs = [
        "forks/prusaslicer/prusaslicer.gcode-output/.gitattributes",
        "forks/prusaslicer/prusaslicer.gcode-output/README.md",
        "forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv",
        "forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv",
        "forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode",
    ],
)
```

    Add a `sh_binary` named `verify_prusa_gcode_output_fixture` with:

```python
srcs = ["verify_prusa_gcode_output_fixture.sh"]
data = [
    ":prusa_gcode_output_bundle",
    "README.md",
    "//packages/parity:BUILD.bazel",
    "//packages/parity:status.tsv",
]
```

    Add a `sh_test` named `verify_prusa_gcode_output_fixture_test` with:

```python
srcs = ["verify_prusa_gcode_output_fixture_test.sh"]
data = [
    "verify_prusa_gcode_output_fixture.sh",
    ":prusa_gcode_output_bundle",
    "README.md",
    "//packages/parity:BUILD.bazel",
    "//packages/parity:status.tsv",
]
```

    Add the G-code fixture files and new verifier scripts to the
    `package_boundary` filegroup. Do not add any target named
    `prusaslicer_gcode_output_parity`.
  </action>
  <acceptance_criteria>
    - `bazel query //packages/parity-fixtures:prusa_gcode_output_bundle` succeeds.
    - `bazel query //packages/parity-fixtures:prusa_gcode_output_expected_gcode_summary` succeeds.
    - `bazel query //packages/parity-fixtures:prusa_gcode_output_gcodewriter_set_speed` succeeds.
    - `bazel query //packages/parity-fixtures:verify_prusa_gcode_output_fixture` succeeds.
    - `bazel query //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test` succeeds.
    - `bazel query //packages/parity:all` succeeds before the negative parity-target absence query is trusted.
    - `! bazel query //packages/parity:prusaslicer_gcode_output_parity` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>bazel query //packages/parity-fixtures:prusa_gcode_output_bundle</automated>
    <automated>bazel query //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
    <automated>bazel query //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test</automated>
    <automated>bazel query //packages/parity:all</automated>
    <automated>! bazel query //packages/parity:prusaslicer_gcode_output_parity</automated>
  </verify>
  <done>The G-code fixture namespace is Bazel-visible as aliases, a bundle, verifier binary, verifier test, and package-boundary entries, with no Phase 48 parity target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fail-closed G-code fixture verifier and mutation tests</name>
  <files>packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh, packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</files>
  <read_first>
    - `packages/parity-fixtures/verify_prusa_project_file_fixture.sh`
    - `packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv`
    - `packages/parity/status.tsv`
    - `packages/parity/BUILD.bazel`
    - `.planning/phases/46-prusa-g-code-fixture-surface/46-CONTEXT.md` decisions D-12 through D-15
    - `.planning/phases/46-prusa-g-code-fixture-surface/46-RESEARCH.md` sections "Pattern 3", "Code Examples", and "Security Domain"
  </read_first>
  <behavior>
    - Test 1: A complete temp fixture copy passes and prints `ok: Prusa G-code output fixture verification passed`.
    - Test 2: Mutating the `.gcode` bytes, SHA-256 expectation, CRLF, non-ASCII byte, provenance header, provenance row, expected-summary header, expected-summary rows, README update route, or README exclusions fails with `error:`.
    - Test 3: Adding `fork.prusaslicer.gcode-output`, `name = "prusaslicer_gcode_output_parity"`, `slic3r_flavors::prusa_gcode_output`, or Rust `prusa_gcode_output` markers fails while the real checkout remains unchanged.
  </behavior>
  <action>
    Create `verify_prusa_gcode_output_fixture.sh` with
    `#!/usr/bin/env bash`, `set -euo pipefail`, and helper functions matching
    the project-file verifier style: `error`, `require_file`, `require_text`,
    `reject_text`, `require_exact_header`, `require_line_count`,
    `require_size`, `require_sha256`, `require_exact_line`,
    `require_ascii_lf`, `reject_status_row`, `reject_parity_target`, and
    `reject_rust_implementation_markers`.

    The verifier must default to real checkout paths when called with no args
    and accept explicit args for mutation tests. Validate these exact contract
    values:

```text
SOURCE_REF=prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
PEELED_COMMIT=9a583bd438b195856f3bcf7ea99b69ba4003a961
FIXTURE_PATH=packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode
EXPECTED_SHA=dc1bb725fb2d81b986356bcdd0b160877dce48b086b3cf71867abc0ecf4467cb
EXPECTED_SIZE=42
EXPECTED_LINE_COUNT=4
EXPECTED_SUMMARY_HEADER=source_ref	fixture_path	metadata_key	metadata_value	marker_key	marker_value	notes
PROVENANCE_HEADER=fixture_id	vendor_id	inventory_id	source_ref	accepted_tag	peeled_commit	source_path	upstream_url	bytes	sha256	line_endings_encoding	role	phase45_scope_record	update_route	status_scope	privacy_post_processing_exclusions	broad_deferrals
```

    The verifier must require the exact four fixture lines:

```text
G1 F99999.123
G1 F1
G1 F203.2
G1 F203.201
```

    The verifier must require the exact provenance row and five exact expected
    summary rows from Plan 46-01. It must reject extra provenance rows and
    extra expected summary rows by enforcing line counts of `2` and `6`.

    The verifier must require fixture README text naming:

    - `Phase 46 supplies fixture bytes and summary-only expected artifacts only.`
    - `The accepted upstream tree has no checked-in .gcode blob`
    - `Update route: update this fixture only after a reviewed intake change updates packages/fork-vendors/forks.tsv, packages/fork-inventories/prusaslicer.tsv, and packages/prusa-gcode-output-scope/gcode-output-scope.md.`
    - `Rust summary parsing remains Phase 47-owned`
    - `executable parity and fork.prusaslicer.gcode-output status publication remain Phase 48-owned`
    - `No base export fixture reuse, live generation, upstream fetching/importing, binary G-code, thumbnails, post-processing, host upload, printer-runtime behavior, network/device behavior, credential handling, Bambu Studio support, OrcaSlicer support, upstream source imports, or sync automation is introduced by this namespace.`
    - `Byte-for-byte G-code parity, full generated-output parity, toolpath geometry, extrusion, timing, support generation, wall seam behavior, arc fitting, STEP import, full 3MF import/export, firmware or printability behavior, GUI export or viewer behavior, fork release builds, and broad PrusaSlicer runtime support remain deferred.`

    The verifier must require `packages/parity-fixtures/README.md` to mention
    `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture`
    only after Plan 46-03 adds docs; before that docs check can be scoped to the
    namespace README and Phase 46 files. Do not make Plan 46-02 depend on
    Plan 46-03 docs.

    The verifier must reject:

    - row ID `fork.prusaslicer.gcode-output` in `packages/parity/status.tsv`
    - `name = "prusaslicer_gcode_output_parity"` in `packages/parity/BUILD.bazel`
    - Rust markers `slic3r_flavors::prusa_gcode_output`, `pub mod prusa_gcode_output`, `prusa_gcode_output_summary`, and `parse_prusa_gcode_output_summary` under `packages/slic3r-rust`
    - affirmative overclaims in README or expected summary: `verified Prusa G-code output parity`, `byte-for-byte G-code parity verified`, `full generated-output parity verified`, `toolpath correctness verified`, `printability verified`, `host upload verified`, `Bambu Studio support verified`, and `OrcaSlicer support verified`
    - verifier script content containing `curl `, `git `, `git\t`, `clone`, `fetch`, `PrusaSlicer --`, `slic3r --`, `send-gcode`, or `host upload`

    Implement the verifier self-scan so it cannot match its own forbidden-term
    list. Use split literals or another self-match-safe implementation, for
    example building terms from adjacent fragments such as `"g""it "` and
    `"host ""upload"` before scanning the verifier file.

    Create `verify_prusa_gcode_output_fixture_test.sh` using temp copies and
    Arrange/Act/Assert comments. Include at least these function names:
    `test_complete_fixture_passes`, `test_wrong_gcode_checksum_fails`,
    `test_crlf_gcode_fails`, `test_non_ascii_gcode_fails`,
    `test_missing_provenance_header_fails`,
    `test_missing_expected_header_fails`,
    `test_missing_expected_marker_row_fails`,
    `test_extra_expected_summary_row_fails`,
    `test_missing_update_route_fails`,
    `test_missing_privacy_exclusions_fails`,
    `test_readme_overclaim_fails`, `test_premature_status_row_fails`,
    `test_premature_parity_target_fails`,
    `test_premature_slic3r_flavors_marker_fails`, and
    `test_premature_rust_marker_fails`.
  </action>
  <acceptance_criteria>
    - `bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh` succeeds.
    - `bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` succeeds.
    - `bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` succeeds and prints `ok: verify_prusa_gcode_output_fixture_test`.
    - `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture` succeeds and prints `ok: Prusa G-code output fixture verification passed`.
    - `bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test` succeeds.
    - `shfmt -d packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` produces no diff.
    - `shellcheck packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` succeeds.
    - `! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv` succeeds.
    - `bazel query //packages/parity:all` succeeds before the negative parity-target absence query is trusted.
    - `! bazel query //packages/parity:prusaslicer_gcode_output_parity` succeeds.
    - `! rg -n 'slic3r_flavors::prusa_gcode_output|pub mod prusa_gcode_output|prusa_gcode_output_summary|parse_prusa_gcode_output_summary' packages/slic3r-rust` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test</automated>
    <automated>shfmt -d packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
    <automated>shellcheck packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
    <automated>! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv</automated>
    <automated>bazel query //packages/parity:all</automated>
    <automated>! bazel query //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>! rg -n 'slic3r_flavors::prusa_gcode_output|pub mod prusa_gcode_output|prusa_gcode_output_summary|parse_prusa_gcode_output_summary' packages/slic3r-rust</automated>
  </verify>
  <done>The fixture verifier and mutation tests fail closed on required Phase 46 integrity, provenance, expected-summary, exclusion, and no-overclaiming boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile Phase 45 scope verifier with Phase 46 artifacts</name>
  <files>packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh, packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</files>
  <read_first>
    - `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh`
    - `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh`
    - `packages/prusa-gcode-output-scope/gcode-output-scope.md`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv`
    - `.planning/phases/45-prusa-g-code-output-scope-gate/45-VERIFICATION.md`
    - `.planning/phases/46-prusa-g-code-fixture-surface/46-CONTEXT.md` decision D-14
    - `.planning/phases/46-prusa-g-code-fixture-surface/46-RESEARCH.md` section "Pattern 4"
  </read_first>
  <behavior>
    - Test 1: `bazel run //packages/prusa-gcode-output-scope:verify` passes after the Phase 46 fixture namespace and `expected-gcode-summary.tsv` exist.
    - Test 2: The scope verifier still rejects `fork.prusaslicer.gcode-output`, `name = "prusaslicer_gcode_output_parity"`, `slic3r_flavors::prusa_gcode_output`, and Rust `prusa_gcode_output` implementation markers.
    - Test 3: Scope verifier tests prove Phase 46 fixture namespace and expected-summary artifact are allowed, while Phase 47/48 artifacts are rejected.
  </behavior>
  <action>
    In `verify_prusa_gcode_output_scope.sh`, update only the Phase 46 absence
    boundary:

    - Remove the `reject_existing_path` call that rejects `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output`.
    - Remove the `reject_existing_path` call that rejects `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv`.
    - Keep `reject_status_row "fork.prusaslicer.gcode-output"`.
    - Keep rejection of `name = "prusaslicer_gcode_output_parity"` in `packages/parity/BUILD.bazel`.
    - Keep `reject_rust_implementation_markers` for `slic3r_flavors::prusa_gcode_output`, `pub mod prusa_gcode_output`, `prusa_gcode_output_summary`, and `parse_prusa_gcode_output_summary`.
    - Do not alter the Phase 45 expected-summary contract columns; they must remain `source_ref`, `fixture_path`, `metadata_key`, `metadata_value`, `marker_key`, `marker_value`, and `notes`.

    In `verify_prusa_gcode_output_scope_test.sh`:

    - Remove `test_premature_fixture_namespace_fails` and `test_premature_expected_summary_artifact_fails` as failing tests.
    - Add `test_phase46_fixture_namespace_is_allowed` that creates `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/` in the temp checkout and expects the scope verifier to pass.
    - Add `test_phase46_expected_summary_artifact_is_allowed` that creates the same namespace plus `expected-gcode-summary.tsv` in the temp checkout and expects the scope verifier to pass.
    - Add or retain rejection tests for `test_premature_status_row_fails`, `test_premature_parity_target_fails`, `test_premature_slic3r_flavors_marker_fails`, and `test_premature_rust_marker_fails`.

    Preserve `set -euo pipefail`, temp checkout roots, and Arrange/Act/Assert
    comments.
  </action>
  <acceptance_criteria>
    - `bazel run //packages/prusa-gcode-output-scope:verify` succeeds after Phase 46 fixture files exist.
    - `bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` succeeds and includes passing tests named `test_phase46_fixture_namespace_is_allowed` and `test_phase46_expected_summary_artifact_is_allowed`.
    - `bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test` succeeds.
    - `! rg -n 'reject_existing_path "expected summary artifact"|reject_existing_path "fixture namespace"' packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` succeeds.
    - `rg -n 'reject_status_row "fork.prusaslicer.gcode-output"|reject_parity_target|reject_rust_implementation_markers' packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` succeeds.
    - `rg -n 'test_phase46_fixture_namespace_is_allowed|test_phase46_expected_summary_artifact_is_allowed|test_premature_slic3r_flavors_marker_fails' packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
    <automated>bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
    <automated>shfmt -d packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>shellcheck packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
  </verify>
  <done>The Phase 45 scope verifier recognizes the Phase 46 fixture surface while still failing on Phase 47/48 Rust, parity, and status artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| fixture files -> verifier | Checked-in fixture-looking files must be validated locally rather than trusted by path. |
| TSV text -> Bash checks | Header and row drift can weaken Phase 47 parser contracts if not checked exactly. |
| Phase 46 verifier -> later-phase absence | The verifier must allow fixture artifacts while rejecting Rust implementation, parity command, and status publication. |
| shell verifier -> local environment | Verifier code must not fetch, generate, import upstream source, upload, or execute printer/runtime behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-46-02-01 | Tampering | `gcodewriter-set-speed.gcode` | mitigate | Verify exact byte count, SHA-256, line count, exact lines, and ASCII LF content. |
| T-46-02-02 | Tampering | `fixture-provenance.tsv` | mitigate | Verify exact header, exact row, line count, update route, scope record traceability, exclusions, and broad deferrals. |
| T-46-02-03 | Tampering | `expected-gcode-summary.tsv` | mitigate | Verify exact Phase 45 header, exact five rows, line count, and absence of rejected columns. |
| T-46-02-04 | Repudiation | docs/status boundary | mitigate | Reject status row `fork.prusaslicer.gcode-output` and parity target `prusaslicer_gcode_output_parity` in Phase 46. |
| T-46-02-05 | Spoofing | Rust implementation boundary | mitigate | Reject `slic3r_flavors::prusa_gcode_output`, `pub mod prusa_gcode_output`, `prusa_gcode_output_summary`, and `parse_prusa_gcode_output_summary` markers until Phase 47. |
| T-46-02-06 | Information Disclosure | verifier shell behavior | mitigate | Reject verifier script content that invokes Git, network fetch, upstream import, slicer generation, host upload, or printer-runtime behavior. |
| T-46-02-07 | Denial of Service | brittle Phase 45 verifier | mitigate | Remove only obsolete Phase 46 absence checks so `bazel run //packages/prusa-gcode-output-scope:verify` keeps passing after fixture creation. |
</threat_model>

<verification>
Run:

```bash
bazel query //packages/parity-fixtures:prusa_gcode_output_bundle
bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture
bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test
bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
bazel run //packages/prusa-gcode-output-scope:verify
bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test
shfmt -d packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
shellcheck packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
bazel query //packages/parity:all
! bazel query //packages/parity:prusaslicer_gcode_output_parity
! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv
! rg -n 'slic3r_flavors::prusa_gcode_output|pub mod prusa_gcode_output|prusa_gcode_output_summary|parse_prusa_gcode_output_summary' packages/slic3r-rust
git diff --check
```
</verification>

<success_criteria>
PGFIX-02 is satisfied for executable fixture-surface verification: maintainers
can run the repo-owned G-code fixture verifier and mutation tests, and the
Phase 45 scope verifier remains coherent after Phase 46 fixture artifacts land
without allowing Phase 47/48 artifacts early.
</success_criteria>

<output>
After completion, create `.planning/phases/46-prusa-g-code-fixture-surface/46-02-SUMMARY.md`
</output>
