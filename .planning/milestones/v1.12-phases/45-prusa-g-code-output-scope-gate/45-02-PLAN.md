---
phase: 45-prusa-g-code-output-scope-gate
plan: "02"
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - packages/prusa-gcode-output-scope/BUILD.bazel
  - packages/prusa-gcode-output-scope/README.md
  - packages/prusa-gcode-output-scope/gcode-output-scope.md
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
autonomous: true
requirements: [PGSEL-01, PGSEL-02]
requirements_addressed: [PGSEL-01, PGSEL-02]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 45-2026-06-06T13-53-22
generated_at: 2026-06-06T14:07:50Z
must_haves:
  truths:
    - "Maintainer can inspect a checked-in `prusaslicer.gcode-output` scope record with every PGSEL-01 field populated."
    - "Maintainer can run `bazel run //packages/prusa-gcode-output-scope:verify` and see a pass only when the scope record, README, inventory row, category-map row, and absence boundary are correct."
    - "Maintainer can run a focused shell test that proves the verifier fails on missing fields, source drift, overclaiming, premature fixture/status/parity artifacts, and missing deferrals."
    - "Maintainer can distinguish the summary-only scope contract from byte-for-byte G-code parity, full generated-output parity, runtime/printer behavior, geometry, support, seam, arc, STEP, release, network/device, Bambu Studio, OrcaSlicer, and sync claims."
  artifacts:
    - path: "packages/prusa-gcode-output-scope/BUILD.bazel"
      provides: "Bazel package boundary, `verify` target, verifier sh_test, and package_boundary filegroup"
      contains: "verify_prusa_gcode_output_scope_test"
    - path: "packages/prusa-gcode-output-scope/README.md"
      provides: "Package entrypoint and non-overclaiming Phase 45 boundary"
      contains: "Phase 45 verification does not prove executable Prusa G-code output parity"
    - path: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      provides: "Reviewed `prusaslicer.gcode-output` scope record"
      contains: "prusaslicer.gcode-output"
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      provides: "Fail-closed scope verifier with positive and absence-boundary checks"
      contains: "set -euo pipefail"
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh"
      provides: "Mutation-style verifier behavior tests"
      contains: "test_premature_status_row_fails"
  key_links:
    - from: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      to: "packages/fork-inventories/prusaslicer.tsv"
      via: "Inventory row ID and source row details"
      pattern: "prusaslicer\\.gcode-output"
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      to: "packages/parity/status.tsv"
      via: "absence check for `fork.prusaslicer.gcode-output`"
      pattern: "fork\\.prusaslicer\\.gcode-output"
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output"
      via: "forbidden Phase 45 fixture namespace check"
      pattern: "prusaslicer\\.gcode-output"
---

<objective>
Create the Phase 45 Prusa G-code output scope gate package.

Purpose: Give maintainers a reviewed, verifier-owned, metadata-only
`prusaslicer.gcode-output` contract before Phase 46 fixture bytes, Phase 47
Rust summary logic, Phase 48 parity target/status publication, or any broad
G-code parity claim exists.

Output: A dedicated `packages/prusa-gcode-output-scope` package with README,
scope record, fail-closed verifier, focused verifier test, and Bazel targets.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-prusa-g-code-output-scope-gate/45-CONTEXT.md
@.planning/phases/45-prusa-g-code-output-scope-gate/45-RESEARCH.md
@.planning/milestones/v1.11-phases/41-prusa-project-file-scope-gate/41-01-SUMMARY.md
@.planning/phases/45-prusa-g-code-output-scope-gate/45-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/prusa-project-file-scope/BUILD.bazel
@packages/prusa-project-file-scope/README.md
@packages/prusa-project-file-scope/project-file-scope.md
@packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh
@packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh
@packages/fork-inventories/prusaslicer.tsv
@packages/fork-inventories/category-map.tsv
@packages/parity/status.tsv
@packages/parity/BUILD.bazel

Bright Builds sources loaded for this plan: local `AGENTS.md`,
`AGENTS.bright-builds.md`, `standards-overrides.md`, and pinned standards
`index.md`, `core/architecture.md`, `core/code-shape.md`,
`core/verification.md`, and `core/testing.md` at commit
`05f8d7a6c9c2e157ec4f922a05273e72dab97676`.

<interfaces>
Phase 41 package shape to mirror:

```python
package(default_visibility = ["//visibility:public"])

exports_files([
    "README.md",
    "project-file-scope.md",
])

sh_binary(
    name = "verify",
    srcs = ["verify_prusa_project_file_scope.sh"],
    data = [
        "README.md",
        "project-file-scope.md",
    ],
    args = [
        "$(location README.md)",
        "$(location project-file-scope.md)",
    ],
)

sh_test(
    name = "verify_prusa_project_file_scope_test",
    srcs = ["verify_prusa_project_file_scope_test.sh"],
    data = ["verify_prusa_project_file_scope.sh"],
)
```

Phase 45 exact source and handoff values:

```text
Inventory row ID: `prusaslicer.gcode-output`
Accepted source identity: `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`
Inventory row source: `packages/fork-inventories/prusaslicer.tsv`
Source path: `src/libslic3r/GCode.cpp`
Companion API evidence: `src/libslic3r/GCode.hpp`
Phase 46 fixture namespace text: `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/`
Phase 46 expected summary path text: `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv`
Phase 47 Rust boundary text: `slic3r_flavors::prusa_gcode_output`
Phase 48 command text: `bazel run //packages/parity:prusaslicer_gcode_output_parity`
Phase 48 status token text: `fork.prusaslicer.gcode-output`
Reviewer signoff: `Peter Ryszkiewicz, 2026-06-06 UTC`
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
| --- | --- | --- | --- | --- |
| D-01 | 45-02 | 1, 2 | Full | Creates a Phase 41-shaped package-local README, scope record, verifier, focused test, and Bazel `verify` target. |
| D-02 | 45-02 | 1, 2 | Full | Scope record and verifier use `prusaslicer.gcode-output` and the reconciled inventory/category-map rows from Plan 45-01. |
| D-03 | 45-02 | 1, 2 | Full | Names the Phase 46 source-pinned ASCII `.gcode` fixture decision and verifies no fixture bytes exist in Phase 45. |
| D-04 | 45-02 | 1, 2 | Full | Names the Phase 46 `expected-gcode-summary.tsv` path and verifies it is not created in Phase 45. |
| D-05 | 45-02 | 1, 2 | Full | Reserves `slic3r_flavors::prusa_gcode_output` as a Phase 47-owned pure boundary and verifies no Rust implementation exists. |
| D-06 | 45-02 | 1, 2 | Full | Reserves `bazel run //packages/parity:prusaslicer_gcode_output_parity` as Phase 48-owned text and verifies the target is absent. |
| D-07 | 45-02 | 1, 2 | Full | Reserves `fork.prusaslicer.gcode-output` as Phase 48-owned text and verifies no status row exists. |
| D-08 | 45-02 | 2 | Full | Verifier fails closed on required field/source/deferral/handoff/signoff drift. |
| D-09 | 45-02 | 2 | Full | Verifier and tests guard fixture, expected summary, Rust, parity, status, source import, network, runtime, host upload, profile-update, release, Bambu, and Orca absence boundaries. |
| D-10 | 45-02 | 2 | Full | Focused shell verifier test includes valid pass and targeted negative mutations. |
| D-12 | 45-02 | 1, 2 | Full | Scope package preserves the evidence ladder: scope gate first, then Phase 46/47/48 work. |
| D-13 | 45-02 | 1, 2 | Full | Broad `generated-outputs` remains in progress, and `fork.prusaslicer.gcode-output` is not published. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the metadata-only scope package records</name>
  <files>packages/prusa-gcode-output-scope/BUILD.bazel, packages/prusa-gcode-output-scope/README.md, packages/prusa-gcode-output-scope/gcode-output-scope.md</files>
  <read_first>
    - `.planning/phases/45-prusa-g-code-output-scope-gate/45-CONTEXT.md` decisions D-01 through D-13
    - `.planning/phases/45-prusa-g-code-output-scope-gate/45-RESEARCH.md` sections "Summary", "Architecture Patterns", "Don't Hand-Roll", "Common Pitfalls", and "Security Domain"
    - `.planning/phases/45-prusa-g-code-output-scope-gate/45-01-SUMMARY.md`
    - `packages/prusa-project-file-scope/BUILD.bazel`
    - `packages/prusa-project-file-scope/README.md`
    - `packages/prusa-project-file-scope/project-file-scope.md`
    - `packages/fork-inventories/prusaslicer.tsv`
    - `packages/fork-inventories/category-map.tsv`
    - `packages/parity/status.tsv`
  </read_first>
  <behavior>
    - Test 1: `gcode-output-scope.md` has exact rows for every PGSEL-01 field.
    - Test 2: `README.md` says Phase 45 verification does not prove executable Prusa G-code output parity or the PGSEL-02 excluded surfaces.
    - Test 3: `BUILD.bazel` exposes `bazel run //packages/prusa-gcode-output-scope:verify` and a package-local shell test target.
  </behavior>
  <action>
    Create `packages/prusa-gcode-output-scope/BUILD.bazel` per D-01 with:

```python
package(default_visibility = ["//visibility:public"])

exports_files([
    "README.md",
    "gcode-output-scope.md",
])

sh_binary(
    name = "verify",
    srcs = ["verify_prusa_gcode_output_scope.sh"],
    data = [
        "README.md",
        "gcode-output-scope.md",
        "//packages/fork-inventories:prusaslicer.tsv",
        "//packages/fork-inventories:category-map.tsv",
        "//packages/parity:status.tsv",
    ],
    args = [
        "$(location README.md)",
        "$(location gcode-output-scope.md)",
        "$(location //packages/fork-inventories:prusaslicer.tsv)",
        "$(location //packages/fork-inventories:category-map.tsv)",
        "$(location //packages/parity:status.tsv)",
    ],
)

sh_test(
    name = "verify_prusa_gcode_output_scope_test",
    srcs = ["verify_prusa_gcode_output_scope_test.sh"],
    data = ["verify_prusa_gcode_output_scope.sh"],
)

filegroup(
    name = "package_boundary",
    srcs = [
        "README.md",
        "gcode-output-scope.md",
        "verify_prusa_gcode_output_scope.sh",
        "verify_prusa_gcode_output_scope_test.sh",
    ],
)
```

Create `README.md` with these exact required sentences per D-03, D-04, D-06,
D-07, D-09, and D-13:

```text
`packages/prusa-gcode-output-scope` owns the Phase 45 reviewed scope gate for `prusaslicer.gcode-output`.
Run `bazel run //packages/prusa-gcode-output-scope:verify` to check the reviewed Phase 45 scope gate.
Phase 45 verification does not prove executable Prusa G-code output parity.
Phase 45 verification does not prove byte-for-byte G-code parity, full generated-output parity, toolpath geometry, extrusion, timing, support generation, wall seam behavior, arc fitting, STEP import, full 3MF import/export, printer-runtime behavior, firmware or printability behavior, GUI export or viewer behavior, binary G-code, thumbnails, post-processing, host upload, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, or sync automation.
This package creates no fixture bytes, expected-gcode-summary.tsv, Rust G-code summary implementation, parity command, status row, upstream source import, vendored fork source tree, Git/network/vendor sync behavior, printer-runtime behavior, host upload, profile auto-update execution, network/device integration, credential handling, Bambu Studio support, OrcaSlicer support, or fork release build.
```

Create `gcode-output-scope.md` with a `## Scope Record` table containing these
exact rows:

```text
| Inventory row ID | `prusaslicer.gcode-output` |
| Accepted source identity | `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961` |
| Inventory row source | `packages/fork-inventories/prusaslicer.tsv` |
| Source path | `src/libslic3r/GCode.cpp` |
| Companion API evidence | `src/libslic3r/GCode.hpp` |
| Fixture source decision | Phase 46 source-pinned ASCII `.gcode` fixture under `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/`; no fixture bytes are checked in during Phase 45. |
| Expected-summary contract | Phase 46 `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv` with `source_ref`, `fixture_path`, `metadata_key`, `metadata_value`, `marker_key`, `marker_value`, and `notes` columns; no expected summary artifact is checked in during Phase 45. |
| Candidate Rust boundary | Phase 47 `slic3r_flavors::prusa_gcode_output` pure data-in/data-out G-code summary boundary; no Rust summary implementation is created in Phase 45. |
| Planned evidence command | Phase 48 command text `bazel run //packages/parity:prusaslicer_gcode_output_parity`; the target is not created in Phase 45. |
| Planned status token | Phase 48 token `fork.prusaslicer.gcode-output` after executable evidence passes; no `packages/parity/status.tsv` row is published in Phase 45. |
| Docs touched | `docs/port/README.md`; `docs/port/package-map.md`; `docs/port/migration-guidance.md`; `docs/port/parity-matrix.md` |
| License or security note | `AGPL-3.0-only`; metadata-only-not-legal-review; no upstream source import; no Git, network, vendor sync, profile auto-update execution, host upload, credential, cloud, network/device, release, or printer-runtime behavior in Phase 45. |
| Deferred scope | Byte-for-byte G-code parity; full generated-output parity; toolpath geometry; extrusion; timing; support generation; wall seam behavior; arc fitting; STEP import; full 3MF import/export; printer-runtime behavior; firmware or printability behavior; GUI export or viewer behavior; binary G-code; thumbnails; post-processing; host upload; network/device integration; profile auto-update execution; fork release builds; Bambu Studio; OrcaSlicer; upstream source imports; sync automation. |
| Reviewer signoff | Peter Ryszkiewicz, 2026-06-06 UTC |
```

Also add a `## Source Row Details` table matching the Plan 45-01 inventory row:

```text
| Source path | `src/libslic3r/GCode.cpp` |
| Companion API evidence | `src/libslic3r/GCode.hpp` |
| Feature surface | `gcode-output` |
| Feature category | `gcode-output` |
| Ownership | `shared-downstream` |
| Complexity | `high` |
| Parity dependency | `generated-outputs` |
| Inventory decision | `future-candidate` |
| Caution flags | `none` |
| Inventory note | Source-observed G-code output planning row; parity requires reviewed source-pinned summary evidence before output behavior is claimed. |
```

Do not create `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/`,
`expected-gcode-summary.tsv`, any Rust `prusa_gcode_output` module, any
`prusaslicer_gcode_output_parity` target, or any `fork.prusaslicer.gcode-output`
status row.
  </action>
  <acceptance_criteria>
    - `rg -n 'packages/prusa-gcode-output-scope.*Phase 45 reviewed scope gate' packages/prusa-gcode-output-scope/README.md` succeeds.
    - `rg -n 'bazel run //packages/prusa-gcode-output-scope:verify' packages/prusa-gcode-output-scope/README.md` succeeds.
    - `rg -n 'Phase 45 verification does not prove executable Prusa G-code output parity' packages/prusa-gcode-output-scope/README.md` succeeds.
    - `rg -n '\| Inventory row ID \| `prusaslicer\.gcode-output` \|' packages/prusa-gcode-output-scope/gcode-output-scope.md` succeeds.
    - `rg -n '\| Planned evidence command \| Phase 48 command text `bazel run //packages/parity:prusaslicer_gcode_output_parity`; the target is not created in Phase 45\. \|' packages/prusa-gcode-output-scope/gcode-output-scope.md` succeeds.
    - `! test -e packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output` succeeds.
    - `! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'prusaslicer\.gcode-output|src/libslic3r/GCode\.cpp|src/libslic3r/GCode\.hpp|expected-gcode-summary\.tsv|slic3r_flavors::prusa_gcode_output|prusaslicer_gcode_output_parity|fork\.prusaslicer\.gcode-output' packages/prusa-gcode-output-scope/README.md packages/prusa-gcode-output-scope/gcode-output-scope.md</automated>
  </verify>
  <done>The new package records every PGSEL-01 field, repeats the PGSEL-02 deferrals, and creates no Phase 46-48 artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fail-closed verifier and mutation tests</name>
  <files>packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh, packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</files>
  <read_first>
    - `packages/prusa-gcode-output-scope/BUILD.bazel`
    - `packages/prusa-gcode-output-scope/README.md`
    - `packages/prusa-gcode-output-scope/gcode-output-scope.md`
    - `packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh`
    - `packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh`
    - `packages/fork-inventories/verify_inventories.sh`
    - `packages/fork-inventories/prusaslicer.tsv`
    - `packages/fork-inventories/category-map.tsv`
    - `packages/parity/status.tsv`
    - `packages/parity/BUILD.bazel`
    - `.planning/phases/45-prusa-g-code-output-scope-gate/45-CONTEXT.md` decisions D-08, D-09, and D-10
  </read_first>
  <behavior>
    - Test 1: A complete temp fixture passes and prints `ok: Prusa G-code output scope verification passed`.
    - Test 2: Wrong inventory row ID, wrong source identity, missing expected-summary contract, missing deferred term, missing reviewer signoff, missing category-map row, premature status row, premature fixture namespace, premature expected summary artifact, and README overclaim each fail with `error:`.
    - Test 3: The real Bazel `verify` target fails if `packages/parity/status.tsv` contains `fork.prusaslicer.gcode-output` or if `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv` exists.
  </behavior>
  <action>
    Implement `verify_prusa_gcode_output_scope.sh` using the Phase 41 helper
    style: `#!/usr/bin/env bash`, `set -euo pipefail`, `error`,
    `require_file`, `require_text`, `require_section_table_row`, exact row
    checks, and small absence-check helpers.

    Required positive checks:

    - `README.md` contains the exact verifier command `bazel run //packages/prusa-gcode-output-scope:verify`.
    - `README.md` contains the three exact non-overclaiming sentences listed in Task 1.
    - `gcode-output-scope.md` contains every exact `## Scope Record` row and every exact `## Source Row Details` row listed in Task 1.
    - `packages/fork-inventories/prusaslicer.tsv` contains exactly one row with first field `prusaslicer.gcode-output`.
    - `packages/fork-inventories/category-map.tsv` references `prusaslicer.gcode-output` exactly once.
    - Deferred-scope terms include every PGSEL-02 term: `Byte-for-byte G-code parity`, `full generated-output parity`, `toolpath geometry`, `extrusion`, `timing`, `support generation`, `wall seam behavior`, `arc fitting`, `STEP import`, `full 3MF import/export`, `printer-runtime behavior`, `firmware or printability behavior`, `GUI export or viewer behavior`, `binary G-code`, `thumbnails`, `post-processing`, `host upload`, `network/device integration`, `profile auto-update execution`, `fork release builds`, `Bambu Studio`, `OrcaSlicer`, `upstream source imports`, and `sync automation`.

    Required absence checks per D-09:

    - Reject `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output` when it exists.
    - Reject `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv` when it exists.
    - Reject `fork.prusaslicer.gcode-output` as a row ID in `packages/parity/status.tsv`.
    - Reject a real `prusaslicer_gcode_output_parity` target in `packages/parity/BUILD.bazel` when the checkout file is readable.
    - Reject Rust implementation markers in `packages/slic3r-rust` when the checkout tree is readable: `pub mod prusa_gcode_output`, `prusa_gcode_output_summary`, or `parse_prusa_gcode_output_summary`.
    - Reject affirmative overclaiming README/scope text: `Phase 45 verifies Prusa G-code output parity`, `verified Prusa G-code output parity`, `byte-for-byte G-code parity verified`, `full generated-output parity verified`, `Phase 45 verifies Bambu Studio support`, `verified Bambu Studio support`, `Bambu Studio support verified`, `Phase 45 verifies OrcaSlicer support`, `verified OrcaSlicer support`, and `OrcaSlicer support verified`.
    - Do not reject explicit absence language from Task 1, including the required negative sentence fragment `no fixture bytes, expected-gcode-summary.tsv, Rust G-code summary implementation, parity command, status row, upstream source import, vendored fork source tree, Git/network/vendor sync behavior, printer-runtime behavior, host upload, profile auto-update execution, network/device integration, credential handling, Bambu Studio support, OrcaSlicer support, or fork release build`.

    The verifier may accept positional args for README, scope record, inventory,
    category map, and status TSV so the Bazel target and temp mutation tests can
    pass isolated files. It should default to the real checkout paths when run
    directly with no args.

    Implement `verify_prusa_gcode_output_scope_test.sh` with Arrange/Act/Assert
    comments. Mirror Phase 41's temp-fixture pattern and include these test
    function names: `test_complete_fixture_passes`,
    `test_wrong_inventory_row_id_fails`, `test_wrong_source_identity_fails`,
    `test_missing_expected_summary_contract_fails`,
    `test_missing_category_map_row_fails`,
    `test_missing_deferred_scope_term_fails`,
    `test_missing_reviewer_signoff_fails`,
    `test_readme_overclaim_fails`, `test_premature_status_row_fails`,
    `test_premature_fixture_namespace_fails`, and
    `test_premature_expected_summary_artifact_fails`.
  </action>
  <acceptance_criteria>
    - `bash -n packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` succeeds.
    - `bash -n packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` succeeds.
    - `bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` succeeds and prints `ok: verify_prusa_gcode_output_scope_test`.
    - `bazel run //packages/prusa-gcode-output-scope:verify` succeeds and prints `ok: Prusa G-code output scope verification passed`.
    - `bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test` succeeds.
    - `shfmt -d packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` produces no diff.
    - `shellcheck packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` succeeds.
    - `! bazel query //packages/parity:prusaslicer_gcode_output_parity` succeeds.
    - `! test -e packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv` succeeds.
    - `! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
    <automated>bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
    <automated>shfmt -d packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>shellcheck packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>! bazel query //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv</automated>
  </verify>
  <done>The verifier passes for the checked-in scope package, the focused mutation test proves fail-closed behavior, and Phase 46-48 artifacts remain absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| scope Markdown -> maintainer claim | Maintainers use the package README and scope record to decide what Phase 45 approves. |
| inventory/status/docs files -> verifier | Shell verifier reads local metadata files and must fail closed on drift or overclaiming. |
| Phase 45 package -> Phase 46-48 artifacts | The package names later owned paths and labels but must not create them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-45-02-01 | Tampering | `gcode-output-scope.md` | mitigate | Verify every required scope table row by exact field/value text. |
| T-45-02-02 | Repudiation | inventory and source identity | mitigate | Require exact `prusaslicer.gcode-output` inventory row, category-map reference, source ref, and source paths. |
| T-45-02-03 | Spoofing | README/docs claim boundary | mitigate | Require non-overclaiming README sentences and reject known overclaiming phrases. |
| T-45-02-04 | Tampering | fixture/expected-summary artifacts | mitigate | Reject `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output` and `expected-gcode-summary.tsv` during Phase 45. |
| T-45-02-05 | Tampering | parity/status publication | mitigate | Reject `fork.prusaslicer.gcode-output` status rows and `//packages/parity:prusaslicer_gcode_output_parity` target availability. |
| T-45-02-06 | Information Disclosure | network/source-import claims | mitigate | Require `no upstream source import`, `no Git, network, vendor sync`, `no host upload`, and `no profile auto-update execution` language. |
</threat_model>

<verification>
Run:

```bash
bazel run //packages/prusa-gcode-output-scope:verify
bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test
shfmt -d packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
shellcheck packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
! bazel query //packages/parity:prusaslicer_gcode_output_parity
! test -e packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv
git diff --check -- packages/prusa-gcode-output-scope/BUILD.bazel packages/prusa-gcode-output-scope/README.md packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
```
</verification>

<success_criteria>
Phase 45 Plan 02 succeeds when the Prusa G-code output scope package is
reviewable, verifier-owned, fail-closed, and still metadata-only.
</success_criteria>

<output>
After completion, create `.planning/phases/45-prusa-g-code-output-scope-gate/45-02-SUMMARY.md`
</output>
