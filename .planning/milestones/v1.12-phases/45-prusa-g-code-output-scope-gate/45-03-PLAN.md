---
phase: 45-prusa-g-code-output-scope-gate
plan: "03"
type: execute
wave: 3
depends_on: ["45-02"]
files_modified:
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/migration-guidance.md
  - docs/port/parity-matrix.md
autonomous: true
requirements: [PGSEL-01, PGSEL-02]
requirements_addressed: [PGSEL-01, PGSEL-02]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 45-2026-06-06T13-53-22
generated_at: 2026-06-06T14:07:50Z
must_haves:
  truths:
    - "Maintainer can find the Phase 45 `prusaslicer.gcode-output` scope gate from the port documentation."
    - "Maintainer can read docs that state Phase 45 does not create fixture bytes, expected summary artifacts, Rust summary implementation, parity command, or status row."
    - "Maintainer can see that broad `generated-outputs` remains in progress and that `fork.prusaslicer.gcode-output` is not verified or published."
    - "Maintainer can run the package verifier and final absence audit after the docs route is added."
  artifacts:
    - path: "docs/port/README.md"
      provides: "Current Prusa G-code output scope gate state"
      contains: "Current Prusa G-code Output Scope Gate State"
    - path: "docs/port/package-map.md"
      provides: "Package role row and Phase 45 ownership note"
      contains: "packages/prusa-gcode-output-scope"
    - path: "docs/port/migration-guidance.md"
      provides: "Fixture/status routing through the Phase 45 scope gate"
      contains: "prusaslicer.gcode-output"
    - path: "docs/port/parity-matrix.md"
      provides: "Non-overclaiming fork parity interpretation for the scope gate"
      contains: "fork.prusaslicer.gcode-output"
  key_links:
    - from: "docs/port/README.md"
      to: "packages/prusa-gcode-output-scope/README.md"
      via: "package path and verify command"
      pattern: "packages/prusa-gcode-output-scope"
    - from: "docs/port/migration-guidance.md"
      to: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      via: "fixture/status work starts from reviewed scope record"
      pattern: "prusaslicer\\.gcode-output"
    - from: "docs/port/parity-matrix.md"
      to: "packages/parity/status.tsv"
      via: "explicit statement that status publication is not Phase 45-owned"
      pattern: "fork\\.prusaslicer\\.gcode-output"
---

<objective>
Make the Phase 45 scope gate discoverable in port documentation without
publishing evidence or broadening parity claims.

Purpose: Maintainers need a route from public control-plane docs to the scope
package, and the docs must repeat the absence boundary so readers do not treat
Phase 45 as G-code output parity.

Output: Minimal docs edits in the same four port surfaces used by Phase 41,
plus final package verifier and absence audit commands.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-prusa-g-code-output-scope-gate/45-CONTEXT.md
@.planning/phases/45-prusa-g-code-output-scope-gate/45-RESEARCH.md
@.planning/milestones/v1.11-phases/41-prusa-project-file-scope-gate/41-01-SUMMARY.md
@.planning/milestones/v1.11-phases/44-executable-prusa-project-file-parity/44-02-SUMMARY.md
@.planning/phases/45-prusa-g-code-output-scope-gate/45-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/prusa-gcode-output-scope/README.md
@packages/prusa-gcode-output-scope/gcode-output-scope.md
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
@docs/port/README.md
@docs/port/package-map.md
@docs/port/migration-guidance.md
@docs/port/parity-matrix.md
@packages/parity/status.tsv

Bright Builds sources loaded for this plan: local `AGENTS.md`,
`AGENTS.bright-builds.md`, `standards-overrides.md`, and pinned standards
`index.md`, `core/architecture.md`, `core/code-shape.md`,
`core/verification.md`, and `core/testing.md` at commit
`05f8d7a6c9c2e157ec4f922a05273e72dab97676`.
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
| --- | --- | --- | --- | --- |
| D-11 | 45-03 | 1, 2 | Full | Updates only the minimum docs needed to route maintainers to the Phase 45 scope gate and state the boundary. |
| D-12 | 45-03 | 1, 2 | Full | Docs preserve the ladder: Phase 45 scope gate, Phase 46 fixture, Phase 47 Rust boundary, Phase 48 evidence/status/docs. |
| D-13 | 45-03 | 1, 2 | Full | Docs keep broad `generated-outputs` in progress and do not publish `fork.prusaslicer.gcode-output`. |
| D-08 | 45-03 | 3 | Full | Final verifier rerun proves required package and docs-facing language remains intact. |
| D-09 | 45-03 | 3 | Full | Final absence audit proves no fixture, expected summary, Rust implementation, parity target, or status row exists. |
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Route the scope package from README and package map</name>
  <files>docs/port/README.md, docs/port/package-map.md</files>
  <read_first>
    - `docs/port/README.md`
    - `docs/port/package-map.md`
    - `packages/prusa-gcode-output-scope/README.md`
    - `packages/prusa-gcode-output-scope/gcode-output-scope.md`
    - `.planning/phases/45-prusa-g-code-output-scope-gate/45-CONTEXT.md` decisions D-11, D-12, and D-13
    - `.planning/milestones/v1.11-phases/41-prusa-project-file-scope-gate/41-01-SUMMARY.md`
  </read_first>
  <action>
    In `docs/port/README.md`, add a section titled exactly:

```markdown
## Current Prusa G-code Output Scope Gate State
```

    Add bullets that include these exact strings:

```text
`packages/prusa-gcode-output-scope` owns the Phase 45 checked-in scope gate for `prusaslicer.gcode-output`; run `bazel run //packages/prusa-gcode-output-scope:verify` to check the reviewed contract.
The accepted source identity is `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, with source path `src/libslic3r/GCode.cpp` and companion API evidence `src/libslic3r/GCode.hpp`.
The Phase 46 fixture path contract is `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/`; no fixture bytes or expected summary artifacts exist in Phase 45.
The Phase 47 Rust boundary is planned as `slic3r_flavors::prusa_gcode_output`, but Phase 45 creates no Rust summary implementation.
The Phase 48 command is planned as `bazel run //packages/parity:prusaslicer_gcode_output_parity`, and the Phase 48 status token is planned as `fork.prusaslicer.gcode-output`; neither the command nor the status row exists in Phase 45.
```

    Add a final deferral bullet with every PGSEL-02 term:

```text
Byte-for-byte G-code parity, full generated-output parity, toolpath geometry, extrusion, timing, support generation, wall seam behavior, arc fitting, STEP import, full 3MF import/export, printer-runtime behavior, firmware or printability behavior, GUI export or viewer behavior, binary G-code, thumbnails, post-processing, host upload, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, and sync automation remain outside the Phase 45 scope gate.
```

    In `docs/port/package-map.md`, add a package table row:

```markdown
| `packages/prusa-gcode-output-scope` | Phase 45 checked-in scope record and verifier package for the narrow `prusaslicer.gcode-output` summary-only evidence contract only |
```

    Add a Phase 45 package-map note with these exact strings:

```text
Phase 45 adds `packages/prusa-gcode-output-scope`, which owns the checked-in `prusaslicer.gcode-output` scope record and verifier only.
It does not create fixture bytes, `expected-gcode-summary.tsv`, Rust G-code summary implementation, parity targets, status rows, upstream source imports, Bambu Studio scope, OrcaSlicer scope, printer-runtime behavior, host upload, profile auto-update execution, or sync automation.
```
  </action>
  <acceptance_criteria>
    - `rg -n '## Current Prusa G-code Output Scope Gate State' docs/port/README.md` succeeds.
    - `rg -n 'bazel run //packages/prusa-gcode-output-scope:verify' docs/port/README.md` succeeds.
    - `rg -n 'neither the command nor the status row exists in Phase 45' docs/port/README.md` succeeds.
    - `rg -n '\| `packages/prusa-gcode-output-scope` \| Phase 45 checked-in scope record and verifier package for the narrow `prusaslicer.gcode-output` summary-only evidence contract only \|' docs/port/package-map.md` succeeds.
    - `rg -n 'It does not create fixture bytes, `expected-gcode-summary.tsv`, Rust G-code summary implementation, parity targets, status rows' docs/port/package-map.md` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'packages/prusa-gcode-output-scope|prusaslicer\.gcode-output|prusaslicer_gcode_output_parity|fork\.prusaslicer\.gcode-output' docs/port/README.md docs/port/package-map.md</automated>
  </verify>
  <done>`docs/port/README.md` and `docs/port/package-map.md` route maintainers to the scope package and state the Phase 45 absence boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Route fixture and status guidance without publishing evidence</name>
  <files>docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <read_first>
    - `docs/port/migration-guidance.md`
    - `docs/port/parity-matrix.md`
    - `packages/prusa-gcode-output-scope/gcode-output-scope.md`
    - `packages/parity/status.tsv`
    - `.planning/phases/45-prusa-g-code-output-scope-gate/45-CONTEXT.md` decisions D-11, D-12, and D-13
    - `.planning/milestones/v1.11-phases/44-executable-prusa-project-file-parity/44-02-SUMMARY.md`
  </read_first>
  <action>
    In `docs/port/migration-guidance.md`, add a fixture-routing bullet in the
    future fork fixture guidance section with these exact strings:

```text
`prusaslicer.gcode-output` fixture work must start from [`packages/prusa-gcode-output-scope`](../../packages/prusa-gcode-output-scope) and its reviewed Phase 45 scope record.
The Phase 46-owned fixture namespace is planned as `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/`, and Phase 45 checks in no `.gcode` fixture bytes or `expected-gcode-summary.tsv`.
The accepted source identity remains `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961` from `src/libslic3r/GCode.cpp`.
```

    In `docs/port/migration-guidance.md`, add a status-routing bullet with
    these exact strings:

```text
`fork.prusaslicer.gcode-output` is not verified or published in Phase 45.
The reserved Phase 48 evidence command is `bazel run //packages/parity:prusaslicer_gcode_output_parity`, and the row may be published only after executable summary-only evidence passes.
```

    In `docs/port/parity-matrix.md`, add a Fork Parity Interpretation bullet
    with these exact strings:

```text
`packages/prusa-gcode-output-scope` is the Phase 45 scope gate for `prusaslicer.gcode-output`.
It records the accepted source identity, Phase 46 fixture and expected-summary contract, Phase 47 Rust boundary, Phase 48 command/status handoff, and deferred scope without creating fixture bytes, expected summary artifacts, Rust summary implementation, a parity command, or a status row.
`fork.prusaslicer.gcode-output` is not verified in `packages/parity/status.tsv` during Phase 45, and broad `generated-outputs` remains `in progress`.
```

    Do not change the `Generated outputs` table row status from `in progress`.
    Do not add `fork.prusaslicer.gcode-output` to `packages/parity/status.tsv`.
  </action>
  <acceptance_criteria>
    - `rg -n 'prusaslicer\.gcode-output` fixture work must start from \[`packages/prusa-gcode-output-scope`\]' docs/port/migration-guidance.md` succeeds.
    - `rg -n '`fork\.prusaslicer\.gcode-output` is not verified or published in Phase 45' docs/port/migration-guidance.md` succeeds.
    - `rg -n '`packages/prusa-gcode-output-scope` is the Phase 45 scope gate for `prusaslicer\.gcode-output`' docs/port/parity-matrix.md` succeeds.
    - `rg -n 'broad `generated-outputs` remains `in progress`' docs/port/parity-matrix.md` succeeds.
    - `awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv` succeeds.
    - `! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'prusaslicer\.gcode-output|fork\.prusaslicer\.gcode-output|generated-outputs' docs/port/migration-guidance.md docs/port/parity-matrix.md</automated>
    <automated>awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv</automated>
    <automated>! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv</automated>
  </verify>
  <done>`docs/port/migration-guidance.md` and `docs/port/parity-matrix.md` route later fixture/status work through the scope gate while keeping Phase 45 non-published and summary-only.</done>
</task>

<task type="auto">
  <name>Task 3: Run final verifier, formatting, and absence audit</name>
  <files>docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <read_first>
    - `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh`
    - `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh`
    - `packages/prusa-gcode-output-scope/README.md`
    - `packages/prusa-gcode-output-scope/gcode-output-scope.md`
    - `docs/port/README.md`
    - `docs/port/package-map.md`
    - `docs/port/migration-guidance.md`
    - `docs/port/parity-matrix.md`
    - `packages/parity/status.tsv`
    - `packages/parity/BUILD.bazel`
  </read_first>
  <action>
    Run the final Phase 45 validation set after docs are updated. Fix only
    Phase 45-owned docs wording or package verifier wording if a check fails.

    Required commands:

```bash
bazel run //packages/fork-inventories:verify
bazel run //packages/prusa-gcode-output-scope:verify
bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test
mdformat --check packages/prusa-gcode-output-scope/README.md packages/prusa-gcode-output-scope/gcode-output-scope.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md
shfmt -d packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
shellcheck packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
! test -e packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output
! test -e packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
! bazel query //packages/parity:prusaslicer_gcode_output_parity
! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv
! rg -n 'pub mod prusa_gcode_output|prusa_gcode_output_summary|parse_prusa_gcode_output_summary' packages/slic3r-rust
git diff --check
```

    Do not run `mdformat` on any phase `*-SUMMARY.md` file per repo-local
    `AGENTS.md` guidance.
  </action>
  <acceptance_criteria>
    - All commands listed in the action pass.
    - `rg -n 'Phase 45 verification does not prove executable Prusa G-code output parity' packages/prusa-gcode-output-scope/README.md` succeeds.
    - `rg -n 'neither the command nor the status row exists in Phase 45' docs/port/README.md` succeeds.
    - `! test -e packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output` succeeds.
    - `! bazel query //packages/parity:prusaslicer_gcode_output_parity` succeeds.
    - `! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>bazel run //packages/fork-inventories:verify</automated>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
    <automated>bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
    <automated>mdformat --check packages/prusa-gcode-output-scope/README.md packages/prusa-gcode-output-scope/gcode-output-scope.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md</automated>
    <automated>! test -e packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output</automated>
    <automated>! bazel query //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Final verification confirms the scope gate is discoverable, non-overclaiming, and still has no fixture, expected summary, Rust implementation, parity target, or status row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| docs -> maintainer interpretation | Maintainers may infer verified support from docs wording if absence boundaries are weak. |
| docs -> status publication | Docs name a reserved status token that must not exist as a checked-in status row during Phase 45. |
| package verifier -> public docs | The package verifier and public docs must agree on the metadata-only boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-45-03-01 | Spoofing | `docs/port/README.md` | mitigate | Add exact wording that Phase 45 does not prove G-code parity and does not create Phase 46-48 artifacts. |
| T-45-03-02 | Repudiation | `docs/port/package-map.md` | mitigate | Add package role and Phase 45 ownership note saying the package is a scope record/verifier only. |
| T-45-03-03 | Tampering | `docs/port/migration-guidance.md` | mitigate | State that `fork.prusaslicer.gcode-output` is not verified or published in Phase 45 and requires Phase 48 executable evidence. |
| T-45-03-04 | Tampering | `packages/parity/status.tsv` | mitigate | Final audit rejects `fork.prusaslicer.gcode-output` rows and confirms `generated-outputs` remains `in progress`. |
| T-45-03-05 | Information Disclosure | network/source-import claims | mitigate | Docs repeat that host upload, network/device, profile auto-update, upstream source import, and sync surfaces are outside Phase 45. |
</threat_model>

<verification>
Run the full final validation set from Task 3, including `bazel run
//packages/prusa-gcode-output-scope:verify`, `bazel test
//packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test`,
Markdown/shell checks, and the fixture/Rust/parity/status absence audit.
</verification>

<success_criteria>
Phase 45 Plan 03 succeeds when public docs route maintainers to the scope gate
without publishing evidence or implying any PGSEL-02 excluded behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/45-prusa-g-code-output-scope-gate/45-03-SUMMARY.md`
</output>
