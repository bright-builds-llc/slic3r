---
phase: 34-rust-flavor-contracts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_contracts/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs
  - packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs
  - packages/slic3r-rust/crates/slic3r_contracts/BUILD.bazel
  - packages/slic3r-rust/BUILD.bazel
  - packages/slic3r-rust/README.md
  - docs/port/contract-inventory.md
  - docs/port/package-map.md
autonomous: true
requirements: [ARCH-01]
requirements_addressed: [ARCH-01]
user_setup: []
must_haves:
  truths:
    - "Developer can import typed Rust contracts for downstream fork identity, flavor identity, vendor source identity, feature origin, parity surface, and checklist status."
    - "Developer can parse raw fork, flavor, source, feature-origin, parity-surface, and checklist-status strings into typed values before they reach core logic."
    - "Canonical vendor source identity accepts only the three selected stable tag plus peeled commit pins and rejects branch-head observations."
    - "Focused Rust tests distinguish base Slic3r, shared downstream, fork-specific, and unknown-needs-review concepts without passing raw vendor strings through example core logic."
    - "Bazel and Cargo verification both include the new contract module and tests."
    - "Docs expose the contract boundary without claiming runtime fork parity, registry composition, online integration, credential handling, or non-free plugin support."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs"
      provides: "Pure Rust fork/flavor contract domain types and strict parsers"
      contains: "pub enum DownstreamFork"
    - path: "packages/slic3r-rust/crates/slic3r_contracts/src/lib.rs"
      provides: "Public crate re-exports for Phase 34 contracts"
      contains: "pub use flavor::"
    - path: "packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs"
      provides: "Behavior-focused ARCH-01 contract tests"
      contains: "unknown-needs-review"
    - path: "packages/slic3r-rust/crates/slic3r_contracts/BUILD.bazel"
      provides: "Package-local Bazel Rust target wiring for source, test, clippy, and rustfmt"
      contains: "flavor_contracts_test"
    - path: "packages/slic3r-rust/BUILD.bazel"
      provides: "Aggregate Rust verify suite coverage for new test"
      contains: "//packages/slic3r-rust/crates/slic3r_contracts:flavor_contracts_test"
    - path: "docs/port/contract-inventory.md"
      provides: "Discoverable contract inventory entry for Rust flavor contracts"
      contains: "Rust Flavor Contracts"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_contracts/src/lib.rs"
      to: "packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs"
      via: "module declaration and public re-exports"
      pattern: "pub mod flavor|pub use flavor::"
    - from: "packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs"
      to: "packages/fork-vendors/forks.tsv"
      via: "canonical source-pin test values copied from Phase 32 vocabulary"
      pattern: "prusaslicer:version_2\\.9\\.5@9a583bd438b195856f3bcf7ea99b69ba4003a961"
    - from: "packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs"
      to: "packages/parity/status.tsv"
      via: "ParitySurface accepted token list"
      pattern: "launcher-packaging"
    - from: "packages/slic3r-rust/crates/slic3r_contracts/BUILD.bazel"
      to: "packages/slic3r-rust/BUILD.bazel"
      via: "new rust_test target included in aggregate verify suite"
      pattern: "flavor_contracts_test"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 34-2026-05-26T21-33-10
generated_at: 2026-05-26T21:51:48Z
---

<objective>
Create pure Rust flavor contract types in `slic3r_contracts` so downstream fork and flavor vocabulary is parsed into domain values before it reaches migration logic.

Purpose: ARCH-01 needs typed, strict, side-effect-free contracts that carry Phase 32 source pins and Phase 33 taxonomy without turning those metadata inputs into runtime fork support.
Output: `flavor.rs` contract module, public re-exports, focused integration tests, Bazel/Cargo verification wiring, and docs that make the boundary discoverable.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-rust-flavor-contracts/34-CONTEXT.md
@.planning/phases/34-rust-flavor-contracts/34-RESEARCH.md
@.planning/phases/32-vendor-source-manifest-and-license-baseline/32-CONTEXT.md
@.planning/phases/33-inventory-templates-and-source-pinned-fork-inventories/33-CONTEXT.md
@.planning/phases/33-inventory-templates-and-source-pinned-fork-inventories/33-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/slic3r-rust/crates/slic3r_contracts/src/lib.rs
@packages/slic3r-rust/crates/slic3r_contracts/tests/parse.rs
@packages/slic3r-rust/crates/slic3r_contracts/BUILD.bazel
@packages/slic3r-rust/BUILD.bazel
@packages/slic3r-rust/README.md
@packages/fork-vendors/forks.tsv
@packages/fork-inventories/category-map.tsv
@packages/parity/status.tsv
@docs/port/contract-inventory.md
@docs/port/package-map.md

<interfaces>
Existing contract crate root:

```rust
#![forbid(unsafe_code)]
pub fn parse_invocation(args: &[String]) -> CliInvocation;
```

Create this public API shape in `packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs` and re-export it from `src/lib.rs`:

```rust
pub enum DownstreamFork {
    PrusaSlicer,
    BambuStudio,
    OrcaSlicer,
}

pub enum FlavorId {
    BaseSlic3r,
    PrusaSlicer,
    BambuStudio,
    OrcaSlicer,
}

pub struct VendorSourceRef {
    // fields private
}

pub enum FeatureOrigin {
    BaseSlic3r,
    SharedDownstream,
    ForkSpecific,
    UnknownNeedsReview,
}

pub struct ParitySurface(&'static str);

pub enum ChecklistStatus {
    FutureCandidate,
    Deferred,
    NoActionBase,
    NeedsReview,
}
```

All six types must implement `as_str()`, `Display`, `FromStr`, and `TryFrom<&str>` where the Rust trait contracts allow it. Use separate explicit parse error types:

```rust
DownstreamForkParseError
FlavorIdParseError
VendorSourceRefParseError
FeatureOriginParseError
ParitySurfaceParseError
ChecklistStatusParseError
```

Canonical tokens:

```text
DownstreamFork: prusaslicer, bambustudio, orcaslicer
FlavorId: base-slic3r, prusaslicer, bambustudio, orcaslicer
VendorSourceRef:
  prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
  bambustudio:v02.06.00.51@b506005bc4ee62124e24bf00e0f58656db3646a6
  orcaslicer:v2.3.2@c724a3f5f51c52336624b689e846c8fbc943a912
FeatureOrigin: base-slic3r, shared-downstream, fork-specific, unknown-needs-review
ParitySurface: cli.version, cli.help, cli.other, export.workflows, transform.workflows, linux.runtime, windows.runtime, linux.packaged-launcher, windows.packaged-launcher, config, config.persistence, file-formats, generated-outputs, launcher-packaging
ChecklistStatus: future-candidate, deferred, no-action-base, needs-review
```

Branch-head observations that must not parse as `VendorSourceRef`:

```text
prusaslicer:master@43f3cdb1a6f25ee8627f5f20b9a21f3e62c6ad9b
bambustudio:master@e150b502b3d2afc98b83dcc9e5720e998f9eb79a
orcaslicer:main@e0c4d11baefa328331be113533c47ee89fda16c6
bambustudio:branch-main@e150b502b3d2afc98b83dcc9e5720e998f9eb79a
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage |
|----------|------|------|----------|
| D-01 | 34-01 | 1,2 | Full - contracts live in `packages/slic3r-rust/crates/slic3r_contracts`. |
| D-02 | 34-01 | 1,2,3 | Full - only pure domain types, parser/display helpers, tests, build wiring, and docs are planned. |
| D-03 | 34-01 | 1 | Full - TSV files are source-of-truth vocabulary for Rust constants/tests, not runtime inputs. |
| D-04 | 34-01 | 1 | Full - `DownstreamFork` models `prusaslicer`, `bambustudio`, and `orcaslicer`. |
| D-05 | 34-01 | 1 | Full - `FlavorId` is separate from `DownstreamFork` and includes `base-slic3r`. |
| D-06 | 34-01 | 1 | Full - `VendorSourceRef` parses only selected tag plus peeled commit pins and rejects branch heads. |
| D-07 | 34-01 | 1 | Full - `FeatureOrigin` models `base-slic3r`, `shared-downstream`, `fork-specific`, and `unknown-needs-review`. |
| D-08 | 34-01 | 1 | Full - `ParitySurface` is a private-field validated newtype over `packages/parity/status.tsv` tokens. |
| D-09 | 34-01 | 1 | Full - `ChecklistStatus` models `future-candidate`, `deferred`, `no-action-base`, and `needs-review` only. |
| D-10 | 34-01 | 1 | Full - strict `FromStr`/`TryFrom<&str>` APIs return explicit parse errors. |
| D-11 | 34-01 | 1 | Full - canonical lowercase hyphenated display is required for every token. |
| D-12 | 34-01 | 1 | Full - no `unwrap()` in production code; use guards, `let...else`, and `?` where appropriate. |
| D-13 | 34-01 | 1 | Full - focused `flavor_contracts.rs` tests cover parse/display, rejection, and taxonomy distinctions. |
| D-14 | 34-01 | 1 | Full - tests use Arrange/Act/Assert and typed boundary examples before core logic. |
| D-15 | 34-01 | 2 | Full - Bazel package tests, rustfmt, clippy, aggregate verify, and Cargo checks are required. |
</decision_coverage>

<dependency_graph>
| Unit | Needs | Creates | Wave |
|------|-------|---------|------|
| Contract module and tests | Phase 32/33/parity vocabularies, existing `slic3r_contracts` crate | Typed Rust contracts, strict parser APIs, behavior tests | 1 |
| Build verification wiring | New test/module paths | Bazel source/test/clippy/rustfmt/verify coverage | 1 |
| Docs visibility | New public API and verification labels | Discoverable contract boundary in Rust and port docs | 1 |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed flavor contracts and focused parser tests</name>
  <files>packages/slic3r-rust/crates/slic3r_contracts/src/lib.rs, packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs, packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_contracts/src/lib.rs`
    - `packages/slic3r-rust/crates/slic3r_contracts/tests/parse.rs`
    - `.planning/phases/34-rust-flavor-contracts/34-CONTEXT.md`
    - `.planning/phases/34-rust-flavor-contracts/34-RESEARCH.md`
    - `.planning/phases/32-vendor-source-manifest-and-license-baseline/32-CONTEXT.md`
    - `.planning/phases/33-inventory-templates-and-source-pinned-fork-inventories/33-CONTEXT.md`
    - `packages/fork-vendors/forks.tsv`
    - `packages/fork-inventories/category-map.tsv`
    - `packages/parity/status.tsv`
  </read_first>
  <behavior>
    - Test 1: `DownstreamFork` parses and displays `prusaslicer`, `bambustudio`, and `orcaslicer`, and rejects `bambu-studio`, `BambuStudio`, and `base-slic3r`.
    - Test 2: `FlavorId` parses and displays `base-slic3r`, `prusaslicer`, `bambustudio`, and `orcaslicer` as a distinct type from `DownstreamFork`; `FlavorId::BaseSlic3r.maybe_downstream_fork()` returns `None`.
    - Test 3: `VendorSourceRef` parses exactly the three canonical Phase 32 pins and exposes `vendor()`, `selected_tag()`, `peeled_commit()`, and canonical `Display`.
    - Test 4: `VendorSourceRef` rejects `prusaslicer:master@43f3cdb1a6f25ee8627f5f20b9a21f3e62c6ad9b`, `bambustudio:master@e150b502b3d2afc98b83dcc9e5720e998f9eb79a`, `orcaslicer:main@e0c4d11baefa328331be113533c47ee89fda16c6`, `bambustudio:branch-main@e150b502b3d2afc98b83dcc9e5720e998f9eb79a`, uppercase commit hex, short commit hex, missing `:`, missing `@`, empty selected tag, and unknown vendor.
    - Test 5: `FeatureOrigin` parses and displays `base-slic3r`, `shared-downstream`, `fork-specific`, and `unknown-needs-review`, and rejects prose labels.
    - Test 6: `ParitySurface` parses and displays all 14 tokens from `packages/parity/status.tsv` and rejects `fork-runtime`, `cli.fork`, and `verified`.
    - Test 7: `ChecklistStatus` parses and displays `future-candidate`, `deferred`, `no-action-base`, and `needs-review`, and rejects executable parity words such as `verified` and `supported`.
    - Test 8: A small test-local function accepts `DownstreamFork`, `FlavorId`, `VendorSourceRef`, `FeatureOrigin`, `ParitySurface`, and `ChecklistStatus` typed values and proves base, shared downstream, fork-specific, and unknown-needs-review concepts are passed without raw vendor strings.
  </behavior>
  <action>Create `packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs` first with the behavior tests above, using explicit `// Arrange`, `// Act`, and `// Assert` sections. Use `std::str::FromStr` and `TryFrom` in tests so the public parser traits are exercised directly.

Create `packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs` with `#![allow(clippy::module_name_repetitions)]` only if Clippy requires it; do not add any dependency. Implement the exact public types listed in `<interfaces>` with short doc comments explaining their contract role. Keep `VendorSourceRef` and `ParitySurface` fields private. Add `as_str()` for all token types. Add `FlavorId::maybe_downstream_fork(self) -> Option<DownstreamFork>`. Add `VendorSourceRef::vendor(self) -> DownstreamFork`, `VendorSourceRef::selected_tag(self) -> &'static str`, and `VendorSourceRef::peeled_commit(self) -> &'static str`. Add named constructors `VendorSourceRef::prusa_slicer_version_2_9_5()`, `VendorSourceRef::bambu_studio_v02_06_00_51()`, and `VendorSourceRef::orca_slicer_v2_3_2()`.

Implement `Display`, `FromStr`, and `TryFrom<&str>` for each contract type. For closed enum types, parse only the canonical lowercase/hyphenated tokens. For `ParitySurface`, use a private-field newtype and accept only the 14 status tokens listed in `<interfaces>`. For `VendorSourceRef`, parse the shape `vendor_id:selected_tag@peeled_commit` with `split_once`, reject missing separators, reject empty selected tags, require exactly 40 lowercase hex commit characters, parse the vendor through `DownstreamFork`, then accept only the three exact Phase 32 source pins. Return explicit error variants from `VendorSourceRefParseError` such as `MissingVendorSeparator { value: String }`, `MissingCommitSeparator { value: String }`, `EmptySelectedTag { value: String }`, `UnknownVendor { value: String }`, `InvalidCommit { value: String }`, and `UnknownSourcePin { value: String }`.

For simpler token types, use parse error structs with `value: String`, for example `DownstreamForkParseError { value: String }`. Derive `Debug`, `Clone`, `PartialEq`, and `Eq` for public contract and error types where possible. Implement `Display` and `std::error::Error` for every parse error type. Do not use `unwrap()` in production code. Do not add filesystem, Git, network, process, source-tree vendoring, registry composition, launcher dispatch, online/cloud integration, credential handling, non-free plugin ingestion, or runtime fork parity behavior.

Update `packages/slic3r-rust/crates/slic3r_contracts/src/lib.rs` with `pub mod flavor;` and `pub use flavor::{...};` re-exporting all six contract types and all six parse error types from `<interfaces>`.</action>
  <verify>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_contracts --test flavor_contracts && rg -n "pub enum DownstreamFork|pub enum FlavorId|pub struct VendorSourceRef|pub enum FeatureOrigin|pub struct ParitySurface|pub enum ChecklistStatus|pub use flavor::" packages/slic3r-rust/crates/slic3r_contracts/src/lib.rs packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs && ! rg -n "\\.unwrap\\(" packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs</automated>
  </verify>
  <acceptance_criteria>
    - `packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs` defines `DownstreamFork`, `FlavorId`, `VendorSourceRef`, `FeatureOrigin`, `ParitySurface`, and `ChecklistStatus`.
    - `src/lib.rs` contains `pub mod flavor;` and `pub use flavor::` re-exports for all six contract types and all six parse error types.
    - `flavor_contracts.rs` contains tests for `bambu-studio`, `branch-main`, `unknown-needs-review`, `launcher-packaging`, `future-candidate`, and `base-slic3r`.
    - `VendorSourceRef` accepts only the three canonical source pins from `packages/fork-vendors/forks.tsv`.
    - `VendorSourceRef` rejects branch-head observations from `forks.tsv` and malformed commits before values reach any example core function.
    - `ParitySurface` accepts every token in `packages/parity/status.tsv` column 1 and does not store arbitrary raw strings.
    - Production code in `flavor.rs` contains no `.unwrap(` calls.
    - No code in `flavor.rs` reads TSV files or performs filesystem, Git, network, process, registry, launcher dispatch, online/cloud, credential, plugin, or runtime fork behavior.
  </acceptance_criteria>
  <done>ARCH-01 contract values exist as strict Rust domain types with parser/display behavior and focused tests proving canonical and rejected inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Bazel and Cargo verification for the new contracts</name>
  <files>packages/slic3r-rust/crates/slic3r_contracts/BUILD.bazel, packages/slic3r-rust/BUILD.bazel</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_contracts/BUILD.bazel`
    - `packages/slic3r-rust/BUILD.bazel`
    - `packages/slic3r-rust/Cargo.toml`
    - `packages/slic3r-rust/crates/slic3r_contracts/src/lib.rs`
    - `packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs`
    - `packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs`
    - `.planning/phases/34-rust-flavor-contracts/34-RESEARCH.md`
  </read_first>
  <action>Update `packages/slic3r-rust/crates/slic3r_contracts/BUILD.bazel` so the `rust_library(name = "slic3r_contracts")` `srcs` list includes both `src/lib.rs` and `src/flavor.rs`. Add a new `rust_test(name = "flavor_contracts_test")` with `srcs = ["tests/flavor_contracts.rs"]`, `deps = [":slic3r_contracts"]`, and `edition = "2024"`. Add `:flavor_contracts_test` to the existing `rust_clippy(name = "clippy")` `deps` list and to the existing `rustfmt_test(name = "rustfmt_check")` `targets` list.

Update `packages/slic3r-rust/BUILD.bazel` so `test_suite(name = "verify")` includes `//packages/slic3r-rust/crates/slic3r_contracts:flavor_contracts_test` alongside the existing `slic3r_contracts_test` entry. Do not remove or rename existing targets. Do not add new external dependencies or Cargo manifest entries.</action>
  <verify>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_contracts:flavor_contracts_test //packages/slic3r-rust/crates/slic3r_contracts:rustfmt_check //packages/slic3r-rust/crates/slic3r_contracts:clippy //packages/slic3r-rust:verify && rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check && rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings && rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features && rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `packages/slic3r-rust/crates/slic3r_contracts/BUILD.bazel` lists `src/flavor.rs` in the library `srcs`.
    - `packages/slic3r-rust/crates/slic3r_contracts/BUILD.bazel` defines `rust_test(name = "flavor_contracts_test")`.
    - `flavor_contracts_test` is included in the package-local `clippy` deps and `rustfmt_check` targets.
    - `packages/slic3r-rust/BUILD.bazel` includes `//packages/slic3r-rust/crates/slic3r_contracts:flavor_contracts_test` in `//packages/slic3r-rust:verify`.
    - The pinned Rust 1.94.1 Cargo fmt, clippy, build, and test commands pass.
    - No Cargo dependency is added for Phase 34 contract errors.
  </acceptance_criteria>
  <done>Bazel and Cargo both verify the new Rust contract module and tests through existing package-local and aggregate workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Document the Rust flavor contract boundary</name>
  <files>packages/slic3r-rust/README.md, docs/port/contract-inventory.md, docs/port/package-map.md</files>
  <read_first>
    - `packages/slic3r-rust/README.md`
    - `docs/port/contract-inventory.md`
    - `docs/port/package-map.md`
    - `packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs`
    - `packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs`
    - `.planning/phases/34-rust-flavor-contracts/34-CONTEXT.md`
    - `.planning/REQUIREMENTS.md`
    - `AGENTS.md`
  </read_first>
  <action>Update `packages/slic3r-rust/README.md` in the existing package overview style. Under layout or notes, state that `crates/slic3r_contracts/` now owns launcher contract types plus Rust flavor contracts for downstream fork identity, flavor identity, vendor source identity, feature origin, parity surface, and checklist status. Add these exact tokens in prose or a compact list: `DownstreamFork`, `FlavorId`, `VendorSourceRef`, `FeatureOrigin`, `ParitySurface`, `ChecklistStatus`. Mention the package-local test label `//packages/slic3r-rust/crates/slic3r_contracts:flavor_contracts_test`.

Update `docs/port/contract-inventory.md` with a `## Rust Flavor Contracts` section. The section must include the exact sentence: `Rust flavor contracts parse fork and flavor metadata into typed Rust values before core migration logic receives them.` Then list the six public contract names, the three canonical source pins, the feature-origin tokens, checklist-status tokens, and parity-surface source `packages/parity/status.tsv`. Include the exact sentence: `These contracts are metadata boundaries only; they do not implement a flavor registry, runtime fork behavior, fork-flavor release builds, online/cloud integration, credential handling, non-free plugin ingestion, or executable fork parity.`

Update `docs/port/package-map.md` with a Phase 34 note under Notes. State that Phase 34 extends `packages/slic3r-rust/crates/slic3r_contracts` with typed fork/flavor contract values and tests, and that Phase 35 owns registry composition. Preserve the existing Phase 32/33 source-intake and inventory-only boundaries. Do not edit the managed Bright Builds block in `AGENTS.md`, do not edit `AGENTS.bright-builds.md`, and do not edit phase summary files.</action>
  <verify>
    <automated>rg -n "DownstreamFork|FlavorId|VendorSourceRef|FeatureOrigin|ParitySurface|ChecklistStatus|//packages/slic3r-rust/crates/slic3r_contracts:flavor_contracts_test|Rust flavor contracts parse fork and flavor metadata into typed Rust values before core migration logic receives them|These contracts are metadata boundaries only|Phase 34 extends `packages/slic3r-rust/crates/slic3r_contracts`" packages/slic3r-rust/README.md docs/port/contract-inventory.md docs/port/package-map.md && git diff --check -- packages/slic3r-rust/crates/slic3r_contracts/src/lib.rs packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs packages/slic3r-rust/crates/slic3r_contracts/BUILD.bazel packages/slic3r-rust/BUILD.bazel packages/slic3r-rust/README.md docs/port/contract-inventory.md docs/port/package-map.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/slic3r-rust/README.md` names all six public contract types and the `flavor_contracts_test` Bazel label.
    - `docs/port/contract-inventory.md` contains the `## Rust Flavor Contracts` section and both exact boundary sentences from the action.
    - `docs/port/package-map.md` contains a Phase 34 note that names `packages/slic3r-rust/crates/slic3r_contracts` and says Phase 35 owns registry composition.
    - Docs do not claim runtime fork behavior, fork-flavor release builds, online/cloud integration, credential handling, non-free plugin ingestion, or executable fork parity.
    - No managed Bright Builds files or phase summary frontmatter are changed.
  </acceptance_criteria>
  <done>Developers can discover the new typed flavor contracts from Rust package docs and port contract docs without reading raw TSV data or inferring runtime support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw metadata string -> Rust contract parser | Fork, flavor, source, feature-origin, parity-surface, and checklist-status strings are untrusted until parsed. |
| Phase 32 source pins -> `VendorSourceRef` | Selected tag plus peeled commit pins become typed source identity, while default branch heads remain drift-only observations. |
| Phase 33 taxonomy -> Rust enums | Ownership and checklist vocabulary crosses from checked-in TSV data into Rust domain values. |
| Typed contracts -> downstream registry/core logic | Later phases will consume these values and must not receive raw vendor strings. |
| Docs/tests -> reviewer interpretation | Source pins and checklist status can be misread as runtime fork parity if wording conflates concepts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Spoofing | `DownstreamFork`, `FlavorId` parsers | mitigate | Strict `FromStr`/`TryFrom<&str>` matching accepts only canonical lowercase tokens and tests reject alternates such as `bambu-studio`, `BambuStudio`, and `base-slic3r` for `DownstreamFork`. |
| T-34-02 | Tampering | `VendorSourceRef` parser | mitigate | Parse the required `vendor_id:selected_tag@peeled_commit` shape, validate known vendor, non-empty tag, 40 lowercase hex commit, then accept only the three exact Phase 32 source pins. |
| T-34-03 | Tampering | Branch-head source observations | mitigate | Tests explicitly reject `master@...`, `main@...`, and `branch-main@...` source refs so drift-only branch heads cannot become stable source identity. |
| T-34-04 | Repudiation | `FeatureOrigin`, `ParitySurface`, `ChecklistStatus` semantics | mitigate | Keep separate Rust types for origin, parity surface, and checklist status; tests reject executable parity words such as `verified` and `supported` in checklist parsing. |
| T-34-05 | Information Disclosure | Contract crate side effects | mitigate | Phase 34 contract code must not add filesystem, Git, network, process, online/cloud, credential, plugin ingestion, source-tree import, or runtime behavior paths. |
| T-34-06 | Elevation of Privilege | Invalid typed value construction | mitigate | `VendorSourceRef` and `ParitySurface` fields remain private, with public construction only through fallible parsers or named canonical constructors. |
| T-34-07 | Denial of Service | Very large raw strings passed to parsers | accept | Parsers perform bounded splits and token/length checks over local strings only; no allocation-heavy recursion, IO, or external calls are introduced. |
</threat_model>

<verification>
Overall verification for this plan:

```bash
bazel test //packages/slic3r-rust/crates/slic3r_contracts:flavor_contracts_test //packages/slic3r-rust/crates/slic3r_contracts:rustfmt_check //packages/slic3r-rust/crates/slic3r_contracts:clippy //packages/slic3r-rust:verify
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features
rg -n "DownstreamFork|FlavorId|VendorSourceRef|FeatureOrigin|ParitySurface|ChecklistStatus|Rust flavor contracts parse fork and flavor metadata into typed Rust values before core migration logic receives them" packages/slic3r-rust/README.md docs/port/contract-inventory.md docs/port/package-map.md
git diff --check -- packages/slic3r-rust/crates/slic3r_contracts/src/lib.rs packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs packages/slic3r-rust/crates/slic3r_contracts/BUILD.bazel packages/slic3r-rust/BUILD.bazel packages/slic3r-rust/README.md docs/port/contract-inventory.md docs/port/package-map.md
```
</verification>

<success_criteria>
- ARCH-01 is materially satisfied by public Rust contract types for downstream fork identity, flavor identity, vendor source identity, feature origin, parity surface, and checklist status.
- Raw strings parse into typed Rust values through strict `FromStr`/`TryFrom<&str>` APIs with explicit errors.
- Canonical display returns the exact TSV/docs tokens from Phase 32, Phase 33, and `packages/parity/status.tsv`.
- Branch-head observations and typo tokens fail before values can enter example core logic.
- `flavor_contracts_test`, package-local rustfmt/clippy, aggregate `//packages/slic3r-rust:verify`, and pinned Rust 1.94.1 Cargo checks pass.
- No implementation reads TSV files at runtime or adds filesystem, Git, network, process, source import, registry composition, runtime fork behavior, online/cloud integration, credential handling, non-free plugin ingestion, or executable fork parity.
</success_criteria>

<output>
After completion, create `.planning/phases/34-rust-flavor-contracts/34-01-SUMMARY.md` with `requirements-completed: [ARCH-01]` using the hyphenated key. Do not run `mdformat` over the summary.
</output>
