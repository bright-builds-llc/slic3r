---
phase: 60-executable-arc-fitting-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_arc_fitting_summary.rs
  - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
  - packages/parity/compare_prusaslicer_arc_fitting.sh
  - packages/parity/BUILD.bazel
autonomous: true
requirements: [ARCEV-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 60-2026-06-24T15-09-13
generated_at: 2026-06-24T15:27:46.600Z
must_haves:
  truths:
    - "Maintainer can run bazel run //packages/parity:prusaslicer_arc_fitting_parity."
    - "The public command validates expected-arc-summary.tsv through slic3r_flavors::prusa_arc_fitting."
    - "Existing bazel run //packages/parity:prusaslicer_gcode_output_parity still passes unchanged."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_arc_fitting_summary.rs"
      provides: "Thin Rust CLI adapter over prusa_arc_fitting_summary_lines"
      contains: "prusa_arc_fitting_summary_lines"
    - path: "packages/parity/compare_prusaslicer_arc_fitting.sh"
      provides: "Public shell comparator for checked-in arc summary evidence"
      contains: "ok: fork.prusaslicer.arc-fitting checked-in summary evidence passed"
    - path: "packages/parity/BUILD.bazel"
      provides: "prusaslicer_arc_fitting_parity sh_binary target"
      contains: "name = \"prusaslicer_arc_fitting_parity\""
  key_links:
    - from: "packages/parity/BUILD.bazel"
      to: "packages/slic3r-rust/crates/slic3r_flavors:prusa_arc_fitting_summary"
      via: "sh_binary data and args"
      pattern: "prusa_arc_fitting_summary"
    - from: "packages/parity/compare_prusaslicer_arc_fitting.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv"
      via: "explicit Bazel runfile argument"
      pattern: "expected-arc-summary.tsv"
---

<objective>
Add the public executable arc-fitting evidence command required by ARCEV-01.

Purpose: Phase 60 must publish a runnable evidence command without widening the existing Prusa G-code output command or claiming generated-output parity.
Output: A Rust summary binary, a public `packages/parity` shell comparator, and Bazel target `//packages/parity:prusaslicer_arc_fitting_parity`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
@.planning/phases/60-executable-arc-fitting-evidence/60-RESEARCH.md
@.planning/milestones/v1.14-phases/56-executable-semantic-g-code-evidence/56-01-SUMMARY.md
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
@packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
@packages/parity/compare_prusaslicer_gcode_output.sh
@packages/parity/BUILD.bazel
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv

<interfaces>
From `packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs`:
```rust
pub use prusa_arc_fitting::{
    parse_prusa_arc_fitting_summary,
    prusa_arc_fitting_summary_lines,
};
```

The existing `prusa_arc_fitting_summary_lines(input: &str)` returns these exact keys:
`arc_summary_path`, `arc_row_count`, `source_ref`, `inventory_source_paths`, `source_anchor`, `fixture_id`, `fixture_path`, `arc_command_counts`, `arc_direction_counts`, `center_offset_observations`, `coordinate_bounds`, `extrusion_observations`, `feedrate_observations`, `evidence_boundary`.

Required public output facts per D-03:
`source identity`, `fixture identity/path`, `G2/G3 counts`, `direction counts`, `center-offset observations`, `coordinate bounds`, `extrusion observations`, `feedrate observations`, and `checked-in-summary evidence boundary`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the prusa_arc_fitting_summary Rust binary</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_arc_fitting_summary.rs, packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel</files>
  <read_first>
    - NEW FILE: packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_arc_fitting_summary.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
    - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <behavior>
    - RED: `bazel query //packages/slic3r-rust/crates/slic3r_flavors:prusa_arc_fitting_summary` fails before the target exists.
    - GREEN: Running the binary with one `expected-arc-summary.tsv` path prints Rust-produced summary lines and exits 0.
    - Error behavior: wrong arity exits nonzero and prints `error: expected expected-arc-summary.tsv`.
  </behavior>
  <action>Create `packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_arc_fitting_summary.rs` with `#![forbid(unsafe_code)]`. Use `std::{env, fs, path::Path, process::ExitCode}` and import `slic3r_flavors::prusa_arc_fitting_summary_lines`. Accept exactly one argument: `expected-arc-summary.tsv`. On wrong arity, return `Err("expected expected-arc-summary.tsv".to_owned())`. Read the caller-supplied local file with `fs::read_to_string(path)` and map read errors to `failed to read {path}: {error}`. Call `prusa_arc_fitting_summary_lines(&input)` and map parser errors to `failed to summarize arc-fitting {path}: {error:?}`. Print every returned line with `println!("{line}")`. In `BUILD.bazel`, add a `rust_binary(name = "prusa_arc_fitting_summary", srcs = ["src/bin/prusa_arc_fitting_summary.rs"], crate_root = "src/bin/prusa_arc_fitting_summary.rs", deps = [":slic3r_flavors"], edition = "2024")`, then add `:prusa_arc_fitting_summary` to both `rust_clippy(name = "clippy")` deps and `rustfmt_test(name = "rustfmt_check")` targets. This implements D-02 and uses the research recommendation for a new arc-specific binary instead of widening `prusa_gcode_output_summary`.</action>
  <verify>
    <automated>bazel query //packages/slic3r-rust/crates/slic3r_flavors:prusa_arc_fitting_summary</automated>
    <automated>bazel build //packages/slic3r-rust/crates/slic3r_flavors:prusa_arc_fitting_summary</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check --test_output=errors</automated>
    <automated>bazel build //packages/slic3r-rust/crates/slic3r_flavors:clippy</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'name = "prusa_arc_fitting_summary"' packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` returns a match.
    - `rg -n 'prusa_arc_fitting_summary_lines' packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_arc_fitting_summary.rs` returns a match.
    - `rg -n 'expected expected-arc-summary.tsv' packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_arc_fitting_summary.rs` returns a match.
    - `rg -n ':prusa_arc_fitting_summary' packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` returns at least three matches: binary name reference, clippy deps, and rustfmt targets.
  </acceptance_criteria>
  <done>`prusa_arc_fitting_summary` exists as a Bazel Rust binary, reads only an explicit local TSV path, delegates parsing to `prusa_arc_fitting_summary_lines`, and is covered by crate clippy/rustfmt wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the public arc-fitting parity command</name>
  <files>packages/parity/compare_prusaslicer_arc_fitting.sh, packages/parity/BUILD.bazel</files>
  <read_first>
    - NEW FILE: packages/parity/compare_prusaslicer_arc_fitting.sh
    - packages/parity/compare_prusaslicer_gcode_output.sh
    - packages/parity/BUILD.bazel
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/fixture-provenance.tsv
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <behavior>
    - RED: `bazel query //packages/parity:prusaslicer_arc_fitting_parity` fails before the target exists.
    - GREEN: `bazel run //packages/parity:prusaslicer_arc_fitting_parity` exits 0 and prints the exact narrow facts listed below.
    - Regression: `bazel run //packages/parity:prusaslicer_gcode_output_parity` still exits 0.
  </behavior>
  <action>Create `packages/parity/compare_prusaslicer_arc_fitting.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Accept exactly four arguments: `summary_binary rust-arc-input.tsv expected-arc-summary.tsv fixture-provenance.tsv`. Require all four files exist and require `summary_binary` to be executable. Run the Rust binary once against `rust-arc-input.tsv` into `actual-arc-summary.tsv` and once against `expected-arc-summary.tsv` into `expected-arc-summary-lines.tsv`, then `diff -u expected-arc-summary-lines.tsv actual-arc-summary.tsv`. On Rust validation failure for the expected artifact, print `error: expected-arc-summary.tsv failed Rust arc validation at {field} in {path}` and include a `diff -u` against the Rust input when possible. On summary mismatch, print `error: expected-arc-summary.tsv mismatch at {field} in {path}` and include `diff -u`. Add `field_value`, `assert_field`, and `first_arc_summary_mismatch_label` helpers equivalent to the G-code comparator pattern but keyed to the summary-line keys. Assert exact values: `arc_summary_path=packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv`, `arc_row_count=12`, `source_ref=prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, `fixture_id=arc-fitting-observations.gcode`, `fixture_path=packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/arc-fitting-observations.gcode`, `arc_command_counts=G2:1;G3:1;total_arc_commands:2`, `arc_direction_counts=clockwise_g2:1;counterclockwise_g3:1`, `center_offset_observations=i_values:5.000,-5.000;j_values:0.000,0.000`, `coordinate_bounds=x_min:0.000;x_max:10.000;y_min:0.000;y_max:0.000`, `extrusion_observations=e_values:0.50000,1.00000;e_axis_observed:true`, `feedrate_observations=F1800:2`, and `evidence_boundary=checked-in-arc-summary-only`. Print exactly these public lines: `ok: fork.prusaslicer.arc-fitting checked-in summary evidence passed`, `source_ref: prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, `fixture: packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/arc-fitting-observations.gcode`, `expected_arc_summary: packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv`, `arc_rows: 12`, `arc_command_counts: G2:1;G3:1;total_arc_commands:2`, `arc_direction_counts: clockwise_g2:1;counterclockwise_g3:1`, `center_offset_observations: i_values:5.000,-5.000;j_values:0.000,0.000`, `coordinate_bounds: x_min:0.000;x_max:10.000;y_min:0.000;y_max:0.000`, `extrusion_observations: e_values:0.50000,1.00000;e_axis_observed:true`, `feedrate_observations: F1800:2`, and `evidence_boundary: checked-in-arc-summary-only`. In `packages/parity/BUILD.bazel`, add `sh_binary(name = "prusaslicer_arc_fitting_parity", srcs = ["compare_prusaslicer_arc_fitting.sh"], data = ["status.tsv", "//packages/slic3r-rust/crates/slic3r_flavors:prusa_arc_fitting_summary", "//packages/parity-fixtures:prusa_arc_fitting_expected_arc_summary", "//packages/parity-fixtures:prusa_arc_fitting_provenance"], args = ["$(location //packages/slic3r-rust/crates/slic3r_flavors:prusa_arc_fitting_summary)", "$(location //packages/parity-fixtures:prusa_arc_fitting_expected_arc_summary)", "$(location //packages/parity-fixtures:prusa_arc_fitting_expected_arc_summary)", "$(location //packages/parity-fixtures:prusa_arc_fitting_provenance)"])`. Do not modify `compare_prusaslicer_gcode_output.sh`, `prusaslicer_gcode_output_parity`, or the `fork.prusaslicer.gcode-output` row; this implements D-01, D-02, D-03, and D-04.</action>
  <verify>
    <automated>bash -n packages/parity/compare_prusaslicer_arc_fitting.sh</automated>
    <automated>shfmt -l -d packages/parity/compare_prusaslicer_arc_fitting.sh</automated>
    <automated>bazel query //packages/parity:prusaslicer_arc_fitting_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_arc_fitting_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'name = "prusaslicer_arc_fitting_parity"' packages/parity/BUILD.bazel` returns a match.
    - `rg -n 'compare_prusaslicer_arc_fitting.sh' packages/parity/BUILD.bazel packages/parity/compare_prusaslicer_arc_fitting.sh` returns matches.
    - `rg -n 'ok: fork.prusaslicer.arc-fitting checked-in summary evidence passed' packages/parity/compare_prusaslicer_arc_fitting.sh` returns a match.
    - `rg -n 'arc_command_counts: G2:1;G3:1;total_arc_commands:2' packages/parity/compare_prusaslicer_arc_fitting.sh` returns a match.
    - `rg -n 'byte parity|byte-for-byte|generated-output parity|algorithm equivalence|geometry parity|tolerance parity|printability|printer-runtime behavior|GUI behavior|release behavior|sync behavior|non-Prusa fork support' packages/parity/compare_prusaslicer_arc_fitting.sh` returns no matches except deferred/error wording if added explicitly as a forbidden-claim guard.
  </acceptance_criteria>
  <done>The public command validates the checked-in arc summary through Rust, prints only the approved arc facts, and leaves the existing Prusa G-code output command contract unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in TSV -> Rust parser | `expected-arc-summary.tsv` is parsed as local evidence input before public output is produced. |
| Bazel runfiles -> shell comparator | Shell receives explicit Bazel runfile paths and must not discover runtime files or external sources. |
| command output -> maintainer interpretation | Public wording must not overclaim byte parity, generated-output parity, runtime, GUI, or fork support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01-01 | Tampering | `prusa_arc_fitting_summary.rs` and comparator | mitigate | Validate TSV through `prusa_arc_fitting_summary_lines`, assert exact fields, and diff Rust-produced summary lines. |
| T-60-01-02 | Information Disclosure | public command | mitigate | Keep inputs local and explicit; no secrets, credentials, private data, network/device behavior, upstream source import, runtime discovery, host upload, or printer-runtime exposure. |
| T-60-01-03 | Spoofing / Repudiation | public output wording | mitigate | Print only exact approved facts and avoid byte parity, generated-output parity, algorithm equivalence, printability, runtime, GUI, release, sync, or non-Prusa support claims. |
</threat_model>

<verification>
Run the task verification commands, then run:

```bash
bazel test //packages/slic3r-rust:verify --test_output=errors
git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_arc_fitting_summary.rs packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel packages/parity/compare_prusaslicer_arc_fitting.sh packages/parity/BUILD.bazel
```
</verification>

<success_criteria>
ARCEV-01 is satisfied for the command layer when `bazel run //packages/parity:prusaslicer_arc_fitting_parity` passes, prints the approved arc facts, uses the Rust boundary, and `bazel run //packages/parity:prusaslicer_gcode_output_parity` still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/60-executable-arc-fitting-evidence/60-01-SUMMARY.md`.
</output>
