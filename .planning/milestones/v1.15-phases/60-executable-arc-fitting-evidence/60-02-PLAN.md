---
phase: 60-executable-arc-fitting-evidence
plan: "02"
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - packages/parity/compare_prusaslicer_arc_fitting_test.sh
  - packages/parity/BUILD.bazel
autonomous: true
requirements: [ARCEV-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 60-2026-06-24T15-09-13
generated_at: 2026-06-24T15:27:46.600Z
must_haves:
  truths:
    - "Maintainer can run bazel test //packages/parity:prusaslicer_arc_fitting_parity_failure_test --test_output=errors."
    - "Mutation coverage rejects every ARCEV-02 drift class through the public Rust-backed command path."
    - "Mutation tests edit only temp copies and leave checked-in expected-arc-summary.tsv unchanged."
  artifacts:
    - path: "packages/parity/compare_prusaslicer_arc_fitting_test.sh"
      provides: "Public arc-fitting mutation guard suite"
      contains: "assert_arc_value_mutation_fails"
    - path: "packages/parity/BUILD.bazel"
      provides: "prusaslicer_arc_fitting_parity_failure_test sh_test target"
      contains: "name = \"prusaslicer_arc_fitting_parity_failure_test\""
  key_links:
    - from: "packages/parity/compare_prusaslicer_arc_fitting_test.sh"
      to: "packages/parity/compare_prusaslicer_arc_fitting.sh"
      via: "public comparator invocation"
      pattern: "compare_prusaslicer_arc_fitting.sh"
    - from: "packages/parity/compare_prusaslicer_arc_fitting_test.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv"
      via: "temp-copy mutation source"
      pattern: "expected-arc-summary.tsv"
---

<objective>
Add fail-closed public mutation guards for ARCEV-02.

Purpose: The public arc-fitting command must fail on fact drift and forbidden deferred-behavior claims without duplicating Rust validation logic in Bash.
Output: `compare_prusaslicer_arc_fitting_test.sh` and Bazel test target `//packages/parity:prusaslicer_arc_fitting_parity_failure_test`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
@.planning/phases/60-executable-arc-fitting-evidence/60-RESEARCH.md
@.planning/milestones/v1.14-phases/56-executable-semantic-g-code-evidence/56-02-SUMMARY.md
@packages/parity/compare_prusaslicer_arc_fitting.sh
@packages/parity/compare_prusaslicer_gcode_output_test.sh
@packages/parity/BUILD.bazel
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv

<interfaces>
The comparator created by Plan 60-01 accepts:
```text
summary_binary rust-arc-input.tsv expected-arc-summary.tsv fixture-provenance.tsv
```

Every mutation test must call `compare_prusaslicer_arc_fitting.sh`; do not re-implement the arc validity decision in Bash. Bash may mutate one TSV field in a temp file and assert stderr names `expected-arc-summary.tsv` plus the mutated field.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add field-specific temp-copy mutation cases</name>
  <files>packages/parity/compare_prusaslicer_arc_fitting_test.sh</files>
  <read_first>
    - NEW FILE: packages/parity/compare_prusaslicer_arc_fitting_test.sh
    - packages/parity/compare_prusaslicer_arc_fitting.sh
    - packages/parity/compare_prusaslicer_gcode_output_test.sh
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <behavior>
    - RED: `rg -n 'arc_command_counts' packages/parity/compare_prusaslicer_arc_fitting_test.sh` fails before the test file exists.
    - GREEN: Each mutation copies `expected-arc-summary.tsv` to a temp path whose basename remains `expected-arc-summary.tsv`, mutates one field, invokes `compare_prusaslicer_arc_fitting.sh`, and asserts nonzero exit.
    - Guard: checked-in `expected-arc-summary.tsv` still contains the exact original rows after the test run.
  </behavior>
  <action>Create `packages/parity/compare_prusaslicer_arc_fitting_test.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Follow the structure of `compare_prusaslicer_gcode_output_test.sh`: compute `workspace_root` from Bazel `TEST_SRCDIR/TEST_WORKSPACE` or repo root, define `comparator`, resolve `summary_binary` from `packages/slic3r-rust/crates/slic3r_flavors/prusa_arc_fitting_summary` or `bazel-bin/packages/slic3r-rust/crates/slic3r_flavors/prusa_arc_fitting_summary`, set `fixture_dir`, `expected_arc_summary`, and `fixture_provenance`, create `tmp_dir`, and clean it with `trap`. Add helpers `fail`, `assert_contains`, `assert_executable`, `assert_file`, `mutate_arc_value(path, arc_field, replacement)`, `mutate_arc_boundary(path, arc_field, replacement)`, `run_comparator(expected_artifact, stdout_file, stderr_file)`, `assert_arc_value_mutation_fails(arc_field, replacement)`, and `assert_arc_boundary_mutation_fails(arc_field, replacement, required_diagnostic)`. Add one value mutation for each ARCEV-02 field with these replacements: `arc_command_counts -> G2:2;G3:1;total_arc_commands:3`, `arc_direction_counts -> clockwise_g2:2;counterclockwise_g3:0`, `center_offset_observations -> i_values:5.000,0.000;j_values:0.000,0.000`, `coordinate_bounds -> x_min:0.000;x_max:12.000;y_min:0.000;y_max:0.000`, `extrusion_observations -> e_values:0.50000,1.50000;e_axis_observed:true`, `feedrate_observations -> F1800:1;F1200:1`, `source_ref -> prusaslicer:version_2.9.5@0000000000000000000000000000000000000000`, `fixture_id -> arc-fitting-unreviewed.gcode`, and `fixture_path -> packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/unreviewed.gcode`. Add one boundary mutation: mutate `arc_command_counts` evidence boundary to `full generated-output parity verified` and assert stderr contains `full generated-output parity`. After all cases, assert the checked-in summary still contains exact original lines for `arc_command_counts`, `fixture_id`, and `fixture_path`. Print `ok: prusaslicer_arc_fitting_parity_failure_test`. This implements D-05, D-06, and D-07.</action>
  <verify>
    <automated>bash -n packages/parity/compare_prusaslicer_arc_fitting_test.sh</automated>
    <automated>shfmt -l -d packages/parity/compare_prusaslicer_arc_fitting_test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'assert_arc_value_mutation_fails' packages/parity/compare_prusaslicer_arc_fitting_test.sh` returns a match.
    - `rg -n 'arc_command_counts|arc_direction_counts|center_offset_observations|coordinate_bounds|extrusion_observations|feedrate_observations|source_ref|fixture_id|fixture_path' packages/parity/compare_prusaslicer_arc_fitting_test.sh` returns matches for all listed fields.
    - `rg -n 'full generated-output parity verified' packages/parity/compare_prusaslicer_arc_fitting_test.sh` returns a match.
    - `rg -n 'expected-arc-summary.tsv' packages/parity/compare_prusaslicer_arc_fitting_test.sh` returns a match.
  </acceptance_criteria>
  <done>The mutation test script exists, covers all ARCEV-02 field drift classes, and invokes the public Rust-backed comparator for every failure assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the public mutation suite into Bazel and run regressions</name>
  <files>packages/parity/BUILD.bazel</files>
  <read_first>
    - packages/parity/BUILD.bazel
    - packages/parity/compare_prusaslicer_arc_fitting_test.sh
    - packages/parity/compare_prusaslicer_arc_fitting.sh
    - packages/parity/compare_prusaslicer_gcode_output_test.sh
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <action>Add `sh_test(name = "prusaslicer_arc_fitting_parity_failure_test", srcs = ["compare_prusaslicer_arc_fitting_test.sh"], data = ["compare_prusaslicer_arc_fitting.sh", "//packages/slic3r-rust/crates/slic3r_flavors:prusa_arc_fitting_summary", "//packages/parity-fixtures:prusa_arc_fitting_expected_arc_summary", "//packages/parity-fixtures:prusa_arc_fitting_provenance"])` to `packages/parity/BUILD.bazel` near the existing Prusa parity failure tests. Run the new test with `--cache_test_results=no` once so all mutation cases execute. Also run `bazel run //packages/parity:prusaslicer_arc_fitting_parity`, `bazel run //packages/parity:prusaslicer_gcode_output_parity`, and `bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test --test_output=errors` to prove D-04/D-12 regression coverage.</action>
  <verify>
    <automated>bazel query //packages/parity:prusaslicer_arc_fitting_parity_failure_test</automated>
    <automated>bazel test --cache_test_results=no //packages/parity:prusaslicer_arc_fitting_parity_failure_test --test_output=errors</automated>
    <automated>bazel run //packages/parity:prusaslicer_arc_fitting_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test --test_output=errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'name = "prusaslicer_arc_fitting_parity_failure_test"' packages/parity/BUILD.bazel` returns a match.
    - `rg -n 'compare_prusaslicer_arc_fitting_test.sh' packages/parity/BUILD.bazel` returns a match.
    - `bazel test --cache_test_results=no //packages/parity:prusaslicer_arc_fitting_parity_failure_test --test_output=errors` exits 0.
    - `rg -n $'arc_command_counts\\tcommand observations\\tG2:1;G3:1;total_arc_commands:2' packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv` returns a match after the test run.
  </acceptance_criteria>
  <done>The Bazel mutation target is public, runs successfully, and regression commands prove the existing Prusa G-code output command/test still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in fixture -> temp mutation | Tests copy source evidence into temp files before mutation. |
| temp mutated TSV -> public comparator | Mutated files cross into the same public Rust-backed validation path maintainers run. |
| stderr diagnostics -> maintainer evidence | Failure messages must identify the changed artifact and field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02-01 | Tampering | mutation tests | mitigate | Mutate only temp copies under `tmp_dir`; assert original checked-in rows remain unchanged after tests. |
| T-60-02-02 | Repudiation | diagnostics | mitigate | Assert stderr contains `expected-arc-summary.tsv` and the exact field name for every drift class. |
| T-60-02-03 | Information Disclosure | test harness | mitigate | Use only local Bazel runfiles and temp files; no secrets, credentials, private data, network/device behavior, upstream source import, runtime discovery, host upload, or printer-runtime exposure. |
</threat_model>

<verification>
Run the task verification commands, then run:

```bash
git diff --check -- packages/parity/compare_prusaslicer_arc_fitting_test.sh packages/parity/BUILD.bazel
```
</verification>

<success_criteria>
ARCEV-02 is satisfied when the public mutation test target fails closed on G2/G3 counts, direction counts, center offsets, coordinate bounds, extrusion observations, feedrate observations, source identity, fixture identity/path, and unsupported deferred-claim text.
</success_criteria>

<output>
After completion, create `.planning/phases/60-executable-arc-fitting-evidence/60-02-SUMMARY.md`.
</output>
