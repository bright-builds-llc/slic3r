---
phase: 60-executable-arc-fitting-evidence
plan: "04"
type: execute
wave: 4
depends_on: ["60-03"]
files_modified:
  - packages/parity/README.md
  - packages/parity-fixtures/README.md
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md
  - packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh
  - packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh
autonomous: true
requirements: [ARCEV-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 60-2026-06-24T15-09-13
generated_at: 2026-06-24T15:27:46.600Z
must_haves:
  truths:
    - "Maintainer can read package docs for the public arc-fitting command and status row."
    - "Fixture docs say Phase 60 published the command/status row and still keep the evidence boundary checked-in-summary-only."
    - "Fixture verifier exact-text checks reject stale future-status wording."
  artifacts:
    - path: "packages/parity/README.md"
      provides: "Parity package docs for prusaslicer_arc_fitting_parity"
      contains: "bazel run //packages/parity:prusaslicer_arc_fitting_parity"
    - path: "packages/parity-fixtures/README.md"
      provides: "Fixture package docs for Phase 60 arc-fitting publication"
      contains: "Phase 60 publishes"
    - path: "packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh"
      provides: "Exact fixture doc checks for published Phase 60 wording"
      contains: "Phase 60 publishes"
  key_links:
    - from: "packages/parity/README.md"
      to: "packages/parity/status.tsv"
      via: "fork.prusaslicer.arc-fitting docs section"
      pattern: "fork\\.prusaslicer\\.arc-fitting"
    - from: "packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh"
      to: "packages/parity-fixtures/README.md"
      via: "require_text exact docs checks"
      pattern: "Phase 60 publishes"
---

<objective>
Publish package-local arc-fitting documentation and align fixture verifier exact-text checks.

Purpose: ARCEV-03 requires package docs to expose the new command/status row without implying byte parity, generated-output parity, runtime, GUI, release, sync, host upload, network/device, or non-Prusa behavior.
Output: Updated `packages/parity` and `packages/parity-fixtures` docs plus fixture verifier/test checks for the published Phase 60 wording.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
@.planning/phases/60-executable-arc-fitting-evidence/60-RESEARCH.md
@.planning/milestones/v1.14-phases/56-executable-semantic-g-code-evidence/56-04-SUMMARY.md
@packages/parity/README.md
@packages/parity-fixtures/README.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md
@packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh
@packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish parity and fixture package docs for arc-fitting</name>
  <files>packages/parity/README.md, packages/parity-fixtures/README.md, packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md</files>
  <read_first>
    - packages/parity/README.md
    - packages/parity-fixtures/README.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md
    - packages/parity/status.tsv
    - packages/parity/compare_prusaslicer_arc_fitting.sh
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <action>Update `packages/parity/README.md` in the Current Surface list with this command text: `` `bazel run //packages/parity:prusaslicer_arc_fitting_parity` runs the shared fixture comparison for the narrow Prusa arc-fitting checked-in summary evidence slice backed by `expected-arc-summary.tsv`. Public proof: checked-in arc summary evidence only.`` Add a Fork Status Rows section for `fork.prusaslicer.arc-fitting` that says exactly these facts: command `bazel run //packages/parity:prusaslicer_arc_fitting_parity`; checked artifact `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv`; Rust boundary `slic3r_flavors::prusa_arc_fitting` and helper `prusa_arc_fitting_summary_lines`; evidence chain `Phase 57 scope contract, Phase 58 fixture corpus, Phase 59 Rust parser/readiness boundary, and Phase 60 public command/status/docs`; `generated-outputs` remains `in progress`; existing semantic Prusa G-code output evidence remains separate under `fork.prusaslicer.gcode-output`. Update `packages/parity-fixtures/README.md` and the fixture-local README to replace future wording with: `Phase 60 publishes bazel run //packages/parity:prusaslicer_arc_fitting_parity and the fork.prusaslicer.arc-fitting status row for checked-in arc summary evidence only.` Keep the existing `arc-fitting-observations.gcode`, `fixture-provenance.tsv`, `expected-arc-summary.tsv`, source ref, source path, and Phase 57/58/59 chain text. Add or keep explicit deferrals for byte-for-byte G-code parity, full generated-output parity, broad generated-output verification, full ArcWelder algorithm equivalence, tolerance or geometry parity, printability, firmware behavior, printer-runtime behavior, GUI behavior, support generation, wall seam behavior, STEP import, full 3MF import/export, binary G-code, thumbnails, post-processing, host upload, network/device behavior, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, sync automation, and non-Prusa fork behavior. This implements D-10 and D-11 for package-local docs.</action>
  <verify>
    <automated>mdformat --check packages/parity/README.md packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md</automated>
    <automated>bazel run //packages/parity:prusaslicer_arc_fitting_parity</automated>
    <automated>bazel run //packages/parity:status</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'bazel run //packages/parity:prusaslicer_arc_fitting_parity' packages/parity/README.md packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md` returns matches in all three files.
    - `rg -n 'fork.prusaslicer.arc-fitting' packages/parity/README.md packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md` returns matches in all three files.
    - `rg -n 'Phase 57 scope contract, Phase 58 fixture corpus, Phase 59 Rust parser/readiness boundary, and Phase 60 public command/status/docs' packages/parity/README.md` returns a match.
    - `rg -n 'existing semantic Prusa G-code output evidence remains separate' packages/parity/README.md` returns a match.
    - `rg -n 'generated-outputs.*in progress|in progress.*generated-outputs' packages/parity/README.md packages/parity-fixtures/README.md` returns matches.
  </acceptance_criteria>
  <done>Package docs expose the arc-fitting command/status row, name the Phase 57-60 chain, preserve the separate G-code output meaning, and retain all deferrals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align fixture verifier exact-text checks with published package docs</name>
  <files>packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh, packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh</files>
  <read_first>
    - packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh
    - packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh
    - packages/parity-fixtures/README.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md
    - packages/parity/status.tsv
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <behavior>
    - RED: The fixture verifier still requires future Phase 60 wording before this task.
    - GREEN: The fixture verifier requires published Phase 60 command/status wording and rejects stale future wording.
  </behavior>
  <action>Update `verify_prusa_arc_fitting_fixture.sh` so `verify_namespace_readme` and `verify_package_readme` require the exact published substring `Phase 60 publishes bazel run //packages/parity:prusaslicer_arc_fitting_parity and the fork.prusaslicer.arc-fitting status row for checked-in arc summary evidence only.` Remove requirements that demand `future fork.prusaslicer.arc-fitting status row` or `Phase 60 owns future` wording. Keep requirements for `Phase 58`, `Phase 59 owns \`slic3r_flavors::prusa_arc_fitting\``, `expected-arc-summary.tsv`, source ref, source path, fixture path, and `generated-outputs` in progress. Update `verify_prusa_arc_fitting_fixture_test.sh` valid README fixtures to the new text. Add one stale-doc mutation case that replaces `Phase 60 publishes bazel run //packages/parity:prusaslicer_arc_fitting_parity and the fork.prusaslicer.arc-fitting status row for checked-in arc summary evidence only.` with `Phase 60 owns future bazel run //packages/parity:prusaslicer_arc_fitting_parity evidence and the future fork.prusaslicer.arc-fitting status row.` and assert the diagnostic mentions `Phase 60 publishes`.</action>
  <verify>
    <automated>bash -n packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh</automated>
    <automated>shfmt -l -d packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_arc_fitting_fixture</automated>
    <automated>bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_arc_fitting_fixture_test --test_output=errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Phase 60 publishes bazel run //packages/parity:prusaslicer_arc_fitting_parity and the fork.prusaslicer.arc-fitting status row for checked-in arc summary evidence only\\.' packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh` returns matches in both files.
    - `rg -n 'future `fork.prusaslicer.arc-fitting` status row|Phase 60 owns future' packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh` returns no matches.
    - `bazel run //packages/parity-fixtures:verify_prusa_arc_fitting_fixture` exits 0.
    - `bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_arc_fitting_fixture_test --test_output=errors` exits 0.
  </acceptance_criteria>
  <done>Fixture verifier exact-text checks enforce the published package docs and reject stale future-status wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package docs -> maintainer interpretation | Docs become public evidence guidance. |
| fixture README -> fixture verifier | Exact verifier checks decide whether package docs are stale or overclaiming. |
| status row -> docs | Package docs must describe the exact checked-in status row without widening it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04-01 | Spoofing | package docs | mitigate | Docs name only the checked-in arc summary evidence slice and exact command/status row. |
| T-60-04-02 | Repudiation | fixture docs | mitigate | Fixture verifier requires exact Phase 60 publication wording and mutation tests reject stale future wording. |
| T-60-04-03 | Information Disclosure | docs | mitigate | Docs state no secrets, credentials, private data, network/device behavior, upstream source import, runtime discovery, host upload, or printer-runtime exposure is introduced. |
</threat_model>

<verification>
Run the task verification commands, then run:

```bash
bazel test //packages/parity:prusaslicer_arc_fitting_parity_failure_test --test_output=errors
git diff --check -- packages/parity/README.md packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh
```
</verification>

<success_criteria>
ARCEV-03 package documentation is satisfied when `packages/parity` and `packages/parity-fixtures` expose the public command/status row, keep existing G-code output separate, preserve deferrals, and the fixture verifier enforces that wording.
</success_criteria>

<output>
After completion, create `.planning/phases/60-executable-arc-fitting-evidence/60-04-SUMMARY.md`.
</output>
