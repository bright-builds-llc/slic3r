---
phase: 59-rust-arc-fitting-evidence-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs
  - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
autonomous: true
requirements:
  - ARCRUST-01
  - ARCRUST-03
user_setup: []
must_haves:
  truths:
    - "Developer can parse caller-supplied Phase 58 arc summary TSV text into typed Rust rows and facts."
    - "Developer can call a pure `prusa_arc_fitting_summary_lines` helper over caller-supplied text without creating a binary, public parity target, status row, docs publication, or runtime discovery."
    - "Cargo and Bazel tests accept the checked-in arc summary and reject malformed or overclaiming arc summaries."
    - "The parser module introduces no filesystem discovery, Git, network, process, environment, clock, source import, live generation, printer-runtime behavior, credentials, or status/docs mutation."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs"
      provides: "Pure typed arc summary parser, row model, facts projection, parse errors, and summary-line helper"
      contains: "parse_prusa_arc_fitting_summary"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs"
      provides: "Public crate re-exports for developer-facing arc parser types and helper"
      contains: "PrusaArcFittingSummary"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs"
      provides: "Cargo and Bazel valid/fail-closed parser coverage"
      contains: "EXPECTED_ARC_SUMMARY"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel"
      provides: "Bazel source/test wiring and compile_data for the Phase 58 arc TSV"
      contains: "prusa_arc_fitting_test"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv"
      via: "include_str! plus Bazel compile_data"
      pattern: "expected-arc-summary.tsv"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs"
      to: "packages/prusa-arc-fitting-scope/arc-fitting-scope.md"
      via: "closed Phase 57 arc field order"
      pattern: "source_ref|inventory_source_paths|source_anchor|fixture_id|fixture_path|arc_command_counts|arc_direction_counts|center_offset_observations|coordinate_bounds|extrusion_observations|feedrate_observations|evidence_boundary"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel"
      to: "//packages/parity-fixtures:prusa_arc_fitting_expected_arc_summary"
      via: "compile_data on prusa_arc_fitting_test"
      pattern: "prusa_arc_fitting_expected_arc_summary"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 59-2026-06-24T13-36-08
generated_at: "2026-06-24T13:53:28Z"
---

<objective>
Add the pure typed Rust parser boundary for the Phase 58 Prusa arc-fitting expected summary.

Purpose: Complete the parser and rejection-coverage portion of ARCRUST-01 and ARCRUST-03 while keeping Phase 60 publication surfaces absent.
Output: New `prusa_arc_fitting` module, developer-facing summary-line helper, public crate re-exports, Cargo tests, and Bazel test wiring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-rust-arc-fitting-evidence-boundary/59-CONTEXT.md
@.planning/phases/59-rust-arc-fitting-evidence-boundary/59-RESEARCH.md
@.planning/phases/57-arc-fitting-scope-contract/57-CONTEXT.md
@.planning/phases/57-arc-fitting-scope-contract/57-VERIFICATION.md
@.planning/phases/58-arc-fitting-fixture-corpus/58-CONTEXT.md
@.planning/phases/58-arc-fitting-fixture-corpus/58-VERIFICATION.md
@.planning/phases/58-arc-fitting-fixture-corpus/58-01-SUMMARY.md
@.planning/phases/58-arc-fitting-fixture-corpus/58-02-SUMMARY.md
@.planning/milestones/v1.14-phases/55-rust-semantic-g-code-summary-boundary/55-01-SUMMARY.md
@packages/prusa-arc-fitting-scope/arc-fitting-scope.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/fixture-provenance.tsv
@packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel

<interfaces>
Existing parser pattern to mirror from `prusa_gcode_output.rs`:
```rust
pub fn parse_prusa_gcode_output_semantic_summary(
    input: &str,
) -> PrusaGcodeOutputSemanticParseResult;

pub fn prusa_gcode_output_semantic_summary_lines(
    input: &str,
) -> Result<Vec<String>, PrusaGcodeOutputSemanticParseError>;
```

New developer-facing Rust surface to create per D-01, D-02, and research recommendation:
```rust
pub fn parse_prusa_arc_fitting_summary(input: &str) -> PrusaArcFittingParseResult;
pub fn prusa_arc_fitting_summary_lines(
    input: &str,
) -> Result<Vec<String>, PrusaArcFittingParseError>;
```

Required TSV header per D-04:
```text
source_ref	fixture_path	arc_field	arc_category	arc_value	evidence_boundary
```

Required arc field order per D-05:
```text
source_ref
inventory_source_paths
source_anchor
fixture_id
fixture_path
arc_command_counts
arc_direction_counts
center_offset_observations
coordinate_bounds
extrusion_observations
feedrate_observations
evidence_boundary
```

Accepted source and fixture values:
```text
source_ref = prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
source_path = src/libslic3r/Geometry/ArcWelder.cpp
fixture_id = arc-fitting-observations.gcode
fixture_path = packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/arc-fitting-observations.gcode
expected_summary_path = packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv
```
</interfaces>
</context>

<decision_coverage>
D-01 full: create `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs` and re-export it from `src/lib.rs`.
D-02 full: parser entrypoints accept `&str` and return typed values or typed errors; no filesystem discovery, Git, network, process, env, clock, source import, generator, printer-runtime, or status/docs mutation is allowed.
D-03 full: add only a crate Rust helper (`prusa_arc_fitting_summary_lines`), no Phase 60 public parity target, status row, binary, or public docs.
D-04 full: parse the exact six-column arc TSV header.
D-05 full: model the twelve approved arc fields as a closed ordered enum.
D-06 full: expose typed summary row and facts models for source ref, inventory/source paths, source anchors, fixture identity/path, G2/G3 counts, direction counts, center offsets, bounds, extrusion, feedrate, and evidence-boundary token.
D-07 full: reject invalid header, wrong columns, missing/duplicate/out-of-order rows, unsupported fields, unexpected source/fixture/value, and unsupported claim text.
D-08 full: enforce exact evidence-boundary text and forbidden deferred-claim rejection through value-boundary validation.
D-12 full: add focused Rust tests for valid parsing, summary-line extraction, fail-closed rows, and no-overclaiming public declarations.
D-13 plan-set coverage: crate-local Bazel target is wired here; aggregate `//packages/slic3r-rust:verify` wiring is Plan 59-02.
D-14 full: developer-facing Rust crate docs/comments are allowed only inside source/tests; public port docs, `packages/parity/status.tsv`, and parity command docs remain untouched.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add arc parser types, valid fixture coverage, summary lines, and Bazel data</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs, packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs, packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs` (confirm absent before creating)
    - `packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs` (confirm absent before creating)
    - `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv`
    - `packages/prusa-arc-fitting-scope/arc-fitting-scope.md`
  </read_first>
  <behavior>
    - Test 1: `parse_prusa_arc_fitting_summary(EXPECTED_ARC_SUMMARY)` returns twelve rows in the Phase 57 field order.
    - Test 2: returned facts expose exact source ref, inventory paths, source anchors, fixture identity/path, arc counts, direction counts, center offsets, coordinate bounds, extrusion observations, feedrate observations, and evidence-boundary token.
    - Test 3: `prusa_arc_fitting_summary_lines(EXPECTED_ARC_SUMMARY)` returns stable developer-facing fact lines without public executable parity wording.
    - Test 4: Bazel `prusa_arc_fitting_test` can include the Phase 58 TSV through `compile_data`.
  </behavior>
  <action>Start with tests, then implement. Create `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs` with `const EXPECTED_ARC_SUMMARY: &str = include_str!("../../../../parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv");` and `const PRUSA_ARC_FITTING_SOURCE: &str = include_str!("../src/prusa_arc_fitting.rs");`. Add Arrange/Act/Assert tests named `parses_expected_arc_summary_rows_and_facts`, `summarizes_expected_arc_summary_lines`, and `public_arc_fitting_declarations_do_not_claim_deferred_behavior`.

Create `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs` per D-01 through D-08. Define `PRUSA_ARC_FITTING_INVENTORY_ID`, `PRUSA_ARC_FITTING_SOURCE_REF`, and `PRUSA_ARC_FITTING_SOURCE_PATH` as `pub(crate) const` so Plan 59-02 can reuse them from `registry.rs`. Use constants with these exact values: `PRUSA_ARC_FITTING_INVENTORY_ID = "prusaslicer.arc-fitting"`, `PRUSA_ARC_FITTING_VENDOR_ID = "prusaslicer"`, `PRUSA_ARC_FITTING_SOURCE_REF = VendorSourceRef::prusa_slicer_version_2_9_5()`, `PRUSA_ARC_FITTING_SOURCE_PATH = "src/libslic3r/Geometry/ArcWelder.cpp"`, `PRUSA_ARC_FITTING_FIXTURE_CORPUS_PATH = "packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting"`, `PRUSA_ARC_FITTING_FIXTURE_PATH = "packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/arc-fitting-observations.gcode"`, `PRUSA_ARC_FITTING_EXPECTED_SUMMARY_PATH = "packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv"`, `PRUSA_ARC_FITTING_SCOPE_RECORD_PATH = "packages/prusa-arc-fitting-scope/arc-fitting-scope.md"`, and `PRUSA_ARC_FITTING_RESERVED_STATUS_TOKEN = "fork.prusaslicer.arc-fitting"`.

Implement the closed parser with `ARC_EXPECTED_HEADER = "source_ref\tfixture_path\tarc_field\tarc_category\tarc_value\tevidence_boundary"` and `ARC_EXPECTED_COLUMN_COUNT = 6`. Add public types `PrusaArcFittingSummary`, `PrusaArcFittingSummaryRow`, `PrusaArcFittingFacts`, `PrusaArcFittingField`, `PrusaArcFittingCategory`, `PrusaArcFittingValue`, `PrusaArcFittingEvidenceBoundary`, `PrusaArcFittingParseError`, and `PrusaArcFittingParseResult`. Field enum variants must be exactly `SourceRef`, `InventorySourcePaths`, `SourceAnchor`, `FixtureId`, `FixturePath`, `ArcCommandCounts`, `ArcDirectionCounts`, `CenterOffsetObservations`, `CoordinateBounds`, `ExtrusionObservations`, `FeedrateObservations`, and `EvidenceBoundary`. Category enum variants must cover exactly `SourceIdentity`, `FixtureIdentity`, `CommandObservations`, `CenterOffsetObservations`, `CoordinateBounds`, `ExtrusionObservations`, `FeedrateObservations`, and `BoundaryText`.

Implement `parse_prusa_arc_fitting_summary(input: &str) -> PrusaArcFittingParseResult` with exact header validation, exact column count, non-empty required values, exact source ref, exact fixture path, typed field/category parsing, expected category validation, exact row value validation, exact evidence-boundary validation, duplicate-row rejection, extra-row rejection, row-order validation, missing-row validation, and facts projection. Add `PrusaArcFittingSummary::rows()` and `PrusaArcFittingSummary::facts()`.

Implement `prusa_arc_fitting_summary_lines(input: &str) -> Result<Vec<String>, PrusaArcFittingParseError>` as a pure helper over `parse_prusa_arc_fitting_summary`. It must return lines for `arc_summary_path`, `arc_row_count`, `source_ref`, `inventory_source_paths`, `source_anchor`, `fixture_id`, `fixture_path`, `arc_command_counts`, `arc_direction_counts`, `center_offset_observations`, `coordinate_bounds`, `extrusion_observations`, `feedrate_observations`, and `evidence_boundary`. Do not add a Rust binary, public Bazel parity target, status row, public docs, filesystem reads, process calls, network calls, environment reads, clock reads, live generation, source imports, printer-runtime behavior, credentials, or status/docs mutation.

Update `src/lib.rs` with `pub mod prusa_arc_fitting;` and re-export all new parser/facts/error/helper symbols needed by the test. Update `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` so the `slic3r_flavors` `rust_library` `srcs` includes `src/prusa_arc_fitting.rs`. Add `rust_test(name = "prusa_arc_fitting_test", srcs = ["tests/prusa_arc_fitting.rs"], compile_data = ["src/prusa_arc_fitting.rs", "//packages/parity-fixtures:prusa_arc_fitting_expected_arc_summary"], deps = [":slic3r_flavors", "//packages/slic3r-rust/crates/slic3r_contracts:slic3r_contracts"], edition = "2024")`, and include `:prusa_arc_fitting_test` in both `rust_clippy` deps and `rustfmt_test` targets.</action>
  <acceptance_criteria>
    - `rg -n "pub mod prusa_arc_fitting|PrusaArcFittingSummary|parse_prusa_arc_fitting_summary|prusa_arc_fitting_summary_lines" packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs` finds all symbols.
    - `rg -n "ARC_EXPECTED_HEADER|EXPECTED_ARC_ROWS|PrusaArcFittingField|PrusaArcFittingFacts|PrusaArcFittingParseError" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs` finds all parser contract symbols.
    - `rg -n "EXPECTED_ARC_SUMMARY|parses_expected_arc_summary_rows_and_facts|summarizes_expected_arc_summary_lines|public_arc_fitting_declarations_do_not_claim_deferred_behavior" packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs` finds valid fixture and helper coverage.
    - `rg -n "prusa_arc_fitting_test|prusa_arc_fitting_expected_arc_summary|src/prusa_arc_fitting.rs" packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` finds all Bazel wiring.
    - `! rg -n "std::(fs|process|env|time)|Command::|SystemTime|UNIX_EPOCH|current_dir|read_to_string|spawn\\(|TcpStream|reqwest|ureq" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_arc_fitting</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_arc_fitting_test --test_output=errors</automated>
  </verify>
  <done>The new Rust module parses the checked-in arc summary into typed rows/facts, exposes a pure summary-line helper, re-exports the developer-facing API, and has Cargo/Bazel valid-fixture coverage without adding public executable parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fail-closed parser rejection coverage</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv`
    - `packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs`
  </read_first>
  <behavior>
    - Test 1: invalid header returns `PrusaArcFittingParseError::InvalidHeader`.
    - Test 2: wrong column count returns `WrongColumnCount { expected: 6, actual: 5 }`.
    - Test 3: missing row returns `MissingRow`.
    - Test 4: duplicate row returns `DuplicateRow`.
    - Test 5: out-of-order rows return `UnexpectedRowOrder`.
    - Test 6: unsupported arc field returns `UnsupportedArcField`.
    - Test 7: unsupported broad claim text returns `UnexpectedEvidenceBoundary`.
    - Test 8: wrong source ref returns `UnexpectedSourceRef`.
    - Test 9: wrong fixture path returns `UnexpectedFixturePath`.
    - Test 10: wrong fixture identity or arc value returns `UnexpectedArcValue`.
  </behavior>
  <action>Add focused rejection tests to `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs`, then fill any missing parse-error branches in `src/prusa_arc_fitting.rs`. Use one concern per test and Arrange/Act/Assert comments. Add helpers `expected_arc_fields() -> [PrusaArcFittingField; 12]` and `replace_arc_cell(data_row_index: usize, column_index: usize, value: &str) -> String`.

Tests must cover these concrete mutation names: `rejects_arc_invalid_header`, `rejects_arc_wrong_column_count`, `rejects_arc_missing_row`, `rejects_arc_duplicate_row`, `rejects_arc_out_of_order_rows`, `rejects_unsupported_arc_field`, `rejects_arc_unsupported_claim_text`, `rejects_arc_wrong_source_ref`, `rejects_arc_wrong_fixture_path`, and `rejects_arc_wrong_value`. For unsupported claim text, mutate the evidence-boundary column to `This proves byte-for-byte G-code parity and printer-runtime behavior.` and assert `UnexpectedEvidenceBoundary`. For wrong values, mutate the `fixture_id` row value to `other.gcode` and assert `UnexpectedArcValue { arc_field: PrusaArcFittingField::FixtureId, .. }`.

Parser error variants must name the failing line number and failing field where practical: `WrongColumnCount { line_number, expected, actual }`, `EmptyRequiredValue { line_number, column }`, `UnexpectedSourceRef { line_number, value }`, `UnexpectedFixturePath { line_number, value }`, `UnsupportedArcField { line_number, value }`, `UnsupportedArcCategory { line_number, value }`, `UnexpectedArcCategory { line_number, arc_field, value }`, `UnexpectedArcValue { line_number, arc_field, value }`, `UnexpectedEvidenceBoundary { line_number, arc_field, value }`, `DuplicateRow { line_number, arc_field }`, `UnexpectedRowOrder { line_number, expected_arc_field, actual_arc_field }`, `MissingRow { arc_field }`, and `ExtraRow { line_number, arc_field }`.

Keep D-02 and the security boundary intact: do not add imports or calls for `std::fs`, `std::process`, `std::env`, `std::time`, current directory discovery, path discovery, Git, network clients, source imports, generation, printer-runtime behavior, credentials, or status/docs mutation.</action>
  <acceptance_criteria>
    - `rg -n "rejects_arc_invalid_header|rejects_arc_wrong_column_count|rejects_arc_missing_row|rejects_arc_duplicate_row|rejects_arc_out_of_order_rows|rejects_unsupported_arc_field|rejects_arc_unsupported_claim_text|rejects_arc_wrong_source_ref|rejects_arc_wrong_fixture_path|rejects_arc_wrong_value" packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs` finds all ten tests.
    - `rg -n "UnexpectedEvidenceBoundary|UnexpectedArcValue|UnexpectedRowOrder|MissingRow|DuplicateRow|UnsupportedArcField" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs` finds fail-closed error variants and uses.
    - `! rg -n "std::(fs|process|env|time)|Command::|SystemTime|UNIX_EPOCH|current_dir|read_to_string|spawn\\(|TcpStream|reqwest|ureq" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_arc_fitting</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_arc_fitting_test --test_output=errors</automated>
  </verify>
  <done>Malformed arc summaries fail closed for header, column, row, field, source, fixture, value, and overclaim drift while the module remains pure data-in/data-out Rust.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-supplied TSV string -> Rust parser | Untrusted text crosses into typed Rust domain values. |
| checked-in evidence values -> facts model | Reviewed fixture facts become developer-facing facts only after exact validation. |
| crate helper -> downstream Phase 60 consumer | Summary lines may later be consumed by a public command, so helper names/output must not overclaim executable parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01-01 | Spoofing | `parse_prusa_arc_fitting_summary` | mitigate | Reject any `source_ref` other than `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961` and any fixture path other than the Phase 58 checked-in fixture path. |
| T-59-01-02 | Tampering | row parser and facts projection | mitigate | Enforce exact header, column count, non-empty values, closed enum fields/categories, exact row order, duplicate rejection, missing-row rejection, and exact expected values before constructing `PrusaArcFittingFacts`. |
| T-59-01-03 | Repudiation | summary-line helper and public declarations | mitigate | Add tests scanning helper/public declaration names and summary lines for deferred-behavior claim words; keep output as checked-in summary facts only. |
| T-59-01-04 | Elevation of Privilege | `src/prusa_arc_fitting.rs` | mitigate | The source must not import or call filesystem discovery, Git, network, process spawning, environment, clock, source import, live generation, printer-runtime behavior, credentials, or status/docs mutation APIs. |
| T-59-01-05 | Information Disclosure | parser module | accept | No secrets, credentials, private data, network/device access, or runtime discovery are introduced; the module consumes caller-supplied text only. |
</threat_model>

<verification>
Plan-level checks after both tasks:

```bash
cargo +1.94.1 fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check
cargo +1.94.1 clippy --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-targets --all-features -- -D warnings
cargo +1.94.1 build --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-targets --all-features
cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-features
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_arc_fitting_test --test_output=errors
bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check
bazel build //packages/slic3r-rust/crates/slic3r_flavors:clippy
git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/parity/BUILD.bazel)"
```
</verification>

<success_criteria>
ARCRUST-01 is materially complete for parser behavior: the arc summary parser is pure, caller-supplied, typed, exact, and side-effect-free.
ARCRUST-03 parser coverage is established here: focused Cargo/Bazel parser tests prove valid rows parse, invalid rows fail closed, and public helper names avoid deferred behavior claims. Plan 59-02 completes metadata, registry, and aggregate verification coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/59-rust-arc-fitting-evidence-boundary/59-01-SUMMARY.md` with `requirements-completed: [ARCRUST-01, ARCRUST-03]`.
</output>
