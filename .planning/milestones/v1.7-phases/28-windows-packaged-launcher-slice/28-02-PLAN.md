---
phase: 28-windows-packaged-launcher-slice
plan: "02"
type: execute
wave: 2
depends_on:
  - "28-01"
files_modified:
  - packages/slic3r-rust/crates/slic3r_cli/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_cli/tests/version.rs
  - packages/parity-fixtures/cli-help/stdout.txt
autonomous: true
requirements:
  - WPKG-03
must_haves:
  truths:
    - "Packaged `--help` no longer implies Windows launcher support stops at the runtime target."
    - "The Rust help output names the Windows packaged launcher tree without claiming installer or release support."
    - "Help parity fixtures and Rust tests match the changed help text exactly."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_cli/src/lib.rs"
      provides: "Rust CLI help output"
      contains: "Windows packaged launcher tree"
    - path: "packages/slic3r-rust/crates/slic3r_cli/tests/version.rs"
      provides: "Unit coverage for help scope text"
      contains: "Windows packaged launcher tree"
    - path: "packages/parity-fixtures/cli-help/stdout.txt"
      provides: "Shared help parity fixture"
      contains: "Windows packaged launcher tree"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_cli/src/lib.rs"
      to: "packages/parity-fixtures/cli-help/stdout.txt"
      via: "exact help fixture alignment"
      pattern: "Windows packaged launcher tree"
    - from: "packages/slic3r-rust/crates/slic3r_cli/tests/version.rs"
      to: "packages/slic3r-rust/crates/slic3r_cli/src/lib.rs"
      via: "help text assertions"
      pattern: "response\\.stdout\\.contains\\(\"Windows packaged launcher tree\"\\)"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-05-23T02-35-56
generated_at: 2026-05-23T02:47:07Z
---

<objective>
Align the Rust CLI help text, help fixture, and CLI unit test with the scoped
Windows packaged launcher tree added in Plan 28-01.

Purpose: Support WPKG-03 by making packaged `--help` accurate without expanding
CLI behavior or implying MSI, signing, release-channel, GUI, or native release
output support.

Output: Updated help string, updated help test assertions, and updated shared
help parity fixture.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-windows-packaged-launcher-slice/28-CONTEXT.md
@.planning/phases/28-windows-packaged-launcher-slice/28-RESEARCH.md
@.planning/phases/28-windows-packaged-launcher-slice/28-01-PLAN.md
@packages/slic3r-rust/crates/slic3r_cli/src/lib.rs
@packages/slic3r-rust/crates/slic3r_cli/tests/version.rs
@packages/parity-fixtures/cli-help/stdout.txt
@packages/parity/BUILD.bazel

<interfaces>
Existing help stanza before this plan:
```text
Rust-backed launcher paths in this milestone:
  macOS packaged launcher, Linux runtime, Linux packaged launcher tree,
  and Windows runtime hand off to this Rust CLI slice
```

Required target wording after this plan:
```text
Rust-backed launcher paths in this milestone:
  macOS packaged launcher, Linux runtime, Linux packaged launcher tree,
  Windows runtime, and Windows packaged launcher tree hand off to this Rust CLI slice
```
</interfaces>
</context>

<tasks>
<task type="auto" id="1">
  <name>Task 1: Update Rust help scope text for Windows packaged launcher tree</name>
  <read_first>
    <file>packages/slic3r-rust/crates/slic3r_cli/src/lib.rs</file>
    <file>packages/slic3r-rust/crates/slic3r_cli/tests/version.rs</file>
    <file>packages/parity-fixtures/cli-help/stdout.txt</file>
    <file>.planning/phases/28-windows-packaged-launcher-slice/28-CONTEXT.md</file>
    <file>.planning/phases/28-windows-packaged-launcher-slice/28-01-PLAN.md</file>
  </read_first>
  <files>
    <file>packages/slic3r-rust/crates/slic3r_cli/src/lib.rs</file>
  </files>
  <action>
    In `packages/slic3r-rust/crates/slic3r_cli/src/lib.rs`, update the
    `Rust-backed launcher paths in this milestone:` help stanza so it contains
    the exact phrase `Windows packaged launcher tree`. Keep the supported
    behavior scoped to the existing Rust CLI slice and keep the deferred
    wording for `merge/cut/layout`, `multi-input transforms`, `release-grade
    packaging`, `GUI packaging`, and `geometry/content parity`. Do not add new
    command handling, new flags, or new CLI behavior.
  </action>
  <verify>
    <automated>rg -n "Windows packaged launcher tree" packages/slic3r-rust/crates/slic3r_cli/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`packages/slic3r-rust/crates/slic3r_cli/src/lib.rs` contains `Windows packaged launcher tree`.</criterion>
    <criterion>`packages/slic3r-rust/crates/slic3r_cli/src/lib.rs` still contains `release-grade packaging`.</criterion>
    <criterion>`packages/slic3r-rust/crates/slic3r_cli/src/lib.rs` does not add a new parser branch for Windows packaging.</criterion>
  </acceptance_criteria>
  <done>The help output advertises the scoped Windows packaged launcher tree without expanding CLI behavior.</done>
</task>

<task type="auto" id="2">
  <name>Task 2: Align help test and parity fixture</name>
  <read_first>
    <file>packages/slic3r-rust/crates/slic3r_cli/tests/version.rs</file>
    <file>packages/parity-fixtures/cli-help/stdout.txt</file>
    <file>packages/slic3r-rust/crates/slic3r_cli/src/lib.rs</file>
    <file>packages/parity/BUILD.bazel</file>
    <file>packages/parity/compare_windows_runtime.sh</file>
  </read_first>
  <files>
    <file>packages/slic3r-rust/crates/slic3r_cli/tests/version.rs</file>
    <file>packages/parity-fixtures/cli-help/stdout.txt</file>
  </files>
  <action>
    Update `packages/slic3r-rust/crates/slic3r_cli/tests/version.rs` so the
    help test asserts `Windows packaged launcher tree`, keeps asserting
    `release-grade packaging`, and keeps rejecting stale
    `packaged launcher behavior` wording. Update
    `packages/parity-fixtures/cli-help/stdout.txt` to match the new help output
    exactly, including the `Windows packaged launcher tree` phrase. Do not
    change unrelated fixture sections.
  </action>
  <verify>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_cli:slic3r_cli_test</automated>
    <automated>bazel run //packages/parity:cli_help_parity</automated>
    <automated>bazel run //packages/parity:windows_runtime_parity</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`packages/slic3r-rust/crates/slic3r_cli/tests/version.rs` contains `Windows packaged launcher tree`.</criterion>
    <criterion>`packages/parity-fixtures/cli-help/stdout.txt` contains `Windows packaged launcher tree`.</criterion>
    <criterion>`bazel run //packages/parity:cli_help_parity` exits 0.</criterion>
    <criterion>`bazel run //packages/parity:windows_runtime_parity` exits 0.</criterion>
  </acceptance_criteria>
  <done>Rust help tests and shared parity fixtures match the updated help output.</done>
</task>

<task type="auto" id="3">
  <name>Task 3: Run Rust verification for help-surface changes</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>AGENTS.bright-builds.md</file>
    <file>packages/slic3r-rust/Cargo.toml</file>
    <file>packages/slic3r-rust/crates/slic3r_cli/BUILD.bazel</file>
    <file>packages/slic3r-rust/crates/slic3r_cli/src/lib.rs</file>
  </read_first>
  <files>
    <file>packages/slic3r-rust/crates/slic3r_cli/src/lib.rs</file>
    <file>packages/slic3r-rust/crates/slic3r_cli/tests/version.rs</file>
  </files>
  <action>
    Because this plan edits Rust source/test files, run the repo-required Rust
    verification with the pinned toolchain against `packages/slic3r-rust`.
    Use manifest-path commands from the repository root:
    `cargo +1.94.1 fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check`,
    `cargo +1.94.1 clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings`,
    `cargo +1.94.1 build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features`, and
    `cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml --all-features`.
    Fix any failure introduced by the help/test changes before completing this
    plan.
  </action>
  <verify>
    <automated>cargo +1.94.1 fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check</automated>
    <automated>cargo +1.94.1 clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings</automated>
    <automated>cargo +1.94.1 build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features</automated>
    <automated>cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml --all-features</automated>
  </verify>
  <acceptance_criteria>
    <criterion>All four cargo commands listed in this task exit 0.</criterion>
    <criterion>`git diff --check` exits 0 after the Rust and fixture edits.</criterion>
  </acceptance_criteria>
  <done>Rust source, Rust tests, and help fixture changes pass repo-required verification.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Maintainer/user -> CLI help output | User-facing text communicates supported and deferred launcher/package scope. |
| Rust help output -> parity fixture | Exact help text is copied into a checked-in fixture consumed by parity commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-28-02-01 | Repudiation | Rust help scope text | mitigate | Include `Windows packaged launcher tree` while retaining `release-grade packaging` and other deferred-scope wording. |
| T-28-02-02 | Tampering | Help parity fixture | mitigate | Update `packages/parity-fixtures/cli-help/stdout.txt` with exact output and verify through `//packages/parity:cli_help_parity`. |
| T-28-02-03 | Information Disclosure | Help text | accept | Help text exposes only public supported/deferred scope and no secrets, local paths, or machine-specific details. |
| T-28-02-04 | Denial of Service | Rust verification commands | accept | Local build/test commands may be slow but introduce no runtime service or persistent process. |
</threat_model>

<verification>
- `rg -n "Windows packaged launcher tree" packages/slic3r-rust/crates/slic3r_cli/src/lib.rs packages/slic3r-rust/crates/slic3r_cli/tests/version.rs packages/parity-fixtures/cli-help/stdout.txt`
- `bazel test //packages/slic3r-rust/crates/slic3r_cli:slic3r_cli_test`
- `bazel run //packages/parity:cli_help_parity`
- `bazel run //packages/parity:windows_runtime_parity`
- `cargo +1.94.1 fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check`
- `cargo +1.94.1 clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings`
- `cargo +1.94.1 build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features`
- `cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml --all-features`
- `git diff --check`
</verification>

<success_criteria>
- Packaged `--help` names the scoped Windows packaged launcher tree.
- Help output still refuses release-grade packaging, GUI packaging, geometry/content parity, and unsupported CLI expansion.
- Help tests and parity fixtures pass.
- WPKG-03 help-surface accuracy is fully covered.
</success_criteria>

<output>
After completion, create `.planning/phases/28-windows-packaged-launcher-slice/28-02-SUMMARY.md` with `requirements-completed` including `WPKG-03` only if all acceptance criteria pass.
</output>
