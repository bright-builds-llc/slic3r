---
phase: 30-packaging-visibility-and-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/parity/status.tsv
  - packages/parity/README.md
  - packages/parity-fixtures/README.md
  - packages/parity-fixtures/linux-packaged-launcher/README.md
  - packages/parity-fixtures/windows-packaged-launcher/README.md
autonomous: true
requirements:
  - PVIS-01
  - PVIS-02
user_setup: []
must_haves:
  truths:
    - "Maintainer can run bazel run //packages/parity:status and see exactly one linux.packaged-launcher row and exactly one windows.packaged-launcher row."
    - "Each packaged launcher status row is verified and points to the exact Phase 29 evidence label for its platform."
    - "Parity and fixture package docs expose the Linux and Windows packaged launcher evidence without saying status publication remains deferred."
    - "Fixture docs keep Linux and Windows support scoped to package-shaped launcher trees and continue excluding release-grade package formats."
  artifacts:
    - path: "packages/parity/status.tsv"
      provides: "Checked-in status data for Linux and Windows packaged launcher surfaces"
      contains: "linux.packaged-launcher"
    - path: "packages/parity/README.md"
      provides: "Parity package discoverability for status rows and evidence commands"
      contains: "//packages/parity:linux_packaged_launcher_parity"
    - path: "packages/parity-fixtures/README.md"
      provides: "Fixture package discoverability for Phase 29 packaged launcher bundles"
      contains: "windows-packaged-launcher"
    - path: "packages/parity-fixtures/linux-packaged-launcher/README.md"
      provides: "Linux packaged launcher fixture boundary and evidence command"
      contains: "//packages/parity:linux_packaged_launcher_parity"
    - path: "packages/parity-fixtures/windows-packaged-launcher/README.md"
      provides: "Windows packaged launcher fixture boundary and evidence command"
      contains: "//packages/parity:windows_packaged_launcher_parity"
  key_links:
    - from: "packages/parity/status.tsv"
      to: "//packages/parity:linux_packaged_launcher_parity"
      via: "linux.packaged-launcher evidence column"
      pattern: "^linux\\.packaged-launcher\\tverified\\t//packages/parity:linux_packaged_launcher_parity\\t"
    - from: "packages/parity/status.tsv"
      to: "//packages/parity:windows_packaged_launcher_parity"
      via: "windows.packaged-launcher evidence column"
      pattern: "^windows\\.packaged-launcher\\tverified\\t//packages/parity:windows_packaged_launcher_parity\\t"
    - from: "packages/parity/README.md"
      to: "packages/parity/status.tsv"
      via: "status publication wording"
      pattern: "linux\\.packaged-launcher|windows\\.packaged-launcher"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 30-2026-05-23T13-19-23
generated_at: 2026-05-23T13:52:01Z
---

<objective>
Publish Linux and Windows packaged launcher status rows and make the matching parity fixture package docs discoverable.

Purpose: Close PVIS-01 and prepare PVIS-02 docs publication with a checked-in status source that names the exact Phase 29 evidence labels.
Output: Updated parity status TSV plus parity and fixture README surfaces.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-packaging-visibility-and-docs/30-CONTEXT.md
@.planning/phases/30-packaging-visibility-and-docs/30-RESEARCH.md
@.planning/phases/29-cross-platform-packaging-evidence/29-01-SUMMARY.md
@packages/parity/status.tsv
@packages/parity/parity_status.sh
@packages/parity/BUILD.bazel
@packages/parity/README.md
@packages/parity-fixtures/README.md
@packages/parity-fixtures/linux-packaged-launcher/README.md
@packages/parity-fixtures/windows-packaged-launcher/README.md

Bright Builds context used for this plan: local AGENTS guidance, AGENTS.bright-builds.md, standards-overrides.md, and repo codebase TESTING/CONVENTIONS guidance. The canonical standards files referenced by AGENTS.md were not present under standards/ in this checkout. Local repo guidance forbids running mdformat on phase *-SUMMARY.md files and requires the exact requirements-completed summary key.

<interfaces>
Status TSV schema from packages/parity/status.tsv:
```text
surface<TAB>status<TAB>evidence<TAB>notes
```

Authoritative Phase 29 Bazel parity labels from packages/parity/BUILD.bazel:
```text
//packages/parity:linux_packaged_launcher_parity
//packages/parity:windows_packaged_launcher_parity
```

Locked decisions implemented here:
```text
D-01, D-02, D-03, D-04: status rows and checked-in dashboard behavior.
D-05, D-06, D-07, D-08: package/fixture doc publication scope for these files only.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exact Linux and Windows status rows</name>
  <files>packages/parity/status.tsv</files>
  <action>Add exactly two checked-in status rows per D-01 and D-02, without changing packages/parity/parity_status.sh per D-03. Use tab separators and these exact surface/status/evidence triples: linux.packaged-launcher -> verified -> //packages/parity:linux_packaged_launcher_parity; windows.packaged-launcher -> verified -> //packages/parity:windows_packaged_launcher_parity. Preserve the existing launcher-packaging row as a macOS-scoped claim per D-04; do not replace it with a broad all-platform packaging row.</action>
  <verify>
    <automated>awk -F '\t' '$1 == "linux.packaged-launcher" { linux++; if ($2 != "verified" || $3 != "//packages/parity:linux_packaged_launcher_parity") bad++ } $1 == "windows.packaged-launcher" { windows++; if ($2 != "verified" || $3 != "//packages/parity:windows_packaged_launcher_parity") bad++ } END { exit(linux == 1 && windows == 1 && bad == 0 ? 0 : 1) }' packages/parity/status.tsv</automated>
    <automated>bazel run //packages/parity:status</automated>
  </verify>
  <done>packages/parity/status.tsv contains exactly one verified Linux packaged launcher row and exactly one verified Windows packaged launcher row, both pointing at the Phase 29 evidence labels.</done>
</task>

<task type="auto">
  <name>Task 2: Publish parity and fixture package documentation</name>
  <files>packages/parity/README.md, packages/parity-fixtures/README.md, packages/parity-fixtures/linux-packaged-launcher/README.md, packages/parity-fixtures/windows-packaged-launcher/README.md</files>
  <action>Update package-local docs per D-05 through D-08. In packages/parity/README.md, replace stale wording that says Linux and Windows packaged launcher status publication remains deferred with wording that says the evidence commands and checked-in status rows now exist. In packages/parity-fixtures/README.md, list the Phase 29 Linux and Windows packaged launcher fixture bundles and their matching evidence labels. In each platform fixture README, add the matching bazel run //packages/parity:*_packaged_launcher_parity command, describe the verified surface as a scoped packaged launcher tree or package-shaped launcher tree per D-06, and keep unsupported release/package surfaces explicitly out of scope per D-07. Do not create a new docs hub per D-08.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const checks = [
  {
    path: 'packages/parity/README.md',
    must: [
      /linux\.packaged-launcher/,
      /windows\.packaged-launcher/,
      /\/\/packages\/parity:linux_packaged_launcher_parity/,
      /\/\/packages\/parity:windows_packaged_launcher_parity/,
      /checked-in status rows|status rows/
    ],
    mustNot: [/status publication remains deferred/]
  },
  {
    path: 'packages/parity-fixtures/README.md',
    must: [
      /linux-packaged-launcher/,
      /windows-packaged-launcher/,
      /\/\/packages\/parity:linux_packaged_launcher_parity/,
      /\/\/packages\/parity:windows_packaged_launcher_parity/
    ],
    mustNot: [/status publication remains deferred/]
  },
  {
    path: 'packages/parity-fixtures/linux-packaged-launcher/README.md',
    must: [
      /\/\/packages\/parity:linux_packaged_launcher_parity/,
      /scoped packaged launcher tree|package-shaped launcher tree/,
      /AppImage|distro package|installer|signing|release/
    ],
    mustNot: [/Linux packaging parity remains deferred|status publication remains deferred/]
  },
  {
    path: 'packages/parity-fixtures/windows-packaged-launcher/README.md',
    must: [
      /\/\/packages\/parity:windows_packaged_launcher_parity/,
      /scoped packaged launcher tree|package-shaped launcher tree/,
      /MSI|installer|signing|release/
    ],
    mustNot: [/Windows packaging-visible behavior remains deferred|status publication remains deferred/]
  }
];
let failed = false;
for (const check of checks) {
  const text = fs.readFileSync(check.path, 'utf8');
  for (const pattern of check.must) {
    if (!pattern.test(text)) {
      console.error(`${check.path}: missing ${pattern}`);
      failed = true;
    }
  }
  for (const pattern of check.mustNot) {
    if (pattern.test(text)) {
      console.error(`${check.path}: forbidden stale wording ${pattern}`);
      failed = true;
    }
  }
}
process.exit(failed ? 1 : 0);
NODE</automated>
    <automated>if rg -n "status publication remains deferred|Linux packaging parity remains deferred|Windows packaging-visible behavior remains deferred" packages/parity/README.md packages/parity-fixtures/README.md packages/parity-fixtures/linux-packaged-launcher/README.md packages/parity-fixtures/windows-packaged-launcher/README.md; then exit 1; fi</automated>
  </verify>
  <done>Parity and fixture package docs each contain their required platform-specific labels and scoped wording, and none of those surfaces still says publication is deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Maintainer -> Bazel status command | Maintainer executes checked-in status data and expects it to describe real evidence labels. |
| Status/docs data -> human reader | TSV rows and README files can mislead maintainers if they overclaim package support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-30-01-01 | Tampering | packages/parity/status.tsv | mitigate | Verify separate Linux and Windows row counters and exact status/evidence triples; reject duplicates and conflicting rows. |
| T-30-01-02 | Spoofing | packages/parity/README.md and fixture README files | mitigate | Per-file assertions require the exact Bazel labels from packages/parity/BUILD.bazel on every doc surface that claims the evidence. |
| T-30-01-03 | Information Disclosure | README documentation | accept | This plan publishes public migration status only and introduces no secrets, credentials, or external service data. |
| T-30-01-04 | Repudiation | checked-in status dashboard | mitigate | Keep bazel run //packages/parity:status as a checked-in dashboard per D-03 and verify it renders after the data edit. |
</threat_model>

<verification>
Plan-level verification:

1. `awk -F '\t' '$1 == "linux.packaged-launcher" { linux++; if ($2 != "verified" || $3 != "//packages/parity:linux_packaged_launcher_parity") bad++ } $1 == "windows.packaged-launcher" { windows++; if ($2 != "verified" || $3 != "//packages/parity:windows_packaged_launcher_parity") bad++ } END { exit(linux == 1 && windows == 1 && bad == 0 ? 0 : 1) }' packages/parity/status.tsv`
2. `bazel run //packages/parity:status`
3. The Task 2 Node per-file documentation assertion command passes.
</verification>

<success_criteria>
- `packages/parity/status.tsv` publishes exactly one `linux.packaged-launcher` row and exactly one `windows.packaged-launcher` row.
- Both rows are `verified` and point at the exact Phase 29 parity labels.
- Parity and fixture package docs expose the same labels without stale status-deferred wording.
- Fixture docs keep unsupported release/package surfaces explicitly outside the verified scope.
</success_criteria>

<output>
After completion, create `.planning/phases/30-packaging-visibility-and-docs/30-01-SUMMARY.md` with frontmatter including:

```yaml
requirements-completed:
  - PVIS-01
```

Do not include LPKG, WPKG, or PKGE IDs in any Phase 30 summary. Do not run mdformat on phase *-SUMMARY.md files.
</output>
