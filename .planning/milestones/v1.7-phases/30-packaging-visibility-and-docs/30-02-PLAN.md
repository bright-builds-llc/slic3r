---
phase: 30-packaging-visibility-and-docs
plan: "02"
type: execute
wave: 2
depends_on:
  - "30-01"
files_modified:
  - docs/port/parity-matrix.md
  - docs/port/contract-inventory.md
  - docs/port/migration-guidance.md
  - docs/port/linux-launcher-slice.md
  - docs/port/windows-launcher-slice.md
  - docs/port/entrypoint-architecture.md
  - docs/port/package-map.md
  - docs/port/README.md
  - packages/launcher/README.md
autonomous: true
requirements:
  - PVIS-02
user_setup: []
must_haves:
  truths:
    - "Maintainer can read the migration and package docs and find the Linux and Windows scoped packaged launcher evidence labels."
    - "Every required doc surface updated by this plan describes verified support as a scoped packaged launcher tree or package-shaped launcher tree."
    - "Docs do not say Linux or Windows packaged launcher evidence/status publication remains deferred."
    - "Docs still avoid overclaiming signing, installers, AppImage, MSI, DMG, GUI packaging, release archives, native/cross-compiled release binaries, broad bundled dependency layout, downstream fork work, or release channels."
  artifacts:
    - path: "docs/port/parity-matrix.md"
      provides: "High-level packaging-visible validation state"
      contains: "linux.packaged-launcher"
    - path: "docs/port/contract-inventory.md"
      provides: "Detailed scoped packaging-visible contract inventory"
      contains: "//packages/parity:windows_packaged_launcher_parity"
    - path: "docs/port/migration-guidance.md"
      provides: "Conservative migration guidance for supported scope and remaining gaps"
      contains: "package-shaped launcher tree"
    - path: "docs/port/linux-launcher-slice.md"
      provides: "Linux launcher docs with Phase 29 packaged evidence"
      contains: "//packages/parity:linux_packaged_launcher_parity"
    - path: "docs/port/windows-launcher-slice.md"
      provides: "Windows launcher docs with Phase 29 packaged evidence"
      contains: "//packages/parity:windows_packaged_launcher_parity"
    - path: "docs/port/entrypoint-architecture.md"
      provides: "Entrypoint boundary docs no longer underclaim shared packaged parity evidence"
      contains: "Phase 29"
    - path: "docs/port/package-map.md"
      provides: "Package ownership map for parity and fixture packages"
      contains: "packages/parity-fixtures"
    - path: "docs/port/README.md"
      provides: "Port docs index with accurate Phase 30 packaging visibility state"
      contains: "linux.packaged-launcher"
    - path: "packages/launcher/README.md"
      provides: "Launcher package docs with current Linux and Windows evidence/status facts"
      contains: "windows_packaged_launcher_parity"
  key_links:
    - from: "docs/port/parity-matrix.md"
      to: "packages/parity/status.tsv"
      via: "status row names from 30-01"
      pattern: "linux\\.packaged-launcher|windows\\.packaged-launcher"
    - from: "docs/port/contract-inventory.md"
      to: "//packages/parity:linux_packaged_launcher_parity"
      via: "evidence label"
      pattern: "//packages/parity:linux_packaged_launcher_parity"
    - from: "packages/launcher/README.md"
      to: "//packages/parity:windows_packaged_launcher_parity"
      via: "launcher package evidence label"
      pattern: "//packages/parity:windows_packaged_launcher_parity"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 30-2026-05-23T13-19-23
generated_at: 2026-05-23T13:52:01Z
---

<objective>
Publish the Phase 29 Linux and Windows packaged launcher evidence through existing migration, launcher, package, and port documentation.

Purpose: Close PVIS-02 by removing stale deferred claims while preserving conservative release/package boundaries.
Output: Updated existing docs surfaces; no new cross-platform docs hub.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-packaging-visibility-and-docs/30-CONTEXT.md
@.planning/phases/30-packaging-visibility-and-docs/30-RESEARCH.md
@.planning/phases/30-packaging-visibility-and-docs/30-01-SUMMARY.md
@.planning/phases/27-linux-packaged-launcher-slice/27-01-SUMMARY.md
@.planning/phases/28-windows-packaged-launcher-slice/28-01-SUMMARY.md
@.planning/phases/28-windows-packaged-launcher-slice/28-03-SUMMARY.md
@.planning/phases/29-cross-platform-packaging-evidence/29-01-SUMMARY.md
@packages/parity/status.tsv
@docs/port/parity-matrix.md
@docs/port/contract-inventory.md
@docs/port/migration-guidance.md
@docs/port/linux-launcher-slice.md
@docs/port/windows-launcher-slice.md
@docs/port/entrypoint-architecture.md
@docs/port/package-map.md
@docs/port/README.md
@packages/launcher/README.md

Bright Builds context used for this plan: local AGENTS guidance, AGENTS.bright-builds.md, standards-overrides.md, and repo codebase ARCHITECTURE/CONVENTIONS/TESTING guidance. Local repo guidance forbids running mdformat on phase *-SUMMARY.md files and requires the exact requirements-completed summary key.

<interfaces>
Inputs created by Plan 30-01:
```text
linux.packaged-launcher -> verified -> //packages/parity:linux_packaged_launcher_parity
windows.packaged-launcher -> verified -> //packages/parity:windows_packaged_launcher_parity
```

Required conservative scope wording per D-06 and D-07:
```text
Verified: scoped packaged launcher tree or package-shaped launcher tree, startup handoff, layout, scope notes, help/version/config/export/transform evidence for the existing Rust-backed slice.
Out of scope: signing, installers, AppImage, MSI, DMG, GUI packaging, release archives, native/cross-compiled release binaries, broad bundled dependency layout, downstream fork work, release channels.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update parity matrix, contract inventory, and migration guidance</name>
  <files>docs/port/parity-matrix.md, docs/port/contract-inventory.md, docs/port/migration-guidance.md</files>
  <action>Run the core docs publication pass required by D-05 through D-08. In docs/port/parity-matrix.md, update Linux runtime, Windows runtime, Launcher path, and Packaging-visible behavior notes so they no longer say Linux or Windows packaged launcher parity/evidence is deferred; the Packaging-visible behavior row must mention linux.packaged-launcher, windows.packaged-launcher, //packages/parity:linux_packaged_launcher_parity, and //packages/parity:windows_packaged_launcher_parity. In docs/port/contract-inventory.md, update launcher and packaging-visible rows so Linux and Windows package-shaped launcher trees are verified through the Phase 29 parity targets while every broader package/release surface from D-07 remains out of scope. In docs/port/migration-guidance.md, replace Scope Now vs Deferred bullets that still treat Linux and Windows packaging-visible parity as unavailable with current scoped support and exact evidence labels. Do not create a new documentation hub per D-08.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const checks = [
  {
    path: 'docs/port/parity-matrix.md',
    must: [
      /linux\.packaged-launcher/,
      /windows\.packaged-launcher/,
      /\/\/packages\/parity:linux_packaged_launcher_parity/,
      /\/\/packages\/parity:windows_packaged_launcher_parity/,
      /scoped packaged launcher tree|package-shaped launcher tree/,
      /AppImage|MSI|DMG|installer|release/
    ]
  },
  {
    path: 'docs/port/contract-inventory.md',
    must: [
      /\/\/packages\/parity:linux_packaged_launcher_parity/,
      /\/\/packages\/parity:windows_packaged_launcher_parity/,
      /scoped packaged launcher tree|package-shaped launcher tree/,
      /AppImage/,
      /MSI/,
      /DMG/,
      /signing|release/
    ]
  },
  {
    path: 'docs/port/migration-guidance.md',
    must: [
      /\/\/packages\/parity:linux_packaged_launcher_parity/,
      /\/\/packages\/parity:windows_packaged_launcher_parity/,
      /scoped packaged launcher tree|package-shaped launcher tree/,
      /AppImage|MSI|DMG|installer|release/
    ]
  }
];
const stale = /status publication remains deferred|Shared (Linux|Windows|cross-platform|packaged).*parity evidence remains (later work|Phase 29 scope)|Linux packaging parity remains deferred|Windows packaging-visible behavior remains deferred|Windows implementation deferred to a later milestone|Windows parity is outside the current milestone/;
let failed = false;
for (const check of checks) {
  const text = fs.readFileSync(check.path, 'utf8');
  for (const pattern of check.must) {
    if (!pattern.test(text)) {
      console.error(`${check.path}: missing ${pattern}`);
      failed = true;
    }
  }
  if (stale.test(text)) {
    console.error(`${check.path}: contains stale deferred wording`);
    failed = true;
  }
}
process.exit(failed ? 1 : 0);
NODE</automated>
  </verify>
  <done>The three core migration docs each contain the required platform labels, scoped-support wording, and explicit release/package exclusions, with stale deferred claims removed per file.</done>
</task>

<task type="auto">
  <name>Task 2: Update platform, entrypoint, package map, and launcher docs</name>
  <files>docs/port/linux-launcher-slice.md, docs/port/windows-launcher-slice.md, docs/port/entrypoint-architecture.md, docs/port/package-map.md, docs/port/README.md, packages/launcher/README.md</files>
  <action>Complete the existing-docs publication pass per D-05 through D-08. In docs/port/linux-launcher-slice.md and docs/port/windows-launcher-slice.md, add the matching bazel run //packages/parity:*_packaged_launcher_parity command under supported/current verification and update Verification Status to say Phase 29 shared packaged launcher evidence exists. In docs/port/entrypoint-architecture.md, update Phase 27 and Phase 28 sections so they no longer say shared packaged parity evidence remains later work; add a narrow Phase 29 note if that is the clearest way to name both evidence commands. In docs/port/package-map.md, update package roles and phase notes so packages/parity and packages/parity-fixtures include Linux and Windows scoped packaged launcher evidence and fixtures. In docs/port/README.md and packages/launcher/README.md, add current Linux and Windows packaged launcher evidence/status facts. Keep all broader package/release surfaces from D-07 out of scope and do not create a new docs hub per D-08.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const checks = [
  {
    path: 'docs/port/linux-launcher-slice.md',
    must: [
      /\/\/packages\/parity:linux_packaged_launcher_parity/,
      /scoped packaged launcher tree|package-shaped launcher tree/,
      /AppImage|distro package|installer|signing|release/
    ]
  },
  {
    path: 'docs/port/windows-launcher-slice.md',
    must: [
      /\/\/packages\/parity:windows_packaged_launcher_parity/,
      /scoped packaged launcher tree|package-shaped launcher tree/,
      /MSI|installer|signing|release|native\/cross-compiled|cross-compiled/
    ]
  },
  {
    path: 'docs/port/entrypoint-architecture.md',
    must: [
      /\/\/packages\/parity:linux_packaged_launcher_parity/,
      /\/\/packages\/parity:windows_packaged_launcher_parity/,
      /Phase 29/
    ]
  },
  {
    path: 'docs/port/package-map.md',
    must: [
      /packages\/parity/,
      /packages\/parity-fixtures/,
      /linux-packaged-launcher/,
      /windows-packaged-launcher/,
      /\/\/packages\/parity:linux_packaged_launcher_parity/,
      /\/\/packages\/parity:windows_packaged_launcher_parity/
    ]
  },
  {
    path: 'docs/port/README.md',
    must: [
      /linux\.packaged-launcher/,
      /windows\.packaged-launcher/,
      /scoped packaged launcher tree|package-shaped launcher tree/
    ]
  },
  {
    path: 'packages/launcher/README.md',
    must: [
      /\/\/packages\/parity:linux_packaged_launcher_parity/,
      /\/\/packages\/parity:windows_packaged_launcher_parity/,
      /scoped packaged launcher tree|package-shaped launcher tree/,
      /AppImage|MSI|DMG|installer|signing|release/
    ]
  }
];
const stale = /status publication remains deferred|Shared (Linux|Windows|cross-platform|packaged).*parity evidence remains (later work|Phase 29 scope)|Linux packaging parity remains deferred|Windows packaging-visible behavior remains deferred|Windows implementation deferred to a later milestone|Windows parity is outside the current milestone/;
let failed = false;
for (const check of checks) {
  const text = fs.readFileSync(check.path, 'utf8');
  for (const pattern of check.must) {
    if (!pattern.test(text)) {
      console.error(`${check.path}: missing ${pattern}`);
      failed = true;
    }
  }
  if (stale.test(text)) {
    console.error(`${check.path}: contains stale deferred wording`);
    failed = true;
  }
}
process.exit(failed ? 1 : 0);
NODE</automated>
    <automated>if rg -n "status publication remains deferred|Shared (Linux|Windows|cross-platform|packaged).*parity evidence remains (later work|Phase 29 scope)|Linux packaging parity remains deferred|Windows packaging-visible behavior remains deferred|Windows implementation deferred to a later milestone|Windows parity is outside the current milestone" docs/port packages/launcher/README.md; then exit 1; fi</automated>
  </verify>
  <done>Each remaining required doc surface has its own required evidence/scope strings, and the stale deferred-wording negative check passes across the updated docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Documentation -> maintainer decisions | Maintainers rely on docs to distinguish verified scoped launcher evidence from unsupported release packaging. |
| Status data -> documentation | Plan 30-02 docs reference status rows created by Plan 30-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-30-02-01 | Spoofing | docs/port documentation | mitigate | Per-file assertions require exact Linux and Windows evidence labels on every doc surface that claims current support. |
| T-30-02-02 | Tampering | docs/port/parity-matrix.md | mitigate | Require both status row names and both evidence labels in the high-level status matrix. |
| T-30-02-03 | Repudiation | docs/port/package-map.md | mitigate | Require package ownership docs to name packages/parity, packages/parity-fixtures, and both fixture bundle directories. |
| T-30-02-04 | Information Disclosure | docs and README files | accept | This plan edits public documentation only and introduces no secrets or external service data. |
| T-30-02-05 | Denial of Service | documentation verification | accept | Verification is local grep/Node parsing over bounded Markdown files. |
</threat_model>

<verification>
Plan-level verification:

1. The Task 1 Node per-file documentation assertion command passes for parity-matrix, contract-inventory, and migration-guidance.
2. The Task 2 Node per-file documentation assertion command passes for platform, entrypoint, package map, docs index, and launcher package docs.
3. `if rg -n "status publication remains deferred|Shared (Linux|Windows|cross-platform|packaged).*parity evidence remains (later work|Phase 29 scope)|Linux packaging parity remains deferred|Windows packaging-visible behavior remains deferred|Windows implementation deferred to a later milestone|Windows parity is outside the current milestone" docs/port packages/launcher/README.md; then exit 1; fi`
</verification>

<success_criteria>
- Existing docs describe verified Linux and Windows support only as scoped/package-shaped launcher tree evidence for the already verified Rust-backed slice.
- Existing docs no longer contain stale statements that Linux or Windows packaged launcher shared evidence/status publication remains unavailable.
- Existing docs still keep signing, installers, AppImage, MSI, DMG, GUI packaging, release archives, native/cross-compiled release binaries, broad bundled dependency layout, downstream fork work, and release channels out of scope wherever broader support could be inferred.
- No new documentation hub or broad restructure is created.
</success_criteria>

<output>
After completion, create `.planning/phases/30-packaging-visibility-and-docs/30-02-SUMMARY.md` with frontmatter including:

```yaml
requirements-completed:
  - PVIS-02
```

Do not include LPKG, WPKG, or PKGE IDs in any Phase 30 summary. Do not run mdformat on phase *-SUMMARY.md files.
</output>
