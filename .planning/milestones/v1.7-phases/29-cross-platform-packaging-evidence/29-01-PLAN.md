---
phase: 29-cross-platform-packaging-evidence
plan: "01"
type: execute
wave: 1
depends_on:
  - "27-01"
  - "28-01"
files_modified:
  - packages/parity/BUILD.bazel
  - packages/parity/README.md
  - packages/parity/compare_packaged_launcher.sh
  - packages/parity-fixtures/BUILD.bazel
  - packages/parity-fixtures/linux-packaged-launcher/README.md
  - packages/parity-fixtures/linux-packaged-launcher/expected-files.txt
  - packages/parity-fixtures/linux-packaged-launcher/expected-packaged-slice.txt
  - packages/parity-fixtures/windows-packaged-launcher/README.md
  - packages/parity-fixtures/windows-packaged-launcher/expected-files.txt
  - packages/parity-fixtures/windows-packaged-launcher/expected-packaged-slice.txt
autonomous: true
requirements:
  - PKGE-01
  - PKGE-02
  - PKGE-03
must_haves:
  truths:
    - "Maintainer can run Linux packaged launcher parity evidence through a checked-in Bazel command."
    - "Maintainer can run Windows packaged launcher parity evidence through a checked-in Bazel command."
    - "The parity evidence compares package layout, scope notes, help/version, config persistence, and representative export/transform behavior against checked-in fixtures."
  artifacts:
    - path: "packages/parity/compare_packaged_launcher.sh"
      provides: "Shared packaged launcher parity comparator"
      contains: "linux packaged launcher"
    - path: "packages/parity/BUILD.bazel"
      provides: "Parity command labels"
      contains: "linux_packaged_launcher_parity"
    - path: "packages/parity-fixtures/linux-packaged-launcher/expected-files.txt"
      provides: "Linux package layout fixture"
      contains: "./bin/slic3r"
    - path: "packages/parity-fixtures/windows-packaged-launcher/expected-files.txt"
      provides: "Windows package layout fixture"
      contains: "./Slic3r-console.exe"
  key_links:
    - from: "packages/parity/BUILD.bazel"
      to: "packages/launcher/package/linux/build_packaged_launcher.sh"
      via: "linux_packaged_launcher_parity data and args"
      pattern: "linux_packaged_launcher_parity"
    - from: "packages/parity/BUILD.bazel"
      to: "packages/launcher/package/win/build_packaged_launcher.sh"
      via: "windows_packaged_launcher_parity data and args"
      pattern: "windows_packaged_launcher_parity"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 29-2026-05-23T11-45-16
generated_at: 2026-05-23T11:45:16.382Z
---

<objective>
Add shared checked-in parity evidence for the scoped Linux and Windows
packaged launcher trees.

Purpose: Let maintainers verify Linux and Windows package layout, startup
handoff, help/version, representative config behavior, and existing scoped
export/transform behavior through reviewable fixtures and Bazel commands.

Output: New `packages/parity` packaged launcher parity labels and fixture
bundles under `packages/parity-fixtures`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-cross-platform-packaging-evidence/29-CONTEXT.md
@.planning/phases/29-cross-platform-packaging-evidence/29-RESEARCH.md
@packages/parity/BUILD.bazel
@packages/parity/compare_linux_runtime.sh
@packages/parity/compare_windows_runtime.sh
@packages/parity/compare_macos_packaged_launcher.sh
@packages/parity-fixtures/BUILD.bazel
@packages/launcher/BUILD.bazel

<interfaces>
Add these exact labels:
```text
bazel run //packages/parity:linux_packaged_launcher_parity
bazel run //packages/parity:windows_packaged_launcher_parity
```
</interfaces>
</context>

<tasks>
<task type="auto" id="1">
  <name>Task 1: Add packaged launcher fixture bundles</name>
  <files>
    <file>packages/parity-fixtures/BUILD.bazel</file>
    <file>packages/parity-fixtures/linux-packaged-launcher/README.md</file>
    <file>packages/parity-fixtures/linux-packaged-launcher/expected-files.txt</file>
    <file>packages/parity-fixtures/linux-packaged-launcher/expected-packaged-slice.txt</file>
    <file>packages/parity-fixtures/windows-packaged-launcher/README.md</file>
    <file>packages/parity-fixtures/windows-packaged-launcher/expected-files.txt</file>
    <file>packages/parity-fixtures/windows-packaged-launcher/expected-packaged-slice.txt</file>
  </files>
  <action>
    Add reviewable Linux and Windows packaged launcher fixture directories.
    Register their files in `packages/parity-fixtures/BUILD.bazel` with aliases
    for expected file lists and scope notes plus filegroups for each platform
    bundle. Reuse existing shared behavior fixtures instead of duplicating
    help/version/config/export/transform output files.
  </action>
  <verify>
    <automated>rg -n "linux_packaged_launcher_bundle|windows_packaged_launcher_bundle" packages/parity-fixtures/BUILD.bazel</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Linux package fixture contains `./bin/slic3r` and `./bin/slic3r_cli`.</criterion>
    <criterion>Windows package fixture contains `./Slic3r-console.exe`.</criterion>
    <criterion>Fixture scope notes match the package notes exactly.</criterion>
  </acceptance_criteria>
</task>

<task type="auto" id="2">
  <name>Task 2: Add shared packaged launcher parity comparator</name>
  <files>
    <file>packages/parity/compare_packaged_launcher.sh</file>
  </files>
  <action>
    Add a Bash comparator that accepts a platform mode, builds the scoped
    package tree under a temp root, compares layout and scope notes against
    fixtures, then runs version/help/config/export/info/repair/split through
    the packaged startup command and compares outputs against existing shared
    fixtures.
  </action>
  <verify>
    <automated>bash -n packages/parity/compare_packaged_launcher.sh</automated>
    <automated>shfmt -d packages/parity/compare_packaged_launcher.sh</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Comparator runs the Linux packaged `bin/slic3r` command for Linux mode.</criterion>
    <criterion>Comparator runs the Windows packaged `Slic3r-console.exe` command for Windows mode.</criterion>
    <criterion>Comparator emits high-signal mismatch messages for fixture differences.</criterion>
  </acceptance_criteria>
</task>

<task type="auto" id="3">
  <name>Task 3: Wire Bazel parity labels and narrow docs</name>
  <files>
    <file>packages/parity/BUILD.bazel</file>
    <file>packages/parity/README.md</file>
  </files>
  <action>
    Add `linux_packaged_launcher_parity` and
    `windows_packaged_launcher_parity` `sh_binary` targets that pass the
    correct package builders, runtime targets, startup script marker, package
    fixtures, and shared behavior fixtures into the comparator. Update the
    parity README with the two new commands while leaving `status.tsv`
    unchanged for Phase 30.
  </action>
  <verify>
    <automated>bazel run //packages/parity:linux_packaged_launcher_parity</automated>
    <automated>bazel run //packages/parity:windows_packaged_launcher_parity</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`bazel run //packages/parity:linux_packaged_launcher_parity` exits 0.</criterion>
    <criterion>`bazel run //packages/parity:windows_packaged_launcher_parity` exits 0.</criterion>
    <criterion>`packages/parity/status.tsv` is not changed by this plan.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `bash -n packages/parity/compare_packaged_launcher.sh`
- `shfmt -d packages/parity/compare_packaged_launcher.sh`
- `bazel run //packages/parity:linux_packaged_launcher_parity`
- `bazel run //packages/parity:windows_packaged_launcher_parity`
- `bazel run //packages/parity:macos_packaged_launcher_parity`
- `bazel run //packages/parity:linux_runtime_parity`
- `bazel run //packages/parity:windows_runtime_parity`
- `git diff --check`
</verification>

<success_criteria>
- PKGE-01 is materially completed by runnable Linux packaged launcher parity evidence.
- PKGE-02 is materially completed by runnable Windows packaged launcher parity evidence.
- PKGE-03 is materially completed by checked-in package layout/scope fixtures and Bazel command wiring.
</success_criteria>

<output>
After completion, create
`.planning/phases/29-cross-platform-packaging-evidence/29-01-SUMMARY.md` with
`requirements-completed: [PKGE-01, PKGE-02, PKGE-03]` if both new parity
commands pass.
</output>
