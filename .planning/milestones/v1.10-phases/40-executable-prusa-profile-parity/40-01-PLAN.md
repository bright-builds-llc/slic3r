---
phase: 40-executable-prusa-profile-parity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_profile.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_profile_schema_summary.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_profile.rs
  - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
  - packages/parity-fixtures/BUILD.bazel
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/expected-summary.tsv
  - packages/parity/BUILD.bazel
  - packages/parity/compare_prusaslicer_profile_schema.sh
  - packages/parity/compare_prusaslicer_profile_schema_test.sh
autonomous: true
requirements:
  - PPAR-01
  - PPAR-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 40-2026-06-02T12-10-38
generated_at: 2026-06-02T12:34:07Z
must_haves:
  truths:
    - "Maintainer can run bazel run //packages/parity:prusaslicer_profile_schema_parity for the Prusa profile-schema evidence slice."
    - "The command compares Rust-backed PrusaResearch.ini summary output to a checked-in deterministic expected artifact."
    - "The command fails non-zero with a field-specific diagnostic when expected data diverges."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_profile.rs"
      provides: "Pure summary line builder over parse_prusa_profile_bundle output"
      contains: "prusa_profile_schema_summary_lines"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_profile_schema_summary.rs"
      provides: "Thin explicit-path Rust adapter that prints summary TSV lines"
    - path: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/expected-summary.tsv"
      provides: "Checked-in deterministic expected summary derived from PrusaResearch.ini"
    - path: "packages/parity/compare_prusaslicer_profile_schema.sh"
      provides: "Fail-closed parity comparator with concise pass/fail summary"
    - path: "packages/parity/compare_prusaslicer_profile_schema_test.sh"
      provides: "Negative mutation test for expected artifact divergence"
    - path: "packages/parity/BUILD.bazel"
      provides: "Public //packages/parity:prusaslicer_profile_schema_parity command and failure test"
  key_links:
    - from: "packages/parity/BUILD.bazel"
      to: "packages/parity/compare_prusaslicer_profile_schema.sh"
      via: "sh_binary(name = \"prusaslicer_profile_schema_parity\")"
      pattern: "prusaslicer_profile_schema_parity"
    - from: "packages/parity/compare_prusaslicer_profile_schema.sh"
      to: "packages/slic3r-rust/crates/slic3r_flavors:prusa_profile_schema_summary"
      via: "Bazel $(location) Rust binary argument"
      pattern: "prusa_profile_schema_summary"
    - from: "packages/parity/compare_prusaslicer_profile_schema.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/expected-summary.tsv"
      via: "diff -u between expected and actual summary"
      pattern: "expected-summary.tsv"
---

<objective>
Create the executable Prusa profile-schema parity evidence command.

Purpose: Phase 40 must turn the Phase 38 fixture and Phase 39 Rust parser into a repo-owned, rerunnable Bazel command before status publication can happen.
Output: A public `bazel run //packages/parity:prusaslicer_profile_schema_parity` command, a checked-in deterministic expected summary artifact, and a negative failure guard.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-executable-prusa-profile-parity/40-CONTEXT.md
@.planning/phases/40-executable-prusa-profile-parity/40-RESEARCH.md
@.planning/phases/39-rust-prusa-profile-boundary/39-01-SUMMARY.md
@.planning/phases/39-rust-prusa-profile-boundary/39-VERIFICATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/index.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_profile.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_profile.rs
@packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
@packages/parity/BUILD.bazel
@packages/parity-fixtures/BUILD.bazel
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini

<interfaces>
Existing Rust parser and metadata contracts:

```rust
pub fn parse_prusa_profile_bundle(input: &str) -> PrusaProfileParseResult;
pub const fn prusa_profile_schema_metadata() -> PrusaProfileSchemaMetadata;

pub struct PrusaProfileBundle {
    pub sections: Vec<PrusaProfileSection>,
}

pub struct PrusaProfileSection {
    pub index: usize,
    pub line_number: usize,
    pub kind: PrusaProfileSectionKind,
    pub name: PrusaProfileSectionName,
    pub entries: Vec<PrusaProfileEntry>,
}

pub struct PrusaProfileEntry {
    pub section_index: usize,
    pub line_number: usize,
    pub key: PrusaProfileKey,
    pub value: PrusaProfileValue,
}
```

Required public command from D-01 and D-02:

```text
bazel run //packages/parity:prusaslicer_profile_schema_parity
```

Required status token and source identity from D-03 through D-05:

```text
fork.prusaslicer.profile-schema
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic Rust-backed Prusa profile summary output</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_profile.rs, packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs, packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_profile_schema_summary.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_profile.rs, packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_profile.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_profile.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel`
    - `packages/slic3r-rust/crates/slic3r_cli/BUILD.bazel`
    - `.planning/phases/40-executable-prusa-profile-parity/40-CONTEXT.md` decisions D-04, D-05, D-06, D-07, D-14, D-15
  </read_first>
  <behavior>
    - Test 1: `prusa_profile_schema_summary_lines(PRUSA_RESEARCH_INI)` returns lines containing `section_count\t6976`, `entry_count\t27340`, and per-kind counts `vendor=1`, `printer_model=35`, `print=633`, `filament=6069`, `printer=238`.
    - Test 2: summary lines include source metadata from `prusa_profile_schema_metadata()` and sample section/entry lines for the first `vendor`, `printer_model`, `print`, `filament`, and `printer` sections.
    - Test 3: the summary function returns the existing typed `PrusaProfileParseError` when parsing fails; it must not perform file, network, Git, process-spawn, profile auto-update, or discovery work.
  </behavior>
  <action>
    Add a pure function `pub fn prusa_profile_schema_summary_lines(input: &str) -> Result<Vec<String>, PrusaProfileParseError>` in `prusa_profile.rs` per D-04 through D-07. It must call `parse_prusa_profile_bundle(input)`, count entries by summing parsed section entries, count section kinds in deterministic order `vendor`, `printer_model`, `print`, `filament`, `printer`, and emit stable TSV lines in this exact order:

```text
surface	fork.prusaslicer.profile-schema
inventory_id	prusaslicer.profile-schema
vendor_id	prusaslicer
flavor_display	PrusaSlicer
source_ref	prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
source_path	resources/profiles/PrusaResearch.ini
fixture_path	packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini
checklist_path	packages/prusa-baseline/profile-schema-checklist.md
status_token	fork.prusaslicer.profile-schema
section_count	6976
entry_count	27340
section_kind_count	vendor	1
section_kind_count	printer_model	35
section_kind_count	print	633
section_kind_count	filament	6069
section_kind_count	printer	238
sample_section	vendor	0	3	vendor
sample_entry	vendor	0	4	repo_id	prusa-fff
sample_section	printer_model	1	10	COREONEL
sample_entry	printer_model	1	11	name	Prusa CORE One L
sample_section	print	36	351	*common*
sample_entry	print	36	352	avoid_crossing_perimeters	0
sample_section	filament	669	8486	*common*
sample_entry	filament	669	8487	cooling	1
sample_section	printer	6738	39231	*common*
sample_entry	printer	6738	39232	printer_technology	FFF
```

The implementation may compute the counts and samples from parsed data, but the tests must assert the exact target values above so artifact drift cannot be silently accepted. Re-export the function from `src/lib.rs`.

Create `src/bin/prusa_profile_schema_summary.rs` as a thin adapter with `#![forbid(unsafe_code)]`. It must accept exactly one explicit fixture path argument, read only that path with `std::fs::read_to_string`, print each summary line to stdout, and exit non-zero with `error:` diagnostics when the argument count, file read, or parser fails. It must not use `std::env` for path discovery beyond `args_os()`, must not use `std::process::Command`, and must not mention or implement network, Git, profile auto-update, sync, release, GUI, or upstream source-tree access per D-14 and D-15.

Update `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` to load `rust_binary`, add `rust_binary(name = "prusa_profile_schema_summary", srcs = ["src/bin/prusa_profile_schema_summary.rs"], crate_root = "src/bin/prusa_profile_schema_summary.rs", deps = [":slic3r_flavors"], edition = "2024")`, and include that binary in crate `clippy` and `rustfmt_check` targets.
  </action>
  <acceptance_criteria>
    - `rg -n "pub fn prusa_profile_schema_summary_lines" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_profile.rs` finds the new pure summary function.
    - `rg -n "prusa_profile_schema_summary_lines" packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_profile.rs` finds the re-export and tests.
    - `rg -n "name = \"prusa_profile_schema_summary\"|rust_binary" packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` finds the Bazel binary target.
    - `rg -n "Command::|Tcp|Udp|reqwest|curl|git|profile auto-update|vendor sync|release packaging|GUI support" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_profile.rs packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_profile_schema_summary.rs` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all</automated>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_profile</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_profile_test //packages/slic3r-rust:verify</automated>
  </verify>
  <done>The Rust summary function and binary produce deterministic, Rust-backed summary lines from the checked-in PrusaResearch.ini fixture without broad Prusa runtime behavior or side-effecting discovery.</done>
</task>

<task type="auto">
  <name>Task 2: Add checked-in expected summary and public parity command</name>
  <files>packages/parity-fixtures/BUILD.bazel, packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/expected-summary.tsv, packages/parity/BUILD.bazel, packages/parity/compare_prusaslicer_profile_schema.sh</files>
  <read_first>
    - `packages/parity/BUILD.bazel`
    - `packages/parity/compare_export_workflows.sh`
    - `packages/parity/compare_transform_workflows.sh`
    - `packages/parity-fixtures/BUILD.bazel`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv`
    - `.planning/phases/40-executable-prusa-profile-parity/40-CONTEXT.md` decisions D-01, D-02, D-03, D-04, D-05, D-07, D-15
  </read_first>
  <action>
    Create `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/expected-summary.tsv` with exactly the TSV lines listed in Task 1. Add it to `packages/parity-fixtures/BUILD.bazel` `exports_files`, add an alias named `prusa_profile_schema_expected_summary`, and add it to `prusa_profile_schema_bundle`.

    Create `packages/parity/compare_prusaslicer_profile_schema.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. The script must accept four explicit arguments in this order:

```text
summary_binary
PrusaResearch.ini
expected-summary.tsv
fixture-provenance.tsv
```

    It must verify all four files exist, run the Rust summary binary against the explicit fixture path into a temp actual file, compare expected and actual using `diff -u`, and fail non-zero on mismatch. On mismatch it must print a field-specific diagnostic before the diff; derive the first mismatched line label by comparing tab-delimited first fields and include labels such as `section_count` or `sample_entry` in stderr. On success it must print a concise summary per D-03 with these exact facts:

```text
ok: fork.prusaslicer.profile-schema parity passed
source_ref: prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
fixture: packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini
expected: packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/expected-summary.tsv
sections: 6976
entries: 27340
```

    Add `sh_binary(name = "prusaslicer_profile_schema_parity")` under `packages/parity/BUILD.bazel` per D-01 and D-02. Its `data` must include the Rust summary binary target, `//packages/parity-fixtures:prusa_profile_schema_prusa_research_ini`, `//packages/parity-fixtures:prusa_profile_schema_expected_summary`, `//packages/parity-fixtures:prusa_profile_schema_provenance`, and `status.tsv`. Its `args` must pass `$(location ...)` for the summary binary, fixture, expected summary, and provenance in the script order above.
  </action>
  <acceptance_criteria>
    - `test -f packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/expected-summary.tsv` succeeds.
    - `rg -n "section_count\\t6976|entry_count\\t27340|sample_entry\\tprinter\\t6738\\t39232\\tprinter_technology\\tFFF" packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/expected-summary.tsv` finds the deterministic expected values.
    - `rg -n "prusa_profile_schema_expected_summary|expected-summary.tsv" packages/parity-fixtures/BUILD.bazel` finds the exported expected artifact.
    - `rg -n "name = \"prusaslicer_profile_schema_parity\"|compare_prusaslicer_profile_schema.sh|prusa_profile_schema_summary|prusa_profile_schema_expected_summary" packages/parity/BUILD.bazel` finds the public Bazel command wiring.
    - `rg -n "fork.prusaslicer.profile-schema|source_ref: prusaslicer:version_2\\.9\\.5@9a583bd438b195856f3bcf7ea99b69ba4003a961|section_count" packages/parity/compare_prusaslicer_profile_schema.sh` finds the required summary and diagnostic anchors.
  </acceptance_criteria>
  <verify>
    <automated>bazel query //packages/parity:prusaslicer_profile_schema_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_profile_schema_parity</automated>
  </verify>
  <done>The public Bazel command compares Rust-backed summary output against the checked-in expected artifact and prints the exact narrow evidence summary.</done>
</task>

<task type="auto">
  <name>Task 3: Add negative failure guard for mutated expected artifacts</name>
  <files>packages/parity/compare_prusaslicer_profile_schema_test.sh, packages/parity/BUILD.bazel</files>
  <read_first>
    - `packages/parity/compare_prusaslicer_profile_schema.sh`
    - `packages/parity/BUILD.bazel`
    - `packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/expected-summary.tsv`
    - `.planning/phases/40-executable-prusa-profile-parity/40-CONTEXT.md` decisions D-08, D-09, D-10, D-14, D-15
  </read_first>
  <action>
    Create `packages/parity/compare_prusaslicer_profile_schema_test.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Follow the existing temp-copy failure-test pattern from `verify_prusa_profile_schema_fixture_test.sh`.

    The test must:
    - Locate the repo workspace under Bazel using `TEST_SRCDIR`/`TEST_WORKSPACE`, falling back to the repo root when run directly.
    - Copy the real `expected-summary.tsv` to a temp file.
    - Mutate only the temp copy by changing `section_count\t6976` to `section_count\t6975`.
    - Run the real comparator script with the real Rust summary binary, real `PrusaResearch.ini`, the mutated temp expected file, and real provenance.
    - Assert the comparator exits non-zero.
    - Assert stderr contains `section_count` and `expected-summary.tsv`.
    - Assert the real checked-in expected artifact still contains `section_count\t6976`.

    Add `sh_test(name = "prusaslicer_profile_schema_parity_failure_test")` in `packages/parity/BUILD.bazel`. Its `data` must include `compare_prusaslicer_profile_schema.sh`, the Rust summary binary target, the fixture alias, the expected summary alias, and the provenance alias.
  </action>
  <acceptance_criteria>
    - `rg -n "section_count.*6975|section_count.*6976|expected-summary.tsv|prusaslicer_profile_schema_parity_failure_test" packages/parity/compare_prusaslicer_profile_schema_test.sh packages/parity/BUILD.bazel` finds the mutation, guard, and Bazel test target.
    - `bash -lc 'grep -Fq $'"'"'section_count\t6976'"'"' packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/expected-summary.tsv'` succeeds after the negative test has run.
    - `rg -n "git|curl|http://|https://|profile auto-update|vendor sync|release packaging" packages/parity/compare_prusaslicer_profile_schema_test.sh packages/parity/compare_prusaslicer_profile_schema.sh` returns no matches except the accepted source URL may remain only in fixture provenance files outside these scripts.
  </acceptance_criteria>
  <verify>
    <automated>bazel test //packages/parity:prusaslicer_profile_schema_parity_failure_test</automated>
    <automated>bazel run //packages/parity:prusaslicer_profile_schema_parity</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Mutating the expected artifact in a temp location makes the parity command fail non-zero with a `section_count` diagnostic while leaving the real checked-in artifact intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazel locations to shell comparator | Explicit Bazel `$(location)` file paths enter the shell script; uncontrolled discovery must not be accepted. |
| Shell comparator to Rust summary binary | The comparator executes a repo-built binary with an explicit fixture path and compares stdout to checked-in expectations. |
| Checked-in expected artifact to status evidence | Maintainers rely on the expected artifact to prove divergence detection; drift must fail closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | `expected-summary.tsv` and comparator | mitigate | Use checked-in expected TSV, exact `diff -u`, and `//packages/parity:prusaslicer_profile_schema_parity_failure_test` that mutates `section_count` and requires non-zero failure. |
| T-40-02 | Tampering / Information Disclosure | Rust summary adapter and shell comparator | mitigate | Pass explicit Bazel fixture paths only; prohibit Git, network, profile auto-update, source-tree discovery, and process spawning beyond executing the built summary binary from the comparator. |
| T-40-03 | Repudiation | Failure diagnostics | mitigate | Comparator must identify the first mismatched field label, with the negative test asserting `section_count` appears in stderr. |
| T-40-04 | Spoofing | Public command label | mitigate | The only maintainer-facing command in this plan is `//packages/parity:prusaslicer_profile_schema_parity` per D-01 and D-02; no broad Prusa runtime command or status row is created in this plan. |

High severity block: if the command accepts non-explicit fixture discovery, performs network/Git/profile auto-update behavior, or passes despite a mutated expected artifact, stop implementation and fix before continuing.
</threat_model>

<verification>
Run the full Plan 01 evidence suite:

```bash
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_profile_test //packages/slic3r-rust:verify
bazel run //packages/parity:prusaslicer_profile_schema_parity
bazel test //packages/parity:prusaslicer_profile_schema_parity_failure_test
git diff --check
```
</verification>

<success_criteria>
- `bazel run //packages/parity:prusaslicer_profile_schema_parity` exists, passes, and prints `fork.prusaslicer.profile-schema`, the accepted source ref, `PrusaResearch.ini`, `expected-summary.tsv`, `sections: 6976`, and `entries: 27340`.
- The checked-in expected artifact contains deterministic source identity, counts, kind counts, and representative sample rows derived from the Phase 38 fixture.
- `bazel test //packages/parity:prusaslicer_profile_schema_parity_failure_test` proves a mutated `section_count` expected artifact fails non-zero with a field-specific diagnostic.
- No full PrusaSlicer runtime, GUI, generated-output, fork release, profile auto-update, network/cloud/credential, plugin, or sync behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/40-executable-prusa-profile-parity/40-01-SUMMARY.md` with `requirements-completed: [PPAR-01, PPAR-02]`.
</output>
