---
phase: 40-executable-prusa-profile-parity
plan: "02"
type: execute
wave: 2
depends_on:
  - "40-01"
files_modified:
  - packages/parity/status.tsv
  - packages/parity/README.md
  - packages/parity-fixtures/BUILD.bazel
  - packages/parity-fixtures/README.md
  - packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh
  - packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md
  - packages/slic3r-rust/README.md
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/migration-guidance.md
  - docs/port/parity-matrix.md
autonomous: true
requirements:
  - PPAR-01
  - PPAR-02
  - PPAR-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 40-2026-06-02T12-10-38
generated_at: 2026-06-02T12:34:07Z
must_haves:
  truths:
    - "Maintainer can inspect exactly one verified status row for fork.prusaslicer.profile-schema."
    - "Fixture verification requires the exact narrow Prusa status row and evidence command after executable evidence exists."
    - "Docs name only the verified Prusa profile-schema parser/config evidence slice and keep broader PrusaSlicer behavior deferred."
  artifacts:
    - path: "packages/parity/status.tsv"
      provides: "Single narrow fork.prusaslicer.profile-schema verified status row"
      contains: "//packages/parity:prusaslicer_profile_schema_parity"
    - path: "packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh"
      provides: "Fixture verifier requiring the exact Phase 40 status row"
      contains: "verify_status_published"
    - path: "packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh"
      provides: "Failure-mode tests for missing, duplicate, and overclaimed Prusa status rows"
    - path: "docs/port/parity-matrix.md"
      provides: "Human-facing parity wording for the narrow verified Prusa evidence slice"
  key_links:
    - from: "packages/parity/status.tsv"
      to: "packages/parity/BUILD.bazel"
      via: "evidence column references //packages/parity:prusaslicer_profile_schema_parity"
      pattern: "fork\\.prusaslicer\\.profile-schema\\tverified\\t//packages/parity:prusaslicer_profile_schema_parity"
    - from: "packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh"
      to: "packages/parity/status.tsv"
      via: "awk exact row validation"
      pattern: "verify_status_published"
    - from: "docs/port/migration-guidance.md"
      to: "packages/parity/status.tsv"
      via: "status publication policy names exact command and deferrals"
      pattern: "fork\\.prusaslicer\\.profile-schema"
---

<objective>
Publish the narrow executable Prusa profile-schema evidence state after Plan 01 proves the command.

Purpose: PPAR-03 requires maintainers to inspect status and docs that name the exact verified evidence slice without overclaiming broad PrusaSlicer behavior.
Output: One conservative status row, fixture verifier updates that require that exact row, and aligned package/port docs.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-executable-prusa-profile-parity/40-CONTEXT.md
@.planning/phases/40-executable-prusa-profile-parity/40-RESEARCH.md
@.planning/phases/40-executable-prusa-profile-parity/40-01-SUMMARY.md
@.planning/phases/39-rust-prusa-profile-boundary/39-02-SUMMARY.md
@.planning/phases/39-rust-prusa-profile-boundary/39-VERIFICATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/core/verification.md
@packages/parity/status.tsv
@packages/parity/README.md
@packages/parity-fixtures/README.md
@packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh
@packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md
@packages/slic3r-rust/README.md
@docs/port/README.md
@docs/port/package-map.md
@docs/port/migration-guidance.md
@docs/port/parity-matrix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish exact narrow status row and update fixture verifier</name>
  <files>packages/parity/status.tsv, packages/parity-fixtures/BUILD.bazel, packages/parity-fixtures/README.md, packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh, packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh, packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md</files>
  <read_first>
    - `packages/parity/status.tsv`
    - `packages/parity-fixtures/BUILD.bazel`
    - `packages/parity-fixtures/README.md`
    - `packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh`
    - `packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md`
    - `.planning/phases/40-executable-prusa-profile-parity/40-CONTEXT.md` decisions D-11, D-12, D-14, D-15
    - `.planning/phases/40-executable-prusa-profile-parity/40-01-SUMMARY.md`
  </read_first>
  <action>
    First confirm Plan 01 evidence exists by running `bazel run //packages/parity:prusaslicer_profile_schema_parity`. If it fails, stop; status publication must not happen before executable evidence exists per D-11.

    Add exactly one row to `packages/parity/status.tsv`:

```tsv
fork.prusaslicer.profile-schema	verified	//packages/parity:prusaslicer_profile_schema_parity	Shared fixture comparison proves the narrow Prusa profile-schema parser/config evidence slice only; full PrusaSlicer runtime support remains deferred
```

    Replace `verify_status_not_published` in `packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh` with `verify_status_published`. The new function must parse `packages/parity/status.tsv` as tab-delimited data and fail unless exactly one row has:

```text
surface = fork.prusaslicer.profile-schema
status = verified
evidence = //packages/parity:prusaslicer_profile_schema_parity
notes contain "narrow Prusa profile-schema parser/config evidence slice only"
notes contain "full PrusaSlicer runtime support remains deferred"
```

    The verifier must fail on missing row, duplicate row, wrong status, wrong evidence command, or notes that omit either required scope phrase. Its error text must include `fork.prusaslicer.profile-schema` and the failing field name, such as `status`, `evidence`, or `notes`.

    Update `packages/parity-fixtures/BUILD.bazel` so `verify_prusa_profile_schema_fixture` data includes `//packages/parity-fixtures:prusa_profile_schema_expected_summary` and the verifier data remains complete after Plan 01.

    Update `packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh`:
    - Rename `test_status_row_fails` to a test proving a missing Prusa row fails.
    - Add a valid copied status file that includes the exact row above for the passing fixture case.
    - Add tests for wrong evidence command, duplicate Prusa row, and overclaiming notes that omit the narrow-slice phrase or the runtime-deferral phrase.
    - Keep the existing checksum, provenance, namespace, and README failure tests.

    Update `packages/parity-fixtures/README.md` and `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md` to name the new `expected-summary.tsv` artifact and the exact command `bazel run //packages/parity:prusaslicer_profile_schema_parity`. Keep update-route language tied to reviewed intake changes, and keep exclusions for Bambu Studio, OrcaSlicer, network/device/cloud/credential behavior, profile auto-update execution, non-free plugin ingestion, full runtime support, GUI support, sync automation, and fork release packaging.
  </action>
  <acceptance_criteria>
    - `awk -F '\t' '$1=="fork.prusaslicer.profile-schema" { count++; if ($2!="verified" || $3!="//packages/parity:prusaslicer_profile_schema_parity" || $4 !~ /narrow Prusa profile-schema parser\\/config evidence slice only/ || $4 !~ /full PrusaSlicer runtime support remains deferred/) exit 2 } END { exit count==1 ? 0 : 1 }' packages/parity/status.tsv` succeeds.
    - `rg -n "verify_status_published|fork\\.prusaslicer\\.profile-schema|prusaslicer_profile_schema_parity|narrow Prusa profile-schema parser/config evidence slice only|full PrusaSlicer runtime support remains deferred" packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh` finds the verifier and test anchors.
    - `rg -n "expected-summary.tsv|bazel run //packages/parity:prusaslicer_profile_schema_parity|full PrusaSlicer runtime support remains deferred|profile auto-update execution|non-free plugin" packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md` finds the artifact, command, and deferral wording.
    - `rg -n "must not publish Prusa profile-schema status in Phase 38|Phase 38 must not create that target" packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bazel run //packages/parity:prusaslicer_profile_schema_parity</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_profile_schema_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_profile_schema_fixture_test</automated>
    <automated>awk -F '\t' '$1=="fork.prusaslicer.profile-schema" && $2=="verified" && $3=="//packages/parity:prusaslicer_profile_schema_parity" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv</automated>
  </verify>
  <done>`packages/parity/status.tsv` contains exactly one narrow verified Prusa row, and fixture verification now requires that exact row and evidence command.</done>
</task>

<task type="auto">
  <name>Task 2: Align package and port docs with narrow executable evidence</name>
  <files>packages/parity/README.md, packages/slic3r-rust/README.md, docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <read_first>
    - `packages/parity/README.md`
    - `packages/slic3r-rust/README.md`
    - `docs/port/README.md`
    - `docs/port/package-map.md`
    - `docs/port/migration-guidance.md`
    - `docs/port/parity-matrix.md`
    - `.planning/phases/40-executable-prusa-profile-parity/40-CONTEXT.md` decisions D-12, D-13, D-14, D-15
    - `.planning/REQUIREMENTS.md` PPAR-03 and Out of Scope table
  </read_first>
  <action>
    Update docs/status wording only as needed to move `fork.prusaslicer.profile-schema` from reserved vocabulary to verified narrow executable evidence per D-12 and D-13.

    Required target wording:
    - `packages/parity/README.md`: Add `bazel run //packages/parity:prusaslicer_profile_schema_parity` to Current Surface and state that `fork.prusaslicer.profile-schema` is verified only for the narrow Prusa profile-schema parser/config evidence slice backed by `expected-summary.tsv`.
    - `packages/slic3r-rust/README.md`: Add the summary helper/binary and parity command, while preserving that `slic3r_flavors::prusa_profile` remains parser/metadata plus summary evidence only.
    - `docs/port/README.md`: Add a current state note naming the exact source ref, fixture path, expected artifact path, and command.
    - `docs/port/package-map.md`: Add Phase 40 package-discoverability text: command/status in `packages/parity`, expected artifact in `packages/parity-fixtures`, parser/summary in `slic3r_flavors`.
    - `docs/port/migration-guidance.md`: Replace reserved-until-command language with verified-for-this-narrow-slice language and keep the policy that future fork status requires executable evidence.
    - `docs/port/parity-matrix.md`: Update Fork Parity Interpretation to name the verified narrow row and command.

    Required deferral wording must remain visible across the docs: full PrusaSlicer runtime support, GUI support, generated-output parity, fork release builds, profile auto-update execution, network/cloud/credential behavior, non-free plugin ingestion, and sync automation remain deferred. Do not add broad Prusa runtime, upstream binary, generated-output, GUI, release, network, cloud, credential, plugin, or sync claims per D-14 and D-15.
  </action>
  <acceptance_criteria>
    - `rg -n "fork\\.prusaslicer\\.profile-schema|//packages/parity:prusaslicer_profile_schema_parity|expected-summary\\.tsv|PrusaResearch\\.ini|prusaslicer:version_2\\.9\\.5@9a583bd438b195856f3bcf7ea99b69ba4003a961" packages/parity/README.md packages/slic3r-rust/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md` finds the exact status token, command, artifact, fixture, and source ref.
    - `rg -n "narrow Prusa profile-schema parser/config evidence slice|full PrusaSlicer runtime support remains deferred|GUI support.*deferred|generated-output parity.*deferred|fork release builds.*deferred|profile auto-update execution.*deferred|network.*cloud.*credential|non-free plugin.*deferred|sync automation.*deferred" packages/parity/README.md packages/slic3r-rust/README.md docs/port/README.md docs/port/migration-guidance.md docs/port/parity-matrix.md` finds narrow-scope and deferral wording.
    - `rg -n "reserved until|reserved vocabulary only|status table has no Prusa row|must remain free of Prusa profile-schema rows|Phase 40 owns .* any packages/parity/status.tsv row" packages/parity/README.md packages/slic3r-rust/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md` returns no stale reservation matches after the row is published.
    - `awk -F '\t' '$1 ~ /fork\\.prusaslicer/ { count++ } END { exit count==1 ? 0 : 1 }' packages/parity/status.tsv` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>bazel run //packages/parity:status | rg "fork\\.prusaslicer\\.profile-schema|prusaslicer_profile_schema_parity"</automated>
    <automated>mdformat --check packages/parity/README.md packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md packages/slic3r-rust/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Package and port docs publish only the narrow executable Prusa profile-schema evidence state and preserve all broad PrusaSlicer deferrals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidence command to status row | The checked-in status table turns executable evidence into a maintainer-visible claim. |
| Status row to fixture verifier | The fixture verifier must reject stale, missing, duplicate, or overbroad status claims. |
| Docs to maintainer interpretation | Docs must not let a narrow parser/config proof read as full PrusaSlicer runtime support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Spoofing / Repudiation | `packages/parity/status.tsv` | mitigate | Publish exactly one `fork.prusaslicer.profile-schema` row with `verified` status and evidence `//packages/parity:prusaslicer_profile_schema_parity`; add awk guard for exact row. |
| T-40-06 | Tampering | `verify_prusa_profile_schema_fixture.sh` | mitigate | Replace the Phase 38 no-row guard with exact row validation and tests for missing, duplicate, wrong evidence, and overclaimed notes. |
| T-40-07 | Repudiation | Package and port docs | mitigate | Required docs wording must name the narrow parser/config evidence slice and explicit deferrals; stale reserved-row wording must be removed. |
| T-40-08 | Information Disclosure / Tampering | Network/profile auto-update wording | mitigate | Docs and verifier must keep profile auto-update execution, network/cloud/credential behavior, non-free plugin ingestion, sync automation, and fork release packaging deferred; no implementation files in this plan may add those behaviors. |

High severity block: if any status row or doc wording claims full PrusaSlicer runtime support, GUI support, generated-output parity, fork release builds, profile auto-update execution, network/cloud/credential behavior, plugin ingestion, or sync automation as verified, stop implementation and correct the wording before verification.
</threat_model>

<verification>
Run the final Phase 40 publication suite:

```bash
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_profile_test //packages/slic3r-rust:verify
bazel run //packages/parity:prusaslicer_profile_schema_parity
bazel test //packages/parity:prusaslicer_profile_schema_parity_failure_test
bazel run //packages/parity-fixtures:verify_prusa_profile_schema_fixture
bazel test //packages/parity-fixtures:verify_prusa_profile_schema_fixture_test
bazel run //packages/parity:status | rg "fork\\.prusaslicer\\.profile-schema|prusaslicer_profile_schema_parity"
awk -F '\t' '$1=="fork.prusaslicer.profile-schema" && $2=="verified" && $3=="//packages/parity:prusaslicer_profile_schema_parity" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv
mdformat --check packages/parity/README.md packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md packages/slic3r-rust/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md
git diff --check
```
</verification>

<success_criteria>
- `packages/parity/status.tsv` has exactly one `fork.prusaslicer.profile-schema` row and it points to `//packages/parity:prusaslicer_profile_schema_parity`.
- `//packages/parity-fixtures:verify_prusa_profile_schema_fixture` requires that exact row and command instead of rejecting all Prusa rows.
- All docs name the exact verified Prusa profile-schema parser/config evidence slice and keep full PrusaSlicer runtime, GUI, generated-output, release, auto-update, network/cloud/credential, plugin, and sync behavior deferred.
- The final verification suite listed above passes.
</success_criteria>

<output>
After completion, create `.planning/phases/40-executable-prusa-profile-parity/40-02-SUMMARY.md` with `requirements-completed: [PPAR-01, PPAR-02, PPAR-03]`.
</output>
