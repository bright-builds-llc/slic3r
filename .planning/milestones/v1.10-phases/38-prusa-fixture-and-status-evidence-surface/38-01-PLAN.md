---
phase: 38-prusa-fixture-and-status-evidence-surface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/parity-fixtures/BUILD.bazel
  - packages/parity-fixtures/README.md
  - packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh
  - packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/.gitattributes
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.idx
  - packages/parity/README.md
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/migration-guidance.md
  - docs/port/parity-matrix.md
autonomous: true
requirements: [EVID-01, EVID-02, EVID-03]
requirements_addressed: [EVID-01, EVID-02, EVID-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 38-2026-06-01T00-33-46
generated_at: 2026-06-01T00:57:13Z
must_haves:
  truths:
    - "Maintainer can inspect the Prusa fixture namespace at packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/."
    - "Maintainer can inspect raw checked-in PrusaResearch.ini and PrusaResearch.idx fixture inputs from accepted PrusaSlicer source pin version_2.9.5 at 9a583bd438b195856f3bcf7ea99b69ba4003a961."
    - "Maintainer can inspect fixture-local provenance, checksums, source paths, update route, and Phase 37 checklist source."
    - "Maintainer can run a static Bazel verifier and failure-mode tests that check fixture presence, provenance, checksums, status non-publication, and forbidden namespace absence without network, auto-update, plugin ingestion, or Prusa parity command execution."
    - "Maintainer can inspect docs/status vocabulary reserving fork.prusaslicer.profile-schema for Phase 40 only while packages/parity/status.tsv remains free of a Prusa row."
    - "Maintainer can confirm no Bambu Studio, OrcaSlicer, network, cloud, credential, non-free plugin, GUI, sync, runtime, or fork release fixture/support scope was introduced."
  artifacts:
    - path: "packages/parity-fixtures/README.md"
      provides: "Package-level Prusa fixture namespace, update route, and status-preparation rules"
      contains: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/"
    - path: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/.gitattributes"
      provides: "Raw fixture byte-preservation policy for the PrusaResearch bundle files"
      contains: "PrusaResearch.ini -text"
    - path: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md"
      provides: "Fixture-local scope, provenance, update route, and explicit exclusions"
      contains: "Phase 38 fixture/status preparation only"
    - path: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv"
      provides: "Grep-verifiable fixture provenance and checksums"
      contains: "prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961"
    - path: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini"
      provides: "Raw vendor-bundle profile/config fixture input"
      sha256: "a6155d92471d3b0ae11bed051bb04a4c1157fd6b05fef22416eafd12bce9c839"
    - path: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.idx"
      provides: "Matching raw vendor-bundle index fixture input"
      sha256: "65fc21319b2954e5df36040e5a581a313fb409ad9337c2007b1d0f9f2b2352f1"
    - path: "packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh"
      provides: "Static fail-closed verifier for fixture/provenance/status boundaries"
      contains: "set -euo pipefail"
    - path: "packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh"
      provides: "Failure-mode shell tests for fixture verifier"
      contains: "test_forbidden_bambu_namespace_fails"
    - path: "packages/parity-fixtures/BUILD.bazel"
      provides: "Bazel filegroup, verifier, and sh_test targets"
      contains: "prusa_profile_schema_bundle"
    - path: "packages/parity/README.md"
      provides: "Reserved Prusa status vocabulary without status.tsv publication"
      contains: "fork.prusaslicer.profile-schema"
    - path: "docs/port/README.md"
      provides: "Control-plane state for Phase 38 fixture/status preparation"
      contains: "Phase 38 fixture/status preparation only"
    - path: "docs/port/package-map.md"
      provides: "Package-map routing for Prusa fixture namespace"
      contains: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema"
    - path: "docs/port/migration-guidance.md"
      provides: "Fixture update and future status publication rules"
      contains: "fork.prusaslicer.profile-schema"
    - path: "docs/port/parity-matrix.md"
      provides: "Human-facing non-overclaiming status interpretation"
      contains: "fork.prusaslicer.profile-schema"
  key_links:
    - from: "packages/parity-fixtures/BUILD.bazel"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini"
      via: "filegroup(name = \"prusa_profile_schema_bundle\")"
      pattern: "prusa_profile_schema_bundle"
    - from: "packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv"
      via: "exact source_ref, source path, size, and SHA-256 checks"
      pattern: "a6155d92471d3b0ae11bed051bb04a4c1157fd6b05fef22416eafd12bce9c839"
    - from: "packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh"
      to: "packages/parity/status.tsv"
      via: "negative Prusa status row guard"
      pattern: "fork\\.prusaslicer|prusaslicer\\.profile-schema|prusaslicer_profile_schema_parity"
    - from: "packages/parity/README.md"
      to: "packages/parity/status.tsv"
      via: "docs-only reserved token, no row"
      pattern: "fork.prusaslicer.profile-schema"
    - from: "docs/port/migration-guidance.md"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md"
      via: "fixture update route and Phase 39/40 boundary wording"
      pattern: "Phase 38 fixture/status preparation only"
---

<objective>
Create the Phase 38 Prusa fixture and status evidence preparation surface.

Purpose: Give maintainers static, inspectable Prusa profile/config fixture
inputs and conservative status vocabulary before Phase 39 Rust parsing and
Phase 40 executable parity/status publication.

Output: Raw checked-in PrusaResearch fixture files, fixture-local provenance,
Bazel exports, static verifier/tests, and docs that reserve but do not publish
`fork.prusaslicer.profile-schema`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-prusa-fixture-and-status-evidence-surface/38-CONTEXT.md
@.planning/phases/38-prusa-fixture-and-status-evidence-surface/38-RESEARCH.md
@.planning/phases/37-prusa-baseline-and-checklist-gate/37-CONTEXT.md
@.planning/phases/37-prusa-baseline-and-checklist-gate/37-01-SUMMARY.md
@.planning/milestones/v1.9-phases/36-parity-fixture-launcher-and-deferral-templates/36-01-SUMMARY.md
@.planning/milestones/v1.9-phases/36-parity-fixture-launcher-and-deferral-templates/36-02-SUMMARY.md
@.planning/milestones/v1.9-phases/32-vendor-source-manifest-and-license-baseline/32-01-SUMMARY.md
@.planning/milestones/v1.9-phases/33-inventory-templates-and-source-pinned-fork-inventories/33-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/parity-fixtures/README.md
@packages/parity-fixtures/BUILD.bazel
@packages/parity/README.md
@packages/parity/status.tsv
@packages/parity/BUILD.bazel
@packages/prusa-baseline/README.md
@packages/prusa-baseline/profile-schema-checklist.md
@packages/fork-vendors/forks.tsv
@packages/fork-inventories/prusaslicer.tsv
@docs/port/README.md
@docs/port/package-map.md
@docs/port/migration-guidance.md
@docs/port/parity-matrix.md

Bright Builds standards read before planning: architecture, code-shape,
verification, testing, and operability at commit
`05f8d7a6c9c2e157ec4f922a05273e72dab97676`. The materially relevant rules are:
repo-native verification before commit, checked-in scripts with shallow
diagnosable shell, focused failure-mode tests, and preserving local
non-overclaiming boundaries.

<interfaces>
Existing fixture BUILD pattern from `packages/parity-fixtures/BUILD.bazel`:

```python
filegroup(
    name = "linux_packaged_launcher_bundle",
    srcs = [
        "linux-packaged-launcher/README.md",
        "linux-packaged-launcher/expected-files.txt",
        "linux-packaged-launcher/expected-packaged-slice.txt",
    ],
)
```

Existing shell verifier pattern from `packages/prusa-baseline/verify_prusa_baseline.sh`:

```bash
require_text() {
	local file="$1"
	local label="$2"
	local pattern="$3"
	if ! grep -Fq "${pattern}" "${file}"; then
		error "${label}: missing required text: ${pattern}"
	fi
}
```

Accepted Prusa source and fixture facts:

```text
Vendor ID: prusaslicer
Inventory ID: prusaslicer.profile-schema
Source ref: prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
Accepted tag: version_2.9.5
Peeled commit: 9a583bd438b195856f3bcf7ea99b69ba4003a961
Source path: resources/profiles/PrusaResearch.ini
Matching index source path: resources/profiles/PrusaResearch.idx
PrusaResearch.ini bytes: 1543688
PrusaResearch.ini SHA-256: a6155d92471d3b0ae11bed051bb04a4c1157fd6b05fef22416eafd12bce9c839
PrusaResearch.idx bytes: 31543
PrusaResearch.idx SHA-256: 65fc21319b2954e5df36040e5a581a313fb409ad9337c2007b1d0f9f2b2352f1
Future status token reserved in docs only: fork.prusaslicer.profile-schema
Planned Phase 40 command shape for docs only: bazel run //packages/parity:prusaslicer_profile_schema_parity
```
</interfaces>
</context>

<must_haves>
Truths:
- Maintainer can inspect Prusa fixture namespace and update rules under
  `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/`.
- Maintainer can inspect raw `PrusaResearch.ini` and `PrusaResearch.idx`
  fixture inputs from the accepted source pin.
- Maintainer can verify exact fixture provenance, sizes, SHA-256 values,
  source paths, update route, boundary wording, status non-publication, and
  forbidden namespace absence with a rerunnable static command.
- Maintainer can inspect docs that reserve `fork.prusaslicer.profile-schema`
  for Phase 40 only.
- Maintainer can confirm `packages/parity/status.tsv` has no Prusa row and
  `//packages/parity:prusaslicer_profile_schema_parity` does not exist.

Artifacts:
- `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md`
- `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv`
- `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/.gitattributes`
- `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini`
- `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.idx`
- `packages/parity-fixtures/BUILD.bazel`
- `packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh`
- `packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh`
- `packages/parity-fixtures/README.md`
- `packages/parity/README.md`
- `docs/port/README.md`
- `docs/port/package-map.md`
- `docs/port/migration-guidance.md`
- `docs/port/parity-matrix.md`

Key links:
- `//packages/parity-fixtures:prusa_profile_schema_bundle` exports the raw
  fixture files and provenance docs.
- `//packages/parity-fixtures:verify_prusa_profile_schema_fixture` checks the
  fixture bundle, provenance manifest, status table, and namespace boundary.
- `packages/parity/README.md` and `docs/port/*` reserve
  `fork.prusaslicer.profile-schema` without modifying `packages/parity/status.tsv`.

Plan shape:
- This phase intentionally uses one plan despite touching 14 files because the
  fixture files, Bazel exports, verifier/tests, package README, parity README,
  and port docs form one inseparable evidence surface. Splitting the docs from
  the verifier would allow a transient state where maintainers can inspect
  fixtures without the matching non-overclaiming/status guard, or inspect docs
  that reference targets not yet created.
</must_haves>

<decision_coverage>
| Decision | Plan Task | Coverage |
| --- | --- | --- |
| D-01 | Task 1 | Full: creates `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/`. |
| D-02 | Task 1 | Full: checks in raw `PrusaResearch.ini` and matching `PrusaResearch.idx` from the accepted source pin. |
| D-03 | Task 2 | Full: exports files through Bazel fixture targets only and explicitly forbids `//packages/parity:*_parity` creation. |
| D-04 | Task 1 | Full: manifest records `prusaslicer.profile-schema`, `prusaslicer`, `version_2.9.5`, `9a583bd438b195856f3bcf7ea99b69ba4003a961`, source paths, and Phase 37 checklist source. |
| D-05 | Tasks 1, 3 | Full: README/manifest/docs state updates require reviewed intake changes to `packages/fork-vendors/forks.tsv` and the Prusa checklist/baseline gate; branch heads are drift-only. |
| D-06 | Task 2 | Full: verifier checks files, manifest pin, source paths, boundary wording, status non-publication, and forbidden fixture namespace absence. |
| D-07 | Task 2 | Full: verifier is static only; it does not fetch upstream, run auto-update, ingest plugins, or execute Prusa parity. |
| D-08 | Tasks 2, 3 | Full: status guard forbids a Prusa row in `packages/parity/status.tsv`. |
| D-09 | Task 3 | Full: docs reserve `fork.prusaslicer.profile-schema` for Phase 40 and require a rerunnable `//packages/parity:*_parity` command before `verified`. |
| D-10 | Task 3 | Full: docs distinguish Phase 38 fixtures/status prep, Phase 39 Rust parsing, and Phase 40 executable parity/status publication. |
| D-11 | Tasks 1, 2, 3 | Full: new fixture README/manifest and docs explicitly exclude Bambu Studio, OrcaSlicer, network/device, cloud, credentials, profile auto-update, non-free plugin ingestion, full Prusa runtime, GUI, sync automation, and fork release packaging. |
| D-12 | Tasks 2, 3 | Full: docs may reference planned Phase 40 command shape but verifier and guards reject creating the command or implying support from source pins/fixtures alone. |
</decision_coverage>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create static Prusa fixture namespace and provenance</name>
  <files>packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/.gitattributes, packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md, packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv, packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini, packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.idx</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .planning/phases/38-prusa-fixture-and-status-evidence-surface/38-CONTEXT.md
    .planning/phases/38-prusa-fixture-and-status-evidence-surface/38-RESEARCH.md
    .planning/phases/37-prusa-baseline-and-checklist-gate/37-01-SUMMARY.md
    packages/prusa-baseline/README.md
    packages/prusa-baseline/profile-schema-checklist.md
    packages/fork-vendors/forks.tsv
    packages/fork-inventories/prusaslicer.tsv
    packages/parity-fixtures/README.md
  </read_first>
  <action>
    Create the fixture namespace `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/` per D-01.

    Add `.gitattributes` with exactly these two lines to preserve raw fixture bytes:
    - `PrusaResearch.ini -text`
    - `PrusaResearch.idx -text`

    Fetch the raw vendor-bundle fixture inputs from the accepted source pin per D-02:
    - `curl -fsSL --output packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini https://raw.githubusercontent.com/prusa3d/PrusaSlicer/9a583bd438b195856f3bcf7ea99b69ba4003a961/resources/profiles/PrusaResearch.ini`
    - `curl -fsSL --output packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.idx https://raw.githubusercontent.com/prusa3d/PrusaSlicer/9a583bd438b195856f3bcf7ea99b69ba4003a961/resources/profiles/PrusaResearch.idx`

    Immediately verify the downloaded bytes before continuing:
    - `PrusaResearch.ini` must be 1543688 bytes and SHA-256 `a6155d92471d3b0ae11bed051bb04a4c1157fd6b05fef22416eafd12bce9c839`
    - `PrusaResearch.idx` must be 31543 bytes and SHA-256 `65fc21319b2954e5df36040e5a581a313fb409ad9337c2007b1d0f9f2b2352f1`

    Create fixture-local `README.md` with these exact scope and provenance strings:
    - `# PrusaSlicer Profile Schema Fixture`
    - `Phase 38 fixture/status preparation only.`
    - `Static fixture input only.`
    - `Source ref: prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`
    - `Accepted tag: version_2.9.5`
    - `Peeled commit: 9a583bd438b195856f3bcf7ea99b69ba4003a961`
    - `Inventory ID: prusaslicer.profile-schema`
    - `Phase 37 checklist source: packages/prusa-baseline/profile-schema-checklist.md`
    - `Update route: update this fixture only after a reviewed intake change updates packages/fork-vendors/forks.tsv and the Prusa checklist/baseline gate.`
    - `Branch-head observations remain drift-only and do not update this fixture.`
    - `Phase 39 creates Rust parsing; Phase 40 creates executable parity evidence and any verified status publication.`
    - `This fixture package does not introduce Bambu Studio fixtures, OrcaSlicer fixtures, network/device integration, cloud behavior, credentials, profile auto-update execution, non-free plugin ingestion, full Prusa runtime support, GUI support, sync automation, or fork release packaging.`
    - `The planned Phase 40 command shape is bazel run //packages/parity:prusaslicer_profile_schema_parity; Phase 38 must not create that target.`

    Create `fixture-provenance.tsv` as a literal-tab-delimited manifest. Use this exact column order:
    `fixture_id`, `vendor_id`, `inventory_id`, `source_ref`, `accepted_tag`, `peeled_commit`, `source_path`, `upstream_url`, `bytes`, `sha256`, `line_endings`, `role`, `phase37_checklist`, `update_route`, `status_scope`, `exclusions`.

    Add one row for `PrusaResearch.ini` with:
    - `fixture_id`: `PrusaResearch.ini`
    - `vendor_id`: `prusaslicer`
    - `inventory_id`: `prusaslicer.profile-schema`
    - `source_ref`: `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`
    - `accepted_tag`: `version_2.9.5`
    - `peeled_commit`: `9a583bd438b195856f3bcf7ea99b69ba4003a961`
    - `source_path`: `resources/profiles/PrusaResearch.ini`
    - `upstream_url`: `https://raw.githubusercontent.com/prusa3d/PrusaSlicer/9a583bd438b195856f3bcf7ea99b69ba4003a961/resources/profiles/PrusaResearch.ini`
    - `bytes`: `1543688`
    - `sha256`: `a6155d92471d3b0ae11bed051bb04a4c1157fd6b05fef22416eafd12bce9c839`
    - `line_endings`: `lf`
    - `role`: `raw-vendor-bundle-input`
    - `phase37_checklist`: `packages/prusa-baseline/profile-schema-checklist.md`
    - `update_route`: `reviewed-intake-change-updates-packages/fork-vendors/forks.tsv-and-prusa-baseline-gate`
    - `status_scope`: `Phase-38-fixture-status-preparation-only`
    - `exclusions`: `no-bambu-no-orca-no-network-no-cloud-no-credentials-no-profile-auto-update-no-non-free-plugin-no-runtime-no-gui-no-sync-no-release`

    Add one row for `PrusaResearch.idx` with the same values except:
    - `fixture_id`: `PrusaResearch.idx`
    - `source_path`: `resources/profiles/PrusaResearch.idx`
    - `upstream_url`: `https://raw.githubusercontent.com/prusa3d/PrusaSlicer/9a583bd438b195856f3bcf7ea99b69ba4003a961/resources/profiles/PrusaResearch.idx`
    - `bytes`: `31543`
    - `sha256`: `65fc21319b2954e5df36040e5a581a313fb409ad9337c2007b1d0f9f2b2352f1`
    - `line_endings`: `crlf`

    Do not create Rust parsing, normalization logic, upstream source-tree import, vendor sync automation, profile auto-update execution, plugin ingestion, `packages/parity/status.tsv` rows, or `//packages/parity:prusaslicer_profile_schema_parity`.
  </action>
  <verify>
    <automated>bash -lc 'test -f packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md; test -f packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv; test -f packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini; test -f packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.idx; test "$(wc -c < packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini | tr -d " ")" = "1543688"; test "$(wc -c < packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.idx | tr -d " ")" = "31543"; shasum -a 256 packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini | grep -F "a6155d92471d3b0ae11bed051bb04a4c1157fd6b05fef22416eafd12bce9c839"; shasum -a 256 packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.idx | grep -F "65fc21319b2954e5df36040e5a581a313fb409ad9337c2007b1d0f9f2b2352f1"; grep -F "Phase 38 fixture/status preparation only." packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md; grep -F "reviewed-intake-change-updates-packages/fork-vendors/forks.tsv-and-prusa-baseline-gate" packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv'</automated>
  </verify>
  <acceptance_criteria>
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/.gitattributes` contains exactly `PrusaResearch.ini -text` and `PrusaResearch.idx -text`.
    - `PrusaResearch.ini` exists, is 1543688 bytes, and has SHA-256 `a6155d92471d3b0ae11bed051bb04a4c1157fd6b05fef22416eafd12bce9c839`.
    - `PrusaResearch.idx` exists, is 31543 bytes, and has SHA-256 `65fc21319b2954e5df36040e5a581a313fb409ad9337c2007b1d0f9f2b2352f1`.
    - `fixture-provenance.tsv` contains `prusaslicer.profile-schema`, `prusaslicer`, `version_2.9.5`, `9a583bd438b195856f3bcf7ea99b69ba4003a961`, both source paths, both SHA-256 values, and `packages/prusa-baseline/profile-schema-checklist.md`.
    - Fixture-local `README.md` contains the exact scope sentence excluding Bambu Studio, OrcaSlicer, network/device integration, cloud behavior, credentials, profile auto-update execution, non-free plugin ingestion, full Prusa runtime support, GUI support, sync automation, and fork release packaging.
    - `packages/parity/status.tsv` is not modified by this task.
  </acceptance_criteria>
  <done>The raw Prusa profile/config fixture namespace and provenance manifest are checked in exactly as static inputs per D-01 through D-05, D-11, and D-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export fixtures and add static fail-closed verification</name>
  <files>packages/parity-fixtures/BUILD.bazel, packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh, packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh</files>
  <read_first>
    .planning/phases/38-prusa-fixture-and-status-evidence-surface/38-CONTEXT.md
    .planning/phases/38-prusa-fixture-and-status-evidence-surface/38-RESEARCH.md
    packages/parity-fixtures/BUILD.bazel
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv
    packages/parity/status.tsv
    packages/prusa-baseline/verify_prusa_baseline.sh
    packages/prusa-baseline/verify_prusa_baseline_test.sh
  </read_first>
  <behavior>
    - Test 1: the complete checked-in fixture bundle passes and prints `ok: Prusa profile-schema fixture verification passed`.
    - Test 2: missing `PrusaResearch.ini` fails with an error mentioning `PrusaResearch.ini`.
    - Test 3: a modified `PrusaResearch.ini` fails with an error mentioning SHA-256 `a6155d92471d3b0ae11bed051bb04a4c1157fd6b05fef22416eafd12bce9c839`.
    - Test 4: missing source ref `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961` fails.
    - Test 5: missing `Static fixture input only.` in the fixture README fails.
    - Test 6: a forbidden namespace path such as `packages/parity-fixtures/forks/bambustudio` fails.
    - Test 7: a status row containing `fork.prusaslicer.profile-schema` or `prusaslicer_profile_schema_parity` fails.
  </behavior>
  <action>
    Update `packages/parity-fixtures/BUILD.bazel` per D-03:
    - add the five fixture-local files and two raw fixture files to `exports_files`
    - add aliases:
      - `prusa_profile_schema_readme` -> `forks/prusaslicer/prusaslicer.profile-schema/README.md`
      - `prusa_profile_schema_provenance` -> `forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv`
      - `prusa_profile_schema_prusa_research_ini` -> `forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.ini`
      - `prusa_profile_schema_prusa_research_idx` -> `forks/prusaslicer/prusaslicer.profile-schema/PrusaResearch.idx`
    - add `filegroup(name = "prusa_profile_schema_bundle", srcs = [...])` containing `.gitattributes`, README, provenance TSV, `PrusaResearch.ini`, and `PrusaResearch.idx`
    - add `sh_binary(name = "verify_prusa_profile_schema_fixture", srcs = ["verify_prusa_profile_schema_fixture.sh"], data = [":prusa_profile_schema_bundle", "//packages/parity:status.tsv", "README.md"], args = [...])`
    - add `sh_test(name = "verify_prusa_profile_schema_fixture_test", srcs = ["verify_prusa_profile_schema_fixture_test.sh"], data = ["verify_prusa_profile_schema_fixture.sh", ":prusa_profile_schema_bundle", "//packages/parity:status.tsv", "README.md"])`
    - add the new fixture files and verifier scripts to `filegroup(name = "package_boundary")`
    - do not edit `packages/parity/BUILD.bazel`; do not add `//packages/parity:prusaslicer_profile_schema_parity`.

    Write `verify_prusa_profile_schema_fixture_test.sh` first using the Phase 37 shell-test pattern:
    - shebang `#!/usr/bin/env bash`
    - `set -euo pipefail`
    - resolve runfiles from `TEST_SRCDIR` and `TEST_WORKSPACE`
    - copy the actual checked-in fixture files to a temp tree for mutation tests
    - use helpers `fail`, `assert_contains`, `remove_line_containing`, `replace_first_line_containing`, `run_verifier`, and `write_valid_fixture_copy`
    - include explicit Arrange/Act/Assert comments in each non-trivial test
    - include test functions named exactly: `test_complete_fixture_passes`, `test_missing_prusa_research_ini_fails`, `test_wrong_ini_checksum_fails`, `test_missing_source_ref_fails`, `test_missing_static_input_boundary_fails`, `test_forbidden_bambu_namespace_fails`, and `test_status_row_fails`
    - print `ok: verify_prusa_profile_schema_fixture_test`

    Then write `verify_prusa_profile_schema_fixture.sh`:
    - shebang `#!/usr/bin/env bash`
    - `set -euo pipefail`
    - accept zero args using source-tree defaults, or exactly six args in this order: fixture README, `fixture-provenance.tsv`, `PrusaResearch.ini`, `PrusaResearch.idx`, `packages/parity/status.tsv`, `packages/parity-fixtures/README.md`
    - use `PRUSA_FIXTURE_FORKS_ROOT` when set for namespace scanning; otherwise use `${BUILD_WORKSPACE_DIRECTORY}/packages/parity-fixtures/forks` when available, then fall back to `${script_dir}/forks`
    - implement `error`, `require_file`, `require_text`, `require_size`, `require_sha256`, `verify_fixture_files`, `verify_provenance`, `verify_scope_wording`, `verify_forbidden_namespaces`, and `verify_status_not_published`
    - check exact file sizes and SHA-256 values from Task 1
    - check exact strings: `prusaslicer.profile-schema`, `prusaslicer`, `version_2.9.5`, `9a583bd438b195856f3bcf7ea99b69ba4003a961`, `resources/profiles/PrusaResearch.ini`, `resources/profiles/PrusaResearch.idx`, `packages/prusa-baseline/profile-schema-checklist.md`, `Phase 38 fixture/status preparation only.`, and `Static fixture input only.`
    - check the README exclusion sentence from Task 1
    - reject path components under `packages/parity-fixtures/forks` matching these lowercase tokens: `bambustudio`, `orca`, `orcaslicer`, `network`, `cloud`, `credential`, `credentials`, `plugin`, `non-free`
    - allow only `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema` as the Phase 38 fork fixture namespace
    - fail if `packages/parity/status.tsv` contains `fork.prusaslicer`, `prusaslicer.profile-schema`, or `prusaslicer_profile_schema_parity`
    - contain no `curl`, no `git ls-remote`, no `eval`, and no `bash -c`
    - never execute `bazel run //packages/parity:prusaslicer_profile_schema_parity`
    - print `ok: Prusa profile-schema fixture verification passed`
  </action>
  <verify>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_profile_schema_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_profile_schema_fixture_test</automated>
    <automated>shfmt -d packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh</automated>
    <automated>bazel query //packages/parity-fixtures:prusa_profile_schema_bundle</automated>
    <automated>bash -lc 'if bazel query //packages/parity:prusaslicer_profile_schema_parity >/tmp/prusa-profile-schema-parity-query.out 2>&1; then cat /tmp/prusa-profile-schema-parity-query.out; exit 1; fi; if rg -n "fork\\.prusaslicer|prusaslicer\\.profile-schema|prusaslicer_profile_schema_parity" packages/parity/status.tsv; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `bazel query //packages/parity-fixtures:prusa_profile_schema_bundle` resolves.
    - `bazel run //packages/parity-fixtures:verify_prusa_profile_schema_fixture` prints `ok: Prusa profile-schema fixture verification passed`.
    - `bazel test //packages/parity-fixtures:verify_prusa_profile_schema_fixture_test` passes.
    - `shfmt -d packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh` exits 0 with no diff.
    - `packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh` contains no `curl`, no `git ls-remote`, no `eval`, and no `bash -c`.
    - `bazel query //packages/parity:prusaslicer_profile_schema_parity` fails because Phase 38 must not create the executable parity target.
    - `rg -n "fork\\.prusaslicer|prusaslicer\\.profile-schema|prusaslicer_profile_schema_parity" packages/parity/status.tsv` returns no matches.
  </acceptance_criteria>
  <done>The fixture bundle is exported and statically verified while proving D-03, D-06, D-07, D-08, D-11, and D-12 boundaries are enforced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Publish fixture/update/status vocabulary without status rows</name>
  <files>packages/parity-fixtures/README.md, packages/parity/README.md, docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .planning/phases/38-prusa-fixture-and-status-evidence-surface/38-CONTEXT.md
    .planning/phases/38-prusa-fixture-and-status-evidence-surface/38-RESEARCH.md
    packages/parity-fixtures/README.md
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/fixture-provenance.tsv
    packages/parity/README.md
    packages/parity/status.tsv
    docs/port/README.md
    docs/port/package-map.md
    docs/port/migration-guidance.md
    docs/port/parity-matrix.md
  </read_first>
  <action>
    Update `packages/parity-fixtures/README.md` per D-01, D-02, D-05, D-06, D-07, and D-11:
    - keep existing fixture rules
    - add current state text naming `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/`
    - state it contains raw static `PrusaResearch.ini` and `PrusaResearch.idx` inputs from `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`
    - state update route exactly: `update only after a reviewed intake change updates packages/fork-vendors/forks.tsv and the Prusa checklist/baseline gate`
    - state `Branch-head observations remain drift-only`
    - state `Phase 38 verification does not fetch upstream source, run profile auto-update, ingest plugins, or execute a Prusa parity command`
    - include the exact exclusion list from Task 1.

    Update `packages/parity/README.md` per D-08 and D-09:
    - state `Phase 38 does not add a Prusa row to packages/parity/status.tsv.`
    - reserve docs-only token `fork.prusaslicer.profile-schema`
    - state the token cannot be marked `verified` until a rerunnable `bazel run //packages/parity:prusaslicer_profile_schema_parity` command exists and passes in Phase 40
    - state source pins, inventories, checklist records, flavor metadata, and static fixtures are planning inputs only and do not prove full PrusaSlicer support.

    Update `docs/port/README.md` with a `## Current Prusa Fixture/Status Preparation State` section after the Phase 37 baseline gate state:
    - mention `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/`
    - mention `//packages/parity-fixtures:prusa_profile_schema_bundle`
    - mention `//packages/parity-fixtures:verify_prusa_profile_schema_fixture`
    - mention raw files `PrusaResearch.ini` and `PrusaResearch.idx`
    - mention `fork.prusaslicer.profile-schema` as docs-only reserved status vocabulary
    - state `Phase 38 fixture/status preparation only.`
    - state `Phase 39 creates Rust parsing; Phase 40 creates executable parity evidence and any verified status publication.`
    - state Phase 38 adds no Rust parsing, no executable Prusa parity command, no Prusa status row, no upstream source-tree import, no vendor sync automation, no profile auto-update execution, no Bambu Studio fixtures, no OrcaSlicer fixtures, no network/cloud/credential fixtures, no non-free plugin fixtures, no GUI support, and no fork release packaging.

    Update `docs/port/package-map.md`:
    - keep the `packages/parity-fixtures` package row, but extend its role text to include the Phase 38 Prusa profile-schema fixture namespace
    - add a Phase 38 note with exact path `packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/`, target `//packages/parity-fixtures:verify_prusa_profile_schema_fixture`, status token `fork.prusaslicer.profile-schema`, and the statement `packages/parity/status.tsv remains unchanged in Phase 38`.

    Update `docs/port/migration-guidance.md`:
    - in fixture protocol, state the Prusa namespace created in Phase 38 and its update route
    - in future fork parity status, state `fork.prusaslicer.profile-schema` is reserved for Phase 40 only and requires `bazel run //packages/parity:prusaslicer_profile_schema_parity`
    - state `packages/parity/status.tsv` must remain free of Prusa profile-schema rows until that command exists and passes.

    Update `docs/port/parity-matrix.md`:
    - in `## Fork Parity Interpretation`, add a bullet stating `fork.prusaslicer.profile-schema` is reserved vocabulary only in Phase 38
    - state static fixtures and source pins do not create full PrusaSlicer support or `verified` status
    - state the checked-in status table has no Prusa row in Phase 38.

    Do not add a Prusa row to `packages/parity/status.tsv`. Do not create or modify `packages/parity/BUILD.bazel`. Do not add `//packages/parity:prusaslicer_profile_schema_parity`. Do not add Rust parsing or normalization logic. Do not add upstream source imports, vendor sync automation, network/cloud/credential behavior, non-free plugin behavior, Bambu Studio fixtures, OrcaSlicer fixtures, GUI support, or fork release packaging.
  </action>
  <verify>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_profile_schema_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_profile_schema_fixture_test</automated>
    <automated>shfmt -d packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh</automated>
    <automated>bash -lc 'grep -F "packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/" packages/parity-fixtures/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md; grep -F "fork.prusaslicer.profile-schema" packages/parity/README.md docs/port/README.md docs/port/migration-guidance.md docs/port/parity-matrix.md; grep -F "Phase 38 does not add a Prusa row to packages/parity/status.tsv." packages/parity/README.md; grep -F "Phase 38 fixture/status preparation only." docs/port/README.md; if rg -n "fork\\.prusaslicer|prusaslicer\\.profile-schema|prusaslicer_profile_schema_parity" packages/parity/status.tsv; then exit 1; fi; if bazel query //packages/parity:prusaslicer_profile_schema_parity >/tmp/prusa-profile-schema-parity-query.out 2>&1; then cat /tmp/prusa-profile-schema-parity-query.out; exit 1; fi'</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `packages/parity-fixtures/README.md` contains the exact Prusa fixture namespace path, source ref, update route, drift-only branch wording, no-fetch/no-auto-update/no-plugin/no-parity-command wording, and exclusion list.
    - `packages/parity/README.md` contains `fork.prusaslicer.profile-schema`, `Phase 38 does not add a Prusa row to packages/parity/status.tsv.`, and `bazel run //packages/parity:prusaslicer_profile_schema_parity`.
    - `docs/port/README.md` contains `Phase 38 fixture/status preparation only.`, `Phase 39 creates Rust parsing; Phase 40 creates executable parity evidence and any verified status publication.`, and the exact no-scope list from the action.
    - `docs/port/package-map.md`, `docs/port/migration-guidance.md`, and `docs/port/parity-matrix.md` contain `fork.prusaslicer.profile-schema`.
    - `packages/parity/status.tsv` contains no `fork.prusaslicer`, no `prusaslicer.profile-schema`, and no `prusaslicer_profile_schema_parity`.
    - `bazel query //packages/parity:prusaslicer_profile_schema_parity` fails.
    - All verification commands in the verify block pass.
  </acceptance_criteria>
  <done>The fixture package and docs expose Phase 38 status vocabulary and update rules while keeping verified Prusa status and executable parity publication reserved for Phase 40 per D-08 through D-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Upstream raw fixture bytes to checked-in repository | One-time retrieved Prusa files become trusted only after exact source ref, size, and SHA-256 verification. |
| Fixture manifest and README to later Rust/parser agents | Text metadata can overclaim support unless exact source, update, and exclusion wording is verified. |
| Static fixture content to runtime behavior | The upstream bundle contains URL-like and credential-like fields; Phase 38 must keep them inert fixture inputs only. |
| Fixture package to parity status package | Docs may reserve a token, but `packages/parity/status.tsv` must not publish a Prusa row before executable evidence exists. |
| Bazel fixture target to parity command namespace | Fixture exports are allowed; `//packages/parity:*_parity` commands are not allowed in Phase 38. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-38-01 | Tampering | `PrusaResearch.ini` and `PrusaResearch.idx` | mitigate | Verify exact byte sizes and SHA-256 values `a6155d92471d3b0ae11bed051bb04a4c1157fd6b05fef22416eafd12bce9c839` and `65fc21319b2954e5df36040e5a581a313fb409ad9337c2007b1d0f9f2b2352f1` in `verify_prusa_profile_schema_fixture.sh`. |
| T-38-02 | Spoofing | `packages/parity/status.tsv` | mitigate | Verifier and final guard fail if `status.tsv` contains `fork.prusaslicer`, `prusaslicer.profile-schema`, or `prusaslicer_profile_schema_parity`; docs say no Prusa row in Phase 38. |
| T-38-03 | Elevation of Privilege | Fixture verifier | mitigate | Verifier must be static local file checks only and contain no `curl`, no `git ls-remote`, no `eval`, and no `bash -c`; it must not run the planned Phase 40 parity command. |
| T-38-04 | Information Disclosure | URL-like and credential-like fields inside raw Prusa bundle | mitigate | Fixture README/docs/verifier required wording states `Static fixture input only.` and excludes network/device integration, cloud behavior, credentials, and profile auto-update execution. |
| T-38-05 | Tampering | `packages/parity-fixtures/forks` namespace | mitigate | Verifier scans namespace paths and rejects `bambustudio`, `orca`, `orcaslicer`, `network`, `cloud`, `credential`, `credentials`, `plugin`, and `non-free` path components. |
| T-38-06 | Repudiation | License/provenance interpretation | mitigate | Manifest and docs cite source ref, upstream URLs, Phase 37 checklist source, and `metadata-only-not-legal-review`; no legal-review-complete wording is added. |
| T-38-07 | Denial of Service | Large raw fixture files in verifier/tests | accept | Files are about 1.6 MB total and static; checksum verification is bounded and no network or heavy parser execution is introduced. |
</threat_model>

<verification>
Run these checks after all tasks complete:

```bash
bazel run //packages/parity-fixtures:verify_prusa_profile_schema_fixture
bazel test //packages/parity-fixtures:verify_prusa_profile_schema_fixture_test
shfmt -d packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh
bazel query //packages/parity-fixtures:prusa_profile_schema_bundle
bash -lc 'if bazel query //packages/parity:prusaslicer_profile_schema_parity >/tmp/prusa-profile-schema-parity-query.out 2>&1; then cat /tmp/prusa-profile-schema-parity-query.out; exit 1; fi'
bash -lc 'if rg -n "fork\\.prusaslicer|prusaslicer\\.profile-schema|prusaslicer_profile_schema_parity" packages/parity/status.tsv; then exit 1; fi'
bash -lc 'for forbidden in bambustudio orca orcaslicer network cloud credential credentials plugin non-free; do if find packages/parity-fixtures/forks -path "*${forbidden}*" -print | grep -q .; then exit 1; fi; done'
git diff --check
```
</verification>

<success_criteria>
- EVID-01 is satisfied: the Prusa fixture namespace exists, update rules are
  documented, and static verification proves no forbidden fork/network/cloud/
  credential/plugin fixture namespace was introduced.
- EVID-02 is satisfied: `PrusaResearch.ini` and `PrusaResearch.idx` are
  checked in, exported by Bazel, trace to accepted source pin
  `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, and
  pass exact size/SHA-256 verification.
- EVID-03 is satisfied: docs reserve `fork.prusaslicer.profile-schema` for
  Phase 40 only, `packages/parity/status.tsv` has no Prusa row, and the planned
  Phase 40 parity command target does not exist.
- No Rust parsing or normalization logic is added.
- No `//packages/parity:prusaslicer_profile_schema_parity` or any executable
  Prusa parity command is added.
- No upstream source-tree import, vendor sync automation, profile auto-update
  execution, network/cloud/credential behavior, non-free plugin behavior, Bambu
  Studio fixtures, OrcaSlicer fixtures, GUI support, or fork release packaging
  is added.
- All verification commands pass.
</success_criteria>

<output>
After completion, create `.planning/phases/38-prusa-fixture-and-status-evidence-surface/38-01-SUMMARY.md`.
The summary frontmatter must use `requirements-completed: [EVID-01, EVID-02, EVID-03]` with the exact hyphenated key.
Do not run `mdformat` over phase `*-SUMMARY.md` files.
</output>
