---
phase: 37-prusa-baseline-and-checklist-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/prusa-baseline/BUILD.bazel
  - packages/prusa-baseline/README.md
  - packages/prusa-baseline/drift-refresh-record.md
  - packages/prusa-baseline/profile-schema-checklist.md
  - packages/prusa-baseline/verify_prusa_baseline.sh
  - packages/prusa-baseline/verify_prusa_baseline_test.sh
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/migration-guidance.md
  - docs/port/parity-matrix.md
autonomous: true
requirements: [PRUSA-01, PRUSA-02, PRUSA-03]
requirements_addressed: [PRUSA-01, PRUSA-02, PRUSA-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 37-2026-05-31T23-02-59
generated_at: 2026-05-31T23:18:08Z
must_haves:
  truths:
    - "Maintainer can inspect and run a Phase 37 PrusaSlicer drift-refresh review record tied to the accepted v1.9 Prusa source pin."
    - "Maintainer can inspect a completed Prusa profile-schema checklist record with all required checklist labels and visibly pending human-review fields."
    - "Maintainer can run a repo-owned Bazel verifier and failure-mode test that fail closed on missing accepted pin values, required labels, pending reviewer fields, and non-overclaiming wording."
    - "Maintainer can find the Prusa baseline package from the port control-plane docs and package map."
    - "Maintainer can confirm Phase 37 did not create Prusa fixtures, fork parity status rows, executable Prusa parity targets, upstream source imports, runtime fork support, GUI support, sync automation, or fork release packaging."
  artifacts:
    - path: "packages/prusa-baseline/BUILD.bazel"
      provides: "Bazel package boundary, verify target, failure-mode sh_test, and package_boundary filegroup"
      contains: "sh_binary"
    - path: "packages/prusa-baseline/README.md"
      provides: "Package entrypoint, supported verification command, and Phase 37 non-overclaiming boundary"
      contains: "Phase 37 verification does not prove Prusa runtime support"
    - path: "packages/prusa-baseline/drift-refresh-record.md"
      provides: "Accepted Prusa source baseline and maintainer drift-review fields"
      contains: "version_2.9.5"
    - path: "packages/prusa-baseline/profile-schema-checklist.md"
      provides: "Required checklist record for prusaslicer.profile-schema"
      contains: "prusaslicer.profile-schema"
    - path: "packages/prusa-baseline/verify_prusa_baseline.sh"
      provides: "Fail-closed exact-text verifier for records and boundary wording"
      contains: "set -euo pipefail"
    - path: "packages/prusa-baseline/verify_prusa_baseline_test.sh"
      provides: "Shell failure-mode tests for missing required Phase 37 content"
      contains: "test_missing"
    - path: "docs/port/README.md"
      provides: "Control-plane route to the Phase 37 Prusa baseline package"
      contains: "packages/prusa-baseline"
    - path: "docs/port/package-map.md"
      provides: "Package role entry for packages/prusa-baseline"
      contains: "packages/prusa-baseline"
    - path: "docs/port/migration-guidance.md"
      provides: "Short routing note that keeps Prusa fixture files in Phase 38 scope and executable parity/status publication in Phase 40 scope"
      contains: "packages/prusa-baseline"
    - path: "docs/port/parity-matrix.md"
      provides: "Human-facing parity vocabulary note that Phase 37 records do not create Prusa runtime support, executable fork parity, fork status rows, or Prusa fixture files"
      contains: "packages/prusa-baseline"
  key_links:
    - from: "packages/prusa-baseline/BUILD.bazel"
      to: "packages/prusa-baseline/verify_prusa_baseline.sh"
      via: "sh_binary(name = \"verify\")"
      pattern: "sh_binary\\("
    - from: "packages/prusa-baseline/verify_prusa_baseline.sh"
      to: "packages/prusa-baseline/drift-refresh-record.md"
      via: "Bazel data args or package-local default path"
      pattern: "drift-refresh-record\\.md"
    - from: "packages/prusa-baseline/verify_prusa_baseline.sh"
      to: "packages/prusa-baseline/profile-schema-checklist.md"
      via: "required checklist labels and exact Prusa source values"
      pattern: "profile-schema-checklist\\.md"
    - from: "docs/port/README.md"
      to: "packages/prusa-baseline/README.md"
      via: "package path and verify command in control-plane docs"
      pattern: "packages/prusa-baseline"
    - from: "docs/port/package-map.md"
      to: "packages/prusa-baseline"
      via: "package table role row"
      pattern: "packages/prusa-baseline"
    - from: "docs/port/migration-guidance.md"
      to: "packages/prusa-baseline/README.md"
      via: "baseline/checklist gate routing note"
      pattern: "packages/prusa-baseline"
    - from: "docs/port/parity-matrix.md"
      to: "packages/prusa-baseline/README.md"
      via: "fork parity interpretation note"
      pattern: "packages/prusa-baseline"
---

<objective>
Create the Phase 37 PrusaSlicer baseline and checklist gate package for the
narrow v1.10 profile schema/config evidence slice.

Purpose: Give maintainers a reviewed-input package before Prusa fixture,
Rust parser, or executable parity work begins.

Output: A dedicated `packages/prusa-baseline` package with record docs, a
fail-closed verifier, failure-mode shell tests, and discoverability from the
port control-plane docs.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-prusa-baseline-and-checklist-gate/37-CONTEXT.md
@.planning/phases/37-prusa-baseline-and-checklist-gate/37-RESEARCH.md
@.planning/milestones/v1.9-phases/36-parity-fixture-launcher-and-deferral-templates/36-01-SUMMARY.md
@.planning/milestones/v1.9-phases/36-parity-fixture-launcher-and-deferral-templates/36-02-SUMMARY.md
@.planning/milestones/v1.9-phases/36-parity-fixture-launcher-and-deferral-templates/36-VERIFICATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/fork-vendors/forks.tsv
@packages/fork-vendors/README.md
@packages/fork-vendors/BUILD.bazel
@packages/fork-inventories/prusaslicer.tsv
@packages/fork-inventories/README.md
@packages/fork-inventories/BUILD.bazel
@packages/fork-templates/BUILD.bazel
@packages/fork-templates/README.md
@packages/fork-templates/manual-drift-refresh-protocol.md
@packages/fork-templates/fork-parity-checklist-template.md
@packages/fork-templates/verify_templates.sh
@packages/fork-templates/verify_templates_test.sh
@docs/port/README.md
@docs/port/package-map.md
@docs/port/migration-guidance.md
@docs/port/parity-matrix.md

<interfaces>
Existing Bazel package pattern from `packages/fork-templates/BUILD.bazel`:

```python
package(default_visibility = ["//visibility:public"])

exports_files([
    "README.md",
    "fork-parity-checklist-template.md",
    "fork-launcher-shape-template.md",
    "manual-drift-refresh-protocol.md",
])

sh_binary(
    name = "verify",
    srcs = ["verify_templates.sh"],
    data = [
        "README.md",
        "fork-parity-checklist-template.md",
        "fork-launcher-shape-template.md",
        "manual-drift-refresh-protocol.md",
    ],
    args = [
        "$(location README.md)",
        "$(location fork-parity-checklist-template.md)",
        "$(location fork-launcher-shape-template.md)",
        "$(location manual-drift-refresh-protocol.md)",
    ],
)

sh_test(
    name = "verify_templates_test",
    srcs = ["verify_templates_test.sh"],
    data = ["verify_templates.sh"],
)
```

Existing verifier helper pattern from `packages/fork-templates/verify_templates.sh`:

```bash
require_text() {
	local file="$1"
	local label="$2"
	local pattern="$3"
	if ! grep -Fq "${pattern}" "${file}"; then
		error "${label}: missing required text: ${pattern}"
	fi
}
```

Accepted Prusa baseline values:

```text
Vendor ID: prusaslicer
Display name: PrusaSlicer
Upstream repo: https://github.com/prusa3d/PrusaSlicer
Selected stable tag: version_2.9.5
Peeled commit SHA: 9a583bd438b195856f3bcf7ea99b69ba4003a961
Default branch: master
Recorded observed branch head: 43f3cdb1a6f25ee8627f5f20b9a21f3e62c6ad9b
Source pin: prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
Profile schema source path: resources/profiles/PrusaResearch.ini
```
</interfaces>
</context>

<must_haves>
Truths:
- Maintainer can inspect and run the Prusa drift-refresh review record for the
  accepted v1.9 source pin.
- Maintainer can inspect the `prusaslicer.profile-schema` checklist record with
  the required field labels from the Phase 36 template.
- The verifier fails closed if required pin values, review fields, checklist
  labels, pending human-review text, or non-overclaiming phrases are removed.
- The port control plane routes maintainers to `packages/prusa-baseline`.
- Phase 37 creates no Prusa fixture files, no fork status rows, no executable
  Prusa parity target, no upstream source import, and no runtime fork support.

Artifacts:
- `packages/prusa-baseline/BUILD.bazel`
- `packages/prusa-baseline/README.md`
- `packages/prusa-baseline/drift-refresh-record.md`
- `packages/prusa-baseline/profile-schema-checklist.md`
- `packages/prusa-baseline/verify_prusa_baseline.sh`
- `packages/prusa-baseline/verify_prusa_baseline_test.sh`
- `docs/port/README.md`
- `docs/port/package-map.md`
- `docs/port/migration-guidance.md`
- `docs/port/parity-matrix.md`

Key links:
- `//packages/prusa-baseline:verify` reads the three checked-in package docs.
- `profile-schema-checklist.md` traces to `packages/fork-inventories/prusaslicer.tsv`.
- `drift-refresh-record.md` traces to `packages/fork-vendors/forks.tsv`.
- `docs/port/README.md` and `docs/port/package-map.md` point to the package and
  its verifier without calling Prusa behavior verified.
</must_haves>

<decision_coverage>
| Decision | Plan Task | Coverage |
| --- | --- | --- |
| D-01 | Task 1 | Full: creates dedicated `packages/prusa-baseline` package. |
| D-02 | Tasks 1, 2 | Full: review-gated docs plus local verifier only. |
| D-03 | Task 3 | Full: routes package from port docs and package map. |
| D-04 | Task 1 | Full: records exact Prusa tag, commit, and repo URL. |
| D-05 | Task 1 | Full: creates drift-review fields shaped by Phase 36 protocol. |
| D-06 | Tasks 1, 2 | Full: requires branch drift as observation only and unchanged accepted pins wording. |
| D-07 | Task 1 | Full: checklist record is `prusaslicer.profile-schema`. |
| D-08 | Tasks 1, 2 | Full: checklist includes and verifier enforces all required labels. |
| D-09 | Task 1 | Full: points candidate module to shared Rust profile/config boundary. |
| D-10 | Task 1 | Full: names planned fixture namespace and parity command as not created in Phase 37. |
| D-11 | Tasks 1, 3 | Full: package docs and port docs state narrow profile/config scope and deferred surfaces. |
| D-12 | Task 2 | Full: verifier fails closed on required fields, pins, labels, pending human-review text, and non-overclaiming phrases. |
</decision_coverage>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Prusa baseline records and package skeleton</name>
  <files>packages/prusa-baseline/BUILD.bazel, packages/prusa-baseline/README.md, packages/prusa-baseline/drift-refresh-record.md, packages/prusa-baseline/profile-schema-checklist.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .planning/phases/37-prusa-baseline-and-checklist-gate/37-CONTEXT.md
    .planning/phases/37-prusa-baseline-and-checklist-gate/37-RESEARCH.md
    packages/fork-vendors/forks.tsv
    packages/fork-inventories/prusaslicer.tsv
    packages/fork-templates/BUILD.bazel
    packages/fork-templates/README.md
    packages/fork-templates/manual-drift-refresh-protocol.md
    packages/fork-templates/fork-parity-checklist-template.md
  </read_first>
  <action>
    Create `packages/prusa-baseline/` as the dedicated Phase 37 package per D-01.

    Before writing `drift-refresh-record.md`, run `bazel run //packages/fork-vendors:verify` and use its current Prusa output in the record:
    - if the Prusa selected tag and peeled commit still verify, record selected stable tag confirmation as `confirmed by bazel run //packages/fork-vendors:verify during Phase 37 execution`
    - record peeled commit confirmation as `confirmed by bazel run //packages/fork-vendors:verify during Phase 37 execution`
    - record branch drift observation as `none observed during Phase 37 execution` when no Prusa branch-drift warning appears, or copy the current Prusa branch-drift warning text if one appears
    - keep reviewer decision and reviewer signoff as pending human-gated fields

    Create `packages/prusa-baseline/BUILD.bazel` first with:
    - `package(default_visibility = ["//visibility:public"])`
    - `exports_files(["README.md", "drift-refresh-record.md", "profile-schema-checklist.md"])`
    - `filegroup(name = "package_boundary", srcs = ["README.md", "drift-refresh-record.md", "profile-schema-checklist.md"])`

    Create `README.md` with:
    - title `# Prusa Baseline and Checklist Gate`
    - package purpose: Phase 37 review-gated Prusa baseline and checklist records for the narrow v1.10 Prusa profile schema/config evidence slice
    - command text `bazel run //packages/prusa-baseline:verify`
    - exact non-overclaiming phrases:
      - `Phase 37 verification does not prove Prusa runtime support.`
      - `Phase 37 verification does not prove executable fork parity.`
      - `source pins, inventories, checklist records, and Prusa baseline records are planning inputs only.`
      - `future executable parity evidence required`
    - exact scope-control sentence: `This package creates no Prusa fixture files, fork parity status rows, executable Prusa parity targets, upstream source imports, vendored fork source trees, automatic sync, runtime fork support, GUI support, network/device integration, profile auto-update execution, or fork release packaging.`
    - links to `drift-refresh-record.md`, `profile-schema-checklist.md`, `packages/fork-vendors/forks.tsv`, `packages/fork-inventories/prusaslicer.tsv`, and `docs/port/README.md`.

    Create `drift-refresh-record.md` with exact accepted source values from D-04:
    - upstream repo `https://github.com/prusa3d/PrusaSlicer`
    - selected stable tag `version_2.9.5`
    - peeled commit `9a583bd438b195856f3bcf7ea99b69ba4003a961`
    - recorded observed branch head `43f3cdb1a6f25ee8627f5f20b9a21f3e62c6ad9b`
    - verifier command `bazel run //packages/fork-vendors:verify`
    - table fields: `Review date`, `Vendor`, `Upstream repo`, `Selected stable tag`, `Selected stable tag confirmation`, `Peeled commit`, `Peeled commit confirmation`, `Branch drift observation`, `Reviewer decision`, `Reviewer signoff`
    - pending human-review field values:
      - `PENDING - human reviewer UTC date required before implementation consumes this gate.`
      - `PENDING - human reviewer must choose keep accepted source pin, plan future intake update, or defer before implementation consumes this gate.`
      - `PENDING - human reviewer name and UTC date required before implementation consumes this gate.`
    - exact D-06 sentence: `accepted source pins remain unchanged unless a future reviewed intake update modifies packages/fork-vendors/forks.tsv`
    - exact drift sentence: `Branch-head data is drift-only observation.`

    Create `profile-schema-checklist.md` with the required Phase 36 checklist labels and the Phase 37 Prusa record:
    - `Inventory row ID` = `prusaslicer.profile-schema`
    - `Source pin` = `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`
    - `Candidate Rust module` = `packages/slic3r-rust shared profile/config boundary for Phase 39; exact crate or module name deferred by the roadmap. No Prusa-only Rust workspace and no copied upstream source tree.`
    - `Fixture need` = `Planned Phase 38 namespace packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/...; not created in Phase 37.`
    - `Evidence command` = `Planned Phase 40 command bazel run //packages/parity:prusaslicer_profile_schema_parity; not created in Phase 37.`
    - `Docs touched` = `docs/port/README.md; docs/port/package-map.md; docs/port/migration-guidance.md; docs/port/parity-matrix.md`
    - `License or security note` = `AGPL-3.0-only; metadata-only-not-legal-review; no network, cloud, credential, profile auto-update, plugin ingestion, or runtime support scope in Phase 37.`
    - `Deferred scope` must name: `Prusa project files`, `STEP import`, `support generation`, `arc fitting`, `wall seam behavior`, `network/device integration`, `profile auto-update execution`, `full fork runtime support`, `GUI support`, `fork release builds`, `sync automation`, `upstream source imports`, `Prusa fixtures`, and `executable Prusa parity commands`.
    - `Reviewer signoff` = `PENDING - human reviewer name and UTC date required before implementation consumes this gate.`
    - include source path `resources/profiles/PrusaResearch.ini`, feature surface `profile-schema`, ownership `fork-specific`, complexity `medium`, parity dependency `config;config.persistence`, and v1.9 decision `future-candidate`.
  </action>
  <verify>
    <automated>bash -lc 'test -f packages/prusa-baseline/BUILD.bazel; test -f packages/prusa-baseline/README.md; test -f packages/prusa-baseline/drift-refresh-record.md; test -f packages/prusa-baseline/profile-schema-checklist.md; grep -F "Phase 37 verification does not prove Prusa runtime support." packages/prusa-baseline/README.md; grep -F "version_2.9.5" packages/prusa-baseline/drift-refresh-record.md; grep -F "9a583bd438b195856f3bcf7ea99b69ba4003a961" packages/prusa-baseline/drift-refresh-record.md; grep -F "| Inventory row ID |" packages/prusa-baseline/profile-schema-checklist.md; grep -F "prusaslicer.profile-schema" packages/prusa-baseline/profile-schema-checklist.md'</automated>
  </verify>
  <acceptance_criteria>
    - `packages/prusa-baseline/BUILD.bazel` exists and exports the three Markdown records.
    - `README.md` contains all four exact non-overclaiming phrases listed in the action.
    - `drift-refresh-record.md` contains `https://github.com/prusa3d/PrusaSlicer`, `version_2.9.5`, `9a583bd438b195856f3bcf7ea99b69ba4003a961`, `Reviewer decision`, `Reviewer signoff`, and the exact unchanged-pins sentence.
    - `profile-schema-checklist.md` contains all nine required checklist labels and the exact source pin `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`.
    - No files are created under `packages/parity-fixtures/forks/`, and `packages/parity/status.tsv` is not modified.
  </acceptance_criteria>
  <done>The dedicated Prusa baseline package skeleton and two review records exist, materially cover PRUSA-01, PRUSA-02, and PRUSA-03 inputs, and do not create Phase 38-40 implementation artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fail-closed verifier and failure-mode tests</name>
  <files>packages/prusa-baseline/BUILD.bazel, packages/prusa-baseline/verify_prusa_baseline.sh, packages/prusa-baseline/verify_prusa_baseline_test.sh</files>
  <read_first>
    .planning/phases/37-prusa-baseline-and-checklist-gate/37-CONTEXT.md
    .planning/phases/37-prusa-baseline-and-checklist-gate/37-RESEARCH.md
    packages/prusa-baseline/README.md
    packages/prusa-baseline/drift-refresh-record.md
    packages/prusa-baseline/profile-schema-checklist.md
    packages/fork-templates/BUILD.bazel
    packages/fork-templates/verify_templates.sh
    packages/fork-templates/verify_templates_test.sh
  </read_first>
  <behavior>
    - Test 1: a complete temporary fixture with required Prusa pins, labels, pending human-review text, and non-overclaiming phrases passes.
    - Test 2: missing `version_2.9.5` fails with an error mentioning `version_2.9.5`.
    - Test 3: missing `9a583bd438b195856f3bcf7ea99b69ba4003a961` fails with an error mentioning that SHA.
    - Test 4: missing checklist label `Evidence command` fails with an error mentioning `Evidence command`.
    - Test 5: missing `PENDING - human reviewer name and UTC date required before implementation consumes this gate.` fails.
    - Test 6: missing `Phase 37 verification does not prove executable fork parity.` fails.
    - Test 7: missing `not created in Phase 37` from fixture or evidence-command entries fails.
  </behavior>
  <action>
    Write `verify_prusa_baseline_test.sh` first using the Phase 36 shell-test pattern:
    - shebang `#!/usr/bin/env bash`
    - `set -euo pipefail`
    - Bazel `TEST_SRCDIR`/`TEST_WORKSPACE` resolver for `packages/prusa-baseline/verify_prusa_baseline.sh`
    - `mktemp -d` with cleanup trap
    - helpers `fail`, `assert_contains`, `remove_line_containing`, `write_valid_fixture`, and `run_verifier`
    - explicit Arrange/Act/Assert comments in each non-trivial test
    - success output `ok: verify_prusa_baseline_test`

    Then write `verify_prusa_baseline.sh`:
    - shebang `#!/usr/bin/env bash`
    - `set -euo pipefail`
    - accept either zero args, using package-local defaults, or exactly three args in this order: README, drift-refresh record, profile-schema checklist
    - fail for any other arg count with `usage: verify_prusa_baseline.sh [README drift-refresh-record profile-schema-checklist]`
    - implement `error`, `require_file`, `require_text`
    - implement focused functions:
      - `verify_accepted_baseline`
      - `verify_drift_record`
      - `verify_checklist_labels`
      - `verify_checklist_values`
      - `verify_non_overclaiming_phrases`
      - `verify_future_only_boundaries`
    - check exact accepted values: `https://github.com/prusa3d/PrusaSlicer`, `version_2.9.5`, `9a583bd438b195856f3bcf7ea99b69ba4003a961`, `43f3cdb1a6f25ee8627f5f20b9a21f3e62c6ad9b`, and `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`
    - check exact checklist labels: `Inventory row ID`, `Source pin`, `Candidate Rust module`, `Fixture need`, `Evidence command`, `Docs touched`, `License or security note`, `Deferred scope`, and `Reviewer signoff`
    - check exact pending human-review phrase: `PENDING - human reviewer name and UTC date required before implementation consumes this gate.`
    - check D-06 exact wording: `accepted source pins remain unchanged unless a future reviewed intake update modifies packages/fork-vendors/forks.tsv`
    - check exact non-overclaiming phrases from Task 1
    - check each deferred scope term named in Task 1
    - check `not created in Phase 37` appears in both the fixture need and evidence command context by requiring that phrase in `profile-schema-checklist.md`
    - never execute or `eval` checklist evidence command text
    - success output `ok: Prusa baseline verification passed`

    Update `BUILD.bazel` to match the Phase 36 package style:
    - keep `exports_files` for the three Markdown records
    - add `sh_binary(name = "verify", srcs = ["verify_prusa_baseline.sh"], data = ["README.md", "drift-refresh-record.md", "profile-schema-checklist.md"], args = ["$(location README.md)", "$(location drift-refresh-record.md)", "$(location profile-schema-checklist.md)"])`
    - add `sh_test(name = "verify_prusa_baseline_test", srcs = ["verify_prusa_baseline_test.sh"], data = ["verify_prusa_baseline.sh"])`
    - update `filegroup(name = "package_boundary")` to include all six package-owned files.
  </action>
  <verify>
    <automated>bazel run //packages/prusa-baseline:verify</automated>
    <automated>bazel test //packages/prusa-baseline:verify_prusa_baseline_test</automated>
    <automated>shfmt -d packages/prusa-baseline/verify_prusa_baseline.sh packages/prusa-baseline/verify_prusa_baseline_test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bazel run //packages/prusa-baseline:verify` prints `ok: Prusa baseline verification passed`.
    - `bazel test //packages/prusa-baseline:verify_prusa_baseline_test` passes.
    - `verify_prusa_baseline.sh` contains `set -euo pipefail`, has no `eval`, and has no `bash -c`.
    - `verify_prusa_baseline_test.sh` contains tests named for missing accepted tag, missing peeled commit, missing checklist label, missing pending human-review text, missing non-overclaiming phrase, and missing Phase 37 future-only wording.
    - `shfmt -d packages/prusa-baseline/verify_prusa_baseline.sh packages/prusa-baseline/verify_prusa_baseline_test.sh` exits 0 with no diff.
  </acceptance_criteria>
  <done>The verifier and test target fail closed on the Phase 37 record requirements in D-12 while staying local-only and wording-based per D-02.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Route package from port docs and run full gate verification</name>
  <files>docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/37-prusa-baseline-and-checklist-gate/37-CONTEXT.md
    .planning/phases/37-prusa-baseline-and-checklist-gate/37-RESEARCH.md
    packages/prusa-baseline/README.md
    docs/port/README.md
    docs/port/package-map.md
    docs/port/migration-guidance.md
    docs/port/parity-matrix.md
    packages/fork-vendors/README.md
    packages/fork-inventories/README.md
  </read_first>
  <action>
    Update `docs/port/README.md` with a `## Current Prusa Baseline Gate State` section after the current fork template package state. The section must mention:
    - `packages/prusa-baseline`
    - `bazel run //packages/prusa-baseline:verify`
    - `version_2.9.5`
    - `9a583bd438b195856f3bcf7ea99b69ba4003a961`
    - `prusaslicer.profile-schema`
    - `Phase 37 verification does not prove Prusa runtime support`
    - `Phase 37 verification does not prove executable fork parity`
    - no Prusa fixture files, no fork parity status rows, no executable Prusa parity target, no upstream source imports, no runtime fork support, no GUI support, no sync automation, and no fork release packaging.

    Update `docs/port/package-map.md`:
    - add a package table row for `packages/prusa-baseline` with role text: `Phase 37 PrusaSlicer baseline and checklist gate package for the narrow v1.10 profile schema/config evidence slice`
    - add a phase note after the Phase 36 note saying Phase 37 adds `packages/prusa-baseline`, records the accepted Prusa baseline `version_2.9.5` at `9a583bd438b195856f3bcf7ea99b69ba4003a961`, records the `prusaslicer.profile-schema` checklist gate, and keeps fixtures, parity status rows, executable parity commands, runtime fork support, GUI support, sync automation, and fork release packaging out of Phase 37.

    Update `docs/port/migration-guidance.md` in the future fork parity status area with one short paragraph:
    - link the Phase 37 package path
    - state that the package is a baseline/checklist gate only
    - state that fixture files remain Phase 38 scope and executable parity/status publication remains Phase 40 scope.

    Update `docs/port/parity-matrix.md` in `## Fork Parity Interpretation` with one short bullet:
    - mention `packages/prusa-baseline`
    - state that Phase 37 records do not create Prusa runtime support, executable fork parity, fork status rows, or Prusa fixture files.

    Do not edit `packages/parity/status.tsv`. Do not create `packages/parity-fixtures/forks/`. Do not create `//packages/parity:prusaslicer_profile_schema_parity` or any Prusa fixture, parser, runtime, GUI, network, sync, or packaging implementation.
  </action>
  <verify>
    <automated>bazel run //packages/prusa-baseline:verify</automated>
    <automated>bazel test //packages/prusa-baseline:verify_prusa_baseline_test</automated>
    <automated>bazel run //packages/fork-vendors:verify</automated>
    <automated>bazel run //packages/fork-inventories:verify</automated>
    <automated>shfmt -d packages/prusa-baseline/verify_prusa_baseline.sh packages/prusa-baseline/verify_prusa_baseline_test.sh</automated>
    <automated>bash -lc 'grep -F "packages/prusa-baseline" docs/port/README.md; grep -F "packages/prusa-baseline" docs/port/package-map.md; test ! -e packages/parity-fixtures/forks/prusaslicer; if rg -n "prusaslicer_profile_schema_parity|fork\\.prusaslicer|prusaslicer.profile-schema" packages/parity/status.tsv; then exit 1; fi; if bazel query //packages/parity:prusaslicer_profile_schema_parity >/tmp/prusa-baseline-query.out 2>&1; then cat /tmp/prusa-baseline-query.out; exit 1; fi'</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `docs/port/README.md` contains `packages/prusa-baseline`, `bazel run //packages/prusa-baseline:verify`, `version_2.9.5`, and `Phase 37 verification does not prove executable fork parity`.
    - `docs/port/package-map.md` contains a `packages/prusa-baseline` package table row and a Phase 37 note.
    - `docs/port/migration-guidance.md` states fixture files remain Phase 38 scope and executable parity/status publication remains Phase 40 scope.
    - `docs/port/parity-matrix.md` states Phase 37 records do not create Prusa runtime support, executable fork parity, fork status rows, or Prusa fixture files.
    - Required commands in the verify block pass.
    - `packages/parity/status.tsv` is unmodified and contains no `prusaslicer.profile-schema` or `prusaslicer_profile_schema_parity` row.
    - `packages/parity-fixtures/forks/prusaslicer` does not exist.
  </acceptance_criteria>
  <done>The Prusa baseline gate is discoverable from the port control plane and the full Phase 37 verification surface passes without expanding into Phase 38-40 implementation scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Maintainer-edited Markdown records to verifier | Local records are editable text and must be treated as untrusted until exact required values and labels are checked. |
| Vendor and inventory source data to Phase 37 package | Accepted Prusa source identity comes from checked-in TSVs, not from branch heads or executor memory. |
| Checklist command text to shell verifier | Evidence command strings in the checklist are display-only planning text and must never be executed by the verifier. |
| Human review state to later implementation phases | Pending reviewer fields must not be represented as completed signoff by an automated executor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-37-01 | Tampering | `packages/prusa-baseline/drift-refresh-record.md` | mitigate | `verify_prusa_baseline.sh` must require exact Prusa repo URL, selected tag, peeled commit, source pin, branch-drift-only wording, and unchanged accepted-pin wording. |
| T-37-02 | Tampering | `packages/prusa-baseline/profile-schema-checklist.md` | mitigate | Verifier must require every Phase 36 checklist label plus the exact `prusaslicer.profile-schema` row values and deferred scope terms. |
| T-37-03 | Repudiation | Human review fields in drift and checklist records | mitigate | Verifier must require explicit `PENDING - human reviewer name and UTC date required before implementation consumes this gate.` text so the agent cannot imply human signoff. |
| T-37-04 | Elevation of Privilege | Checklist evidence command string | mitigate | Verifier must use `grep -F` exact-text checks only and must not contain `eval` or `bash -c`; checklist command text is not executed. |
| T-37-05 | Spoofing | Branch-head drift observation represented as accepted source | mitigate | Drift record and verifier must state branch-head data is drift-only observation and accepted pins change only through future reviewed intake updates to `packages/fork-vendors/forks.tsv`. |
| T-37-06 | Information Disclosure | Network, credential, cloud, plugin, and profile auto-update surfaces | accept | Phase 37 creates local Markdown and shell verification only; no credentials, cloud calls, online integration, plugin ingestion, or profile auto-update execution are introduced. |
</threat_model>

<verification>
Run these checks after all tasks complete:

```bash
bazel run //packages/prusa-baseline:verify
bazel test //packages/prusa-baseline:verify_prusa_baseline_test
bazel run //packages/fork-vendors:verify
bazel run //packages/fork-inventories:verify
shfmt -d packages/prusa-baseline/verify_prusa_baseline.sh packages/prusa-baseline/verify_prusa_baseline_test.sh
bash -lc 'grep -F "packages/prusa-baseline" docs/port/README.md; grep -F "packages/prusa-baseline" docs/port/package-map.md; test ! -e packages/parity-fixtures/forks/prusaslicer; if rg -n "prusaslicer_profile_schema_parity|fork\\.prusaslicer|prusaslicer.profile-schema" packages/parity/status.tsv; then exit 1; fi; if bazel query //packages/parity:prusaslicer_profile_schema_parity >/tmp/prusa-baseline-query.out 2>&1; then cat /tmp/prusa-baseline-query.out; exit 1; fi'
git diff --check
```
</verification>

<success_criteria>
- `packages/prusa-baseline` exists with a Bazel `verify` target, failure-mode
  shell test target, README, drift-refresh record, profile-schema checklist,
  verifier script, and verifier test script.
- PRUSA-01 is satisfied by the accepted Prusa drift-refresh record, manual
  verifier command, branch-drift observation field, reviewer decision field, and
  reviewer signoff field.
- PRUSA-02 is satisfied by the `prusaslicer.profile-schema` checklist record
  containing all required Phase 36 checklist labels and exact source pin.
- PRUSA-03 is satisfied by package and docs wording that separates the narrow
  v1.10 profile schema/config gate from deferred project-file, STEP import,
  support generation, arc fitting, wall seam, network/device, full runtime, GUI,
  release, sync, fixture, and executable parity work.
- All verification commands pass.
- No Phase 38-40 artifacts are created: no Prusa fixture files, no Prusa parity
  status rows, no executable Prusa parity target, no upstream source import, no
  runtime fork support, no GUI support, no network/device integration, no sync
  automation, and no fork release packaging.
</success_criteria>

<output>
After completion, create `.planning/phases/37-prusa-baseline-and-checklist-gate/37-01-SUMMARY.md`.
The summary frontmatter must use `requirements-completed: [PRUSA-01, PRUSA-02, PRUSA-03]` with the exact hyphenated key.
Do not run `mdformat` over phase `*-SUMMARY.md` files.
</output>
