---
phase: 31-create-github-actions-workflows-that-create-release-builds-f
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .github/workflows/release-build-artifacts.yml
  - tools/release/build_release_artifact.sh
  - docs/port/release-build-automation.md
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/migration-guidance.md
  - packages/launcher/README.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - REL-03
  - REL-04
  - REL-05
  - REL-06
user_setup: []
must_haves:
  truths:
    - "Maintainer can inspect a GitHub Actions workflow that builds macOS, Linux, and Windows base Slic3r release artifacts."
    - "The workflow runs the matching packaged launcher parity target for each platform before uploading artifacts."
    - "The release artifact script writes provenance containing platform, commit, ref, build mode, package scope, package target, and evidence target."
    - "Docs describe the release build artifact outputs and keep signing, notarization, installers, AppImage, MSI, DMG, GUI packaging, fork-flavor builds, and release-channel publishing out of scope."
    - "Phase 31 requirements and roadmap state are updated after implementation."
  artifacts:
    - path: ".github/workflows/release-build-artifacts.yml"
      provides: "GitHub Actions matrix release artifact workflow"
      contains: "windows-latest"
    - path: "tools/release/build_release_artifact.sh"
      provides: "Repo-owned platform release artifact builder"
      contains: "release-provenance.txt"
    - path: "docs/port/release-build-automation.md"
      provides: "Maintainer-facing release build automation documentation"
      contains: "workflow_dispatch"
    - path: ".planning/phases/31-create-github-actions-workflows-that-create-release-builds-f/31-01-SUMMARY.md"
      provides: "Phase 31 closeout summary"
      contains: "requirements-completed"
  key_links:
    - from: ".github/workflows/release-build-artifacts.yml"
      to: "tools/release/build_release_artifact.sh"
      via: "workflow run step"
      pattern: "tools/release/build_release_artifact.sh"
    - from: "tools/release/build_release_artifact.sh"
      to: "//packages/parity:linux_packaged_launcher_parity"
      via: "Linux evidence target"
      pattern: "//packages/parity:linux_packaged_launcher_parity"
    - from: "tools/release/build_release_artifact.sh"
      to: "//packages/launcher:windows_packaged_launcher_tree"
      via: "Windows package target"
      pattern: "//packages/launcher:windows_packaged_launcher_tree"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 31-2026-05-24T13-42-03
generated_at: 2026-05-24T13:42:03Z
---

<objective>
Create cross-platform release build automation for the base Rust-backed Slic3r
package.

Purpose: Close REL-03 through REL-06 by wiring GitHub Actions to the existing
Rust/Bazel package and parity surfaces, adding provenance, and documenting the
supported release-build artifact boundary.
Output: GitHub Actions workflow, repo-owned release artifact script, docs, and
Phase 31 planning closeout.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-create-github-actions-workflows-that-create-release-builds-f/31-CONTEXT.md
@.planning/phases/31-create-github-actions-workflows-that-create-release-builds-f/31-RESEARCH.md
@packages/launcher/BUILD.bazel
@packages/parity/BUILD.bazel
@docs/port/migration-guidance.md
@packages/launcher/README.md

Bright Builds context used for this plan: local AGENTS guidance,
AGENTS.bright-builds.md, standards-overrides.md, and repo codebase
verification/documentation discipline. Local repo guidance forbids running
mdformat on phase *-SUMMARY.md files and requires the exact
requirements-completed summary key.

<interfaces>
Existing package targets:
```text
//packages/launcher:macos_packaged_launcher_bundle
//packages/launcher:linux_packaged_launcher_tree
//packages/launcher:windows_packaged_launcher_tree
```

Existing evidence targets:
```text
//packages/parity:macos_packaged_launcher_parity
//packages/parity:linux_packaged_launcher_parity
//packages/parity:windows_packaged_launcher_parity
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repo-owned release artifact script</name>
  <files>tools/release/build_release_artifact.sh</files>
  <action>Create a Bash script that accepts a platform (`macos`, `linux`, `windows`, or `all`) and optional output root, runs the matching packaged launcher parity target with Bazel opt mode, materializes the matching package tree, writes `release-provenance.txt`, creates a tar archive, and writes a platform manifest. Keep generated output under `.planning/.tmp/release-builds` by default.</action>
  <verify>
    <automated>bash -n tools/release/build_release_artifact.sh</automated>
    <automated>bash tools/release/build_release_artifact.sh macos .planning/.tmp/phase31-release-smoke</automated>
  </verify>
  <done>The script produces a macOS release artifact archive and provenance locally while naming all three platform package/evidence labels.</done>
</task>

<task type="auto">
  <name>Task 2: Add GitHub Actions release artifact workflow</name>
  <files>.github/workflows/release-build-artifacts.yml</files>
  <action>Add a matrix workflow for `macos-latest`, `ubuntu-latest`, and `windows-latest`, using Bash, checkout, Bazelisk setup, the repo-owned release artifact script, and upload-artifact. Trigger manually and on version tags. Keep permissions to `contents: read`.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const workflow = fs.readFileSync('.github/workflows/release-build-artifacts.yml', 'utf8');
for (const needle of ['macos-latest', 'ubuntu-latest', 'windows-latest', 'workflow_dispatch', 'tools/release/build_release_artifact.sh', 'actions/upload-artifact@v4', 'contents: read']) {
  if (!workflow.includes(needle)) throw new Error(`workflow missing ${needle}`);
}
NODE</automated>
  </verify>
  <done>The workflow can build and upload base release artifacts for every supported platform without publishing a GitHub Release.</done>
</task>

<task type="auto">
  <name>Task 3: Publish release build automation docs</name>
  <files>docs/port/release-build-automation.md, docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, packages/launcher/README.md</files>
  <action>Document the workflow, local script, artifact contents, provenance fields, evidence reuse, and unsupported release/distribution surfaces. Link the new doc from the port docs index and package map. Update launcher docs to mention Phase 31 release build artifact automation.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const checks = [
  ['docs/port/release-build-automation.md', ['Release Build Automation', 'workflow_dispatch', 'release-provenance.txt', '//packages/parity:linux_packaged_launcher_parity', 'signing']],
  ['docs/port/README.md', ['release-build-automation.md']],
  ['docs/port/package-map.md', ['Phase 31', 'release-build-artifacts.yml']],
  ['packages/launcher/README.md', ['Phase 31', 'release artifact']]
];
for (const [file, needles] of checks) {
  const text = fs.readFileSync(file, 'utf8');
  for (const needle of needles) {
    if (!text.includes(needle)) throw new Error(`${file} missing ${needle}`);
  }
}
NODE</automated>
  </verify>
  <done>Maintainers can discover what the release workflow builds, how to run it, and what remains explicitly out of scope.</done>
</task>

<task type="auto">
  <name>Task 4: Close Phase 31 planning state</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/31-create-github-actions-workflows-that-create-release-builds-f/31-01-SUMMARY.md, .planning/phases/31-create-github-actions-workflows-that-create-release-builds-f/31-VERIFICATION.md</files>
  <action>Mark Phase 31 requirements complete after verification, update roadmap/state progress, write the plan summary with `requirements-completed: [REL-03, REL-04, REL-05, REL-06]`, and write a verification report with `status: passed` only if the must-haves are verified.</action>
  <verify>
    <automated>node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 31 --require-plans --require-verification --raw</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Lifecycle validation passes for Phase 31 and the wrapper can safely commit/push.</done>
</task>

</tasks>
