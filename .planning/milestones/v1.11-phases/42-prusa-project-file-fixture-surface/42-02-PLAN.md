---
phase: 42-prusa-project-file-fixture-surface
plan: "02"
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - packages/parity-fixtures/BUILD.bazel
  - packages/parity-fixtures/verify_prusa_project_file_fixture.sh
  - packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
autonomous: true
requirements: [PFIX-02]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 42-2026-06-03T20-35-51
generated_at: 2026-06-03T20:49:37Z
must_haves:
  truths:
    - "Maintainer can run a repo-owned project-file fixture verifier through Bash and Bazel."
    - "The verifier fails when fixture bytes, provenance, expected artifact columns/rows, update route text, scope traceability, or non-overclaiming boundary text are missing or inconsistent."
    - "The verifier guards against premature Phase 43 parser, Phase 44 parity target, and Phase 44 status row artifacts."
  artifacts:
    - path: "packages/parity-fixtures/BUILD.bazel"
      provides: "Project-file fixture aliases, bundle, verifier binary, and verifier test target"
      contains: "verify_prusa_project_file_fixture"
    - path: "packages/parity-fixtures/verify_prusa_project_file_fixture.sh"
      provides: "Fail-closed fixture verifier"
      contains: "ok: Prusa project-file fixture verification passed"
    - path: "packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh"
      provides: "Failure-mode verifier tests"
      contains: "wrong project-file checksum"
  key_links:
    - from: "packages/parity-fixtures/BUILD.bazel"
      to: "packages/parity-fixtures/verify_prusa_project_file_fixture.sh"
      via: "sh_binary srcs"
      pattern: "verify_prusa_project_file_fixture\\.sh"
    - from: "packages/parity-fixtures/verify_prusa_project_file_fixture.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv"
      via: "expected artifact checks"
      pattern: "expected-project-summary\\.tsv"
    - from: "packages/parity-fixtures/verify_prusa_project_file_fixture.sh"
      to: "packages/parity/status.tsv"
      via: "negative status-row guard"
      pattern: "fork\\.prusaslicer\\.project-file"
---

<objective>
Wire the project-file fixture surface into repo-owned Bazel targets and add a local fail-closed verifier with failure-mode tests.

Purpose: PFIX-02 requires maintainers to prove the fixture surface fails closed when required provenance, expected artifacts, update rules, scope text, or non-overclaiming guards drift.
Output: `verify_prusa_project_file_fixture`, `verify_prusa_project_file_fixture_test`, project-file fixture aliases/filegroup, and passing Bash/Bazel verification.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-CONTEXT.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-RESEARCH.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-01-SUMMARY.md
@packages/parity-fixtures/BUILD.bazel
@packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh
@packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh
@packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh
@packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh
@packages/parity/status.tsv

<interfaces>
From `packages/parity-fixtures/BUILD.bazel`, keep the existing fixture pattern:

```python
sh_binary(
    name = "verify_prusa_profile_schema_fixture",
    srcs = ["verify_prusa_profile_schema_fixture.sh"],
    data = [
        ":prusa_profile_schema_bundle",
        "//packages/parity:status.tsv",
    ],
)

sh_test(
    name = "verify_prusa_profile_schema_fixture_test",
    srcs = ["verify_prusa_profile_schema_fixture_test.sh"],
    data = [
        "verify_prusa_profile_schema_fixture.sh",
        ":prusa_profile_schema_bundle",
        "//packages/parity:status.tsv",
    ],
)
```

For Phase 42, create analogous project-file target names per D-11: `prusa_project_file_bundle`, `verify_prusa_project_file_fixture`, and `verify_prusa_project_file_fixture_test`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export project-file fixture aliases and bundle</name>
  <read_first>
    - .planning/phases/42-prusa-project-file-fixture-surface/42-CONTEXT.md
    - .planning/phases/42-prusa-project-file-fixture-surface/42-01-SUMMARY.md
    - packages/parity-fixtures/BUILD.bazel
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/.gitattributes
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/seam_test_object.3mf
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/fixture-provenance.tsv
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv
  </read_first>
  <files>packages/parity-fixtures/BUILD.bazel</files>
  <action>
    Modify `packages/parity-fixtures/BUILD.bazel` per D-11. Add the five project-file namespace artifacts to the existing `exports_files([...])` list: `.gitattributes`, `README.md`, `expected-project-summary.tsv`, `fixture-provenance.tsv`, and `seam_test_object.3mf`.

    Add aliases named exactly `prusa_project_file_readme`, `prusa_project_file_provenance`, `prusa_project_file_expected_project_summary`, and `prusa_project_file_seam_test_object`, each pointing at its matching file under `forks/prusaslicer/prusaslicer.project-file/`.

    Add `filegroup(name = "prusa_project_file_bundle", srcs = [...])` containing exactly the five namespace artifacts. Add the same five artifacts to the existing `package_boundary` filegroup. Do not add or modify `packages/parity/status.tsv`, `packages/parity/BUILD.bazel`, or any Rust source file in this task.
  </action>
  <verify>
    <automated>bazel query //packages/parity-fixtures:prusa_project_file_bundle && bazel query //packages/parity-fixtures:prusa_project_file_expected_project_summary && bazel query //packages/parity-fixtures:prusa_project_file_seam_test_object</automated>
  </verify>
  <acceptance_criteria>
    - `bazel query //packages/parity-fixtures:prusa_project_file_bundle` prints `//packages/parity-fixtures:prusa_project_file_bundle`.
    - `bazel query //packages/parity-fixtures:prusa_project_file_expected_project_summary` prints `//packages/parity-fixtures:prusa_project_file_expected_project_summary`.
    - `rg -n 'prusa_project_file_bundle|prusa_project_file_expected_project_summary|prusa_project_file_seam_test_object' packages/parity-fixtures/BUILD.bazel` finds all three names.
    - `git diff -- packages/parity/status.tsv packages/parity/BUILD.bazel packages/slic3r-rust` shows no Phase 42 changes.
  </acceptance_criteria>
  <done>The project-file fixture files are Bazel-visible through stable aliases and a bundle, without publishing parity/status/Rust surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fail-closed project-file fixture verifier and tests</name>
  <read_first>
    - .planning/phases/42-prusa-project-file-fixture-surface/42-CONTEXT.md
    - .planning/phases/42-prusa-project-file-fixture-surface/42-RESEARCH.md
    - packages/parity-fixtures/BUILD.bazel
    - packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh
    - packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh
    - packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh
    - packages/parity/status.tsv
    - packages/parity-fixtures/verify_prusa_project_file_fixture.sh
    - packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
  </read_first>
  <files>packages/parity-fixtures/BUILD.bazel, packages/parity-fixtures/verify_prusa_project_file_fixture.sh, packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh</files>
  <behavior>
    - Test 1: A complete project-file fixture copy passes and prints `ok: Prusa project-file fixture verification passed`.
    - Test 2: Mutating `seam_test_object.3mf` fails and stderr contains `9fa91ee2f54a33dc65fd681bb24dce666c77a501196815548a051d54e857bdc2`.
    - Test 3: Removing the `source_ref	fixture_path	archive_member	project_marker	deferred_semantics	notes` expected-artifact header fails with `expected-project-summary.tsv`.
    - Test 4: Removing any required archive member row for `[Content_Types].xml`, `_rels/.rels`, `3D/3dmodel.model`, `Metadata/thumbnail.png`, `Metadata/Slic3r_PE.config`, or `Metadata/Slic3r_PE_model.config` fails with that archive member name.
    - Test 5: Removing `packages/prusa-project-file-scope/project-file-scope.md` from provenance or README fails with that path.
    - Test 6: Adding `fork.prusaslicer.project-file` to a temp `status.tsv`, adding `prusaslicer_project_file_parity` to a temp parity BUILD, or adding `prusa_project_file` parser text under a temp Rust source root fails with the corresponding forbidden token.
  </behavior>
  <action>
    Write tests first in `verify_prusa_project_file_fixture_test.sh`, then implement `verify_prusa_project_file_fixture.sh` per D-12/D-13/D-14/D-15. Use Bash with `set -euo pipefail`, helper functions equivalent to the existing profile-schema verifier, exact file checks, targeted TSV header/row checks, `zipinfo -1` or `unzip -Z -1` archive member checks, and `unzip -p` marker checks for `slic3rpe:Version3mf`, `PrusaSlicer-2.8.0-alpha3`, `; generated by PrusaSlicer 2.8.0-alpha3`, and `<config>`.

    The verifier must check: fixture file exists; byte count `2514963`; SHA-256 `9fa91ee2f54a33dc65fd681bb24dce666c77a501196815548a051d54e857bdc2`; provenance header/row fields from Plan 42-01; expected-project-summary header exactly `source_ref	fixture_path	archive_member	project_marker	deferred_semantics	notes`; the seven expected rows from Plan 42-01; README update-route text; Phase 41 scope record path; no `fork.prusaslicer.project-file` row in `packages/parity/status.tsv`; no `//packages/parity:prusaslicer_project_file_parity` target; and no Phase 43 parser surface `prusa_project_file` under `packages/slic3r-rust/crates/slic3r_flavors/src` or `packages/slic3r-rust/crates/slic3r_flavors/tests`.

    Add `sh_binary(name = "verify_prusa_project_file_fixture", srcs = ["verify_prusa_project_file_fixture.sh"], data = [...])` and `sh_test(name = "verify_prusa_project_file_fixture_test", srcs = ["verify_prusa_project_file_fixture_test.sh"], data = [...])` to `packages/parity-fixtures/BUILD.bazel`. The binary data must include `:prusa_project_file_bundle`, `README.md`, `//packages/parity:status.tsv`, and `//packages/parity:BUILD.bazel`. The test data must include the verifier script, `:prusa_project_file_bundle`, `README.md`, `//packages/parity:status.tsv`, `//packages/parity:BUILD.bazel`, and the Rust flavor source/test roots needed for negative guards. Do not call `curl`, `git`, network services, profile auto-update behavior, upstream source imports, or plugin ingestion from verifier or tests.
  </action>
  <verify>
    <automated>bash packages/parity-fixtures/verify_prusa_project_file_fixture.sh && bash packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh && bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture && bazel test //packages/parity-fixtures:verify_prusa_project_file_fixture_test && shfmt -d packages/parity-fixtures/verify_prusa_project_file_fixture.sh packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash packages/parity-fixtures/verify_prusa_project_file_fixture.sh` prints exactly `ok: Prusa project-file fixture verification passed`.
    - `bash packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh` prints exactly `ok: verify_prusa_project_file_fixture_test`.
    - `bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture` passes.
    - `bazel test //packages/parity-fixtures:verify_prusa_project_file_fixture_test` passes.
    - `rg -n 'curl|git clone|git fetch|profile auto-update|plugin ingestion' packages/parity-fixtures/verify_prusa_project_file_fixture.sh packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh` returns no matches.
    - `bazel query //packages/parity:prusaslicer_project_file_parity` still fails because Phase 44 owns that target.
    - `rg -n 'fork\.prusaslicer\.project-file' packages/parity/status.tsv` returns no matches.
  </acceptance_criteria>
  <done>The repo-owned verifier and failure-mode tests fail closed on fixture/provenance/expected-artifact drift and on premature Phase 43/44 artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in fixture -> verifier | The verifier treats repository fixture files and TSVs as untrusted inputs that must match the Phase 42 contract exactly. |
| future-phase artifacts -> Phase 42 status | Premature Rust parser, parity target, or status row artifacts would overstate Phase 42 evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02-01 | Tampering | `verify_prusa_project_file_fixture.sh` | mitigate | Verify file existence, byte count, SHA-256, expected TSV rows, archive members, marker strings, README text, and provenance row fields with exact local checks. |
| T-42-02-02 | Repudiation | `fixture-provenance.tsv` | mitigate | Failure-mode tests mutate provenance and require non-zero verifier exits naming the missing source ref, scope path, or update route. |
| T-42-02-03 | Spoofing | Phase 44 status/target names | mitigate | Negative guards require absence of `fork.prusaslicer.project-file` and `//packages/parity:prusaslicer_project_file_parity` until Phase 44. |
| T-42-02-04 | Information Disclosure | verifier runtime | mitigate | Verifier and tests must not run `curl`, Git fetch/clone, network services, profile auto-update, upstream imports, or plugin ingestion. |
| T-42-02-05 | Denial of Service | ZIP inspection | accept | The checked-in fixture is small enough for local `zipinfo`/`unzip -p`; exact size/hash checks run before marker checks. |
</threat_model>

<verification>
Run the full Plan 42-02 verification:

```bash
bash packages/parity-fixtures/verify_prusa_project_file_fixture.sh
bash packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture
bazel test //packages/parity-fixtures:verify_prusa_project_file_fixture_test
shfmt -d packages/parity-fixtures/verify_prusa_project_file_fixture.sh packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
bazel query //packages/parity-fixtures:prusa_project_file_bundle
bash -lc '! bazel query //packages/parity:prusaslicer_project_file_parity >/tmp/phase42-prusa-project-file-target.out 2>&1'
bash -lc '! rg -n "fork\\.prusaslicer\\.project-file|prusaslicer_project_file_parity" packages/parity/status.tsv'
bash -lc '! rg -n "prusa_project_file" packages/slic3r-rust/crates/slic3r_flavors/src packages/slic3r-rust/crates/slic3r_flavors/tests'
git diff --check
```
</verification>

<success_criteria>
PFIX-02 is complete for the verifier surface: maintainers can run Bash and Bazel verification, and the verifier fails closed on missing/inconsistent fixture bytes, provenance, expected artifacts, update routes, scope traceability, non-overclaiming text, and premature Phase 43/44 artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/42-prusa-project-file-fixture-surface/42-02-SUMMARY.md` with `requirements-completed: [PFIX-02]`.
</output>
