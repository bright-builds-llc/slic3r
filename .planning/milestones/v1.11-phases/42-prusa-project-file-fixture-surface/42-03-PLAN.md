---
phase: 42-prusa-project-file-fixture-surface
plan: "03"
type: execute
wave: 3
depends_on: ["42-01", "42-02"]
files_modified:
  - packages/parity-fixtures/README.md
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/migration-guidance.md
  - docs/port/parity-matrix.md
autonomous: true
requirements: [PFIX-01, PFIX-02]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 42-2026-06-03T20-35-51
generated_at: 2026-06-03T20:49:37Z
must_haves:
  truths:
    - "Maintainer can discover the project-file fixture namespace, expected artifact, and verifier command from package docs."
    - "Maintainer can discover from port docs that Phase 42 fixture evidence exists while Phase 43 Rust parsing and Phase 44 executable parity/status remain unavailable."
    - "Docs preserve all Phase 42 deferrals and do not claim full 3MF import/export, GUI project behavior, generated-output parity, or broader runtime support."
  artifacts:
    - path: "packages/parity-fixtures/README.md"
      provides: "Fixture package route to project-file namespace and verifier"
      contains: "verify_prusa_project_file_fixture"
    - path: "docs/port/README.md"
      provides: "Control-plane Phase 42 state"
      contains: "Current Prusa Project-File Fixture Surface State"
    - path: "docs/port/migration-guidance.md"
      provides: "Project-file fixture and status routing"
      contains: "fork.prusaslicer.project-file is unavailable until"
    - path: "docs/port/parity-matrix.md"
      provides: "Non-overclaiming fork parity interpretation"
      contains: "Phase 42"
  key_links:
    - from: "docs/port/README.md"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/"
      via: "fixture namespace path"
      pattern: "packages/parity-fixtures/forks/prusaslicer/prusaslicer\\.project-file/"
    - from: "packages/parity-fixtures/README.md"
      to: "//packages/parity-fixtures:verify_prusa_project_file_fixture"
      via: "Bazel verifier command"
      pattern: "verify_prusa_project_file_fixture"
---

<objective>
Publish the Phase 42 fixture surface through package and port docs without claiming executable project-file parity.

Purpose: PFIX-01/PFIX-02 are maintainer-facing requirements; the docs must make the new fixture namespace, provenance, expected artifact, verifier command, and deferrals discoverable.
Output: Updated `packages/parity-fixtures` and `docs/port` surfaces that route maintainers to Phase 42 artifacts and preserve Phase 43/44 boundaries.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-CONTEXT.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-RESEARCH.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-01-SUMMARY.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-02-SUMMARY.md
@packages/parity-fixtures/README.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md
@docs/port/README.md
@docs/port/package-map.md
@docs/port/migration-guidance.md
@docs/port/parity-matrix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document fixture package route and namespace README</name>
  <read_first>
    - .planning/phases/42-prusa-project-file-fixture-surface/42-CONTEXT.md
    - .planning/phases/42-prusa-project-file-fixture-surface/42-01-SUMMARY.md
    - .planning/phases/42-prusa-project-file-fixture-surface/42-02-SUMMARY.md
    - packages/parity-fixtures/README.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.profile-schema/README.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md
  </read_first>
  <files>packages/parity-fixtures/README.md, packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md</files>
  <action>
    Update `packages/parity-fixtures/README.md` per D-16 to list the Phase 42 namespace `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/`, checked-in fixture `seam_test_object.3mf`, provenance `fixture-provenance.tsv`, expected artifact `expected-project-summary.tsv`, bundle target `//packages/parity-fixtures:prusa_project_file_bundle`, and verifier command `bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture`.

    Update the namespace README so it also names `expected-project-summary.tsv`, the exact verifier command, and the exact status boundary string `Executable project-file parity remains unavailable until Phase 44.` Keep the update route string from Plan 42-01 unchanged. Add the exact deferral sentence per D-17: `This fixture package does not introduce full PrusaSlicer runtime support, GUI project behavior, full 3MF import/export, generated-output parity, STEP import, support generation, arc fitting, wall seam behavior, network/device integration, profile auto-update execution, fork release builds, Bambu Studio support, OrcaSlicer support, upstream source imports, or sync automation.`
  </action>
  <verify>
    <automated>grep -F 'bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture' packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md && grep -F 'expected-project-summary.tsv' packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md && grep -F 'Executable project-file parity remains unavailable until Phase 44.' packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/parity-fixtures/README.md` contains `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/`.
    - Both fixture README files contain `bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture`.
    - The project-file namespace README contains the exact D-17 deferral sentence from the action.
    - Neither README contains `bazel run //packages/parity:prusaslicer_project_file_parity` as an available command; if mentioned, it must be described as Phase 44 unavailable/deferred text only.
  </acceptance_criteria>
  <done>Fixture package docs expose the Phase 42 fixture/verifier surface and preserve the Phase 44 executable parity boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Update port docs with Phase 42 handoff and deferrals</name>
  <read_first>
    - .planning/phases/42-prusa-project-file-fixture-surface/42-CONTEXT.md
    - .planning/phases/42-prusa-project-file-fixture-surface/42-01-SUMMARY.md
    - .planning/phases/42-prusa-project-file-fixture-surface/42-02-SUMMARY.md
    - docs/port/README.md
    - docs/port/package-map.md
    - docs/port/migration-guidance.md
    - docs/port/parity-matrix.md
    - packages/parity/status.tsv
  </read_first>
  <files>docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <action>
    Update `docs/port/README.md` with a `## Current Prusa Project-File Fixture Surface State` section that states: `Phase 42 adds fixture bytes, fixture provenance, expected-project-summary.tsv, and bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture for prusaslicer.project-file.` Also state that Phase 43 `slic3r_flavors::prusa_project_file` and Phase 44 `bazel run //packages/parity:prusaslicer_project_file_parity` are still unavailable.

    Update `docs/port/package-map.md` so the `packages/parity-fixtures` package role and phase notes mention the project-file namespace, `expected-project-summary.tsv`, and `verify_prusa_project_file_fixture`. Do not change `packages/parity` to claim a project-file command or status row.

    Update `docs/port/migration-guidance.md` so the fixture update protocol says the project-file fixture now exists at `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/`, uses `seam_test_object.3mf`, and is checked by `bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture`. Keep `fork.prusaslicer.project-file is unavailable until a real Phase 44 bazel run //packages/parity:prusaslicer_project_file_parity command exists and passes against the Phase 42 expected artifact.` as the status rule.

    Update `docs/port/parity-matrix.md` so Fork Parity Interpretation says Phase 42 supplies the fixture namespace, provenance, and expected artifact only. Preserve all D-17 deferrals: full PrusaSlicer runtime support, GUI project behavior, full 3MF import/export, generated-output parity, STEP import, support generation, arc fitting, wall seam behavior, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, and sync automation.
  </action>
  <verify>
    <automated>mdformat --check packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md && rg -n 'verify_prusa_project_file_fixture|expected-project-summary.tsv|packages/parity-fixtures/forks/prusaslicer/prusaslicer\\.project-file/' docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md && bash -lc '! rg -n "fork\\.prusaslicer\\.project-file" packages/parity/status.tsv'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/port/README.md` contains `## Current Prusa Project-File Fixture Surface State`.
    - `docs/port/migration-guidance.md` contains `fork.prusaslicer.project-file is unavailable until a real Phase 44`.
    - `docs/port/parity-matrix.md` contains `Phase 42 supplies the fixture namespace, provenance, and expected artifact only`.
    - `mdformat --check` passes for the six non-summary Markdown files listed in the verify command.
    - `rg -n 'fork\.prusaslicer\.project-file' packages/parity/status.tsv` returns no matches.
  </acceptance_criteria>
  <done>Port docs route maintainers to Phase 42 fixture evidence and retain the non-overclaiming boundary for later Rust/parity/status phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture/docs -> maintainer interpretation | Maintainers rely on docs to understand whether a fixture is evidence or executable parity. |
| docs -> future phases | Phase 43/44 executors will consume these docs as handoff context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03-01 | Spoofing | `docs/port/*.md` | mitigate | Require docs to state Phase 42 fixture evidence exists while Phase 43 parser and Phase 44 parity/status remain unavailable. |
| T-42-03-02 | Repudiation | fixture update docs | mitigate | Require reviewed update route naming `packages/fork-vendors/forks.tsv`, `packages/fork-inventories/prusaslicer.tsv`, and `packages/prusa-project-file-scope/project-file-scope.md`. |
| T-42-03-03 | Tampering | status boundary docs | mitigate | Require absence of `fork.prusaslicer.project-file` in `packages/parity/status.tsv` and prohibit docs from describing the Phase 44 command as available. |
| T-42-03-04 | Information Disclosure | docs | accept | Docs add no secrets, credentials, network/device details, or plugin data. |
</threat_model>

<verification>
Run final Phase 42 docs and end-to-end verification:

```bash
bazel run //packages/prusa-project-file-scope:verify
bazel run //packages/parity-fixtures:verify_prusa_profile_schema_fixture
bash packages/parity-fixtures/verify_prusa_project_file_fixture.sh
bash packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture
bazel test //packages/parity-fixtures:verify_prusa_project_file_fixture_test
shfmt -d packages/parity-fixtures/verify_prusa_project_file_fixture.sh packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
mdformat --check packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md
bash -lc '! bazel query //packages/parity:prusaslicer_project_file_parity >/tmp/phase42-prusa-project-file-target.out 2>&1'
bash -lc '! rg -n "fork\\.prusaslicer\\.project-file|prusaslicer_project_file_parity" packages/parity/status.tsv'
bash -lc '! rg -n "prusa_project_file" packages/slic3r-rust/crates/slic3r_flavors/src packages/slic3r-rust/crates/slic3r_flavors/tests'
git diff --check
```
</verification>

<success_criteria>
PFIX-01 and PFIX-02 are maintainer-discoverable from package docs and port docs, with the fixture surface verified locally and all broader Prusa project-file parity claims still deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/42-prusa-project-file-fixture-surface/42-03-SUMMARY.md` with `requirements-completed: [PFIX-01, PFIX-02]`.
</output>
