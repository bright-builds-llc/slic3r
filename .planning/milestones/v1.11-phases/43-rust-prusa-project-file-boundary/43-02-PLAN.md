---
phase: 43-rust-prusa-project-file-boundary
plan: "02"
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
  - packages/parity-fixtures/BUILD.bazel
  - packages/parity-fixtures/verify_prusa_project_file_fixture.sh
  - packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
autonomous: true
requirements: [PPROJ-02, PPROJ-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 43-2026-06-05T13-01-41
generated_at: 2026-06-05T13:16:56Z
must_haves:
  truths:
    - "The existing flavor registry project-file row traces through the same typed constants as `slic3r_flavors::prusa_project_file`."
    - "The Phase 42 fixture verifier allows the reviewed Phase 43 Rust project-file boundary."
    - "The same verifier still rejects Phase 44 parity target and status row publication."
    - "Cargo, Bazel, and shell verifier tests cover the registry metadata and updated guard behavior."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      provides: "Registry project-file provenance coupled to Rust boundary constants"
      contains: "PRUSA_PROJECT_FILE_INVENTORY_ID"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs"
      provides: "Traceability and no-overclaiming regression tests"
      contains: "prusa_project_file_metadata"
    - path: "packages/parity-fixtures/verify_prusa_project_file_fixture.sh"
      provides: "Fixture verifier guard updated for Phase 43"
      contains: "verify_parity_target_absent"
    - path: "packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh"
      provides: "Shell regression proving Phase 43 Rust surface is allowed"
      contains: "test_phase43_rust_surface_is_allowed"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs"
      via: "imports project-file constants"
      pattern: "crate::prusa_project_file"
    - from: "packages/parity-fixtures/verify_prusa_project_file_fixture.sh"
      to: "packages/parity/status.tsv"
      via: "negative status-row guard"
      pattern: "fork\\.prusaslicer\\.project-file"
    - from: "packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh"
      to: "packages/parity-fixtures/verify_prusa_project_file_fixture.sh"
      via: "failure-mode test invocation"
      pattern: "run_verifier"
---

<objective>
Connect the new project-file Rust boundary to registry metadata and update the Phase 42 verifier guard for the Phase 43 boundary.

Purpose: PPROJ-02 requires traceability from Rust metadata back to source and fixture metadata, while PPROJ-03 requires verification that Phase 43 has no executable parity/status side effects.
Output: Registry metadata tests using the new constants and a fixture verifier that permits Phase 43 Rust code while still rejecting Phase 44 publication.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-CONTEXT.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-RESEARCH.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-01-SUMMARY.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-02-SUMMARY.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-VERIFICATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
@packages/parity-fixtures/BUILD.bazel
@packages/parity-fixtures/verify_prusa_project_file_fixture.sh
@packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
@packages/parity/status.tsv
@packages/parity/BUILD.bazel

<interfaces>
From Plan 43-01, the executor should use the new project-file metadata API and constants directly:

```rust
pub const PRUSA_PROJECT_FILE_INVENTORY_ID: &str;
pub const PRUSA_PROJECT_FILE_SOURCE_REF: VendorSourceRef;
pub const PRUSA_PROJECT_FILE_SOURCE_PATH: &str;
pub const fn prusa_project_file_metadata() -> PrusaProjectFileMetadata;
```

The registry row must remain a `FutureCandidate` and must not become executable status evidence in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Couple registry project-file metadata to the Rust boundary</name>
  <read_first>
    - .planning/phases/43-rust-prusa-project-file-boundary/43-CONTEXT.md
    - .planning/phases/43-rust-prusa-project-file-boundary/43-01-SUMMARY.md
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
    - packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
    - packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs
  </read_first>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs</files>
  <behavior>
    - Test 1: `prusaslicer.project-file` registry capability remains `FlavorId::PrusaSlicer`, `FeatureOrigin::SharedDownstream`, `ChecklistStatus::FutureCandidate`, and `ParitySurface::file_formats()`.
    - Test 2: Project-file registry provenance uses inventory ID `prusaslicer.project-file`, source ref `VendorSourceRef::prusa_slicer_version_2_9_5()`, and source path `src/libslic3r/Format/3mf.cpp`.
    - Test 3: `prusa_project_file_metadata()` exposes fixture path, expected summary path, scope record path, `FeatureOrigin::SharedDownstream`, `ParitySurface::file_formats()`, `ChecklistStatus::FutureCandidate`, and reserved future status token `fork.prusaslicer.project-file`.
    - Test 4: no-overclaiming helper-name test includes project-file helper names and metadata values without treating the reserved future token as verified support.
  </behavior>
  <action>
    Update `packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` per D-08 through D-10. Import `PRUSA_PROJECT_FILE_INVENTORY_ID`, `PRUSA_PROJECT_FILE_SOURCE_PATH`, and `PRUSA_PROJECT_FILE_SOURCE_REF` from `crate::prusa_project_file`. Replace the hardcoded `PRUSA_PROJECT_FILE_PATHS` and `PRUSA_PROJECT_FILE_PROVENANCE` literals with those constants where possible:

    - `static PRUSA_PROJECT_FILE_PATHS: [&str; 1] = [PRUSA_PROJECT_FILE_SOURCE_PATH];`
    - `inventory_id: PRUSA_PROJECT_FILE_INVENTORY_ID`
    - `vendor_source: PRUSA_PROJECT_FILE_SOURCE_REF`

    Leave the capability row status as `ChecklistStatus::FutureCandidate`, the origin as `FeatureOrigin::SharedDownstream`, and the dependency as `&FILE_FORMATS_PARITY`. Do not add a status row, command target, runtime support flag, or broader 3MF/import/export capability.

    Update `packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs`. Import `prusa_project_file_metadata`. Add focused tests named `prusa_project_file_registry_row_traces_to_source_and_parity_dependency` and `prusa_project_file_metadata_exposes_fixture_scope_expected_summary_and_reserved_status`. Extend `runtime_claim_words_do_not_become_public_helper_names` so it includes `prusa_project_file_metadata` and the new metadata values, but keeps the reserved future token as a traceability field, not a verified status assertion. Use Arrange/Act/Assert comments.
  </action>
  <verify>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test flavor_registry && bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test</automated>
  </verify>
  <acceptance_criteria>
    - `rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test flavor_registry` passes.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test` passes.
    - `rg -n 'PRUSA_PROJECT_FILE_INVENTORY_ID|PRUSA_PROJECT_FILE_SOURCE_PATH|PRUSA_PROJECT_FILE_SOURCE_REF' packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` finds all three constants.
    - `rg -n 'prusa_project_file_registry_row_traces_to_source_and_parity_dependency|prusa_project_file_metadata_exposes_fixture_scope_expected_summary_and_reserved_status|prusa_project_file_metadata' packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` finds the new test coverage.
    - `rg -n 'verified|supported|runtime|release|executable' packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` returns no new overclaiming helper names or notes for `prusaslicer.project-file`.
  </acceptance_criteria>
  <done>The registry project-file row is coupled to the same typed Rust source constants and has focused traceability/no-overclaiming tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Allow Phase 43 Rust surface in fixture verifier while preserving Phase 44 guards</name>
  <read_first>
    - .planning/phases/43-rust-prusa-project-file-boundary/43-CONTEXT.md
    - .planning/phases/42-prusa-project-file-fixture-surface/42-02-SUMMARY.md
    - .planning/phases/42-prusa-project-file-fixture-surface/42-VERIFICATION.md
    - packages/parity-fixtures/BUILD.bazel
    - packages/parity-fixtures/verify_prusa_project_file_fixture.sh
    - packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs
    - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_project_file.rs
    - packages/parity/status.tsv
    - packages/parity/BUILD.bazel
  </read_first>
  <files>packages/parity-fixtures/BUILD.bazel, packages/parity-fixtures/verify_prusa_project_file_fixture.sh, packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh</files>
  <behavior>
    - Test 1: A complete fixture copy passes even when `rust-src/prusa_project_file.rs` and `rust-tests/prusa_project_file.rs` exist in the temp Rust roots.
    - Test 2: A temp `status.tsv` containing `fork.prusaslicer.project-file` still fails.
    - Test 3: A temp parity BUILD containing `prusaslicer_project_file_parity` still fails.
    - Test 4: The direct Bash verifier passes in the real checkout after Plan 43-01 added `slic3r_flavors::prusa_project_file`.
  </behavior>
  <action>
    Update the Phase 42 verifier guard per the user constraint and D-14/D-15. In `verify_prusa_project_file_fixture.sh`, remove the prohibition that scans Rust source/test roots for `prusa_project_file`. Keep all fixture/provenance/expected-summary/archive/README checks. Keep `verify_status_row_absent` and `verify_parity_target_absent` unchanged or stricter so Phase 44 publication remains rejected.

    Simplify the verifier argument contract from nine explicit arguments to seven if the Rust roots are no longer needed: fixture README, provenance file, expected project summary, project file, parity status file, parity BUILD file, and package README. Update the usage string and tests to match. If keeping the existing nine-argument shape is less invasive, the script may accept the last two arguments but must not reject Phase 43 `prusa_project_file` Rust code.

    In `verify_prusa_project_file_fixture_test.sh`, replace `test_premature_rust_surface_fails` with `test_phase43_rust_surface_is_allowed`. The new test should create temp `rust-src/prusa_project_file.rs` and `rust-tests/prusa_project_file.rs` files containing `prusa_project_file` and then assert the verifier still passes. Preserve the existing `test_premature_status_row_fails` and `test_premature_parity_target_fails` negative tests.

    In `packages/parity-fixtures/BUILD.bazel`, remove obsolete Rust source/test data from `verify_prusa_project_file_fixture` and `verify_prusa_project_file_fixture_test` if the verifier no longer takes Rust roots. If the executor keeps the nine-argument verifier shape, add the Plan 43-01 files `//packages/slic3r-rust/crates/slic3r_flavors:src/prusa_project_file.rs` and `//packages/slic3r-rust/crates/slic3r_flavors:tests/prusa_project_file.rs` to the data lists so Bazel runfiles reflect the real Phase 43 surface. Do not add `//packages/parity:prusaslicer_project_file_parity` or edit `packages/parity/status.tsv`.
  </action>
  <verify>
    <automated>bash packages/parity-fixtures/verify_prusa_project_file_fixture.sh && bash packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh && bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture && bazel test //packages/parity-fixtures:verify_prusa_project_file_fixture_test && shfmt -d packages/parity-fixtures/verify_prusa_project_file_fixture.sh packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash packages/parity-fixtures/verify_prusa_project_file_fixture.sh` prints exactly `ok: Prusa project-file fixture verification passed`.
    - `bash packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh` prints exactly `ok: verify_prusa_project_file_fixture_test`.
    - `bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture` passes after `src/prusa_project_file.rs` exists.
    - `bazel test //packages/parity-fixtures:verify_prusa_project_file_fixture_test` passes.
    - `rg -n 'test_phase43_rust_surface_is_allowed' packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh` finds the new allow test.
    - `rg -n 'forbidden premature Rust project-file surface|verify_rust_surface_absent' packages/parity-fixtures/verify_prusa_project_file_fixture.sh packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh` returns no matches.
    - `bazel query //packages/parity:prusaslicer_project_file_parity` still fails because Phase 44 owns that target.
    - `rg -n 'fork\.prusaslicer\.project-file' packages/parity/status.tsv` returns no matches.
  </acceptance_criteria>
  <done>The fixture verifier no longer treats the Phase 43 Rust boundary as premature, while status-row and parity-target negative guards remain active.</done>
</task>

<task type="auto">
  <name>Task 3: Run combined Rust and verifier verification</name>
  <read_first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - packages/slic3r-rust/Cargo.toml
    - packages/slic3r-rust/BUILD.bazel
    - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
    - packages/parity-fixtures/BUILD.bazel
    - packages/parity-fixtures/verify_prusa_project_file_fixture.sh
    - packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
  </read_first>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs, packages/parity-fixtures/BUILD.bazel, packages/parity-fixtures/verify_prusa_project_file_fixture.sh, packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh</files>
  <action>
    Run the relevant verification for the changed Rust and shell verifier surfaces. Follow the Rust pre-commit sequence from AGENTS: format, clippy, build, and tests with the pinned Rust toolchain. Run the direct and Bazel shell verifier checks because this plan touches shell verifier files. Run `git diff --check` and the schema drift check for Phase 43. Fix any failures in the files owned by this plan only.
  </action>
  <verify>
    <automated>rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all && rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings && rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features && rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features && bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test //packages/slic3r-rust:verify //packages/parity-fixtures:verify_prusa_project_file_fixture_test && bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture && git diff --check && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 43</automated>
  </verify>
  <acceptance_criteria>
    - All four Cargo commands pass.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test //packages/slic3r-rust:verify //packages/parity-fixtures:verify_prusa_project_file_fixture_test` passes.
    - `bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture` passes.
    - `git diff --check` exits 0.
    - `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 43` reports no drift.
  </acceptance_criteria>
  <done>Registry metadata, Rust verification, and the updated fixture verifier guard are all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust parser metadata -> registry metadata | Registry values must not drift from the parser metadata constants or imply stronger evidence than Phase 43 provides. |
| Phase 43 Rust surface -> Phase 42 verifier | The old verifier guard must distinguish the reviewed Rust boundary from premature Phase 44 publication. |
| Repo files -> shell verifier | Status and Bazel BUILD files are checked for forbidden publication tokens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02-01 | Spoofing | `registry.rs` project-file provenance | mitigate | Import and use `PRUSA_PROJECT_FILE_*` constants from `prusa_project_file.rs`; add registry tests for inventory/source/path dependency values. |
| T-43-02-02 | Repudiation | `fork.prusaslicer.project-file` status token | mitigate | Keep token in Rust metadata as reserved only; verifier and acceptance checks require status row absence. |
| T-43-02-03 | Tampering | `verify_prusa_project_file_fixture.sh` | mitigate | Preserve exact fixture/provenance/expected-summary checks while removing only the obsolete Phase 43 Rust-surface absence guard. |
| T-43-02-04 | Elevation of Privilege | Phase 44 parity target | mitigate | Keep `prusaslicer_project_file_parity` rejected in `packages/parity/BUILD.bazel` with direct and temp-file shell tests. |
| T-43-02-05 | Denial of Service | shell tests | accept | Tests operate on temp copies of checked-in fixture files and run locally with bounded file sizes. |
</threat_model>

<verification>
Run the full Plan 43-02 verification:

```bash
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features
bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test //packages/slic3r-rust:verify //packages/parity-fixtures:verify_prusa_project_file_fixture_test
bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture
shfmt -d packages/parity-fixtures/verify_prusa_project_file_fixture.sh packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
bash -lc '! bazel query //packages/parity:prusaslicer_project_file_parity >/tmp/phase43-plan02-project-file-target.out 2>&1'
bash -lc '! rg -n "fork\\.prusaslicer\\.project-file" packages/parity/status.tsv'
git diff --check
node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 43
```
</verification>

<success_criteria>
PPROJ-02 and PPROJ-03 are complete for registry and verifier guard integration: metadata traceability is coupled through typed Rust constants, the Phase 43 Rust surface is allowed by the fixture verifier, Phase 44 status/target publication remains rejected, and all Rust/shell verification passes.
</success_criteria>

<output>
After completion, create `.planning/phases/43-rust-prusa-project-file-boundary/43-02-SUMMARY.md` with `requirements-completed: [PPROJ-02, PPROJ-03]`.
</output>
