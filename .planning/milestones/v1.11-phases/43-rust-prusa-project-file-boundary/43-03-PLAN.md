---
phase: 43-rust-prusa-project-file-boundary
plan: "03"
type: execute
wave: 3
depends_on: ["43-01", "43-02"]
files_modified:
  - packages/slic3r-rust/README.md
  - packages/parity-fixtures/README.md
  - packages/parity/README.md
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/migration-guidance.md
  - docs/port/parity-matrix.md
autonomous: true
requirements: [PPROJ-02, PPROJ-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 43-2026-06-05T13-01-41
generated_at: 2026-06-05T13:16:56Z
must_haves:
  truths:
    - "Developer can discover the Phase 43 project-file Rust boundary from the Rust package and port docs."
    - "Docs trace the boundary to source ref, source path, fixture path, expected summary path, scope record path, and reserved future status token."
    - "Docs state that Phase 44 still owns executable parity and `packages/parity/status.tsv` publication."
    - "Markdown, fixture verifier, status absence, parity-target absence, and schema-drift checks pass."
  artifacts:
    - path: "packages/slic3r-rust/README.md"
      provides: "Rust package documentation for `slic3r_flavors::prusa_project_file`"
      contains: "parse_prusa_project_file_summary"
    - path: "docs/port/README.md"
      provides: "Port control-plane state for the Phase 43 Rust project-file boundary"
      contains: "Current Prusa Project-File Rust Boundary State"
    - path: "docs/port/migration-guidance.md"
      provides: "Status publication rule that keeps Phase 44 as executable parity owner"
      contains: "fork.prusaslicer.project-file"
    - path: "packages/parity/README.md"
      provides: "Parity package status-row guardrail"
      contains: "prusaslicer_project_file_parity"
  key_links:
    - from: "packages/slic3r-rust/README.md"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv"
      via: "expected summary traceability text"
      pattern: "expected-project-summary\\.tsv"
    - from: "docs/port/README.md"
      to: "packages/prusa-project-file-scope/project-file-scope.md"
      via: "scope record traceability text"
      pattern: "project-file-scope\\.md"
    - from: "packages/parity/README.md"
      to: "packages/parity/status.tsv"
      via: "status-row publication boundary"
      pattern: "packages/parity/status\\.tsv"
---

<objective>
Publish the Phase 43 Rust project-file boundary through package and port docs without claiming executable parity.

Purpose: PPROJ-02 requires source/fixture/status-token traceability that maintainers can inspect, and PPROJ-03 requires visible confirmation that executable parity, status publication, and side-effecting behavior remain outside Phase 43.
Output: Updated Rust, fixture, parity, and port docs that name the parser boundary, exact metadata chain, tests/verifier commands, and Phase 44 deferrals.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-CONTEXT.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-RESEARCH.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-01-SUMMARY.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-02-SUMMARY.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@packages/slic3r-rust/README.md
@packages/parity-fixtures/README.md
@packages/parity/README.md
@docs/port/README.md
@docs/port/package-map.md
@docs/port/migration-guidance.md
@docs/port/parity-matrix.md
@packages/parity/status.tsv
@packages/parity/BUILD.bazel

<interfaces>
The docs should name these Phase 43 Rust APIs exactly:

```text
slic3r_flavors::prusa_project_file
parse_prusa_project_file_summary
prusa_project_file_metadata
prusa_project_file_summary_lines
//packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_test
```

The docs must also name these Phase 44-only surfaces as unavailable in Phase 43:

```text
bazel run //packages/parity:prusaslicer_project_file_parity
fork.prusaslicer.project-file
packages/parity/status.tsv
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Rust boundary traceability without status publication</name>
  <read_first>
    - .planning/phases/43-rust-prusa-project-file-boundary/43-CONTEXT.md
    - .planning/phases/43-rust-prusa-project-file-boundary/43-01-SUMMARY.md
    - .planning/phases/43-rust-prusa-project-file-boundary/43-02-SUMMARY.md
    - packages/slic3r-rust/README.md
    - packages/parity-fixtures/README.md
    - packages/parity/README.md
    - docs/port/README.md
    - docs/port/package-map.md
    - docs/port/migration-guidance.md
    - docs/port/parity-matrix.md
    - packages/parity/status.tsv
  </read_first>
  <files>packages/slic3r-rust/README.md, packages/parity-fixtures/README.md, packages/parity/README.md, docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <action>
    Update docs per D-15 and D-16 with narrow, grep-stable language.

    In `packages/slic3r-rust/README.md`, add Phase 43 after the Phase 39/40 profile-schema text. Name `slic3r_flavors::prusa_project_file`, `parse_prusa_project_file_summary`, `prusa_project_file_metadata`, `prusa_project_file_summary_lines`, and `//packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_test`. State that production Rust accepts caller-supplied TSV text and returns typed project-file summary values with no filesystem discovery, Git, network, process, profile auto-update, release, upstream import, or vendor sync behavior. Include exact traceability strings:
    - `prusaslicer.project-file`
    - `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`
    - `src/libslic3r/Format/3mf.cpp`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/seam_test_object.3mf`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv`
    - `packages/prusa-project-file-scope/project-file-scope.md`
    - `fork.prusaslicer.project-file`

    In `packages/parity-fixtures/README.md`, update the Phase 42 project-file section to say Phase 43 now consumes `expected-project-summary.tsv` through the Rust parser boundary, while fixture verification remains local and Phase 44 executable parity remains unavailable.

    In `packages/parity/README.md`, add a project-file paragraph under `Fork Status Rows`: `fork.prusaslicer.project-file` remains unavailable in `packages/parity/status.tsv` until the Phase 44 command `bazel run //packages/parity:prusaslicer_project_file_parity` exists and passes. State that the Phase 43 reserved future status token is traceability metadata only.

    In `docs/port/README.md`, replace the Phase 42 line that says Phase 43 is still unavailable with a new section heading exactly `## Current Prusa Project-File Rust Boundary State`. State the APIs, test target, traceability chain, and Phase 44 status/command absence.

    In `docs/port/package-map.md`, add a Phase 43 note that `packages/slic3r-rust/crates/slic3r_flavors` owns the typed `prusa_project_file` parser/metadata boundary over `expected-project-summary.tsv`; `packages/parity` still does not own a project-file command or status row until Phase 44.

    In `docs/port/migration-guidance.md`, update the future fork status rule to say Phase 43 parser/metadata readiness now exists, but `fork.prusaslicer.project-file` remains unavailable until the Phase 44 Bazel parity command exists and passes against the Phase 42 expected artifact.

    In `docs/port/parity-matrix.md`, update the fork interpretation to say Phase 43 supplies `slic3r_flavors::prusa_project_file` parser/metadata readiness only. Preserve the statement that Phase 42 supplies fixture namespace/provenance/expected artifact only and Phase 44 owns executable parity/status.

    Do not edit `packages/parity/status.tsv`. Do not add a new executable command, binary, release behavior, vendor sync behavior, or full 3MF parser wording.
  </action>
  <verify>
    <automated>rg -n 'slic3r_flavors::prusa_project_file|parse_prusa_project_file_summary|prusa_project_file_metadata|prusa_project_file_summary_lines|prusa_project_file_test|expected-project-summary.tsv|packages/prusa-project-file-scope/project-file-scope.md|fork\.prusaslicer\.project-file|prusaslicer_project_file_parity' packages/slic3r-rust/README.md packages/parity-fixtures/README.md packages/parity/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md</automated>
  </verify>
  <acceptance_criteria>
    - The `rg` command in `<verify>` finds every required API, path, reserved token, and Phase 44 command/status boundary string.
    - `rg -n 'Current Prusa Project-File Rust Boundary State' docs/port/README.md` finds exactly one section heading.
    - `rg -n 'Phase 44|packages/parity/status.tsv|prusaslicer_project_file_parity|fork\.prusaslicer\.project-file' packages/parity/README.md docs/port/README.md docs/port/migration-guidance.md docs/port/parity-matrix.md` finds Phase 44 ownership wording in all four files.
    - `rg -n 'full 3MF import/export|full PrusaSlicer runtime support|GUI project behavior|generated-output parity|STEP import|support generation|arc fitting|wall seam behavior|network/device integration|profile auto-update execution|fork release builds|Bambu Studio|OrcaSlicer|upstream source imports|sync automation' docs/port/README.md docs/port/migration-guidance.md docs/port/parity-matrix.md packages/slic3r-rust/README.md packages/parity/README.md` finds deferred-scope wording.
    - `git diff -- packages/parity/status.tsv packages/parity/BUILD.bazel` shows no changes.
  </acceptance_criteria>
  <done>Docs make the Phase 43 Rust boundary discoverable and traceable without claiming executable project-file parity or status publication.</done>
</task>

<task type="auto">
  <name>Task 2: Run docs, verifier, and status-scope checks</name>
  <read_first>
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - packages/slic3r-rust/README.md
    - packages/parity-fixtures/README.md
    - packages/parity/README.md
    - docs/port/README.md
    - docs/port/package-map.md
    - docs/port/migration-guidance.md
    - docs/port/parity-matrix.md
    - packages/parity-fixtures/verify_prusa_project_file_fixture.sh
    - packages/parity/status.tsv
    - packages/parity/BUILD.bazel
  </read_first>
  <files>packages/slic3r-rust/README.md, packages/parity-fixtures/README.md, packages/parity/README.md, docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <action>
    Run formatting and scope verification for the touched docs. Use `mdformat --check` on the seven non-summary Markdown files; do not run `mdformat` over any `*-SUMMARY.md` file per repo-local guidance. Run the project-file fixture verifier because this plan changes docs it checks. Confirm the Phase 44 parity target and status row are still absent. Run `git diff --check` and the Phase 43 schema drift check. Fix only docs owned by this plan if checks fail.
  </action>
  <verify>
    <automated>mdformat --check packages/slic3r-rust/README.md packages/parity-fixtures/README.md packages/parity/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md && bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture && bash -lc '! bazel query //packages/parity:prusaslicer_project_file_parity >/tmp/phase43-plan03-project-file-target.out 2>&1' && bash -lc '! rg -n "fork\\.prusaslicer\\.project-file" packages/parity/status.tsv' && git diff --check && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 43</automated>
  </verify>
  <acceptance_criteria>
    - `mdformat --check` passes for the seven touched Markdown docs and no `*-SUMMARY.md` file is passed to mdformat.
    - `bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture` passes.
    - `bazel query //packages/parity:prusaslicer_project_file_parity` fails as expected.
    - `rg -n 'fork\.prusaslicer\.project-file' packages/parity/status.tsv` returns no matches.
    - `git diff --check` exits 0.
    - `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 43` reports no drift.
  </acceptance_criteria>
  <done>Documentation is formatted, verifier-compatible, and preserves Phase 44 command/status absence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust boundary facts -> maintainer docs | Documentation must accurately describe parser readiness without upgrading it to verified parity. |
| Reserved token -> status publication | `fork.prusaslicer.project-file` appears in docs as future traceability but must not appear in `packages/parity/status.tsv`. |
| Fixture verifier -> docs | The fixture verifier checks some docs text and must remain green after docs wording changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03-01 | Repudiation | `packages/parity/README.md` and port docs | mitigate | Require grep-stable Phase 44 ownership wording for `prusaslicer_project_file_parity`, `fork.prusaslicer.project-file`, and `packages/parity/status.tsv`. |
| T-43-03-02 | Spoofing | project-file traceability text | mitigate | Include exact source ref, source path, fixture path, expected summary path, and scope record path in docs. |
| T-43-03-03 | Tampering | `packages/parity/status.tsv` | mitigate | Acceptance and verification require no status row changes and no `fork.prusaslicer.project-file` match in status TSV. |
| T-43-03-04 | Information Disclosure | docs content | accept | Docs contain only checked-in source/fixture metadata and no secrets, credentials, or network endpoints. |
| T-43-03-05 | Elevation of Privilege | docs overclaiming executable behavior | mitigate | Preserve deferred-scope wording for full runtime, GUI, full 3MF import/export, generated output, network/device, release, Bambu, Orca, upstream imports, and sync automation. |
</threat_model>

<verification>
Run the full Plan 43-03 verification:

```bash
rg -n 'slic3r_flavors::prusa_project_file|parse_prusa_project_file_summary|prusa_project_file_metadata|prusa_project_file_summary_lines|prusa_project_file_test|expected-project-summary.tsv|packages/prusa-project-file-scope/project-file-scope.md|fork\.prusaslicer\.project-file|prusaslicer_project_file_parity' packages/slic3r-rust/README.md packages/parity-fixtures/README.md packages/parity/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md
mdformat --check packages/slic3r-rust/README.md packages/parity-fixtures/README.md packages/parity/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md
bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture
bash -lc '! bazel query //packages/parity:prusaslicer_project_file_parity >/tmp/phase43-plan03-project-file-target.out 2>&1'
bash -lc '! rg -n "fork\\.prusaslicer\\.project-file" packages/parity/status.tsv'
git diff --check
node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 43
```
</verification>

<success_criteria>
PPROJ-02 and PPROJ-03 are complete for documentation and scope control: docs expose the Phase 43 Rust parser/metadata boundary and traceability, fixture docs/verifier remain aligned, and Phase 44 command/status publication remains absent.
</success_criteria>

<output>
After completion, create `.planning/phases/43-rust-prusa-project-file-boundary/43-03-SUMMARY.md` with `requirements-completed: [PPROJ-02, PPROJ-03]`.
</output>
