---
phase: 43-rust-prusa-project-file-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_project_file.rs
  - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
  - packages/slic3r-rust/BUILD.bazel
autonomous: true
requirements: [PPROJ-01, PPROJ-02, PPROJ-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 43-2026-06-05T13-01-41
generated_at: 2026-06-05T13:16:56Z
must_haves:
  truths:
    - "Developer can call `slic3r_flavors::prusa_project_file` with caller-supplied TSV text and receive typed project-file summary values."
    - "Malformed summary input fails with typed errors instead of panics, silent acceptance, filesystem discovery, process execution, Git, or network behavior."
    - "Developer can trace the boundary metadata to `prusaslicer.project-file`, the Prusa source pin, source path, fixture path, expected summary path, scope record path, and reserved future status token."
    - "Cargo and Bazel run focused Rust tests for the new project-file boundary."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs"
      provides: "Pure std-only project-file expected-summary TSV parser, typed rows, metadata, and summary lines"
      contains: "parse_prusa_project_file_summary"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_project_file.rs"
      provides: "Focused parser, metadata, malformed-row, and no-overclaiming tests"
      contains: "EXPECTED_PROJECT_SUMMARY"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs"
      provides: "Public `slic3r_flavors::prusa_project_file` module and re-exports"
      contains: "pub mod prusa_project_file"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel"
      provides: "Bazel source/test/clippy/rustfmt wiring"
      contains: "prusa_project_file_test"
    - path: "packages/slic3r-rust/BUILD.bazel"
      provides: "Aggregate Rust verify coverage for the new test"
      contains: "prusa_project_file_test"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_project_file.rs"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv"
      via: "include_str! fixture summary input"
      pattern: "expected-project-summary\\.tsv"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs"
      to: "packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs"
      via: "FlavorId, VendorSourceRef, FeatureOrigin, ParitySurface, ChecklistStatus metadata fields"
      pattern: "VendorSourceRef::prusa_slicer_version_2_9_5"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel"
      to: "packages/parity-fixtures:prusa_project_file_expected_project_summary"
      via: "rust_test compile_data"
      pattern: "prusa_project_file_expected_project_summary"
---

<objective>
Create the pure Rust `slic3r_flavors::prusa_project_file` domain boundary for the Phase 42 expected project summary.

Purpose: PPROJ-01, PPROJ-02, and PPROJ-03 require typed, side-effect-free Rust values and tests before Phase 44 can build executable parity evidence.
Output: A std-only parser/summary module, public re-exports, focused Cargo/Bazel tests, and aggregate Rust verification wiring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-CONTEXT.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-RESEARCH.md
@.planning/milestones/v1.10-phases/39-rust-prusa-profile-boundary/39-01-SUMMARY.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-VERIFICATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_profile.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_profile.rs
@packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
@packages/slic3r-rust/BUILD.bazel
@packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/fixture-provenance.tsv
@packages/prusa-project-file-scope/project-file-scope.md

<interfaces>
From `packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs`:

```rust
pub enum FlavorId {
    PrusaSlicer,
}

pub struct VendorSourceRef { /* private fields */ }
impl VendorSourceRef {
    pub const fn prusa_slicer_version_2_9_5() -> Self;
    pub const fn as_str(self) -> &'static str;
}

pub enum FeatureOrigin {
    SharedDownstream,
}

pub struct ParitySurface(&'static str);
impl ParitySurface {
    pub const fn file_formats() -> Self;
}

pub enum ChecklistStatus {
    FutureCandidate,
}
```

From `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_profile.rs`, mirror the pure boundary shape:

```rust
pub const fn prusa_profile_schema_metadata() -> PrusaProfileSchemaMetadata;
pub fn parse_prusa_profile_bundle(input: &str) -> PrusaProfileParseResult;
pub fn prusa_profile_schema_summary_lines(
    input: &str,
) -> Result<Vec<String>, PrusaProfileParseError>;
```

The Phase 43 public module must be named exactly `slic3r_flavors::prusa_project_file` per D-02, unless an implementation-discovered compile constraint requires only local naming adjustments.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure Prusa project-file parser and metadata API</name>
  <read_first>
    - .planning/phases/43-rust-prusa-project-file-boundary/43-CONTEXT.md
    - .planning/phases/43-rust-prusa-project-file-boundary/43-RESEARCH.md
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_profile.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
    - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_profile.rs
    - packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/fixture-provenance.tsv
    - packages/prusa-project-file-scope/project-file-scope.md
  </read_first>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs, packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_project_file.rs</files>
  <behavior>
    - Test 1: `parse_prusa_project_file_summary(EXPECTED_PROJECT_SUMMARY)` returns seven typed rows in fixture order.
    - Test 2: `prusa_project_file_metadata()` returns exact D-08 values, including source ref `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, source path `src/libslic3r/Format/3mf.cpp`, fixture path `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/seam_test_object.3mf`, expected summary path `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv`, scope record path `packages/prusa-project-file-scope/project-file-scope.md`, and reserved future status token `fork.prusaslicer.project-file`.
    - Test 3: `prusa_project_file_summary_lines(EXPECTED_PROJECT_SUMMARY)` includes `row_count	7` and only presence-level `evidence_row` lines.
    - Test 4: invalid header, wrong column count, empty required value, unexpected source ref, unexpected fixture path, unsupported archive member, unsupported project marker, unsupported deferred semantics, duplicate row, missing row, and extra row inputs each return a `PrusaProjectFileParseError` variant.
    - Test 5: public type and helper names do not use runtime/support/import/export/load/save/geometry/profile/generated-output claim wording except inside explicit deferred-scope metadata strings.
  </behavior>
  <action>
    Add `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs` per D-01 through D-10. Keep the module std-only and production data-in/data-out: no `std::fs`, `std::process`, `std::env`, `std::net`, path discovery, Git, network, profile auto-update, upstream source import, release, or vendor sync behavior per D-03.

    Define exact metadata constants as `pub(crate) const` unless implementation finds a compelling reason for a specific constant to be fully public. Plan 43-02 consumes at least `PRUSA_PROJECT_FILE_INVENTORY_ID`, `PRUSA_PROJECT_FILE_SOURCE_PATH`, and `PRUSA_PROJECT_FILE_SOURCE_REF` from sibling module `registry.rs`, so those three constants must not be private:
    - `pub(crate) const PRUSA_PROJECT_FILE_INVENTORY_ID: &str = "prusaslicer.project-file"`
    - `pub(crate) const PRUSA_PROJECT_FILE_VENDOR_ID: &str = "prusaslicer"`
    - `pub(crate) const PRUSA_PROJECT_FILE_SOURCE_REF: VendorSourceRef = VendorSourceRef::prusa_slicer_version_2_9_5()`
    - `pub(crate) const PRUSA_PROJECT_FILE_SOURCE_PATH: &str = "src/libslic3r/Format/3mf.cpp"`
    - `pub(crate) const PRUSA_PROJECT_FILE_FIXTURE_PATH: &str = "packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/seam_test_object.3mf"`
    - `pub(crate) const PRUSA_PROJECT_FILE_EXPECTED_SUMMARY_PATH: &str = "packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv"`
    - `pub(crate) const PRUSA_PROJECT_FILE_SCOPE_RECORD_PATH: &str = "packages/prusa-project-file-scope/project-file-scope.md"`
    - `pub(crate) const PRUSA_PROJECT_FILE_RESERVED_STATUS_TOKEN: &str = "fork.prusaslicer.project-file"`

    Define `PrusaProjectFileMetadata` with fields `inventory_id`, `vendor_id`, `flavor_id: FlavorId`, `origin: FeatureOrigin`, `parity_dependency: ParitySurface`, `checklist_status: ChecklistStatus`, `source_ref: VendorSourceRef`, `source_path`, `fixture_path`, `expected_summary_path`, `scope_record_path`, and `reserved_future_status_token`. Return it from `pub const fn prusa_project_file_metadata()`.

    Define typed summary values rather than raw maps: `PrusaProjectFileSummary`, `PrusaProjectFileSummaryRow`, `PrusaProjectFileArchiveMember`, `PrusaProjectFileMarker`, `PrusaProjectFileDeferredSemantics`, and `PrusaProjectFileNote`. Each enum/newtype must expose `as_str()` for exact TSV/doc tokens. Preserve the Phase 42 columns exactly as field concepts per D-05: `source_ref`, `fixture_path`, `archive_member`, `project_marker`, `deferred_semantics`, and `notes`.

    Implement:
    - `pub type PrusaProjectFileParseResult = Result<PrusaProjectFileSummary, PrusaProjectFileParseError>;`
    - `pub fn parse_prusa_project_file_summary(input: &str) -> PrusaProjectFileParseResult`
    - `pub fn prusa_project_file_summary_lines(input: &str) -> Result<Vec<String>, PrusaProjectFileParseError>`

    The parser must require the exact header `source_ref	fixture_path	archive_member	project_marker	deferred_semantics	notes`, reject wrong column counts, reject empty required values, reject source refs other than `VendorSourceRef::prusa_slicer_version_2_9_5()`, reject fixture paths other than `PRUSA_PROJECT_FILE_FIXTURE_PATH`, reject unsupported archive members/markers/deferred semantics, reject duplicate row keys, reject missing expected row keys, and reject any extra row outside the seven Phase 42 expected rows. It must validate the exact seven rows from `expected-project-summary.tsv`, while keeping marker semantics presence-level only per D-06.

    Add `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_project_file.rs`. Use `include_str!("../../../../parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv")` for checked-in fixture summary tests per D-13. Use Arrange/Act/Assert comments for non-trivial setup per D-12. Re-export the new module and public types/functions from `src/lib.rs` as `pub mod prusa_project_file;` plus `pub use prusa_project_file::{...};`.
  </action>
  <verify>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_project_file</automated>
  </verify>
  <acceptance_criteria>
    - `rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_project_file` passes.
    - `rg -n 'pub mod prusa_project_file|parse_prusa_project_file_summary|prusa_project_file_metadata|prusa_project_file_summary_lines' packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs` finds all four public surface names.
    - `bash -lc 'set -euo pipefail; for name in PRUSA_PROJECT_FILE_INVENTORY_ID PRUSA_PROJECT_FILE_SOURCE_PATH PRUSA_PROJECT_FILE_SOURCE_REF; do rg -n "pub\\(crate\\) const ${name}\\b" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs; done'` proves the shared constants consumed by Plan 43-02 have `pub(crate)` visibility.
    - `rg -n 'std::fs|std::process|std::env|std::net|Command|read_to_string|File::|Tcp|Udp|git|vendor sync|profile auto-update' packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs` returns no matches.
    - `rg -n 'packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv|packages/prusa-project-file-scope/project-file-scope.md|fork\.prusaslicer\.project-file' packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_project_file.rs` finds traceability strings.
    - `rg -n 'full 3MF import/export|GUI project behavior|generated-output parity|load/save behavior|mesh geometry|profile semantics' packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs` returns no matches outside error/test strings that explicitly reject overclaiming.
  </acceptance_criteria>
  <done>The crate exposes a pure, typed `slic3r_flavors::prusa_project_file` parser/metadata API with focused Cargo tests and no production side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Wire project-file Rust tests into Bazel and aggregate verification</name>
  <read_first>
    - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
    - packages/slic3r-rust/BUILD.bazel
    - packages/parity-fixtures/BUILD.bazel
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs
    - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_project_file.rs
  </read_first>
  <files>packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel, packages/slic3r-rust/BUILD.bazel</files>
  <action>
    Update `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` per D-14. Add `src/prusa_project_file.rs` to the `rust_library(name = "slic3r_flavors", srcs = [...])` list. Add a `rust_test(name = "prusa_project_file_test", srcs = ["tests/prusa_project_file.rs"], compile_data = ["//packages/parity-fixtures:prusa_project_file_expected_project_summary"], deps = [":slic3r_flavors"], edition = "2024")`.

    Add `:prusa_project_file_test` to both the crate `rust_clippy(name = "clippy", deps = [...])` deps and `rustfmt_test(name = "rustfmt_check", targets = [...])` targets. Update `packages/slic3r-rust/BUILD.bazel` so the aggregate `test_suite(name = "verify", tests = [...])` includes `//packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_test`. Do not create a `rust_binary`, a parity command, a new workspace crate, or any `packages/parity/status.tsv` row.
  </action>
  <verify>
    <automated>rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all && rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings && rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features && rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features && bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_test //packages/slic3r-rust:verify</automated>
  </verify>
  <acceptance_criteria>
    - All four Rust pre-commit commands pass with the pinned toolchain: `cargo fmt`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_test //packages/slic3r-rust:verify` passes.
    - `rg -n 'prusa_project_file_test|src/prusa_project_file.rs|prusa_project_file_expected_project_summary' packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel packages/slic3r-rust/BUILD.bazel` finds the source, test, compile_data, clippy/rustfmt, and aggregate verify wiring.
    - `bazel query //packages/parity:prusaslicer_project_file_parity` still fails because Phase 44 owns that target.
    - `rg -n 'fork\.prusaslicer\.project-file' packages/parity/status.tsv` returns no matches.
    - `git diff --check` passes.
  </acceptance_criteria>
  <done>The new Rust parser tests run through Cargo, crate-local Bazel, clippy/rustfmt, and the aggregate `//packages/slic3r-rust:verify` target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TSV text -> Rust parser | Caller-supplied summary text enters typed Rust domain code and must be validated before use. |
| Fixture metadata -> Rust traceability | Source ref, fixture path, expected summary path, scope record path, and future status token must not drift from Phase 41/42 evidence. |
| Test fixture loading -> production code | Tests may use `include_str!`, but production code must remain side-effect free. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01-01 | Tampering | `parse_prusa_project_file_summary` | mitigate | Validate exact header, column count, source ref, fixture path, allowed members, allowed markers, allowed deferred semantics, duplicate rows, missing rows, and extra rows with typed errors. |
| T-43-01-02 | Spoofing | `PrusaProjectFileMetadata` | mitigate | Use `VendorSourceRef::prusa_slicer_version_2_9_5()`, `FlavorId::PrusaSlicer`, `FeatureOrigin::SharedDownstream`, `ParitySurface::file_formats()`, and exact Phase 41/42 paths. |
| T-43-01-03 | Repudiation | `reserved_future_status_token` | mitigate | Name the token only as `reserved_future_status_token`; tests and acceptance greps keep `packages/parity/status.tsv` unchanged until Phase 44. |
| T-43-01-04 | Elevation of Privilege | production Rust module | mitigate | Prohibit `std::fs`, `std::process`, `std::env`, `std::net`, Git, network, profile auto-update, release, and vendor sync behavior in production code. |
| T-43-01-05 | Information Disclosure | test fixture access | accept | Tests use checked-in `include_str!` fixture summary text only; no secrets, credentials, or external service calls are involved. |
</threat_model>

<verification>
Run the full Plan 43-01 verification:

```bash
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_test //packages/slic3r-rust:verify
bash -lc '! bazel query //packages/parity:prusaslicer_project_file_parity >/tmp/phase43-plan01-project-file-target.out 2>&1'
bash -lc '! rg -n "fork\\.prusaslicer\\.project-file" packages/parity/status.tsv'
bash -lc '! rg -n "std::fs|std::process|std::env|std::net|Command|read_to_string|File::|Tcp|Udp|git|vendor sync|profile auto-update" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs'
git diff --check
node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify schema-drift 43
```
</verification>

<success_criteria>
PPROJ-01, PPROJ-02, and PPROJ-03 are materially satisfied for the Rust parser boundary: the new module parses the selected expected summary into typed values, exposes source/fixture/scope/status-token metadata, has focused Rust tests, is wired into Cargo and Bazel verification, and performs no production side effects.
</success_criteria>

<output>
After completion, create `.planning/phases/43-rust-prusa-project-file-boundary/43-01-SUMMARY.md` with `requirements-completed: [PPROJ-01, PPROJ-02, PPROJ-03]`.
</output>
