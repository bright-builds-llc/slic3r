---
phase: 41-prusa-project-file-scope-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/prusa-project-file-scope/BUILD.bazel
  - packages/prusa-project-file-scope/README.md
  - packages/prusa-project-file-scope/project-file-scope.md
  - packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh
  - packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/migration-guidance.md
  - docs/port/parity-matrix.md
autonomous: true
requirements: [PSEL-01, PSEL-02]
requirements_addressed: [PSEL-01, PSEL-02]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 41-2026-06-03T01-42-30
generated_at: 2026-06-03T01:57:57Z
must_haves:
  truths:
    - "Maintainer can inspect a checked-in prusaslicer.project-file scope record with every PSEL-01 field populated."
    - "Maintainer can trace the scope record to the accepted Prusa source identity and packages/fork-inventories/prusaslicer.tsv row."
    - "Maintainer can inspect the fixture source strategy, expected-artifact contract, candidate Rust boundary, planned evidence command, planned status token, docs touched, license/security note, deferred scope, and reviewer signoff before later phases consume the gate."
    - "Maintainer can run a local Bazel verifier and failure-mode test that fail closed on missing record fields, wrong source identity, missing deferrals, and premature parity/status wording."
    - "Maintainer can distinguish the narrow project-file evidence contract from full 3MF import/export, full PrusaSlicer runtime support, GUI behavior, generated-output parity, STEP import, support generation, arc fitting, wall seam behavior, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, and sync automation."
  artifacts:
    - path: "packages/prusa-project-file-scope/project-file-scope.md"
      provides: "Checked-in Phase 41 prusaslicer.project-file scope record"
      contains: "prusaslicer.project-file"
    - path: "packages/prusa-project-file-scope/README.md"
      provides: "Package entrypoint, verifier command, and non-overclaiming boundary"
      contains: "Phase 41 verification does not prove executable Prusa project-file parity"
    - path: "packages/prusa-project-file-scope/BUILD.bazel"
      provides: "Bazel package boundary, verify target, verifier sh_test, and package_boundary filegroup"
      contains: "verify_prusa_project_file_scope_test"
    - path: "packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh"
      provides: "Fail-closed exact-text verifier for the scope package"
      contains: "set -euo pipefail"
    - path: "packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh"
      provides: "Failure-mode tests for missing Phase 41 scope contract fields and overclaiming wording"
      contains: "test_missing"
    - path: "docs/port/README.md"
      provides: "Control-plane route to the Phase 41 project-file scope package"
      contains: "packages/prusa-project-file-scope"
    - path: "docs/port/package-map.md"
      provides: "Package-map role entry for packages/prusa-project-file-scope"
      contains: "packages/prusa-project-file-scope"
    - path: "docs/port/migration-guidance.md"
      provides: "Future fork fixture/status routing for the project-file evidence contract"
      contains: "prusaslicer.project-file"
    - path: "docs/port/parity-matrix.md"
      provides: "Human-facing non-overclaiming note for the project-file scope gate"
      contains: "fork.prusaslicer.project-file"
  key_links:
    - from: "packages/prusa-project-file-scope/project-file-scope.md"
      to: "packages/fork-inventories/prusaslicer.tsv"
      via: "Inventory row ID and source row details"
      pattern: "prusaslicer\\.project-file"
    - from: "packages/prusa-project-file-scope/BUILD.bazel"
      to: "packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh"
      via: 'sh_binary(name = "verify")'
      pattern: "sh_binary\\("
    - from: "packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh"
      to: "packages/prusa-project-file-scope/project-file-scope.md"
      via: "Exact Scope Record and Source Row Details table checks"
      pattern: "project-file-scope\\.md"
    - from: "docs/port/README.md"
      to: "packages/prusa-project-file-scope/README.md"
      via: "Package path and verify command in control-plane docs"
      pattern: "packages/prusa-project-file-scope"
    - from: "docs/port/package-map.md"
      to: "packages/prusa-project-file-scope"
      via: "Package table role row"
      pattern: "packages/prusa-project-file-scope"

---

<objective>
Create the Phase 41 Prusa project-file scope gate package for the narrow
`prusaslicer.project-file` evidence contract.

Purpose: Let maintainers review and lock the project-file evidence contract
before fixture bytes, expected artifacts, Rust parser code, parity commands,
status rows, or broad runtime claims exist.

Output: A dedicated `packages/prusa-project-file-scope` package with a
checked-in scope record, package README, fail-closed Bazel verifier, verifier
failure-mode tests, and conservative routing from the port control-plane docs.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-prusa-project-file-scope-gate/41-CONTEXT.md
@.planning/phases/41-prusa-project-file-scope-gate/41-RESEARCH.md
@.planning/milestones/v1.10-phases/37-prusa-baseline-and-checklist-gate/37-01-SUMMARY.md
@.planning/milestones/v1.10-phases/38-prusa-fixture-and-status-evidence-surface/38-01-SUMMARY.md
@.planning/milestones/v1.10-phases/39-rust-prusa-profile-boundary/39-01-SUMMARY.md
@.planning/milestones/v1.10-phases/39-rust-prusa-profile-boundary/39-02-SUMMARY.md
@.planning/milestones/v1.10-phases/40-executable-prusa-profile-parity/40-01-SUMMARY.md
@.planning/milestones/v1.10-phases/40-executable-prusa-profile-parity/40-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/fork-inventories/prusaslicer.tsv
@packages/fork-inventories/category-map.tsv
@packages/prusa-baseline/BUILD.bazel
@packages/prusa-baseline/README.md
@packages/prusa-baseline/profile-schema-checklist.md
@packages/prusa-baseline/verify_prusa_baseline.sh
@packages/prusa-baseline/verify_prusa_baseline_test.sh
@packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
@packages/parity/status.tsv
@docs/port/README.md
@docs/port/package-map.md
@docs/port/migration-guidance.md
@docs/port/parity-matrix.md

Bright Builds standards loaded for this plan: pinned `standards/index.md`,
`standards/core/architecture.md`, `standards/core/code-shape.md`,
`standards/core/verification.md`, `standards/core/testing.md`, and
`standards/languages/rust.md` at commit
`05f8d7a6c9c2e157ec4f922a05273e72dab97676`. The applicable rules are
repo-native verification, checked-in scripts instead of hidden inline logic,
focused failure-mode tests, and conservative docs/status claims.

<interfaces>
Existing Phase 37 Bazel gate pattern:

```python
package(default_visibility = ["//visibility:public"])

exports_files([
    "README.md",
    "drift-refresh-record.md",
    "profile-schema-checklist.md",
])

sh_binary(
    name = "verify",
    srcs = ["verify_prusa_baseline.sh"],
    data = [
        "README.md",
        "drift-refresh-record.md",
        "profile-schema-checklist.md",
    ],
    args = [
        "$(location README.md)",
        "$(location drift-refresh-record.md)",
        "$(location profile-schema-checklist.md)",
    ],
)

sh_test(
    name = "verify_prusa_baseline_test",
    srcs = ["verify_prusa_baseline_test.sh"],
    data = ["verify_prusa_baseline.sh"],
)
```

Existing verifier helper pattern:

```bash
require_section_table_row() {
	local file="$1"
	local label="$2"
	local section="$3"
	local field="$4"
	local value="$5"
	local row="| ${field} | ${value} |"

	if ! awk -v section="${section}" -v row="${row}" '
		$0 == section { in_section = 1; next }
		in_section && /^## / { exit }
		in_section && $0 == row { found = 1; exit }
		END { exit found ? 0 : 1 }
	' "${file}"; then
		error "${label}: missing required table row in ${section}: ${row}"
	fi
}
```

Accepted Prusa project-file source facts:

```text
Inventory row ID: prusaslicer.project-file
Vendor ID: prusaslicer
Accepted source identity: prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
Accepted tag: version_2.9.5
Peeled commit: 9a583bd438b195856f3bcf7ea99b69ba4003a961
Source path: src/libslic3r/Format/3mf.cpp
Feature surface: project-file
Feature category: project-file
Ownership: shared-downstream
Complexity: medium
Parity dependency: file-formats
Inventory decision: future-candidate
Caution flags: none
Inventory note: Source-observed project file planning row; future parity requires fixture-backed load/save evidence.
Existing registry row: packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs has capability_id "prusaslicer.project-file" and source path "src/libslic3r/Format/3mf.cpp".
```

</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
|----------|------|------|----------|-------|
| D-01 | 41-01 | 1 | Full | Scope record is checked in at `packages/prusa-project-file-scope/project-file-scope.md`. |
| D-02 | 41-01 | 1, 2 | Full | Mandatory PSEL-01 fields are exact table rows and verifier checks. |
| D-03 | 41-01 | 1 | Full | Scope package is checked-in Markdown plus Bazel-owned verification. |
| D-04 | 41-01 | 1, 2 | Full | Scope row starts from `packages/fork-inventories/prusaslicer.tsv`. |
| D-05 | 41-01 | 1, 2 | Full | Accepted source identity remains `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`. |
| D-06 | 41-01 | 1, 3 | Full | Source path is `src/libslic3r/Format/3mf.cpp`; full 3MF import/export is deferred. |
| D-07 | 41-01 | 1 | Full | Fixture source and expected artifact are contract rows only; no Phase 42 files are created. |
| D-08 | 41-01 | 1, 2 | Full | Contract names Phase 42 provenance/update/expected-artifact shape and verifier enforces the rows. |
| D-09 | 41-01 | 1, 2, 3 | Full | Plan repeats the v1.10 trust chain as scoped contract, verifier, downstream command/status wording. |
| D-10 | 41-01 | 1 | Full | Candidate Rust boundary and planned evidence command are named but not created. |
| D-11 | 41-01 | 1, 3 | Full | Record names downstream fixture path, Rust boundary, planned status token, and command. |
| D-12 | 41-01 | 1, 3 | Full | Planned status token stays `fork.prusaslicer.project-file` and no status row is published. |
| D-13 | 41-01 | 1, 2, 3 | Full | Deferred scope list is explicit and verifier/docs require it. |
| D-14 | 41-01 | 3 | Full | Docs clarify contract and deferrals without executable parity claims. |
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Create the checked-in project-file scope record</name>
  <files>packages/prusa-project-file-scope/BUILD.bazel, packages/prusa-project-file-scope/README.md, packages/prusa-project-file-scope/project-file-scope.md</files>
  <read_first>
    - `packages/prusa-project-file-scope/BUILD.bazel` if it already exists; otherwise read `packages/prusa-baseline/BUILD.bazel`
    - `packages/prusa-project-file-scope/README.md` if it already exists; otherwise read `packages/prusa-baseline/README.md`
    - `packages/prusa-project-file-scope/project-file-scope.md` if it already exists; otherwise read `packages/prusa-baseline/profile-schema-checklist.md`
    - `.planning/phases/41-prusa-project-file-scope-gate/41-CONTEXT.md` decisions D-01 through D-14
    - `.planning/phases/41-prusa-project-file-scope-gate/41-RESEARCH.md` Summary, Architecture Patterns, Recommended Actions, and Common Pitfalls
    - `.planning/REQUIREMENTS.md` PSEL-01, PSEL-02, and Out of Scope
    - `packages/fork-inventories/prusaslicer.tsv`
    - `packages/fork-inventories/category-map.tsv`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs`
  </read_first>
  <action>
    Create `packages/prusa-project-file-scope/` as the Phase 41 scope package, not as a fixture, parser, parity, or status package.

```
Create `packages/prusa-project-file-scope/BUILD.bazel` as a minimal package skeleton with:
- `package(default_visibility = ["//visibility:public"])`
- `exports_files(["README.md", "project-file-scope.md"])`

Do not add `sh_binary`, `sh_test`, or `package_boundary` in Task 1 because the verifier script and failure-mode test files are created by Task 2.

Create `packages/prusa-project-file-scope/README.md` with these exact anchors:
- `packages/prusa-project-file-scope` owns the Phase 41 reviewed scope gate for `prusaslicer.project-file`.
- Verification command: `bazel run //packages/prusa-project-file-scope:verify`.
- `Phase 41 verification does not prove executable Prusa project-file parity.`
- `Phase 41 verification does not prove full 3MF import/export, full PrusaSlicer runtime support, or GUI project behavior.`
- `This package creates no fixture bytes, expected artifacts, Rust parser, parity command, status row, upstream source import, vendored fork source tree, Git/network/vendor sync behavior, profile auto-update execution, network/device integration, credential handling, Bambu Studio support, OrcaSlicer support, or fork release build.`
- Link the record as `project-file-scope.md`.

Create `packages/prusa-project-file-scope/project-file-scope.md` with `# Prusa Project-File Scope Gate` and a `## Scope Record` table whose rows are exactly:
```

```markdown
| Required Field | Maintainer Entry |
| --- | --- |
| Inventory row ID | `prusaslicer.project-file` |
| Accepted source identity | `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961` |
| Inventory row source | `packages/fork-inventories/prusaslicer.tsv` |
| Source path | `src/libslic3r/Format/3mf.cpp` |
| Companion API evidence | `src/libslic3r/Format/3mf.hpp` |
| Fixture source decision | Phase 42 source-pinned upstream `tests/data/seam_test_object.3mf`; no fixture bytes are checked in during Phase 41. |
| Expected-artifact contract | Phase 42 `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv` with `source_ref`, `fixture_path`, `archive_member`, `project_marker`, `deferred_semantics`, and `notes` columns; no expected artifact is checked in during Phase 41. |
| Candidate Rust boundary | Phase 43 `slic3r_flavors::prusa_project_file` data-in/data-out project summary boundary; no Rust parser is created in Phase 41. |
| Planned evidence command | Phase 44 command text `bazel run //packages/parity:prusaslicer_project_file_parity`; the target is not created in Phase 41. |
| Planned status token | Phase 44 token `fork.prusaslicer.project-file` after executable evidence passes; no `packages/parity/status.tsv` row is published in Phase 41. |
| Docs touched | `docs/port/README.md`; `docs/port/package-map.md`; `docs/port/migration-guidance.md`; `docs/port/parity-matrix.md` |
| License or security note | `AGPL-3.0-only`; metadata-only-not-legal-review; no upstream source import; no Git, network, vendor sync, profile auto-update execution, credential, cloud, or network/device behavior in Phase 41. |
| Deferred scope | Full PrusaSlicer runtime support; GUI project behavior; full 3MF import/export; generated-output parity; STEP import; support generation; arc fitting; wall seam behavior; network/device integration; profile auto-update execution; fork release builds; Bambu Studio; OrcaSlicer; upstream source imports; sync automation. |
| Reviewer signoff | Peter Ryszkiewicz, 2026-06-03 UTC |
```

```
Add a `## Source Row Details` table with these exact rows:
```

```markdown
| Field | Value |
| --- | --- |
| Source path | `src/libslic3r/Format/3mf.cpp` |
| Feature surface | `project-file` |
| Feature category | `project-file` |
| Ownership | `shared-downstream` |
| Complexity | `medium` |
| Parity dependency | `file-formats` |
| Inventory decision | `future-candidate` |
| Caution flags | `none` |
| Inventory note | Source-observed project file planning row; future parity requires fixture-backed load/save evidence. |
```

```
Add a `## Boundary` section that says the record is a Phase 41 scope gate only and must not be treated as fixture evidence, Rust parser readiness, status publication, or executable parity.
```

</action>
  <acceptance_criteria>
    - `test -f packages/prusa-project-file-scope/BUILD.bazel && test -f packages/prusa-project-file-scope/README.md && test -f packages/prusa-project-file-scope/project-file-scope.md` succeeds.
    - `rg -n "sh_binary|sh_test|package_boundary|verify_prusa_project_file_scope" packages/prusa-project-file-scope/BUILD.bazel` returns no matches.
    - `rg -n "prusaslicer\\.project-file|prusaslicer:version_2\\.9\\.5@9a583bd438b195856f3bcf7ea99b69ba4003a961|src/libslic3r/Format/3mf\\.cpp|tests/data/seam_test_object\\.3mf|expected-project-summary\\.tsv|slic3r_flavors::prusa_project_file|prusaslicer_project_file_parity|fork\\.prusaslicer\\.project-file|Peter Ryszkiewicz, 2026-06-03 UTC" packages/prusa-project-file-scope/project-file-scope.md` finds every required anchor.
    - `rg -n "Phase 41 verification does not prove executable Prusa project-file parity|creates no fixture bytes, expected artifacts, Rust parser, parity command, status row" packages/prusa-project-file-scope/README.md` finds the non-overclaiming anchors.
    - `rg -n "tests/data/seam_test_object\\.3mf|expected-project-summary\\.tsv" packages/parity-fixtures/forks/prusaslicer 2>/dev/null` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bazel query //packages/prusa-project-file-scope:README.md</automated>
    <automated>bazel query //packages/prusa-project-file-scope:project-file-scope.md</automated>
    <automated>bash -lc '! rg -n "sh_binary|sh_test|package_boundary|verify_prusa_project_file_scope" packages/prusa-project-file-scope/BUILD.bazel'</automated>
    <automated>rg -n "prusaslicer\\.project-file|prusaslicer_project_file_parity|fork\\.prusaslicer\\.project-file" packages/prusa-project-file-scope/README.md packages/prusa-project-file-scope/project-file-scope.md</automated>
  </verify>
  <done>The Phase 41 package skeleton and scope record exist with all PSEL-01 fields, source row details, and explicit Phase 41-only boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fail-closed scope verification and failure-mode tests</name>
  <files>packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh, packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh, packages/prusa-project-file-scope/BUILD.bazel</files>
  <read_first>
    - `packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh` if it already exists; otherwise read `packages/prusa-baseline/verify_prusa_baseline.sh`
    - `packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh` if it already exists; otherwise read `packages/prusa-baseline/verify_prusa_baseline_test.sh`
    - `packages/prusa-project-file-scope/BUILD.bazel`
    - `packages/prusa-project-file-scope/README.md`
    - `packages/prusa-project-file-scope/project-file-scope.md`
    - `.planning/phases/41-prusa-project-file-scope-gate/41-CONTEXT.md` decisions D-02, D-05, D-07, D-08, D-10, D-13, D-14
    - `.planning/phases/41-prusa-project-file-scope-gate/41-RESEARCH.md` Pattern 2 and Common Pitfalls
  </read_first>
  <behavior>
    - Complete README plus scope record passes and prints `ok: Prusa project-file scope verification passed`.
    - Missing or wrong `prusaslicer.project-file` row fails with `error:` and `Inventory row ID`.
    - Missing or wrong accepted source identity fails with `error:` and the accepted source pin.
    - Missing fixture source decision, expected-artifact contract, candidate Rust boundary, planned evidence command, planned status token, docs touched, license/security note, deferred scope, or reviewer signoff fails with an exact field diagnostic.
    - Missing deferral terms such as full 3MF import/export, full PrusaSlicer runtime support, GUI project behavior, Bambu Studio, OrcaSlicer, upstream source imports, or sync automation fails.
    - README text that omits the Phase 41-only non-overclaiming phrases fails.
  </behavior>
  <action>
    Implement `verify_prusa_project_file_scope.sh` as a Bash 3.2-compatible checked-in script with `#!/usr/bin/env bash` and `set -euo pipefail`. Match the Phase 37 verifier style: `error`, `require_file`, `require_text`, `require_section_table_row`, and `require_count_at_least`.

```
The script must accept either no args, using package-local `README.md` and `project-file-scope.md`, or exactly two args: `README project-file-scope`. Any other arg count must fail with `usage: verify_prusa_project_file_scope.sh [README project-file-scope]`.

Required verifier checks:
- README contains `bazel run //packages/prusa-project-file-scope:verify`.
- README contains both non-overclaiming sentences from Task 1.
- README contains the exact no-created-artifacts sentence from Task 1.
- `## Scope Record` contains every exact row from Task 1.
- `## Source Row Details` contains every exact row from Task 1.
- The scope record contains at least two occurrences of `Phase 41`.
- The deferred scope contains every term from D-13: full PrusaSlicer runtime support, GUI project behavior, full 3MF import/export, generated-output parity, STEP import, support generation, arc fitting, wall seam behavior, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, and sync automation.
- The scope record and README do not claim executable parity; enforce by requiring the sentences that say no parser, no command target, and no status row in Phase 41.

Implement `verify_prusa_project_file_scope_test.sh` with temp fixture files and focused test functions. Use Arrange/Act/Assert comments for each test. Include at least these tests:
- `test_complete_fixture_passes`
- `test_wrong_inventory_row_id_fails`
- `test_wrong_source_identity_fails`
- `test_missing_expected_artifact_contract_fails`
- `test_missing_planned_command_fails`
- `test_missing_status_token_fails`
- `test_missing_deferred_scope_term_fails`
- `test_missing_readme_no_created_artifacts_sentence_fails`

After creating the verifier files, update `packages/prusa-project-file-scope/BUILD.bazel` from the Task 1 skeleton by adding:
- `sh_binary(name = "verify", srcs = ["verify_prusa_project_file_scope.sh"], data = ["README.md", "project-file-scope.md"], args = ["$(location README.md)", "$(location project-file-scope.md)"])`
- `sh_test(name = "verify_prusa_project_file_scope_test", srcs = ["verify_prusa_project_file_scope_test.sh"], data = ["verify_prusa_project_file_scope.sh"])`
- `filegroup(name = "package_boundary", srcs = ["README.md", "project-file-scope.md", "verify_prusa_project_file_scope.sh", "verify_prusa_project_file_scope_test.sh"])`
```

</action>
  <acceptance_criteria>
    - `bash packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh` prints `ok: Prusa project-file scope verification passed`.
    - `bash packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh` passes and includes the test names listed in this task.
    - `rg -n "require_section_table_row|require_count_at_least|usage: verify_prusa_project_file_scope\\.sh \\[README project-file-scope\\]|ok: Prusa project-file scope verification passed" packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh` finds the verifier anchors.
    - `rg -n "test_wrong_inventory_row_id_fails|test_wrong_source_identity_fails|test_missing_expected_artifact_contract_fails|test_missing_planned_command_fails|test_missing_status_token_fails|test_missing_deferred_scope_term_fails|test_missing_readme_no_created_artifacts_sentence_fails" packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh` finds the failure-mode tests.
    - `bazel query //packages/prusa-project-file-scope:package_boundary` returns the package boundary target after `verify_prusa_project_file_scope.sh` and `verify_prusa_project_file_scope_test.sh` exist.
    - `rg -n "bash -c|eval|curl|git clone|git fetch|bazel run //packages/parity:prusaslicer_project_file_parity" packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bash packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh</automated>
    <automated>bash packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh</automated>
    <automated>bazel query //packages/prusa-project-file-scope:package_boundary</automated>
    <automated>bazel run //packages/prusa-project-file-scope:verify</automated>
    <automated>bazel test //packages/prusa-project-file-scope:verify_prusa_project_file_scope_test</automated>
    <automated>shfmt -d packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh</automated>
  </verify>
  <done>The package verifier and shell failure-mode tests fail closed on missing project-file scope fields, source identity drift, missing deferrals, and premature executable/status wording.</done>
</task>

<task type="auto">
  <name>Task 3: Route the scope package through docs and prove no later-phase artifacts were added</name>
  <files>docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <read_first>
    - `docs/port/README.md`
    - `docs/port/package-map.md`
    - `docs/port/migration-guidance.md`
    - `docs/port/parity-matrix.md`
    - `packages/prusa-project-file-scope/README.md`
    - `packages/prusa-project-file-scope/project-file-scope.md`
    - `.planning/phases/41-prusa-project-file-scope-gate/41-CONTEXT.md` decisions D-11, D-12, D-13, D-14
    - `.planning/ROADMAP.md` Phase 41 through Phase 44 goals
    - `.planning/REQUIREMENTS.md` PSEL-02 and Out of Scope
    - `.planning/milestones/v1.10-phases/40-executable-prusa-profile-parity/40-02-SUMMARY.md`
  </read_first>
  <action>
    Update docs only as needed to make the Phase 41 scope gate discoverable and non-overclaiming.

```
Required docs outcomes:
- `docs/port/README.md`: add a `Current Prusa Project-File Scope Gate State` section or equivalent nearby section that names `packages/prusa-project-file-scope`, `bazel run //packages/prusa-project-file-scope:verify`, `prusaslicer.project-file`, accepted source identity `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, source path `src/libslic3r/Format/3mf.cpp`, Phase 42 fixture path contract `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/`, Phase 43 Rust boundary `slic3r_flavors::prusa_project_file`, Phase 44 command `bazel run //packages/parity:prusaslicer_project_file_parity`, and Phase 44 status token `fork.prusaslicer.project-file`.
- `docs/port/package-map.md`: add `packages/prusa-project-file-scope` to the package table and add a Phase 41 note saying it owns the checked-in scope record and verifier only.
- `docs/port/migration-guidance.md`: update future fork fixture/status guidance so `prusaslicer.project-file` points to the Phase 41 scope record before Phase 42 fixture work and keeps `fork.prusaslicer.project-file` unavailable until a real Phase 44 parity command exists.
- `docs/port/parity-matrix.md`: add fork parity interpretation wording saying `fork.prusaslicer.project-file` is not a verified row and `prusaslicer.project-file` scope records do not prove full 3MF import/export or Prusa runtime support.

Keep the docs concise. Do not add fixture bytes, expected artifact files, Rust modules, Bazel parity targets, status rows, Bambu Studio docs, OrcaSlicer docs, upstream source imports, or sync automation.
```

</action>
  <acceptance_criteria>
    - `rg -n "packages/prusa-project-file-scope|bazel run //packages/prusa-project-file-scope:verify|prusaslicer\\.project-file|prusaslicer:version_2\\.9\\.5@9a583bd438b195856f3bcf7ea99b69ba4003a961|src/libslic3r/Format/3mf\\.cpp|prusaslicer_project_file_parity|fork\\.prusaslicer\\.project-file" docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md` finds the docs anchors.
    - `rg -n "full 3MF import/export|full PrusaSlicer runtime support|GUI project behavior|generated-output parity|STEP import|support generation|arc fitting|wall seam behavior|network/device integration|profile auto-update execution|fork release builds|Bambu Studio|OrcaSlicer|upstream source imports|sync automation" docs/port/README.md docs/port/migration-guidance.md docs/port/parity-matrix.md` finds the deferral anchors.
    - `rg -n "fork\\.prusaslicer\\.project-file|prusaslicer_project_file_parity|expected-project-summary\\.tsv" packages/parity/status.tsv packages/parity/BUILD.bazel packages/parity-fixtures/BUILD.bazel packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` returns matches only in the existing registry if they pre-existed; no status row, parity target, expected artifact export, or Rust parser module is added by this task.
    - `find packages/parity-fixtures/forks/prusaslicer -path "*prusaslicer.project-file*" -print` prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>bazel run //packages/prusa-project-file-scope:verify</automated>
    <automated>bazel test //packages/prusa-project-file-scope:verify_prusa_project_file_scope_test</automated>
    <automated>mdformat --check packages/prusa-project-file-scope/README.md packages/prusa-project-file-scope/project-file-scope.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md</automated>
    <automated>bash -lc '! bazel query //packages/parity:prusaslicer_project_file_parity >/tmp/phase41-prusa-project-file-target.out 2>&1'</automated>
    <automated>bash -lc '! rg -n "fork\\.prusaslicer\\.project-file|prusaslicer_project_file_parity" packages/parity/status.tsv'</automated>
    <automated>bash -lc '! find packages/parity-fixtures/forks/prusaslicer -path "*prusaslicer.project-file*" -print -quit | grep .'</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>The port docs route maintainers to the Phase 41 scope gate and preserve the boundary that fixtures, Rust parser work, parity command, and status publication belong to Phases 42-44.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checked-in scope text -> verifier | Markdown is untrusted review text; verifier must check exact fixed strings and never execute field values. |
| Source inventory -> scope record | Phase 41 copies source identity from fork inventory into a maintainer record; tampering would mislead later fixture and parser work. |
| Scope docs -> downstream phases | Later phases consume fixture paths, command names, status token, and deferred scope from this package. |
| Scope package -> public parity/status surfaces | Phase 41 must not publish executable parity evidence, status rows, fixture bytes, or parser code prematurely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | `project-file-scope.md` source identity rows | mitigate | Task 2 verifier checks exact `prusaslicer.project-file`, source pin, source path, inventory row source, and source row details from `packages/fork-inventories/prusaslicer.tsv`. |
| T-41-02 | Spoofing | README/docs parity wording | mitigate | Tasks 1 and 3 require exact text that Phase 41 does not prove executable Prusa project-file parity, full 3MF import/export, full runtime support, or GUI behavior. |
| T-41-03 | Repudiation | Reviewer signoff field | mitigate | Task 1 records exact `Reviewer signoff | Peter Ryszkiewicz, 2026-06-03 UTC`; Task 2 fails if the row is missing or changed. |
| T-41-04 | Elevation of Privilege | Planned command text in Markdown | mitigate | Task 2 verifier treats command text as display-only fixed text and forbids `eval`, `bash -c`, `curl`, `git clone`, `git fetch`, or execution of `//packages/parity:prusaslicer_project_file_parity`. |
| T-41-05 | Information Disclosure / Tampering | Upstream source tree and network behavior | mitigate | Tasks 1-3 require no upstream source imports, no vendored fork source tree, no Git/network/vendor sync behavior, and local-only Bazel/Bash verification. |
| T-41-06 | Repudiation | Premature status or evidence publication | mitigate | Task 3 requires negative guards proving no `packages/parity/status.tsv` row, no `//packages/parity:prusaslicer_project_file_parity` target, and no `prusaslicer.project-file` fixture namespace. |
| T-41-07 | Repudiation | Legal/security overclaiming | mitigate | Task 1 records `metadata-only-not-legal-review`; docs must not claim legal review, runtime security review, credential review, cloud/network review, or plugin ingestion approval. |
</threat_model>

<verification>
Overall phase checks:

```bash
bazel run //packages/prusa-project-file-scope:verify
bazel test //packages/prusa-project-file-scope:verify_prusa_project_file_scope_test
shfmt -d packages/prusa-project-file-scope/verify_prusa_project_file_scope.sh packages/prusa-project-file-scope/verify_prusa_project_file_scope_test.sh
mdformat --check packages/prusa-project-file-scope/README.md packages/prusa-project-file-scope/project-file-scope.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md
bash -lc '! bazel query //packages/parity:prusaslicer_project_file_parity >/tmp/phase41-prusa-project-file-target.out 2>&1'
bash -lc '! rg -n "fork\\.prusaslicer\\.project-file|prusaslicer_project_file_parity" packages/parity/status.tsv'
bash -lc '! find packages/parity-fixtures/forks/prusaslicer -path "*prusaslicer.project-file*" -print -quit | grep .'
git diff --check
```

</verification>

<success_criteria>

- `packages/prusa-project-file-scope/project-file-scope.md` contains every PSEL-01 field with exact Phase 41 contract values.
- `//packages/prusa-project-file-scope:verify` passes and fails closed through shell tests when mandatory rows or deferrals are removed.
- Port docs route maintainers to `packages/prusa-project-file-scope` and keep `prusaslicer.project-file` separate from full 3MF import/export and broader PrusaSlicer runtime claims.
- No Phase 42 fixture namespace, Phase 43 Rust parser module, Phase 44 parity command, Phase 44 status row, upstream source import, Git/network/vendor sync behavior, Bambu Studio scope, or OrcaSlicer scope is introduced.
- Requirements `PSEL-01` and `PSEL-02` are materially complete.
</success_criteria>

<output>
After completion, create `.planning/phases/41-prusa-project-file-scope-gate/41-01-SUMMARY.md` with `requirements-completed: [PSEL-01, PSEL-02]`.
</output>
