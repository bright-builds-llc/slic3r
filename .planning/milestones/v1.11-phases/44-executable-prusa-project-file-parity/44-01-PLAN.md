---
phase: 44-executable-prusa-project-file-parity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_project_file_summary.rs
  - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
  - packages/parity/compare_prusaslicer_project_file.sh
  - packages/parity/compare_prusaslicer_project_file_test.sh
  - packages/parity/BUILD.bazel
autonomous: true
requirements: [PPEV-01, PPEV-02]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 44-2026-06-05T23-05-16
generated_at: 2026-06-05T23:19:54Z
must_haves:
  truths:
    - "Maintainer can run bazel run //packages/parity:prusaslicer_project_file_parity."
    - "The command prints fork.prusaslicer.project-file, the accepted Prusa source ref, fixture path, expected artifact path, and rows: 7."
    - "A mutated temporary expected-project-summary.tsv copy causes //packages/parity:prusaslicer_project_file_parity_failure_test to fail the comparator without mutating the checked-in expected artifact."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_project_file_summary.rs"
      provides: "Thin explicit-path Rust summary CLI for prusa_project_file_summary_lines"
    - path: "packages/parity/compare_prusaslicer_project_file.sh"
      provides: "Fail-closed project-file comparator for Rust-backed summary output"
    - path: "packages/parity/compare_prusaslicer_project_file_test.sh"
      provides: "Mutation failure guard for expected project summary divergence"
    - path: "packages/parity/BUILD.bazel"
      provides: "Public prusaslicer_project_file_parity sh_binary and failure sh_test"
  key_links:
    - from: "packages/parity/BUILD.bazel"
      to: "packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_summary"
      via: "sh_binary data and args"
      pattern: "prusa_project_file_summary"
    - from: "packages/parity/compare_prusaslicer_project_file.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv"
      via: "explicit rust-summary input and expected-artifact arguments"
      pattern: "expected-project-summary.tsv"
    - from: "packages/parity/compare_prusaslicer_project_file_test.sh"
      to: "packages/parity/compare_prusaslicer_project_file.sh"
      via: "temp mutated expected artifact passed as comparator expected side"
      pattern: "mutated_expected"
---

<objective>
Create the runnable Phase 44 Prusa project-file parity evidence command and its divergence failure guard.

Purpose: PPEV-01 and PPEV-02 require a maintainer-runnable Bazel command that uses the Phase 43 Rust summary boundary and a negative test proving expected-artifact drift is not accepted.
Output: Rust summary CLI, `packages/parity` comparator, Bazel parity target, and Bazel mutation failure test.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-executable-prusa-project-file-parity/44-CONTEXT.md
@.planning/phases/44-executable-prusa-project-file-parity/44-RESEARCH.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-01-SUMMARY.md
@.planning/phases/42-prusa-project-file-fixture-surface/42-02-SUMMARY.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-01-SUMMARY.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-02-SUMMARY.md
@.planning/milestones/v1.10-phases/40-executable-prusa-profile-parity/40-01-SUMMARY.md
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_profile_schema_summary.rs
@packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
@packages/parity/compare_prusaslicer_profile_schema.sh
@packages/parity/compare_prusaslicer_profile_schema_test.sh
@packages/parity/BUILD.bazel
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/fixture-provenance.tsv

<interfaces>
From `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs`:
```rust
pub fn prusa_project_file_summary_lines(
    input: &str,
) -> Result<Vec<String>, PrusaProjectFileParseError>;

pub const fn prusa_project_file_metadata() -> PrusaProjectFileMetadata;
```

Expected summary output keys from `prusa_project_file_summary_lines`:
```text
surface
inventory_id
vendor_id
flavor_id
origin
parity_dependency
checklist_status
source_ref
source_path
fixture_path
expected_summary_path
scope_record_path
reserved_future_status_token
row_count
evidence_row
```

Existing profile-summary CLI adapter pattern from `src/bin/prusa_profile_schema_summary.rs`:
```rust
let input = fs::read_to_string(path)
    .map_err(|error| format!("failed to read {}: {error}", path.display()))?;
let lines = prusa_profile_schema_summary_lines(&input)
    .map_err(|error| format!("failed to parse {}: {error:?}", path.display()))?;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Rust project-file summary CLI adapter</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_project_file_summary.rs, packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel</files>
  <read_first>
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_profile_schema_summary.rs
    - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
    - .planning/phases/44-executable-prusa-project-file-parity/44-CONTEXT.md
    - .planning/phases/44-executable-prusa-project-file-parity/44-RESEARCH.md
  </read_first>
  <action>Create `packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_project_file_summary.rs` as a thin explicit-path CLI per D-03. It must use `#![forbid(unsafe_code)]`, accept exactly one argument named by the error text `error: expected exactly one expected-project-summary.tsv fixture path argument`, read that path with `std::fs::read_to_string`, call `slic3r_flavors::prusa_project_file_summary_lines(&input)`, print each returned line with `println!("{line}")`, and return `ExitCode::FAILURE` with `error: failed to read {path}: ...` or `error: failed to summarize {path}: ...` diagnostics. Do not add network, Git, process spawning, filesystem discovery, profile auto-update, upstream import, release, or sync behavior per D-08. In `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel`, add `rust_binary(name = "prusa_project_file_summary", srcs = ["src/bin/prusa_project_file_summary.rs"], crate_root = "src/bin/prusa_project_file_summary.rs", deps = [":slic3r_flavors"], edition = "2024")` and add `:prusa_project_file_summary` to both the `rust_clippy(name = "clippy")` deps and the `rustfmt_test(name = "rustfmt_check")` targets.</action>
  <verify>
    <automated>rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all</automated>
    <automated>bazel query //packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_summary</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'prusa_project_file_summary_lines|expected exactly one expected-project-summary.tsv fixture path argument|fs::read_to_string' packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_project_file_summary.rs` returns matches.
    - `rg -n 'name = "prusa_project_file_summary"|:prusa_project_file_summary' packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` returns matches in the binary, clippy deps, and rustfmt targets.
    - `! rg -n 'std::process|std::net|Command|git|profile auto-update|vendor sync|upstream source' packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_project_file_summary.rs` succeeds.
  </acceptance_criteria>
  <done>The Rust CLI target builds under Bazel, is included in crate clippy/rustfmt coverage, and exposes only the existing Phase 43 data-in/data-out summary boundary through one explicit file path.</done>
</task>

<task type="auto">
  <name>Task 2: Add the public project-file parity comparator and Bazel command</name>
  <files>packages/parity/compare_prusaslicer_project_file.sh, packages/parity/BUILD.bazel</files>
  <read_first>
    - packages/parity/compare_prusaslicer_profile_schema.sh
    - packages/parity/BUILD.bazel
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/fixture-provenance.tsv
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs
    - .planning/phases/44-executable-prusa-project-file-parity/44-CONTEXT.md
  </read_first>
  <action>Create `packages/parity/compare_prusaslicer_project_file.sh` per D-01, D-02, D-04, D-05, and D-06. The script must use `#!/usr/bin/env bash` and `set -euo pipefail`, require exactly four arguments with the usage text `error: expected summary_binary rust-summary-input.tsv expected-project-summary.tsv fixture-provenance.tsv`, and assert the summary binary is executable and the three input files exist. To avoid the same-file self-comparison trap, name the second argument `rust_summary_input` and the third `expected_artifact`; generate `actual-summary.tsv` by running `${summary_binary} "${rust_summary_input}"`, generate `expected-summary-lines.tsv` by running `${summary_binary} "${expected_artifact}"`, and diff those generated files with `diff -u`. If Rust summary generation for `expected_artifact` fails, print `error: expected-project-summary.tsv failed Rust summary validation for ${expected_artifact}` and include `diff -u "${rust_summary_input}" "${expected_artifact}"` when the files differ. If generated summaries differ, print `error: expected-project-summary.tsv mismatch at ${mismatch_label} in ${expected_artifact}` and the unified diff. The mismatch label must use the first differing generated line: for `evidence_row` lines use `archive_member/project_marker/deferred_semantics` from fields 2-4; otherwise use field 1; use `line_count` for count-only differences. On success print exactly these labels: `ok: fork.prusaslicer.project-file parity passed`, `source_ref: prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, `fixture: packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/seam_test_object.3mf`, `expected: packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv`, and `rows: 7`. In `packages/parity/BUILD.bazel`, add `sh_binary(name = "prusaslicer_project_file_parity", srcs = ["compare_prusaslicer_project_file.sh"], data = ["status.tsv", "//packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_summary", "//packages/parity-fixtures:prusa_project_file_expected_project_summary", "//packages/parity-fixtures:prusa_project_file_provenance"], args = ["$(location //packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_summary)", "$(location //packages/parity-fixtures:prusa_project_file_expected_project_summary)", "$(location //packages/parity-fixtures:prusa_project_file_expected_project_summary)", "$(location //packages/parity-fixtures:prusa_project_file_provenance)"])`.</action>
  <verify>
    <automated>shfmt -d packages/parity/compare_prusaslicer_project_file.sh</automated>
    <automated>bazel query //packages/parity:prusaslicer_project_file_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_project_file_parity</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'expected summary_binary rust-summary-input.tsv expected-project-summary.tsv fixture-provenance.tsv|rust_summary_input|expected_artifact|fork\\.prusaslicer\\.project-file parity passed|rows: 7' packages/parity/compare_prusaslicer_project_file.sh` returns matches.
    - `rg -n 'name = "prusaslicer_project_file_parity"|compare_prusaslicer_project_file.sh|prusa_project_file_summary|prusa_project_file_expected_project_summary|prusa_project_file_provenance' packages/parity/BUILD.bazel` returns matches.
    - `bazel run //packages/parity:prusaslicer_project_file_parity` output includes `ok: fork.prusaslicer.project-file parity passed`, the accepted source ref, the fixture path, the expected artifact path, and `rows: 7`.
  </acceptance_criteria>
  <done>The public parity command exists, runs through Bazel, uses Rust-backed summary output, and reports the exact narrow evidence slice without adding status publication yet.</done>
</task>

<task type="auto">
  <name>Task 3: Add the mutation failure guard for expected-artifact divergence</name>
  <files>packages/parity/compare_prusaslicer_project_file_test.sh, packages/parity/BUILD.bazel</files>
  <read_first>
    - packages/parity/compare_prusaslicer_profile_schema_test.sh
    - packages/parity/compare_prusaslicer_project_file.sh
    - packages/parity/BUILD.bazel
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv
    - .planning/phases/44-executable-prusa-project-file-parity/44-CONTEXT.md
  </read_first>
  <action>Create `packages/parity/compare_prusaslicer_project_file_test.sh` per D-07 and D-08. The script must use the existing profile-schema failure test structure: compute `workspace_root`, locate `packages/parity/compare_prusaslicer_project_file.sh`, locate `packages/slic3r-rust/crates/slic3r_flavors/prusa_project_file_summary` or fall back to `bazel-bin/packages/slic3r-rust/crates/slic3r_flavors/prusa_project_file_summary`, copy the checked-in `expected-project-summary.tsv` to a temp `mutated_expected`, and call the comparator with original checked-in expected project summary as `rust_summary_input` and `mutated_expected` as `expected_artifact`. Mutate only the temp copy by swapping the two valid `3D/3dmodel.model` rows so the Rust parser still accepts both files and the comparator reaches the generated-summary diff path. Assert the comparator exits non-zero; assert stderr contains `expected-project-summary.tsv`, `3D/3dmodel.model`, `@@`, and `diff`; assert `mutated_expected` has `Application=PrusaSlicer-2.8.0-alpha3` before `slic3rpe:Version3mf`; and assert the checked-in expected artifact still contains the exact line with `3D/3dmodel.model	slic3rpe:Version3mf	member-marker-only`. Print `ok: prusaslicer_project_file_parity_failure_test` on success. Add `sh_test(name = "prusaslicer_project_file_parity_failure_test", srcs = ["compare_prusaslicer_project_file_test.sh"], data = ["compare_prusaslicer_project_file.sh", "//packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_summary", "//packages/parity-fixtures:prusa_project_file_expected_project_summary", "//packages/parity-fixtures:prusa_project_file_provenance"])` to `packages/parity/BUILD.bazel`.</action>
  <verify>
    <automated>shfmt -d packages/parity/compare_prusaslicer_project_file.sh packages/parity/compare_prusaslicer_project_file_test.sh</automated>
    <automated>bazel test //packages/parity:prusaslicer_project_file_parity_failure_test</automated>
    <automated>bazel run //packages/parity:prusaslicer_project_file_parity</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'mutated_expected|3D/3dmodel.model|expected-project-summary.tsv|prusaslicer_project_file_parity_failure_test|slic3rpe:Version3mf' packages/parity/compare_prusaslicer_project_file_test.sh` returns matches.
    - `rg -n 'name = "prusaslicer_project_file_parity_failure_test"|compare_prusaslicer_project_file_test.sh|prusa_project_file_summary' packages/parity/BUILD.bazel` returns matches.
    - `bazel test //packages/parity:prusaslicer_project_file_parity_failure_test` passes while the checked-in `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv` remains unchanged in `git diff -- packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv`.
  </acceptance_criteria>
  <done>The failure guard proves expected artifact divergence is rejected, reports an actionable project-file row label and diff, and mutates only temp files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in TSV -> Rust CLI | Caller-supplied project-file expected-summary text crosses into the typed Rust parser. |
| Rust CLI -> shell comparator | Generated summary lines cross from Rust into Bash diff and diagnostics. |
| Bazel runfiles -> comparator args | Bazel provides explicit file paths; the script must not discover unrelated files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | `compare_prusaslicer_project_file.sh` | mitigate | Diff Rust-generated summary lines for `rust_summary_input` and `expected_artifact`; add `prusaslicer_project_file_parity_failure_test` with a temp mutated expected artifact. |
| T-44-02 | Repudiation | parity success output | mitigate | Print exact status token, accepted source ref, fixture path, expected artifact path, and row count on every success per D-06. |
| T-44-03 | Information Disclosure | Rust summary CLI | mitigate | Read exactly one explicit path; reject network, Git, process, discovery, profile auto-update, upstream import, release, and sync behavior by grep and code review. |
| T-44-04 | Tampering | Bazel command wiring | mitigate | Use explicit `$(location ...)` arguments for the Rust binary, expected project summary, and provenance file; no glob or repo-root search belongs in the comparator. |
</threat_model>

<verification>
Overall Plan 01 verification must pass:

```bash
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_test //packages/slic3r-rust:verify
shfmt -d packages/parity/compare_prusaslicer_project_file.sh packages/parity/compare_prusaslicer_project_file_test.sh
bazel query //packages/parity:prusaslicer_project_file_parity
bazel run //packages/parity:prusaslicer_project_file_parity
bazel test //packages/parity:prusaslicer_project_file_parity_failure_test
git diff --check
```
</verification>

<success_criteria>
PPEV-01 and PPEV-02 are satisfied for the executable command surface: `bazel run //packages/parity:prusaslicer_project_file_parity` exists, passes, prints exact evidence metadata, and the failure test proves mutated expected project-file evidence is rejected.
</success_criteria>

<output>
After completion, create `.planning/phases/44-executable-prusa-project-file-parity/44-01-SUMMARY.md` with `requirements-completed: [PPEV-01, PPEV-02]`.
</output>
