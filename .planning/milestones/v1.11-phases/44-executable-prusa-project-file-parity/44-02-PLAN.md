---
phase: 44-executable-prusa-project-file-parity
plan: "02"
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - packages/parity/status.tsv
  - packages/parity-fixtures/verify_prusa_project_file_fixture.sh
  - packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
  - packages/parity/README.md
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md
  - packages/slic3r-rust/README.md
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/migration-guidance.md
  - docs/port/parity-matrix.md
autonomous: true
requirements: [PPEV-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 44-2026-06-05T23-05-16
generated_at: 2026-06-05T23:19:54Z
must_haves:
  truths:
    - "Maintainer can inspect exactly one fork.prusaslicer.project-file status row with status verified and evidence //packages/parity:prusaslicer_project_file_parity."
    - "The project-file fixture verifier requires the exact status row and the real Phase 44 parity target instead of rejecting them as premature."
    - "Docs name the exact narrow prusaslicer.project-file expected-summary evidence slice and keep full PrusaSlicer runtime, GUI, generated-output, release, network/device, profile auto-update, and sync surfaces deferred."
  artifacts:
    - path: "packages/parity/status.tsv"
      provides: "Single verified project-file status row"
      contains: "fork.prusaslicer.project-file"
    - path: "packages/parity-fixtures/verify_prusa_project_file_fixture.sh"
      provides: "Fail-closed publication verifier for project-file status and target"
    - path: "packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh"
      provides: "Missing/wrong/duplicate/overclaim status failure coverage"
    - path: "docs/port/parity-matrix.md"
      provides: "Human-facing verified narrow project-file evidence interpretation"
  key_links:
    - from: "packages/parity/status.tsv"
      to: "packages/parity/BUILD.bazel"
      via: "evidence label //packages/parity:prusaslicer_project_file_parity"
      pattern: "fork\\.prusaslicer\\.project-file.*prusaslicer_project_file_parity"
    - from: "packages/parity-fixtures/verify_prusa_project_file_fixture.sh"
      to: "packages/parity/status.tsv"
      via: "exact awk status-row validation"
      pattern: "verify_status_published"
    - from: "docs/port/migration-guidance.md"
      to: "packages/parity/status.tsv"
      via: "status interpretation text"
      pattern: "fork\\.prusaslicer\\.project-file"
---

<objective>
Publish the verified project-file parity status and docs after the Plan 01 command exists and passes.

Purpose: PPEV-03 requires maintainers to see the exact verified evidence slice in status/docs while adjacent Prusa runtime, GUI, generated-output, release, network/device, profile auto-update, and sync surfaces remain deferred.
Output: Single status row, fixture verifier publication checks, failure-mode tests, and aligned package/port docs.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-executable-prusa-project-file-parity/44-CONTEXT.md
@.planning/phases/44-executable-prusa-project-file-parity/44-RESEARCH.md
@.planning/phases/43-rust-prusa-project-file-boundary/43-03-SUMMARY.md
@.planning/milestones/v1.10-phases/40-executable-prusa-profile-parity/40-02-SUMMARY.md
@.planning/phases/44-executable-prusa-project-file-parity/44-01-SUMMARY.md
@packages/parity/status.tsv
@packages/parity/BUILD.bazel
@packages/parity/README.md
@packages/parity-fixtures/verify_prusa_project_file_fixture.sh
@packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md
@packages/slic3r-rust/README.md
@docs/port/README.md
@docs/port/package-map.md
@docs/port/migration-guidance.md
@docs/port/parity-matrix.md

<interfaces>
Status TSV contract from `packages/parity/status.tsv`:
```text
surface	status	evidence	notes
```

Exact Phase 44 status row to add:
```text
fork.prusaslicer.project-file	verified	//packages/parity:prusaslicer_project_file_parity	Shared fixture comparison proves the narrow Prusa project-file expected-summary evidence slice backed by the Phase 42 fixture and Phase 43 Rust summary boundary only; full 3MF import/export, PrusaSlicer runtime, GUI, generated-output, STEP, support generation, arc fitting, wall seam, network/device, profile auto-update, fork release, and sync surfaces remain deferred
```

Plan 01 command labels that must already exist:
```text
//packages/parity:prusaslicer_project_file_parity
//packages/parity:prusaslicer_project_file_parity_failure_test
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the exact status row and update fixture publication guards</name>
  <files>packages/parity/status.tsv, packages/parity-fixtures/verify_prusa_project_file_fixture.sh, packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh</files>
  <read_first>
    - packages/parity/status.tsv
    - packages/parity/BUILD.bazel
    - packages/parity-fixtures/verify_prusa_project_file_fixture.sh
    - packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
    - packages/parity-fixtures/verify_prusa_profile_schema_fixture.sh
    - packages/parity-fixtures/verify_prusa_profile_schema_fixture_test.sh
    - .planning/phases/44-executable-prusa-project-file-parity/44-CONTEXT.md
    - .planning/phases/44-executable-prusa-project-file-parity/44-RESEARCH.md
    - .planning/phases/44-executable-prusa-project-file-parity/44-01-SUMMARY.md
  </read_first>
  <action>After verifying `bazel run //packages/parity:prusaslicer_project_file_parity` passes, append exactly one tab-delimited status row to `packages/parity/status.tsv`: `fork.prusaslicer.project-file	verified	//packages/parity:prusaslicer_project_file_parity	Shared fixture comparison proves the narrow Prusa project-file expected-summary evidence slice backed by the Phase 42 fixture and Phase 43 Rust summary boundary only; full 3MF import/export, PrusaSlicer runtime, GUI, generated-output, STEP, support generation, arc fitting, wall seam, network/device, profile auto-update, fork release, and sync surfaces remain deferred`. In `packages/parity-fixtures/verify_prusa_project_file_fixture.sh`, replace `verify_status_row_absent` with `verify_status_published` and replace `verify_parity_target_absent` with `verify_parity_target_published`. `verify_status_published` must fail unless exactly one row has `$1=="fork.prusaslicer.project-file"`, `$2=="verified"`, `$3=="//packages/parity:prusaslicer_project_file_parity"`, and notes containing these exact substrings: `narrow Prusa project-file expected-summary evidence slice`, `Phase 42 fixture`, `Phase 43 Rust summary boundary`, `full 3MF import/export`, `PrusaSlicer runtime`, `GUI`, `generated-output`, `network/device`, `profile auto-update`, `fork release`, and `sync surfaces remain deferred`. It must reject missing, wrong-status, wrong-evidence, and duplicate rows with diagnostics that include `fork.prusaslicer.project-file`. `verify_parity_target_published` must fail unless `packages/parity/BUILD.bazel` contains `name = "prusaslicer_project_file_parity"`, `compare_prusaslicer_project_file.sh`, and `prusa_project_file_summary`. Update `packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh` so `test_complete_fixture_passes` expects the published row and target to pass, and replace the old premature status/target failure tests with `test_missing_project_file_status_row_fails`, `test_wrong_project_file_status_fails`, `test_wrong_project_file_evidence_fails`, `test_duplicate_project_file_status_row_fails`, `test_project_file_status_overclaim_fails`, and `test_missing_project_file_parity_target_fails`. The overclaim test must change the notes to `full PrusaSlicer runtime support verified` and expect the verifier to reject it. Keep all mutation tests on temp copies; do not mutate checked-in fixture or status artifacts per D-08.</action>
  <verify>
    <automated>bazel run //packages/parity:prusaslicer_project_file_parity</automated>
    <automated>bash packages/parity-fixtures/verify_prusa_project_file_fixture.sh</automated>
    <automated>bash packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_project_file_fixture_test</automated>
    <automated>shfmt -d packages/parity-fixtures/verify_prusa_project_file_fixture.sh packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F '\t' '$1=="fork.prusaslicer.project-file" && $2=="verified" && $3=="//packages/parity:prusaslicer_project_file_parity" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `rg -n 'verify_status_published|verify_parity_target_published|narrow Prusa project-file expected-summary evidence slice|Phase 42 fixture|Phase 43 Rust summary boundary' packages/parity-fixtures/verify_prusa_project_file_fixture.sh` returns matches.
    - `rg -n 'test_missing_project_file_status_row_fails|test_wrong_project_file_status_fails|test_wrong_project_file_evidence_fails|test_duplicate_project_file_status_row_fails|test_project_file_status_overclaim_fails|test_missing_project_file_parity_target_fails' packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh` returns matches.
    - `! rg -n 'verify_status_row_absent|verify_parity_target_absent|forbidden premature row fork\\.prusaslicer\\.project-file|forbidden premature target prusaslicer_project_file_parity' packages/parity-fixtures/verify_prusa_project_file_fixture.sh packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh` succeeds.
  </acceptance_criteria>
  <done>The fixture verifier now requires, rather than rejects, the Phase 44 command target and exact status row, with failure-mode tests for missing, wrong, duplicate, and overclaiming publication states.</done>
</task>

<task type="auto">
  <name>Task 2: Align package and port docs with verified narrow project-file evidence</name>
  <files>packages/parity/README.md, packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md, packages/slic3r-rust/README.md, docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <read_first>
    - packages/parity/README.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md
    - packages/slic3r-rust/README.md
    - docs/port/README.md
    - docs/port/package-map.md
    - docs/port/migration-guidance.md
    - docs/port/parity-matrix.md
    - packages/parity/status.tsv
    - .planning/phases/44-executable-prusa-project-file-parity/44-CONTEXT.md
    - .planning/phases/44-executable-prusa-project-file-parity/44-RESEARCH.md
    - .planning/phases/44-executable-prusa-project-file-parity/44-01-SUMMARY.md
  </read_first>
  <action>Update docs per D-09, D-10, D-11, D-12, and D-13 without rewriting unrelated milestone history. In `packages/parity/README.md`, add `bazel run //packages/parity:prusaslicer_project_file_parity` to Current Surface and replace the unavailable `fork.prusaslicer.project-file` paragraph with text saying the row is verified only for the narrow `prusaslicer.project-file` expected-summary evidence slice backed by `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/expected-project-summary.tsv`, the Phase 42 fixture, and the Phase 43 Rust summary boundary. In `packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md`, replace `Executable project-file parity remains unavailable until Phase 44.` with `Executable project-file parity is provided by bazel run //packages/parity:prusaslicer_project_file_parity for the narrow expected-summary evidence slice.` and keep the fixture provenance, update route, and exclusions. In `packages/slic3r-rust/README.md`, add `//packages/slic3r-rust/crates/slic3r_flavors:prusa_project_file_summary` and `bazel run //packages/parity:prusaslicer_project_file_parity` while stating the Rust adapter reads only caller-supplied `expected-project-summary.tsv` and does not inspect Git, network, process, profile auto-update, upstream source import, release, or sync surfaces. In `docs/port/README.md`, change the Phase 44 text under Current Prusa Project-File Rust Boundary State to `Phase 44 provides executable parity and status publication` and name the command and row. In `docs/port/package-map.md`, add Phase 44 ownership text that `packages/parity` owns `bazel run //packages/parity:prusaslicer_project_file_parity` and the `fork.prusaslicer.project-file` status row, while `packages/parity-fixtures` owns the fixture/expected artifact and `slic3r_flavors` owns Rust summary logic. In `docs/port/migration-guidance.md`, replace the unavailable status rule with verified narrow-slice wording tied to the exact command and expected artifact. In `docs/port/parity-matrix.md`, replace the not-verified/future wording with verified narrow-slice wording and preserve the deferral list for full 3MF import/export, PrusaSlicer runtime, GUI project behavior, generated-output parity, STEP import, support generation, arc fitting, wall seam behavior, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, and sync automation.</action>
  <verify>
    <automated>mdformat --check packages/parity/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md packages/slic3r-rust/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md</automated>
    <automated>bazel run //packages/parity:status</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'bazel run //packages/parity:prusaslicer_project_file_parity|fork\\.prusaslicer\\.project-file|expected-project-summary.tsv|Phase 42 fixture|Phase 43 Rust summary boundary' packages/parity/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md packages/slic3r-rust/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md` returns matches.
    - `! rg -n 'Executable project-file parity remains unavailable until Phase 44|Phase 44 still owns executable parity and status publication|future[[:space:]]+`bazel run //packages/parity:prusaslicer_project_file_parity`' packages/parity/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md packages/slic3r-rust/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md` succeeds.
    - `rg -n 'full 3MF import/export|full PrusaSlicer runtime support|GUI project behavior|generated-output parity|STEP import|support generation|arc fitting|wall seam behavior|network/device integration|profile auto-update execution|fork release builds|Bambu Studio|OrcaSlicer|upstream source imports|sync automation' packages/parity/README.md docs/port/README.md docs/port/migration-guidance.md docs/port/parity-matrix.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md` returns matches.
    - `bazel run //packages/parity:status | rg 'fork\\.prusaslicer\\.project-file|//packages/parity:prusaslicer_project_file_parity'` returns the verified row and evidence label.
  </acceptance_criteria>
  <done>Package and port docs publish the verified narrow project-file expected-summary evidence slice and continue to defer adjacent Prusa runtime, GUI, generated-output, release, network/device, profile auto-update, and sync surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parity command -> status row | A passing command becomes a human-readable verified status claim. |
| status TSV -> docs | Checked-in status evidence is interpreted by maintainer-facing docs. |
| temp test fixtures -> verifier | Mutation tests supply altered status/build files to prove fail-closed publication checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05 | Spoofing | `packages/parity/status.tsv` | mitigate | Add exactly one row with status `verified`, evidence `//packages/parity:prusaslicer_project_file_parity`, and narrow evidence notes; enforce exact row with awk in the fixture verifier. |
| T-44-06 | Repudiation | package and port docs | mitigate | Replace Phase 44 unavailable wording with exact command/row text and preserve explicit deferred-scope phrases for all adjacent Prusa surfaces. |
| T-44-07 | Tampering | `verify_prusa_project_file_fixture.sh` | mitigate | Replace absence guards with exact publication checks and add failure tests for missing, wrong-status, wrong-evidence, duplicate, and overclaiming rows. |
| T-44-08 | Tampering | verifier mutation tests | mitigate | Keep all negative cases in temp copies and assert checked-in fixture/status artifacts are not modified. |
</threat_model>

<verification>
Overall Plan 02 verification must pass after Plan 01:

```bash
bazel run //packages/parity:prusaslicer_project_file_parity
bazel test //packages/parity:prusaslicer_project_file_parity_failure_test
bazel run //packages/parity-fixtures:verify_prusa_project_file_fixture
bazel test //packages/parity-fixtures:verify_prusa_project_file_fixture_test
bazel run //packages/parity:status | rg 'fork\.prusaslicer\.project-file|//packages/parity:prusaslicer_project_file_parity'
awk -F '\t' '$1=="fork.prusaslicer.project-file" && $2=="verified" && $3=="//packages/parity:prusaslicer_project_file_parity" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv
shfmt -d packages/parity-fixtures/verify_prusa_project_file_fixture.sh packages/parity-fixtures/verify_prusa_project_file_fixture_test.sh
mdformat --check packages/parity/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.project-file/README.md packages/slic3r-rust/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md
git diff --check
```
</verification>

<success_criteria>
PPEV-03 is satisfied: status and docs publish exactly one verified `fork.prusaslicer.project-file` row tied to the passing command and describe only the narrow expected-summary evidence slice while all adjacent Prusa runtime and broader fork surfaces remain deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/44-executable-prusa-project-file-parity/44-02-SUMMARY.md` with `requirements-completed: [PPEV-03]`.
</output>
