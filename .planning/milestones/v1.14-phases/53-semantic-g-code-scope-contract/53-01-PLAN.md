---
phase: 53-semantic-g-code-scope-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/prusa-gcode-output-scope/gcode-output-scope.md
  - packages/prusa-gcode-output-scope/README.md
autonomous: true
requirements:
  - GSSCOPE-01
  - GSSCOPE-03
must_haves:
  truths:
    - "Maintainer can inspect a v1.14 semantic Prusa G-code contract with exactly nine allowed TSV-friendly fields."
    - "Maintainer can trace the semantic plan to the accepted source identity, inventory row, fixture namespace, current artifacts, planned semantic artifact, planned Rust boundary, planned public command, status boundary, security note, deferred scope, and reviewer signoff."
    - "README exposes the semantic scope contract and verification command without public status, parity, fixture, Rust parser, or port-doc publication changes."
  artifacts:
    - path: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      provides: "Human-readable v1.14 semantic field contract and traceability surface"
      contains: "## v1.14 Semantic Evidence Scope"
    - path: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      provides: "Semantic traceability to planned artifact and downstream boundaries"
      contains: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv"
    - path: "packages/prusa-gcode-output-scope/README.md"
      provides: "Package-local semantic scope and verification command discoverability"
      contains: "Phase 53 semantic verification"
  key_links:
    - from: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      to: "packages/fork-inventories/prusaslicer.tsv"
      via: "Inventory row traceability"
      pattern: "prusaslicer\\.gcode-output"
    - from: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv"
      via: "Planned semantic summary traceability row"
      pattern: "expected-gcode-semantic-summary\\.tsv"
    - from: "packages/prusa-gcode-output-scope/README.md"
      to: "//packages/prusa-gcode-output-scope:verify"
      via: "Bazel verification command"
      pattern: "bazel run //packages/prusa-gcode-output-scope:verify"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 53-2026-06-21T00-15-35
generated_at: 2026-06-21T00:36:21Z
---

<objective>
Add the reviewed v1.14 semantic Prusa G-code field contract and traceability surface inside the existing scope package.

Purpose: Give maintainers an inspectable, closed semantic contract before Phase 54 fixture artifacts, Phase 55 Rust parsing, or Phase 56 public semantic evidence exists.
Output: Updated `packages/prusa-gcode-output-scope/gcode-output-scope.md` and package README text.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-semantic-g-code-scope-contract/53-CONTEXT.md
@.planning/phases/53-semantic-g-code-scope-contract/53-RESEARCH.md
@.planning/milestones/v1.13-phases/49-structural-g-code-scope-contract/49-01-SUMMARY.md
@.planning/milestones/v1.13-phases/49-structural-g-code-scope-contract/49-02-SUMMARY.md
@.planning/milestones/v1.13-phases/52-executable-structural-g-code-evidence/52-04-SUMMARY.md
@packages/prusa-gcode-output-scope/gcode-output-scope.md
@packages/prusa-gcode-output-scope/README.md
@packages/fork-inventories/prusaslicer.tsv
@packages/fork-inventories/category-map.tsv
@packages/parity/status.tsv

<interfaces>
Existing public package command:
```text
bazel run //packages/prusa-gcode-output-scope:verify
```

Existing accepted source identity:
```text
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
```

Existing public evidence command to preserve as planned text only in Phase 53:
```text
bazel run //packages/parity:prusaslicer_gcode_output_parity
```
</interfaces>
</context>

<decision_coverage>
D-01 full: extend `packages/prusa-gcode-output-scope`, not a new package.
D-02 full: keep `gcode-output-scope.md` as source of truth with additive v1.14 sections.
D-03 full: update README only enough for semantic contract and verifier command.
D-04 full: contract names exactly the nine allowed semantic fields.
D-05 full: each semantic field states its evidence boundary.
D-06 full: field names remain TSV-friendly and closed for later verifier enforcement.
D-07 full: forbidden overclaiming boundaries stay explicit.
D-09 full: `generated-outputs` remains exactly `in progress`; no status wording change.
D-10 full: traceability names source, inventory, category, fixture, current artifacts, planned semantic artifact, Rust boundary, public command, and status boundary.
D-11 full: planned semantic summary path is named exactly.
D-12 full: planned Rust boundary is `slic3r_flavors::prusa_gcode_output`.
D-13 full: planned public command remains `bazel run //packages/parity:prusaslicer_gcode_output_parity`.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Add the reviewed semantic field contract and traceability record</name>
  <files>packages/prusa-gcode-output-scope/gcode-output-scope.md</files>
  <read_first>
    - packages/prusa-gcode-output-scope/gcode-output-scope.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-CONTEXT.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - packages/fork-inventories/prusaslicer.tsv
    - packages/fork-inventories/category-map.tsv
    - packages/parity/status.tsv
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv
  </read_first>
  <action>Add two additive sections after `## v1.13 Structural Traceability` and before `## Boundary`, per D-01 and D-02. Do not edit `packages/parity/status.tsv`, `packages/parity/README.md`, `docs/port/*`, fixture artifacts, or Rust files.

Create `## v1.14 Semantic Evidence Scope` with this exact table shape:

```markdown
| Semantic Field | Category | Evidence Boundary |
```

The body must contain exactly these nine rows, in this order, per D-04, D-05, and D-06:

```markdown
| source_ref | source identity | Accepted PrusaSlicer source identity only: `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`. |
| fixture_id | fixture identity | Fixture identity only: `gcodewriter-set-speed.gcode`. |
| fixture_path | fixture identity | Checked-in fixture path only: `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode`. |
| command_class_counts | command classes | Counts of approved command classes in the selected fixture summary only; no byte-for-byte G-code parity or generator parity. |
| movement_class_counts | movement classes | Counts of approved movement classes in the selected fixture summary only; no toolpath geometry, travel, or printability claim. |
| coordinate_bounds | coordinate bounds | Bounded coordinate observations only; no toolpath geometry or printability claim. |
| extrusion_total | extrusion total | Summary totals only; no material-use, runtime, or printability claim. |
| feedrate_observations | feedrate observations | Feedrate metadata only; no timing, firmware, or printer-runtime behavior claim. |
| layer_marker_relationships | layer or marker relationships | Fixture-summary relationships only; no GUI, viewer, runtime, support, seam, or arc behavior claim. |
```

Create `## v1.14 Semantic Traceability` with a two-column table `| Required Link | Exact Target |` and rows for these exact link names: `Inventory row`, `Category-map row`, `Accepted source identity`, `Fixture namespace`, `Current expected summary`, `Current structural summary`, `Fixture provenance`, `Planned semantic summary`, `Planned Rust semantic boundary`, `Planned public evidence command`, `Deferred status boundary`, `Docs touched`, `Security note`, `Deferred semantic scope`, and `Semantic reviewer signoff`.

The traceability values must include these exact strings per D-10 through D-13: `prusaslicer.gcode-output`, `gcode.shared`, `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv`, `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv`, `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv`, `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv`, `slic3r_flavors::prusa_gcode_output`, `bazel run //packages/parity:prusaslicer_gcode_output_parity`, and ``generated-outputs` stays `in progress``. Use `Semantic reviewer signoff | Peter Ryszkiewicz, 2026-06-21 UTC`.</action>
  <acceptance_criteria>
    - `rg -n "^## v1.14 Semantic Evidence Scope$" packages/prusa-gcode-output-scope/gcode-output-scope.md` finds exactly one section.
    - `rg -n "^## v1.14 Semantic Traceability$" packages/prusa-gcode-output-scope/gcode-output-scope.md` finds exactly one section.
    - `rg -n "source_ref|fixture_id|fixture_path|command_class_counts|movement_class_counts|coordinate_bounds|extrusion_total|feedrate_observations|layer_marker_relationships" packages/prusa-gcode-output-scope/gcode-output-scope.md` finds all nine semantic fields.
    - `rg -n "expected-gcode-semantic-summary.tsv|slic3r_flavors::prusa_gcode_output|bazel run //packages/parity:prusaslicer_gcode_output_parity|Semantic reviewer signoff" packages/prusa-gcode-output-scope/gcode-output-scope.md` finds all planned downstream boundaries.
    - `git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/slic3r-rust packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv` returns no output.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^## v1.14 Semantic Evidence Scope$|^## v1.14 Semantic Traceability$|command_class_counts|movement_class_counts|coordinate_bounds|extrusion_total|feedrate_observations|layer_marker_relationships|expected-gcode-semantic-summary.tsv|Semantic reviewer signoff" packages/prusa-gcode-output-scope/gcode-output-scope.md</automated>
    <automated>awk '/^## v1.14 Semantic Evidence Scope$/ { in_section = 1; next } in_section && /^## / { exit } in_section && /^\\|/ && $0 !~ /Semantic Field/ && $0 !~ /---/ { count++ } END { exit count == 9 ? 0 : 1 }' packages/prusa-gcode-output-scope/gcode-output-scope.md</automated>
    <automated>git diff --check -- packages/prusa-gcode-output-scope/gcode-output-scope.md</automated>
  </verify>
  <done>`gcode-output-scope.md` contains the closed nine-field semantic scope and full traceability record without creating Phase 54, Phase 55, or Phase 56 artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Add narrow README discoverability for the semantic scope contract</name>
  <files>packages/prusa-gcode-output-scope/README.md</files>
  <read_first>
    - packages/prusa-gcode-output-scope/README.md
    - packages/prusa-gcode-output-scope/gcode-output-scope.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-CONTEXT.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-RESEARCH.md
    - .planning/milestones/v1.13-phases/49-structural-g-code-scope-contract/49-01-SUMMARY.md
    - .planning/milestones/v1.13-phases/52-executable-structural-g-code-evidence/52-04-SUMMARY.md
  </read_first>
  <action>Update only `packages/prusa-gcode-output-scope/README.md`, per D-03. Keep the existing verification command `bazel run //packages/prusa-gcode-output-scope:verify`. Add or adjust minimal package-local wording so the README says the package owns the Phase 45 reviewed scope gate, Phase 49 structural evidence scope contract, and Phase 53 semantic evidence scope contract for `prusaslicer.gcode-output`.

Add these concrete README facts without broad rewrites:

- `Phase 53 semantic verification allows only source identity, fixture identity, command class counts, movement class counts, coordinate bounds, extrusion totals, feedrate observations, and layer/marker relationships for the planned v1.14 semantic Prusa G-code evidence chain.`
- `Phase 53 semantic verification keeps generated-outputs exactly in progress and does not prove byte-for-byte G-code parity, broad generated-output verification, toolpath geometry parity, printability, printer-runtime behavior, support generation, wall seam behavior, arc fitting, GUI export/viewer behavior, release behavior, network/device behavior, Bambu Studio support, OrcaSlicer support, upstream source imports, or sync automation.`
- `Phase 53 only records the planned semantic summary artifact `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv`, the planned Phase 55 `slic3r_flavors::prusa_gcode_output` semantic boundary, and the planned Phase 56 `bazel run //packages/parity:prusaslicer_gcode_output_parity` evidence command; it does not create semantic fixture artifacts, Rust semantic parsing, public semantic parity evidence, or status publication.`

Update the Records and Boundary paragraphs only as needed to mention `## v1.14 Semantic Evidence Scope` and `## v1.14 Semantic Traceability`. Preserve all existing Phase 45, Phase 49, and Phase 52 no-overclaiming language per D-07 and D-09.</action>
  <acceptance_criteria>
    - `rg -n "Phase 53 semantic verification allows only source identity, fixture identity, command class counts, movement class counts, coordinate bounds, extrusion totals, feedrate observations, and layer/marker relationships" packages/prusa-gcode-output-scope/README.md` finds the new allowed-field wording.
    - `rg -n "expected-gcode-semantic-summary.tsv|slic3r_flavors::prusa_gcode_output|bazel run //packages/parity:prusaslicer_gcode_output_parity" packages/prusa-gcode-output-scope/README.md` finds the planned downstream boundaries.
    - `rg -n "does not create semantic fixture artifacts, Rust semantic parsing, public semantic parity evidence, or status publication" packages/prusa-gcode-output-scope/README.md` finds the Phase 53 boundary.
    - `rg -n "Run `bazel run //packages/prusa-gcode-output-scope:verify`" packages/prusa-gcode-output-scope/README.md` still finds the verification command.
    - `git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/slic3r-rust` returns no output.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 53 semantic verification|expected-gcode-semantic-summary.tsv|slic3r_flavors::prusa_gcode_output|public semantic parity evidence|bazel run //packages/prusa-gcode-output-scope:verify" packages/prusa-gcode-output-scope/README.md</automated>
    <automated>git diff --check -- packages/prusa-gcode-output-scope/README.md</automated>
    <automated>mdformat --check packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md</automated>
  </verify>
  <done>`README.md` names the semantic contract and verifier command while preserving the package-local, non-publication boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer documentation -> downstream fixture/parser phases | Human-readable contract text becomes the source of field names consumed by Phase 54 and Phase 55. |
| Scope package -> public status/docs | Phase 53 text must not be mistaken for public semantic evidence or broad generated-output publication. |
| Inventory/status TSVs -> scope traceability | Existing source and status rows are referenced as evidence inputs and must not be spoofed by prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Spoofing | `gcode-output-scope.md` traceability rows | mitigate | Require the exact accepted source identity `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, exact inventory row ID `prusaslicer.gcode-output`, and exact `gcode.shared` category-map reference. |
| T-53-02 | Tampering | Semantic field contract | mitigate | Use a closed nine-row `## v1.14 Semantic Evidence Scope` table with TSV-friendly field names and no catch-all row. Plan 53-02 enforces exact rows and row count. |
| T-53-03 | Repudiation | Reviewer and downstream boundary traceability | mitigate | Add exact `Semantic reviewer signoff`, `Security note`, `Deferred semantic scope`, and planned artifact/Rust/command rows so reviewers can audit what was approved. |
| T-53-04 | Information Disclosure | Scope package docs | accept | No secrets, credentials, private data, or runtime file reads are introduced; the package references only checked-in public repo paths and source IDs. |
| T-53-05 | Elevation of Privilege | Phase 53 scope package | accept | No executable runtime, auth, network, device, host upload, release, or sync surface is added in this plan. |
</threat_model>

<verification>
Run after both tasks:

<automated>mdformat --check packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md</automated>
<automated>git diff --check -- packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md</automated>
<automated>rg -n "## v1.14 Semantic Evidence Scope|## v1.14 Semantic Traceability|Phase 53 semantic verification|generated-outputs.*in progress" packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md</automated>
</verification>

<success_criteria>
- `gcode-output-scope.md` has exactly one `## v1.14 Semantic Evidence Scope` section with exactly nine allowed semantic field rows.
- `gcode-output-scope.md` has `## v1.14 Semantic Traceability` rows for source identity, inventory, category map, fixture namespace, current summaries, planned semantic summary, planned Rust boundary, planned public command, status boundary, security note, deferred scope, and reviewer signoff.
- `README.md` names Phase 53 semantic verification and the package-local verifier command without claiming semantic executable evidence or editing public status/docs.
- No Phase 54 fixture artifact, Phase 55 Rust parser, Phase 56 public parity evidence, or public status/docs publication is created.
</success_criteria>

<output>
After completion, create `.planning/phases/53-semantic-g-code-scope-contract/53-01-SUMMARY.md`
</output>
