---
phase: 53-semantic-g-code-scope-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - 53-01-PLAN.md
files_modified:
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
autonomous: true
requirements:
  - GSSCOPE-02
  - GSSCOPE-03
must_haves:
  truths:
    - "Maintainer can run the scope verifier and see the Phase 53 semantic contract pass only when all required rows, traceability, reviewer signoff, and status boundaries are present."
    - "Verifier fails closed for missing semantic field rows, unsupported semantic fields, duplicate semantic rows, missing semantic traceability, missing semantic reviewer signoff, semantic overclaim text, and generated-output status promotion."
    - "The broad `generated-outputs` row remains exactly one `in progress` row and the narrow `fork.prusaslicer.gcode-output` row remains limited to the current structural evidence slice."
  artifacts:
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      provides: "Fail-closed semantic scope verifier"
      exports: ["verify_semantic_scope_contract", "verify_semantic_traceability"]
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh"
      provides: "Semantic scope mutation coverage"
      contains: "test_missing_allowed_semantic_field_fails"
  key_links:
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      to: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      via: "exact section row and row-count checks"
      pattern: "SEMANTIC_SCOPE_SECTION"
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      to: "packages/parity/status.tsv"
      via: "generated-outputs exact in-progress guard"
      pattern: "verify_generated_outputs_in_progress"
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh"
      to: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      via: "isolated temp checkout mutation fixtures"
      pattern: "run_verifier"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 53-2026-06-21T00-15-35
generated_at: 2026-06-21T00:36:21Z
---

<objective>
Enforce the Phase 53 semantic scope contract through the existing fail-closed Bash verifier and focused mutation coverage.

Purpose: Make the reviewed semantic contract executable so unsupported fields, traceability drift, overclaiming, and premature generated-output publication cannot silently pass.
Output: Updated verifier script and mutation test script under `packages/prusa-gcode-output-scope`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-semantic-g-code-scope-contract/53-CONTEXT.md
@.planning/phases/53-semantic-g-code-scope-contract/53-RESEARCH.md
@.planning/phases/53-semantic-g-code-scope-contract/53-01-SUMMARY.md
@.planning/milestones/v1.13-phases/49-structural-g-code-scope-contract/49-02-SUMMARY.md
@.planning/milestones/v1.13-phases/52-executable-structural-g-code-evidence/52-04-SUMMARY.md
@packages/prusa-gcode-output-scope/gcode-output-scope.md
@packages/prusa-gcode-output-scope/README.md
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
@packages/prusa-gcode-output-scope/BUILD.bazel
@packages/parity/status.tsv

<interfaces>
Existing verifier helpers to extend:
```bash
require_section_table_exact_row "${scope_file}" "gcode-output-scope.md" "${SECTION}" "${ROW}"
require_section_table_row "${scope_file}" "gcode-output-scope.md" "${SECTION}" "${FIELD}" "${VALUE}"
require_section_table_body_row_count "${scope_file}" "gcode-output-scope.md" "${SECTION}" "${EXPECTED_COUNT}"
verify_generated_outputs_in_progress
reject_overclaiming_text
```

Existing mutation harness helpers to reuse:
```bash
write_valid_fixture "${dir}"
run_verifier "${dir}" "${stdout_file}" "${stderr_file}"
remove_line_containing "${file}" "${pattern}"
replace_first_line_containing "${file}" "${pattern}" "${replacement}"
insert_line_before "${file}" "${pattern}" "${inserted_line}"
assert_contains "${file}" "${pattern}"
```
</interfaces>
</context>

<decision_coverage>
D-04 full: verifier requires exactly the nine approved semantic fields.
D-05 full: verifier requires exact boundary text for coordinate bounds, extrusion totals, feedrate observations, and layer/marker relationships.
D-06 full: unsupported, duplicate, or compact unsupported semantic fields fail closed by exact rows plus exact row count.
D-07 full: overclaim scans include Phase 53 semantic claims for byte parity, broad generated-output verification, toolpath geometry, printability, runtime, support, seam, arc, GUI, release, network/device, non-Prusa forks, upstream imports, and sync.
D-08 full: mutation tests cover missing required semantic field, unsupported semantic field, duplicate semantic rows, missing semantic traceability, missing semantic reviewer signoff, and semantic overclaim text.
D-09 full: verifier keeps `generated-outputs` exactly one `in progress` row and does not require semantic status publication.
D-10 full: verifier requires semantic traceability rows for accepted inventory/category/source/fixture/current artifacts/planned artifact/Rust boundary/public command/status boundary.
D-11 full: verifier requires `expected-gcode-semantic-summary.tsv` as planned text.
D-12 full: verifier requires `slic3r_flavors::prusa_gcode_output` as the planned Rust boundary.
D-13 full: verifier requires `bazel run //packages/parity:prusaslicer_gcode_output_parity` as the planned public evidence command.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED semantic mutation coverage</name>
  <files>packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</files>
  <read_first>
    - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
    - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
    - packages/prusa-gcode-output-scope/gcode-output-scope.md
    - packages/prusa-gcode-output-scope/README.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-CONTEXT.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-RESEARCH.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: removing the semantic `command_class_counts` row fails and the diagnostic mentions `command_class_counts`.
    - Test 2: inserting unsupported semantic row `toolpath_geometry` before `## v1.14 Semantic Traceability` fails with `expected exactly 9 semantic field rows`.
    - Test 3: duplicating the semantic `movement_class_counts` row fails with `expected exactly 9 semantic field rows`.
    - Test 4: removing semantic traceability row `Planned semantic summary` fails and the diagnostic mentions `Planned semantic summary`.
    - Test 5: removing semantic traceability row `Semantic reviewer signoff` fails and the diagnostic mentions `Semantic reviewer signoff`.
    - Test 6: appending `Phase 53 semantic evidence proves printability.` to README or scope text fails with `forbidden Prusa G-code scope claim` or `forbidden Prusa G-code scope overclaim`.
    - Test 7: adding a second `generated-outputs` status row with `verified` still fails with `generated-outputs must be exactly one in progress row`.
  </behavior>
  <action>Update only `verify_prusa_gcode_output_scope_test.sh`. Extend `write_valid_fixture` to include the exact Phase 53 README text and the exact `## v1.14 Semantic Evidence Scope` and `## v1.14 Semantic Traceability` sections produced by Plan 53-01. Keep all existing Phase 45, Phase 49, and Phase 52 valid fixture content.

Add focused mutation tests using existing helper style and Arrange, Act, Assert comments:

- `test_missing_allowed_semantic_field_fails`
- `test_unsupported_semantic_field_fails`
- `test_duplicate_semantic_field_fails`
- `test_missing_semantic_traceability_fails`
- `test_missing_semantic_reviewer_signoff_fails`
- `test_scope_semantic_overclaim_fails`
- `test_readme_semantic_overclaim_fails`

Each test must mutate one behavior only, per D-08 and Bright Builds testing guidance. Add each test to the explicit test invocation list at the bottom. Before Task 2, run the mutation suite and capture RED evidence that at least one new semantic mutation passes incorrectly under the current verifier, for example `FAIL: missing allowed semantic field fixture passed` or `FAIL: unsupported semantic field fixture passed`.</action>
  <acceptance_criteria>
    - `rg -n "test_missing_allowed_semantic_field_fails|test_unsupported_semantic_field_fails|test_duplicate_semantic_field_fails|test_missing_semantic_traceability_fails|test_missing_semantic_reviewer_signoff_fails|test_scope_semantic_overclaim_fails|test_readme_semantic_overclaim_fails" packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` finds all seven test functions.
    - `rg -n "command_class_counts|toolpath_geometry|movement_class_counts|Planned semantic summary|Semantic reviewer signoff|Phase 53 semantic evidence proves printability" packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` finds all required mutation values.
    - The RED command exits nonzero before verifier implementation and stderr contains one of the new semantic failure markers.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "test_missing_allowed_semantic_field_fails|test_unsupported_semantic_field_fails|test_duplicate_semantic_field_fails|test_missing_semantic_traceability_fails|test_missing_semantic_reviewer_signoff_fails|test_scope_semantic_overclaim_fails|test_readme_semantic_overclaim_fails" packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bash -n packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bash -c 'set +e; bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh >/tmp/phase53-semantic-red.out 2>/tmp/phase53-semantic-red.err; status="$?"; test "${status}" -ne 0; rg -n "missing allowed semantic field fixture passed|unsupported semantic field fixture passed|duplicate semantic field fixture passed|semantic overclaim fixture passed" /tmp/phase53-semantic-red.err'</automated>
  </verify>
  <done>New semantic mutation tests exist, start from a valid Phase 53 fixture, and demonstrate RED failure before verifier implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GREEN semantic verifier enforcement</name>
  <files>packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh, packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</files>
  <read_first>
    - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
    - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
    - packages/prusa-gcode-output-scope/gcode-output-scope.md
    - packages/prusa-gcode-output-scope/README.md
    - packages/prusa-gcode-output-scope/BUILD.bazel
    - packages/parity/status.tsv
    - .planning/phases/53-semantic-g-code-scope-contract/53-CONTEXT.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-RESEARCH.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-01-SUMMARY.md
  </read_first>
  <behavior>
    - Valid Phase 53 scope package passes `bazel run //packages/prusa-gcode-output-scope:verify`.
    - All semantic mutation tests from Task 1 fail closed with specific diagnostics.
    - The existing structural mutation tests continue to pass.
    - `generated-outputs` remains exactly one `in progress` row; semantic enforcement does not require a Phase 56 status wording change.
  </behavior>
  <action>Update `verify_prusa_gcode_output_scope.sh` to enforce the semantic contract from Plan 53-01. Add constants:

```bash
readonly SEMANTIC_SCOPE_SECTION="## v1.14 Semantic Evidence Scope"
readonly SEMANTIC_TRACEABILITY_SECTION="## v1.14 Semantic Traceability"
readonly SEMANTIC_FIELD_ROW_COUNT="9"
```

Generalize `require_section_table_body_row_count` so it can skip either a `Structural Field` or `Semantic Field` header, or add a semantic-specific row-count helper. The semantic count diagnostic must include `expected exactly 9 semantic field rows` and `required fields include command_class_counts` so missing-field failures are specific.

Add `verify_semantic_scope_contract` that requires exact rows for all nine semantic fields from Plan 53-01, including the exact boundary strings for `coordinate_bounds`, `extrusion_total`, `feedrate_observations`, and `layer_marker_relationships`, per D-04 through D-06. Add `verify_semantic_traceability` that requires all traceability rows named in Plan 53-01, including `Planned semantic summary`, `Planned Rust semantic boundary`, `Planned public evidence command`, `Deferred status boundary`, `Security note`, `Deferred semantic scope`, and `Semantic reviewer signoff`, per D-10 through D-13.

Extend `verify_readme` to require the Phase 53 README sentences from Plan 53-01. Extend `reject_overclaiming_text` so affirmative Phase 53 semantic wording such as `Phase 53 semantic evidence proves printability`, `Phase 53 semantic verification proves byte-for-byte G-code parity`, `Phase 53 semantic evidence verifies printer-runtime behavior`, `Phase 53 semantic evidence proves support generation`, `Phase 53 semantic evidence proves wall seam behavior`, `Phase 53 semantic evidence proves arc fitting`, `Phase 53 semantic evidence proves GUI export/viewer behavior`, `Phase 53 semantic evidence proves release behavior`, `Phase 53 semantic evidence proves network/device behavior`, `Phase 53 semantic evidence proves non-Prusa fork behavior`, `Phase 53 semantic evidence proves Bambu Studio support`, `Phase 53 semantic evidence proves OrcaSlicer support`, `Phase 53 semantic evidence proves upstream source imports`, and `Phase 53 semantic evidence proves sync automation` fails closed, per D-07 and D-08.

Call `verify_semantic_scope_contract` and `verify_semantic_traceability` in the main verifier flow after structural checks and before status/deferred/inventory checks. Preserve existing `verify_generated_outputs_in_progress`, `verify_status_published`, and `GCODE_OUTPUT_STATUS_ROW` structural wording exactly; do not require semantic status publication in Phase 53, per D-09.</action>
  <acceptance_criteria>
    - `rg -n "SEMANTIC_SCOPE_SECTION|SEMANTIC_TRACEABILITY_SECTION|SEMANTIC_FIELD_ROW_COUNT=\"9\"|verify_semantic_scope_contract|verify_semantic_traceability" packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` finds the new constants and functions.
    - `rg -n "expected exactly 9 semantic field rows|required fields include command_class_counts" packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` finds the required diagnostic text.
    - `rg -n "Phase 53 semantic evidence proves printability|Phase 53 semantic verification proves byte-for-byte G-code parity|Phase 53 semantic evidence verifies printer-runtime behavior" packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` finds semantic overclaim guards or tests.
    - `bazel run //packages/prusa-gcode-output-scope:verify` prints `ok: Prusa G-code output scope verification passed`.
    - `bazel test --cache_test_results=no //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test` passes.
    - `git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/slic3r-rust` returns no output.
  </acceptance_criteria>
  <verify>
    <automated>bash -n packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
    <automated>bazel test --cache_test_results=no //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
    <automated>shfmt -l -d packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>shellcheck packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>git diff --check -- packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
  </verify>
  <done>Semantic verifier enforcement passes valid scope files and every required semantic mutation fails closed while current structural status wording remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown scope file -> Bash verifier | Untrusted or drifted contract text is parsed by local shell checks. |
| Status TSV -> scope verifier | Existing status rows constrain whether broad generated outputs can be promoted. |
| Mutation fixtures -> verifier behavior | Tests simulate malicious or accidental drift without mutating repo source artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-06 | Tampering | Semantic field table validation | mitigate | Exact row checks plus exact `SEMANTIC_FIELD_ROW_COUNT="9"` reject missing, duplicate, compact unsupported, or unsupported semantic rows. |
| T-53-07 | Repudiation | Semantic traceability and signoff | mitigate | `verify_semantic_traceability` requires planned artifact, Rust boundary, public command, deferred status boundary, security note, deferred scope, and `Semantic reviewer signoff`. |
| T-53-08 | Tampering | `packages/parity/status.tsv` broad generated-output row | mitigate | Preserve and run `verify_generated_outputs_in_progress` so `generated-outputs` must be exactly one `in progress` row. |
| T-53-09 | Spoofing | Accepted source and fixture identity | mitigate | Semantic exact rows require the accepted source identity, `gcodewriter-set-speed.gcode`, and checked-in fixture path instead of freeform prose. |
| T-53-10 | Denial of Service | Local Bash verifier runtime | accept | Verifier remains deterministic over small checked-in Markdown/TSV files; no network, Git, filesystem discovery, generator, or external service loop is introduced. |
| T-53-11 | Elevation of Privilege | Scope verifier execution | accept | The verifier reads explicit local files and emits diagnostics only; no credential, host upload, network/device, release, sync, or runtime execution path is added. |
</threat_model>

<verification>
Run after both tasks:

<automated>bash -n packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
<automated>bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
<automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
<automated>bazel test --cache_test_results=no //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
<automated>shfmt -l -d packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
<automated>shellcheck packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
<automated>git diff --check -- packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
</verification>

<success_criteria>
- The verifier requires the Phase 53 semantic scope and traceability sections using exact rows and exact semantic row count.
- Mutation tests prove missing required semantic fields, unsupported semantic fields, duplicate semantic rows, missing semantic traceability, missing semantic reviewer signoff, semantic overclaim text, and generated-output promotion fail closed.
- `generated-outputs` remains exactly one `in progress` row and `fork.prusaslicer.gcode-output` remains limited to current structural evidence wording in Phase 53.
- No Phase 54 semantic fixture artifact, Phase 55 Rust semantic parser, Phase 56 public semantic parity evidence, or public docs/status publication is created.
</success_criteria>

<output>
After completion, create `.planning/phases/53-semantic-g-code-scope-contract/53-02-SUMMARY.md`
</output>
