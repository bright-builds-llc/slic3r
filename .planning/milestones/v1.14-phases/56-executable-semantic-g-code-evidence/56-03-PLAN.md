---
phase: 56-executable-semantic-g-code-evidence
plan: "03"
type: execute
wave: 3
depends_on:
  - "56-01"
  - "56-02"
files_modified:
  - packages/parity/status.tsv
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
autonomous: true
requirements:
  - GSEV-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 56-2026-06-21T16-41-17
generated_at: 2026-06-21T17:08:02.864Z
must_haves:
  truths:
    - Maintainer can inspect `packages/parity/status.tsv` and see `fork.prusaslicer.gcode-output` published as a narrow semantic Prusa G-code evidence slice.
    - The broad `generated-outputs` row remains exactly one `in progress` row.
    - Fixture and scope verifiers fail closed when the status row drifts back to structural-only wording or a wrong evidence target.
  artifacts:
    - path: packages/parity/status.tsv
      provides: exact semantic status row
    - path: packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
      provides: fixture-level exact status enforcement
    - path: packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
      provides: scope-level exact status enforcement
  key_links:
    - from: packages/parity/status.tsv
      to: //packages/parity:prusaslicer_gcode_output_parity
      via: evidence column
      pattern: fork.prusaslicer.gcode-output\tverified\t//packages/parity:prusaslicer_gcode_output_parity
    - from: verifier scripts
      to: packages/parity/status.tsv
      via: exact `GCODE_OUTPUT_STATUS_ROW`
      pattern: narrow semantic Prusa G-code evidence slice
---

<objective>
Publish exact semantic status wording and update fixture/scope verifier enforcement to make that wording fail closed.

Purpose: Complete the status portion of GSEV-03 only after public semantic evidence and mutation guards exist.
Output: One exact semantic `fork.prusaslicer.gcode-output` row, unchanged broad `generated-outputs` status, and reconciled verifier constants/tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-01-SUMMARY.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-02-SUMMARY.md
@packages/parity/status.tsv
@packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish exact semantic status row and verifier constants</name>
  <files>packages/parity/status.tsv, packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh, packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh</files>
  <read_first>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-01-SUMMARY.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-02-SUMMARY.md
    packages/parity/status.tsv
    packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
    packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
  </read_first>
  <action>
    Replace only the `fork.prusaslicer.gcode-output` notes column in `packages/parity/status.tsv`; keep status `verified` and evidence `//packages/parity:prusaslicer_gcode_output_parity`. The new notes text must be exactly:

    `Shared fixture comparison proves the narrow semantic Prusa G-code evidence slice backed by the Phase 53 closed semantic scope contract, Phase 54 semantic fixture summary, Phase 55 Rust semantic parser/readiness boundary, and Phase 56 public parity command only; byte-for-byte G-code parity, full generated-output parity, toolpath geometry, extrusion behavior, timing, support generation, wall seam behavior, arc fitting, STEP import, full 3MF import/export, printer-runtime behavior, firmware or printability, GUI export or viewer behavior, binary G-code, thumbnails, post-processing, host upload, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, and sync automation remain deferred`

    Keep the `generated-outputs` row exactly `in progress`; do not edit its status token. Update `GCODE_OUTPUT_STATUS_ROW` in `packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh` and `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` to the exact full tab-delimited status row. Preserve existing forbidden-claim split literal patterns where they prevent self-scan false positives.
  </action>
  <acceptance_criteria>
    - `awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `awk -F '\t' '$1=="fork.prusaslicer.gcode-output" && $2=="verified" && $3=="//packages/parity:prusaslicer_gcode_output_parity" && index($4, "narrow semantic Prusa G-code evidence slice") && index($4, "Phase 53 closed semantic scope contract") && index($4, "Phase 54 semantic fixture summary") && index($4, "Phase 55 Rust semantic parser/readiness boundary") && index($4, "Phase 56 public parity command") { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `rg -n "narrow semantic Prusa G-code evidence slice|Phase 53 closed semantic scope contract|Phase 54 semantic fixture summary|Phase 55 Rust semantic parser/readiness boundary|Phase 56 public parity command" packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` finds the exact semantic row language in both verifiers.
    - `! rg -n "narrow structural Prusa G-code evidence slice" packages/parity/status.tsv packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh && shfmt -l -d packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh && bazel run //packages/parity:status</automated>
  </verify>
  <done>Status publication names the narrow semantic evidence chain, preserves the public target, and keeps broad `generated-outputs` exactly `in progress`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update verifier mutation tests for semantic status enforcement</name>
  <files>packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh, packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</files>
  <read_first>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-01-SUMMARY.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-02-SUMMARY.md
    packages/parity/status.tsv
    packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
    packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
    packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
    packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
  </read_first>
  <behavior>
    - Wrong evidence target mutation fails against the semantic status row language in both fixture and scope verifier suites.
    - Stale structural-only status wording fails because it no longer matches `GCODE_OUTPUT_STATUS_ROW`.
    - Promoted or duplicated `generated-outputs` rows still fail.
  </behavior>
  <action>
    Update embedded valid fixture status rows and wrong-evidence mutation rows in both verifier test scripts to use the exact semantic notes text from Task 1. Add or update a stale-structural-status mutation in each suite: replace the semantic `fork.prusaslicer.gcode-output` row with a row whose evidence target is still `//packages/parity:prusaslicer_gcode_output_parity` but whose notes contain `narrow structural Prusa G-code evidence slice`; assert the verifier fails with a diagnostic containing `fork.prusaslicer.gcode-output status/evidence/notes`. Keep existing duplicate status, missing status, wrong evidence target, and promoted `generated-outputs` mutation cases.
  </action>
  <acceptance_criteria>
    - `rg -n "narrow semantic Prusa G-code evidence slice|wrong_gcode_output_parity|stale.*structural|narrow structural Prusa G-code evidence slice" packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` finds semantic-row tests plus stale structural mutation coverage.
    - `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture` exits 0.
    - `bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test --test_output=errors` exits 0.
    - `bazel run //packages/prusa-gcode-output-scope:verify` exits 0.
    - `bazel test --cache_test_results=no //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test --test_output=errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh && shfmt -l -d packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh && bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test --test_output=errors</automated>
  </verify>
  <done>Fixture and scope verifier test suites enforce semantic status wording, wrong-target rejection, stale structural wording rejection, and broad status restraint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Status TSV to docs/verifiers | `packages/parity/status.tsv` is a public evidence source consumed by package verifiers and docs. |
| Verifier scripts to status data | Bash verifiers read checked-in status rows and fail closed on drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03-01 | Spoofing | `fork.prusaslicer.gcode-output` status row | mitigate | Require exact status, evidence target, and semantic notes text in `status.tsv` and both verifier constants. |
| T-56-03-02 | Repudiation | stale structural-only status wording | mitigate | Add mutation coverage proving structural-only notes no longer pass after semantic publication. |
| T-56-03-03 | Tampering | broad `generated-outputs` status | mitigate | Keep exact one-row `generated-outputs` `in progress` check and promoted-row mutation coverage. |
| T-56-03-04 | Elevation of Privilege | release/sync/network/runtime creep | mitigate | Status text lists these surfaces as deferred and no runtime or external integration files are introduced. |
</threat_model>

<verification>
Run after both tasks:

```bash
bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
shfmt -l -d packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
shellcheck packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
bazel run //packages/parity:status
bazel run //packages/parity:prusaslicer_gcode_output_parity
bazel test --cache_test_results=no //packages/parity:prusaslicer_gcode_output_parity_failure_test --test_output=errors
bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture
bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test --test_output=errors
bazel run //packages/prusa-gcode-output-scope:verify
bazel test --cache_test_results=no //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test --test_output=errors
git diff --check -- packages/parity/status.tsv packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
```

After creating the required phase summary, run closeout verification for the generated summary file. Do not run `mdformat` over phase `*-SUMMARY.md` files; repo-local `AGENTS.md` requires `summary-extract` and `git diff --check` instead so YAML metadata stays parseable.

```bash
node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/56-executable-semantic-g-code-evidence/56-03-SUMMARY.md
git diff --check -- .planning/phases/56-executable-semantic-g-code-evidence/56-03-SUMMARY.md
```
</verification>

<success_criteria>
GSEV-03 status publication is complete when the narrow `fork.prusaslicer.gcode-output` row names the Phase 53 to Phase 56 semantic evidence chain, the broad `generated-outputs` row remains exactly `in progress`, and fixture/scope verifiers fail closed on stale structural wording or wrong evidence targets.
</success_criteria>

<output>
After completion, create `.planning/phases/56-executable-semantic-g-code-evidence/56-03-SUMMARY.md` with `requirements-completed: [GSEV-03]`.
</output>
