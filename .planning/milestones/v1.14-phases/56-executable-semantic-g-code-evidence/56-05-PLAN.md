---
phase: 56-executable-semantic-g-code-evidence
plan: "05"
type: execute
wave: 5
depends_on:
  - "56-04"
files_modified:
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/parity-matrix.md
  - docs/port/migration-guidance.md
autonomous: true
requirements:
  - GSEV-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 56-2026-06-21T16-41-17
generated_at: 2026-06-21T17:08:02.864Z
must_haves:
  truths:
    - Maintainer can inspect public port docs and see `fork.prusaslicer.gcode-output` described as the narrow semantic Prusa G-code evidence slice.
    - Public port docs keep broad `generated-outputs` in progress.
    - Public port docs do not leave stale structural-only current-state wording after semantic publication.
  artifacts:
    - path: docs/port/parity-matrix.md
      provides: generated-output and fork status wording
    - path: docs/port/migration-guidance.md
      provides: maintainer migration boundary wording
    - path: docs/port/README.md
      provides: public port overview with semantic current state
    - path: docs/port/package-map.md
      provides: package ownership and phase history wording
  key_links:
    - from: docs/port/parity-matrix.md
      to: packages/parity/status.tsv
      via: matching `generated-outputs` and `fork.prusaslicer.gcode-output` wording
      pattern: narrow semantic Prusa G-code evidence slice
    - from: docs/port/package-map.md
      to: //packages/parity:prusaslicer_gcode_output_parity
      via: package ownership text
      pattern: bazel run //packages/parity:prusaslicer_gcode_output_parity
---

<objective>
Update public port docs, including `docs/port/README.md`, so they publish the exact narrow semantic evidence slice while keeping broad generated outputs in progress.

Purpose: Complete the public port documentation portion of GSEV-03 and close stale structural-only current-state wording found during research.
Output: Updated parity matrix, migration guidance, port README, and package map.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-04-SUMMARY.md
@packages/parity/status.tsv
@docs/port/README.md
@docs/port/package-map.md
@docs/port/parity-matrix.md
@docs/port/migration-guidance.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update parity matrix and migration guidance</name>
  <files>docs/port/parity-matrix.md, docs/port/migration-guidance.md</files>
  <read_first>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-04-SUMMARY.md
    packages/parity/status.tsv
    docs/port/parity-matrix.md
    docs/port/migration-guidance.md
  </read_first>
  <action>
    In `docs/port/parity-matrix.md`, update the `Generated outputs` row and the Prusa G-code section so `fork.prusaslicer.gcode-output` is verified only for the narrow semantic Prusa G-code evidence slice through `bazel run //packages/parity:prusaslicer_gcode_output_parity`. Include this exact chain: Phase 53 closed semantic scope contract, Phase 54 semantic fixture summary, Phase 55 Rust semantic parser/readiness boundary, and Phase 56 public parity command. Keep the `Generated outputs` status cell exactly `in progress` and state that broad generated-output verification remains deferred.

    In `docs/port/migration-guidance.md`, replace current structural-only current-state wording with semantic evidence wording. Name `expected-gcode-summary.tsv`, `expected-gcode-structural-summary.tsv`, `expected-gcode-semantic-summary.tsv`, `prusa_gcode_output_semantic_summary_lines`, and `bazel run //packages/parity:prusaslicer_gcode_output_parity`. State that this evidence does not prove byte-for-byte G-code parity, broad generated-output verification, toolpath geometry, printability, printer-runtime behavior, support generation, wall seam behavior, arc fitting, GUI behavior, release behavior, sync automation, or non-Prusa fork behavior.
  </action>
  <acceptance_criteria>
    - `rg -n "narrow semantic Prusa G-code evidence slice|Phase 53 closed semantic scope contract|Phase 54 semantic fixture summary|Phase 55 Rust semantic parser/readiness boundary|Phase 56 public parity command" docs/port/parity-matrix.md docs/port/migration-guidance.md` finds semantic evidence chain wording.
    - `rg -n "\\| Generated outputs \\|.*\\| .*in progress.* \\|" docs/port/parity-matrix.md` finds the broad generated-output row still in progress.
    - `rg -n "expected-gcode-summary.tsv|expected-gcode-structural-summary.tsv|expected-gcode-semantic-summary.tsv|prusa_gcode_output_semantic_summary_lines" docs/port/migration-guidance.md` finds all exact artifact/helper names.
    - `! rg -n "narrow structural Prusa G-code evidence slice|Phase 52 public structural|structural-only Prusa G-code" docs/port/parity-matrix.md docs/port/migration-guidance.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mdformat --check docs/port/parity-matrix.md docs/port/migration-guidance.md && bazel run //packages/parity:status</automated>
  </verify>
  <done>Parity matrix and migration guidance publish semantic current-state wording while keeping broad generated outputs in progress and deferred surfaces explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Update port README and package map ownership</name>
  <files>docs/port/README.md, docs/port/package-map.md</files>
  <read_first>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-04-SUMMARY.md
    packages/parity/status.tsv
    docs/port/README.md
    docs/port/package-map.md
    docs/port/parity-matrix.md
    docs/port/migration-guidance.md
  </read_first>
  <action>
    Update `docs/port/README.md` wherever the Prusa G-code current state still says structural-only. It must say `bazel run //packages/parity:prusaslicer_gcode_output_parity` is the shared public command for the narrow semantic Prusa G-code evidence slice backed by Phase 53 scope, Phase 54 semantic fixture summary, Phase 55 Rust semantic parser/readiness boundary, and Phase 56 public parity command/status wiring. Keep broad generated-outputs in progress and keep deferred surfaces explicit.

    Update `docs/port/package-map.md` package ownership text so `packages/parity` owns the public semantic Prusa G-code command/status wiring, `packages/parity-fixtures` owns the marker/structural/semantic checked-in summaries, `packages/prusa-gcode-output-scope` owns the closed semantic scope contract, and `packages/slic3r-rust` owns the Rust semantic parser/readiness/summary-line boundary. Keep Phase 49 through Phase 52 as historical structural rungs only when naming history; current published state must be semantic.
  </action>
  <acceptance_criteria>
    - `rg -n "narrow semantic Prusa G-code evidence slice|Phase 53 closed semantic scope contract|Phase 54 semantic fixture summary|Phase 55 Rust semantic parser/readiness boundary|Phase 56 public parity command" docs/port/README.md docs/port/package-map.md` finds current semantic wording.
    - `rg -n "packages/parity.*public semantic Prusa G-code|packages/parity-fixtures.*marker/structural/semantic|packages/prusa-gcode-output-scope.*semantic scope|packages/slic3r-rust.*semantic parser/readiness" docs/port/package-map.md` finds package ownership wording.
    - `rg -n "broad generated-outputs.*in progress|Generated outputs.*in progress" docs/port/README.md docs/port/package-map.md` finds broad-status restraint wording.
    - `! rg -n "narrow structural Prusa G-code evidence slice|Phase 52 public structural|structural-only Prusa G-code" docs/port/README.md docs/port/package-map.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mdformat --check docs/port/README.md docs/port/package-map.md && bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
  </verify>
  <done>Port README and package map publish semantic current-state ownership, include `docs/port/README.md` drift closure, and keep broad generated-output status in progress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs to maintainer decisions | Port docs communicate what evidence is verified and what remains deferred. |
| Status TSV to port docs | Port docs must match `packages/parity/status.tsv` without promoting broad generated-output status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-05-01 | Spoofing | public port evidence wording | mitigate | Use exact status token, public command, and Phase 53 to Phase 56 semantic chain in all touched docs. |
| T-56-05-02 | Repudiation | stale structural-only current state | mitigate | Add grep acceptance checks that reject current-state phrases `narrow structural Prusa G-code evidence slice`, `Phase 52 public structural`, and `structural-only Prusa G-code` in touched port docs. |
| T-56-05-03 | Tampering | broad generated-output status | mitigate | Require `Generated outputs` to remain `in progress` and keep deferred surfaces explicit. |
| T-56-05-04 | Information Disclosure | docs-only publication | accept | Docs introduce no secrets, credentials, file reads, network, runtime, release, host upload, or sync behavior. |
</threat_model>

<verification>
Run after both tasks:

```bash
mdformat --check docs/port/README.md docs/port/package-map.md docs/port/parity-matrix.md docs/port/migration-guidance.md
bazel run //packages/parity:status
bazel run //packages/parity:prusaslicer_gcode_output_parity
bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture
bazel run //packages/prusa-gcode-output-scope:verify
awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv
git diff --check -- docs/port/README.md docs/port/package-map.md docs/port/parity-matrix.md docs/port/migration-guidance.md
```

After creating the required phase summary, run closeout verification for the generated summary file. Do not run `mdformat` over phase `*-SUMMARY.md` files; repo-local `AGENTS.md` requires `summary-extract` and `git diff --check` instead so YAML metadata stays parseable.

```bash
node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/56-executable-semantic-g-code-evidence/56-05-SUMMARY.md
git diff --check -- .planning/phases/56-executable-semantic-g-code-evidence/56-05-SUMMARY.md
```
</verification>

<success_criteria>
GSEV-03 public port docs are complete when the parity matrix, migration guidance, port README, and package map all describe `fork.prusaslicer.gcode-output` as the narrow semantic Prusa G-code evidence slice, keep broad `generated-outputs` in progress, and contain no stale structural-only current-state wording.
</success_criteria>

<output>
After completion, create `.planning/phases/56-executable-semantic-g-code-evidence/56-05-SUMMARY.md` with `requirements-completed: [GSEV-03]`.
</output>
