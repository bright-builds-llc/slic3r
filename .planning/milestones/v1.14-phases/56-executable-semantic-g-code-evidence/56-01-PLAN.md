---
phase: 56-executable-semantic-g-code-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
  - packages/parity/BUILD.bazel
  - packages/parity/compare_prusaslicer_gcode_output.sh
autonomous: true
requirements:
  - GSEV-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 56-2026-06-21T16-41-17
generated_at: 2026-06-21T17:08:02.864Z
must_haves:
  truths:
    - Maintainer can run `bazel run //packages/parity:prusaslicer_gcode_output_parity` and get marker, structural, and semantic Prusa G-code evidence.
    - The public target name remains exactly `//packages/parity:prusaslicer_gcode_output_parity`.
    - Semantic validation is performed through the Rust `slic3r_flavors` boundary, not through a Bash semantic parser.
  artifacts:
    - path: packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
      provides: `prusa_gcode_output_semantic_summary_lines`
    - path: packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
      provides: `--semantic expected-gcode-semantic-summary.tsv` mode
    - path: packages/parity/compare_prusaslicer_gcode_output.sh
      provides: public marker, structural, and semantic comparison orchestration
    - path: packages/parity/BUILD.bazel
      provides: unchanged public target with semantic fixture data
  key_links:
    - from: packages/parity/BUILD.bazel
      to: //packages/parity:prusaslicer_gcode_output_parity
      via: unchanged sh_binary target name
      pattern: name = "prusaslicer_gcode_output_parity"
    - from: packages/parity/compare_prusaslicer_gcode_output.sh
      to: packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
      via: `--semantic` invocation
      pattern: --semantic
    - from: packages/parity/compare_prusaslicer_gcode_output.sh
      to: packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
      via: expected semantic artifact argument
      pattern: expected-gcode-semantic-summary.tsv
---

<objective>
Extend the existing public Prusa G-code evidence command so it validates the Phase 54 semantic summary artifact through the Phase 55 Rust semantic boundary.

Purpose: Complete the executable portion of GSEV-01 without creating a companion command or widening generated-output claims.
Output: Rust semantic summary lines, `--semantic` binary mode, Bazel semantic fixture wiring, and public comparator semantic validation.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
@.planning/phases/55-rust-semantic-g-code-summary-boundary/55-01-SUMMARY.md
@.planning/phases/55-rust-semantic-g-code-summary-boundary/55-02-SUMMARY.md
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
@packages/parity/BUILD.bazel
@packages/parity/compare_prusaslicer_gcode_output.sh

<interfaces>
From `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs`:
```rust
pub fn parse_prusa_gcode_output_semantic_summary(
    input: &str,
) -> Result<PrusaGcodeOutputSemanticSummary, PrusaGcodeOutputSemanticParseError>;

pub fn prusa_gcode_output_structural_summary_lines(
    input: &str,
) -> Result<Vec<String>, PrusaGcodeOutputStructuralParseError>;
```

From `packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs`:
```rust
const USAGE_ERROR: &str =
    "expected expected-gcode-summary.tsv or --structural expected-gcode-structural-summary.tsv";
```

From `packages/parity/compare_prusaslicer_gcode_output.sh`:
```bash
if [[ "$#" -ne 6 ]]; then
    printf 'error: expected summary_binary rust-summary-input.tsv expected-gcode-summary.tsv rust-structural-input.tsv expected-gcode-structural-summary.tsv fixture-provenance.tsv\n' >&2
    exit 2
fi
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add semantic summary lines and binary mode</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs, packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs, packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs</files>
  <read_first>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
    .planning/phases/55-rust-semantic-g-code-summary-boundary/55-01-SUMMARY.md
    .planning/phases/55-rust-semantic-g-code-summary-boundary/55-02-SUMMARY.md
    packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
    packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
    packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
    packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
  </read_first>
  <behavior>
    - Test 1: `prusa_gcode_output_semantic_summary_lines(EXPECTED_GCODE_SEMANTIC_SUMMARY)` returns exactly these keys in order: `semantic_summary_path`, `semantic_row_count`, `source_ref`, `fixture_id`, `fixture_path`, `command_class_counts`, `movement_class_counts`, `coordinate_bounds`, `extrusion_total`, `feedrate_observations`, `layer_marker_relationships`.
    - Test 2: the summary binary accepts `--semantic expected-gcode-semantic-summary.tsv`, exits successfully, and prints `semantic_row_count\t9` plus `feedrate_observations\tF99999.123;F1;F203.2;F203.201`.
    - Test 3: invalid binary arguments print usage containing `--semantic expected-gcode-semantic-summary.tsv`.
  </behavior>
  <action>
    Add `pub fn prusa_gcode_output_semantic_summary_lines(input: &str) -> Result<Vec<String>, PrusaGcodeOutputSemanticParseError>` to `prusa_gcode_output.rs`. It must call `parse_prusa_gcode_output_semantic_summary(input)?`, use `prusa_gcode_output_semantic_readiness()`, project `summary.facts()`, and return stable tab-delimited lines with these exact values for the checked-in fixture: `semantic_summary_path` = `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv`, `semantic_row_count` = `9`, `source_ref` = `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, `fixture_id` = `gcodewriter-set-speed.gcode`, `fixture_path` = `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode`, `command_class_counts` = `G1:4;feedrate_only:4`, `movement_class_counts` = `travel:0;extrusion:0;coordinate_motion:0;feedrate_only:4`, `coordinate_bounds` = `x:none;y:none;z:none`, `extrusion_total` = `e_axis_observed:false;extrusion_total:not_observed`, `feedrate_observations` = `F99999.123;F1;F203.2;F203.201`, and `layer_marker_relationships` = `layer_markers:0;marker_relationships:none`.

    Re-export `prusa_gcode_output_semantic_summary_lines` from `src/lib.rs`. Update `prusa_gcode_output_summary.rs` to import the helper, change usage text to exactly `expected expected-gcode-summary.tsv, --structural expected-gcode-structural-summary.tsv, or --semantic expected-gcode-semantic-summary.tsv`, add a `--semantic` branch beside `--structural`, and implement `run_semantic_summary` with the same read/print adapter pattern. Do not add filesystem discovery, Git, network, process execution beyond the existing binary file read, generator behavior, status mutation, docs publication, or any new crate dependency.
  </action>
  <acceptance_criteria>
    - `rg -n "pub fn prusa_gcode_output_semantic_summary_lines" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` finds the new helper.
    - `rg -n "prusa_gcode_output_semantic_summary_lines" packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs` finds the export, binary import/use, and tests.
    - `rg -n -- "--semantic expected-gcode-semantic-summary.tsv" packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs` finds the usage text and test expectation.
    - `rg -n "semantic_row_count|movement_class_counts|coordinate_bounds|extrusion_total|feedrate_observations|layer_marker_relationships" packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs` finds semantic summary-line assertions.
  </acceptance_criteria>
  <verify>
    <automated>cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_gcode_output</automated>
  </verify>
  <done>Rust exposes semantic summary lines, the binary supports `--semantic`, existing marker and structural modes still pass their tests, and invalid usage text names all three accepted invocation forms.</done>
</task>

<task type="auto">
  <name>Task 2: Wire semantic validation into the public parity target</name>
  <files>packages/parity/BUILD.bazel, packages/parity/compare_prusaslicer_gcode_output.sh</files>
  <read_first>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
    packages/parity/BUILD.bazel
    packages/parity/compare_prusaslicer_gcode_output.sh
    packages/parity-fixtures/BUILD.bazel
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
  </read_first>
  <action>
    Update `packages/parity/BUILD.bazel` without changing the public target name `prusaslicer_gcode_output_parity`. Add `//packages/parity-fixtures:prusa_gcode_output_expected_gcode_semantic_summary` to the `data` list for both `prusaslicer_gcode_output_parity` and `prusaslicer_gcode_output_parity_failure_test`. Add two args to the sh_binary immediately after the structural pair and before provenance: `$(location //packages/parity-fixtures:prusa_gcode_output_expected_gcode_semantic_summary)` as the Rust semantic input and the same location again as the expected semantic artifact.

    Update `compare_prusaslicer_gcode_output.sh` from six to eight positional arguments. The usage error must be exactly `error: expected summary_binary rust-summary-input.tsv expected-gcode-summary.tsv rust-structural-input.tsv expected-gcode-structural-summary.tsv rust-semantic-input.tsv expected-gcode-semantic-summary.tsv fixture-provenance.tsv`. Add `rust_semantic_input` and `expected_semantic_artifact` variables, assert both files with labels `rust-semantic-input.tsv` and `expected-gcode-semantic-summary.tsv`, and add temp outputs `actual_semantic_summary` and `expected_semantic_summary_lines`.

    After structural validation, invoke `"${summary_binary}" --semantic "${rust_semantic_input}"` and `"${summary_binary}" --semantic "${expected_semantic_artifact}"`, diff the generated semantic summary lines, and on expected-artifact validation failures print diagnostics that include `expected-gcode-semantic-summary.tsv`, a semantic field label from column 3, the expected artifact path, and a unified diff. Add semantic fact assertions for `semantic_summary_path`, `semantic_row_count`, `source_ref`, `fixture_id`, `fixture_path`, `command_class_counts`, `movement_class_counts`, `coordinate_bounds`, `extrusion_total`, `feedrate_observations`, and `layer_marker_relationships` with the exact values from Task 1.

    Change the final success line to `ok: fork.prusaslicer.gcode-output semantic evidence passed`. Keep the existing marker and structural output lines, and add these exact semantic output labels: `semantic_expected: packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv`, `semantic_rows: 9`, `semantic_command_classes: G1:4;feedrate_only:4`, `semantic_movement_classes: travel:0;extrusion:0;coordinate_motion:0;feedrate_only:4`, `semantic_coordinate_bounds: x:none;y:none;z:none`, `semantic_extrusion_total: e_axis_observed:false;extrusion_total:not_observed`, `semantic_feedrates: F99999.123;F1;F203.2;F203.201`, and `semantic_layer_markers: layer_markers:0;marker_relationships:none`. Do not print `generated-output parity`, `byte-for-byte parity`, `printability`, `runtime`, `support`, `seam`, or `arc` as achieved behavior.
  </action>
  <acceptance_criteria>
    - `rg -n "name = \"prusaslicer_gcode_output_parity\"" packages/parity/BUILD.bazel` still finds the unchanged public target name.
    - `rg -n "prusa_gcode_output_expected_gcode_semantic_summary" packages/parity/BUILD.bazel` finds the semantic alias in both public command wiring and failure-test data.
    - `rg -n "rust-semantic-input.tsv|expected-gcode-semantic-summary.tsv|actual_semantic_summary|expected_semantic_summary_lines|--semantic" packages/parity/compare_prusaslicer_gcode_output.sh` finds the semantic argument, temp output, and Rust binary invocation wiring.
    - `rg -n "semantic_row_count|movement_class_counts|coordinate_bounds|extrusion_total|feedrate_observations|layer_marker_relationships" packages/parity/compare_prusaslicer_gcode_output.sh` finds semantic fact assertions.
    - `bazel run //packages/parity:prusaslicer_gcode_output_parity` exits 0 and stdout contains `ok: fork.prusaslicer.gcode-output semantic evidence passed`, `semantic_rows: 9`, and `semantic_feedrates: F99999.123;F1;F203.2;F203.201`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n packages/parity/compare_prusaslicer_gcode_output.sh && shfmt -l -d packages/parity/compare_prusaslicer_gcode_output.sh && bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
  </verify>
  <done>The existing public parity command validates marker, structural, and semantic artifacts through the Rust binary and prints narrow semantic evidence facts while preserving the public target name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazel to Bash comparator | Bazel passes explicit checked-in artifact paths into `compare_prusaslicer_gcode_output.sh`. |
| Bash comparator to Rust binary | Bash invokes the Rust binary over caller-supplied local TSV paths. |
| TSV artifact to Rust parser | Checked-in semantic TSV text is parsed into typed Rust facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01-01 | Tampering | `expected-gcode-semantic-summary.tsv` validation | mitigate | Validate exact header, row order, field names, source/fixture identity, values, and evidence-boundary text through `parse_prusa_gcode_output_semantic_summary`; diff Rust-generated summary lines. |
| T-56-01-02 | Information Disclosure | Bash/Rust file handling | mitigate | Read only explicit Bazel-provided local artifact paths; do not add discovery, Git, network, device, credential, host upload, release, or sync behavior. |
| T-56-01-03 | Denial of Service | malformed TSV input | mitigate | Rust parser returns typed errors and Bash exits nonzero with field-specific diagnostics instead of panicking or silently accepting drift. |
| T-56-01-04 | Spoofing | public evidence command identity | mitigate | Preserve target name `//packages/parity:prusaslicer_gcode_output_parity`; do not create a companion command that could split status evidence. |
</threat_model>

<verification>
Run after both tasks:

```bash
cargo +1.94.1 fmt --manifest-path packages/slic3r-rust/Cargo.toml --all
cargo +1.94.1 clippy --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-targets --all-features -- -D warnings
cargo +1.94.1 build --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-targets --all-features
cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-features
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test --test_output=errors
bazel run //packages/parity:prusaslicer_gcode_output_parity
git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs packages/parity/BUILD.bazel packages/parity/compare_prusaslicer_gcode_output.sh
```

After creating the required phase summary, run closeout verification for the generated summary file. Do not run `mdformat` over phase `*-SUMMARY.md` files; repo-local `AGENTS.md` requires `summary-extract` and `git diff --check` instead so YAML metadata stays parseable.

```bash
node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/56-executable-semantic-g-code-evidence/56-01-SUMMARY.md
git diff --check -- .planning/phases/56-executable-semantic-g-code-evidence/56-01-SUMMARY.md
```
</verification>

<success_criteria>
GSEV-01 is satisfied for command integration when `bazel run //packages/parity:prusaslicer_gcode_output_parity` validates marker, structural, and semantic artifacts through Rust and prints the narrow semantic evidence facts without changing the public target name or promoting broad `generated-outputs`.
</success_criteria>

<output>
After completion, create `.planning/phases/56-executable-semantic-g-code-evidence/56-01-SUMMARY.md` with `requirements-completed: [GSEV-01]`.
</output>
