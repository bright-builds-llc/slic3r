---
phase: 55-rust-semantic-g-code-summary-boundary
plan: "02"
type: execute
wave: 2
depends_on:
  - 55-01-PLAN.md
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
autonomous: true
requirements:
  - GSRUST-02
  - GSRUST-03
must_haves:
  truths:
    - "Developer can inspect semantic Prusa G-code readiness metadata through slic3r_flavors without reading files or discovering paths."
    - "Registry metadata traces the semantic boundary to source identity, fixture corpus, semantic expected summary, planned command, planned status token, and deferred generated-output surfaces."
    - "Phase 55 keeps broad generated-outputs in progress and leaves the existing fork.prusaslicer.gcode-output status publication unchanged."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      provides: "Semantic readiness metadata type and helper"
      contains: "prusa_gcode_output_semantic_readiness"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs"
      provides: "Public re-export for semantic readiness API"
      contains: "PrusaGcodeOutputSemanticReadiness"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      provides: "Existing registry boundary with semantic pre-publication wording"
      contains: "semantic summary parser/readiness metadata"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs"
      provides: "Cargo and Bazel readiness/registry/no-overclaiming coverage"
      contains: "prusa_gcode_output_semantic_readiness"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      via: "shared Prusa G-code source and readiness constants"
      pattern: "PRUSA_GCODE_OUTPUT_SOURCE_REF|prusa_gcode_output_semantic_readiness"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs"
      to: "packages/parity/status.tsv"
      via: "status restraint checks"
      pattern: "generated-outputs.*in progress|fork.prusaslicer.gcode-output.*narrow structural"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      via: "public re-export"
      pattern: "PrusaGcodeOutputSemanticReadiness"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 55-2026-06-21T14-58-10
generated_at: 2026-06-21T15:15:20.151Z
---

<objective>
Expose semantic Prusa G-code readiness metadata through the existing `slic3r_flavors` registry boundary without status publication.

Purpose: Complete GSRUST-02 and the registry/no-overclaiming portion of GSRUST-03 after Plan 55-01 creates the semantic parser API.
Output: Additive readiness metadata, exact registry wording, public re-exports, and Cargo/Bazel registry tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-rust-semantic-g-code-summary-boundary/55-CONTEXT.md
@.planning/phases/55-rust-semantic-g-code-summary-boundary/55-RESEARCH.md
@.planning/phases/55-rust-semantic-g-code-summary-boundary/55-01-SUMMARY.md
@.planning/phases/54-semantic-g-code-fixture-corpus/54-01-SUMMARY.md
@.planning/phases/54-semantic-g-code-fixture-corpus/54-02-SUMMARY.md
@.planning/milestones/v1.13-phases/51-rust-structural-g-code-summary-boundary/51-02-SUMMARY.md
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
@packages/parity/status.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
@packages/prusa-gcode-output-scope/gcode-output-scope.md

<interfaces>
Plan 55-01 creates these semantic parser symbols for this plan to use:

```rust
pub const PRUSA_GCODE_OUTPUT_EXPECTED_SEMANTIC_SUMMARY_PATH: &str;
pub struct PrusaGcodeOutputSemanticSummary;
pub struct PrusaGcodeOutputSemanticFacts;
pub enum PrusaGcodeOutputSemanticField;
pub enum PrusaGcodeOutputSemanticParseError;
pub fn parse_prusa_gcode_output_semantic_summary(
    input: &str,
) -> PrusaGcodeOutputSemanticParseResult;
```

Existing registry capability shape:

```rust
pub struct FlavorCapability {
    pub capability_id: &'static str,
    pub parity_dependencies: &'static [ParitySurface],
    pub checklist_status: ChecklistStatus,
    pub future_parity_notes: &'static str,
}
```

Required semantic readiness values:

```text
source_ref = prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
fixture_corpus_path = packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output
expected_semantic_summary_path = packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
parser_boundary = slic3r_flavors::prusa_gcode_output::parse_prusa_gcode_output_semantic_summary
planned_public_command = //packages/parity:prusaslicer_gcode_output_parity
planned_status_token = fork.prusaslicer.gcode-output
generated_outputs_status = in progress
```
</interfaces>
</context>

<decision_coverage>
D-03 full: keep marker, structural, and Plan 55-01 semantic parser APIs stable while adding readiness metadata.
D-11 full: expose semantic readiness through `slic3r_flavors` metadata and registry boundaries with source, fixture corpus, expected summary, planned command, planned status token, and deferred surfaces.
D-12 full: readiness wording states semantic parser/readiness metadata is Phase 55 developer-facing while public semantic evidence and status/docs publication are Phase 56-owned.
D-13 full: do not edit `packages/parity/status.tsv`; preserve `generated-outputs` as `in progress` and the existing `fork.prusaslicer.gcode-output` structural publication.
D-14 full: readiness and registry names do not claim byte parity, printability, runtime, support, seam, arc, GUI, release, upstream import, sync, or non-Prusa behavior.
D-15 full: registry/readiness wording names deferred surfaces explicitly so broad semantic claims do not pass as readiness metadata.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add semantic readiness metadata and public re-exports</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs, packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs</files>
  <read_first>
    - .planning/phases/55-rust-semantic-g-code-summary-boundary/55-CONTEXT.md
    - .planning/phases/55-rust-semantic-g-code-summary-boundary/55-RESEARCH.md
    - .planning/phases/55-rust-semantic-g-code-summary-boundary/55-01-SUMMARY.md
    - .planning/milestones/v1.13-phases/51-rust-structural-g-code-summary-boundary/51-02-SUMMARY.md
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
    - packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
    - packages/parity/status.tsv
  </read_first>
  <behavior>
    - Test 1: `prusa_gcode_output_semantic_readiness()` returns accepted Prusa source identity, fixture corpus path, semantic summary path, parser boundary, planned command, planned status token, generated-output status boundary, pre-publication boundary, and deferred surfaces.
    - Test 2: `prusa_gcode_output_metadata()` exposes `expected_semantic_summary_path` without changing existing marker or structural path values.
    - Test 3: `slic3r_flavors` re-exports `PrusaGcodeOutputSemanticReadiness` and `prusa_gcode_output_semantic_readiness`.
  </behavior>
  <action>Start with `tests/flavor_registry.rs`, then implement readiness metadata.

In `packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs`:
- Import `prusa_gcode_output_semantic_readiness`.
- Add `prusa_gcode_output_semantic_readiness_exposes_pre_publication_metadata_without_status_publication`.
- Assert these exact values:
  - `inventory_id == "prusaslicer.gcode-output"`
  - `source_ref == VendorSourceRef::prusa_slicer_version_2_9_5()`
  - `inventory_source_paths == &["src/libslic3r/GCode.cpp", "src/libslic3r/GCode.hpp"]`
  - `fixture_corpus_path == "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output"`
  - `fixture_path == "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode"`
  - `expected_semantic_summary_path == "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv"`
  - `parser_boundary == "slic3r_flavors::prusa_gcode_output::parse_prusa_gcode_output_semantic_summary"`
  - `planned_public_command == "//packages/parity:prusaslicer_gcode_output_parity"`
  - `planned_status_token == "fork.prusaslicer.gcode-output"`
  - `generated_outputs_status == "in progress"`
  - `pre_publication_boundary == "Phase 55 exposes semantic parser/readiness metadata only; Phase 56 owns public semantic parity evidence and status/docs publication."`
  - `deferred_surfaces` contains exactly these strings in order: `byte-for-byte G-code parity`, `broad generated-output verification`, `toolpath geometry parity`, `printability`, `printer-runtime behavior`, `support generation`, `wall seam behavior`, `arc fitting`, `GUI export/viewer behavior`, `release behavior`, `network/device behavior`, `non-Prusa fork behavior`, `upstream source imports`, `sync automation`.
- Extend the existing metadata test to assert `metadata.expected_semantic_summary_path` equals the semantic TSV path above.

In `src/prusa_gcode_output.rs`:
- Add `const PRUSA_GCODE_OUTPUT_FIXTURE_CORPUS_PATH: &str = "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output";`.
- Add `static PRUSA_GCODE_OUTPUT_DEFERRED_SEMANTIC_SURFACES: [&str; 14]` with the exact ordered strings listed above.
- Add `expected_semantic_summary_path: &'static str` to `PrusaGcodeOutputMetadata` and set it to `PRUSA_GCODE_OUTPUT_EXPECTED_SEMANTIC_SUMMARY_PATH` in `prusa_gcode_output_metadata()`.
- Add:

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct PrusaGcodeOutputSemanticReadiness {
    pub inventory_id: &'static str,
    pub source_ref: VendorSourceRef,
    pub inventory_source_paths: &'static [&'static str],
    pub fixture_corpus_path: &'static str,
    pub fixture_path: &'static str,
    pub expected_semantic_summary_path: &'static str,
    pub parser_boundary: &'static str,
    pub planned_public_command: &'static str,
    pub planned_status_token: &'static str,
    pub generated_outputs_status: &'static str,
    pub pre_publication_boundary: &'static str,
    pub deferred_surfaces: &'static [&'static str],
}
```

- Add `pub const fn prusa_gcode_output_semantic_readiness() -> PrusaGcodeOutputSemanticReadiness` returning the exact values asserted by the test.
- Keep this metadata side-effect free: no `std::fs`, `std::process`, environment reads, network, Git, generator, release, sync, or status mutation.

In `src/lib.rs`, re-export `PrusaGcodeOutputSemanticReadiness` and `prusa_gcode_output_semantic_readiness`.</action>
  <acceptance_criteria>
    - `rg -n "PrusaGcodeOutputSemanticReadiness|prusa_gcode_output_semantic_readiness|expected_semantic_summary_path|PRUSA_GCODE_OUTPUT_DEFERRED_SEMANTIC_SURFACES|PRUSA_GCODE_OUTPUT_FIXTURE_CORPUS_PATH" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` finds all symbols.
    - `rg -n "PrusaGcodeOutputSemanticReadiness|prusa_gcode_output_semantic_readiness" packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs` finds re-exports.
    - `rg -n "prusa_gcode_output_semantic_readiness_exposes_pre_publication_metadata_without_status_publication|expected_semantic_summary_path|Phase 55 exposes semantic parser/readiness metadata only" packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` finds readiness tests.
    - `cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test flavor_registry` exits 0.
    - `cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_gcode_output` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "PrusaGcodeOutputSemanticReadiness|prusa_gcode_output_semantic_readiness|expected_semantic_summary_path|PRUSA_GCODE_OUTPUT_DEFERRED_SEMANTIC_SURFACES|PRUSA_GCODE_OUTPUT_FIXTURE_CORPUS_PATH" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs</automated>
    <automated>rg -n "PrusaGcodeOutputSemanticReadiness|prusa_gcode_output_semantic_readiness" packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs</automated>
    <automated>rg -n "prusa_gcode_output_semantic_readiness_exposes_pre_publication_metadata_without_status_publication|expected_semantic_summary_path|Phase 55 exposes semantic parser/readiness metadata only" packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs</automated>
    <automated>cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test flavor_registry</automated>
    <automated>cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_gcode_output</automated>
  </verify>
  <done>Semantic readiness metadata is inspectable through `slic3r_flavors`, traces the Phase 55 parser boundary and Phase 54 semantic artifact, and remains explicitly pre-publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update registry wording and prove status restraint</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs</files>
  <read_first>
    - .planning/phases/55-rust-semantic-g-code-summary-boundary/55-CONTEXT.md
    - .planning/phases/55-rust-semantic-g-code-summary-boundary/55-RESEARCH.md
    - .planning/phases/55-rust-semantic-g-code-summary-boundary/55-01-SUMMARY.md
    - packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
    - packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
    - packages/parity/status.tsv
    - packages/parity/README.md
    - docs/port/parity-matrix.md
    - docs/port/migration-guidance.md
    - docs/port/package-map.md
  </read_first>
  <behavior>
    - Test 1: the `prusaslicer.gcode-output` registry capability still has `ChecklistStatus::FutureCandidate` and `ParitySurface::generated_outputs()`.
    - Test 2: `future_parity_notes` names semantic parser/readiness metadata as Phase 55 developer-facing and Phase 56 publication-owned.
    - Shell verification: status rows remain unchanged: broad `generated-outputs` is `in progress` and `fork.prusaslicer.gcode-output` remains the narrow structural evidence row.
    - Test 3: no registry helper or note claims byte parity, printability, runtime, support, seam, arc, GUI, release, sync, or non-Prusa behavior.
  </behavior>
  <action>Update the existing Prusa G-code registry note in `src/registry.rs`; do not add a second capability row and do not edit `packages/parity/status.tsv`.

Set the `future_parity_notes` for capability id `prusaslicer.gcode-output` to this exact string:

```text
Source-observed G-code output planning row; semantic summary parser/readiness metadata is available for Phase 55 developers, while public semantic parity evidence and status/docs publication remain Phase 56-owned before broader generated-output behavior is claimed. The broad generated-outputs surface stays in progress; no byte-for-byte G-code parity, printability, printer-runtime, support, seam, arc, GUI, release, sync, or non-Prusa fork behavior is claimed.
```

In `tests/flavor_registry.rs`:
- Replace `PRUSA_GCODE_OUTPUT_STRUCTURAL_READINESS_NOTES` with `PRUSA_GCODE_OUTPUT_SEMANTIC_READINESS_NOTES` containing the exact string above.
- Update `prusa_gcode_output_registry_row_traces_to_source_and_generated_output_dependency` so it asserts the exact note, `ChecklistStatus::FutureCandidate`, `ParitySurface::generated_outputs()`, empty caution flags, source provenance, and FutureCandidate filtering.
- Do not add a Rust `include_str!` status-row test and do not edit `BUILD.bazel` in this plan. Status-row restraint is proven by the shell verification commands below against these exact row fragments:
  - `generated-outputs\tin progress\tdocs/port/cli-slice.md`
  - `fork.prusaslicer.gcode-output\tverified\t//packages/parity:prusaslicer_gcode_output_parity\tShared fixture comparison proves the narrow structural Prusa G-code evidence slice`
- Do not edit public parity docs, port docs, status rows, public parity commands, or `src/bin/prusa_gcode_output_summary.rs`.</action>
  <acceptance_criteria>
    - `rg -n "semantic summary parser/readiness metadata is available for Phase 55 developers|public semantic parity evidence and status/docs publication remain Phase 56-owned|broad generated-outputs surface stays in progress" packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` finds exact registry/test wording.
    - `cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test flavor_registry` exits 0.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test --test_output=errors` exits 0.
    - `awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `awk -F '\t' '$1=="fork.prusaslicer.gcode-output" && $2=="verified" && $3=="//packages/parity:prusaslicer_gcode_output_parity" && $4 ~ /narrow structural Prusa G-code evidence slice/ { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/parity/BUILD.bazel packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs)"` passes.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "semantic summary parser/readiness metadata is available for Phase 55 developers|public semantic parity evidence and status/docs publication remain Phase 56-owned|broad generated-outputs surface stays in progress" packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs</automated>
    <automated>cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test flavor_registry</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test --test_output=errors</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check</automated>
    <automated>bazel build //packages/slic3r-rust/crates/slic3r_flavors:clippy</automated>
    <automated>awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv</automated>
    <automated>awk -F '\t' '$1=="fork.prusaslicer.gcode-output" && $2=="verified" && $3=="//packages/parity:prusaslicer_gcode_output_parity" && $4 ~ /narrow structural Prusa G-code evidence slice/ { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv</automated>
    <automated>git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs</automated>
    <automated>test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/parity/BUILD.bazel packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs)"</automated>
  </verify>
  <done>The registry exposes semantic readiness as developer-facing metadata only, and automated checks prove public status/docs/command publication did not change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed parser API -> registry/readiness metadata | Parser readiness becomes inspectable static metadata consumed by downstream agents. |
| registry metadata -> public status assumptions | Internal notes could be mistaken for public parity/status publication. |
| repo status/docs files -> Phase 55 implementation | Existing public status/docs must remain outside this phase's write boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02-01 | Spoofing | `PrusaGcodeOutputSemanticReadiness` | mitigate | Readiness returns the same accepted `VendorSourceRef::prusa_slicer_version_2_9_5()` and fixture constants used by the parser. |
| T-55-02-02 | Tampering | registry `future_parity_notes` | mitigate | Tests assert exact pre-publication wording and exact generated-output deferral language. |
| T-55-02-03 | Repudiation | status boundary | mitigate | Shell verification proves `generated-outputs` remains `in progress` and `fork.prusaslicer.gcode-output` remains the narrow structural row. |
| T-55-02-04 | Information Disclosure | readiness metadata | accept | Low risk because all exposed values are already checked-in source, fixture, parser-boundary, and deferred-scope strings; no secrets or external data are accessed. |
| T-55-02-05 | Denial of Service | readiness helper | accept | Low risk because readiness is a const/static metadata accessor with no loops over untrusted input and no external resource access. |
| T-55-02-06 | Elevation of Privilege | accidental publication side effects | mitigate | Plan forbids edits to public status/docs, parity command behavior, binary semantic mode, filesystem discovery, Git, network, process, generator, release, or sync behavior; verification checks the forbidden diffs. |
</threat_model>

<verification>
Overall plan verification:

```bash
cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test flavor_registry
cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_gcode_output
bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test --test_output=errors
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test --test_output=errors
bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check
bazel build //packages/slic3r-rust/crates/slic3r_flavors:clippy
awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv
awk -F '\t' '$1=="fork.prusaslicer.gcode-output" && $2=="verified" && $3=="//packages/parity:prusaslicer_gcode_output_parity" && $4 ~ /narrow structural Prusa G-code evidence slice/ { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv
test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/parity/BUILD.bazel packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs)"
```
</verification>

<success_criteria>
- `prusa_gcode_output_semantic_readiness()` exists, is re-exported, and exposes exact source, fixture corpus, semantic artifact, parser boundary, planned command, planned status token, pre-publication boundary, and deferred surfaces.
- Registry metadata for `prusaslicer.gcode-output` names semantic readiness without claiming public semantic evidence.
- `generated-outputs` remains `in progress`; `fork.prusaslicer.gcode-output` remains the existing narrow structural verified row.
- Cargo and Bazel tests prove readiness metadata and registry status restraint.
- No public status/docs publication, binary semantic mode, generator behavior, filesystem discovery, Git, network, process, release, sync, runtime, or non-Prusa behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/55-rust-semantic-g-code-summary-boundary/55-02-SUMMARY.md` with `requirements-completed` covering `GSRUST-02` and the GSRUST-03 coverage materially completed by this plan.
</output>
