---
phase: 55-rust-semantic-g-code-summary-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
  - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
autonomous: true
requirements:
  - GSRUST-01
  - GSRUST-03
must_haves:
  truths:
    - "Developer can parse caller-supplied Phase 54 semantic TSV text into typed Rust rows and facts."
    - "Cargo and Bazel tests accept the checked-in semantic fixture and reject malformed or overclaiming semantic summaries."
    - "The semantic parser is pure data-in/data-out and introduces no filesystem discovery, Git, network, process, generator, runtime, release, sync, status, or public parity behavior."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      provides: "Typed semantic summary parser, row model, parse errors, and facts projection"
      contains: "parse_prusa_gcode_output_semantic_summary"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs"
      provides: "Public re-exports for semantic parser types and function"
      contains: "PrusaGcodeOutputSemanticSummary"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs"
      provides: "Cargo and Bazel valid/fail-closed semantic parser coverage"
      contains: "EXPECTED_GCODE_SEMANTIC_SUMMARY"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel"
      provides: "Bazel compile_data for the semantic TSV under existing prusa_gcode_output_test"
      contains: "prusa_gcode_output_expected_gcode_semantic_summary"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv"
      via: "include_str! compile data"
      pattern: "expected-gcode-semantic-summary.tsv"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      to: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      via: "closed Phase 53 semantic field order"
      pattern: "source_ref|fixture_id|fixture_path|command_class_counts|movement_class_counts|coordinate_bounds|extrusion_total|feedrate_observations|layer_marker_relationships"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel"
      to: "//packages/parity-fixtures:prusa_gcode_output_expected_gcode_semantic_summary"
      via: "compile_data on existing prusa_gcode_output_test"
      pattern: "prusa_gcode_output_expected_gcode_semantic_summary"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 55-2026-06-21T14-58-10
generated_at: 2026-06-21T15:15:20.151Z
---

<objective>
Add the pure typed Rust parser boundary for the Phase 54 semantic Prusa G-code expected summary.

Purpose: Complete the parser and rejection coverage portion of GSRUST-01 and GSRUST-03 while preserving the existing marker and structural APIs.
Output: Additive semantic parser types, function, public re-exports, Cargo tests, and Bazel compile-data wiring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-rust-semantic-g-code-summary-boundary/55-CONTEXT.md
@.planning/phases/55-rust-semantic-g-code-summary-boundary/55-RESEARCH.md
@.planning/phases/53-semantic-g-code-scope-contract/53-CONTEXT.md
@.planning/phases/54-semantic-g-code-fixture-corpus/54-CONTEXT.md
@.planning/phases/54-semantic-g-code-fixture-corpus/54-VERIFICATION.md
@.planning/phases/53-semantic-g-code-scope-contract/53-01-SUMMARY.md
@.planning/phases/54-semantic-g-code-fixture-corpus/54-01-SUMMARY.md
@.planning/phases/54-semantic-g-code-fixture-corpus/54-02-SUMMARY.md
@.planning/milestones/v1.13-phases/51-rust-structural-g-code-summary-boundary/51-01-SUMMARY.md
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
@packages/prusa-gcode-output-scope/gcode-output-scope.md
@packages/parity/status.tsv

<interfaces>
Existing structural parser pattern to mirror:

```rust
pub fn parse_prusa_gcode_output_structural_summary(
    input: &str,
) -> PrusaGcodeOutputStructuralParseResult;

pub struct PrusaGcodeOutputStructuralSummary {
    rows: Vec<PrusaGcodeOutputStructuralSummaryRow>,
    facts: PrusaGcodeOutputStructuralFacts,
}
```

Required semantic TSV header:

```text
source_ref	fixture_path	semantic_field	semantic_category	semantic_value	evidence_boundary
```

Required semantic field order:

```text
source_ref
fixture_id
fixture_path
command_class_counts
movement_class_counts
coordinate_bounds
extrusion_total
feedrate_observations
layer_marker_relationships
```

Accepted source identity:

```text
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
```

Checked-in fixture path:

```text
packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode
```
</interfaces>
</context>

<decision_coverage>
D-01 full: extend `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs`; do not create a new crate or workspace.
D-02 full: implement semantic parsing as `&str -> Result<PrusaGcodeOutputSemanticSummary, PrusaGcodeOutputSemanticParseError>` with no file, Git, network, process, generator, runtime, status, release, or sync behavior.
D-03 full: keep marker and structural parser function/type names stable and add semantic APIs.
D-04 full: parse the exact six-column semantic header.
D-05 full: accept exactly the nine Phase 53/54 semantic fields in order.
D-06 full: preserve the accepted Prusa source ref, `gcodewriter-set-speed.gcode`, and checked-in fixture path as typed/domain constants.
D-07 full: use typed semantic fields/categories and narrow string-backed semantic values.
D-08 full: add Cargo and Bazel fail-closed tests for valid rows, invalid headers, wrong columns, missing rows, duplicate rows, out-of-order rows, unsupported fields, unsupported claim text, wrong source refs, and wrong fixture identities.
D-09 full: new tests use one concern per test and Arrange/Act/Assert comments when setup is non-trivial.
D-10 full: reuse `prusa_gcode_output_test` and add semantic TSV compile data.
D-14 full: helper names remain semantic-summary/readiness scoped and do not claim byte parity, printability, runtime, support, seam, arc, GUI, release, upstream import, sync, or non-Prusa fork behavior.
D-15 full: evidence-boundary parsing exact-matches approved text and rejects broad claim text.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add semantic parser types, valid fixture coverage, and Bazel data</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs, packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs, packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel</files>
  <read_first>
    - .planning/phases/55-rust-semantic-g-code-summary-boundary/55-CONTEXT.md
    - .planning/phases/55-rust-semantic-g-code-summary-boundary/55-RESEARCH.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-CONTEXT.md
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-CONTEXT.md
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-VERIFICATION.md
    - .planning/milestones/v1.13-phases/51-rust-structural-g-code-summary-boundary/51-01-SUMMARY.md
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
    - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
    - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
    - packages/prusa-gcode-output-scope/gcode-output-scope.md
  </read_first>
  <behavior>
    - Test 1: `parse_prusa_gcode_output_semantic_summary(EXPECTED_GCODE_SEMANTIC_SUMMARY)` returns nine rows in the Phase 53 field order.
    - Test 2: the returned semantic facts expose source ref, fixture id/path, command class counts, movement class counts, coordinate bounds, extrusion total, feedrate observations, and layer/marker relationships exactly as the TSV records them.
    - Test 3: Bazel `prusa_gcode_output_test` can include the semantic TSV through `compile_data`.
  </behavior>
  <action>Start with tests, then implement. Update the existing `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs` test target rather than adding a new test file, per D-10.

In `tests/prusa_gcode_output.rs`:
- Add `const EXPECTED_GCODE_SEMANTIC_SUMMARY: &str = include_str!("../../../../parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv");`.
- Import the new public symbols: `PrusaGcodeOutputSemanticField`, `PrusaGcodeOutputSemanticParseError`, and `parse_prusa_gcode_output_semantic_summary`.
- Add `parses_expected_gcode_semantic_summary_rows_and_facts` with Arrange/Act/Assert comments. Assert exactly 9 rows, first field `PrusaGcodeOutputSemanticField::SourceRef`, last field `PrusaGcodeOutputSemanticField::LayerMarkerRelationships`, and facts values:
  - `source_ref.as_str() == "prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961"`
  - `fixture_id == "gcodewriter-set-speed.gcode"`
  - `fixture_path == "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode"`
  - `command_class_counts == "G1:4;feedrate_only:4"`
  - `movement_class_counts == "travel:0;extrusion:0;coordinate_motion:0;feedrate_only:4"`
  - `coordinate_bounds == "x:none;y:none;z:none"`
  - `extrusion_total == "e_axis_observed:false;extrusion_total:not_observed"`
  - `feedrate_observations == "F99999.123;F1;F203.2;F203.201"`
  - `layer_marker_relationships == "layer_markers:0;marker_relationships:none"`.

In `src/prusa_gcode_output.rs`, add semantic support beside the existing marker and structural sections:
- Add `pub const PRUSA_GCODE_OUTPUT_EXPECTED_SEMANTIC_SUMMARY_PATH: &str = "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv";`.
- Add `const SEMANTIC_EXPECTED_HEADER: &str = "source_ref\tfixture_path\tsemantic_field\tsemantic_category\tsemantic_value\tevidence_boundary";`, `const SEMANTIC_EXPECTED_COLUMN_COUNT: usize = 6;`, and `const SEMANTIC_COLUMNS: [&str; 6] = ["source_ref", "fixture_path", "semantic_field", "semantic_category", "semantic_value", "evidence_boundary"];`.
- Add exact evidence-boundary constants for all nine rows using the literal text from `expected-gcode-semantic-summary.tsv`.
- Add `PrusaGcodeOutputSemanticSummary`, `PrusaGcodeOutputSemanticSummaryRow`, `PrusaGcodeOutputSemanticFacts`, `PrusaGcodeOutputSemanticField`, `PrusaGcodeOutputSemanticCategory`, `PrusaGcodeOutputSemanticValue`, `PrusaGcodeOutputSemanticParseError`, and `PrusaGcodeOutputSemanticParseResult`.
- Use these exact field variants: `SourceRef`, `FixtureId`, `FixturePath`, `CommandClassCounts`, `MovementClassCounts`, `CoordinateBounds`, `ExtrusionTotal`, `FeedrateObservations`, `LayerMarkerRelationships`.
- Use these exact category variants: `SourceIdentity`, `FixtureIdentity`, `CommandClasses`, `MovementClasses`, `CoordinateBounds`, `ExtrusionTotal`, `FeedrateObservations`, `LayerMarkerRelationships`.
- Use narrow semantic values with typed source ref plus string-backed values, for example `PrusaGcodeOutputSemanticValue::SourceRef(VendorSourceRef)` and `PrusaGcodeOutputSemanticValue::Text(&'static str)`.
- Add `const EXPECTED_SEMANTIC_ROWS: [ExpectedGcodeOutputSemanticRow; 9]` in the same order as the TSV.
- Implement `parse_prusa_gcode_output_semantic_summary(input: &str) -> PrusaGcodeOutputSemanticParseResult` with the same control-flow shape as `parse_prusa_gcode_output_structural_summary`: exact header, column count, required values, source ref, fixture path, typed field/category, expected category, expected semantic value, exact evidence boundary, duplicate detection, expected row-key validation, row-order validation, missing-row validation, and facts projection.
- Add `rows(&self) -> &[PrusaGcodeOutputSemanticSummaryRow]` and `facts(&self) -> PrusaGcodeOutputSemanticFacts`.
- Do not add a semantic CLI mode, do not edit `src/bin/prusa_gcode_output_summary.rs`, and do not edit `packages/parity/status.tsv`.

In `src/lib.rs`, re-export the new semantic types and `parse_prusa_gcode_output_semantic_summary`.

In `BUILD.bazel`, add `"//packages/parity-fixtures:prusa_gcode_output_expected_gcode_semantic_summary"` to the existing `prusa_gcode_output_test` `compile_data` list. Do not add a separate semantic test target.</action>
  <acceptance_criteria>
    - `rg -n "PRUSA_GCODE_OUTPUT_EXPECTED_SEMANTIC_SUMMARY_PATH|SEMANTIC_EXPECTED_HEADER|EXPECTED_SEMANTIC_ROWS|PrusaGcodeOutputSemanticSummary|PrusaGcodeOutputSemanticFacts|PrusaGcodeOutputSemanticField|PrusaGcodeOutputSemanticCategory|PrusaGcodeOutputSemanticParseError|parse_prusa_gcode_output_semantic_summary" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` finds all symbols.
    - `rg -n "PrusaGcodeOutputSemanticSummary|PrusaGcodeOutputSemanticField|PrusaGcodeOutputSemanticParseError|parse_prusa_gcode_output_semantic_summary" packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs` finds semantic re-exports.
    - `rg -n "EXPECTED_GCODE_SEMANTIC_SUMMARY|parses_expected_gcode_semantic_summary_rows_and_facts|command_class_counts|feedrate_observations|layer_marker_relationships" packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs` finds valid fixture coverage.
    - `rg -n "prusa_gcode_output_expected_gcode_semantic_summary" packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` finds the compile_data alias under `prusa_gcode_output_test`.
    - `cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_gcode_output` exits 0.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test --test_output=errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "PRUSA_GCODE_OUTPUT_EXPECTED_SEMANTIC_SUMMARY_PATH|SEMANTIC_EXPECTED_HEADER|EXPECTED_SEMANTIC_ROWS|PrusaGcodeOutputSemanticSummary|PrusaGcodeOutputSemanticFacts|PrusaGcodeOutputSemanticField|PrusaGcodeOutputSemanticCategory|PrusaGcodeOutputSemanticParseError|parse_prusa_gcode_output_semantic_summary" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs</automated>
    <automated>rg -n "PrusaGcodeOutputSemanticSummary|PrusaGcodeOutputSemanticField|PrusaGcodeOutputSemanticParseError|parse_prusa_gcode_output_semantic_summary" packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs</automated>
    <automated>rg -n "EXPECTED_GCODE_SEMANTIC_SUMMARY|parses_expected_gcode_semantic_summary_rows_and_facts|command_class_counts|feedrate_observations|layer_marker_relationships" packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs</automated>
    <automated>rg -n "prusa_gcode_output_expected_gcode_semantic_summary" packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel</automated>
    <automated>cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_gcode_output</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test --test_output=errors</automated>
  </verify>
  <done>The semantic parser accepts the checked-in fixture, exposes typed rows and facts, is publicly re-exported, and Bazel can compile the test with the semantic TSV.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add semantic fail-closed rejection coverage</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs</files>
  <read_first>
    - .planning/phases/55-rust-semantic-g-code-summary-boundary/55-CONTEXT.md
    - .planning/phases/55-rust-semantic-g-code-summary-boundary/55-RESEARCH.md
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
    - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
    - packages/prusa-gcode-output-scope/gcode-output-scope.md
    - packages/parity/status.tsv
  </read_first>
  <behavior>
    - Test 1: invalid semantic header returns `PrusaGcodeOutputSemanticParseError::InvalidHeader`.
    - Test 2: wrong semantic column count returns `WrongColumnCount { expected: 6, actual: 5 }`.
    - Test 3: missing row returns `MissingRow` for the removed semantic field.
    - Test 4: duplicate row returns `DuplicateRow` for the duplicated semantic field.
    - Test 5: out-of-order rows return `UnexpectedRowOrder`.
    - Test 6: unsupported semantic field returns `UnsupportedSemanticField`.
    - Test 7: unsupported claim text in evidence boundary returns `UnexpectedEvidenceBoundary`.
    - Test 8: wrong source ref returns `UnexpectedSourceRef`.
    - Test 9: wrong fixture path and wrong fixture id return fixture-identity errors.
    - Test 10: public declarations still reject broad behavior names while allowing exact Phase 53-approved semantic `extrusion_total` field naming.
  </behavior>
  <action>Add focused semantic rejection tests to `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs`, then fill any missing parser error branches in `src/prusa_gcode_output.rs`.

In the test file:
- Add helper `expected_semantic_fields() -> [PrusaGcodeOutputSemanticField; 9]` returning the Phase 53 field order.
- Add helper `replace_semantic_cell(data_row_index: usize, column_index: usize, value: &str) -> String` mirroring `replace_structural_cell`.
- Add these tests with Arrange/Act/Assert comments:
  - `rejects_semantic_invalid_header`
  - `rejects_semantic_wrong_column_count`
  - `rejects_semantic_missing_row`
  - `rejects_semantic_duplicate_row`
  - `rejects_semantic_out_of_order_rows`
  - `rejects_unsupported_semantic_field`
  - `rejects_semantic_unsupported_boundary_claim`
  - `rejects_semantic_unexpected_source_ref`
  - `rejects_semantic_unexpected_fixture_path`
  - `rejects_semantic_unexpected_fixture_id`
- Use exact mutation values:
  - header mutation: replace first `source_ref` with `wrong_source_ref`
  - wrong columns: `only\tfive\tcolumns\tin\trow`
  - unsupported field: `toolpath_geometry`
  - unsupported claim text: `full generated-output parity verified`
  - wrong source ref: `bambustudio:v02.06.00.51@b506005bc4ee62124e24bf00e0f58656db3646a6`
  - wrong fixture path: `fixtures/unreviewed.gcode`
  - wrong fixture id: `unreviewed-speed.gcode`
- Update `public_declarations_do_not_claim_deferred_behavior` only as needed for approved semantic fields. Add a precise allowlist entry for the exact line `pub extrusion_total: &'static str,` if the semantic facts struct exposes that field. Do not broaden the allowlist to words or regexes.

In the parser file:
- Ensure errors include these variants exactly: `InvalidHeader`, `WrongColumnCount`, `EmptyRequiredValue`, `UnexpectedSourceRef`, `UnexpectedFixturePath`, `UnsupportedSemanticField`, `UnsupportedSemanticCategory`, `UnexpectedSemanticCategory`, `UnexpectedSemanticValue`, `UnexpectedEvidenceBoundary`, `DuplicateRow`, `UnexpectedRowOrder`, `MissingRow`, and `ExtraRow`.
- Ensure wrong fixture id is rejected as `UnexpectedSemanticValue { semantic_field: PrusaGcodeOutputSemanticField::FixtureId, .. }`.
- Ensure unsupported claim text is rejected by exact evidence-boundary matching, not by substring allowlists.
- Keep parser helpers private unless a public symbol is needed by tests or downstream plans.
- Do not add `unwrap()`, filesystem reads, environment reads, subprocess calls, network calls, Git calls, generator calls, status publication, binary mode changes, or public docs changes.</action>
  <acceptance_criteria>
    - `rg -n "rejects_semantic_invalid_header|rejects_semantic_wrong_column_count|rejects_semantic_missing_row|rejects_semantic_duplicate_row|rejects_semantic_out_of_order_rows|rejects_unsupported_semantic_field|rejects_semantic_unsupported_boundary_claim|rejects_semantic_unexpected_source_ref|rejects_semantic_unexpected_fixture_path|rejects_semantic_unexpected_fixture_id" packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs` finds all tests.
    - `rg -n "UnsupportedSemanticField|UnsupportedSemanticCategory|UnexpectedSemanticCategory|UnexpectedSemanticValue|UnexpectedEvidenceBoundary|UnexpectedRowOrder|MissingRow|ExtraRow" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` finds semantic error variants or uses.
    - `cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_gcode_output` exits 0.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test --test_output=errors` exits 0.
    - `test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/parity/BUILD.bazel packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs)"` passes.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "rejects_semantic_invalid_header|rejects_semantic_wrong_column_count|rejects_semantic_missing_row|rejects_semantic_duplicate_row|rejects_semantic_out_of_order_rows|rejects_unsupported_semantic_field|rejects_semantic_unsupported_boundary_claim|rejects_semantic_unexpected_source_ref|rejects_semantic_unexpected_fixture_path|rejects_semantic_unexpected_fixture_id" packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs</automated>
    <automated>rg -n "UnsupportedSemanticField|UnsupportedSemanticCategory|UnexpectedSemanticCategory|UnexpectedSemanticValue|UnexpectedEvidenceBoundary|UnexpectedRowOrder|MissingRow|ExtraRow" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs</automated>
    <automated>cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_gcode_output</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test --test_output=errors</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check</automated>
    <automated>git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel</automated>
    <automated>test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/parity/BUILD.bazel packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs)"</automated>
  </verify>
  <done>Cargo and Bazel prove the semantic parser accepts only the Phase 54 artifact shape and rejects malformed, drifted, or overclaiming semantic summaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-supplied TSV -> Rust parser | Untrusted or drifted semantic summary text enters pure Rust parsing. |
| semantic parser -> typed facts | Validated rows become facts consumed by later readiness/evidence code. |
| checked-in fixture -> Bazel test data | Bazel exposes fixture text to tests without runtime discovery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01-01 | Spoofing | `parse_prusa_gcode_output_semantic_summary` | mitigate | Reject any `source_ref`, `fixture_path`, or `fixture_id` not matching the accepted Prusa source and `gcodewriter-set-speed.gcode` constants. |
| T-55-01-02 | Tampering | semantic TSV rows | mitigate | Validate exact header, six columns, non-empty cells, closed field set, one row per field, exact order, exact values, and exact evidence-boundary text. |
| T-55-01-03 | Repudiation | semantic evidence boundary text | mitigate | Add tests proving broad claim text such as `full generated-output parity verified` is rejected with `UnexpectedEvidenceBoundary`. |
| T-55-01-04 | Information Disclosure | parser implementation | accept | Low risk because parser only consumes caller-supplied strings and has no file, environment, Git, network, process, or credential access. |
| T-55-01-05 | Denial of Service | parser allocation over input lines | accept | Low risk for checked-in tiny TSV artifacts; line/column validation fails closed and no unbounded recursion or external resource access is introduced. |
| T-55-01-06 | Elevation of Privilege | accidental side effects in parser | mitigate | Keep parser pure `&str -> Result<_, _>` and verify no binary mode, status file, public docs, process, network, Git, generator, release, or sync changes are present. |
</threat_model>

<verification>
Overall plan verification:

```bash
cargo +1.94.1 test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_gcode_output
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test --test_output=errors
bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check
git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/parity/BUILD.bazel packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs)"
```
</verification>

<success_criteria>
- `parse_prusa_gcode_output_semantic_summary` exists and is re-exported.
- The parser accepts `expected-gcode-semantic-summary.tsv` and exposes typed facts for all nine approved semantic fields.
- Cargo and Bazel parser tests reject invalid semantic headers, wrong column counts, missing rows, duplicate rows, out-of-order rows, unsupported semantic fields, broad claim text, wrong source refs, wrong fixture paths, and wrong fixture ids.
- The existing marker and structural parser APIs remain stable.
- No public status/docs publication, binary semantic mode, generator behavior, filesystem discovery, Git, network, process, release, sync, runtime, or non-Prusa behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/55-rust-semantic-g-code-summary-boundary/55-01-SUMMARY.md` with `requirements-completed` covering `GSRUST-01` and the GSRUST-03 coverage materially completed by this plan.
</output>
