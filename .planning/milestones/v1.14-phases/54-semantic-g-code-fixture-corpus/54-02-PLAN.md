---
phase: 54-semantic-g-code-fixture-corpus
plan: "02"
type: execute
wave: 2
depends_on:
  - 54-01-PLAN.md
files_modified:
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
autonomous: true
requirements:
  - GSFIX-03
  - GSFIX-01
  - GSFIX-02
must_haves:
  truths:
    - "Maintainer can run the existing Bazel fixture verifier and have semantic TSV schema, row order, row count, provenance, README text, and no-overclaiming checks enforced."
    - "Mutation tests fail closed for missing semantic rows, duplicate rows, out-of-order rows, unsupported fields, unsupported broad-behavior claims, provenance mismatch, and stale semantic documentation references."
    - "Verifier changes protect the Phase 54 sidecar and docs without adding Rust parsing, public status/docs publication, or public parity command changes."
  artifacts:
    - path: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      provides: "Fail-closed semantic fixture verifier"
      exports: ["verify_semantic_summary"]
    - path: "packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh"
      provides: "Semantic drift mutation coverage"
      contains: "test_missing_semantic_row_fails"
  key_links:
    - from: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv"
      via: "semantic_summary_file default and explicit argument"
      pattern: "expected-gcode-semantic-summary.tsv"
    - from: "packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh"
      to: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      via: "run_verifier temp checkout arguments"
      pattern: "semantic_file"
    - from: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md"
      via: "required semantic artifact and update-route text"
      pattern: "Semantic expected artifact"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 54-2026-06-21T12-41-13
generated_at: 2026-06-21T12:55:42Z
---

<objective>
Extend the existing Prusa G-code output fixture verifier and mutation harness so Phase 54 semantic fixture drift fails closed.

Purpose: Protect the new checked-in semantic sidecar and narrow docs before Phase 55 Rust semantic parsing or Phase 56 public semantic evidence consumes them.
Output: Updated Bash verifier and Bash mutation test under `packages/parity-fixtures`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-semantic-g-code-fixture-corpus/54-CONTEXT.md
@.planning/phases/54-semantic-g-code-fixture-corpus/54-RESEARCH.md
@.planning/phases/53-semantic-g-code-scope-contract/53-CONTEXT.md
@.planning/phases/53-semantic-g-code-scope-contract/53-VERIFICATION.md
@.planning/phases/54-semantic-g-code-fixture-corpus/54-01-SUMMARY.md
@.planning/milestones/v1.13-phases/50-structural-g-code-fixture-expansion/50-02-SUMMARY.md
@packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
@packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
@packages/parity-fixtures/BUILD.bazel
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
@packages/parity-fixtures/README.md
@packages/parity/status.tsv

<interfaces>
Existing verifier command to preserve:
```text
bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture
```

Existing mutation test command to preserve:

```text
bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test
```

Verifier script conventions to preserve:

```bash
#!/usr/bin/env bash
set -euo pipefail
error() { printf 'error: %s\n' "$*" >&2; exit 1; }
```

Required semantic TSV header:

```text
source_ref	fixture_path	semantic_field	semantic_category	semantic_value	evidence_boundary
```

</interfaces>
</context>

<decision_coverage>
D-05 full: verifier and mutation coverage reject missing, duplicate, unsupported, and out-of-order semantic rows.
D-06 full: verifier checks the exact narrow feedrate-only values and absence observations from the selected fixture.
D-07 full: verifier checks exact fixture-summary-only evidence boundaries and rejects broad semantic overclaims.
D-08 full: extend `packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh`; do not create a new verifier package.
D-09 full: verifier checks exact semantic header, row count, allowed field set, field counts, provenance alignment, exact rows/values, README text, and forbidden broad-claim text.
D-10 full: mutation suite covers missing rows, duplicate rows, out-of-order rows, unsupported fields, unsupported broad-behavior claims, provenance mismatch, and stale documentation references with Arrange/Act/Assert comments.
D-11 full: verifier consumes the semantic sidecar through the existing Bazel target and temp-checkout harness after Plan 54-01 wires the file into the bundle.
D-14 full: this plan must not edit Rust crates, `packages/parity/status.tsv`, public parity command behavior, `packages/parity/README.md`, or public `docs/port/*` files.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED semantic mutation coverage</name>
  <files>packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</files>
  <read_first>
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-CONTEXT.md
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-RESEARCH.md
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-01-SUMMARY.md
    - packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
    - packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
    - packages/parity-fixtures/README.md
  </read_first>
  <behavior>
    - Test 1: removing the `coordinate_bounds` semantic row fails and stderr mentions `missing semantic field` and `coordinate_bounds`.
    - Test 2: duplicating the `feedrate_observations` semantic row fails and stderr mentions `duplicate semantic field` and `feedrate_observations`.
    - Test 3: moving `feedrate_observations` before `extrusion_total` fails and stderr mentions `missing required row for feedrate_observations` or `semantic rows out of order`.
    - Test 4: appending unsupported semantic field `toolpath_geometry` fails and stderr mentions `unsupported semantic field` and `toolpath_geometry`.
    - Test 5: replacing a semantic evidence boundary with `verified Prusa G-code output parity` fails and stderr mentions `forbidden`.
    - Test 6: changing the first-column source ref on one semantic row fails and stderr mentions `provenance mismatch`.
    - Test 7: changing the `fixture_id` semantic value to `other-fixture.gcode` fails and stderr mentions `fixture_id`.
    - Test 8: removing the fixture README semantic artifact line fails and stderr mentions `Semantic expected artifact`.
    - Test 9: removing the package README checked-in-artifacts-only sentence fails and stderr mentions `Fixture verification checks checked-in artifacts only`.
  </behavior>
  <action>Update only `packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh`. Preserve `#!/usr/bin/env bash`, `set -euo pipefail`, early-return helper style, and existing mutation helpers.

In `write_valid_fixture_copy`, copy `expected-gcode-semantic-summary.tsv` from the source fixture directory into the temp fixture directory. During this RED task only, keep `run_verifier` on the existing explicit argument list so the current verifier ignores semantic sidecar drift; this proves the gap before GREEN implementation.

Add focused tests, each with explicit `# Arrange`, `# Act`, and `# Assert` comments:

- `test_missing_semantic_row_fails`
- `test_duplicate_semantic_row_fails`
- `test_out_of_order_semantic_row_fails`
- `test_unsupported_semantic_field_fails`
- `test_semantic_overclaim_fails`
- `test_semantic_provenance_mismatch_fails`
- `test_semantic_fixture_identity_mismatch_fails`
- `test_missing_semantic_readme_reference_fails`
- `test_missing_package_semantic_boundary_fails`

Use existing helpers where possible. If a row-reordering helper is needed, add a small portable `awk` helper that rewrites the temp file and does not use GNU-only `sed -i` or Bash associative arrays.

Add the new test functions to the explicit invocation list at the bottom. Capture RED evidence with a command that exits nonzero before verifier implementation and finds at least one new failure marker such as `missing semantic row fixture passed`, `duplicate semantic row fixture passed`, `out-of-order semantic row fixture passed`, `unsupported semantic field fixture passed`, or `semantic overclaim fixture passed`.</action>
<acceptance_criteria>
- `rg -n "test_missing_semantic_row_fails|test_duplicate_semantic_row_fails|test_out_of_order_semantic_row_fails|test_unsupported_semantic_field_fails|test_semantic_overclaim_fails|test_semantic_provenance_mismatch_fails|test_semantic_fixture_identity_mismatch_fails|test_missing_semantic_readme_reference_fails|test_missing_package_semantic_boundary_fails" packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` finds all nine tests.
- `rg -n "# Arrange|# Act|# Assert" packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` shows the new tests use Arrange, Act, Assert comments.
- `rg -n "expected-gcode-semantic-summary.tsv|coordinate_bounds|feedrate_observations|toolpath_geometry|verified Prusa G-code output parity|other-fixture.gcode|Semantic expected artifact|Fixture verification checks checked-in artifacts only" packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` finds all required mutation values.
- The RED command exits nonzero before verifier implementation and stderr contains at least one new semantic failure marker.
</acceptance_criteria>
<verify>
<automated>rg -n "test_missing_semantic_row_fails|test_duplicate_semantic_row_fails|test_out_of_order_semantic_row_fails|test_unsupported_semantic_field_fails|test_semantic_overclaim_fails|test_semantic_provenance_mismatch_fails|test_semantic_fixture_identity_mismatch_fails|test_missing_semantic_readme_reference_fails|test_missing_package_semantic_boundary_fails" packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>bash -c 'set +e; bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh >/tmp/phase54-semantic-red.out 2>/tmp/phase54-semantic-red.err; red_status="$?"; test "${red_status}" -ne 0; rg -n "missing semantic row fixture passed|duplicate semantic row fixture passed|out-of-order semantic row fixture passed|unsupported semantic field fixture passed|semantic overclaim fixture passed" /tmp/phase54-semantic-red.err'</automated>
</verify>
<done>Semantic mutation tests exist with Arrange/Act/Assert comments and demonstrate the current verifier ignores at least one semantic drift class before GREEN implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GREEN semantic verifier enforcement</name>
  <files>packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh, packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</files>
  <read_first>
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-CONTEXT.md
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-RESEARCH.md
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-01-SUMMARY.md
    - packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
    - packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
    - packages/parity-fixtures/BUILD.bazel
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
    - packages/parity-fixtures/README.md
    - packages/parity/status.tsv
  </read_first>
  <behavior>
    - Valid Phase 54 fixture corpus passes `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture`.
    - Every semantic mutation test from Task 1 fails closed with a specific diagnostic.
    - Existing marker, structural, status-row, parity-target, and forbidden-verifier-behavior mutation tests still pass.
    - The script remains Bash 3.2-compatible and does not introduce Git, network, generator, host-upload, slicer runtime, release, or sync behavior.
  </behavior>
  <action>Update `packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh` and then update `packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` to pass the semantic file explicitly.

In the verifier script:

- Preserve `#!/usr/bin/env bash`, `set -euo pipefail`, the existing `error` helper, and early-return helper style.
- In default mode, set `semantic_summary_file="${fixture_dir}/expected-gcode-semantic-summary.tsv"`.
- In explicit argument mode, change the accepted counts from `8 || 9` to `9 || 10`. The explicit argument order must be:
  1. fixture README
  1. fixture provenance
  1. expected G-code summary
  1. expected G-code structural summary
  1. expected G-code semantic summary
  1. G-code file
  1. parity status
  1. parity BUILD
  1. parity-fixtures README
  1. optional checkout root
- Update the usage string to include `expected-gcode-semantic-summary`.
- Add constants:

```bash
readonly SEMANTIC_SUMMARY_HEADER=$'source_ref\tfixture_path\tsemantic_field\tsemantic_category\tsemantic_value\tevidence_boundary'
readonly SEMANTIC_SUMMARY_ROW_COUNT="10"
readonly SEMANTIC_REQUIRED_FIELDS="source_ref fixture_id fixture_path command_class_counts movement_class_counts coordinate_bounds extrusion_total feedrate_observations layer_marker_relationships"
```

- Add `SEMANTIC_EXPECTED_ROWS` with the exact nine rows from Plan 54-01.
- Add semantic helpers mirroring the structural helpers: `require_semantic_column_count`, `require_semantic_allowed_fields`, `require_semantic_field_counts`, `require_semantic_provenance_alignment`, `require_semantic_value`, `require_semantic_exact_rows`, and `verify_semantic_summary`.
- `require_semantic_allowed_fields` must reject unsupported fields with `error: expected-gcode-semantic-summary.tsv: unsupported semantic field: <field>`.
- `require_semantic_field_counts` must diagnose missing and duplicate rows with `missing semantic field` and `duplicate semantic field`.
- `require_semantic_exact_rows` must make row order observable. Either compare the full body exactly against `SEMANTIC_EXPECTED_ROWS`, or add an explicit order check that reports `semantic rows out of order`.
- `verify_semantic_summary` must check exact header, six-column rows, allowed fields, field counts, provenance alignment, exact source/fixture values, exact rows/order, exact line count `10`, fixture README semantic artifact text, package README checked-in-artifacts-only text, and forbidden broad metadata/claim text.
- Extend `reject_overclaiming_text` to scan `${semantic_summary_file}` in addition to fixture README, marker summary, and structural summary. Reject at least these exact phrases across checked files: `verified Prusa G-code output parity`, `byte-for-byte G-code parity verified`, `full generated-output parity verified`, `toolpath correctness verified`, `printability verified`, `printer-runtime behavior verified`, `host upload verified`, `Bambu Studio support verified`, and `OrcaSlicer support verified`.
- Add `${semantic_summary_file}` to the required-file loop.
- Call `verify_semantic_summary` after `verify_structural_summary` and before `verify_readme_scope`.
- Keep `verify_status_published`, `verify_parity_target_published`, and `GCODE_OUTPUT_STATUS_ROW` structural wording unchanged. Do not require a semantic status row.

In the test script:

- Update `run_verifier` to pass the semantic file as argument 5 and shift later arguments accordingly.
- Keep all Task 1 tests and all existing tests.
- Ensure each added mutation test has Arrange/Act/Assert comments.

Do not edit Rust crates, `packages/parity/status.tsv`, `packages/parity/README.md`, public parity command behavior, or public `docs/port/*` files.</action>
<acceptance_criteria>
- `rg -n "semantic_summary_file|SEMANTIC_SUMMARY_HEADER|SEMANTIC_SUMMARY_ROW_COUNT=\"10\"|SEMANTIC_REQUIRED_FIELDS|SEMANTIC_EXPECTED_ROWS|verify_semantic_summary" packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh` finds the semantic verifier implementation.
- `rg -n "unsupported semantic field|missing semantic field|duplicate semantic field|semantic rows out of order|expected-gcode-semantic-summary.tsv" packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh` finds fail-closed diagnostics.
- `rg -n "expected-gcode-semantic-summary.tsv" packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` confirms the semantic file is copied and passed to the verifier.
- `bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` passes.
- `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture` prints `ok: Prusa G-code output fixture verification passed`.
- `bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test` passes.
- `test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/slic3r-rust)"` passes.
</acceptance_criteria>
<verify>
<automated>rg -n 'semantic_summary_file|SEMANTIC_SUMMARY_HEADER|SEMANTIC_SUMMARY_ROW_COUNT="10"|SEMANTIC_REQUIRED_FIELDS|SEMANTIC_EXPECTED_ROWS|verify_semantic_summary' packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh</automated>
<automated>bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
<automated>bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test</automated>
<automated>shfmt -l -d packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>shellcheck packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>git diff --check -- packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/slic3r-rust)"</automated>
</verify>
<done>The existing fixture verifier accepts and enforces the semantic sidecar, every semantic mutation fails closed, and no Phase 55/56 or public publication scope is introduced.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Semantic TSV -> Bash verifier | Static TSV rows are parsed as untrusted text and must match the closed Phase 53 contract exactly. |
| Temp mutation fixtures -> verifier behavior | Tests simulate accidental or malicious fixture drift without mutating checked-in source artifacts. |
| Fixture docs -> verifier | README text is treated as enforceable boundary language, not advisory prose. |
| Verifier -> public status/docs | The local verifier must protect Phase 54 artifacts without promoting public semantic evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-07 | Tampering | `expected-gcode-semantic-summary.tsv` | mitigate | Exact header, six-column, exact row count, closed field set, duplicate/missing field counts, provenance alignment, exact values, and exact row/order checks. |
| T-54-08 | Spoofing | Source and fixture identity rows | mitigate | Require every semantic row first column to equal the accepted source ref and second column to equal the checked-in fixture path; require exact `fixture_id` value. |
| T-54-09 | Repudiation | Semantic docs/update route | mitigate | Verifier requires fixture README semantic artifact text and package README checked-in-artifacts-only boundary text. |
| T-54-10 | Tampering | Unsupported broad claims | mitigate | Add semantic TSV to forbidden-claim scan and mutation coverage for overclaim text. |
| T-54-11 | Denial of Service | Bash verifier portability | mitigate | Preserve Bash 3.2-compatible helpers and use portable `awk -F '\t'`, `grep -Fq`, `grep -Fxq`, `wc`, `tr`, and `shasum` patterns. |
| T-54-12 | Elevation of Privilege | Verifier runtime behavior | accept | Verifier reads explicit local files only and self-scans for forbidden Git/network/generator/host-upload behavior terms; no credentials, external services, runtime execution, release, or sync surfaces are added. |
| T-54-13 | Information Disclosure | Static fixture metadata | accept | Static source refs and fixture paths are already public repository metadata; no secrets or private data are introduced. |
</threat_model>

<verification>
Run after both tasks:

<automated>bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
<automated>bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test</automated>
<automated>shfmt -l -d packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>shellcheck packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>git diff --check -- packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
<automated>test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/slic3r-rust)"</automated>
</verification>

<success_criteria>

- `verify_prusa_gcode_output_fixture.sh` validates the semantic sidecar by exact header, row count, allowed fields, duplicate/missing field counts, provenance alignment, exact rows/order, README references, and forbidden broad-claim text.
- `verify_prusa_gcode_output_fixture_test.sh` contains focused Arrange/Act/Assert mutation tests for missing rows, duplicate rows, out-of-order rows, unsupported fields, unsupported broad-behavior claims, provenance mismatch, fixture identity drift, and stale documentation references.
- Existing Bazel verifier and mutation test targets pass.
- No Rust parser, public parity command behavior, `packages/parity/status.tsv`, `packages/parity/README.md`, or public `docs/port/*` change is introduced.
  </success_criteria>

<output>
After completion, create `.planning/phases/54-semantic-g-code-fixture-corpus/54-02-SUMMARY.md`
</output>
