---
phase: 64-rust-wall-seam-evidence-boundary
plan: "02"
type: execute
wave: 2
depends_on:
  - "64-01"
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
  - packages/slic3r-rust/BUILD.bazel
autonomous: true
requirements:
  - SEAMRUST-02
  - SEAMRUST-03
requirements_addressed:
  - SEAMRUST-02
  - SEAMRUST-03
user_setup: []
must_haves:
  truths:
    - "Developer can inspect static wall-seam metadata and readiness values tracing the parser to source identity, source anchors, fixture corpus, expected summary, scope record, planned command, planned status token, and deferred generated-output surfaces."
    - "Developer can find `prusaslicer.wall-seam` in the static flavor registry as a PrusaSlicer shared-downstream FutureCandidate with `generated-outputs` dependency."
    - "Developer can run aggregate Rust verification that includes the wall-seam parser and registry tests."
    - "Developer can confirm public status/docs, public parity target, printer-runtime behavior, source import, Git/network/process APIs, credentials, release/sync behavior, and public docs mutation remain absent."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs"
      provides: "Static metadata and readiness helpers for the wall-seam boundary"
      contains: "prusa_wall_seam_readiness"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs"
      provides: "Public crate re-exports for metadata and readiness helpers"
      contains: "PrusaWallSeamReadiness"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      provides: "Static registry capability row for prusaslicer.wall-seam"
      contains: "prusaslicer.wall-seam"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs"
      provides: "Readiness, registry, no-overclaiming, and status-restraint coverage"
      contains: "prusa_wall_seam_readiness"
    - path: "packages/slic3r-rust/BUILD.bazel"
      provides: "Aggregate Rust verification target including wall-seam parser coverage"
      contains: "prusa_wall_seam_test"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs"
      via: "shared wall-seam constants"
      pattern: "PRUSA_WALL_SEAM_INVENTORY_ID|PRUSA_WALL_SEAM_SOURCE_REF|PRUSA_WALL_SEAM_SOURCE_PATH"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs"
      to: "packages/parity/status.tsv"
      via: "public-boundary shell checks and exact metadata assertions"
      pattern: "fork.prusaslicer.wall-seam"
    - from: "packages/slic3r-rust/BUILD.bazel"
      to: "//packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_test"
      via: "aggregate verify test_suite entry"
      pattern: "prusa_wall_seam_test"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 64-2026-06-30T22-34-45
generated_at: "2026-06-30T22:54:33.856Z"
---

<objective>
Add static wall-seam readiness metadata, registry visibility, aggregate Rust verification wiring, and public-boundary guards.

Purpose: Complete SEAMRUST-02 and finish SEAMRUST-03 coverage while keeping Phase 65 public executable evidence, status, and docs unavailable.
Output: Wall-seam metadata/readiness helpers, registry row, registry tests, aggregate Bazel verify entry, and boundary checks.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/64-rust-wall-seam-evidence-boundary/64-CONTEXT.md
@.planning/phases/64-rust-wall-seam-evidence-boundary/64-RESEARCH.md
@.planning/phases/64-rust-wall-seam-evidence-boundary/64-01-SUMMARY.md
@.planning/phases/62-wall-seam-scope-contract/62-01-SUMMARY.md
@.planning/phases/62-wall-seam-scope-contract/62-03-SUMMARY.md
@.planning/phases/63-wall-seam-fixture-corpus/63-02-SUMMARY.md
@packages/prusa-wall-seam-scope/wall-seam-scope.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/fixture-provenance.tsv
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
@packages/slic3r-rust/BUILD.bazel
@packages/parity/status.tsv
@packages/parity/BUILD.bazel

<interfaces>
Existing registry shape to extend:
```rust
pub struct FlavorCapability {
    pub flavor_id: FlavorId,
    pub capability_id: &'static str,
    pub feature_surface: &'static str,
    pub feature_category: &'static str,
    pub origin: FeatureOrigin,
    pub parity_dependencies: &'static [ParitySurface],
    pub checklist_status: ChecklistStatus,
    pub provenance: &'static [FlavorProvenance],
    pub caution_flags: &'static [&'static str],
    pub future_parity_notes: &'static str,
}
```

New metadata/readiness helpers to expose per D-09 and D-10:
```rust
pub const fn prusa_wall_seam_metadata() -> PrusaWallSeamMetadata;
pub const fn prusa_wall_seam_readiness() -> PrusaWallSeamReadiness;
```

Required planned strings are traceability metadata only:
```text
planned_public_command = //packages/parity:prusaslicer_wall_seam_parity
planned_status_token = fork.prusaslicer.wall-seam
generated_outputs_status = in progress
publication_boundary = Phase 64 parser/readiness only; Phase 65 owns public executable evidence and status/docs publication.
```
</interfaces>
</context>

<decision_coverage>
D-09 full: add static wall-seam readiness metadata tracing accepted source identity, source path, source anchors, fixture corpus path, fixture path, expected summary path, scope record path, parser boundary, planned Phase 65 command, planned status token, generated-output status restraint, publication boundary, and deferred generated-output surfaces.
D-10 full: add `prusaslicer.wall-seam` to the Prusa registry capability list as source-observed `FutureCandidate` metadata only; registry notes keep wall seam separate from existing semantic G-code and arc-fitting rows.
D-11 full: use `ChecklistStatus::FutureCandidate`, `FeatureOrigin::SharedDownstream`, `FlavorId::PrusaSlicer`, and `ParitySurface::generated_outputs()` for wall-seam metadata and registry wiring.
D-13 plan-set full: aggregate `//packages/slic3r-rust:verify` includes the wall-seam test after Plan 64-01 creates it.
D-14 full: add registry tests for capability, readiness metadata, generated-output dependency, planned command/status token, and no-overclaiming note text.
D-15 full: focused Cargo tests, focused Bazel tests, flavor registry tests, and aggregate package verify are all named as verification commands.
D-16 full: Phase 64 exposes only developer-facing parser/readiness APIs; no public Phase 65 parity command is created.
D-17 full: public status token remains planned metadata only; tests and checks enforce no public executable evidence claim before Phase 65.
D-18 full: `generated-outputs` remains `in progress`; existing `fork.prusaslicer.gcode-output` and `fork.prusaslicer.arc-fitting` meanings remain unchanged.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add wall-seam metadata and readiness helpers</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs, packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs`
    - `packages/prusa-wall-seam-scope/wall-seam-scope.md`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/fixture-provenance.tsv`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv`
    - `AGENTS.md`
    - `standards/core/architecture.md`
    - `standards/languages/rust.md`
  </read_first>
  <behavior>
    - Test 1: `prusa_wall_seam_metadata()` exposes exact inventory, vendor, flavor, origin, dependency, source, fixture, expected summary, scope record, and reserved status token values.
    - Test 2: `prusa_wall_seam_readiness()` exposes exact source paths, source anchors, fixture corpus/path, expected summary path, parser boundary, planned command, planned status token, generated-output status, publication boundary, and deferred surfaces.
    - Test 3: readiness helper names remain developer-facing and do not claim byte parity, verified parity, printability, runtime, support, GUI, release, sync, executable public evidence, arc-fitting behavior, or non-Prusa fork behavior.
  </behavior>
  <action>Add tests first in `packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs`, then implement metadata in `src/prusa_wall_seam.rs` and re-exports in `src/lib.rs`.

In `src/prusa_wall_seam.rs`, add `pub struct PrusaWallSeamMetadata` with fields `inventory_id`, `vendor_id`, `flavor_id`, `origin`, `parity_dependency`, `checklist_status`, `source_ref`, `source_path`, `fixture_corpus_path`, `fixture_path`, `expected_wall_seam_summary_path`, `scope_record_path`, and `reserved_future_status_token`. Implement `pub const fn prusa_wall_seam_metadata() -> PrusaWallSeamMetadata` returning exact values from Plan 64-01 constants and `FlavorId::PrusaSlicer`, `FeatureOrigin::SharedDownstream`, `ParitySurface::generated_outputs()`, `ChecklistStatus::FutureCandidate` per D-09 and D-11.

Add `pub struct PrusaWallSeamReadiness` with fields `inventory_id`, `source_ref`, `inventory_source_paths`, `source_anchors`, `fixture_corpus_path`, `fixture_path`, `expected_wall_seam_summary_path`, `scope_record_path`, `parser_boundary`, `planned_public_command`, `planned_status_token`, `generated_outputs_status`, `publication_boundary`, and `deferred_surfaces`. Implement `pub const fn prusa_wall_seam_readiness() -> PrusaWallSeamReadiness` with exact values: `inventory_source_paths = &["packages/fork-inventories/prusaslicer.tsv", "src/libslic3r/GCode/SeamAligned.cpp"]`, `source_anchors = &["SeamAligned.cpp#L16", "SeamAligned.cpp#L115-L148", "SeamAligned.cpp#L272-L313", "SeamAligned.cpp#L463-L525"]`, `parser_boundary = "slic3r_flavors::prusa_wall_seam::parse_prusa_wall_seam_summary"`, `planned_public_command = "//packages/parity:prusaslicer_wall_seam_parity"`, `planned_status_token = "fork.prusaslicer.wall-seam"`, `generated_outputs_status = "in progress"`, and `publication_boundary = "Phase 64 parser/readiness only; Phase 65 owns public executable evidence and status/docs publication."` per D-09, D-16, and D-17.

Use `deferred_surfaces` containing exactly these values: `byte-for-byte G-code parity`, `broad generated-output verification`, `full wall-seam algorithm equivalence`, `wall-seam geometry equivalence`, `seam visibility`, `printability`, `firmware behavior`, `printer-runtime behavior`, `GUI behavior`, `support generation`, `STEP import`, `full 3MF import/export`, `binary G-code`, `thumbnails`, `post-processing`, `host upload`, `network/device behavior`, `profile auto-update execution`, `fork release builds`, `Bambu Studio`, `OrcaSlicer`, `upstream source imports`, `release behavior`, `sync automation`, and `non-Prusa fork behavior`.

In `src/lib.rs`, re-export `PrusaWallSeamMetadata`, `PrusaWallSeamReadiness`, `prusa_wall_seam_metadata`, and `prusa_wall_seam_readiness`. Do not add a binary, public parity command, public docs, status row, filesystem discovery, Git/network/process APIs, source import, live generation, printer-runtime behavior, credentials, release behavior, sync behavior, or docs/status mutation per D-03 and D-16 through D-18.</action>
  <acceptance_criteria>
    - `rg -n "PrusaWallSeamMetadata|PrusaWallSeamReadiness|prusa_wall_seam_metadata|prusa_wall_seam_readiness" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs` finds all symbols.
    - `rg -n "Phase 64 parser/readiness only|//packages/parity:prusaslicer_wall_seam_parity|fork\\.prusaslicer\\.wall-seam|SeamAligned.cpp#L463-L525" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` finds exact readiness values.
    - `rg -n "byte-for-byte G-code parity|full wall-seam algorithm equivalence|non-Prusa fork behavior" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` finds deferred surfaces only in readiness/test assertions, not helper names.
    - `! rg -n "std::(fs|process|env|time)|Command::|SystemTime|UNIX_EPOCH|current_dir|read_to_string|spawn\\(|TcpStream|reqwest|ureq" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs`
  </acceptance_criteria>
  <verify>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test flavor_registry</automated>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_wall_seam</automated>
  </verify>
  <done>Developers can inspect exact static wall-seam metadata and readiness values through `slic3r_flavors` without side effects or publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add registry row, aggregate Rust verification, and public-boundary guards</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs, packages/slic3r-rust/BUILD.bazel</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs`
    - `packages/slic3r-rust/BUILD.bazel`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs`
    - `packages/parity/status.tsv`
    - `packages/parity/BUILD.bazel`
    - `packages/parity/README.md`
    - `docs/port/package-map.md`
    - `docs/port/parity-matrix.md`
    - `docs/port/migration-guidance.md`
    - `AGENTS.md`
    - `standards/core/verification.md`
  </read_first>
  <behavior>
    - Test 1: `maybe_capability("prusaslicer.wall-seam")` returns a PrusaSlicer shared-downstream FutureCandidate with `ParitySurface::generated_outputs()` dependency and source provenance from `SeamAligned.cpp`.
    - Test 2: shared-downstream and future-candidate registry filters include `prusaslicer.wall-seam` without removing `prusaslicer.gcode-output` or `prusaslicer.arc-fitting`.
    - Test 3: registry notes explicitly keep public executable evidence/status/docs and all deferred generated-output/runtime/fork surfaces out of Phase 64 claims.
    - Test 4: `//packages/slic3r-rust:verify` includes `//packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_test`.
    - Test 5: `packages/parity/status.tsv` still has zero `fork.prusaslicer.wall-seam` rows, and `//packages/parity:prusaslicer_wall_seam_parity` remains undeclared.
  </behavior>
  <action>Update `src/registry.rs` per D-10 and D-11. Import `PRUSA_WALL_SEAM_INVENTORY_ID`, `PRUSA_WALL_SEAM_SOURCE_PATH`, and `PRUSA_WALL_SEAM_SOURCE_REF` from `crate::prusa_wall_seam`. Add `static PRUSA_WALL_SEAM_PATHS: [&str; 1] = [PRUSA_WALL_SEAM_SOURCE_PATH];` and `static PRUSA_WALL_SEAM_PROVENANCE: [FlavorProvenance; 1]` with `inventory_id: PRUSA_WALL_SEAM_INVENTORY_ID`, `vendor_source: PRUSA_WALL_SEAM_SOURCE_REF`, `source_paths: &PRUSA_WALL_SEAM_PATHS`, and `ownership: FeatureOrigin::SharedDownstream`.

Change `static PRUSA_CAPABILITIES: [FlavorCapability; 4]` to `[FlavorCapability; 5]` and add a new capability row for `prusaslicer.wall-seam` after `prusaslicer.arc-fitting` and before `prusaslicer.profile-schema`. Do not change the existing `prusaslicer.gcode-output` row text or `prusaslicer.arc-fitting` row text except for tests that update expected capability lists. The row must use `flavor_id: FlavorId::PrusaSlicer`, `capability_id: "prusaslicer.wall-seam"`, `feature_surface: "wall-seam"`, `feature_category: "wall-seam"`, `origin: FeatureOrigin::SharedDownstream`, `parity_dependencies: &GENERATED_OUTPUTS_PARITY`, `checklist_status: ChecklistStatus::FutureCandidate`, `provenance: &PRUSA_WALL_SEAM_PROVENANCE`, `caution_flags: &[]`, and this exact `future_parity_notes` text: `Source-observed wall-seam planning row; Phase 64 parser/readiness metadata is developer-facing only, while public executable evidence and status/docs publication remain Phase 65-owned. The broad generated-outputs surface stays in progress; no byte-for-byte G-code parity, broad generated-output verification, full wall-seam algorithm equivalence, wall-seam geometry equivalence, seam visibility, printability, firmware behavior, printer-runtime behavior, GUI behavior, support generation, arc-fitting behavior, release behavior, sync behavior, upstream import, host upload, network/device behavior, Bambu Studio, OrcaSlicer, or non-Prusa fork behavior is claimed.`

Update `tests/flavor_registry.rs`: import `prusa_wall_seam_metadata`, `prusa_wall_seam_readiness`, and `PrusaWallSeamReadiness`; add `PRUSA_WALL_SEAM_READINESS_NOTES` with the exact registry note above; update `shared_downstream_filter_returns_source_observed_prusa_rows` to expect `["prusaslicer.project-file", "prusaslicer.gcode-output", "prusaslicer.arc-fitting", "prusaslicer.wall-seam"]`; add tests `prusa_wall_seam_registry_row_traces_to_source_and_generated_output_dependency`, `prusa_wall_seam_metadata_exposes_fixture_scope_expected_summary_and_reserved_status`, `prusa_wall_seam_readiness_exposes_pre_publication_boundary`, `future_candidate_filter_includes_prusa_wall_seam_boundary_row`, and `prusa_wall_seam_registry_notes_negate_deferred_surfaces`. Update the existing public-helper-name test to include `prusa_wall_seam_metadata`, `prusa_wall_seam_readiness`, `parse_prusa_wall_seam_summary`, and `prusa_wall_seam_summary_lines` while keeping metadata values limited to values that should not contain runtime/executable/release/support claim words.

Update `packages/slic3r-rust/BUILD.bazel` by adding `//packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_test` to the `test_suite(name = "verify", tests = [...])` per D-13 and D-15. Do not edit `packages/parity/status.tsv`, `packages/parity/BUILD.bazel`, `packages/parity/README.md`, or `docs/port/*` per D-16 through D-18.</action>
  <acceptance_criteria>
    - `rg -n "PRUSA_WALL_SEAM_PROVENANCE|prusaslicer\\.wall-seam|Phase 64 parser/readiness metadata is developer-facing only" packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` finds the registry row and exact note.
    - `rg -n "prusa_wall_seam_registry_row_traces_to_source_and_generated_output_dependency|prusa_wall_seam_readiness_exposes_pre_publication_boundary|prusa_wall_seam_registry_notes_negate_deferred_surfaces|future_candidate_filter_includes_prusa_wall_seam_boundary_row" packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` finds all registry tests.
    - `rg -n "prusa_wall_seam_test" packages/slic3r-rust/BUILD.bazel` finds the aggregate verify entry.
    - `awk -F '\t' '$1 == "fork.prusaslicer.wall-seam" { count++ } END { if ((count + 0) != 0) exit 1 }' packages/parity/status.tsv` exits 0.
    - `test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md packages/parity/BUILD.bazel docs/port)"`
    - `! bazel query //packages/parity:prusaslicer_wall_seam_parity`
  </acceptance_criteria>
  <verify>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test flavor_registry</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test //packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_test --test_output=errors</automated>
    <automated>bazel test //packages/slic3r-rust:verify --test_output=errors</automated>
  </verify>
  <done>The static registry exposes wall seam as a future candidate with generated-output dependency, aggregate Rust verification includes the wall-seam parser test, and public parity/status/docs surfaces remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static metadata -> developer interpretation | Metadata strings can overstate achieved evidence if planned Phase 65 values are not clearly marked as planned/pre-publication. |
| flavor registry -> public status semantics | Registry rows can imply readiness or verified parity if status/dependency wording is too broad. |
| aggregate verification -> release confidence | Missing aggregate wiring would let parser tests pass locally but disappear from repo-owned verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-02-01 | Spoofing | `prusa_wall_seam_metadata` and registry provenance | mitigate | Reuse exact source ref, inventory id, source path, fixture path, scope path, and `VendorSourceRef::prusa_slicer_version_2_9_5()` constants. |
| T-64-02-02 | Tampering | `registry.rs` capability row | mitigate | Assert origin, checklist status, generated-output dependency, provenance, and exact future-parity note in `flavor_registry.rs`. |
| T-64-02-03 | Repudiation | readiness and registry notes | mitigate | Exact tests must prove Phase 64 is parser/readiness only and Phase 65 owns public executable evidence/status/docs publication. |
| T-64-02-04 | Elevation of Privilege | Rust module and registry integration | mitigate | No filesystem discovery, Git, network, process spawning, environment, clock, source import, live generation, printer-runtime behavior, credentials, release/sync behavior, or public docs/status mutation is introduced. |
| T-64-02-05 | Denial of Service | aggregate verification | mitigate | Add `prusa_wall_seam_test` to `//packages/slic3r-rust:verify` so the repo-owned Rust verification surface exercises the new parser. |
| T-64-02-06 | Information Disclosure | metadata/readiness | accept | Metadata references checked-in public fixture paths and source identities only; no secrets, credentials, private data, network/device access, or runtime discovery are added. |
</threat_model>

<verification>
Plan-level checks after both tasks:

```bash
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test flavor_registry --test prusa_wall_seam
bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test //packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_test --test_output=errors
bazel test //packages/slic3r-rust:verify --test_output=errors
git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs packages/slic3r-rust/BUILD.bazel
test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md packages/parity/BUILD.bazel docs/port)"
awk -F '\t' '$1 == "fork.prusaslicer.wall-seam" { count++ } END { if ((count + 0) != 0) exit 1 }' packages/parity/status.tsv
! bazel query //packages/parity:prusaslicer_wall_seam_parity
```
</verification>

<success_criteria>
SEAMRUST-02 is complete: developers can inspect exact static readiness/registry metadata for the wall-seam boundary.
SEAMRUST-03 is complete with Plan 64-01: Cargo and Bazel coverage prove valid rows parse, invalid rows fail closed, optional internals use clear names where optionality exists, public helper names do not overclaim deferred behavior, and aggregate Rust verification includes the wall-seam parser tests.
</success_criteria>

<output>
After completion, create `.planning/phases/64-rust-wall-seam-evidence-boundary/64-02-SUMMARY.md` with `requirements-completed: [SEAMRUST-02, SEAMRUST-03]`.
</output>
