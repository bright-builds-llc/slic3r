---
phase: 64-rust-wall-seam-evidence-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs
  - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
autonomous: true
requirements:
  - SEAMRUST-01
  - SEAMRUST-03
requirements_addressed:
  - SEAMRUST-01
  - SEAMRUST-03
user_setup: []
must_haves:
  truths:
    - "Developer can parse caller-supplied Phase 63 wall-seam summary TSV text into typed Rust rows and facts."
    - "Developer can call a pure `prusa_wall_seam_summary_lines` helper over caller-supplied text without creating a binary, public parity target, status row, public docs publication, or runtime discovery."
    - "Cargo and Bazel tests accept the checked-in wall-seam summary and reject malformed or overclaiming wall-seam summaries."
    - "The parser module introduces no filesystem discovery, Git, network, process, environment, clock, source import, live generation, printer-runtime behavior, credentials, release, sync, or status/docs mutation."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs"
      provides: "Pure typed wall-seam summary parser, row model, facts projection, parse errors, and summary-line helper"
      contains: "parse_prusa_wall_seam_summary"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs"
      provides: "Public crate re-exports for developer-facing wall-seam parser types and helper"
      contains: "PrusaWallSeamSummary"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs"
      provides: "Cargo and Bazel valid/fail-closed parser coverage"
      contains: "EXPECTED_WALL_SEAM_SUMMARY"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel"
      provides: "Bazel source/test wiring and compile_data for the Phase 63 wall-seam TSV"
      contains: "prusa_wall_seam_test"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv"
      via: "include_str! plus Bazel compile_data"
      pattern: "expected-wall-seam-summary.tsv"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs"
      to: "packages/prusa-wall-seam-scope/wall-seam-scope.md"
      via: "closed Phase 62 wall-seam field order"
      pattern: "source_ref|inventory_source_paths|source_anchor|fixture_id|fixture_path|seam_transition_observations|layer_context_observations|travel_context_observations|coordinate_bounds|extrusion_observations|retraction_observations|evidence_boundary"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel"
      to: "//packages/parity-fixtures:prusa_wall_seam_expected_wall_seam_summary"
      via: "compile_data on prusa_wall_seam_test"
      pattern: "prusa_wall_seam_expected_wall_seam_summary"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 64-2026-06-30T22-34-45
generated_at: "2026-06-30T22:54:33.856Z"
---

<objective>
Add the pure typed Rust parser boundary for the Phase 63 Prusa wall-seam expected summary.

Purpose: Complete the parser and fail-closed coverage portion of SEAMRUST-01 and SEAMRUST-03 while keeping Phase 65 publication surfaces absent.
Output: New `prusa_wall_seam` module, developer-facing summary-line helper, public crate re-exports, Cargo tests, and Bazel test wiring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/64-rust-wall-seam-evidence-boundary/64-CONTEXT.md
@.planning/phases/64-rust-wall-seam-evidence-boundary/64-RESEARCH.md
@.planning/phases/62-wall-seam-scope-contract/62-01-SUMMARY.md
@.planning/phases/62-wall-seam-scope-contract/62-02-SUMMARY.md
@.planning/phases/62-wall-seam-scope-contract/62-03-SUMMARY.md
@.planning/phases/63-wall-seam-fixture-corpus/63-01-SUMMARY.md
@.planning/phases/63-wall-seam-fixture-corpus/63-02-SUMMARY.md
@packages/prusa-wall-seam-scope/wall-seam-scope.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/fixture-provenance.tsv
@packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs
@packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel

<interfaces>
Existing parser pattern to mirror from `prusa_arc_fitting.rs`:
```rust
pub fn parse_prusa_arc_fitting_summary(input: &str) -> PrusaArcFittingParseResult;
pub fn prusa_arc_fitting_summary_lines(
    input: &str,
) -> Result<Vec<String>, PrusaArcFittingParseError>;
```

New developer-facing Rust surface to create per D-01, D-02, and D-03:
```rust
pub fn parse_prusa_wall_seam_summary(input: &str) -> PrusaWallSeamParseResult;
pub fn prusa_wall_seam_summary_lines(
    input: &str,
) -> Result<Vec<String>, PrusaWallSeamParseError>;
```

Required TSV header per D-04:
```text
source_ref	fixture_path	wall_seam_field	wall_seam_category	wall_seam_value	evidence_boundary
```

Required wall-seam field order per D-05:
```text
source_ref
inventory_source_paths
source_anchor
fixture_id
fixture_path
seam_transition_observations
layer_context_observations
travel_context_observations
coordinate_bounds
extrusion_observations
retraction_observations
evidence_boundary
```

Accepted source and fixture values:
```text
source_ref = prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
source_path = src/libslic3r/GCode/SeamAligned.cpp
fixture_id = wall-seam-observations.gcode
fixture_path = packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/wall-seam-observations.gcode
expected_summary_path = packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv
```
</interfaces>
</context>

<decision_coverage>
D-01 full: create `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs`.
D-02 full: export `pub mod prusa_wall_seam;` and re-export parser, facts, errors, and helper names from `src/lib.rs` without deferred-claim wording in public helper names.
D-03 full: parser input is caller-supplied `&str`; no filesystem discovery, Git, network, process, generation, printer-runtime, release, or sync behavior is allowed.
D-04 full: parse the exact six-column `source_ref`, `fixture_path`, `wall_seam_field`, `wall_seam_category`, `wall_seam_value`, `evidence_boundary` TSV shape.
D-05 full: model the twelve approved wall-seam fields as a closed ordered enum.
D-06 full: expose typed summary row, facts, field, category, value, boundary, and precise parse-error models.
D-07 full: compare source refs through `VendorSourceRef::prusa_slicer_version_2_9_5()`.
D-08 full: optional or nullable internals are avoided; any necessary `Option` local uses `maybe_` naming.
D-12 full: add focused Cargo integration tests proving valid parsing, expected facts, fail-closed invalid rows, and public helper name restraint.
D-13 plan-set full: crate-local Bazel source/test wiring is created here; aggregate `//packages/slic3r-rust:verify` wiring is Plan 64-02.
D-15 plan-set full: focused Cargo and Bazel parser coverage is created here; registry and aggregate coverage are Plan 64-02.
D-16 full: add only library APIs and tests; do not add `//packages/parity:prusaslicer_wall_seam_parity`.
D-17 full: keep `fork.prusaslicer.wall-seam` planned metadata only and absent from public status.
D-18 full: leave `generated-outputs`, `fork.prusaslicer.gcode-output`, and `fork.prusaslicer.arc-fitting` public status wording untouched.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add wall-seam parser types, valid fixture coverage, summary lines, and Bazel data</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs, packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs, packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs` (confirm absent before creating)
    - `packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs` (confirm absent before creating)
    - `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_arc_fitting.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv`
    - `packages/prusa-wall-seam-scope/wall-seam-scope.md`
    - `AGENTS.md`
    - `standards/core/testing.md`
    - `standards/languages/rust.md`
  </read_first>
  <behavior>
    - Test 1: `parse_prusa_wall_seam_summary(EXPECTED_WALL_SEAM_SUMMARY)` returns twelve rows in the Phase 62 field order.
    - Test 2: returned facts expose exact source ref, inventory paths, source anchors, fixture identity/path, seam transition observations, layer context observations, travel context observations, coordinate bounds, extrusion observations, retraction observations, and evidence-boundary token.
    - Test 3: `prusa_wall_seam_summary_lines(EXPECTED_WALL_SEAM_SUMMARY)` returns stable developer-facing fact lines without public executable parity wording.
    - Test 4: Bazel `prusa_wall_seam_test` includes the Phase 63 TSV through `compile_data`.
  </behavior>
  <action>Start with tests, then implement. Create `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs` with `const EXPECTED_WALL_SEAM_SUMMARY: &str = include_str!("../../../../parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv");` and `const PRUSA_WALL_SEAM_SOURCE: &str = include_str!("../src/prusa_wall_seam.rs");`. Add Arrange/Act/Assert tests named `parses_expected_wall_seam_summary_rows_and_facts`, `summarizes_expected_wall_seam_summary_lines`, and `public_wall_seam_declarations_do_not_claim_deferred_behavior` per D-12.

Create `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs` per D-01 through D-08. Define `PRUSA_WALL_SEAM_INVENTORY_ID`, `PRUSA_WALL_SEAM_SOURCE_REF`, and `PRUSA_WALL_SEAM_SOURCE_PATH` as `pub(crate) const` so Plan 64-02 can reuse them from `registry.rs`. Use constants with these exact values: `PRUSA_WALL_SEAM_INVENTORY_ID = "prusaslicer.wall-seam"`, `PRUSA_WALL_SEAM_VENDOR_ID = "prusaslicer"`, `PRUSA_WALL_SEAM_SOURCE_REF = VendorSourceRef::prusa_slicer_version_2_9_5()`, `PRUSA_WALL_SEAM_SOURCE_PATH = "src/libslic3r/GCode/SeamAligned.cpp"`, `PRUSA_WALL_SEAM_FIXTURE_CORPUS_PATH = "packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam"`, `PRUSA_WALL_SEAM_FIXTURE_PATH = "packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/wall-seam-observations.gcode"`, `PRUSA_WALL_SEAM_EXPECTED_SUMMARY_PATH = "packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv"`, `PRUSA_WALL_SEAM_SCOPE_RECORD_PATH = "packages/prusa-wall-seam-scope/wall-seam-scope.md"`, and `PRUSA_WALL_SEAM_RESERVED_STATUS_TOKEN = "fork.prusaslicer.wall-seam"`.

Implement the closed parser with `WALL_SEAM_EXPECTED_HEADER = "source_ref\tfixture_path\twall_seam_field\twall_seam_category\twall_seam_value\tevidence_boundary"` and `WALL_SEAM_EXPECTED_COLUMN_COUNT = 6` per D-04. Add public types `PrusaWallSeamSummary`, `PrusaWallSeamSummaryRow`, `PrusaWallSeamFacts`, `PrusaWallSeamField`, `PrusaWallSeamCategory`, `PrusaWallSeamValue`, `PrusaWallSeamEvidenceBoundary`, `PrusaWallSeamParseError`, and `PrusaWallSeamParseResult`. Field enum variants must be exactly `SourceRef`, `InventorySourcePaths`, `SourceAnchor`, `FixtureId`, `FixturePath`, `SeamTransitionObservations`, `LayerContextObservations`, `TravelContextObservations`, `CoordinateBounds`, `ExtrusionObservations`, `RetractionObservations`, and `EvidenceBoundary`. Category enum variants must cover exactly `SourceIdentity`, `FixtureIdentity`, `SeamTransitionObservations`, `LayerContextObservations`, `TravelContextObservations`, `CoordinateBounds`, `ExtrusionObservations`, `RetractionObservations`, and `BoundaryText`.

Expected values must match the Phase 63 TSV exactly: `inventory_source_paths = "packages/fork-inventories/prusaslicer.tsv;src/libslic3r/GCode/SeamAligned.cpp"`, `source_anchor = "SeamAligned.cpp#L16;SeamAligned.cpp#L115-L148;SeamAligned.cpp#L272-L313;SeamAligned.cpp#L463-L525"`, `fixture_id = "wall-seam-observations.gcode"`, `seam_transition_observations = "seam_markers:seam_start,seam_resume;transition_count:2"`, `layer_context_observations = "layer_index:0;z_values:0.200"`, `travel_context_observations = "travel_moves:1;travel_from:12.500,8.000;travel_to:14.000,8.750"`, `coordinate_bounds = "x_min:12.500;x_max:15.250;y_min:8.000;y_max:9.500;z_min:0.200;z_max:0.200"`, `extrusion_observations = "e_values:0.12000,0.28000;e_axis_observed:true"`, `retraction_observations = "e_marker_values:0.28000,0.24000;retraction_marker_observed:true"`, and `evidence_boundary = "checked-in-wall-seam-summary-only"`.

Implement `parse_prusa_wall_seam_summary(input: &str) -> PrusaWallSeamParseResult` with exact header validation, exact column count, non-empty required values, exact source ref through `VendorSourceRef::prusa_slicer_version_2_9_5()` per D-07, exact fixture path, typed field/category parsing, expected category validation, exact row value validation, exact evidence-boundary validation, duplicate-row rejection, extra-row rejection, row-order validation, missing-row validation, and facts projection. Add `PrusaWallSeamSummary::rows()` and `PrusaWallSeamSummary::facts()`.

Implement `prusa_wall_seam_summary_lines(input: &str) -> Result<Vec<String>, PrusaWallSeamParseError>` as a pure helper over `parse_prusa_wall_seam_summary` per D-02 and D-03. It must return lines for `wall_seam_summary_path`, `wall_seam_row_count`, `source_ref`, `inventory_source_paths`, `source_anchor`, `fixture_id`, `fixture_path`, `seam_transition_observations`, `layer_context_observations`, `travel_context_observations`, `coordinate_bounds`, `extrusion_observations`, `retraction_observations`, and `evidence_boundary`. Do not add a Rust binary, public Bazel parity target, status row, public docs, filesystem reads, process calls, network calls, environment reads, clock reads, live generation, source imports, printer-runtime behavior, credentials, release work, sync work, or status/docs mutation per D-03, D-16, D-17, and D-18.

Update `src/lib.rs` with `pub mod prusa_wall_seam;` and re-export all new parser/facts/error/helper symbols needed by the test. Update `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` so the `slic3r_flavors` `rust_library` `srcs` includes `src/prusa_wall_seam.rs`. Add `rust_test(name = "prusa_wall_seam_test", srcs = ["tests/prusa_wall_seam.rs"], compile_data = ["src/prusa_wall_seam.rs", "//packages/parity-fixtures:prusa_wall_seam_expected_wall_seam_summary"], deps = [":slic3r_flavors", "//packages/slic3r-rust/crates/slic3r_contracts:slic3r_contracts"], edition = "2024")`, and include `:prusa_wall_seam_test` in both `rust_clippy` deps and `rustfmt_test` targets.</action>
  <acceptance_criteria>
    - `rg -n "pub mod prusa_wall_seam|PrusaWallSeamSummary|parse_prusa_wall_seam_summary|prusa_wall_seam_summary_lines" packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs` finds all symbols.
    - `rg -n "WALL_SEAM_EXPECTED_HEADER|EXPECTED_WALL_SEAM_ROWS|PrusaWallSeamField|PrusaWallSeamFacts|PrusaWallSeamParseError" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs` finds all parser contract symbols.
    - `rg -n "EXPECTED_WALL_SEAM_SUMMARY|parses_expected_wall_seam_summary_rows_and_facts|summarizes_expected_wall_seam_summary_lines|public_wall_seam_declarations_do_not_claim_deferred_behavior" packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs` finds valid fixture and helper coverage.
    - `rg -n "prusa_wall_seam_test|prusa_wall_seam_expected_wall_seam_summary|src/prusa_wall_seam.rs" packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` finds all Bazel wiring.
    - `! rg -n "std::(fs|process|env|time)|Command::|SystemTime|UNIX_EPOCH|current_dir|read_to_string|spawn\\(|TcpStream|reqwest|ureq" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs`
  </acceptance_criteria>
  <verify>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_wall_seam</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_test --test_output=errors</automated>
  </verify>
  <done>The new Rust module parses the checked-in wall-seam summary into typed rows/facts, exposes a pure summary-line helper, re-exports the developer-facing API, and has Cargo/Bazel valid-fixture coverage without adding public executable parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fail-closed wall-seam parser rejection coverage</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv`
    - `packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_arc_fitting.rs`
    - `standards/core/testing.md`
    - `standards/languages/rust.md`
  </read_first>
  <behavior>
    - Test 1: invalid header returns `PrusaWallSeamParseError::InvalidHeader`.
    - Test 2: wrong column count returns `WrongColumnCount { expected: 6, actual: 5 }`.
    - Test 3: missing row returns `MissingRow`.
    - Test 4: duplicate row returns `DuplicateRow`.
    - Test 5: out-of-order rows return `UnexpectedRowOrder`.
    - Test 6: unsupported wall-seam field returns `UnsupportedWallSeamField`.
    - Test 7: unsupported category returns `UnsupportedWallSeamCategory`.
    - Test 8: unsupported broad claim text returns `UnexpectedEvidenceBoundary`.
    - Test 9: wrong source ref returns `UnexpectedSourceRef`.
    - Test 10: wrong fixture path returns `UnexpectedFixturePath`.
    - Test 11: wrong fixture identity or wall-seam value returns `UnexpectedWallSeamValue`.
  </behavior>
  <action>Add focused rejection tests to `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs`, then fill any missing parse-error branches in `src/prusa_wall_seam.rs`. Use one concern per test and Arrange/Act/Assert comments per the Bright Builds testing standard. Add helpers `expected_wall_seam_fields() -> [PrusaWallSeamField; 12]`, `replace_wall_seam_cell(data_row_index: usize, column_index: usize, replacement: &str) -> String`, and `joined_word(prefix: &str, suffix: &str) -> String` when needed by no-overclaim scans.

Tests must cover these concrete mutation names per D-06 and D-12: `rejects_wall_seam_invalid_header`, `rejects_wall_seam_wrong_column_count`, `rejects_wall_seam_missing_row`, `rejects_wall_seam_duplicate_row`, `rejects_wall_seam_out_of_order_rows`, `rejects_unsupported_wall_seam_field`, `rejects_unsupported_wall_seam_category`, `rejects_wall_seam_unsupported_claim_text`, `rejects_wall_seam_wrong_source_ref`, `rejects_wall_seam_wrong_fixture_path`, and `rejects_wall_seam_wrong_value`. For unsupported claim text, mutate the evidence-boundary column to `This proves byte-for-byte G-code parity and printer-runtime behavior.` and assert `UnexpectedEvidenceBoundary`. For wrong values, mutate the `fixture_id` row value to `other.gcode` and assert `UnexpectedWallSeamValue { wall_seam_field: PrusaWallSeamField::FixtureId, .. }`.

Parser error variants must name the failing line number and failing field where practical: `WrongColumnCount { line_number, expected, actual }`, `EmptyRequiredValue { line_number, column }`, `UnexpectedSourceRef { line_number, value }`, `UnexpectedFixturePath { line_number, value }`, `UnsupportedWallSeamField { line_number, value }`, `UnsupportedWallSeamCategory { line_number, value }`, `UnexpectedWallSeamCategory { line_number, wall_seam_field, value }`, `UnexpectedWallSeamValue { line_number, wall_seam_field, value }`, `UnexpectedEvidenceBoundary { line_number, wall_seam_field, value }`, `DuplicateRow { line_number, wall_seam_field }`, `UnexpectedRowOrder { line_number, expected_wall_seam_field, actual_wall_seam_field }`, `MissingRow { wall_seam_field }`, and `ExtraRow { line_number, wall_seam_field }`.

Keep D-03 and the security boundary intact: do not add imports or calls for `std::fs`, `std::process`, `std::env`, `std::time`, current directory discovery, path discovery, Git, network clients, source imports, generation, printer-runtime behavior, credentials, release work, sync work, or status/docs mutation.</action>
  <acceptance_criteria>
    - `rg -n "rejects_wall_seam_invalid_header|rejects_wall_seam_wrong_column_count|rejects_wall_seam_missing_row|rejects_wall_seam_duplicate_row|rejects_wall_seam_out_of_order_rows|rejects_unsupported_wall_seam_field|rejects_unsupported_wall_seam_category|rejects_wall_seam_unsupported_claim_text|rejects_wall_seam_wrong_source_ref|rejects_wall_seam_wrong_fixture_path|rejects_wall_seam_wrong_value" packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs` finds all eleven tests.
    - `rg -n "UnexpectedEvidenceBoundary|UnexpectedWallSeamValue|UnexpectedRowOrder|MissingRow|DuplicateRow|UnsupportedWallSeamField|UnsupportedWallSeamCategory" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs` finds fail-closed error variants and uses.
    - `! rg -n "std::(fs|process|env|time)|Command::|SystemTime|UNIX_EPOCH|current_dir|read_to_string|spawn\\(|TcpStream|reqwest|ureq" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs`
  </acceptance_criteria>
  <verify>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_flavors --test prusa_wall_seam</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_test --test_output=errors</automated>
  </verify>
  <done>Malformed wall-seam summaries fail closed for header, column, row, field, category, source, fixture, value, and overclaim drift while the module remains pure data-in/data-out Rust.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-supplied TSV string -> Rust parser | Untrusted text crosses into typed Rust domain values. |
| checked-in evidence values -> facts model | Reviewed fixture facts become developer-facing facts only after exact validation. |
| crate helper -> downstream Phase 65 consumer | Summary lines may later be consumed by a public command, so helper names/output must not overclaim executable parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01-01 | Spoofing | `parse_prusa_wall_seam_summary` | mitigate | Reject any `source_ref` other than `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961` and any fixture path other than the Phase 63 checked-in fixture path. |
| T-64-01-02 | Tampering | row parser and facts projection | mitigate | Enforce exact header, column count, non-empty values, closed enum fields/categories, exact row order, duplicate rejection, missing-row rejection, and exact expected values before constructing `PrusaWallSeamFacts`. |
| T-64-01-03 | Repudiation | summary-line helper and public declarations | mitigate | Add tests scanning helper/public declaration names and summary lines for deferred-behavior claim words; keep output as checked-in summary facts only. |
| T-64-01-04 | Elevation of Privilege | `src/prusa_wall_seam.rs` | mitigate | The source must not import or call filesystem discovery, Git, network, process spawning, environment, clock, source import, live generation, printer-runtime behavior, credentials, release, sync, or status/docs mutation APIs. |
| T-64-01-05 | Information Disclosure | parser module | accept | No secrets, credentials, private data, network/device access, or runtime discovery are introduced; the module consumes caller-supplied checked-in text only. |
</threat_model>

<verification>
Plan-level checks after both tasks:

```bash
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --workspace --all-features
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_test --test_output=errors
bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check
bazel build //packages/slic3r-rust/crates/slic3r_flavors:clippy
git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_wall_seam.rs packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md packages/parity/BUILD.bazel docs/port)"
! bazel query //packages/parity:prusaslicer_wall_seam_parity
```
</verification>

<success_criteria>
SEAMRUST-01 is materially complete for parser behavior: the wall-seam summary parser is pure, caller-supplied, typed, exact, and side-effect-free.
SEAMRUST-03 parser coverage is established here: focused Cargo/Bazel parser tests prove valid rows parse, invalid rows fail closed, and public helper names avoid deferred behavior claims. Plan 64-02 completes metadata, registry, and aggregate verification coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/64-rust-wall-seam-evidence-boundary/64-01-SUMMARY.md` with `requirements-completed: [SEAMRUST-01, SEAMRUST-03]`.
</output>
