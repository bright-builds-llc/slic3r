---
phase: 28-windows-packaged-launcher-slice
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/launcher/BUILD.bazel
  - packages/launcher/package/win/build_packaged_launcher.sh
  - packages/launcher/package/win/packaged_slice.txt
  - packages/launcher/package/win/test_packaged_launcher.sh
autonomous: true
requirements:
  - WPKG-01
  - WPKG-02
  - WPKG-03
must_haves:
  truths:
    - "Maintainer can build `.planning/.tmp/windows-packaged-launcher/Slic3r-windows` through `//packages/launcher:windows_packaged_launcher_tree`."
    - "Maintainer can smoke `Slic3r-windows/Slic3r-console.exe` through `//packages/launcher:windows_packaged_launcher_smoke`."
    - "The Windows package tree contains only direct Rust console startup handoff and scope metadata, not a Linux/macOS shell shim or installer surface."
  artifacts:
    - path: "packages/launcher/package/win/build_packaged_launcher.sh"
      provides: "Bazel-facing Windows package tree builder"
      contains: "Slic3r-console.exe"
    - path: "packages/launcher/package/win/packaged_slice.txt"
      provides: "In-artifact scope note"
      contains: "scoped Windows packaging-visible launcher slice"
    - path: "packages/launcher/package/win/test_packaged_launcher.sh"
      provides: "Bazel smoke coverage for layout and startup behavior"
      contains: "Slic3r-windows/Slic3r-console.exe"
    - path: "packages/launcher/BUILD.bazel"
      provides: "Bazel build and smoke targets"
      contains: "windows_packaged_launcher_smoke"
  key_links:
    - from: "packages/launcher/BUILD.bazel"
      to: "//packages/launcher:windows_slic3r"
      via: "sh_binary data and args"
      pattern: "windows_packaged_launcher_tree[\\s\\S]*:windows_slic3r"
    - from: "packages/launcher/package/win/test_packaged_launcher.sh"
      to: "packages/launcher/package/win/build_packaged_launcher.sh"
      via: "smoke test invokes builder in a temp root"
      pattern: "\\$\\{build_package\\}.*\\$\\{windows_launcher\\}"
    - from: "packages/launcher/package/win/build_packaged_launcher.sh"
      to: ".planning/.tmp/windows-packaged-launcher/Slic3r-windows/Slic3r-console.exe"
      via: "copy direct Rust runtime executable"
      pattern: "Slic3r-console\\.exe"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-05-23T02-35-56
generated_at: 2026-05-23T02:47:07Z
---

<objective>
Create the scoped Windows package-shaped launcher tree and Bazel smoke target.

Purpose: Satisfy WPKG-01 and WPKG-02 with a maintainer-runnable artifact that
uses the existing Rust Windows runtime directly, while satisfying WPKG-03 by
keeping startup logic thin and explicitly non-installer-shaped.

Output: `windows_packaged_launcher_tree`, `windows_packaged_launcher_smoke`, a
Windows package builder script, a Windows package smoke script, and scoped
artifact notes.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-windows-packaged-launcher-slice/28-CONTEXT.md
@.planning/phases/28-windows-packaged-launcher-slice/28-RESEARCH.md
@.planning/phases/27-linux-packaged-launcher-slice/27-01-SUMMARY.md
@packages/launcher/BUILD.bazel
@packages/launcher/package/linux/build_packaged_launcher.sh
@packages/launcher/package/linux/test_packaged_launcher.sh
@packages/launcher/package/linux/packaged_slice.txt
@packages/launcher/package/win/test_windows_launcher.sh
@packages/slic3r-rust/crates/slic3r_cli/src/bin/slic3r_windows_runtime.rs
@packages/legacy-slic3r/package/win/package_win32.ps1

<interfaces>
Existing Bazel labels to use exactly:
```python
alias(
    name = "windows_slic3r",
    actual = "//packages/slic3r-rust/crates/slic3r_cli:slic3r_windows_runtime",
)
```

New Bazel labels this plan must create:
```text
//packages/launcher:windows_packaged_launcher_tree
//packages/launcher:windows_packaged_launcher_smoke
```

Required package tree shape:
```text
.planning/.tmp/windows-packaged-launcher/
└── Slic3r-windows/
    ├── Slic3r-console.exe
    └── share/slic3r/packaged-slice.txt
```
</interfaces>
</context>

<tasks>
<task type="auto" id="1">
  <name>Task 1: Add Windows packaged tree builder and scope note</name>
  <read_first>
    <file>packages/launcher/package/linux/build_packaged_launcher.sh</file>
    <file>packages/launcher/package/linux/packaged_slice.txt</file>
    <file>packages/legacy-slic3r/package/win/package_win32.ps1</file>
    <file>.planning/phases/28-windows-packaged-launcher-slice/28-CONTEXT.md</file>
    <file>.planning/phases/28-windows-packaged-launcher-slice/28-RESEARCH.md</file>
  </read_first>
  <files>
    <file>packages/launcher/package/win/build_packaged_launcher.sh</file>
    <file>packages/launcher/package/win/packaged_slice.txt</file>
  </files>
  <action>
    Create `packages/launcher/package/win/build_packaged_launcher.sh` with
    `#!/usr/bin/env bash` and `set -euo pipefail`. It must accept
    `windows_launcher`, `slice_notes`, and optional `output_root`, defaulting
    `output_root` to `${repo_root}/.planning/.tmp/windows-packaged-launcher`.
    Reuse the Linux builder's `resolve_input` pattern, falling back to
    `${repo_root}/bazel-bin/packages/slic3r-rust/crates/slic3r_cli/slic3r_windows_runtime`
    when the resolved launcher is not executable. Materialize
    `${output_root}/Slic3r-windows`, copy the launcher to
    `${artifact_root}/Slic3r-console.exe`, `chmod +x` that file, copy
    `slice_notes` to `${artifact_root}/share/slic3r/packaged-slice.txt`, and
    print `Packaged Windows launcher ready: ${artifact_root}`. Do not parse CLI
    flags, do not invoke PowerShell, and do not add a Linux/macOS shell startup
    shim.

    Create `packages/launcher/package/win/packaged_slice.txt` with the exact
    phrase `scoped Windows packaging-visible launcher slice`. It must list
    `Slic3r-console.exe` as the direct Rust console runtime executable and
    explicitly defer MSI, zip release archives, signing, release-channel
    automation, `Slic3r.exe` GUI packaging behavior, broad bundled
    DLL/dependency layout, and shared cross-platform packaged parity evidence.
  </action>
  <verify>
    <automated>bash -n packages/launcher/package/win/build_packaged_launcher.sh</automated>
    <automated>shfmt -d packages/launcher/package/win/build_packaged_launcher.sh</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`packages/launcher/package/win/build_packaged_launcher.sh` contains `set -euo pipefail`.</criterion>
    <criterion>`packages/launcher/package/win/build_packaged_launcher.sh` contains `Slic3r-windows`.</criterion>
    <criterion>`packages/launcher/package/win/build_packaged_launcher.sh` contains `Slic3r-console.exe`.</criterion>
    <criterion>`packages/launcher/package/win/build_packaged_launcher.sh` contains `Packaged Windows launcher ready:`.</criterion>
    <criterion>`packages/launcher/package/win/packaged_slice.txt` contains `scoped Windows packaging-visible launcher slice`.</criterion>
    <criterion>`packages/launcher/package/win/packaged_slice.txt` contains `MSI` and `release-channel automation`.</criterion>
  </acceptance_criteria>
  <done>The builder produces the exact Windows package tree shape and the scope note records supported and deferred surfaces without business logic.</done>
</task>

<task type="auto" id="2">
  <name>Task 2: Expose Windows packaged build and smoke targets in Bazel</name>
  <read_first>
    <file>packages/launcher/BUILD.bazel</file>
    <file>packages/launcher/package/linux/build_packaged_launcher.sh</file>
    <file>packages/launcher/package/linux/test_packaged_launcher.sh</file>
    <file>packages/launcher/package/win/test_windows_launcher.sh</file>
    <file>.planning/phases/28-windows-packaged-launcher-slice/28-CONTEXT.md</file>
  </read_first>
  <files>
    <file>packages/launcher/BUILD.bazel</file>
  </files>
  <action>
    In `packages/launcher/BUILD.bazel`, add
    `package/win/build_packaged_launcher.sh`, `package/win/packaged_slice.txt`,
    and `package/win/test_packaged_launcher.sh` to `exports_files` and the
    `package_boundary` filegroup. Add a `sh_binary` named
    `windows_packaged_launcher_tree` with `srcs =
    ["package/win/build_packaged_launcher.sh"]`, `data = [":windows_slic3r",
    "package/win/packaged_slice.txt"]`, and args
    `$(location :windows_slic3r)` plus
    `$(location package/win/packaged_slice.txt)`. Add a `sh_test` named
    `windows_packaged_launcher_smoke` with `srcs =
    ["package/win/test_packaged_launcher.sh"]`, `data = [":windows_slic3r",
    "package/win/build_packaged_launcher.sh",
    "package/win/packaged_slice.txt"]`, and args for the builder, runtime, and
    notes in that order. Do not add `package/linux/startup_script.sh`,
    `package/osx/startup_script.sh`, or a PowerShell target to either Windows
    packaged target.
  </action>
  <verify>
    <automated>bazel query //packages/launcher:windows_packaged_launcher_tree</automated>
    <automated>bazel query //packages/launcher:windows_packaged_launcher_smoke</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`packages/launcher/BUILD.bazel` contains `name = "windows_packaged_launcher_tree"`.</criterion>
    <criterion>`packages/launcher/BUILD.bazel` contains `name = "windows_packaged_launcher_smoke"`.</criterion>
    <criterion>`packages/launcher/BUILD.bazel` contains `package/win/build_packaged_launcher.sh`.</criterion>
    <criterion>`packages/launcher/BUILD.bazel` contains `package/win/packaged_slice.txt`.</criterion>
    <criterion>The `windows_packaged_launcher_tree` target data contains `:windows_slic3r`.</criterion>
  </acceptance_criteria>
  <done>Bazel exposes maintainer-runnable build and smoke labels for the Windows package tree.</done>
</task>

<task type="auto" id="3">
  <name>Task 3: Add Windows packaged startup smoke coverage</name>
  <read_first>
    <file>packages/launcher/package/win/test_windows_launcher.sh</file>
    <file>packages/launcher/package/linux/test_packaged_launcher.sh</file>
    <file>packages/launcher/package/win/build_packaged_launcher.sh</file>
    <file>packages/launcher/package/win/packaged_slice.txt</file>
    <file>.planning/phases/28-windows-packaged-launcher-slice/28-CONTEXT.md</file>
  </read_first>
  <files>
    <file>packages/launcher/package/win/test_packaged_launcher.sh</file>
  </files>
  <action>
    Create `packages/launcher/package/win/test_packaged_launcher.sh` with
    `#!/usr/bin/env bash` and `set -euo pipefail`. It must accept
    `build_package`, `windows_launcher`, and `slice_notes`, resolve inputs with
    the existing Windows runtime smoke pattern, build into
    `mktemp -d /tmp/slic3r-windows-packaged-launcher.XXXXXX`, and execute
    `${temp_root}/Slic3r-windows/Slic3r-console.exe` directly. Assert that
    `${temp_root}/Slic3r-windows/Slic3r-console.exe` is executable and
    `${temp_root}/Slic3r-windows/share/slic3r/packaged-slice.txt` exists.
    Through `Slic3r-console.exe`, run and assert `--version`, `--help`,
    `--save`, `--datadir --load`, `--export-gcode`, `--info`, `--repair`, and
    `--split`, matching the representative flows in
    `package/win/test_windows_launcher.sh` and
    `package/linux/test_packaged_launcher.sh`. Verify the notes contain
    `scoped Windows packaging-visible launcher slice`. The smoke script must
    not invoke `package/linux/startup_script.sh`, `package/osx/startup_script.sh`,
    `bin/slic3r`, or any startup `.sh` inside the Windows artifact.
  </action>
  <verify>
    <automated>bash -n packages/launcher/package/win/test_packaged_launcher.sh</automated>
    <automated>shfmt -d packages/launcher/package/win/test_packaged_launcher.sh</automated>
    <automated>bazel test //packages/launcher:windows_packaged_launcher_smoke</automated>
    <automated>bazel run //packages/launcher:windows_packaged_launcher_tree</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`packages/launcher/package/win/test_packaged_launcher.sh` contains `Slic3r-windows/Slic3r-console.exe`.</criterion>
    <criterion>`packages/launcher/package/win/test_packaged_launcher.sh` contains `--export-gcode`.</criterion>
    <criterion>`packages/launcher/package/win/test_packaged_launcher.sh` contains `--repair`.</criterion>
    <criterion>`packages/launcher/package/win/test_packaged_launcher.sh` contains `scoped Windows packaging-visible launcher slice`.</criterion>
    <criterion>`bazel test //packages/launcher:windows_packaged_launcher_smoke` exits 0.</criterion>
    <criterion>`bazel run //packages/launcher:windows_packaged_launcher_tree` creates `.planning/.tmp/windows-packaged-launcher/Slic3r-windows/Slic3r-console.exe`.</criterion>
  </acceptance_criteria>
  <done>The smoke target proves the Windows packaged layout and direct executable startup for the verified help/version/config/export/transform slice.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Bazel target data -> Bash builder args | Local file paths for the Rust runtime and scope note cross into shell script execution. |
| Bash builder -> `.planning/.tmp/windows-packaged-launcher` | Script writes an inspectable package tree under a repo-local temporary path. |
| Smoke script -> packaged executable | The smoke test executes the copied `Slic3r-console.exe` directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-28-01-01 | Spoofing | `build_packaged_launcher.sh` launcher input | mitigate | Source the default executable from Bazel `data = [":windows_slic3r"]`; fallback only to `bazel-bin/packages/slic3r-rust/crates/slic3r_cli/slic3r_windows_runtime`; never search `PATH`. |
| T-28-01-02 | Tampering | Bash path handling | mitigate | Use `set -euo pipefail`, the existing `resolve_input` pattern, quoted variable expansions, and no `eval` or unquoted globs. |
| T-28-01-03 | Tampering | Output cleanup | mitigate | Remove only `${output_root}/Slic3r-windows`, not `${output_root}` itself; default output stays under `.planning/.tmp/windows-packaged-launcher`. |
| T-28-01-04 | Repudiation | Scope of packaged artifact | mitigate | Write `packaged-slice.txt` into the artifact with explicit support and deferred-scope wording for MSI, signing, release channels, GUI, DLL layout, and shared parity evidence. |
| T-28-01-05 | Elevation of Privilege | Windows package smoke startup | accept | Local smoke executes a repo-built binary under Bazel-controlled test inputs; no privileged operation, network call, credential access, or installer step is introduced. |
</threat_model>

<verification>
- `bash -n packages/launcher/package/win/build_packaged_launcher.sh packages/launcher/package/win/test_packaged_launcher.sh`
- `shfmt -d packages/launcher/package/win/build_packaged_launcher.sh packages/launcher/package/win/test_packaged_launcher.sh`
- `bazel query //packages/launcher:windows_packaged_launcher_tree`
- `bazel query //packages/launcher:windows_packaged_launcher_smoke`
- `bazel test //packages/launcher:windows_packaged_launcher_smoke`
- `bazel run //packages/launcher:windows_packaged_launcher_tree`
- `git diff --check`
</verification>

<success_criteria>
- `bazel run //packages/launcher:windows_packaged_launcher_tree` produces `.planning/.tmp/windows-packaged-launcher/Slic3r-windows/Slic3r-console.exe`.
- `bazel test //packages/launcher:windows_packaged_launcher_smoke` passes and executes `Slic3r-console.exe` directly.
- The new scripts contain no CLI business behavior and no Linux/macOS startup shim for the Windows package path.
- WPKG-01, WPKG-02, and the startup-boundary portion of WPKG-03 are fully covered.
</success_criteria>

<output>
After completion, create `.planning/phases/28-windows-packaged-launcher-slice/28-01-SUMMARY.md` with `requirements-completed` including `WPKG-01`, `WPKG-02`, and `WPKG-03` only if all acceptance criteria pass.
</output>
