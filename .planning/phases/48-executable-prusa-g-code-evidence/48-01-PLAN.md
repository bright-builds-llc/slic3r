---
phase: 48-executable-prusa-g-code-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
  - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
  - packages/parity/compare_prusaslicer_gcode_output.sh
  - packages/parity/compare_prusaslicer_gcode_output_test.sh
  - packages/parity/BUILD.bazel
autonomous: true
requirements: [PGEV-01, PGEV-02]
requirements_addressed: [PGEV-01, PGEV-02]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 48-2026-06-14T18-49-25
generated_at: 2026-06-14T19:19:13Z
must_haves:
  truths:
    - "Maintainer can run `bazel run //packages/parity:prusaslicer_gcode_output_parity` for the selected summary-only Prusa G-code evidence slice."
    - "The command prints the exact source ref, fixture path, expected summary path, and accepted row count without claiming runtime or byte-for-byte output support."
    - "The public failure test mutates `line_4` evidence and proves the comparator exits non-zero with a `line_4` label and unified diff."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs"
      provides: "Thin Rust executable adapter over `prusa_gcode_output_summary_lines`"
      contains: "expected-gcode-summary.tsv"
    - path: "packages/parity/compare_prusaslicer_gcode_output.sh"
      provides: "Package-local comparator for the public Bazel evidence command"
      contains: "ok: fork.prusaslicer.gcode-output parity passed"
    - path: "packages/parity/compare_prusaslicer_gcode_output_test.sh"
      provides: "Mutation guard proving checked-in expected summary drift fails"
      contains: "line_4"
    - path: "packages/parity/BUILD.bazel"
      provides: "Public `prusaslicer_gcode_output_parity` sh_binary and failure sh_test"
      contains: "prusaslicer_gcode_output_parity"
  key_links:
    - from: "packages/parity/BUILD.bazel"
      to: "//packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary"
      via: "sh_binary data and `$(location)` arg"
      pattern: "prusa_gcode_output_summary"
    - from: "packages/parity/BUILD.bazel"
      to: "//packages/parity-fixtures:prusa_gcode_output_expected_gcode_summary"
      via: "sh_binary data and args"
      pattern: "prusa_gcode_output_expected_gcode_summary"
    - from: "packages/parity/compare_prusaslicer_gcode_output.sh"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs"
      via: "explicit summary binary path argument"
      pattern: "summary_binary"
---

<objective>
Create the runnable Phase 48 Prusa G-code output evidence command and its
fail-closed mutation guard.

Purpose: PGEV-01 requires a repo-owned Bazel command for the selected
summary-only Prusa G-code slice. PGEV-02 requires executable evidence that
fails when the Rust-backed summary or checked-in expected summary diverges.

Output: `//packages/parity:prusaslicer_gcode_output_parity`, a thin Rust
summary binary, a package-local comparator, and a Bazel-wired mutation failure
test. Do not publish `fork.prusaslicer.gcode-output` in `status.tsv` in this
plan; Plan 48-02 owns status publication after this command passes.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-executable-prusa-g-code-evidence/48-CONTEXT.md
@.planning/phases/48-executable-prusa-g-code-evidence/48-RESEARCH.md
@.planning/phases/47-rust-prusa-g-code-summary-boundary/47-VERIFICATION.md
@.planning/phases/47-rust-prusa-g-code-summary-boundary/47-01-SUMMARY.md
@.planning/phases/47-rust-prusa-g-code-summary-boundary/47-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/parity/compare_prusaslicer_project_file.sh
@packages/parity/compare_prusaslicer_project_file_test.sh
@packages/parity/BUILD.bazel
@packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_project_file_summary.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
@packages/parity-fixtures/BUILD.bazel
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv

Bright Builds sources informing this plan: local `AGENTS.md`,
`AGENTS.bright-builds.md`, `standards-overrides.md`, and pinned canonical
architecture, code-shape, verification, testing, and Rust standards at
`05f8d7a6c9c2e157ec4f922a05273e72dab97676`.

<interfaces>
Existing Rust function to call:

```rust
pub fn prusa_gcode_output_summary_lines(
    input: &str,
) -> Result<Vec<String>, PrusaGcodeOutputParseError>;
```

Expected public command and output labels:

```text
bazel run //packages/parity:prusaslicer_gcode_output_parity
ok: fork.prusaslicer.gcode-output parity passed
source_ref: prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
fixture: packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode
expected: packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
rows: 5
```

The checked-in summary artifact has five accepted data rows. The mutation guard
must target the final marker row:

```text
line_4	G1 F203.201
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
| --- | --- | --- | --- | --- |
| D-01 | 48-01 | 2 | Full | Creates the public `bazel run //packages/parity:prusaslicer_gcode_output_parity` command. |
| D-02 | 48-01 | 1, 2 | Full | Adds a package-local shell comparator, Bazel `sh_binary`, and thin Rust summary binary mirroring `prusaslicer_project_file_parity`. |
| D-03 | 48-01 | 1, 2 | Full | Runs the Phase 47 Rust summary boundary against the checked-in expected G-code summary artifact; no raw G-code parser input, generation, Git, network, printer, profile, release, or sync behavior is added. |
| D-04 | 48-01 | 2 | Full | Successful output names source ref, fixture path, expected summary path, and row count only. |
| D-05 | 48-01 | 3 | Full | Adds `compare_prusaslicer_gcode_output_test.sh` as a Bazel `sh_test`. |
| D-06 | 48-01 | 3 | Full | Mutates `line_4` so the failure is tied to Phase 46/47 marker evidence. |
| D-14 | 48-01 | 1, 2, 3 | Partial | Runs the new command and failure test here; Plan 48-03 runs the full phase suite. |
| D-15 | 48-01 | 1 | Full | Adds the Rust summary binary to clippy/rustfmt coverage and verifies through `//packages/slic3r-rust:verify`. |
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Add the Rust G-code summary binary</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs, packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel</files>
  <read_first>
    - `AGENTS.md`
    - `AGENTS.bright-builds.md`
    - `standards-overrides.md`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_project_file_summary.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-CONTEXT.md`
  </read_first>
  <action>
    Create `packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs` by mirroring `prusa_project_file_summary.rs` and substituting `prusa_gcode_output_summary_lines`.

    The binary must:
    - use `#![forbid(unsafe_code)]`
    - accept exactly one argument
    - print `error: expected exactly one expected-gcode-summary.tsv fixture path argument` on wrong arity
    - read only the caller-provided path with `fs::read_to_string`
    - call `prusa_gcode_output_summary_lines(&input)`
    - print each summary line to stdout
    - return `ExitCode::FAILURE` on read or parse errors

    Update `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` with:

```python
rust_binary(
    name = "prusa_gcode_output_summary",
    srcs = ["src/bin/prusa_gcode_output_summary.rs"],
    crate_root = "src/bin/prusa_gcode_output_summary.rs",
    deps = [":slic3r_flavors"],
    edition = "2024",
)
```

    Add `:prusa_gcode_output_summary` to the `rust_clippy` `deps` list and the `rustfmt_test` `targets` list. Do not modify parser logic in `src/prusa_gcode_output.rs`; Phase 47 already owns that pure boundary per D-03.
  </action>
  <verify>
    <automated>bazel build //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary</automated>
    <automated>bazel test //packages/slic3r-rust:verify</automated>
    <automated>rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs` passes.
    - `rg -n 'prusa_gcode_output_summary_lines' packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs` succeeds.
    - `rg -n 'expected exactly one expected-gcode-summary.tsv fixture path argument' packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs` succeeds.
    - `rg -n 'name = "prusa_gcode_output_summary"' packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` succeeds.
    - `rg -n ':prusa_gcode_output_summary' packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` finds the binary, clippy, and rustfmt references.
  </acceptance_criteria>
  <done>The thin Rust summary binary builds and participates in the crate clippy/rustfmt aggregate surfaces per D-02, D-03, and D-15.</done>
</task>

<task type="auto">
  <name>Task 2: Add the public G-code parity comparator target</name>
  <files>packages/parity/compare_prusaslicer_gcode_output.sh, packages/parity/BUILD.bazel</files>
  <read_first>
    - `packages/parity/compare_prusaslicer_project_file.sh`
    - `packages/parity/BUILD.bazel`
    - `packages/parity-fixtures/BUILD.bazel`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-CONTEXT.md`
  </read_first>
  <action>
    Create executable script `packages/parity/compare_prusaslicer_gcode_output.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`, modeled on `compare_prusaslicer_project_file.sh`.

    The script must require exactly four arguments:

```text
summary_binary rust-summary-input.tsv expected-gcode-summary.tsv fixture-provenance.tsv
```

    It must verify the summary binary is executable and each TSV exists. It must run:

```bash
"${summary_binary}" "${rust_summary_input}" >"${actual_summary}"
"${summary_binary}" "${expected_artifact}" >"${expected_summary_lines}"
```

    If expected-artifact validation fails, print a failure label derived from the raw seven-column TSV using `metadata_key/metadata_value/marker_key`, then print `diff -u ${rust_summary_input} ${expected_artifact}`. A `line_4` mutation must therefore print a label containing `line_4`.

    If both summaries parse but differ, print:

```text
error: expected-gcode-summary.tsv mismatch at <label> in <expected_artifact>
diff -u <expected_summary_lines> <actual_summary>
```

    Add field assertions after diff:
    - `surface` is `fork.prusaslicer.gcode-output`
    - `inventory_id` is `prusaslicer.gcode-output`
    - `vendor_id` is `prusaslicer`
    - `source_ref` is `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`
    - `fixture_path` is `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode`
    - `expected_summary_path` is `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv`
    - `reserved_future_status_token` is `fork.prusaslicer.gcode-output`
    - `row_count` is `5`

    Successful output must be exactly these five lines:

```text
ok: fork.prusaslicer.gcode-output parity passed
source_ref: prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
fixture: packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode
expected: packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
rows: 5
```

    Update `packages/parity/BUILD.bazel` with `sh_binary(name = "prusaslicer_gcode_output_parity", ...)` using `srcs = ["compare_prusaslicer_gcode_output.sh"]`, `data = ["status.tsv", "//packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary", "//packages/parity-fixtures:prusa_gcode_output_expected_gcode_summary", "//packages/parity-fixtures:prusa_gcode_output_provenance"]`, and args in this order: summary binary, expected G-code summary, expected G-code summary, provenance. Make the new script executable with `chmod +x`.
  </action>
  <verify>
    <automated>bash -n packages/parity/compare_prusaslicer_gcode_output.sh</automated>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>bazel query //packages/parity:prusaslicer_gcode_output_parity</automated>
  </verify>
  <acceptance_criteria>
    - `test -x packages/parity/compare_prusaslicer_gcode_output.sh` passes.
    - `rg -n 'name = "prusaslicer_gcode_output_parity"' packages/parity/BUILD.bazel` succeeds.
    - `rg -n 'prusa_gcode_output_summary|prusa_gcode_output_expected_gcode_summary|prusa_gcode_output_provenance' packages/parity/BUILD.bazel` succeeds.
    - `bazel run //packages/parity:prusaslicer_gcode_output_parity` exits 0 and stdout contains `ok: fork.prusaslicer.gcode-output parity passed`, `rows: 5`, and the exact expected summary path.
    - `rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv` still finds no row; status publication belongs to Plan 48-02.
  </acceptance_criteria>
  <done>The public Bazel parity command exists, runs the Rust-backed summary boundary over the checked-in expected artifact, and prints only the exact narrow evidence facts per D-01 through D-04.</done>
</task>

<task type="auto">
  <name>Task 3: Add the line_4 parity failure mutation test</name>
  <files>packages/parity/compare_prusaslicer_gcode_output_test.sh, packages/parity/BUILD.bazel</files>
  <read_first>
    - `packages/parity/compare_prusaslicer_project_file_test.sh`
    - `packages/parity/compare_prusaslicer_gcode_output.sh`
    - `packages/parity/BUILD.bazel`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-CONTEXT.md`
  </read_first>
  <action>
    Create executable script `packages/parity/compare_prusaslicer_gcode_output_test.sh`, modeled on `compare_prusaslicer_project_file_test.sh`.

    The test must:
    - locate runfiles through `TEST_SRCDIR`/`TEST_WORKSPACE` when Bazel supplies them
    - copy `expected-gcode-summary.tsv` into a temp dir
    - mutate only the copied `line_4` marker value from `G1 F203.201` to `G1 F203.200`
    - run `compare_prusaslicer_gcode_output.sh` with the original checked-in expected summary as `rust-summary-input.tsv` and the mutated temp copy as `expected-gcode-summary.tsv`
    - assert the command exits non-zero
    - assert stderr contains `expected-gcode-summary.tsv`, `line_4`, `diff`, `@@`, and `G1 F203.200`
    - assert the checked-in expected summary still contains `line_4\tG1 F203.201`
    - print `ok: prusaslicer_gcode_output_parity_failure_test`

    Add `sh_test(name = "prusaslicer_gcode_output_parity_failure_test", ...)` to `packages/parity/BUILD.bazel` with data: `compare_prusaslicer_gcode_output.sh`, `//packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary`, `//packages/parity-fixtures:prusa_gcode_output_expected_gcode_summary`, and `//packages/parity-fixtures:prusa_gcode_output_provenance`. Make the new test script executable with `chmod +x`.
  </action>
  <verify>
    <automated>bash -n packages/parity/compare_prusaslicer_gcode_output_test.sh</automated>
    <automated>bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test</automated>
    <automated>bash packages/parity/compare_prusaslicer_gcode_output_test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x packages/parity/compare_prusaslicer_gcode_output_test.sh` passes.
    - `rg -n 'G1 F203\.200|G1 F203\.201|line_4|expected-gcode-summary.tsv|@@' packages/parity/compare_prusaslicer_gcode_output_test.sh` succeeds.
    - `rg -n 'name = "prusaslicer_gcode_output_parity_failure_test"' packages/parity/BUILD.bazel` succeeds.
    - `bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test` passes.
    - `bazel run //packages/parity:prusaslicer_gcode_output_parity` still exits 0 after the mutation test.
  </acceptance_criteria>
  <done>The Phase 48 failure guard proves a real `line_4` G-code marker divergence fails through the public comparator with a mismatch label and diff per D-05 and D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Bazel runfiles -> shell comparator | The comparator receives file paths and an executable path from Bazel `$(location)` expansion. |
| Checked-in TSV -> Rust parser | The summary binary reads caller-provided TSV text and the pure Rust parser validates the expected contract. |
| Comparator stdout -> maintainer interpretation | Successful output can influence whether maintainers trust the narrow evidence slice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-48-01-01 | Tampering | `compare_prusaslicer_gcode_output.sh` | mitigate | Verify all input files exist, verify the summary binary is executable, diff Rust-backed summary output, assert exact `row_count=5`, and add a `line_4` mutation failure test. |
| T-48-01-02 | Spoofing | `prusaslicer_gcode_output_parity` stdout | mitigate | Print only exact source ref, fixture path, expected summary path, and row count; avoid text that says byte-for-byte, runtime, printer, geometry, or full generated-output support. |
| T-48-01-03 | Elevation of Privilege | Rust summary binary and shell comparator | mitigate | Keep the Rust binary as an explicit-path file reader over `prusa_gcode_output_summary_lines`; do not add Git, network, process spawning, PrusaSlicer execution, raw G-code parsing, printer, profile, release, or sync behavior. |
| T-48-01-04 | Denial of Service | Temp-file comparator flow | accept | Inputs are small checked-in TSV files and temp directories are removed with `trap`; no unbounded external input or service endpoint is introduced. |
</threat_model>

<verification>
Run:

```bash
bazel run //packages/parity:prusaslicer_gcode_output_parity
bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test
bazel test //packages/slic3r-rust:verify
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check
git diff --check
```
</verification>

<success_criteria>
PGEV-01 and PGEV-02 are satisfied for the executable command layer when the
public Bazel command passes, the `line_4` mutation test fails closed through
the public comparator, and no `fork.prusaslicer.gcode-output` status row has
been added before Plan 48-02.
</success_criteria>

<output>
After completion, create `.planning/phases/48-executable-prusa-g-code-evidence/48-01-SUMMARY.md` with `requirements-completed: [PGEV-01, PGEV-02]` if both the command and failure guard pass.
</output>
