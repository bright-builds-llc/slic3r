---
phase: 48-executable-prusa-g-code-evidence
plan: "03"
type: execute
wave: 3
depends_on: ["48-01", "48-02"]
files_modified:
  - packages/parity/README.md
  - packages/parity-fixtures/README.md
  - packages/slic3r-rust/README.md
  - docs/port/README.md
  - docs/port/parity-matrix.md
  - docs/port/migration-guidance.md
  - docs/port/package-map.md
autonomous: true
requirements: [PGEV-03]
requirements_addressed: [PGEV-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 48-2026-06-14T18-49-25
generated_at: 2026-06-14T19:19:13Z
must_haves:
  truths:
    - "Maintainer can find the new Prusa G-code parity command in package and port docs."
    - "Maintainer can read that Phase 46 proves fixture integrity, Phase 47 proves typed summary parsing, and Phase 48 proves executable summary-only evidence/status wiring."
    - "Docs keep broad `generated-outputs` in progress and explicitly defer byte-for-byte, runtime, printer, geometry, support, seam, arc, STEP, GUI, release, network, Bambu, Orca, upstream import, and sync surfaces."
    - "Final phase verification reruns the new command, mutation guard, existing verifiers, Rust aggregate checks, formatting checks, and `git diff --check`."
  artifacts:
    - path: "packages/parity/README.md"
      provides: "Public package command and fork status docs for the G-code row"
      contains: "bazel run //packages/parity:prusaslicer_gcode_output_parity"
    - path: "docs/port/parity-matrix.md"
      provides: "High-level status dashboard naming the exact verified slice"
      contains: "fork.prusaslicer.gcode-output"
    - path: "docs/port/migration-guidance.md"
      provides: "Fixture/status evolution rules for the verified G-code evidence slice"
      contains: "summary-only Prusa G-code evidence slice"
  key_links:
    - from: "packages/parity/README.md"
      to: "packages/parity/status.tsv"
      via: "documents exact `fork.prusaslicer.gcode-output` row"
      pattern: "fork\\.prusaslicer\\.gcode-output"
    - from: "docs/port/README.md"
      to: "//packages/parity:prusaslicer_gcode_output_parity"
      via: "Current visibility state command list"
      pattern: "prusaslicer_gcode_output_parity"
    - from: "docs/port/package-map.md"
      to: "packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary"
      via: "package ownership text"
      pattern: "prusa_gcode_output_summary"
---

<objective>
Publish the Phase 48 docs/status interpretation and run final verification for
the executable Prusa G-code evidence slice.

Purpose: PGEV-03 requires maintainers to inspect exact status/docs for the
narrow verified slice while broad generated-output and runtime surfaces remain
deferred.

Output: Updated package docs, port docs, and a final verification summary.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-executable-prusa-g-code-evidence/48-CONTEXT.md
@.planning/phases/48-executable-prusa-g-code-evidence/48-RESEARCH.md
@.planning/phases/48-executable-prusa-g-code-evidence/48-01-SUMMARY.md
@.planning/phases/48-executable-prusa-g-code-evidence/48-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/parity/README.md
@packages/parity-fixtures/README.md
@packages/slic3r-rust/README.md
@docs/port/README.md
@docs/port/parity-matrix.md
@docs/port/migration-guidance.md
@docs/port/package-map.md
@packages/parity/status.tsv

<interfaces>
Docs must name all of these concrete Phase 48 facts:

```text
bazel run //packages/parity:prusaslicer_gcode_output_parity
fork.prusaslicer.gcode-output
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/
packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
//packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary
```

Docs must preserve the exact distinction:

```text
Phase 46 proves fixture surface integrity.
Phase 47 proves typed summary parsing.
Phase 48 proves executable summary-only evidence/status wiring.
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
| --- | --- | --- | --- | --- |
| D-11 | 48-03 | 1, 2 | Full | Updates required package and public docs, plus directly affected package-map/migration/Rust/fixture docs. |
| D-12 | 48-03 | 1, 2 | Full | Docs name command, status token, source identity, fixture namespace, expected artifact, Rust boundary, and deferrals. |
| D-13 | 48-03 | 1, 2 | Full | Docs distinguish Phase 46 fixture integrity, Phase 47 typed summary parsing, and Phase 48 executable evidence/status wiring. |
| D-14 | 48-03 | 3 | Full | Runs the new command, failure test, existing fixture/scope verifier suites, Rust checks, formatting checks, and `git diff --check`. |
| D-15 | 48-03 | 3 | Full | Final verification includes `bazel test //packages/slic3r-rust:verify` after the new summary binary is wired. |
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Update package docs for the G-code evidence command</name>
  <files>packages/parity/README.md, packages/parity-fixtures/README.md, packages/slic3r-rust/README.md</files>
  <read_first>
    - `packages/parity/README.md`
    - `packages/parity-fixtures/README.md`
    - `packages/slic3r-rust/README.md`
    - `packages/parity/status.tsv`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-CONTEXT.md`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-01-SUMMARY.md`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-02-SUMMARY.md`
  </read_first>
  <action>
    Update `packages/parity/README.md`:
    - add a Current Surface bullet for `bazel run //packages/parity:prusaslicer_gcode_output_parity`
    - add a Fork Status Rows paragraph stating `fork.prusaslicer.gcode-output` is verified only for the narrow summary-only `prusaslicer.gcode-output` evidence slice
    - name `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv`, the Phase 46 fixture, and `//packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary`
    - name the exact source ref and fixture path
    - state that byte-for-byte G-code parity, full generated-output parity, toolpath geometry, extrusion, timing, support generation, wall seam behavior, arc fitting, STEP, full 3MF, printer-runtime, firmware or printability, GUI, binary G-code, thumbnails, post-processing, host upload, network/device, profile auto-update, fork release, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, and sync automation remain deferred

    Update `packages/parity-fixtures/README.md` so the G-code paragraph says Phase 47 owns parser/metadata and Phase 48 now publishes `bazel run //packages/parity:prusaslicer_gcode_output_parity` plus the exact `fork.prusaslicer.gcode-output` row. Keep the text that the fixture namespace itself does not prove byte-for-byte or runtime behavior.

    Update `packages/slic3r-rust/README.md` to add:
    - `//packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_summary`
    - `bazel run //packages/parity:prusaslicer_gcode_output_parity`
    - a note that the Rust adapter reads only caller-supplied `expected-gcode-summary.tsv` and does not inspect Git, network, process, raw G-code generation, printer-runtime, profile-update, release, upstream import, or sync surfaces
  </action>
  <verify>
    <automated>rg -n 'prusaslicer_gcode_output_parity|fork\.prusaslicer\.gcode-output|expected-gcode-summary.tsv|prusa_gcode_output_summary' packages/parity/README.md packages/parity-fixtures/README.md packages/slic3r-rust/README.md</automated>
    <automated>mdformat --check packages/parity/README.md packages/parity-fixtures/README.md packages/slic3r-rust/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'bazel run //packages/parity:prusaslicer_gcode_output_parity' packages/parity/README.md packages/parity-fixtures/README.md packages/slic3r-rust/README.md` succeeds.
    - `rg -n 'fork\.prusaslicer\.gcode-output' packages/parity/README.md packages/parity-fixtures/README.md` succeeds.
    - `rg -n 'Phase 46 fixture|Phase 47 Rust summary|Phase 48' packages/parity/README.md packages/parity-fixtures/README.md` succeeds.
    - `rg -n 'byte-for-byte G-code parity|full generated-output parity|printer-runtime|Bambu Studio|OrcaSlicer|sync automation remain deferred' packages/parity/README.md packages/parity-fixtures/README.md packages/slic3r-rust/README.md` succeeds.
  </acceptance_criteria>
  <done>Package docs expose the runnable command and exact status row while preserving narrow summary-only claims per D-11 through D-13.</done>
</task>

<task type="auto">
  <name>Task 2: Update port docs with Phase 48 publication state</name>
  <files>docs/port/README.md, docs/port/parity-matrix.md, docs/port/migration-guidance.md, docs/port/package-map.md</files>
  <read_first>
    - `docs/port/README.md`
    - `docs/port/parity-matrix.md`
    - `docs/port/migration-guidance.md`
    - `docs/port/package-map.md`
    - `packages/parity/status.tsv`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-CONTEXT.md`
  </read_first>
  <action>
    Update `docs/port/README.md`:
    - add `bazel run //packages/parity:prusaslicer_gcode_output_parity` to Current Parity Visibility State
    - replace stale G-code text saying the Phase 47 boundary is planned or Phase 48 row is absent with a current state section stating:
      `Phase 46 proves fixture surface integrity. Phase 47 proves typed summary parsing. Phase 48 proves executable summary-only evidence/status wiring.`
    - name the exact status token `fork.prusaslicer.gcode-output`

    Update `docs/port/parity-matrix.md`:
    - keep the `Generated outputs` row status `in progress`
    - add fork interpretation text saying `fork.prusaslicer.gcode-output` is verified in `packages/parity/status.tsv` only for the narrow summary-only Prusa G-code evidence slice through `bazel run //packages/parity:prusaslicer_gcode_output_parity`
    - state broad `generated-outputs` remains `in progress`

    Update `docs/port/migration-guidance.md`:
    - replace `fork.prusaslicer.gcode-output is not verified or published in Phase 46` with verified Phase 48 wording
    - keep fixture update routing through `packages/prusa-gcode-output-scope`
    - preserve the deferred G-code scope list

    Update `docs/port/package-map.md`:
    - update `packages/parity` role to include the narrow Prusa G-code output slice
    - update `packages/slic3r-rust` role/notes to include the Phase 47 `prusa_gcode_output` parser and Phase 48 `prusa_gcode_output_summary` adapter
    - add a Phase 48 note that `packages/parity` owns `bazel run //packages/parity:prusaslicer_gcode_output_parity` and the `fork.prusaslicer.gcode-output` status row, `packages/parity-fixtures` owns `expected-gcode-summary.tsv`, and `slic3r_flavors` owns summary logic for the narrow evidence slice
  </action>
  <verify>
    <automated>rg -n 'prusaslicer_gcode_output_parity|fork\.prusaslicer\.gcode-output|summary-only Prusa G-code evidence slice' docs/port/README.md docs/port/parity-matrix.md docs/port/migration-guidance.md docs/port/package-map.md</automated>
    <automated>awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv</automated>
    <automated>mdformat --check docs/port/README.md docs/port/parity-matrix.md docs/port/migration-guidance.md docs/port/package-map.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Phase 46 proves fixture surface integrity\. Phase 47 proves typed summary parsing\. Phase 48 proves executable summary-only evidence/status wiring\.' docs/port/README.md docs/port/parity-matrix.md docs/port/migration-guidance.md docs/port/package-map.md` succeeds in at least one file.
    - `rg -n 'broad `generated-outputs` remains `in progress`|Generated outputs.*`in progress`' docs/port/parity-matrix.md` succeeds.
    - `rg -n 'byte-for-byte G-code parity|full generated-output parity|toolpath geometry|extrusion|timing|support generation|wall seam|arc fitting|STEP|printer-runtime|Bambu Studio|OrcaSlicer|sync automation' docs/port/README.md docs/port/parity-matrix.md docs/port/migration-guidance.md` succeeds.
    - `rg -n 'not verified or published in Phase 46|remain Phase 48-owned and absent in Phase 46|Phase 47 Rust boundary is planned' docs/port/README.md docs/port/parity-matrix.md docs/port/migration-guidance.md docs/port/package-map.md` finds no matches.
  </acceptance_criteria>
  <done>Port docs publish the Phase 48 command/status state and preserve the exact narrow claim boundary per D-11 through D-13.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 48 verification suite</name>
  <files>None - verification-only task</files>
  <read_first>
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-CONTEXT.md`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-RESEARCH.md`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-01-SUMMARY.md`
    - `.planning/phases/48-executable-prusa-g-code-evidence/48-02-SUMMARY.md`
    - `packages/parity/BUILD.bazel`
    - `packages/parity/status.tsv`
    - `packages/parity-fixtures/BUILD.bazel`
    - `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel`
  </read_first>
  <action>
    Run the full Phase 48 verification suite required by D-14 and D-15. Do not modify source files in this task unless a command exposes a real defect; if a defect is fixed, rerun the full suite.

    Required commands:

```bash
bazel run //packages/parity:prusaslicer_gcode_output_parity
bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test
bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture
bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test
bazel run //packages/prusa-gcode-output-scope:verify
bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test
bazel test //packages/slic3r-rust:verify
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features
shfmt -d packages/parity/compare_prusaslicer_gcode_output.sh packages/parity/compare_prusaslicer_gcode_output_test.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
shellcheck packages/parity/compare_prusaslicer_gcode_output.sh packages/parity/compare_prusaslicer_gcode_output_test.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
mdformat --check packages/parity/README.md packages/parity-fixtures/README.md packages/slic3r-rust/README.md docs/port/README.md docs/port/parity-matrix.md docs/port/migration-guidance.md docs/port/package-map.md
git diff --check
```
  </action>
  <verify>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test</automated>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
    <automated>bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
    <automated>bazel test //packages/slic3r-rust:verify</automated>
    <automated>rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check</automated>
    <automated>rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings</automated>
    <automated>rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features</automated>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `bazel run //packages/parity:prusaslicer_gcode_output_parity` prints `ok: fork.prusaslicer.gcode-output parity passed` and `rows: 5`.
    - `bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test` passes.
    - Both G-code scope and fixture verifier Bazel run/test targets pass after publication.
    - `awk -F '\t' '$1=="fork.prusaslicer.gcode-output" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` passes.
    - `awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv` passes.
    - `git diff --check` passes.
  </acceptance_criteria>
  <done>The full Phase 48 command, mutation guard, status/docs publication, prior verifier reconciliation, Rust aggregate checks, formatter checks, and whitespace checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Docs/status -> maintainer interpretation | Public documentation and status rows communicate what is verified. |
| Verification commands -> phase closeout | Passing commands are used as evidence that the phase goal is complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-48-03-01 | Spoofing | Package and port docs | mitigate | Docs must name the exact command and status token and distinguish Phase 46 fixture integrity, Phase 47 typed parsing, and Phase 48 executable evidence/status wiring. |
| T-48-03-02 | Information Disclosure | Docs overclaiming | mitigate | Preserve the deferred-scope list across docs and keep broad `generated-outputs` as `in progress`. |
| T-48-03-03 | Tampering | Final verification | mitigate | Rerun command, mutation test, scope/fixture verifiers, Rust aggregate checks, shell/Markdown checks, and `git diff --check`. |
| T-48-03-04 | Repudiation | Phase evidence record | mitigate | Summary must record exact commands and outcomes; `requirements-completed` should list `PGEV-03` only when docs/status verification passes. |
</threat_model>

<verification>
Overall phase verification is the Task 3 command suite. At minimum, the final
summary must show passing output for:

```bash
bazel run //packages/parity:prusaslicer_gcode_output_parity
bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test
bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture
bazel run //packages/prusa-gcode-output-scope:verify
bazel test //packages/slic3r-rust:verify
git diff --check
```
</verification>

<success_criteria>
PGEV-03 is satisfied when maintainers can inspect exact package/docs/status
publication for `fork.prusaslicer.gcode-output`, broad `generated-outputs`
remains in progress, all deferred G-code surfaces remain explicitly deferred,
and final verification passes.
</success_criteria>

<output>
After completion, create `.planning/phases/48-executable-prusa-g-code-evidence/48-03-SUMMARY.md` with `requirements-completed: [PGEV-03]`.
</output>
