---
phase: 54-semantic-g-code-fixture-corpus
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
  - packages/parity-fixtures/README.md
  - packages/parity-fixtures/BUILD.bazel
autonomous: true
requirements:
  - GSFIX-01
  - GSFIX-02
must_haves:
  truths:
    - "Maintainer can inspect a source-pinned semantic expected summary beside the existing Prusa G-code marker and structural summaries."
    - "The semantic summary has exactly one ordered row for each Phase 53 approved semantic field and no unsupported generated-output claim text."
    - "Fixture and package README text names the semantic sidecar, source/update route, and deferred generator/runtime/network/sync/status boundaries."
    - "Bazel owns the semantic sidecar through the existing Prusa G-code output fixture bundle and package boundary."
  artifacts:
    - path: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv"
      provides: "Checked-in Phase 54 semantic expected summary"
      contains: "source_ref\tfixture_path\tsemantic_field\tsemantic_category\tsemantic_value\tevidence_boundary"
    - path: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md"
      provides: "Fixture-local semantic sidecar provenance, update route, and deferred boundary"
      contains: "Semantic expected artifact: `expected-gcode-semantic-summary.tsv`"
    - path: "packages/parity-fixtures/README.md"
      provides: "Package-level narrow fixture verification wording"
      contains: "Phase 54 adds `expected-gcode-semantic-summary.tsv`"
    - path: "packages/parity-fixtures/BUILD.bazel"
      provides: "Bazel export, alias, fixture bundle, and package-boundary ownership"
      contains: "prusa_gcode_output_expected_gcode_semantic_summary"
  key_links:
    - from: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv"
      via: "Phase 53 approved semantic field names"
      pattern: "source_ref|fixture_id|fixture_path|command_class_counts|movement_class_counts|coordinate_bounds|extrusion_total|feedrate_observations|layer_marker_relationships"
    - from: "packages/parity-fixtures/BUILD.bazel"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv"
      via: "exports_files, alias, and prusa_gcode_output_bundle membership"
      pattern: "expected-gcode-semantic-summary.tsv"
    - from: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv"
      via: "source ref, fixture id, and update route wording"
      pattern: "reviewed intake change updates"
key_links:
  - from: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
    to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv"
    via: "Phase 53 approved semantic field names"
    pattern: "command_class_counts|feedrate_observations"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 54-2026-06-21T12-41-13
generated_at: 2026-06-21T12:55:42Z
---

<objective>
Add the Phase 54 semantic Prusa G-code fixture sidecar, narrow fixture documentation, and Bazel ownership wiring.

Purpose: Give Phase 55 and Phase 56 a reviewed, source-pinned semantic fixture corpus without introducing Rust parsing, public parity behavior, status publication, or broad generated-output claims.
Output: One checked-in semantic expected-summary TSV plus fixture/package README and Bazel ownership updates.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-semantic-g-code-fixture-corpus/54-CONTEXT.md
@.planning/phases/54-semantic-g-code-fixture-corpus/54-RESEARCH.md
@.planning/phases/53-semantic-g-code-scope-contract/53-CONTEXT.md
@.planning/phases/53-semantic-g-code-scope-contract/53-VERIFICATION.md
@.planning/phases/53-semantic-g-code-scope-contract/53-01-SUMMARY.md
@.planning/phases/53-semantic-g-code-scope-contract/53-02-SUMMARY.md
@.planning/milestones/v1.13-phases/50-structural-g-code-fixture-expansion/50-01-SUMMARY.md
@packages/prusa-gcode-output-scope/gcode-output-scope.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
@packages/parity-fixtures/BUILD.bazel
@packages/parity-fixtures/README.md
@packages/parity/status.tsv

<interfaces>
Phase 53 approved semantic fields, in required order:
```text
source_ref
fixture_id
fixture_path
command_class_counts
movement_class_counts
coordinate_bounds
extrusion_total
feedrate_observations
layer_marker_relationships
```

Required semantic TSV header:

```text
source_ref	fixture_path	semantic_field	semantic_category	semantic_value	evidence_boundary
```

Accepted source identity:

```text
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
```

Checked-in fixture path:

```text
packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode
```

Existing Bazel fixture bundle to extend:

```text
//packages/parity-fixtures:prusa_gcode_output_bundle
```

</interfaces>
</context>

<decision_coverage>
D-01 full: keep all Phase 54 corpus files inside `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/`.
D-02 full: create `expected-gcode-semantic-summary.tsv` with the exact planned artifact name.
D-03 full: use only `gcodewriter-set-speed.gcode` and source ref `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`; do not add generated, downloaded, imported, or new fixture sources.
D-04 full: TSV rows use only the Phase 53 closed field set.
D-05 full: TSV contains exactly one row per approved field in the required order.
D-06 full: semantic values stay limited to feedrate-only observations and explicit absence of coordinate, extrusion, and layer-marker data.
D-07 full: every semantic row has a fixture-summary-only evidence boundary.
D-11 full: `BUILD.bazel` exports and bundles the semantic sidecar for the existing verifier target to consume in Plan 54-02.
D-12 full: fixture README names the semantic expected artifact and reviewed update route.
D-13 full: package README keeps fixture verification local to checked-in artifacts with no fetch, generation, upload, runtime, network, sync, or public status behavior.
D-14 full: this plan must not edit Rust crates, `packages/parity/status.tsv`, public parity command behavior, or public `docs/port/*` files.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Add the exact semantic expected summary TSV</name>
  <files>packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv</files>
  <read_first>
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-CONTEXT.md
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-RESEARCH.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-CONTEXT.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-VERIFICATION.md
    - packages/prusa-gcode-output-scope/gcode-output-scope.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
  </read_first>
  <action>Create `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv`, per D-01 through D-07. Use US-ASCII text with LF line endings. The file must have exactly 10 lines: one header plus nine rows. Use this exact header:

```text
source_ref	fixture_path	semantic_field	semantic_category	semantic_value	evidence_boundary
```

Use these exact rows, in this order:

```text
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode	source_ref	source identity	prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	Accepted PrusaSlicer source identity only: `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`.
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode	fixture_id	fixture identity	gcodewriter-set-speed.gcode	Fixture identity only: `gcodewriter-set-speed.gcode`.
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode	fixture_path	fixture identity	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode	Checked-in fixture path only: `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode`.
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode	command_class_counts	command classes	G1:4;feedrate_only:4	Counts of command classes in the selected fixture summary only; no byte-for-byte G-code parity or generator parity.
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode	movement_class_counts	movement classes	travel:0;extrusion:0;coordinate_motion:0;feedrate_only:4	Counts of movement classes in the selected fixture summary only; no toolpath geometry, travel, or printability claim.
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode	coordinate_bounds	coordinate bounds	x:none;y:none;z:none	No coordinate axes observed in the selected fixture summary; no toolpath geometry or printability claim.
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode	extrusion_total	extrusion total	e_axis_observed:false;extrusion_total:not_observed	No extrusion axis observed in the selected fixture summary; no material-use, runtime, or printability claim.
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode	feedrate_observations	feedrate observations	F99999.123;F1;F203.2;F203.201	Feedrate metadata only from the selected fixture summary; no timing, firmware, or printer-runtime behavior claim.
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode	layer_marker_relationships	layer or marker relationships	layer_markers:0;marker_relationships:none	No layer markers observed in the selected fixture summary; no GUI, viewer, runtime, support, seam, or arc behavior claim.
```

Do not add byte counts, SHA-256, status-row text, `generated-outputs`, public parity command names, Rust parser names, geometry/toolpath claims, timing claims, material-use claims, printability claims, GUI claims, support/seam/arc claims, or non-Prusa fork claims to this TSV.</action>
<acceptance_criteria>
- `head -n 1 packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv | grep -Fx $'source_ref\tfixture_path\tsemantic_field\tsemantic_category\tsemantic_value\tevidence_boundary'` passes.
- `wc -l < packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv | tr -d ' '` prints `10`.
- `awk -F '\t' 'NR > 1 && NF != 6 { exit 1 }' packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv` exits 0.
- `awk -F '\t' 'NR > 1 { print $3 }' packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv` prints exactly `source_ref`, `fixture_id`, `fixture_path`, `command_class_counts`, `movement_class_counts`, `coordinate_bounds`, `extrusion_total`, `feedrate_observations`, and `layer_marker_relationships` in that order.
- `! rg -n 'byte-for-byte G-code parity verified|full generated-output parity verified|toolpath correctness verified|printability verified|printer-runtime behavior verified|Bambu Studio support verified|OrcaSlicer support verified|prusaslicer_gcode_output_parity|fork\.prusaslicer\.gcode-output|generated-outputs' packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv` passes.
</acceptance_criteria>
<verify>
<automated>head -n 1 packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv | grep -Fx $'source_ref\tfixture_path\tsemantic_field\tsemantic_category\tsemantic_value\tevidence_boundary'</automated>
<automated>test "$(wc -l < packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv | tr -d ' ')" = "10"</automated>
<automated>awk -F '\t' 'NR > 1 && NF != 6 { exit 1 }' packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv</automated>
<automated>git diff --check -- packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv</automated>
</verify>
<done>The semantic TSV exists, is source-pinned, contains exactly the nine Phase 53 fields in order, and stays limited to narrow fixture-summary observations.</done>
</task>

<task type="auto">
  <name>Task 2: Wire semantic sidecar ownership and narrow fixture docs</name>
  <files>packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md, packages/parity-fixtures/README.md, packages/parity-fixtures/BUILD.bazel</files>
  <read_first>
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-CONTEXT.md
    - .planning/phases/54-semantic-g-code-fixture-corpus/54-RESEARCH.md
    - .planning/phases/53-semantic-g-code-scope-contract/53-VERIFICATION.md
    - .planning/milestones/v1.13-phases/50-structural-g-code-fixture-expansion/50-01-SUMMARY.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv
    - packages/parity-fixtures/README.md
    - packages/parity-fixtures/BUILD.bazel
    - packages/parity/status.tsv
  </read_first>
  <action>Update only `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md`, `packages/parity-fixtures/README.md`, and `packages/parity-fixtures/BUILD.bazel`, per D-11 through D-14. Preserve existing Phase 46, Phase 50, and Phase 52 wording that the current verifier checks.

In the fixture README:

- Add `Phase 54 adds `expected-gcode-semantic-summary.tsv` as a semantic sidecar for the same source-pinned fixture.` near the opening Phase history.
- Add the provenance bullet `- Semantic expected artifact: `expected-gcode-semantic-summary.tsv\`\` after the structural expected artifact bullet.
- Add a semantic description paragraph: `Phase 54 adds `expected-gcode-semantic-summary.tsv`as a semantic sidecar for the same selected fixture. It records only source identity, fixture identity, command class counts, movement class counts, coordinate-bound absence, extrusion-axis absence, feedrate observations, and layer/marker absence from the checked-in`G1 F...` lines.`
- Update the status boundary to state: `Phase 55 owns Rust semantic parsing/readiness, and Phase 56 owns public semantic parity/status/docs publication. Phase 54 does not update `packages/parity/status.tsv`, public parity command behavior, Rust parser crates, or public `docs/port/\*` files.`
- Keep the existing update route tied to `packages/fork-vendors/forks.tsv`, `packages/fork-inventories/prusaslicer.tsv`, and `packages/prusa-gcode-output-scope/gcode-output-scope.md`.
- Keep explicit exclusions for generated fixtures, downloaded fixtures, imported upstream trees, live generation, upstream fetching/importing, post-processing, host upload, printer-runtime behavior, network/device behavior, sync automation, thumbnails, printability, firmware, GUI, support, seam, arc, Bambu Studio, OrcaSlicer, and broad generated-output parity.

In `packages/parity-fixtures/README.md`:

- Extend the Prusa G-code output paragraph to say Phase 53 closes the semantic field contract and Phase 54 adds `expected-gcode-semantic-summary.tsv` to the same fixture namespace and `//packages/parity-fixtures:prusa_gcode_output_bundle`.
- Add the exact sentence: `Fixture verification checks checked-in artifacts only; it does not fetch upstream source, generate fixtures, upload G-code, run slicer runtime behavior, execute printer behavior, sync sources, or publish public status.`
- Preserve the current structural Phase 52 status wording and broad deferred-scope paragraph.

In `packages/parity-fixtures/BUILD.bazel`:

- Add `forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv` to the top-level `exports_files([...])` list.
- Add an alias:

```python
alias(
    name = "prusa_gcode_output_expected_gcode_semantic_summary",
    actual = "forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv",
)
```

- Add the semantic TSV to `prusa_gcode_output_bundle`.
- Add the semantic TSV to `package_boundary`.
- Keep the existing `verify_prusa_gcode_output_fixture` and `verify_prusa_gcode_output_fixture_test` target names unchanged. Their data already includes `:prusa_gcode_output_bundle`; do not create a new verifier target.

Do not edit Rust crates, `packages/parity/status.tsv`, `packages/parity/README.md`, public parity command behavior, or public `docs/port/*` files.</action>
<acceptance_criteria>
- `rg -n "Semantic expected artifact: `expected-gcode-semantic-summary.tsv`|Phase 54 adds `expected-gcode-semantic-summary.tsv` as a semantic sidecar|Phase 55 owns Rust semantic parsing/readiness|Phase 56 owns public semantic parity/status/docs publication" packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md` finds all required fixture README text.
- `rg -n "Phase 53 closes the semantic field contract|Phase 54 adds `expected-gcode-semantic-summary.tsv`|Fixture verification checks checked-in artifacts only; it does not fetch upstream source, generate fixtures, upload G-code, run slicer runtime behavior, execute printer behavior, sync sources, or publish public status" packages/parity-fixtures/README.md` finds all required package README text.
- `rg -n "expected-gcode-semantic-summary.tsv|prusa_gcode_output_expected_gcode_semantic_summary" packages/parity-fixtures/BUILD.bazel` finds export, alias, bundle, and package-boundary wiring.
- `bazel query //packages/parity-fixtures:prusa_gcode_output_expected_gcode_semantic_summary` succeeds.
- `bazel query 'labels(srcs, //packages/parity-fixtures:prusa_gcode_output_bundle)' | rg 'expected-gcode-semantic-summary.tsv'` succeeds.
- `git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/slic3r-rust` returns no output.
</acceptance_criteria>
<verify>
<automated>rg -n "expected-gcode-semantic-summary.tsv|semantic sidecar|Phase 55 owns Rust semantic parsing/readiness|Phase 56 owns public semantic parity/status/docs publication" packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md packages/parity-fixtures/README.md packages/parity-fixtures/BUILD.bazel</automated>
<automated>bazel query //packages/parity-fixtures:prusa_gcode_output_expected_gcode_semantic_summary</automated>
<automated>bazel query 'labels(srcs, //packages/parity-fixtures:prusa_gcode_output_bundle)' | rg 'expected-gcode-semantic-summary.tsv'</automated>
<automated>mdformat --check packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md</automated>
<automated>git diff --check -- packages/parity-fixtures/BUILD.bazel packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md</automated>
<automated>test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/slic3r-rust)"</automated>
</verify>
<done>The semantic sidecar is discoverable in fixture docs, package docs, and Bazel ownership surfaces without changing public status, public docs, parity command behavior, or Rust crates.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 53 scope contract -> semantic TSV | Approved Markdown field names become checked-in TSV rows consumed by later parser and parity phases. |
| Checked-in fixture docs -> maintainer understanding | README text constrains what the artifact claims and what remains deferred. |
| Bazel package boundary -> downstream verifier/tests | Bazel data wiring determines whether later verifier and mutation tests see the semantic TSV under runfiles. |
| Fixture corpus -> public status/docs | Phase 54 artifacts must not publish or imply public semantic parity status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Spoofing | `expected-gcode-semantic-summary.tsv` source and fixture identity | mitigate | Require exact `source_ref`, exact checked-in `fixture_path`, and exact `fixture_id` rows copied from Phase 53 and fixture provenance. |
| T-54-02 | Tampering | Semantic TSV field set and ordering | mitigate | Create exactly 10 LF-delimited lines with the closed nine-row semantic field order; Plan 54-02 adds fail-closed verifier enforcement and mutation guards. |
| T-54-03 | Repudiation | Fixture README update route | mitigate | Require README text that ties updates to reviewed intake changes through fork vendor, inventory, and scope records. |
| T-54-04 | Information Disclosure | Fixture corpus contents | accept | The plan adds static public repo fixture metadata only; no secrets, credentials, private data, network, device, host upload, or runtime surfaces are introduced. |
| T-54-05 | Elevation of Privilege | Bazel fixture ownership | accept | Bazel wiring only exposes a static file through existing fixture targets; it does not add process, Git, network, generator, release, or sync behavior. |
| T-54-06 | Tampering | Premature public status/docs publication | mitigate | Acceptance criteria require no diffs under `packages/parity/status.tsv`, `packages/parity/README.md`, `docs/port`, or Rust parser crates. |
</threat_model>

<verification>
Run after both tasks:

<automated>head -n 1 packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv | grep -Fx $'source_ref\tfixture_path\tsemantic_field\tsemantic_category\tsemantic_value\tevidence_boundary'</automated>
<automated>test "$(wc -l < packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv | tr -d ' ')" = "10"</automated>
<automated>awk -F '\t' 'NR > 1 && NF != 6 { exit 1 }' packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv</automated>
<automated>rg -n "expected-gcode-semantic-summary.tsv|semantic sidecar|Fixture verification checks checked-in artifacts only" packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md packages/parity-fixtures/README.md packages/parity-fixtures/BUILD.bazel</automated>
<automated>bazel query //packages/parity-fixtures:prusa_gcode_output_expected_gcode_semantic_summary</automated>
<automated>bazel query 'labels(srcs, //packages/parity-fixtures:prusa_gcode_output_bundle)' | rg 'expected-gcode-semantic-summary.tsv'</automated>
<automated>mdformat --check packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md</automated>
<automated>git diff --check -- packages/parity-fixtures/BUILD.bazel packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv</automated>
<automated>test -z "$(git diff --name-only -- packages/parity/status.tsv packages/parity/README.md docs/port packages/slic3r-rust)"</automated>
</verification>

<success_criteria>

- `expected-gcode-semantic-summary.tsv` exists with the exact six-column header and exactly nine ordered Phase 53 semantic field rows.
- Fixture README and package README make the semantic sidecar inspectable and preserve reviewed update-route plus no-fetch/no-generation/no-runtime/no-status boundaries.
- `BUILD.bazel` exports the semantic TSV, provides `prusa_gcode_output_expected_gcode_semantic_summary`, and includes the file in `prusa_gcode_output_bundle` and `package_boundary`.
- No Rust crate, `packages/parity/status.tsv`, public parity command behavior, `packages/parity/README.md`, or public `docs/port/*` file is changed.
  </success_criteria>

<output>
After completion, create `.planning/phases/54-semantic-g-code-fixture-corpus/54-01-SUMMARY.md`
</output>
