---
phase: 45-prusa-g-code-output-scope-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/fork-inventories/prusaslicer.tsv
  - packages/fork-inventories/category-map.tsv
autonomous: true
requirements: [PGSEL-01]
requirements_addressed: [PGSEL-01]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 45-2026-06-06T13-53-22
generated_at: 2026-06-06T14:07:50Z
must_haves:
  truths:
    - "Maintainer can verify that `prusaslicer.gcode-output` exists as an inventory row before any scope record cites it."
    - "Maintainer can trace `prusaslicer.gcode-output` to the accepted PrusaSlicer source identity and G-code source files."
    - "Maintainer can verify that the category map references `prusaslicer.gcode-output` exactly once."
  artifacts:
    - path: "packages/fork-inventories/prusaslicer.tsv"
      provides: "Source-observed `prusaslicer.gcode-output` inventory row"
      contains: "prusaslicer.gcode-output"
    - path: "packages/fork-inventories/category-map.tsv"
      provides: "Exact-once category-map reference for `prusaslicer.gcode-output`"
      contains: "gcode.shared"
  key_links:
    - from: "packages/fork-inventories/prusaslicer.tsv"
      to: "packages/fork-vendors/forks.tsv"
      via: "source_ref"
      pattern: "prusaslicer:version_2\\.9\\.5@9a583bd438b195856f3bcf7ea99b69ba4003a961"
    - from: "packages/fork-inventories/category-map.tsv"
      to: "packages/fork-inventories/prusaslicer.tsv"
      via: "inventory_ids"
      pattern: "prusaslicer\\.gcode-output"
---

<objective>
Reconcile the locked Phase 45 `prusaslicer.gcode-output` inventory claim before
the scope package cites it.

Purpose: The Phase 45 context requires an exact inventory row, but the current
Prusa inventory does not contain that row. This plan makes the metadata source
truthful and verifier-owned before the scope record is created.

Output: One new Prusa inventory row and one exact-once category-map reference.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-prusa-g-code-output-scope-gate/45-CONTEXT.md
@.planning/phases/45-prusa-g-code-output-scope-gate/45-RESEARCH.md
@.planning/milestones/v1.9-phases/33-inventory-templates-and-source-pinned-fork-inventories/33-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/fork-inventories/README.md
@packages/fork-inventories/prusaslicer.tsv
@packages/fork-inventories/category-map.tsv
@packages/fork-inventories/BUILD.bazel
@packages/fork-vendors/forks.tsv
@packages/parity/status.tsv

Bright Builds sources loaded for this plan: local `AGENTS.md`,
`AGENTS.bright-builds.md`, `standards-overrides.md`, and pinned standards
`index.md`, `core/architecture.md`, `core/code-shape.md`,
`core/verification.md`, and `core/testing.md` at commit
`05f8d7a6c9c2e157ec4f922a05273e72dab97676`.

<interfaces>
Inventory TSV header from `packages/fork-inventories/README.md`:

```text
# inventory_id	vendor_id	source_ref	source_paths	feature_surface	feature_category	ownership	complexity	parity_dependency	v1_9_decision	caution_flags	future_parity_notes
```

Category-map TSV header from `packages/fork-inventories/README.md`:

```text
# map_id	feature_category	ownership	v1_9_decision	inventory_ids	notes
```

Accepted Prusa source identity from `packages/fork-vendors/forks.tsv`:

```text
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
| --- | --- | --- | --- | --- |
| D-02 | 45-01 | 1, 2 | Full | Adds the exact `prusaslicer.gcode-output` inventory row before the Phase 45 scope record cites it. |
| D-12 | 45-01 | 1 | Full | Keeps the row as planning metadata only and preserves the evidence ladder. |
| D-13 | 45-01 | 1, 2 | Full | Leaves broad `generated-outputs` in progress and does not publish `fork.prusaslicer.gcode-output`. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Prusa G-code output inventory row</name>
  <files>packages/fork-inventories/prusaslicer.tsv</files>
  <read_first>
    - `packages/fork-inventories/README.md`
    - `packages/fork-inventories/prusaslicer.tsv`
    - `packages/fork-vendors/forks.tsv`
    - `packages/parity/status.tsv`
    - `.planning/phases/45-prusa-g-code-output-scope-gate/45-CONTEXT.md` decisions D-02, D-12, and D-13
    - `.planning/phases/45-prusa-g-code-output-scope-gate/45-RESEARCH.md` sections "Architecture Patterns", "Common Pitfalls", and "Security Domain"
  </read_first>
  <behavior>
    - Test 1: `packages/fork-inventories/prusaslicer.tsv` contains exactly one literal-tab-delimited row whose first field is `prusaslicer.gcode-output`.
    - Test 2: The row's `source_ref` is `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`.
    - Test 3: The row uses `generated-outputs` as a parity dependency without changing `packages/parity/status.tsv`.
  </behavior>
  <action>
    Add this exact literal-tab-delimited row to `packages/fork-inventories/prusaslicer.tsv`, near the adjacent generated-output rows, per D-02:

```text
prusaslicer.gcode-output	prusaslicer	prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	src/libslic3r/GCode.cpp;src/libslic3r/GCode.hpp	gcode-output	gcode-output	shared-downstream	high	generated-outputs	future-candidate	none	Source-observed G-code output planning row; parity requires reviewed source-pinned summary evidence before output behavior is claimed.
```

Do not edit `packages/parity/status.tsv`, create fixture files, create
`expected-gcode-summary.tsv`, add Rust code, or add a parity target. The row is
source-observed planning metadata only; D-13 keeps broad `generated-outputs` in
progress and leaves `fork.prusaslicer.gcode-output` unpublished.
  </action>
  <acceptance_criteria>
    - `awk -F '\t' '$1=="prusaslicer.gcode-output" { count++ } END { exit count == 1 ? 0 : 1 }' packages/fork-inventories/prusaslicer.tsv` succeeds.
    - `rg -n $'prusaslicer\\.gcode-output\\tprusaslicer\\tprusaslicer:version_2\\.9\\.5@9a583bd438b195856f3bcf7ea99b69ba4003a961\\tsrc/libslic3r/GCode\\.cpp;src/libslic3r/GCode\\.hpp\\tgcode-output\\tgcode-output\\tshared-downstream\\thigh\\tgenerated-outputs\\tfuture-candidate\\tnone\\tSource-observed G-code output planning row; parity requires reviewed source-pinned summary evidence before output behavior is claimed\\.' packages/fork-inventories/prusaslicer.tsv` succeeds.
    - `! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>awk -F '\t' '$1=="prusaslicer.gcode-output" { count++ } END { exit count == 1 ? 0 : 1 }' packages/fork-inventories/prusaslicer.tsv</automated>
    <automated>! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv</automated>
  </verify>
  <done>`prusaslicer.gcode-output` exists exactly once in `packages/fork-inventories/prusaslicer.tsv` with the accepted source ref, G-code source paths, `generated-outputs` dependency, and metadata-only note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the exact-once category-map reference</name>
  <files>packages/fork-inventories/category-map.tsv</files>
  <read_first>
    - `packages/fork-inventories/README.md`
    - `packages/fork-inventories/category-map.tsv`
    - `packages/fork-inventories/prusaslicer.tsv`
    - `packages/fork-inventories/verify_inventories.sh`
    - `.planning/phases/45-prusa-g-code-output-scope-gate/45-CONTEXT.md` decisions D-02 and D-13
  </read_first>
  <behavior>
    - Test 1: `category-map.tsv` references `prusaslicer.gcode-output` exactly once.
    - Test 2: The map row groups the row under `gcode-output`, `shared-downstream`, and `future-candidate`.
    - Test 3: `bazel run //packages/fork-inventories:verify` passes after the new row and category-map entry exist.
  </behavior>
  <action>
    Add this exact literal-tab-delimited row to `packages/fork-inventories/category-map.tsv`, near the other generated-output candidate rows:

```text
gcode.shared	gcode-output	shared-downstream	future-candidate	prusaslicer.gcode-output	Prusa G-code output row needs reviewed source-pinned summary evidence before generated-output behavior is claimed.
```

Do not add Bambu Studio or OrcaSlicer inventory IDs to this row. Phase 45 is
locked to PrusaSlicer only, and D-09 rejects Bambu Studio and OrcaSlicer support
claims in this package.
  </action>
  <acceptance_criteria>
    - `awk -F '\t' '$5 ~ /(^|;)prusaslicer.gcode-output(;|$)/ { count++ } END { exit count == 1 ? 0 : 1 }' packages/fork-inventories/category-map.tsv` succeeds.
    - `rg -n $'gcode\\.shared\\tgcode-output\\tshared-downstream\\tfuture-candidate\\tprusaslicer\\.gcode-output\\tPrusa G-code output row needs reviewed source-pinned summary evidence before generated-output behavior is claimed\\.' packages/fork-inventories/category-map.tsv` succeeds.
    - `bazel run //packages/fork-inventories:verify` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>bazel run //packages/fork-inventories:verify</automated>
    <automated>git diff --check -- packages/fork-inventories/prusaslicer.tsv packages/fork-inventories/category-map.tsv</automated>
  </verify>
  <done>`category-map.tsv` maps `prusaslicer.gcode-output` exactly once, and the fork inventory verifier passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| inventory metadata -> scope package | Scope records and docs will treat inventory rows as source identity inputs. |
| TSV text -> verifier logic | Local shell verifiers parse literal TSV text and can be bypassed if rows are malformed or duplicated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-45-01-01 | Tampering | `packages/fork-inventories/prusaslicer.tsv` | mitigate | Add one exact `prusaslicer.gcode-output` row and run `bazel run //packages/fork-inventories:verify`. |
| T-45-01-02 | Repudiation | `packages/fork-inventories/category-map.tsv` | mitigate | Add one exact category-map row and acceptance-check exact-once mapping with `awk`. |
| T-45-01-03 | Spoofing | `source_ref` and `source_paths` fields | mitigate | Use the accepted `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961` source ref and `src/libslic3r/GCode.cpp;src/libslic3r/GCode.hpp` paths. |
| T-45-01-04 | Repudiation | status publication boundary | mitigate | Verify `packages/parity/status.tsv` has no `fork.prusaslicer.gcode-output` row in this plan. |
</threat_model>

<verification>
Run:

```bash
bazel run //packages/fork-inventories:verify
! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv
git diff --check -- packages/fork-inventories/prusaslicer.tsv packages/fork-inventories/category-map.tsv
```
</verification>

<success_criteria>
Phase 45 Plan 01 succeeds when the checked-in inventory and category map make
`prusaslicer.gcode-output` a truthful source-observed planning row and no status
publication, fixture, Rust, or parity artifact has been created.
</success_criteria>

<output>
After completion, create `.planning/phases/45-prusa-g-code-output-scope-gate/45-01-SUMMARY.md`
</output>
