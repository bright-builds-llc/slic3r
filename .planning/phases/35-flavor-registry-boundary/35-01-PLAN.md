---
phase: 35-flavor-registry-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs
  - packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs
autonomous: true
requirements: [ARCH-03]
requirements_addressed: [ARCH-03]
user_setup: []
must_haves:
  truths:
    - "Static registry data can construct canonical `ParitySurface` values without runtime parsing, `expect()`, `unwrap()`, or duplicate raw-string validation."
    - "The `ParitySurface` private-field newtype remains strict; callers still cannot construct arbitrary parity-surface tokens."
    - "Every canonical token from `packages/parity/status.tsv` has a named public `const fn` constructor and focused test coverage."
    - "The contract crate remains side-effect free and does not gain registry composition, TSV runtime parsing, filesystem, Git, network, process, environment, clock, or release behavior."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs"
      provides: "Public const constructors for canonical parity-surface tokens"
      contains: "pub const fn cli_version() -> Self"
    - path: "packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs"
      provides: "Behavior tests proving constructors return canonical tokens"
      contains: "parity_surface_constructors_return_canonical_tokens"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs"
      to: "packages/parity/status.tsv"
      via: "one named constructor per status token"
      pattern: "pub const fn generated_outputs\\(\\) -> Self"
    - from: "packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs"
      to: "packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs"
      via: "constructor display tests"
      pattern: "parity_surface_constructors_return_canonical_tokens"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 35-2026-05-27T11-24-13
generated_at: 2026-05-27T11:53:21Z
---

<objective>
Add public `const fn` constructors for canonical `ParitySurface` tokens so Phase 35 registry metadata can stay static, typed, and side-effect free.

Purpose: ARCH-03 requires a pure registry boundary. The registry crate should not parse parity-surface strings at runtime or use `expect()` during static construction.
Output: Named `ParitySurface` constructors plus focused contract tests that keep the private-field newtype strict.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-flavor-registry-boundary/35-CONTEXT.md
@.planning/phases/35-flavor-registry-boundary/35-RESEARCH.md
@.planning/phases/34-rust-flavor-contracts/34-CONTEXT.md
@.planning/phases/34-rust-flavor-contracts/34-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs
@packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs
@packages/parity/status.tsv

<interfaces>
Existing `ParitySurface` contract shape from `packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs`:

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct ParitySurface(&'static str);

impl ParitySurface {
    pub const fn as_str(self) -> &'static str;
}
```

Add these public associated constructors without exposing the tuple field:

```rust
impl ParitySurface {
    pub const fn cli_version() -> Self;
    pub const fn cli_help() -> Self;
    pub const fn cli_other() -> Self;
    pub const fn export_workflows() -> Self;
    pub const fn transform_workflows() -> Self;
    pub const fn linux_runtime() -> Self;
    pub const fn windows_runtime() -> Self;
    pub const fn linux_packaged_launcher() -> Self;
    pub const fn windows_packaged_launcher() -> Self;
    pub const fn config() -> Self;
    pub const fn config_persistence() -> Self;
    pub const fn file_formats() -> Self;
    pub const fn generated_outputs() -> Self;
    pub const fn launcher_packaging() -> Self;
}
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage |
|----------|------|------|----------|
| D-01 | 35-01 | 1 | Partial support - keeps prerequisite contract construction in `slic3r_contracts`; registry composition remains in `slic3r_flavors` in Plan 35-02. |
| D-02 | 35-01 | 1 | Full for this plan - `slic3r_contracts` stays focused on typed vocabulary and constructors, not registry composition. |
| D-04 | 35-01 | 1 | Support - capability records in Plan 35-02 can carry typed `ParitySurface` values statically. |
| D-08 | 35-01 | 1 | Support - static typed registry data can use constructors instead of runtime parsing. |
| D-10 | 35-01 | 1 | Full for this plan - no side-effecting APIs are added to contract construction. |
| D-12 | 35-01 | 1 | Support - tests prove constructors preserve canonical parity-surface values. |
</decision_coverage>

<dependency_graph>
| Unit | Needs | Creates | Wave |
|------|-------|---------|------|
| Parity surface constructors | Phase 34 `ParitySurface` private-field newtype and `packages/parity/status.tsv` tokens | Static typed parity constructors consumed by Plan 35-02 | 1 |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add static parity-surface constructors</name>
  <files>packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs, packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs`
    - `packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs`
    - `packages/parity/status.tsv`
    - `.planning/phases/35-flavor-registry-boundary/35-CONTEXT.md`
    - `.planning/phases/35-flavor-registry-boundary/35-RESEARCH.md`
    - `.planning/phases/34-rust-flavor-contracts/34-01-SUMMARY.md`
    - `AGENTS.md`
  </read_first>
  <behavior>
    - Test 1: `ParitySurface::cli_version().as_str()` returns `cli.version`.
    - Test 2: `ParitySurface::cli_help().as_str()` returns `cli.help`.
    - Test 3: `ParitySurface::cli_other().as_str()` returns `cli.other`.
    - Test 4: `ParitySurface::export_workflows().as_str()` returns `export.workflows`.
    - Test 5: `ParitySurface::transform_workflows().as_str()` returns `transform.workflows`.
    - Test 6: `ParitySurface::linux_runtime().as_str()` returns `linux.runtime`.
    - Test 7: `ParitySurface::windows_runtime().as_str()` returns `windows.runtime`.
    - Test 8: `ParitySurface::linux_packaged_launcher().as_str()` returns `linux.packaged-launcher`.
    - Test 9: `ParitySurface::windows_packaged_launcher().as_str()` returns `windows.packaged-launcher`.
    - Test 10: `ParitySurface::config().as_str()` returns `config`.
    - Test 11: `ParitySurface::config_persistence().as_str()` returns `config.persistence`.
    - Test 12: `ParitySurface::file_formats().as_str()` returns `file-formats`.
    - Test 13: `ParitySurface::generated_outputs().as_str()` returns `generated-outputs`.
    - Test 14: `ParitySurface::launcher_packaging().as_str()` returns `launcher-packaging`.
    - Test 15: Every constructor result equals the corresponding `ParitySurface::try_from(token)` result, so constructors and parsers cannot drift.
  </behavior>
  <action>Add a focused test named `parity_surface_constructors_return_canonical_tokens` to `packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs` before implementation. Use an array of `(ParitySurface, &'static str)` pairs, explicit `// Arrange`, `// Act`, and `// Assert` sections, and compare both `.as_str()` and `ParitySurface::try_from(token)`.

Then update `packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs` by adding the 14 public associated `const fn` constructors listed in `<interfaces>` inside the existing `impl ParitySurface` block. Each constructor must return `Self("<canonical-token>")`; do not make the tuple field public, do not add `From<&'static str>`, do not add `LazyLock`, and do not use `expect()`, `unwrap()`, or runtime parsing to construct canonical values.

Keep this change limited to the contract crate prerequisite per D-01 and D-02. Do not add registry composition, runtime TSV parsing, build scripts, generators, filesystem reads, Git calls, network calls, process execution, environment reads, clocks, release behavior, launcher dispatch, fork-specific workspaces, online/cloud integration, credential handling, non-free plugin ingestion, or executable fork parity behavior per D-08 through D-11.</action>
  <verify>
    <automated>rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_contracts --test flavor_contracts && rg -n "pub const fn cli_version\\(\\) -> Self|pub const fn config_persistence\\(\\) -> Self|pub const fn generated_outputs\\(\\) -> Self|parity_surface_constructors_return_canonical_tokens" packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs && ! rg -n "\\.unwrap\\(|expect\\(|LazyLock|std::fs|std::process|std::env|SystemTime|Instant|include_str!|build.rs" packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs</automated>
  </verify>
  <acceptance_criteria>
    - `packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs` still declares `pub struct ParitySurface(&'static str);` with a private tuple field.
    - `flavor.rs` contains all 14 constructor names listed in `<interfaces>`.
    - `flavor_contracts.rs` contains `parity_surface_constructors_return_canonical_tokens`.
    - The constructor test compares constructor output to `ParitySurface::try_from(token)` for every token in `packages/parity/status.tsv`.
    - `rg -n "pub const fn generated_outputs\\(\\) -> Self" packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs` returns a match.
    - The contract crate source contains no `.unwrap(`, `expect(`, `LazyLock`, `std::fs`, `std::process`, `std::env`, `SystemTime`, `Instant`, `include_str!`, or build-script behavior.
  </acceptance_criteria>
  <done>Canonical parity-surface values are available as public `const fn` constructors while `ParitySurface` remains a strict private-field newtype.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `packages/parity/status.tsv` -> `ParitySurface` constructors | Canonical status tokens become public typed constructors for downstream static metadata. |
| `ParitySurface` constructors -> future registry data | Plan 35-02 will consume these constructors without runtime parsing or panics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01-01 | Spoofing | `ParitySurface` construction | mitigate | Keep the tuple field private and add only named constructors for canonical tokens from `packages/parity/status.tsv`; do not add arbitrary `From<&str>` construction. |
| T-35-01-02 | Tampering | Constructor/parser drift | mitigate | Add a test that compares every constructor result to `ParitySurface::try_from(token)` for the same canonical token. |
| T-35-01-03 | Information Disclosure | Contract crate side effects | mitigate | Verify no filesystem, Git, network, process, environment, clock, runtime TSV parsing, or release APIs are introduced in `flavor.rs`. |
| T-35-01-04 | Repudiation | Runtime parity overclaim | mitigate | Constructors only represent existing parity-surface labels; this plan adds no support/verified status API and no registry capability claims. |
| T-35-01-05 | Denial of Service | Runtime initialization | mitigate | Use `const fn` constructors instead of lazy runtime parsing or panicking initialization paths. |
</threat_model>

<verification>
Overall verification for this plan:

```bash
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml -p slic3r_contracts --test flavor_contracts
bazel test //packages/slic3r-rust/crates/slic3r_contracts:flavor_contracts_test //packages/slic3r-rust/crates/slic3r_contracts:rustfmt_check //packages/slic3r-rust/crates/slic3r_contracts:clippy
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features
rg -n "pub const fn cli_version\\(\\) -> Self|pub const fn generated_outputs\\(\\) -> Self|parity_surface_constructors_return_canonical_tokens" packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs
! rg -n "\\.unwrap\\(|expect\\(|LazyLock|std::fs|std::process|std::env|SystemTime|Instant|include_str!|build.rs" packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs
git diff --check -- packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs packages/slic3r-rust/crates/slic3r_contracts/tests/flavor_contracts.rs
```
</verification>

<success_criteria>
- `ParitySurface` exposes public `const fn` constructors for every canonical token in `packages/parity/status.tsv`.
- The constructors are tested against `ParitySurface::try_from(...)`.
- `ParitySurface` remains a private-field validated newtype.
- No side-effecting registry code or runtime parsing path is added to `slic3r_contracts`.
- The pinned Rust 1.94.1 fmt, clippy, build, and test commands required by `AGENTS.md` are run before any commit that includes this Rust change.
</success_criteria>

<output>
After completion, create `.planning/phases/35-flavor-registry-boundary/35-01-SUMMARY.md` with `requirements-completed: []` using the hyphenated key. This plan supports ARCH-03 but does not materially complete the full requirement by itself. Do not run `mdformat` over the summary.
</output>
