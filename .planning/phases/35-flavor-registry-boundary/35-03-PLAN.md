---
phase: 35-flavor-registry-boundary
plan: "03"
type: execute
wave: 3
depends_on: ["35-02"]
files_modified:
  - packages/slic3r-rust/README.md
  - docs/port/README.md
  - docs/port/contract-inventory.md
  - docs/port/package-map.md
autonomous: true
requirements: [ARCH-02, ARCH-03]
requirements_addressed: [ARCH-02, ARCH-03]
user_setup: []
must_haves:
  truths:
    - "Developer can discover that `slic3r_flavors` is the flavor registry boundary without reading the implementation first."
    - "Docs state that base Slic3r behavior stays in shared core packages while future fork behavior plugs in through capability-oriented metadata."
    - "Docs state that registry entries are planning and architecture metadata only, not runtime fork parity, launcher selection, release behavior, online/cloud support, credential handling, or plugin ingestion."
    - "Docs trace the registry boundary back to Phase 32 vendor source refs, Phase 33 inventories, and Phase 34 typed contracts."
    - "Docs make clear that future fork work does not require PrusaSlicer, Bambu Studio, or OrcaSlicer-specific Rust workspaces or copied base behavior."
  artifacts:
    - path: "packages/slic3r-rust/README.md"
      provides: "Rust workspace discoverability for `slic3r_flavors` and its verification label"
      contains: "crates/slic3r_flavors/"
    - path: "docs/port/README.md"
      provides: "Port control-plane status for the current flavor registry boundary"
      contains: "Current Flavor Registry Boundary State"
    - path: "docs/port/contract-inventory.md"
      provides: "Contract inventory section for the registry boundary"
      contains: "Rust Flavor Registry Boundary"
    - path: "docs/port/package-map.md"
      provides: "Package ownership boundary note for Phase 35"
      contains: "Phase 35 adds `packages/slic3r-rust/crates/slic3r_flavors`"
  key_links:
    - from: "packages/slic3r-rust/README.md"
      to: "packages/slic3r-rust/crates/slic3r_flavors"
      via: "layout and verification command docs"
      pattern: "crates/slic3r_flavors/"
    - from: "docs/port/contract-inventory.md"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      via: "registry boundary description"
      pattern: "FlavorRegistryEntry|FlavorCapability|FlavorProvenance"
    - from: "docs/port/package-map.md"
      to: "packages/slic3r-rust"
      via: "package role and Phase 35 note"
      pattern: "slic3r_flavors"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 35-2026-05-27T11-24-13
generated_at: 2026-05-27T11:53:21Z
---

<objective>
Document the Phase 35 flavor registry boundary across the Rust package and port control-plane docs.

Purpose: ARCH-02 and ARCH-03 require the module boundary to be inspectable, not just implemented. The docs must prevent registry metadata from being mistaken for fork runtime parity or copied fork workspaces.
Output: Updated Rust package README and port docs that name `slic3r_flavors`, its API shape, source evidence, verification label, and metadata-only scope.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-flavor-registry-boundary/35-CONTEXT.md
@.planning/phases/35-flavor-registry-boundary/35-RESEARCH.md
@.planning/phases/34-rust-flavor-contracts/34-01-SUMMARY.md
@.planning/phases/35-flavor-registry-boundary/35-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/slic3r-rust/README.md
@packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
@docs/port/README.md
@docs/port/contract-inventory.md
@docs/port/package-map.md
@packages/fork-vendors/forks.tsv
@packages/fork-inventories/category-map.tsv

<interfaces>
Document these public registry API names from Plan 35-02:

```rust
FlavorRegistryEntry
FlavorCapability
FlavorProvenance
all_flavors()
maybe_flavor(FlavorId)
all_capabilities()
capabilities_by_origin(FeatureOrigin)
capabilities_by_checklist_status(ChecklistStatus)
```

Use these exact phrases where specified by task actions:

```text
Rust flavor registry entries are planning and architecture metadata only.
They do not mark fork behavior as verified or supported.
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage |
|----------|------|------|----------|
| D-01 | 35-03 | 1,2 | Full in docs - names `slic3r_flavors` as the registry composition crate. |
| D-02 | 35-03 | 1,2 | Full in docs - distinguishes `slic3r_contracts` vocabulary from registry composition. |
| D-03 | 35-03 | 1,2 | Full in docs - states no Prusa/Bambu/Orca-specific Rust workspaces or copied base behavior. |
| D-04 | 35-03 | 2 | Full in docs - documents `FlavorRegistryEntry`, `FlavorCapability`, and capability-oriented metadata. |
| D-05 | 35-03 | 2 | Full in docs - describes `FeatureOrigin` taxonomy rather than untyped buckets. |
| D-06 | 35-03 | 2 | Full in docs - describes capability/provenance traceability to source refs and inventory rows. |
| D-07 | 35-03 | 1,2 | Full in docs - lists accessors and filtering helpers as metadata queries only. |
| D-08 | 35-03 | 1,2 | Full in docs - states registry data is static Rust metadata, not runtime TSV parsing. |
| D-09 | 35-03 | 2 | Full in docs - identifies `forks.tsv`, inventories, and `category-map.tsv` as source evidence. |
| D-10 | 35-03 | 1,2 | Full in docs - lists forbidden side effects. |
| D-11 | 35-03 | 2 | Full in docs - generator/build-script expansion remains future work if volume warrants it. |
| D-14 | 35-03 | 1,2 | Full - updates Rust README and port docs for discoverability. |
| D-15 | 35-03 | 1,2 | Full - docs explicitly state entries do not mark fork behavior verified or supported. |
</decision_coverage>

<dependency_graph>
| Unit | Needs | Creates | Wave |
|------|-------|---------|------|
| Rust package docs | Plan 35-02 crate/API labels | Workspace README links, layout note, and verification command | 3 |
| Port control-plane docs | Plan 35-02 API and static registry evidence | Discoverable module boundary and metadata-only language | 3 |
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Publish the Rust workspace registry boundary</name>
  <files>packages/slic3r-rust/README.md, docs/port/README.md</files>
  <read_first>
    - `packages/slic3r-rust/README.md`
    - `docs/port/README.md`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs`
    - `.planning/phases/35-flavor-registry-boundary/35-CONTEXT.md`
    - `.planning/phases/35-flavor-registry-boundary/35-02-SUMMARY.md`
    - `AGENTS.md`
  </read_first>
  <action>Update `packages/slic3r-rust/README.md` in the existing layout and command style. Add `crates/slic3r_flavors/` to the Layout list with wording that it is a pure, static flavor registry crate that depends on `slic3r_contracts` and composes base, shared downstream, and fork-specific capability metadata. Add the package-local test label `//packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test` under Bazel commands. Add a short Notes bullet that says the registry is static metadata only and does not implement runtime fork behavior, launcher dispatch, fork-flavor release builds, online/cloud integration, credential handling, non-free plugin ingestion, or executable fork parity.

Update `docs/port/README.md` with a concise `## Current Flavor Registry Boundary State` section near the existing current-state sections. State that `packages/slic3r-rust/crates/slic3r_flavors` consumes Phase 34 typed contracts and exposes `FlavorRegistryEntry`, `FlavorCapability`, `FlavorProvenance`, `all_flavors()`, `maybe_flavor(FlavorId)`, and capability filtering helpers. Include the exact sentences from `<interfaces>`: `Rust flavor registry entries are planning and architecture metadata only.` and `They do not mark fork behavior as verified or supported.` Also state that no Git, filesystem, network, process, environment, clock, release, launcher-dispatch, runtime TSV parsing, online/cloud, credential, or plugin ingestion behavior belongs in this boundary.</action>
  <verify>
    <automated>rg -n "crates/slic3r_flavors/|//packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test|Current Flavor Registry Boundary State|FlavorRegistryEntry|FlavorCapability|FlavorProvenance|all_flavors\\(\\)|maybe_flavor\\(FlavorId\\)|Rust flavor registry entries are planning and architecture metadata only|They do not mark fork behavior as verified or supported" packages/slic3r-rust/README.md docs/port/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/slic3r-rust/README.md` contains `crates/slic3r_flavors/`.
    - `packages/slic3r-rust/README.md` contains `//packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test`.
    - `docs/port/README.md` contains `## Current Flavor Registry Boundary State`.
    - `docs/port/README.md` contains `FlavorRegistryEntry`, `FlavorCapability`, `FlavorProvenance`, `all_flavors()`, and `maybe_flavor(FlavorId)`.
    - Both exact metadata-only sentences from `<interfaces>` appear in `docs/port/README.md`.
    - Docs do not describe the registry as runtime dispatch, launcher selection, supported fork behavior, verified fork behavior, release behavior, online/cloud support, credential handling, or plugin ingestion.
  </acceptance_criteria>
  <done>The Rust workspace README and port overview make the registry boundary discoverable with explicit metadata-only scope.</done>
</task>

<task type="auto">
  <name>Task 2: Update contract inventory and package ownership docs</name>
  <files>docs/port/contract-inventory.md, docs/port/package-map.md</files>
  <read_first>
    - `docs/port/contract-inventory.md`
    - `docs/port/package-map.md`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs`
    - `packages/fork-vendors/forks.tsv`
    - `packages/fork-inventories/category-map.tsv`
    - `.planning/REQUIREMENTS.md`
    - `.planning/phases/35-flavor-registry-boundary/35-CONTEXT.md`
    - `.planning/phases/35-flavor-registry-boundary/35-RESEARCH.md`
    - `.planning/phases/35-flavor-registry-boundary/35-02-SUMMARY.md`
    - `AGENTS.md`
  </read_first>
  <action>Update `docs/port/contract-inventory.md` by adding a `## Rust Flavor Registry Boundary` section near the existing Rust flavor contract section. Include these exact public API names: `FlavorRegistryEntry`, `FlavorCapability`, `FlavorProvenance`, `all_flavors()`, `maybe_flavor(FlavorId)`, `all_capabilities()`, `capabilities_by_origin(FeatureOrigin)`, and `capabilities_by_checklist_status(ChecklistStatus)`. State that capability records use typed `FlavorId`, `VendorSourceRef`, `FeatureOrigin`, `ParitySurface`, and `ChecklistStatus` values. State that source-reference and ownership traceability lives on capability/provenance records and traces to `packages/fork-vendors/forks.tsv`, `packages/fork-inventories/*.tsv`, and `packages/fork-inventories/category-map.tsv`.

Also include the exact sentences: `Rust flavor registry entries are planning and architecture metadata only.` and `They do not mark fork behavior as verified or supported.` State that current needs-review metadata is represented by `ChecklistStatus::NeedsReview` on source-backed rows such as `orcaslicer.calibration-flow`; do not claim an inventory-backed `FeatureOrigin::UnknownNeedsReview` row exists unless a TSV row actually exists. State that generated Rust registry data, fork parity checklist templates, fixture namespaces, launcher-shape wording, drift-refresh protocol templates, runtime fork behavior, fork-specific CLI dispatch, fork release builds, online/cloud integration, credential handling, and non-free plugin support remain deferred.

Update `docs/port/package-map.md` in the package table and notes. In the `packages/slic3r-rust` package role, mention separate implementation, contract, CLI, and flavor-registry crate boundaries. Add a Phase 35 note containing this exact string: `Phase 35 adds `packages/slic3r-rust/crates/slic3r_flavors``. State that base Slic3r behavior remains in shared core packages and future fork behavior plugs in through capability-oriented metadata, not PrusaSlicer, Bambu Studio, or OrcaSlicer-specific Rust workspaces or copied base behavior. Preserve the Phase 32/33/34 no-runtime-parity and source-intake boundaries.</action>
  <verify>
    <automated>rg -n "Rust Flavor Registry Boundary|FlavorRegistryEntry|FlavorCapability|FlavorProvenance|all_flavors\\(\\)|maybe_flavor\\(FlavorId\\)|capabilities_by_origin\\(FeatureOrigin\\)|capabilities_by_checklist_status\\(ChecklistStatus\\)|Rust flavor registry entries are planning and architecture metadata only|They do not mark fork behavior as verified or supported|orcaslicer\\.calibration-flow|Phase 35 adds `packages/slic3r-rust/crates/slic3r_flavors`|capability-oriented metadata" docs/port/contract-inventory.md docs/port/package-map.md && git diff --check -- packages/slic3r-rust/README.md docs/port/README.md docs/port/contract-inventory.md docs/port/package-map.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/port/contract-inventory.md` contains `## Rust Flavor Registry Boundary`.
    - `docs/port/contract-inventory.md` names all public API names listed in the action.
    - `docs/port/contract-inventory.md` states capability records use typed `FlavorId`, `VendorSourceRef`, `FeatureOrigin`, `ParitySurface`, and `ChecklistStatus` values.
    - `docs/port/contract-inventory.md` names `packages/fork-vendors/forks.tsv`, `packages/fork-inventories/*.tsv`, and `packages/fork-inventories/category-map.tsv`.
    - `docs/port/contract-inventory.md` contains the exact needs-review distinction for `ChecklistStatus::NeedsReview` and `orcaslicer.calibration-flow`, without claiming current inventory-backed `FeatureOrigin::UnknownNeedsReview` evidence.
    - `docs/port/package-map.md` contains `Phase 35 adds `packages/slic3r-rust/crates/slic3r_flavors``.
    - `docs/port/package-map.md` states future fork behavior uses capability-oriented metadata, not vendor-specific Rust workspaces or copied base behavior.
    - `git diff --check` passes for all four documentation files.
  </acceptance_criteria>
  <done>Port docs publish the module boundary, source traceability, metadata-only scope, and no-workspace-copy architecture required by ARCH-02 and ARCH-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry implementation -> docs | Public API and scope language must match the actual crate without inventing unimplemented behavior. |
| Source-observed metadata -> reviewer interpretation | Docs can accidentally make source pins, inventories, or registry entries sound like verified runtime parity. |
| Needs-review inventory rows -> unknown-origin vocabulary | Docs must not invent source-backed `unknown-needs-review` ownership evidence that current TSVs do not contain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03-01 | Spoofing | Docs naming of registry state | mitigate | Use exact public API names from Plan 35-02 and exact metadata-only sentences so docs do not imply hidden runtime support. |
| T-35-03-02 | Repudiation | Runtime parity claims | mitigate | State that registry entries do not mark fork behavior verified or supported, and list deferred runtime/fork/release/cloud/credential/plugin scopes. |
| T-35-03-03 | Tampering | Needs-review evidence wording | mitigate | Document `ChecklistStatus::NeedsReview` rows such as `orcaslicer.calibration-flow` and explicitly avoid claiming current inventory-backed `FeatureOrigin::UnknownNeedsReview` evidence. |
| T-35-03-04 | Information Disclosure | Network/cloud/plugin scope | mitigate | Docs preserve that network, cloud, credential, and non-free plugin rows are cautioned metadata only and add no integration behavior. |
| T-35-03-05 | Elevation of Privilege | Vendor-specific Rust workspace implication | mitigate | Docs state future fork behavior plugs into shared packages through capability-oriented metadata, not copied base behavior or vendor-specific workspaces. |
</threat_model>

<verification>
Overall verification for this plan:

```bash
rg -n "crates/slic3r_flavors/|//packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test|Current Flavor Registry Boundary State|Rust Flavor Registry Boundary|FlavorRegistryEntry|FlavorCapability|FlavorProvenance|all_flavors\\(\\)|maybe_flavor\\(FlavorId\\)|capabilities_by_origin\\(FeatureOrigin\\)|capabilities_by_checklist_status\\(ChecklistStatus\\)|Rust flavor registry entries are planning and architecture metadata only|They do not mark fork behavior as verified or supported|Phase 35 adds `packages/slic3r-rust/crates/slic3r_flavors`" packages/slic3r-rust/README.md docs/port/README.md docs/port/contract-inventory.md docs/port/package-map.md
git diff --check -- packages/slic3r-rust/README.md docs/port/README.md docs/port/contract-inventory.md docs/port/package-map.md
rustup run 1.94.1 cargo fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check
rustup run 1.94.1 cargo clippy --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features -- -D warnings
rustup run 1.94.1 cargo build --manifest-path packages/slic3r-rust/Cargo.toml --all-targets --all-features
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features
```
</verification>

<success_criteria>
- ARCH-02 is materially satisfied in docs by a documented module boundary that keeps base Slic3r behavior in shared core packages and routes future fork behavior through capability-oriented metadata.
- ARCH-03 is materially satisfied in docs by a pure registry boundary that maps metadata without side-effecting operations.
- Docs explicitly avoid claiming runtime fork parity, fork-specific CLI dispatch, fork release builds, online/cloud integration, credential handling, non-free plugin ingestion, or verified/support status for registry entries.
- Documentation references current registry APIs and source evidence accurately.
- The pinned Rust 1.94.1 fmt, clippy, build, and test commands required by `AGENTS.md` are run before any commit that includes this phase's Rust/docs changes.
</success_criteria>

<output>
After completion, create `.planning/phases/35-flavor-registry-boundary/35-03-SUMMARY.md` with `requirements-completed: [ARCH-02]` if the documented module boundary is complete, or `[ARCH-02, ARCH-03]` only if this plan is the final closeout summary for both requirements. Use the hyphenated key and do not run `mdformat` over the summary.
</output>
