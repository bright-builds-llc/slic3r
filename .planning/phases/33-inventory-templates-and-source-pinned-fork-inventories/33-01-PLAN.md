---
phase: 33-inventory-templates-and-source-pinned-fork-inventories
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/fork-inventories/BUILD.bazel
  - packages/fork-inventories/README.md
  - packages/fork-inventories/inventory-template.tsv
  - packages/fork-inventories/prusaslicer.tsv
  - packages/fork-inventories/bambustudio.tsv
  - packages/fork-inventories/orcaslicer.tsv
  - packages/fork-inventories/category-map.tsv
  - packages/fork-inventories/verify_inventories.sh
  - packages/fork-inventories/verify_inventories_test.sh
  - docs/port/README.md
  - docs/port/package-map.md
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04, INV-05]
requirements_addressed: [INV-01, INV-02, INV-03, INV-04, INV-05]
user_setup: []
must_haves:
  truths:
    - "Maintainer can copy a checked-in TSV inventory template with required source, ownership, surface, complexity, parity dependency, v1.9 decision, caution, and future parity fields."
    - "Maintainer can inspect PrusaSlicer, Bambu Studio, and OrcaSlicer source-pinned inventories anchored to Phase 32 selected stable tags and peeled commits."
    - "Maintainer can distinguish base Slic3r, shared downstream, fork-specific, and unknown-needs-review ownership without reading prose-only notes."
    - "Maintainer can inspect a cross-fork category map where every inventory row appears exactly once and stale or duplicate row references fail verification."
    - "Maintainer can run Bazel-owned inventory verification without fetching, cloning, building, or importing upstream fork source trees."
    - "Port docs state that inventories are source-observed planning inputs only and do not prove runtime fork parity or network/plugin support."
  artifacts:
    - path: "packages/fork-inventories/inventory-template.tsv"
      provides: "Reusable inventory row contract"
      contains: "inventory_id\tvendor_id\tsource_ref\tsource_paths\tfeature_surface\tfeature_category\townership\tcomplexity\tparity_dependency\tv1_9_decision\tcaution_flags\tfuture_parity_notes"
    - path: "packages/fork-inventories/prusaslicer.tsv"
      provides: "PrusaSlicer source-pinned inventory"
      contains: "prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961"
    - path: "packages/fork-inventories/bambustudio.tsv"
      provides: "Bambu Studio source-pinned inventory"
      contains: "bambustudio:v02.06.00.51@b506005bc4ee62124e24bf00e0f58656db3646a6"
    - path: "packages/fork-inventories/orcaslicer.tsv"
      provides: "OrcaSlicer source-pinned inventory"
      contains: "orcaslicer:v2.3.2@c724a3f5f51c52336624b689e846c8fbc943a912"
    - path: "packages/fork-inventories/category-map.tsv"
      provides: "Cross-fork row grouping and implementation/deferred separation"
      contains: "map_id\tfeature_category\townership\tv1_9_decision\tinventory_ids\tnotes"
    - path: "packages/fork-inventories/verify_inventories.sh"
      provides: "Bash verifier for TSV shape, pins, enums, coverage, parity dependencies, and category references"
      contains: "set -euo pipefail"
    - path: "packages/fork-inventories/verify_inventories_test.sh"
      provides: "Verifier behavior tests"
      contains: "Arrange"
    - path: "packages/fork-inventories/BUILD.bazel"
      provides: "Bazel verify command and sh_test"
      contains: "verify_inventories_test"
  key_links:
    - from: "packages/fork-inventories/verify_inventories.sh"
      to: "packages/fork-vendors/forks.tsv"
      via: "source_ref derived from selected_stable_tag and peeled_commit_sha"
      pattern: "selected_stable_tag.*peeled_commit_sha|peeled_commit_sha.*selected_stable_tag"
    - from: "packages/fork-inventories/verify_inventories.sh"
      to: "packages/parity/status.tsv"
      via: "parity_dependency membership validation"
      pattern: "parity_dependency"
    - from: "packages/fork-inventories/category-map.tsv"
      to: "packages/fork-inventories/*.tsv"
      via: "inventory_ids row references"
      pattern: "inventory_ids"
    - from: "docs/port/README.md"
      to: "packages/fork-inventories"
      via: "port documentation package link and command"
      pattern: "//packages/fork-inventories:verify"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 33-2026-05-26T17-23-32
generated_at: 2026-05-26T17:42:13Z
---

<objective>
Create the Phase 33 fork inventory package as source-pinned maintainer data, not runtime fork support.

Purpose: Maintainers need a validated inventory contract and initial fork rows before later architecture and parity phases can reason about downstream fork behavior.
Output: `packages/fork-inventories` with TSV inventories, a cross-fork category map, a Bazel verifier/test target, package-local docs, and port-doc links.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-inventory-templates-and-source-pinned-fork-inventories/33-CONTEXT.md
@.planning/phases/33-inventory-templates-and-source-pinned-fork-inventories/33-RESEARCH.md
@.planning/phases/32-vendor-source-manifest-and-license-baseline/32-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/fork-vendors/forks.tsv
@packages/fork-vendors/BUILD.bazel
@packages/fork-vendors/verify_forks.sh
@packages/fork-vendors/README.md
@packages/parity/status.tsv
@packages/parity/BUILD.bazel
@packages/parity/README.md
@docs/port/README.md
@docs/port/package-map.md
@docs/port/migration-guidance.md
@docs/port/contract-inventory.md

<interfaces>
Existing vendor source registry contract from `packages/fork-vendors/forks.tsv`:

```text
# vendor_id	display_name	official_repo_url	selected_stable_tag	tag_kind	tag_ref_sha	tag_object_sha	peeled_commit_sha	default_branch	observed_default_branch_head	capture_date_utc	lineage_ids	source_paths	refresh_command	spdx_identifier	observed_license_id	license_source	attribution_notes	provenance_notes	caution_flags	caution_notes
```

Accepted source refs for inventory rows:

```text
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
bambustudio:v02.06.00.51@b506005bc4ee62124e24bf00e0f58656db3646a6
orcaslicer:v2.3.2@c724a3f5f51c52336624b689e846c8fbc943a912
```

Existing parity dependency vocabulary comes from `packages/parity/status.tsv` column 1:

```text
# surface	status	evidence	notes
```

New inventory TSV contract to create:

```text
# inventory_id	vendor_id	source_ref	source_paths	feature_surface	feature_category	ownership	complexity	parity_dependency	v1_9_decision	caution_flags	future_parity_notes
```

New category map TSV contract to create:

```text
# map_id	feature_category	ownership	v1_9_decision	inventory_ids	notes
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage |
|----------|------|------|----------|
| D-01 | 33-01 | 1,2,3 | Full - dedicated `packages/fork-inventories` package separate from `packages/fork-vendors`. |
| D-02 | 33-01 | 1,2 | Full - checked-in TSV files and shell verification, no parser dependency. |
| D-03 | 33-01 | 1 | Full - template TSV, one TSV per fork, and category map TSV. |
| D-04 | 33-01 | 1,2 | Full - required row fields are created and verifier-enforced. |
| D-05 | 33-01 | 1,2 | Full - source refs use selected tags and peeled commits from Phase 32 only. |
| D-06 | 33-01 | 1,2 | Full - enum-like fields are documented and verifier-enforced. |
| D-07 | 33-01 | 1,2 | Full - ownership enum is `base-slic3r`, `shared-downstream`, `fork-specific`, `unknown-needs-review`. |
| D-08 | 33-01 | 1,2 | Full - decisions are `future-candidate`, `deferred`, `no-action-base`, `needs-review`, and category-map rows keep deferred rows separate. |
| D-09 | 33-01 | 1,2,3 | Full - network/cloud/plugin/credential/non-free rows are caution-only deferred rows, docs reject runtime scope. |
| D-10 | 33-01 | 1,2 | Full - PrusaSlicer rows separate base, shared downstream, and Prusa-specific surfaces. |
| D-11 | 33-01 | 1,2 | Full - Bambu rows cover project, profile, network, support, STEP, arc, and assembly surfaces. |
| D-12 | 33-01 | 1,2 | Full - Orca rows cover calibration, wall/seam, support, adaptive mesh, profile library, and community-profile surfaces. |
| D-13 | 33-01 | 1,2 | Full - category-map groups rows and machine-checks references. |
| D-14 | 33-01 | 2 | Full - Bazel verifier checks shape, enums, vendors, pins, coverage, and cross-map references without upstream network access. |
| D-15 | 33-01 | 1,3 | Full - package and port docs state inventories are planning inputs only and not executable parity. |
| D-16 | 33-01 | 3 | Full - `docs/port/README.md` and `docs/port/package-map.md` link the package and command. |
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Create source-pinned inventory TSVs and package-local contract docs</name>
  <files>packages/fork-inventories/README.md, packages/fork-inventories/inventory-template.tsv, packages/fork-inventories/prusaslicer.tsv, packages/fork-inventories/bambustudio.tsv, packages/fork-inventories/orcaslicer.tsv, packages/fork-inventories/category-map.tsv</files>
  <read_first>
    - `.planning/phases/33-inventory-templates-and-source-pinned-fork-inventories/33-CONTEXT.md`
    - `.planning/phases/33-inventory-templates-and-source-pinned-fork-inventories/33-RESEARCH.md`
    - `packages/fork-vendors/forks.tsv`
    - `packages/fork-vendors/README.md`
    - `packages/parity/status.tsv`
    - `docs/port/migration-guidance.md`
  </read_first>
  <action>Create `packages/fork-inventories/` and add the five TSV data files per D-01 through D-03. Use the exact inventory header `# inventory_id	vendor_id	source_ref	source_paths	feature_surface	feature_category	ownership	complexity	parity_dependency	v1_9_decision	caution_flags	future_parity_notes` for the template and per-fork inventory files. Use the exact category-map header `# map_id	feature_category	ownership	v1_9_decision	inventory_ids	notes`. Use literal tabs, LF rows, semicolon-delimited multi-value fields with no spaces around semicolons, and no blank required fields.

For inventory rows, use these enum values per D-06 through D-08: `ownership` = `base-slic3r`, `shared-downstream`, `fork-specific`, `unknown-needs-review`; `complexity` = `none`, `low`, `medium`, `high`, `unknown-needs-review`; `v1_9_decision` = `future-candidate`, `deferred`, `no-action-base`, `needs-review`; `caution_flags` = `none` or a semicolon list from `network-scope`, `cloud-scope`, `credential-scope`, `non-free-plugin-scope`, `license-provenance`, `runtime-parity-not-verified`. Use `parity_dependency` = `none` or semicolon-delimited surface values from `packages/parity/status.tsv` column 1.

Populate `prusaslicer.tsv` with rows anchored to `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, including at least `base-core`, `project-file`, `profile-schema`, and a source-observed `network-device` caution row for `src/slic3r/Utils/PrusaConnect.cpp`. The Prusa network row must have `v1_9_decision` = `deferred`, include `network-scope;credential-scope;runtime-parity-not-verified`, and say `Inventory only; no online integration, credential handling, device communication, or runtime fork support in v1.9.` in `future_parity_notes`.

Populate `bambustudio.tsv` with rows anchored to `bambustudio:v02.06.00.51@b506005bc4ee62124e24bf00e0f58656db3646a6`, covering `project-file`, `profile-schema`, `network-device`, `support-generation`, `step-import`, `arc-fitting`, and `assembly-workflow` per D-11. Include inherited/shared rows where the source paths represent Prusa-family or shared downstream surfaces, and mark network/cloud/plugin/credential rows as deferred caution-only rows per D-09.

Populate `orcaslicer.tsv` with rows anchored to `orcaslicer:v2.3.2@c724a3f5f51c52336624b689e846c8fbc943a912`, covering `calibration-flow`, `wall-seam`, `support-generation`, `adaptive-mesh`, `profile-library`, and `community-profile` per D-12. Include an inherited network/plugin caution row only as deferred inventory metadata if sourced from the pinned tree or Phase 32 caution evidence; do not claim online integration or runtime support.

Populate `category-map.tsv` so every inventory row ID from the three per-fork TSVs appears in exactly one `inventory_ids` list. Do not group `deferred` rows with `future-candidate` rows. Add `packages/fork-inventories/README.md` with the exact command phrase `bazel run //packages/fork-inventories:verify`, the exact boundary phrase `Inventories are source-observed planning inputs only. They do not prove executable parity, user-facing behavior, GUI support, fork release builds, or support for non-free/network plugins.`, the schema/enums above, and a note that selected stable tags and peeled commits from `packages/fork-vendors/forks.tsv` are the only accepted row source refs.</action>
  <verify>
    <automated>bash -c 'test -f packages/fork-inventories/inventory-template.tsv && test -f packages/fork-inventories/prusaslicer.tsv && test -f packages/fork-inventories/bambustudio.tsv && test -f packages/fork-inventories/orcaslicer.tsv && test -f packages/fork-inventories/category-map.tsv && awk -F "\t" "BEGIN { bad=0 } /^#/ || NF == 0 { next } NF != 12 { print FILENAME \":\" NR \": expected 12 fields, got \" NF; bad=1 } END { exit bad }" packages/fork-inventories/inventory-template.tsv packages/fork-inventories/prusaslicer.tsv packages/fork-inventories/bambustudio.tsv packages/fork-inventories/orcaslicer.tsv && awk -F "\t" "BEGIN { bad=0 } /^#/ || NF == 0 { next } NF != 6 { print FILENAME \":\" NR \": expected 6 fields, got \" NF; bad=1 } END { exit bad }" packages/fork-inventories/category-map.tsv && rg -n "prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961|bambustudio:v02.06.00.51@b506005bc4ee62124e24bf00e0f58656db3646a6|orcaslicer:v2.3.2@c724a3f5f51c52336624b689e846c8fbc943a912|Inventories are source-observed planning inputs only" packages/fork-inventories'</automated>
  </verify>
  <acceptance_criteria>
    - `packages/fork-inventories/inventory-template.tsv` exists and uses the exact 12-column header.
    - `prusaslicer.tsv`, `bambustudio.tsv`, and `orcaslicer.tsv` use the same 12-column header and contain only Phase 32 selected tag plus peeled commit source refs.
    - Bambu rows include `project-file`, `profile-schema`, `network-device`, `support-generation`, `step-import`, `arc-fitting`, and `assembly-workflow`.
    - Orca rows include `calibration-flow`, `wall-seam`, `support-generation`, `adaptive-mesh`, `profile-library`, and `community-profile`.
    - The Prusa network row is present only as a deferred source-observed caution row and its note contains `Inventory only; no online integration, credential handling, device communication, or runtime fork support in v1.9.`
    - `category-map.tsv` contains every per-fork inventory ID exactly once, with deferred rows separated from future-candidate rows.
    - README includes `bazel run //packages/fork-inventories:verify` and the exact source-observed planning-inputs boundary phrase.
  </acceptance_criteria>
  <done>The package-local data contract and initial inventories satisfy INV-01 through INV-05 data coverage without creating upstream import, clone/vendor/build, or runtime fork parity scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Bash verifier, Bazel target, and verifier behavior tests</name>
  <files>packages/fork-inventories/BUILD.bazel, packages/fork-inventories/verify_inventories.sh, packages/fork-inventories/verify_inventories_test.sh</files>
  <read_first>
    - `packages/fork-vendors/BUILD.bazel`
    - `packages/fork-vendors/verify_forks.sh`
    - `packages/fork-vendors/verify_forks_test.sh`
    - `packages/parity/BUILD.bazel`
    - `packages/fork-inventories/inventory-template.tsv`
    - `packages/fork-inventories/prusaslicer.tsv`
    - `packages/fork-inventories/bambustudio.tsv`
    - `packages/fork-inventories/orcaslicer.tsv`
    - `packages/fork-inventories/category-map.tsv`
  </read_first>
  <behavior>
    - Test 1: A valid fixture with one vendor, one inventory row, one matching category-map reference, and one valid parity dependency passes.
    - Test 2: A row whose `source_ref` uses an observed default branch head or a wrong commit fails with a source-ref error.
    - Test 3: An unknown `vendor_id`, an invalid enum value, an empty required field, or a bad 12-column inventory row fails.
    - Test 4: A stale category-map row reference fails.
    - Test 5: An inventory row missing from `category-map.tsv` or appearing in two category-map rows fails.
    - Test 6: A `parity_dependency` not present in `packages/parity/status.tsv` column 1 fails.
    - Test 7: Missing required Bambu or Orca feature-surface coverage fails.
    - Test 8: A network/cloud/credential/non-free caution row that is not `deferred` or lacks `runtime-parity-not-verified` fails.
  </behavior>
  <action>Create `packages/fork-inventories/BUILD.bazel` following the `packages/fork-vendors` and `packages/parity` `sh_binary`/`sh_test` pattern. Export the README and TSV files. Add `sh_binary(name = "verify")` with `srcs = ["verify_inventories.sh"]`, data entries for the five TSV files plus `//packages/fork-vendors:forks.tsv` and `//packages/parity:status.tsv`, and args passing those locations in this order: template, prusaslicer, bambustudio, orcaslicer, category-map, forks, parity status. Add `sh_test(name = "verify_inventories_test")` with `srcs = ["verify_inventories_test.sh"]` and data `["verify_inventories.sh"]`. Add a `package_boundary` filegroup including README, all TSV files, and both scripts.

Write `verify_inventories_test.sh` first. Use `#!/usr/bin/env bash`, `set -euo pipefail`, temp directories, explicit `# Arrange`, `# Act`, and `# Assert` comments, and focused helper assertions. The tests must invoke `verify_inventories.sh` directly with fixture files and prove the failure modes listed in `<behavior>` before the implementation is considered complete.

Implement `verify_inventories.sh` in Bash 3.2-compatible style. Use small helper functions with early failures like `error`, `validate_required_fields`, `validate_no_spaced_semicolons`, `validate_enum`, and `validate_inventory_file`. Do not use Bash 4 associative arrays, `mapfile`, `eval`, `source`, `xargs`, `git`, `curl`, `ssh`, `ls-remote`, `clone`, `fetch`, or any upstream network access. It may use `awk`, `sort`, `uniq`, `grep`, and temp files for set membership. Validate: LF-only rows, exact field counts, non-empty required fields, enum values, vendor-prefixed globally unique `inventory_id`, `vendor_id` membership from `forks.tsv`, exact `source_ref` = `<vendor_id>:<selected_stable_tag>@<peeled_commit_sha>`, `source_ref` not equal to any `observed_default_branch_head`, non-empty `source_paths`, parity dependencies from `status.tsv` or `none`, caution flag enums, network/cloud/credential/non-free caution rows are `deferred` and include `runtime-parity-not-verified`, required surface coverage for each fork, `category-map.tsv` shape, category-map row references exist, every inventory ID appears exactly once in the category map, and grouped map rows share the same `ownership` and `v1_9_decision` as referenced rows.</action>
  <verify>
    <automated>bash -n packages/fork-inventories/verify_inventories.sh && bash -n packages/fork-inventories/verify_inventories_test.sh && bash packages/fork-inventories/verify_inventories_test.sh && bazel test //packages/fork-inventories:verify_inventories_test && bazel run //packages/fork-inventories:verify && ! rg -n "\\b(git|curl|ssh|ls-remote|clone|fetch|eval|source|xargs)\\b" packages/fork-inventories/verify_inventories.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bazel test //packages/fork-inventories:verify_inventories_test` passes.
    - `bazel run //packages/fork-inventories:verify` passes against the checked-in Phase 33 TSV files.
    - `verify_inventories.sh` performs no upstream network, clone, fetch, source import, or command-evaluation behavior.
    - The verifier fails stale category-map references, duplicate row references, missing row references, unknown vendor IDs, source refs not matching Phase 32 selected tag plus peeled commit, invalid enums, unknown parity dependencies, missing required surfaces, and unsafe network/plugin caution rows.
    - `BUILD.bazel` exposes public `verify`, `verify_inventories_test`, exported TSV/README files, and `package_boundary`.
  </acceptance_criteria>
  <done>The Bazel-owned verifier and tests enforce the inventory contract, source pin boundary, coverage, and exact-once category-map references with no upstream network access.</done>
</task>

<task type="auto">
  <name>Task 3: Link the inventory boundary from port documentation</name>
  <files>docs/port/README.md, docs/port/package-map.md</files>
  <read_first>
    - `docs/port/README.md`
    - `docs/port/package-map.md`
    - `packages/fork-inventories/README.md`
    - `packages/fork-inventories/BUILD.bazel`
    - `.planning/REQUIREMENTS.md`
  </read_first>
  <action>Update `docs/port/README.md` with a new current-state section or bullets for Phase 33. Include these exact phrases: `packages/fork-inventories`, `bazel run //packages/fork-inventories:verify`, `source-pinned feature inventory templates and per-fork inventory TSVs`, and `Inventories are source-observed planning inputs only. They do not prove executable parity, user-facing behavior, GUI support, fork release builds, or support for non-free/network plugins.` Also state that the verifier validates TSV shape, source pins, enum values, required surface coverage, parity dependencies, and category-map references without fetching, cloning, building, or importing upstream fork source trees.

Update `docs/port/package-map.md` with a package table row for `packages/fork-inventories` and a Phase 33 note. The package role must say it owns feature inventory templates, PrusaSlicer/Bambu Studio/OrcaSlicer source-pinned inventory TSVs, the cross-fork category map, and inventory verification. The Phase 33 note must preserve the Phase 32 boundary: no upstream source import, no clone/vendor/build, no runtime fork parity claim, no online/cloud integration, no credential handling, and no non-free plugin ingestion.</action>
  <verify>
    <automated>rg -n "packages/fork-inventories|//packages/fork-inventories:verify|source-pinned feature inventory templates and per-fork inventory TSVs|Inventories are source-observed planning inputs only|without fetching, cloning, building, or importing upstream fork source trees|no runtime fork parity claim" docs/port/README.md docs/port/package-map.md packages/fork-inventories/README.md && git diff --check -- packages/fork-inventories/BUILD.bazel packages/fork-inventories/README.md packages/fork-inventories/inventory-template.tsv packages/fork-inventories/prusaslicer.tsv packages/fork-inventories/bambustudio.tsv packages/fork-inventories/orcaslicer.tsv packages/fork-inventories/category-map.tsv packages/fork-inventories/verify_inventories.sh packages/fork-inventories/verify_inventories_test.sh docs/port/README.md docs/port/package-map.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/port/README.md` links the new package and exact Bazel command.
    - `docs/port/package-map.md` includes a package table row and Phase 33 note for `packages/fork-inventories`.
    - Both port docs use the source-observed planning-inputs boundary and do not claim executable parity, runtime support, GUI support, fork release builds, online/cloud integration, credential handling, or non-free plugin ingestion.
    - No managed Bright Builds block or managed `AGENTS.bright-builds.md` content is edited.
  </acceptance_criteria>
  <done>Reviewers can find the Phase 33 inventory package and command from the port docs, and the docs preserve the intake-only boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer-edited TSV -> verifier | Human-edited inventory data crosses into shell validation and Bazel test/run targets. |
| Phase 32 vendor registry -> Phase 33 inventories | Source pins and caution metadata from `packages/fork-vendors/forks.tsv` become accepted inventory evidence. |
| Inventory data/docs -> reviewer interpretation | Source-observed rows can be misread as executable parity or runtime support if wording is loose. |
| Bazel shell target -> local environment | Shell scripts run locally and must not execute TSV content or contact upstream remotes. |
| Category map -> per-fork inventories | Cross-fork references can drift from row IDs unless machine-checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `*.tsv` inventory rows | mitigate | `verify_inventories.sh` rejects CR characters, bad field counts, empty required fields, invalid enums, spaced semicolon delimiters, duplicate inventory IDs, and unknown vendor IDs. |
| T-33-02 | Spoofing | `source_ref` fields | mitigate | Verifier derives the exact accepted source ref from `packages/fork-vendors/forks.tsv` as `<vendor_id>:<selected_stable_tag>@<peeled_commit_sha>` and rejects default branch heads or wrong commits. |
| T-33-03 | Repudiation | Inventory and port docs wording | mitigate | README and port docs must include the exact source-observed planning-inputs phrase and must not claim executable parity, runtime support, GUI support, fork release builds, online integration, or plugin support. |
| T-33-04 | Information Disclosure | Network/cloud/plugin/credential rows | mitigate | Rows with network, cloud, credential, or non-free plugin caution flags must be `deferred`, include `runtime-parity-not-verified`, and remain inventory-only with no credential handling or upstream network behavior. |
| T-33-05 | Tampering | `category-map.tsv` references | mitigate | Verifier fails unknown row references, missing row references, duplicate row references, and map groups whose ownership or decision does not match referenced rows. |
| T-33-06 | Elevation of Privilege | Shell parsing of TSV content | mitigate | Verifier must not use `eval`, `source`, `xargs`, or execute TSV-derived content; it only parses fields with Bash/awk/grep/sort and exits on invalid data. |
| T-33-07 | Denial of Service | Oversized or malformed local TSV input | accept | Low severity for checked-in maintainer metadata; field-count, LF, and required-field checks bound the normal failure mode to fast local validation. |
</threat_model>

<verification>
Overall verification for this plan:

```bash
bash -n packages/fork-inventories/verify_inventories.sh
bash -n packages/fork-inventories/verify_inventories_test.sh
bash packages/fork-inventories/verify_inventories_test.sh
bazel test //packages/fork-inventories:verify_inventories_test
bazel run //packages/fork-inventories:verify
rg -n "packages/fork-inventories|//packages/fork-inventories:verify|Inventories are source-observed planning inputs only|without fetching, cloning, building, or importing upstream fork source trees" packages/fork-inventories/README.md docs/port/README.md docs/port/package-map.md
git diff --check -- packages/fork-inventories/BUILD.bazel packages/fork-inventories/README.md packages/fork-inventories/inventory-template.tsv packages/fork-inventories/prusaslicer.tsv packages/fork-inventories/bambustudio.tsv packages/fork-inventories/orcaslicer.tsv packages/fork-inventories/category-map.tsv packages/fork-inventories/verify_inventories.sh packages/fork-inventories/verify_inventories_test.sh docs/port/README.md docs/port/package-map.md
```
</verification>

<success_criteria>
- INV-01 through INV-05 are materially satisfied by checked-in TSV templates, per-fork inventories, the category map, the verifier, tests, and docs links.
- `packages/fork-inventories` exists as a separate package from `packages/fork-vendors`.
- Every inventory row uses a source ref pinned to the Phase 32 selected stable tag and peeled commit.
- Every inventory row appears exactly once in `category-map.tsv`; stale references and duplicate row references fail verification.
- `bazel test //packages/fork-inventories:verify_inventories_test` passes.
- `bazel run //packages/fork-inventories:verify` passes.
- No plan work imports, clones, vendors, builds, or fetches upstream fork source trees.
- No plan work claims executable fork parity, runtime fork support, online/cloud integration, credential handling, or non-free plugin ingestion.
</success_criteria>

<output>
After completion, create `.planning/phases/33-inventory-templates-and-source-pinned-fork-inventories/33-01-SUMMARY.md` with `requirements-completed: [INV-01, INV-02, INV-03, INV-04, INV-05]` using the hyphenated key. Do not run `mdformat` over the summary.
</output>
