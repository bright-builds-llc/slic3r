---
phase: 36-parity-fixture-launcher-and-deferral-templates
plan: "02"
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - docs/port/README.md
  - docs/port/migration-guidance.md
  - docs/port/parity-matrix.md
  - docs/port/package-map.md
  - packages/parity-fixtures/README.md
  - packages/parity/README.md
  - packages/fork-vendors/README.md
  - packages/fork-inventories/README.md
autonomous: true
requirements: [PAR-01, PAR-02, PAR-03, PAR-04]
requirements_addressed: [PAR-01, PAR-02, PAR-03, PAR-04]
user_setup: []
must_haves:
  truths:
    - "Maintainer can inspect one central v1.9 fork-parity deferral block that names every PAR-03 deferred scope."
    - "Maintainer can inspect fixture namespace policy for future fork fixtures without new fixture files in Phase 36."
    - "Maintainer can inspect parity-status policy that forbids v1.9 fork rows and reserves `verified` for future executable parity evidence."
    - "Maintainer can discover the checklist template and manual drift-refresh protocol from the port docs, vendor docs, and inventory docs."
  artifacts:
    - path: "docs/port/README.md"
      provides: "Central v1.9 fork-parity deferral block and current template-package state"
      contains: "v1.9 Fork Parity Deferrals"
    - path: "docs/port/package-map.md"
      provides: "Discoverable package-map entry for `packages/fork-templates`"
      contains: "packages/fork-templates"
    - path: "packages/parity-fixtures/README.md"
      provides: "Future fork fixture namespace policy"
      contains: "packages/parity-fixtures/forks/<vendor_id>/<inventory_id-or-slug>/<scenario>/"
    - path: "packages/parity/README.md"
      provides: "Future fork status row policy"
      contains: "fork.<inventory_id>"
    - path: "docs/port/migration-guidance.md"
      provides: "Cross-linked fixture/status/deferral guidance"
      contains: "packages/fork-templates"
    - path: "docs/port/parity-matrix.md"
      provides: "Human-facing fork parity status interpretation"
      contains: "no v1.9 fork rows"
    - path: "packages/fork-vendors/README.md"
      provides: "Manual drift-refresh protocol link"
      contains: "manual-drift-refresh-protocol.md"
    - path: "packages/fork-inventories/README.md"
      provides: "Checklist template link for future inventory rows"
      contains: "fork-parity-checklist-template.md"
  key_links:
    - from: "packages/fork-templates/README.md"
      to: "docs/port/README.md#v19-fork-parity-deferrals"
      via: "central deferral block created in this plan"
      pattern: "v1.9 Fork Parity Deferrals"
    - from: "packages/parity-fixtures/README.md"
      to: "packages/parity/README.md"
      via: "future fixture namespace requires future parity evidence before status publication"
      pattern: "fork\\.<inventory_id>"
    - from: "packages/fork-vendors/README.md"
      to: "packages/fork-templates/manual-drift-refresh-protocol.md"
      via: "manual drift-refresh link"
      pattern: "manual-drift-refresh-protocol\\.md"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 36-2026-05-27T13-38-25
generated_at: 2026-05-27T13:51:20Z
---

<objective>
Wire Phase 36 template, fixture namespace, parity-status, deferral, and drift-refresh policy into the port and package documentation.

Purpose: PAR-02 and PAR-03 require visible maintainer guidance that reserves fork fixture/status vocabulary for future executable evidence while keeping the complete v1.9 deferral list centralized. PAR-01 and PAR-04 also need discoverability links from the inventory and vendor docs.
Output: Updated port docs and package READMEs that point to `packages/fork-templates`, reserve future fork fixture/status vocabulary, and keep fork runtime/release/cloud/plugin work deferred.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-parity-fixture-launcher-and-deferral-templates/36-CONTEXT.md
@.planning/phases/36-parity-fixture-launcher-and-deferral-templates/36-RESEARCH.md
@.planning/phases/36-parity-fixture-launcher-and-deferral-templates/36-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@docs/port/README.md
@docs/port/migration-guidance.md
@docs/port/parity-matrix.md
@docs/port/package-map.md
@packages/fork-templates/README.md
@packages/fork-templates/fork-parity-checklist-template.md
@packages/fork-templates/manual-drift-refresh-protocol.md
@packages/parity-fixtures/README.md
@packages/parity/README.md
@packages/parity/status.tsv
@packages/fork-vendors/README.md
@packages/fork-inventories/README.md

<interfaces>
Central deferral block must live in `docs/port/README.md` with this heading so package docs can link to the stable anchor:

```markdown
## v1.9 Fork Parity Deferrals
```

The complete PAR-03/D-08 deferral list must explicitly name:

```text
full fork parity ports
GUI migration
fork-flavor release builds
signing
installers
release channels
nightly vendor sync
cloud or network device integrations
profile auto-update execution
non-free plugin ingestion
```

Future fork fixture/status vocabulary from D-04 through D-06:

```text
packages/parity-fixtures/forks/<vendor_id>/<inventory_id-or-slug>/<scenario>/
fork.<inventory_id>
//packages/parity:*_parity
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage |
|----------|------|------|----------|
| D-01 | 36-02 | 1, 3 | Full - package-map and docs make the dedicated `packages/fork-templates` package discoverable. |
| D-02 | 36-02 | 3 | Full - inventory docs link future rows to the checklist template created in Plan 36-01. |
| D-03 | 36-02 | 1, 3 | Full - docs repeat that template verification does not prove fork parity. |
| D-04 | 36-02 | 2 | Full - fixture docs reserve the required future path shape without creating files. |
| D-05 | 36-02 | 2, 3 | Full - docs forbid Phase 36 fork rows and verification checks `packages/parity/status.tsv` stays untouched. |
| D-06 | 36-02 | 2 | Full - future status token and evidence-command policy is documented. |
| D-07 | 36-02 | 1 | Full - adds one central deferral block to `docs/port/README.md` and links to it from other docs. |
| D-08 | 36-02 | 1 | Full - central deferral block names every required deferred scope. |
| D-09 | 36-02 | 3 | Full - vendor docs link the manual protocol and retain the existing verifier as a manual comparison tool. |
| D-10 | 36-02 | 3 | Full - vendor docs preserve stable tag/peeled commit versus drift-only branch-head wording. |
| D-11 | 36-02 | 3 | Full - docs state drift review is not nightly sync automation and does not update source refs automatically. |
</decision_coverage>

<dependency_graph>
| Unit | Needs | Creates | Wave |
|------|-------|---------|------|
| Central deferral docs | Plan 36-01 package link targets and PAR-03/D-07/D-08 | Stable docs anchor and package-map discoverability | 2 |
| Fixture/status policy docs | Existing parity docs and D-04 through D-06 | Future fork fixture namespace and status-token policy | 2 |
| Vendor/inventory links and final checks | Plan 36-01 package docs and central deferral anchor | Cross-package discoverability and final verification coverage | 2 |
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Add central v1.9 fork deferrals and package-map entry</name>
  <files>docs/port/README.md, docs/port/package-map.md</files>
  <action>Add `## v1.9 Fork Parity Deferrals` to `docs/port/README.md` per D-07/D-08. The block must explicitly name full fork parity ports, GUI migration, fork-flavor release builds, signing, installers, release channels, nightly vendor sync, cloud or network device integrations, profile auto-update execution, and non-free plugin ingestion as out of scope for v1.9. Also add a current Phase 36 template-package state note naming `packages/fork-templates`, `bazel run //packages/fork-templates:verify`, and the manual drift protocol while stating that source pins, inventories, templates, and registry metadata do not prove fork runtime support. Update `docs/port/package-map.md` with a `packages/fork-templates` package row and a Phase 36 note linking to the central deferral block instead of duplicating the complete list.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; for pattern in "v1.9 Fork Parity Deferrals" "full fork parity ports" "GUI migration" "fork-flavor release builds" "signing" "installers" "release channels" "nightly vendor sync" "cloud or network device integrations" "profile auto-update execution" "non-free plugin ingestion"; do rg -n "$pattern" docs/port/README.md >/dev/null; done'</automated>
    <automated>bash -lc 'set -euo pipefail; for pattern in "packages/fork-templates" "fork-parity deferral" "template verification does not prove fork parity"; do rg -n "$pattern" docs/port/README.md docs/port/package-map.md >/dev/null; done'</automated>
  </verify>
  <done>The central deferral block exists with every PAR-03 item, and `packages/fork-templates` is discoverable from the package map and port README.</done>
</task>

<task type="auto">
  <name>Task 2: Document future fork fixture namespace and parity status policy</name>
  <files>packages/parity-fixtures/README.md, packages/parity/README.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <action>Update `packages/parity-fixtures/README.md` with `Future Fork Fixture Namespace`, reserving `packages/parity-fixtures/forks/<vendor_id>/<inventory_id-or-slug>/<scenario>/` per D-04 and stating that Phase 36 does not create fork fixture files. Update `packages/parity/README.md` with `Future Fork Status Rows`, stating that Phase 36 does not add fork rows to `packages/parity/status.tsv`, future fork tokens should use `fork.<inventory_id>` or an inventory-derived stable slug, and `verified` requires a real `//packages/parity:*_parity` evidence command per D-05/D-06. Update `docs/port/migration-guidance.md` to link the central deferral block, reserve the fork fixture namespace, and clarify that future fork status requires executable parity evidence. Update `docs/port/parity-matrix.md` with a fork-parity interpretation note saying there are no v1.9 fork rows and future fork status must not be inferred from source pins, inventories, templates, or registry metadata.</action>
  <verify>
    <automated>rg -n "packages/parity-fixtures/forks/<vendor_id>/<inventory_id-or-slug>/<scenario>/" packages/parity-fixtures/README.md docs/port/migration-guidance.md</automated>
    <automated>rg -n "fork\\.<inventory_id>|//packages/parity:\\*_parity|packages/parity/status.tsv" packages/parity/README.md docs/port/parity-matrix.md docs/port/migration-guidance.md</automated>
    <automated>test -z "$(git status --porcelain -- packages/parity/status.tsv packages/parity-fixtures/forks)"</automated>
  </verify>
  <done>Fixture and parity docs reserve the future fork vocabulary, and the status TSV plus fork fixture tree remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Link vendor and inventory docs, then run phase verification</name>
  <files>packages/fork-vendors/README.md, packages/fork-inventories/README.md, docs/port/README.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md, docs/port/package-map.md, packages/parity-fixtures/README.md, packages/parity/README.md</files>
  <action>Update `packages/fork-vendors/README.md` to link `../fork-templates/manual-drift-refresh-protocol.md`, clarify that `bazel run //packages/fork-vendors:verify` remains the manual comparison tool, and state that branch drift observations do not update accepted source pins automatically per D-09 through D-11. Update `packages/fork-inventories/README.md` to link `../fork-templates/fork-parity-checklist-template.md` and state that future verified fork parity requires checklist review plus executable parity evidence; inventory rows remain planning inputs. Run the full Phase 36 verification set from the research: template verifier, template verifier tests, shell formatting check, relevant existing package verifiers, no-status-row/no-fork-fixture guard, and whitespace check. When writing `36-02-SUMMARY.md`, use YAML frontmatter with the exact hyphenated key `requirements-completed: [PAR-02, PAR-03]`; do not run `mdformat` over the summary file per repo-local guidance.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; for pattern in "manual-drift-refresh-protocol.md" "bazel run //packages/fork-vendors:verify" "drift observations do not change accepted source pins by themselves"; do rg -n "$pattern" packages/fork-vendors/README.md >/dev/null; done'</automated>
    <automated>bash -lc 'set -euo pipefail; for pattern in "fork-parity-checklist-template.md" "future executable parity evidence" "planning inputs only"; do rg -n "$pattern" packages/fork-inventories/README.md >/dev/null; done'</automated>
    <automated>bazel run //packages/fork-templates:verify</automated>
    <automated>bazel test //packages/fork-templates:verify_templates_test</automated>
    <automated>shfmt -d packages/fork-templates/verify_templates.sh packages/fork-templates/verify_templates_test.sh</automated>
    <automated>bazel run //packages/fork-vendors:verify</automated>
    <automated>bazel run //packages/fork-inventories:verify</automated>
    <automated>test -z "$(git status --porcelain -- packages/parity/status.tsv packages/parity-fixtures/forks)"</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Vendor/inventory docs link the new templates and manual drift protocol, all Phase 36 verification commands pass, and no status TSV or fork fixture files are changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Port docs -> maintainer support claims | Documentation decides what maintainers may treat as verified fork support. |
| Package docs -> future fork fixture/status creation | Package READMEs define names and gates used by later fork parity work. |
| Vendor/inventory docs -> source drift decisions | Existing source and inventory docs point maintainers at manual review flow for future milestones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Spoofing / Repudiation | `docs/port/README.md`, `docs/port/parity-matrix.md` | mitigate | Centralize complete v1.9 deferrals and state that fork support cannot be inferred from source pins, inventories, templates, or registry metadata. |
| T-36-06 | Tampering | `packages/parity/status.tsv` and `packages/parity-fixtures/forks` | mitigate | Do not edit the status TSV or create fork fixture files; verification uses `git status --porcelain -- packages/parity/status.tsv packages/parity-fixtures/forks` so tracked, staged, and untracked changes are caught. |
| T-36-07 | Spoofing | `packages/parity-fixtures/README.md` | mitigate | Reserve future fork fixture namespace in documentation only and verify no `packages/parity-fixtures/forks/` files are added. |
| T-36-08 | Tampering / Elevation of Privilege | vendor drift docs | mitigate | Clarify that branch drift observations do not update accepted source pins automatically and that reviewer decisions are required. |
</threat_model>

<verification>
Overall Plan 36-02 verification:

```bash
bazel run //packages/fork-templates:verify
bazel test //packages/fork-templates:verify_templates_test
shfmt -d packages/fork-templates/verify_templates.sh packages/fork-templates/verify_templates_test.sh
bazel run //packages/fork-vendors:verify
bazel run //packages/fork-inventories:verify
test -z "$(git status --porcelain -- packages/parity/status.tsv packages/parity-fixtures/forks)"
git diff --check
```
</verification>

<success_criteria>
Plan 36-02 is complete when the central v1.9 deferral block exists with every PAR-03 item, future fork fixture/status vocabulary is documented without adding fork rows or fixture files, vendor and inventory docs link the new templates/protocol, and all listed verification commands pass.
</success_criteria>

<output>
After completion, create `.planning/phases/36-parity-fixture-launcher-and-deferral-templates/36-02-SUMMARY.md`.

The summary YAML must include `requirements-completed: [PAR-02, PAR-03]` using the exact hyphenated key. Because this is the final Phase 36 plan, the summary body should also state that Phase 36 coverage across `PAR-01`, `PAR-02`, `PAR-03`, and `PAR-04` is complete after both plans. Do not run `mdformat` over `36-02-SUMMARY.md`.
</output>
