---
phase: 36-parity-fixture-launcher-and-deferral-templates
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/fork-templates/BUILD.bazel
  - packages/fork-templates/README.md
  - packages/fork-templates/fork-parity-checklist-template.md
  - packages/fork-templates/fork-launcher-shape-template.md
  - packages/fork-templates/manual-drift-refresh-protocol.md
  - packages/fork-templates/verify_templates.sh
  - packages/fork-templates/verify_templates_test.sh
autonomous: true
requirements: [PAR-01, PAR-03, PAR-04]
requirements_addressed: [PAR-01, PAR-03, PAR-04]
user_setup: []
must_haves:
  truths:
    - "Maintainer can inspect a dedicated `packages/fork-templates` package for Phase 36 fork parity templates."
    - "Maintainer can use a Markdown fork parity checklist template with every PAR-01 required field."
    - "Maintainer can follow a manual drift-refresh protocol that uses the existing fork vendor verifier and distinguishes accepted source pins from drift-only branch observations."
    - "Maintainer can run a repo-owned template verifier that checks required fields and non-overclaiming wording without proving fork parity."
  artifacts:
    - path: "packages/fork-templates/BUILD.bazel"
      provides: "Bazel `verify`, `verify_templates_test`, and package boundary wiring"
      contains: "sh_binary("
    - path: "packages/fork-templates/fork-parity-checklist-template.md"
      provides: "PAR-01 fixed-field Markdown checklist"
      contains: "Inventory row ID"
    - path: "packages/fork-templates/fork-launcher-shape-template.md"
      provides: "Maintainer launcher-shape planning template that keeps fork launcher/release behavior deferred"
      contains: "fork-flavor release builds"
    - path: "packages/fork-templates/manual-drift-refresh-protocol.md"
      provides: "Manual PAR-04 drift-refresh runbook"
      contains: "bazel run //packages/fork-vendors:verify"
    - path: "packages/fork-templates/verify_templates.sh"
      provides: "Local template contract verifier"
      contains: "template verification does not prove fork parity"
    - path: "packages/fork-templates/verify_templates_test.sh"
      provides: "Focused shell tests for verifier failure modes"
      contains: "missing"
  key_links:
    - from: "packages/fork-templates/BUILD.bazel"
      to: "packages/fork-templates/verify_templates.sh"
      via: "sh_binary(name = \"verify\")"
      pattern: "name = \"verify\""
    - from: "packages/fork-templates/verify_templates.sh"
      to: "packages/fork-templates/fork-parity-checklist-template.md"
      via: "required checklist label checks"
      pattern: "Inventory row ID"
    - from: "packages/fork-templates/manual-drift-refresh-protocol.md"
      to: "packages/fork-vendors/README.md"
      via: "existing vendor verification command"
      pattern: "bazel run //packages/fork-vendors:verify"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 36-2026-05-27T13-38-25
generated_at: 2026-05-27T13:51:20Z
---

<objective>
Create the dedicated Phase 36 `packages/fork-templates` package for fork parity checklists, launcher-shape planning, manual drift refresh, and local template verification.

Purpose: PAR-01 and PAR-04 require repeatable maintainer templates before later fork parity milestones. Per D-01 through D-03, the package must be separate from inventories/parity and must verify template completeness without claiming executable fork parity.
Output: Markdown templates, a manual drift-refresh runbook, package-local Bazel verifier wiring, and focused shell verifier tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-parity-fixture-launcher-and-deferral-templates/36-CONTEXT.md
@.planning/phases/36-parity-fixture-launcher-and-deferral-templates/36-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/fork-inventories/BUILD.bazel
@packages/fork-inventories/verify_inventories.sh
@packages/fork-inventories/verify_inventories_test.sh
@packages/fork-vendors/BUILD.bazel
@packages/fork-vendors/README.md
@packages/fork-vendors/verify_forks.sh

<interfaces>
Existing package-local Bazel verifier pattern from `packages/fork-inventories/BUILD.bazel`:

```python
sh_binary(
    name = "verify",
    srcs = ["verify_inventories.sh"],
    data = ["inventory-template.tsv"],
    args = ["$(location inventory-template.tsv)"],
)

sh_test(
    name = "verify_inventories_test",
    srcs = ["verify_inventories_test.sh"],
    data = ["verify_inventories.sh"],
)
```

Required checklist labels from PAR-01 and D-02:

```text
Inventory row ID
Source pin
Candidate Rust module
Fixture need
Evidence command
Docs touched
License or security note
Deferred scope
Reviewer signoff
```

Required non-overclaiming phrases for template verification:

```text
template verification does not prove fork parity
source pins and inventories are planning inputs only
future executable parity evidence
do not add fork rows to packages/parity/status.tsv in v1.9
drift observations do not change accepted source pins by themselves
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage |
|----------|------|------|----------|
| D-01 | 36-01 | 1, 2 | Full - creates the dedicated `packages/fork-templates` package. |
| D-02 | 36-01 | 1 | Full - checklist template uses a Markdown required-field table with all required labels. |
| D-03 | 36-01 | 2, 3 | Full - adds a Bazel-owned verifier that checks fields/wording and states it does not prove fork parity. |
| D-07 | 36-01 | 1 | Full for package side - package docs link to the central deferral block that Plan 36-02 adds. |
| D-08 | 36-01 | 1 | Full for package side - launcher-shape template points at the complete central deferral list instead of repeating it. |
| D-09 | 36-01 | 1 | Full - manual runbook uses existing vendor verification and forbids upstream source-tree clone/build/import/vendor behavior. |
| D-10 | 36-01 | 1, 2 | Full - runbook and verifier require the stable-tag/peeled-commit versus drift-only branch-head distinction. |
| D-11 | 36-01 | 1 | Full - runbook does not add a report target, scheduler, or automatic ref update path. |
</decision_coverage>

<dependency_graph>
| Unit | Needs | Creates | Wave |
|------|-------|---------|------|
| Template package docs | Phase 36 decisions, PAR-01/PAR-03/PAR-04, existing vendor verifier command | Maintainer-facing checklist, launcher-shape template, drift runbook, package README | 1 |
| Template verifier | Template package docs and existing shell verifier pattern | `//packages/fork-templates:verify` and verifier behavior tests | 1 |
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Create fork template package docs</name>
  <files>packages/fork-templates/README.md, packages/fork-templates/fork-parity-checklist-template.md, packages/fork-templates/fork-launcher-shape-template.md, packages/fork-templates/manual-drift-refresh-protocol.md</files>
  <action>Create `packages/fork-templates/` and add the four Markdown package artifacts. The README must name `bazel run //packages/fork-templates:verify`, link to `docs/port/README.md#v19-fork-parity-deferrals`, and state exactly that template verification does not prove fork parity per D-03. The checklist template must contain a fixed Markdown table with the exact D-02/PAR-01 labels listed in the context block, require future `//packages/parity:*_parity` evidence before verified status, and state that source pins and inventories are planning inputs only. The launcher-shape template must record future fork launcher intent without adding launcher targets, release builds, signing, installers, or release channels in Phase 36, and must point to the central deferral block per D-07/D-08. The manual drift-refresh protocol must instruct maintainers to run `bazel run //packages/fork-vendors:verify`, record selected stable tag and peeled commit confirmations, treat branch-head differences as drift-only observations, require reviewer decisions, and avoid cloning, fetching worktrees, building, importing, vendoring, scheduling sync, or updating source refs automatically per D-09 through D-11.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; for pattern in "Inventory row ID" "Source pin" "Candidate Rust module" "Fixture need" "Evidence command" "Docs touched" "License or security note" "Deferred scope" "Reviewer signoff"; do rg -n "$pattern" packages/fork-templates/fork-parity-checklist-template.md >/dev/null; done'</automated>
    <automated>bash -lc 'set -euo pipefail; for pattern in "template verification does not prove fork parity" "source pins and inventories are planning inputs only" "future executable parity evidence"; do rg -n "$pattern" packages/fork-templates/README.md packages/fork-templates/fork-parity-checklist-template.md >/dev/null; done'</automated>
    <automated>bash -lc 'set -euo pipefail; for pattern in "bazel run //packages/fork-vendors:verify" "drift observations do not change accepted source pins by themselves" "selected stable tag" "peeled commit"; do rg -n "$pattern" packages/fork-templates/manual-drift-refresh-protocol.md >/dev/null; done'</automated>
  </verify>
  <done>The new package docs exist, the checklist has all nine required fields, and the drift protocol is manual, review-gated, and source-pin conservative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Bazel verifier and shell tests</name>
  <files>packages/fork-templates/BUILD.bazel, packages/fork-templates/verify_templates.sh, packages/fork-templates/verify_templates_test.sh</files>
  <behavior>
    - Test 1: a complete package fixture passes and prints `ok: fork template verification passed`.
    - Test 2: removing one required checklist label fails with an `error:` message naming the missing text.
    - Test 3: removing the phrase `template verification does not prove fork parity` fails.
    - Test 4: removing the drift-only source-pin distinction fails.
  </behavior>
  <action>Write `BUILD.bazel` using the package-local pattern from `packages/fork-inventories`: export the Markdown files, add `sh_binary(name = "verify")` with the four Markdown files in `data` and `args`, add `sh_test(name = "verify_templates_test")`, and add `filegroup(name = "package_boundary")`. Write `verify_templates.sh` with `#!/usr/bin/env bash`, `set -euo pipefail`, early-return helper functions, visible `error:` failures, default package-local file paths when no args are provided, and a four-argument mode for tests. It must validate file existence, every required checklist label, the required non-overclaiming phrases from the context block, the central deferral link, and the manual drift command. It must not call Git, network, Bazel, or write repository files. Write `verify_templates_test.sh` using the existing `TEST_SRCDIR`/`TEST_WORKSPACE` lookup pattern, temp fixtures, and explicit `# Arrange`, `# Act`, `# Assert` comments.</action>
  <verify>
    <automated>bazel test //packages/fork-templates:verify_templates_test</automated>
    <automated>shfmt -d packages/fork-templates/verify_templates.sh packages/fork-templates/verify_templates_test.sh</automated>
  </verify>
  <done>`//packages/fork-templates:verify_templates_test` passes, shell formatting diff is empty, and the verifier fails closed on missing fields or missing boundary wording.</done>
</task>

<task type="auto">
  <name>Task 3: Verify package boundary and summary metadata expectations</name>
  <files>packages/fork-templates/BUILD.bazel, packages/fork-templates/README.md, packages/fork-templates/fork-parity-checklist-template.md, packages/fork-templates/fork-launcher-shape-template.md, packages/fork-templates/manual-drift-refresh-protocol.md, packages/fork-templates/verify_templates.sh, packages/fork-templates/verify_templates_test.sh</files>
  <action>Run the package verification surface and whitespace check after Task 1 and Task 2. Confirm this plan did not edit `packages/parity/status.tsv` and did not create `packages/parity-fixtures/forks/` entries. When writing `36-01-SUMMARY.md`, use YAML frontmatter with the exact hyphenated key `requirements-completed: [PAR-01, PAR-04]`; do not run `mdformat` over the summary file per repo-local guidance.</action>
  <verify>
    <automated>bazel run //packages/fork-templates:verify</automated>
    <automated>bazel test //packages/fork-templates:verify_templates_test</automated>
    <automated>shfmt -d packages/fork-templates/verify_templates.sh packages/fork-templates/verify_templates_test.sh</automated>
    <automated>test -z "$(git status --porcelain -- packages/parity/status.tsv packages/parity-fixtures/forks)"</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>The package-local verifier, verifier tests, shell formatting check, no-status-row/no-fork-fixture guard, and whitespace check all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer template input -> future fork parity review | Markdown templates influence later decisions about when fork behavior can be called verified. |
| Bazel `sh_binary` -> repository Markdown files | The verifier reads package files passed through Bazel `data`/`args` and must fail closed on missing or incomplete files. |
| Manual drift protocol -> upstream fork refs | The runbook points maintainers at existing remote-ref comparison without importing or changing upstream source state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Spoofing / Repudiation | `packages/fork-templates/README.md`, checklist template | mitigate | Require exact wording that template verification does not prove fork parity and that future verified fork status needs executable parity evidence. |
| T-36-02 | Tampering | `verify_templates.sh` | mitigate | Validate explicit file arguments, require files to exist, check exact labels/phrases, and avoid implicit repo writes or network calls. |
| T-36-03 | Information Disclosure / Tampering | manual drift protocol | mitigate | Use existing `//packages/fork-vendors:verify` as the manual comparison tool and forbid clone/build/import/vendor/automatic ref-update behavior. |
| T-36-04 | Denial of Service | verifier tests | accept | Shell checks are small local file reads; no long-running or external calls are added by `//packages/fork-templates:verify`. |
</threat_model>

<verification>
Overall Plan 36-01 verification:

```bash
bazel run //packages/fork-templates:verify
bazel test //packages/fork-templates:verify_templates_test
shfmt -d packages/fork-templates/verify_templates.sh packages/fork-templates/verify_templates_test.sh
test -z "$(git status --porcelain -- packages/parity/status.tsv packages/parity-fixtures/forks)"
git diff --check
```
</verification>

<success_criteria>
Plan 36-01 is complete when `packages/fork-templates` exists as a Bazel-visible package, the checklist template covers every PAR-01 required field, the drift protocol satisfies PAR-04 without automation or source ingestion, the verifier and verifier tests pass, and no fork parity status rows or fork fixture files are added.
</success_criteria>

<output>
After completion, create `.planning/phases/36-parity-fixture-launcher-and-deferral-templates/36-01-SUMMARY.md`.

The summary YAML must include `requirements-completed: [PAR-01, PAR-04]` using the exact hyphenated key. Do not run `mdformat` over `36-01-SUMMARY.md`.
</output>
