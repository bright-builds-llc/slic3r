---
phase: 58-arc-fitting-fixture-corpus
plan: "02"
type: execute
wave: 2
depends_on:
  - "58-01"
files_modified:
  - packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh
  - packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh
  - packages/parity-fixtures/BUILD.bazel
  - packages/parity-fixtures/README.md
autonomous: true
requirements:
  - ARCFIX-01
  - ARCFIX-02
  - ARCFIX-03
user_setup: []
must_haves:
  truths:
    - "Maintainer can run a package-local arc-fitting fixture verifier through Bash and Bazel."
    - "Missing, duplicate, out-of-order, unsupported, wrong-source, wrong-fixture, overclaim, stale-doc, and provenance drift cases fail closed with named diagnostics."
    - "Maintainer can confirm Phase 58 still does not publish public arc-fitting parity, Rust parser behavior, public docs, or a fork.prusaslicer.arc-fitting status row."
  artifacts:
    - path: "packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh"
      provides: "Fail-closed verifier for arc-fitting fixture corpus"
      contains: "ARC_REQUIRED_FIELDS"
    - path: "packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh"
      provides: "Mutation coverage for ARCFIX-03 drift classes"
      contains: "test_out_of_order_arc_row_fails"
    - path: "packages/parity-fixtures/BUILD.bazel"
      provides: "Bazel sh_binary and sh_test targets"
      contains: "verify_prusa_arc_fitting_fixture_test"
    - path: "packages/parity-fixtures/README.md"
      provides: "Package-level Phase 58 fixture docs and verification command"
      contains: "verify_prusa_arc_fitting_fixture"
  key_links:
    - from: "packages/parity-fixtures/BUILD.bazel"
      to: "packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh"
      via: "sh_binary srcs"
      pattern: "name = \"verify_prusa_arc_fitting_fixture\""
    - from: "packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv"
      via: "default package-local fixture path"
      pattern: "expected-arc-summary\\.tsv"
    - from: "packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh"
      to: "packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh"
      via: "temp fixture mutation harness"
      pattern: "run_verifier"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 58-2026-06-23T19-50-26
generated_at: "2026-06-23T20:10:05.307Z"
---

<objective>
Add fail-closed verification and mutation coverage for the Phase 58 arc-fitting fixture corpus.

Purpose: This turns the checked-in fixture namespace from inspectable artifacts into enforceable evidence that rejects drift and overclaims before Phase 59 Rust parsing consumes it.
Output: Bash verifier, Bash mutation suite, Bazel verifier/test targets, and package README updates naming the Phase 58 command.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-arc-fitting-fixture-corpus/58-CONTEXT.md
@.planning/phases/58-arc-fitting-fixture-corpus/58-RESEARCH.md
@.planning/phases/58-arc-fitting-fixture-corpus/58-01-SUMMARY.md
@.planning/phases/57-arc-fitting-scope-contract/57-VERIFICATION.md
@packages/prusa-arc-fitting-scope/arc-fitting-scope.md
@packages/parity-fixtures/BUILD.bazel
@packages/parity-fixtures/README.md
@packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
@packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/fixture-provenance.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/arc-fitting-observations.gcode
@packages/parity/status.tsv

<interfaces>
Plan 01 must have created these exact target names and files before this plan runs:
```text
//packages/parity-fixtures:prusa_arc_fitting_bundle
//packages/parity-fixtures:prusa_arc_fitting_expected_arc_summary
packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv
packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/fixture-provenance.tsv
packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/arc-fitting-observations.gcode
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-closed verifier and mutation suite</name>
  <files>packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh, packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh, packages/parity-fixtures/BUILD.bazel</files>
  <read_first>
    - `packages/parity-fixtures/BUILD.bazel`
    - `packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh`
    - `packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/fixture-provenance.tsv`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/arc-fitting-observations.gcode`
    - `packages/parity/status.tsv`
    - Confirm these new files are absent before creating them: `packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh`, `packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh`.
  </read_first>
  <behavior>
    - Valid Phase 58 fixture corpus exits 0 and prints exactly `ok: Prusa arc-fitting fixture verification passed`.
    - Missing arc row, duplicate arc row, out-of-order arc row, and unsupported arc field each exit nonzero and name `expected-arc-summary.tsv` plus the failed field.
    - Wrong source ref, wrong fixture path, wrong fixture ID, wrong provenance row, and wrong fixture SHA each exit nonzero with diagnostics naming the affected artifact.
    - Unsupported broad claim text in namespace README, package README, provenance, expected summary, or verifier source exits nonzero.
    - Stale namespace/package documentation references exit nonzero and name the missing required text.
  </behavior>
  <action>Implement `verify_prusa_arc_fitting_fixture.sh` per D-07 and D-08 using the existing Bash/Bazel verifier style. Use `#!/usr/bin/env bash`, `set -euo pipefail`, package-local defaults for zero-argument runs, and an explicit-argument mode accepting these files in order: fixture README, fixture provenance, expected arc summary, arc observations G-code, parity status TSV, parity-fixtures README, and optional checkout root. Define exact constants: `SOURCE_REF=prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, `FIXTURE_PATH=packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/arc-fitting-observations.gcode`, `FIXTURE_ID=arc-fitting-observations.gcode`, `EXPECTED_SHA=b1db8e3ef28d47f045f1eec852e4f83675da920b312abeeb3f3e40a5927f796f`, `EXPECTED_SIZE=94`, `ARC_SUMMARY_HEADER=$'source_ref\tfixture_path\tarc_field\tarc_category\tarc_value\tevidence_boundary'`, `ARC_SUMMARY_ROW_COUNT=13`, and `ARC_REQUIRED_FIELDS="source_ref inventory_source_paths source_anchor fixture_id fixture_path arc_command_counts arc_direction_counts center_offset_observations coordinate_bounds extrusion_observations feedrate_observations evidence_boundary"`.

The verifier must check file existence, ASCII/LF encoding for `.gcode` and TSV artifacts, exact byte count, exact SHA-256, exact headers, exact line counts, exactly six columns for arc summary rows, allowed arc fields only, one row per required field, exact ordered rows, source/fixture provenance alignment, field-specific values from Plan 01, namespace README text, package README text once Task 2 adds it, and absence of `fork.prusaslicer.arc-fitting` rows in `packages/parity/status.tsv`. Add self-scan guards for verifier behavior terms using split literals where needed: `curl `, `git `, `clone`, `fetch`, `PrusaSlicer --`, `slic3r --`, `send-gcode`, and `host upload`.

Implement `verify_prusa_arc_fitting_fixture_test.sh` per D-09 with isolated `mktemp` roots, `write_valid_fixture_copy`, `run_verifier`, `assert_contains`, and one focused test per ARCFIX-03 drift class: valid fixture passes, missing row, duplicate row, out-of-order row, unsupported arc field, unsupported broad claim text, wrong source ref, wrong fixture identity, wrong fixture path, stale namespace README reference, stale package README reference, provenance mismatch, fixture checksum drift, and forbidden verifier behavior. Each test must include `# Arrange`, `# Act`, and `# Assert` sections.

Update `packages/parity-fixtures/BUILD.bazel` to add a `sh_binary(name = "verify_prusa_arc_fitting_fixture", srcs = ["verify_prusa_arc_fitting_fixture.sh"], data = [":prusa_arc_fitting_bundle", "README.md", "//packages/parity:status.tsv"], ...)` and a `sh_test(name = "verify_prusa_arc_fitting_fixture_test", srcs = ["verify_prusa_arc_fitting_fixture_test.sh"], data = ["verify_prusa_arc_fitting_fixture.sh", ":prusa_arc_fitting_bundle", "README.md", "//packages/parity:status.tsv"])`. Add both scripts to `package_boundary`. Do not add `//packages/parity:prusaslicer_arc_fitting_parity`, `packages/slic3r-rust`, `docs/port/*`, or a `fork.prusaslicer.arc-fitting` status row.</action>
  <verify>
    <automated>bash -n packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh</automated>
    <automated>bash -n packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh</automated>
    <automated>bash packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh</automated>
    <automated>bash packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_arc_fitting_fixture_test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ARC_REQUIRED_FIELDS|require_arc_allowed_fields|require_arc_field_counts|require_arc_exact_rows" packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh` returns matches.
    - `rg -n "test_missing_arc_row_fails|test_duplicate_arc_row_fails|test_out_of_order_arc_row_fails|test_unsupported_arc_field_fails|test_wrong_source_ref_fails|test_wrong_fixture_identity_fails|test_stale_package_readme_reference_fails|test_forbidden_verifier_behavior_fails" packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh` returns matches.
    - `rg -n "verify_prusa_arc_fitting_fixture|verify_prusa_arc_fitting_fixture_test" packages/parity-fixtures/BUILD.bazel` returns matches.
    - `test "$(awk -F '\t' '$1 == "fork.prusaslicer.arc-fitting" { count++ } END { print count + 0 }' packages/parity/status.tsv)" = "0"` passes.
  </acceptance_criteria>
  <done>The verifier passes valid Phase 58 artifacts, mutation tests prove all ARCFIX-03 drift classes, and Bazel exposes both verifier and test targets without publishing public parity/status/Rust/doc surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Publish package-level fixture command docs and final boundary checks</name>
  <files>packages/parity-fixtures/README.md</files>
  <read_first>
    - `packages/parity-fixtures/README.md`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/README.md`
    - `packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh`
    - `.planning/phases/58-arc-fitting-fixture-corpus/58-CONTEXT.md`
  </read_first>
  <action>Update `packages/parity-fixtures/README.md` per D-10. Add a Phase 58 paragraph in the Fork Fixture Namespace and Current State sections with these concrete facts: namespace `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/`, fixture `arc-fitting-observations.gcode`, provenance `fixture-provenance.tsv`, expected artifact `expected-arc-summary.tsv`, bundle target `//packages/parity-fixtures:prusa_arc_fitting_bundle`, verification command `bazel run //packages/parity-fixtures:verify_prusa_arc_fitting_fixture`, source ref `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, and source path `src/libslic3r/Geometry/ArcWelder.cpp`. State that Phase 59 owns `slic3r_flavors::prusa_arc_fitting`, Phase 60 owns `bazel run //packages/parity:prusaslicer_arc_fitting_parity` and `fork.prusaslicer.arc-fitting`, and Phase 58 does not update `packages/parity/status.tsv`, public parity behavior, Rust crates, or `docs/port/*`. Preserve D-12 by keeping broad `generated-outputs` as `in progress` and preserving the existing `fork.prusaslicer.gcode-output` row meaning. Do not use achieved-language claims for byte-for-byte G-code parity, full ArcWelder algorithm equivalence, tolerance or geometry parity, printability, firmware behavior, printer-runtime behavior, GUI behavior, support generation, wall seam behavior, release behavior, host upload, network/device behavior, profile auto-update execution, Bambu Studio, OrcaSlicer, upstream import, or sync automation.</action>
  <verify>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_arc_fitting_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_arc_fitting_fixture_test</automated>
    <automated>git diff --check -- packages/parity-fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Phase 58 adds the Prusa arc-fitting fixture namespace|verify_prusa_arc_fitting_fixture|prusa_arc_fitting_bundle|expected-arc-summary.tsv" packages/parity-fixtures/README.md` returns matches.
    - `rg -n "Phase 59 owns.*prusa_arc_fitting|Phase 60 owns.*prusaslicer_arc_fitting_parity|Phase 58 does not update.*packages/parity/status.tsv" packages/parity-fixtures/README.md` returns matches.
    - `test "$(awk -F '\t' '$1 == "generated-outputs" { if ($2 == "in progress") count++ } END { print count + 0 }' packages/parity/status.tsv)" = "1"` passes.
    - `test "$(awk -F '\t' '$1 == "fork.prusaslicer.gcode-output" { count++ } END { print count + 0 }' packages/parity/status.tsv)" = "1"` passes.
    - `test "$(awk -F '\t' '$1 == "fork.prusaslicer.arc-fitting" { count++ } END { print count + 0 }' packages/parity/status.tsv)" = "0"` passes.
  </acceptance_criteria>
  <done>Package-level docs expose the Phase 58 corpus, bundle, and verifier command while preserving all Phase 59/60 and deferred-scope boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in TSV/G-code -> Bash verifier | Verifier treats repository artifacts as inputs that may be malformed or tampered with. |
| shell arguments -> filesystem reads | Explicit verifier mode accepts path arguments from tests and must not execute or expand untrusted fixture content. |
| package docs -> public claims | README wording can accidentally spoof achieved parity/status if not checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-05 | Tampering | `verify_prusa_arc_fitting_fixture.sh` | mitigate | Enforce exact SHA-256, byte count, ASCII/LF, headers, row counts, allowed fields, ordered rows, source/fixture identity, and package docs text. |
| T-58-06 | Repudiation | mutation suite | mitigate | Add one focused mutation test per ARCFIX-03 drift class with diagnostic assertions naming the artifact or field. |
| T-58-07 | Elevation of Privilege | shell verifier argument handling | mitigate | Quote variable expansions, avoid `eval`, never execute fixture content, and use local file reads plus POSIX text tools only. |
| T-58-08 | Information Disclosure | verifier behavior | mitigate | Self-scan for network, Git, generator, slicer-runtime, send-gcode, and host-upload behavior terms; keep verification offline and local. |
| T-58-09 | Spoofing | package README/status boundary | mitigate | Require exact README boundary snippets and exact status row counts for `generated-outputs`, `fork.prusaslicer.gcode-output`, and absent `fork.prusaslicer.arc-fitting`. |
</threat_model>

<verification>
Run all task-level commands plus:

```bash
bazel run //packages/parity-fixtures:verify_prusa_arc_fitting_fixture
bazel test //packages/parity-fixtures:verify_prusa_arc_fitting_fixture_test
test "$(awk -F '\t' '$1 == "fork.prusaslicer.arc-fitting" { count++ } END { print count + 0 }' packages/parity/status.tsv)" = "0"
git diff --check -- packages/parity-fixtures
```
</verification>

<success_criteria>
ARCFIX-01, ARCFIX-02, and ARCFIX-03 are complete when the Phase 58 fixture corpus is documented, Bazel-owned, verified by `//packages/parity-fixtures:verify_prusa_arc_fitting_fixture`, covered by mutation tests for all listed drift classes, and still has no public arc-fitting status row, public parity target, Rust parser, or public port-doc publication.
</success_criteria>

<output>
After completion, create `.planning/phases/58-arc-fitting-fixture-corpus/58-02-SUMMARY.md`
</output>
