---
phase: 50-structural-g-code-fixture-expansion
plan: "02"
type: tdd
wave: 2
depends_on:
  - "50-01"
files_modified:
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
  - packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
autonomous: true
requirements:
  - GCFIX-02
  - GCFIX-03
user_setup: []
must_haves:
  truths:
    - "Maintainer can run the existing Bazel-owned G-code fixture verifier and it checks the structural sidecar schema, exact rows, provenance, and update boundaries."
    - "The mutation suite has one focused negative case for each GCFIX-03 class: value drift, missing row, duplicate row, unsupported field, broad-behavior claim, and provenance mismatch."
    - "Structural verifier failures name `expected-gcode-structural-summary.tsv` and the affected structural field or provenance mismatch."
    - "The verifier remains file-based and does not add Git, network, upstream generation, host upload, Rust parser, parity status publication, or new verifier target behavior."
  artifacts:
    - path: "packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh"
      provides: "Fail-closed fixture verifier for the structural TSV"
      contains: "verify_structural_summary"
    - path: "packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh"
      provides: "Mutation coverage for GCFIX-03 structural drift classes"
      contains: "test_structural_value_drift_fails"
  key_links:
    - from: "verify_prusa_gcode_output_fixture.sh"
      to: "expected-gcode-structural-summary.tsv"
      via: "new explicit verifier argument and default checkout path"
      pattern: "structural_summary_file"
    - from: "verify_prusa_gcode_output_fixture.sh"
      to: "fixture-provenance.tsv"
      via: "source_ref, fixture_path, fixture_id, source literal, and update-route alignment checks"
      pattern: "provenance mismatch"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 50-2026-06-17T16-13-19
generated_at: 2026-06-17T16:40:01Z
---

<objective>
Extend the existing Prusa G-code fixture verifier and mutation suite so the Phase 50 structural sidecar fails closed.

Purpose: Complete the executable-verifier portion of GCFIX-02 and all GCFIX-03 mutation evidence while preserving the existing fixture package ownership boundary.
Output: Updated Bash verifier and temp-checkout mutation tests for structural summary schema, rows, values, provenance alignment, and overclaim rejection.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-structural-g-code-fixture-expansion/50-CONTEXT.md
@.planning/phases/50-structural-g-code-fixture-expansion/50-RESEARCH.md
@.planning/phases/50-structural-g-code-fixture-expansion/50-01-PLAN.md
@.planning/phases/49-structural-g-code-scope-contract/49-01-SUMMARY.md
@.planning/phases/49-structural-g-code-scope-contract/49-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/parity-fixtures/BUILD.bazel
@packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
@packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
@packages/parity-fixtures/README.md
@packages/prusa-gcode-output-scope/gcode-output-scope.md
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
@packages/parity/status.tsv
@packages/parity/BUILD.bazel

Relevant Bright Builds pages read during planning:
- `standards/index.md`
- `standards/core/architecture.md`
- `standards/core/code-shape.md`
- `standards/core/verification.md`
- `standards/core/testing.md`

No project-local `.claude/skills/` or `.agents/skills/` directory exists in this checkout.

<interfaces>
Existing verifier argument flow before this plan:

```bash
verify_prusa_gcode_output_fixture.sh \
  fixture-README \
  fixture-provenance \
  expected-gcode-summary \
  gcode-file \
  parity-status \
  parity-BUILD \
  parity-fixtures-README \
  [checkout-root]
```

After this plan, add the structural summary argument between `expected-gcode-summary` and `gcode-file`:

```bash
verify_prusa_gcode_output_fixture.sh \
  fixture-README \
  fixture-provenance \
  expected-gcode-summary \
  expected-gcode-structural-summary \
  gcode-file \
  parity-status \
  parity-BUILD \
  parity-fixtures-README \
  [checkout-root]
```

Existing test helpers to reuse:

```bash
write_valid_fixture_copy "${dir}"
run_verifier "${dir}" "${stdout_file}" "${stderr_file}" ["${maybe_verifier}"]
remove_line_containing "${file}" "${pattern}"
replace_first_line_containing "${file}" "${pattern}" "${replacement}"
assert_contains "${file}" "${pattern}"
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage |
|----------|------|------|----------|
| D-06 | 50-02 | 2 | Full: extends `verify_prusa_gcode_output_fixture.sh` and `//packages/parity-fixtures:verify_prusa_gcode_output_fixture`; creates no new verifier target. |
| D-07 | 50-02 | 2 | Full: checks header, exact 16 rows, required fields, row count, duplicates, source/fixture alignment, provenance/update-route alignment, and no-overclaim boundaries. |
| D-10 | 50-02 | 1 | Full: adds valid baseline plus focused structural mutation cases to the existing temp-checkout suite. |
| D-11 | 50-02 | 1, 2 | Full: covers value drift, missing row, duplicate row, unsupported field, broad claim, and provenance mismatch. |
| D-12 | 50-02 | 1, 2 | Full: keeps temp-checkout style and requires diagnostics naming the sidecar or affected field. |
| D-13 | 50-02 | 2 | Full: enforces the Phase 49 field set without changing field names. |
| D-14 | 50-02 | 2 | Full: keeps `packages/parity/status.tsv`, `packages/parity/BUILD.bazel`, and Rust parser files unchanged. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED structural mutation coverage</name>
  <files>packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</files>
  <read_first>
    packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
    packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
    .planning/phases/50-structural-g-code-fixture-expansion/50-CONTEXT.md
    .planning/phases/50-structural-g-code-fixture-expansion/50-RESEARCH.md
  </read_first>
  <behavior>
    - Valid fixture copy still passes the current verifier before structural checks are added.
    - Changing `command_count_g1` from `4` to `3` in `expected-gcode-structural-summary.tsv` must fail.
    - Removing the `ordered_marker_3` structural row must fail.
    - Appending a duplicate `command_count_total` row must fail.
    - Appending unsupported field `geometry_count` must fail.
    - Adding forbidden claim `verified Prusa G-code output parity` to a structural evidence boundary must fail.
    - Changing a structural row source_ref to `prusaslicer:version_2.9.4@0000000000000000000000000000000000000000` must fail as provenance mismatch.
  </behavior>
  <action>
    Update only `packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` in this RED task.

    In `write_valid_fixture_copy`, copy the new sidecar from `${source_fixture_dir}/expected-gcode-structural-summary.tsv` to `${fixture_dir}/expected-gcode-structural-summary.tsv`. Do not change `run_verifier` to pass the structural sidecar argument in this RED task; the current verifier ignores the new file, so the focused negative tests below must fail with their `... fixture passed` messages before GREEN implementation.

    Add these test functions with explicit `# Arrange`, `# Act`, and `# Assert` comments, using the existing temp-checkout helper style:

    `test_structural_value_drift_fails`: set `structural_file="${dir}/packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv"`, call `write_valid_fixture_copy`, replace the first line containing `$'\tcommand_count_g1\tcommand counts\t4\t'` with the same row except structural_value `3`, run verifier, fail with `structural value drift fixture passed` if it exits 0, assert stderr contains `expected-gcode-structural-summary.tsv|command_count_g1`.

    `test_missing_structural_row_fails`: remove the line containing `$'\tordered_marker_3\tordered markers\tG1 F203.2\t'`, run verifier, fail with `missing structural row fixture passed` if it exits 0, assert stderr contains `ordered_marker_3|expected-gcode-structural-summary.tsv`.

    `test_duplicate_structural_row_fails`: append an exact duplicate of the existing `command_count_total` row from the structural TSV, run verifier, fail with `duplicate structural row fixture passed` if it exits 0, assert stderr contains `command_count_total|duplicate|expected-gcode-structural-summary.tsv`.

    `test_unsupported_structural_field_fails`: append this exact tab-delimited row, run verifier, fail with `unsupported structural field fixture passed` if it exits 0, assert stderr contains `unsupported structural field|geometry_count|expected-gcode-structural-summary.tsv`:

    ```text
prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961	packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode	geometry_count	unsupported generated-output semantics	999	Unsupported generated-output field that must fail closed.
    ```

    `test_structural_overclaim_fails`: replace the first line containing `$'\tcommand_count_total\tcommand counts\t4\t'` with the same field/category/value but evidence boundary `verified Prusa G-code output parity`, run verifier, fail with `structural overclaim fixture passed` if it exits 0, assert stderr contains `forbidden|verified Prusa G-code output parity|expected-gcode-structural-summary.tsv`.

    `test_structural_provenance_mismatch_fails`: replace the first line containing `$'\tsource_ref\tsource identity\t'` so the first TSV column is `prusaslicer:version_2.9.4@0000000000000000000000000000000000000000` while the structural_value remains the locked Phase 50 source ref, run verifier, fail with `structural provenance mismatch fixture passed` if it exits 0, assert stderr contains `provenance mismatch|source_ref|expected-gcode-structural-summary.tsv`.

    Add calls to these tests immediately after `test_extra_expected_summary_row_fails` so the structural RED cases run before README/status/parity publication cases.

    Run the RED command before modifying the verifier. It must exit nonzero because at least one new structural mutation is currently ignored. Record the failing marker in the summary.
  </action>
  <verify>
    <automated>rg -n 'test_structural_value_drift_fails|test_missing_structural_row_fails|test_duplicate_structural_row_fails|test_unsupported_structural_field_fails|test_structural_overclaim_fails|test_structural_provenance_mismatch_fails|expected-gcode-structural-summary.tsv' packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
    <automated>set +e; bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh >/tmp/phase50-structural-red.out 2>/tmp/phase50-structural-red.err; status="$?"; set -e; test "${status}" -ne 0; rg -n 'structural value drift fixture passed|missing structural row fixture passed|duplicate structural row fixture passed|unsupported structural field fixture passed|structural overclaim fixture passed|structural provenance mismatch fixture passed' /tmp/phase50-structural-red.err</automated>
  </verify>
  <acceptance_criteria>
    - `verify_prusa_gcode_output_fixture_test.sh` copies `expected-gcode-structural-summary.tsv` in `write_valid_fixture_copy`.
    - `verify_prusa_gcode_output_fixture_test.sh` contains `test_structural_value_drift_fails`.
    - `verify_prusa_gcode_output_fixture_test.sh` contains `test_missing_structural_row_fails`.
    - `verify_prusa_gcode_output_fixture_test.sh` contains `test_duplicate_structural_row_fails`.
    - `verify_prusa_gcode_output_fixture_test.sh` contains `test_unsupported_structural_field_fails`.
    - `verify_prusa_gcode_output_fixture_test.sh` contains `test_structural_overclaim_fails`.
    - `verify_prusa_gcode_output_fixture_test.sh` contains `test_structural_provenance_mismatch_fails`.
    - `/tmp/phase50-structural-red.err` contains one of the six `... fixture passed` failure markers from this task.
  </acceptance_criteria>
  <done>Focused RED mutation tests exist for every GCFIX-03 failure class and prove the current verifier is insufficient.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GREEN structural verifier checks</name>
  <files>packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh, packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</files>
  <read_first>
    packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
    packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
    packages/parity-fixtures/README.md
    packages/parity-fixtures/BUILD.bazel
    packages/prusa-gcode-output-scope/gcode-output-scope.md
    packages/parity/status.tsv
    packages/parity/BUILD.bazel
    .planning/phases/50-structural-g-code-fixture-expansion/50-CONTEXT.md
    .planning/phases/50-structural-g-code-fixture-expansion/50-RESEARCH.md
  </read_first>
  <behavior>
    - Valid checkout passes `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture`.
    - Valid mutation temp fixture passes `bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh`.
    - Structural header, six-column shape, 17 line count, exact rows, required field set, unsupported fields, duplicate fields, source_ref/fixture_path alignment, fixture_id/source literal alignment, and update-route text are all checked.
    - The verifier rejects forbidden broad-claim text in fixture README, `expected-gcode-summary.tsv`, and `expected-gcode-structural-summary.tsv`.
    - The verifier remains Bash 3.2-compatible; use `awk` or indexed lists, not `declare -A`.
  </behavior>
  <action>
    Update `packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh` per D-06 and D-07.

    Add default zero-argument path `structural_summary_file="${fixture_dir}/expected-gcode-structural-summary.tsv"`. Change explicit argument mode from `7 or 8` args to `8 or 9` args, with argument 4 assigned to `structural_summary_file`, argument 5 assigned to `gcode_file`, argument 6 to `status_file`, argument 7 to `parity_build_file`, argument 8 to `package_readme`, and optional argument 9 to `checkout_root`. Update the usage string to exactly include `expected-gcode-structural-summary` between `expected-gcode-summary` and `gcode-file`.

    Add these constants near existing summary/provenance constants:

    ```bash
readonly STRUCTURAL_SUMMARY_HEADER=$'source_ref\tfixture_path\tstructural_field\tstructural_category\tstructural_value\tevidence_boundary'
readonly STRUCTURAL_SUMMARY_ROW_COUNT="17"
readonly INVENTORY_SOURCE_PATHS="src/libslic3r/GCode.cpp;src/libslic3r/GCode.hpp"
readonly FIXTURE_SOURCE_LITERAL="tests/fff_print/test_gcodewriter.cpp#L20-L35"
readonly FIXTURE_ID="gcodewriter-set-speed.gcode"
    ```

    Add exact structural expected rows, either as readonly row constants or a compact newline-separated list. The checks must compare these exact field/category/value/boundary tuples:

    | structural_field | structural_category | structural_value | evidence_boundary |
    | --- | --- | --- | --- |
    | source_ref | source identity | `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961` | Accepted PrusaSlicer source identity only: `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`. |
    | inventory_source_paths | source identity | `src/libslic3r/GCode.cpp;src/libslic3r/GCode.hpp` | Inventory source paths only: `src/libslic3r/GCode.cpp;src/libslic3r/GCode.hpp`. |
    | fixture_source_literal | source identity | `tests/fff_print/test_gcodewriter.cpp#L20-L35` | Source literal only: `tests/fff_print/test_gcodewriter.cpp#L20-L35`. |
    | fixture_id | fixture identity | `gcodewriter-set-speed.gcode` | Fixture identity only: `gcodewriter-set-speed.gcode`. |
    | fixture_path | fixture identity | `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode` | Checked-in fixture path only: `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode`. |
    | command_count_total | command counts | `4` | Count of G-code command rows in the selected fixture only; no generated-output behavior claimed. |
    | command_count_g1 | command counts | `4` | Count of `G1` command rows in the selected fixture only; no toolpath geometry claimed. |
    | section_count_total | section counts | `1` | Count of structural sections in the selected summary only; no GUI, print, or runtime section behavior claimed. |
    | ordered_marker_1 | ordered markers | `G1 F99999.123` | First ordered marker value from the selected fixture summary only. |
    | ordered_marker_2 | ordered markers | `G1 F1` | Second ordered marker value from the selected fixture summary only. |
    | ordered_marker_3 | ordered markers | `G1 F203.2` | Third ordered marker value from the selected fixture summary only. |
    | ordered_marker_4 | ordered markers | `G1 F203.201` | Fourth ordered marker value from the selected fixture summary only. |
    | movement_axis_present | movement/extrusion indicators | `false` | Boolean structural indicator for movement-axis text presence only; no toolpath geometry, travel, or printability claim. |
    | extrusion_axis_present | movement/extrusion indicators | `false` | Boolean structural indicator for extrusion-axis text presence only; no extrusion amount, material, or printability claim. |
    | temperature_marker_count | temperature/tool-change markers | `0` | Count of temperature marker commands in the selected fixture only; no printer-runtime behavior claimed. |
    | tool_change_marker_count | temperature/tool-change markers | `0` | Count of tool-change marker commands in the selected fixture only; no multi-tool runtime behavior claimed. |

    Add `verify_structural_summary`. It must:
    - call `require_exact_header "${structural_summary_file}" "expected-gcode-structural-summary.tsv" "${STRUCTURAL_SUMMARY_HEADER}"`;
    - call `require_line_count "${structural_summary_file}" "expected-gcode-structural-summary.tsv" "${STRUCTURAL_SUMMARY_ROW_COUNT}"`;
    - require every exact row listed above;
    - use `awk -F '\t'` to require `NF == 6` for every data row;
    - use `awk -F '\t'` to reject any `$3` outside the 16 required field names with error text containing `expected-gcode-structural-summary.tsv: unsupported structural field: <field>`;
    - use `awk -F '\t'` to require each required field appears exactly once, with duplicate/missing diagnostics containing `expected-gcode-structural-summary.tsv`, the structural field name, actual count, and expected `1`;
    - reject any row where `$1 != SOURCE_REF` or `$2 != FIXTURE_PATH` with diagnostic containing `expected-gcode-structural-summary.tsv: provenance mismatch for <field>`;
    - cross-check `source_ref`, `fixture_path`, `fixture_id`, and `fixture_source_literal` structural values against `SOURCE_REF`, `FIXTURE_PATH`, `FIXTURE_ID`, and `FIXTURE_SOURCE_LITERAL`;
    - require the fixture README still contains `Structural expected artifact: `expected-gcode-structural-summary.tsv`` and the update-route text already enforced by `verify_readme_scope`;
    - reject file-integrity/publication metadata in the structural TSV: `bytes`, `sha256`, `reviewed-intake`, `fork.prusaslicer.gcode-output`, `generated-outputs`, and `prusaslicer_gcode_output_parity`.

    Update `reject_overclaiming_text` to scan `${fixture_readme}`, `${expected_summary_file}`, and `${structural_summary_file}`. Keep existing forbidden strings and add at least `verified Prusa G-code output parity`, `byte-for-byte G-code parity verified`, `full generated-output parity verified`, `toolpath correctness verified`, `printability verified`, `host upload verified`, `Bambu Studio support verified`, and `OrcaSlicer support verified` for the structural sidecar too.

    Add `${structural_summary_file}` to the `required_file` loop. Call `verify_structural_summary` after `verify_expected_summary` and before `verify_readme_scope`.

    Update `packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh`: change `run_verifier` to pass `"${fixture_dir}/expected-gcode-structural-summary.tsv"` between expected summary and G-code file. Keep the RED test names and diagnostics from Task 1, but update assertions if the final verifier diagnostic is more precise; each assertion must still match the sidecar filename and affected structural field or forbidden/provenance text.

    Do not add a new Bazel target. Do not touch `packages/parity/status.tsv`, `packages/parity/BUILD.bazel`, or Rust parser files.
  </action>
  <verify>
    <automated>rg -n 'structural_summary_file|STRUCTURAL_SUMMARY_HEADER|STRUCTURAL_SUMMARY_ROW_COUNT|verify_structural_summary|expected-gcode-structural-summary.tsv|unsupported structural field|provenance mismatch' packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
    <automated>bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
    <automated>bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
    <automated>shfmt -d packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
    <automated>shellcheck packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
    <automated>bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test</automated>
    <automated>bazel test --cache_test_results=no //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
    <automated>test -z "$(git diff --name-only -- packages/parity packages/slic3r-rust)"</automated>
    <automated>git diff --check -- packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `verify_prusa_gcode_output_fixture.sh` contains `structural_summary_file`.
    - `verify_prusa_gcode_output_fixture.sh` contains `readonly STRUCTURAL_SUMMARY_HEADER=$'source_ref\tfixture_path\tstructural_field\tstructural_category\tstructural_value\tevidence_boundary'`.
    - `verify_prusa_gcode_output_fixture.sh` contains `verify_structural_summary`.
    - `verify_prusa_gcode_output_fixture.sh` contains `unsupported structural field` and `provenance mismatch`.
    - `verify_prusa_gcode_output_fixture_test.sh` passes `expected-gcode-structural-summary.tsv` to `run_verifier`.
    - `verify_prusa_gcode_output_fixture_test.sh` contains the six structural mutation tests from Task 1.
    - `bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh` prints `ok: verify_prusa_gcode_output_fixture_test`.
    - `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture` prints `ok: Prusa G-code output fixture verification passed`.
    - `bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test` passes.
    - `bazel test --cache_test_results=no //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test` passes.
    - `git diff --name-only -- packages/parity packages/slic3r-rust` prints no files.
  </acceptance_criteria>
  <done>The existing fixture verifier enforces the structural sidecar exactly, and mutation tests fail closed for every GCFIX-03 drift class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checked-in TSV to Bash verifier | The structural TSV is untrusted input and must be validated at the file boundary. |
| Fixture/provenance/docs to verifier | Multiple checked-in text artifacts must agree on source identity, fixture identity, source literal, and update route. |
| Verifier executable behavior | The verifier is executable code and must not expand into network, Git, generation, upload, Rust parser, or publication behavior. |
| Mutation temp checkout to verifier | Tests must prove one failure class per isolated temp checkout without mutating real repo artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-06 | Tampering | `expected-gcode-structural-summary.tsv` | mitigate | Add exact header/row checks, closed field allowlist, per-field count checks, unsupported-field rejection, and structural value drift mutation test. |
| T-50-07 | Repudiation | Sidecar/provenance alignment | mitigate | Cross-check source_ref, fixture_path, fixture_id, fixture_source_literal, README update-route text, and provenance constants; mutation test provenance mismatch. |
| T-50-08 | Spoofing | Fixture-owned README and structural evidence boundaries | mitigate | Scan fixture README, summary TSV, and structural TSV for forbidden broad parity/support/runtime claims; mutation test structural overclaim text. |
| T-50-09 | Denial of Service | Bazel verification workflow | mitigate | Plan verification commands run Bazel serially to avoid output-base lock contention; do not plan concurrent Bazel invocations. |
| T-50-10 | Elevation of Privilege | Verifier behavior expansion | mitigate | Preserve self-scan forbidden behavior checks for `curl`, Git, clone/fetch, PrusaSlicer/Slic3r generation, send-gcode, and host upload terms; no new external service, credential, or process boundary is introduced. |
</threat_model>

<verification>
Run these checks after both tasks. Run Bazel commands serially in the order listed; do not run concurrent Bazel invocations because research found output-base lock contention.

```bash
rg -n 'structural_summary_file|STRUCTURAL_SUMMARY_HEADER|STRUCTURAL_SUMMARY_ROW_COUNT|verify_structural_summary|expected-gcode-structural-summary.tsv|unsupported structural field|provenance mismatch' packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
bash -n packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
shfmt -d packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
shellcheck packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture
bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test
bazel test --cache_test_results=no //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test
test -z "$(git diff --name-only -- packages/parity packages/slic3r-rust)"
git diff --check -- packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
```
</verification>

<success_criteria>
- The fixture verifier accepts the new structural sidecar argument in default and explicit temp-checkout modes.
- The verifier checks exact structural schema, row count, required fields, duplicate rows, unsupported fields, exact values, source/fixture alignment, provenance/update-route alignment, and overclaim rejection.
- Mutation tests prove value drift, missing row, duplicate row, unsupported field, broad-behavior claim, and provenance mismatch fail closed.
- No new verifier target is created; `//packages/parity-fixtures:verify_prusa_gcode_output_fixture` remains the owner.
- `packages/parity/status.tsv`, `packages/parity/BUILD.bazel`, and Rust parser files remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/50-structural-g-code-fixture-expansion/50-02-SUMMARY.md`
</output>
