---
phase: 47-rust-prusa-g-code-summary-boundary
plan: "02"
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
autonomous: true
requirements: [PGSUM-02, PGSUM-03]
requirements_addressed: [PGSUM-02, PGSUM-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 47-2026-06-14T15-07-52
generated_at: 2026-06-14T15:23:57Z
must_haves:
  truths:
    - "Developer can find `prusaslicer.gcode-output` in the static `slic3r_flavors` registry as a Prusa shared-downstream capability."
    - "Registry metadata traces the G-code capability to `prusa_gcode_output_metadata()` constants instead of duplicated source literals."
    - "The capability remains `ChecklistStatus::FutureCandidate` with `ParitySurface::generated_outputs()` dependency and no verified status publication."
    - "Registry tests prove shared-downstream and future-candidate filters include `prusaslicer.gcode-output` without adding broad runtime or executable evidence claims."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      provides: "Static `FlavorCapability` row and provenance for `prusaslicer.gcode-output`"
      contains: "PRUSA_GCODE_OUTPUT_PROVENANCE"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs"
      provides: "Registry tests for G-code output traceability, filters, and public-name guards"
      contains: "prusa_gcode_output_registry_row_traces_to_source_and_generated_output_dependency"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      via: "imported metadata constants"
      pattern: "PRUSA_GCODE_OUTPUT_INVENTORY_ID"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      via: "`maybe_capability(\"prusaslicer.gcode-output\")`"
      pattern: "prusaslicer\\.gcode-output"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      via: "`prusa_gcode_output_metadata()`"
      pattern: "prusa_gcode_output_metadata"
---

<objective>
Add the `prusaslicer.gcode-output` capability to the pure
`slic3r_flavors` registry as planning metadata only.

Purpose: PGSUM-02 requires the G-code output capability to be traceable from
Rust metadata back to the accepted Prusa source identity, fixture namespace,
expected summary artifact, planned status token, and broad generated-output
deferrals. D-05 requires this registry row, while Phase 48 still owns the
parity command and `packages/parity/status.tsv` publication.

Output: Registry metadata and tests. Do not commit or push; the parent wrapper
owns final git mutation after clean phase verification.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-rust-prusa-g-code-summary-boundary/47-CONTEXT.md
@.planning/phases/47-rust-prusa-g-code-summary-boundary/47-RESEARCH.md
@.planning/phases/47-rust-prusa-g-code-summary-boundary/47-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_contracts/src/flavor.rs
@packages/fork-inventories/prusaslicer.tsv
@packages/fork-inventories/category-map.tsv
@packages/parity/status.tsv

Bright Builds sources informing this plan: local `AGENTS.md`,
`AGENTS.bright-builds.md`, `standards-overrides.md`, and pinned canonical
architecture, code-shape, verification, testing, and Rust standards at
`05f8d7a6c9c2e157ec4f922a05273e72dab97676`.

<interfaces>
Registry structs from `registry.rs`:

```rust
pub struct FlavorCapability {
    pub flavor_id: FlavorId,
    pub capability_id: &'static str,
    pub feature_surface: &'static str,
    pub feature_category: &'static str,
    pub origin: FeatureOrigin,
    pub parity_dependencies: &'static [ParitySurface],
    pub checklist_status: ChecklistStatus,
    pub provenance: &'static [FlavorProvenance],
    pub caution_flags: &'static [&'static str],
    pub future_parity_notes: &'static str,
}

pub struct FlavorProvenance {
    pub inventory_id: &'static str,
    pub vendor_source: VendorSourceRef,
    pub source_paths: &'static [&'static str],
    pub ownership: FeatureOrigin,
}
```

Plan 47-01 creates:

```rust
pub const fn prusa_gcode_output_metadata() -> PrusaGcodeOutputMetadata;
pub(crate) const PRUSA_GCODE_OUTPUT_INVENTORY_ID: &str;
pub(crate) const PRUSA_GCODE_OUTPUT_SOURCE_REF: VendorSourceRef;
pub(crate) const PRUSA_GCODE_OUTPUT_SOURCE_PATH: &str;
```
</interfaces>
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
| --- | --- | --- | --- | --- |
| D-01 | 47-01 | 1, 2 | Full | Parser module exists before this plan. |
| D-02 | 47-01 | 2 | Full | Parser purity is tested before registry consumption. |
| D-03 | 47-01 | 2 | Full | Public exports exist before registry tests import metadata. |
| D-04 | 47-01 | 2 | Full | Metadata constants exist before registry wiring. |
| D-05 | 47-02 | 1, 2 | Full | Adds `prusaslicer.gcode-output` to registry as `SharedDownstream`, generated-output dependent, future-candidate metadata only. |
| D-06 | 47-01 | 1, 2 | Full | Header parsing covered in Plan 47-01. |
| D-07 | 47-01 | 1, 2 | Full | Accepted row set covered in Plan 47-01. |
| D-08 | 47-01 | 2 | Full | Typed value modeling covered in Plan 47-01. |
| D-09 | 47-01 | 1, 2 | Full | Summary output covered in Plan 47-01. |
| D-10 | 47-01 | 1 | Full | Parser tests covered in Plan 47-01. |
| D-11 | 47-02 | 2 | Full | Registry tests assert shared-downstream and future-candidate filters include the G-code row. |
| D-12 | 47-02 | 2 | Full | Registry public-name guard includes the G-code metadata helper without adding verified/runtime/release/sync names. |
| D-13 | 47-02 | 2 | Full | Existing Bazel `flavor_registry_test`, clippy, and rustfmt surfaces verify the registry updates. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Prusa G-code output registry capability</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_project_file.rs`
    - `packages/fork-inventories/prusaslicer.tsv`
    - `packages/fork-inventories/category-map.tsv`
    - `.planning/phases/47-rust-prusa-g-code-summary-boundary/47-CONTEXT.md` decisions D-04, D-05, D-11, and D-12
  </read_first>
  <behavior>
    - Registry exposes `prusaslicer.gcode-output` as a PrusaSlicer shared-downstream capability.
    - Registry provenance uses the constants from `crate::prusa_gcode_output`.
    - Capability dependency is exactly `[ParitySurface::generated_outputs()]`.
    - Capability status is exactly `ChecklistStatus::FutureCandidate`.
    - Capability notes remain planning metadata and do not claim executable parity or runtime support.
  </behavior>
  <action>
    Update the top imports in `registry.rs` to import these constants:

```rust
use crate::prusa_gcode_output::{
    PRUSA_GCODE_OUTPUT_INVENTORY_ID, PRUSA_GCODE_OUTPUT_SOURCE_PATH,
    PRUSA_GCODE_OUTPUT_SOURCE_REF,
};
```

    Add these static values near the existing Prusa project-file provenance:

```rust
static PRUSA_GCODE_OUTPUT_PATHS: [&str; 1] = [PRUSA_GCODE_OUTPUT_SOURCE_PATH];
static PRUSA_GCODE_OUTPUT_PROVENANCE: [FlavorProvenance; 1] = [FlavorProvenance {
    inventory_id: PRUSA_GCODE_OUTPUT_INVENTORY_ID,
    vendor_source: PRUSA_GCODE_OUTPUT_SOURCE_REF,
    source_paths: &PRUSA_GCODE_OUTPUT_PATHS,
    ownership: FeatureOrigin::SharedDownstream,
}];
```

    Change `static PRUSA_CAPABILITIES: [FlavorCapability; 2]` to `[FlavorCapability; 3]`.

    Add this `FlavorCapability` after `prusaslicer.project-file` and before `prusaslicer.profile-schema`:

```rust
FlavorCapability {
    flavor_id: FlavorId::PrusaSlicer,
    capability_id: "prusaslicer.gcode-output",
    feature_surface: "gcode-output",
    feature_category: "gcode-output",
    origin: FeatureOrigin::SharedDownstream,
    parity_dependencies: &GENERATED_OUTPUTS_PARITY,
    checklist_status: ChecklistStatus::FutureCandidate,
    provenance: &PRUSA_GCODE_OUTPUT_PROVENANCE,
    caution_flags: &[],
    future_parity_notes: "Source-observed G-code output planning row; summary-only parity requires fixture-backed Rust summary evidence and the Phase 48 executable command before output behavior is claimed.",
},
```

    Do not add a status row, parity command, runtime dispatch path, printer path,
    release path, network path, host-upload path, profile-update path, or sync
    path. Do not add Bambu Studio or OrcaSlicer G-code capabilities.
  </action>
  <acceptance_criteria>
    - `rg -n 'PRUSA_GCODE_OUTPUT_INVENTORY_ID|PRUSA_GCODE_OUTPUT_SOURCE_PATH|PRUSA_GCODE_OUTPUT_SOURCE_REF' packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` succeeds.
    - `rg -n 'static PRUSA_GCODE_OUTPUT_PROVENANCE' packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` succeeds.
    - `rg -n 'static PRUSA_CAPABILITIES: \\[FlavorCapability; 3\\]' packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` succeeds.
    - `rg -n 'capability_id: "prusaslicer\\.gcode-output"' packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` succeeds.
    - `rg -n 'parity_dependencies: &GENERATED_OUTPUTS_PARITY' packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` succeeds.
    - `rg -n 'status.tsv|prusaslicer_gcode_output_parity|fork\\.prusaslicer\\.gcode-output' packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` finds no matches outside the reserved metadata token imported from Plan 47-01.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'capability_id: "prusaslicer\.gcode-output"' packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs</automated>
    <automated>rg -n 'static PRUSA_CAPABILITIES: \[FlavorCapability; 3\]' packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs</automated>
  </verify>
  <done>`registry.rs` contains one `prusaslicer.gcode-output` capability using typed metadata constants, generated-output dependency, shared-downstream origin, and future-candidate status only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update registry tests and public-name guards</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs</files>
  <read_first>
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs`
    - `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs`
  </read_first>
  <behavior>
    - Shared-downstream filter returns `prusaslicer.project-file` and `prusaslicer.gcode-output` in registry order.
    - A dedicated test compares the G-code capability to `prusa_gcode_output_metadata()` values.
    - Future-candidate registry coverage includes `prusaslicer.gcode-output`.
    - Runtime/release/sync/public helper guard includes `prusa_gcode_output_metadata` as an allowed helper and does not flag required metadata values.
  </behavior>
  <action>
    Update imports to include:

```rust
prusa_gcode_output_metadata,
```

    Rename `shared_downstream_filter_returns_source_observed_project_file_row`
    to `shared_downstream_filter_returns_source_observed_prusa_rows` and set
    its assertion to:

```rust
assert_eq!(
    shared_capability_ids,
    ["prusaslicer.project-file", "prusaslicer.gcode-output"]
);
```

    Add a test named
    `prusa_gcode_output_registry_row_traces_to_source_and_generated_output_dependency`
    with this structure:

```rust
// Arrange
let metadata = prusa_gcode_output_metadata();
let maybe_gcode_output = maybe_capability(metadata.inventory_id);
let expected_dependencies = [metadata.parity_dependency];

// Act
let gcode_output = match maybe_gcode_output {
    Some(capability) => capability,
    None => {
        assert!(maybe_gcode_output.is_some(), "prusaslicer.gcode-output capability missing");
        return;
    }
};

// Assert
assert_eq!(gcode_output.flavor_id, FlavorId::PrusaSlicer);
assert_eq!(gcode_output.capability_id, metadata.inventory_id);
assert_eq!(gcode_output.feature_surface, "gcode-output");
assert_eq!(gcode_output.feature_category, "gcode-output");
assert_eq!(gcode_output.origin, FeatureOrigin::SharedDownstream);
assert_eq!(gcode_output.checklist_status, ChecklistStatus::FutureCandidate);
assert_eq!(gcode_output.parity_dependencies, expected_dependencies);
assert_eq!(gcode_output.provenance.len(), 1);
assert_eq!(gcode_output.provenance[0].inventory_id, metadata.inventory_id);
assert_eq!(gcode_output.provenance[0].vendor_source, metadata.source_ref);
assert_eq!(gcode_output.provenance[0].source_paths, [metadata.source_path]);
assert_eq!(gcode_output.provenance[0].ownership, FeatureOrigin::SharedDownstream);
```

    Add a metadata test named
    `prusa_gcode_output_metadata_exposes_fixture_scope_expected_summary_and_reserved_status`
    asserting every exact D-04 value:

```rust
assert_eq!(metadata.inventory_id, "prusaslicer.gcode-output");
assert_eq!(metadata.vendor_id, "prusaslicer");
assert_eq!(metadata.flavor_id, FlavorId::PrusaSlicer);
assert_eq!(metadata.origin, FeatureOrigin::SharedDownstream);
assert_eq!(metadata.parity_dependency, ParitySurface::generated_outputs());
assert_eq!(metadata.checklist_status, ChecklistStatus::FutureCandidate);
assert_eq!(metadata.source_ref, VendorSourceRef::prusa_slicer_version_2_9_5());
assert_eq!(metadata.source_path, "tests/fff_print/test_gcodewriter.cpp");
assert_eq!(metadata.fixture_path, "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode");
assert_eq!(metadata.expected_summary_path, "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv");
assert_eq!(metadata.scope_record_path, "packages/prusa-gcode-output-scope/gcode-output-scope.md");
assert_eq!(metadata.reserved_future_status_token, "fork.prusaslicer.gcode-output");
```

    Add or update a future-candidate filter test so it proves the G-code row is
    visible through `capabilities_by_checklist_status(ChecklistStatus::FutureCandidate)`.
    The test must collect capability IDs and assert that the collection
    contains both `prusaslicer.project-file` and `prusaslicer.gcode-output`.
    If the existing test only checks Bambu or profile-schema rows, keep those
    assertions and add the G-code assertion without weakening existing coverage.

    Update `runtime_claim_words_do_not_become_public_helper_names`:

    - Add `let gcode_output_metadata = prusa_gcode_output_metadata();`.
    - Add `"prusa_gcode_output_metadata"` to `public_helper_names`.
    - Add the G-code metadata fields to `metadata_values`.
    - Keep risky words limited to overclaiming terms: `verified`, `supported`, `runtime`, `dispatch`, `release`, `executable`, `printer`, `firmware`, `sync`, `host`, `network`, `credential`, `cloud`.

    Do not forbid the required tokens `generated-outputs`,
    `gcode-output`, or `fork.prusaslicer.gcode-output` in metadata values,
    because D-04 and the contract types require those exact planning tokens.
  </action>
  <acceptance_criteria>
    - `rg -n 'shared_downstream_filter_returns_source_observed_prusa_rows' packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` succeeds.
    - `rg -n '\"prusaslicer.project-file\", \"prusaslicer.gcode-output\"' packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` succeeds.
    - `rg -n 'prusa_gcode_output_registry_row_traces_to_source_and_generated_output_dependency' packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` succeeds.
    - `rg -n 'prusa_gcode_output_metadata_exposes_fixture_scope_expected_summary_and_reserved_status' packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` succeeds.
    - `rg -n 'capabilities_by_checklist_status\\(ChecklistStatus::FutureCandidate\\)|prusaslicer\\.gcode-output' packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` succeeds after the future-candidate filter test is updated.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test` passes.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test` still passes.
    - `! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:clippy</automated>
    <automated>! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv</automated>
    <automated>git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs</automated>
  </verify>
  <done>Registry tests prove the G-code output row is traceable, shared-downstream, future-candidate, generated-output dependent, and still not a published parity/status surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| parser metadata -> registry capability | Constants from `prusa_gcode_output` become registry-visible planning metadata. |
| registry capability -> downstream consumers | Consumers can filter capabilities by origin/status and may overread the row as verified behavior. |
| registry tests -> public claims | Public helper names and metadata assertions define what the Rust API appears to promise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-47-02-01 | Spoofing | `FlavorProvenance` | mitigate | Build provenance from `PRUSA_GCODE_OUTPUT_*` constants and assert source ref/path against `prusa_gcode_output_metadata()`. |
| T-47-02-02 | Tampering | `PRUSA_CAPABILITIES` | mitigate | Tests assert exact capability ID, feature surface/category, origin, dependency, status, provenance length, and source path. |
| T-47-02-03 | Repudiation | `future_parity_notes` and filters | mitigate | Capability note says summary-only evidence and Phase 48 command are required before output behavior is claimed; tests keep row in `FutureCandidate`. |
| T-47-02-04 | Elevation of Privilege | registry expansion | accept | Registry remains static data with no filesystem, process, network, release, printer, or sync functions. Existing crate purity and clippy/rustfmt checks cover this surface. |
| T-47-02-05 | Tampering | public helper names | mitigate | Update public-name guard to include `prusa_gcode_output_metadata` and reject runtime/release/executable/printer/network/sync/credential/cloud terms. |
</threat_model>

<verification>
Run:

```bash
bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test
bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check
bazel test //packages/slic3r-rust/crates/slic3r_flavors:clippy
! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv
! bazel query //packages/parity:prusaslicer_gcode_output_parity
git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
```
</verification>

<success_criteria>
PGSUM-02 is satisfied for the registry traceability path: the G-code output
capability is present as static, typed, future-candidate Prusa metadata with a
generated-output dependency and exact provenance. PGSUM-03 remains satisfied
because tests prevent filter drift and overclaiming public helper names.
</success_criteria>

<output>
After completion, create `.planning/phases/47-rust-prusa-g-code-summary-boundary/47-02-SUMMARY.md`
</output>
