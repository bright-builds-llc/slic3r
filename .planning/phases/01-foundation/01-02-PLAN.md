______________________________________________________________________

phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:

- 01-01
  files_modified:
- packages/legacy-slic3r/
- packages/legacy-slic3r/README.md
- packages/legacy-slic3r/BUILD.bazel
- BUILD.bazel
- README.md
  autonomous: true
  requirements:
- MONO-02
  user_setup: []
  must_haves:
  truths:
  - Contributors can see the retained legacy implementation in the locked package location without confusing it for the preferred future implementation.
  - The legacy tree is relocated or mirrored into `packages/legacy-slic3r` without behavioral cleanup masquerading as structural work.
    artifacts:
  - `packages/legacy-slic3r` contains the retained legacy tree.
  - `packages/legacy-slic3r/README.md` explains the reference-only role.
  - Bazel-visible wrapper targets exist for the retained package.
    key_links:
  - Root Bazel targets and package-local Bazel targets point at the retained legacy package location.
  - The legacy package docs explicitly connect its “reference oracle” role to the broader migration docs.

______________________________________________________________________

<objective>
Relocate or mirror the legacy Slic3r tree into the retained reference package and fence it off as the visible oracle package.

Purpose: make the legacy boundary real in the repo structure before later phases wrap legacy build/test behavior and before contributors start touching the Rust path.
Output: `packages/legacy-slic3r` populated, reference-only messaging in place, and Bazel wrappers wired to the retained package location.
</objective>

\<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

[Relevant source files:]
@README.md
@Build.PL
@slic3r.pl
@lib/
@src/
@xs/
@package/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the legacy tree into the retained reference package</name>
  <files>packages/legacy-slic3r/, BUILD.bazel</files>
  <action>Move or mirror the current legacy tree into `packages/legacy-slic3r` using a git-aware relocation strategy that preserves behavior and minimizes content edits. Treat this as a structural relocation only. Do not “clean up” the legacy code during the move. If any integration edits are unavoidable for Bazel visibility, keep them minimal and document them as parity-preserving.</action>
  <verify>Inspect the resulting tree and git move summary to confirm the legacy source now lives under `packages/legacy-slic3r` and that the diff is structural rather than behavioral.</verify>
  <done>The retained legacy implementation lives under `packages/legacy-slic3r` and the migration boundary is visible in the repo layout.</done>
</task>

<task type="auto">
  <name>Task 2: Mark the retained package as the visible reference oracle</name>
  <files>packages/legacy-slic3r/README.md, packages/legacy-slic3r/BUILD.bazel, README.md</files>
  <action>Add the locked reference-only messaging: the package must be named and described as the legacy reference package, remain visible in the tree, and state that new feature work targets the Rust implementation unless explicitly noted. Add only the Bazel wrapper stubs needed to make the package addressable from the top-level interface; do not attempt full legacy-script coverage yet.</action>
  <verify>Search the repo for the “legacy reference package” messaging and confirm the retained package is reachable from the top-level Bazel/package map surface.</verify>
  <done>Contributors can see `packages/legacy-slic3r`, understand its oracle role, and find Bazel-facing wrapper targets without mistaking it for the preferred implementation path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git diff --stat` for this step shows a structural relocation into `packages/legacy-slic3r`
- [ ] `rg -n "legacy reference package|behavioral oracle|new feature work targets the Rust implementation" packages/legacy-slic3r README.md`
- [ ] The root Bazel surface still resolves after the relocation
</verification>

\<success_criteria>

- All tasks completed
- Legacy tree is clearly inside `packages/legacy-slic3r`
- Reference-only messaging is explicit and visible
- No legacy cleanup or scope creep is bundled into the relocation
  \</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
