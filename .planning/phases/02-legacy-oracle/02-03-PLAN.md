---
phase: 02-legacy-oracle
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - packages/legacy-slic3r/README.md
  - docs/port/README.md
  - docs/port/checklist.md
  - docs/port/package-map.md
  - docs/port/parity-matrix.md
autonomous: true
requirements:
  - LEGA-02
  - LEGA-03
user_setup: []
must_haves:
  truths:
    - Contributors can tell from the repo and docs that the legacy package remains reference-only even though Bazel can build and test it.
    - Maintainers can see which legacy tests are the trusted oracle set, which are deferred, and what macOS prerequisites the wrapped oracle path assumes.
  artifacts:
    - `packages/legacy-slic3r/README.md` documents the reference-only oracle role and Bazel wrapper caveats.
    - `docs/port/` reflects the trusted oracle set, deferred legacy surfaces, and macOS prerequisites.
    - The parity and package-map docs remain aligned with the actual repo state.
  key_links:
    - The legacy package README and `docs/port/` describe the same oracle boundary and caveat model.
    - The documented trusted oracle set matches the Bazel labels exposed in Plans 01 and 02.
---

<objective>
Document the legacy oracle surface so contributors understand exactly what Phase 2 enabled and what remains deferred.

Purpose: keep the retained package visibly reference-only while making the oracle build/test path and its macOS assumptions transparent.
Output: updated package and port-control-plane docs describing the trusted oracle set, deferred legacy surfaces, and wrapper caveats.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-legacy-oracle/02-CONTEXT.md
@.planning/phases/02-legacy-oracle/02-RESEARCH.md

# Prior plan outputs:
@.planning/phases/02-legacy-oracle/02-01-SUMMARY.md
@.planning/phases/02-legacy-oracle/02-02-SUMMARY.md

[Relevant source files:]
@packages/legacy-slic3r/README.md
@docs/port/README.md
@docs/port/checklist.md
@docs/port/package-map.md
@docs/port/parity-matrix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the reference-only oracle boundary and prerequisites</name>
  <files>packages/legacy-slic3r/README.md, docs/port/README.md, docs/port/package-map.md</files>
  <action>Update the package-level and port-control-plane docs so they clearly state that Bazel support exists to preserve the oracle, not to revive the legacy package as a preferred implementation surface. Document the macOS prerequisites and caveat model for the wrapped legacy path, including any manual assumptions the wrappers still rely on.</action>
  <verify>Search the updated docs for the phrases that establish the legacy package as reference-only and for the macOS prerequisite/caveat guidance.</verify>
  <done>Contributors can read the repo and package docs and understand the oracle boundary, the purpose of Bazel support, and the macOS assumptions of the retained path.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the trusted oracle test set and deferred legacy surfaces</name>
  <files>docs/port/checklist.md, docs/port/parity-matrix.md, docs/port/README.md</files>
  <action>Document which legacy build/test surfaces are now trusted oracle checks on macOS, which legacy surfaces are retained but deferred, and how reviewers should interpret that distinction. Keep the status model aligned with the existing Phase 1 docs and avoid implying GUI or release packaging coverage that this phase does not provide.</action>
  <verify>Search `docs/port/` for the trusted oracle terminology, deferred legacy surface notes, and the existing status vocabulary to confirm the docs stay internally consistent.</verify>
  <done>The docs make the trusted oracle set and deferred legacy surfaces explicit, and reviewers can tell what Phase 2 actually guarantees.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The legacy package README and `docs/port/` both describe the package as reference-only
- [ ] The trusted oracle set and deferred legacy surfaces are documented explicitly
- [ ] The docs do not imply GUI or release packaging support in this phase
</verification>

<success_criteria>

- All tasks completed
- The retained legacy package stays visibly reference-only
- Oracle caveats and prerequisites are transparent to contributors
- The trusted oracle set is documented well enough for later parity planning
</success_criteria>

<output>
After completion, create `.planning/phases/02-legacy-oracle/02-03-SUMMARY.md`
</output>
