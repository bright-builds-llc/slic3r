---
phase: 02-legacy-oracle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/legacy-slic3r/BUILD.bazel
  - packages/BUILD.bazel
  - BUILD.bazel
  - tools/bazel/legacy/check_legacy_prereqs.sh
  - tools/bazel/legacy/build_legacy_oracle.sh
autonomous: true
requirements:
  - MONO-03
  - LEGA-01
user_setup: []
must_haves:
  truths:
    - Maintainer can invoke the retained legacy oracle build through a Bazel target on macOS.
    - Bazel exposes the legacy build path honestly, with actionable prerequisite failures instead of opaque shell errors.
  artifacts:
    - Bazel-visible build targets exist for the retained legacy package.
    - A prerequisite-check script exists for the macOS legacy oracle path.
    - A wrapper script exists that drives the chosen retained legacy build entrypoint.
  key_links:
    - Root and package-level Bazel labels point at the same legacy build wrapper.
    - The prerequisite check gates the build wrapper before the legacy bootstrap path runs.
---

<objective>
Wrap the retained legacy build path in Bazel so the legacy package is usable as a macOS oracle without pretending it is already hermetic.

Purpose: make the reference implementation buildable through the new top-level build system while preserving the existing legacy mechanics.
Output: Bazel targets, prerequisite gates, and a build wrapper for the retained legacy package.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-legacy-oracle/02-CONTEXT.md
@.planning/phases/02-legacy-oracle/02-RESEARCH.md

[Relevant source files:]
@BUILD.bazel
@packages/BUILD.bazel
@packages/legacy-slic3r/BUILD.bazel
@packages/legacy-slic3r/Build.PL
@packages/legacy-slic3r/xs/Build.PL
@packages/legacy-slic3r/src/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prerequisite gate and legacy build wrapper</name>
  <files>tools/bazel/legacy/check_legacy_prereqs.sh, tools/bazel/legacy/build_legacy_oracle.sh, packages/legacy-slic3r/BUILD.bazel</files>
  <action>Create a macOS-focused prerequisite check for the retained legacy oracle path and a wrapper script that drives the selected legacy build entrypoint without rewriting legacy internals. Prefer the retained `Build.PL`/XS bootstrap path as the first-class oracle entrypoint unless the repo evidence makes that impossible. Fail fast with actionable output for missing tools or env assumptions such as `cpanm`, Xcode CLT, or Boost path variables. Do not attempt hermetic dependency cleanup here.</action>
  <verify>Run the prerequisite check directly and then invoke the new package-level Bazel build target to confirm the wrapper either builds successfully or fails with the documented prerequisite message.</verify>
  <done>The retained legacy package has a real Bazel-exposed build wrapper and a clear prerequisite gate for the macOS oracle path.</done>
</task>

<task type="auto">
  <name>Task 2: Expose stable Bazel labels for the legacy build surface</name>
  <files>BUILD.bazel, packages/BUILD.bazel, packages/legacy-slic3r/BUILD.bazel</files>
  <action>Add stable root and package-level labels for the legacy oracle build path so contributors can discover and invoke it from Bazel. Keep the target structure thin and explicit: this phase is exposing the oracle, not redesigning the build graph. Ensure the labels line up with the existing Phase 1 package map and do not introduce GUI or packaging scope creep.</action>
  <verify>Run the intended top-level Bazel build label and confirm it resolves to the retained legacy wrapper path instead of an ad hoc shell command.</verify>
  <done>Contributors can discover and invoke the retained legacy build path from the Bazel surface using stable labels.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The macOS prerequisite gate prints actionable failures for missing legacy prerequisites
- [ ] The legacy build wrapper is reachable from Bazel labels at the package and/or root surface
- [ ] The wrapped build path does not introduce GUI or release packaging scope into Phase 2
</verification>

<success_criteria>

- All tasks completed
- A real Bazel-driven legacy build path exists on macOS
- Prerequisites are surfaced honestly instead of hidden
- The retained package remains a wrapper-driven oracle surface rather than a modernization target
</success_criteria>

<output>
After completion, create `.planning/phases/02-legacy-oracle/02-01-SUMMARY.md`
</output>
