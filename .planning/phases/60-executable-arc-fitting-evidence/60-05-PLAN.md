---
phase: 60-executable-arc-fitting-evidence
plan: "05"
type: execute
wave: 5
depends_on: ["60-03", "60-04"]
files_modified:
  - packages/prusa-arc-fitting-scope/README.md
  - packages/prusa-arc-fitting-scope/arc-fitting-scope.md
  - packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
  - packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh
autonomous: true
requirements: [ARCEV-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 60-2026-06-24T15-09-13
generated_at: 2026-06-24T15:37:11.000Z
must_haves:
  truths:
    - "Maintainer can read the arc-fitting scope docs and see that Phase 60 published the public command/status row."
    - "Scope docs keep the Phase 57 scope contract historical boundary while removing stale Phase 60 planned/future wording."
    - "Scope verifier exact-text checks reject stale Phase 60 planned/future wording and preserve forbidden-claim guards."
  artifacts:
    - path: "packages/prusa-arc-fitting-scope/arc-fitting-scope.md"
      provides: "Published scope contract wording for Phase 60 arc-fitting evidence"
      contains: "Phase 60 published `fork.prusaslicer.arc-fitting`"
    - path: "packages/prusa-arc-fitting-scope/README.md"
      provides: "Scope package overview with published public command/status wording"
      contains: "bazel run //packages/parity:prusaslicer_arc_fitting_parity"
    - path: "packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh"
      provides: "Exact scope doc checks for published Phase 60 wording"
      contains: "Published narrow status row"
  key_links:
    - from: "packages/prusa-arc-fitting-scope/arc-fitting-scope.md"
      to: "packages/parity/status.tsv"
      via: "Published narrow status row traceability"
      pattern: "fork\\.prusaslicer\\.arc-fitting"
    - from: "packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh"
      to: "packages/prusa-arc-fitting-scope/arc-fitting-scope.md"
      via: "require_section_table_row exact checks"
      pattern: "Published narrow status row"
---

<objective>
Update the arc-fitting scope contract and scope verifier from Phase 60 planned wording to Phase 60 published wording.

Purpose: ARCEV-03 requires public scope docs to reflect the exact narrow publication after the command, mutation guards, status row, and package docs exist, without widening the evidence boundary.
Output: Scope README, scope contract, and scope verifier/test checks that require published Phase 60 command/status wording and reject stale planned/future wording.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
@.planning/phases/60-executable-arc-fitting-evidence/60-RESEARCH.md
@.planning/phases/60-executable-arc-fitting-evidence/60-03-SUMMARY.md
@.planning/phases/60-executable-arc-fitting-evidence/60-04-SUMMARY.md
@packages/parity/status.tsv
@packages/prusa-arc-fitting-scope/README.md
@packages/prusa-arc-fitting-scope/arc-fitting-scope.md
@packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
@packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish scope docs for the Phase 60 arc-fitting evidence row</name>
  <files>packages/prusa-arc-fitting-scope/README.md, packages/prusa-arc-fitting-scope/arc-fitting-scope.md</files>
  <read_first>
    - packages/prusa-arc-fitting-scope/README.md
    - packages/prusa-arc-fitting-scope/arc-fitting-scope.md
    - packages/parity/status.tsv
    - packages/parity/README.md
    - packages/parity-fixtures/README.md
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <action>Update `packages/prusa-arc-fitting-scope/README.md` so the package overview says Phase 57 recorded the scope contract and Phase 60 now publishes `bazel run //packages/parity:prusaslicer_arc_fitting_parity` and `fork.prusaslicer.arc-fitting` for checked-in arc summary evidence only. In `packages/prusa-arc-fitting-scope/arc-fitting-scope.md`, replace the Scope Record row `Planned evidence command` with `Public evidence command` and value `Phase 60 published \`bazel run //packages/parity:prusaslicer_arc_fitting_parity\`; Phase 57 only planned the target and did not create it.` Replace the Scope Record row `Planned status token` with `Published narrow status row` and value `Phase 60 published \`fork.prusaslicer.arc-fitting\` after executable evidence passed; Phase 57 only planned the row and did not publish it.` In the Arc-Fitting Traceability table, replace `Planned public evidence command` with `Public evidence command` and value `` `bazel run //packages/parity:prusaslicer_arc_fitting_parity` ``. Replace `Planned narrow status row` with `Published narrow status row` and value `Phase 60 published \`fork.prusaslicer.arc-fitting\` as the narrow v1.15 checked-in arc summary evidence slice backed by the Phase 57 scope contract, Phase 58 fixture corpus, Phase 59 Rust parser/readiness boundary, and Phase 60 public parity command.` Rename `## Planned Status Wording` to `## Published Status Wording`. In that section, use the sentence `The Phase 60 published status token is \`fork.prusaslicer.arc-fitting\`.` followed by `Phase 60 published \`fork.prusaslicer.arc-fitting\` as the narrow v1.15 checked-in arc summary evidence slice backed by the Phase 57 scope contract, Phase 58 fixture corpus, Phase 59 Rust parser/readiness boundary, and Phase 60 public parity command.` Keep `generated-outputs` as `in progress`, keep `fork.prusaslicer.gcode-output` limited to the existing semantic Prusa G-code evidence slice, and keep every deferred term from D-03 and D-11. Do not add UI, runtime, release, sync, network/device, host upload, upstream source import, printer-runtime, or non-Prusa support claims. This implements D-10, D-11, and D-13 for scope docs.</action>
  <verify>
    <automated>mdformat --check packages/prusa-arc-fitting-scope/README.md packages/prusa-arc-fitting-scope/arc-fitting-scope.md</automated>
    <automated>bazel run //packages/prusa-arc-fitting-scope:verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Public evidence command|Published narrow status row|Published Status Wording' packages/prusa-arc-fitting-scope/arc-fitting-scope.md` returns matches for all three labels.
    - `rg -n 'Phase 60 published `fork.prusaslicer.arc-fitting` as the narrow v1.15 checked-in arc summary evidence slice backed by the Phase 57 scope contract, Phase 58 fixture corpus, Phase 59 Rust parser/readiness boundary, and Phase 60 public parity command\\.' packages/prusa-arc-fitting-scope/arc-fitting-scope.md` returns a match.
    - `rg -n 'bazel run //packages/parity:prusaslicer_arc_fitting_parity|fork.prusaslicer.arc-fitting' packages/prusa-arc-fitting-scope/README.md packages/prusa-arc-fitting-scope/arc-fitting-scope.md` returns matches in both files.
    - `! rg -n 'Planned evidence command|Planned status token|Planned public evidence command|Planned narrow status row|## Planned Status Wording|Phase 60 planned' packages/prusa-arc-fitting-scope/README.md packages/prusa-arc-fitting-scope/arc-fitting-scope.md` exits 0.
  </acceptance_criteria>
  <done>Scope docs publish the Phase 60 command/status row, retain historical Phase 57 boundaries, preserve deferred surfaces, and contain no stale Phase 60 planned/future wording.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align scope verifier exact-text checks with published scope docs</name>
  <files>packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh, packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh</files>
  <read_first>
    - packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
    - packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh
    - packages/prusa-arc-fitting-scope/README.md
    - packages/prusa-arc-fitting-scope/arc-fitting-scope.md
    - packages/parity/status.tsv
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <behavior>
    - RED: The scope verifier still accepts or requires Phase 60 planned/future wording before this task.
    - GREEN: The scope verifier requires published Phase 60 command/status wording and rejects stale planned/future wording.
  </behavior>
  <action>Update `verify_prusa_arc_fitting_scope.sh` so `verify_scope_record` requires the exact Scope Record rows from Task 1: `Public evidence command` with value `Phase 60 published \`bazel run //packages/parity:prusaslicer_arc_fitting_parity\`; Phase 57 only planned the target and did not create it.` and `Published narrow status row` with value `Phase 60 published \`fork.prusaslicer.arc-fitting\` after executable evidence passed; Phase 57 only planned the row and did not publish it.` Update `verify_traceability_record` to require `Public evidence command` and `Published narrow status row` with the exact values from Task 1. Replace any section constant or section check for `## Planned Status Wording` with `## Published Status Wording`. Add explicit `reject_text` checks for these stale strings in `arc-fitting-scope.md`: `Planned evidence command`, `Planned status token`, `Planned public evidence command`, `Planned narrow status row`, `## Planned Status Wording`, and `Phase 60 planned \`fork.prusaslicer.arc-fitting\``. Update `verify_prusa_arc_fitting_scope_test.sh` valid fixtures to the new rows and section. Add one focused stale-scope-doc mutation that replaces `Published narrow status row` with `Planned narrow status row` and asserts the diagnostic mentions `Published narrow status row`; add one focused stale-section mutation that replaces `## Published Status Wording` with `## Planned Status Wording` and asserts the diagnostic mentions `Published Status Wording`. Keep existing inventory, status-row, deferred-scope, local-only, and forbidden-claim mutation coverage.</action>
  <verify>
    <automated>bash -n packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh</automated>
    <automated>shfmt -l -d packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh</automated>
    <automated>bazel run //packages/prusa-arc-fitting-scope:verify</automated>
    <automated>bazel test --cache_test_results=no //packages/prusa-arc-fitting-scope:verify_prusa_arc_fitting_scope_test --test_output=errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Published narrow status row|Published Status Wording|Phase 60 published' packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh` returns matches in both files.
    - `rg -n 'Planned evidence command|Planned status token|Planned public evidence command|Planned narrow status row|## Planned Status Wording|Phase 60 planned `fork.prusaslicer.arc-fitting`' packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh` returns no matches.
    - `bazel run //packages/prusa-arc-fitting-scope:verify` exits 0.
    - `bazel test --cache_test_results=no //packages/prusa-arc-fitting-scope:verify_prusa_arc_fitting_scope_test --test_output=errors` exits 0.
  </acceptance_criteria>
  <done>Scope verifier exact-text checks enforce published Phase 60 scope docs and mutation tests reject stale planned/future wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scope docs -> maintainer interpretation | Scope docs explain what the public arc-fitting evidence proves and does not prove. |
| scope docs -> scope verifier | Exact checks decide whether scope docs are stale or overclaiming. |
| status row -> scope docs | Scope docs must describe the checked-in `fork.prusaslicer.arc-fitting` row without promoting broad generated-output status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-05-01 | Spoofing | scope docs | mitigate | Require exact `bazel run //packages/parity:prusaslicer_arc_fitting_parity` and `fork.prusaslicer.arc-fitting` wording tied to checked-in arc summary evidence only. |
| T-60-05-02 | Tampering | scope verifier | mitigate | Add exact-text checks and mutation tests that reject stale planned/future wording. |
| T-60-05-03 | Repudiation | published evidence boundary | mitigate | Preserve Phase 57-60 chain wording and explicit deferrals for byte parity, generated-output parity, runtime, GUI, release, sync, network/device, host upload, upstream import, printer-runtime, and non-Prusa behavior. |
| T-60-05-04 | Information Disclosure | scope docs | mitigate | Docs state no secrets, credentials, private data, network/device behavior, upstream source import, runtime discovery, host upload, or printer-runtime exposure is introduced. |
</threat_model>

<verification>
Run the task verification commands, then run:

```bash
bazel run //packages/parity:prusaslicer_arc_fitting_parity
bazel run //packages/parity:prusaslicer_gcode_output_parity
bazel run //packages/parity-fixtures:verify_prusa_arc_fitting_fixture
bazel run //packages/prusa-arc-fitting-scope:verify
git diff --check -- packages/prusa-arc-fitting-scope/README.md packages/prusa-arc-fitting-scope/arc-fitting-scope.md packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh
```
</verification>

<success_criteria>
ARCEV-03 scope documentation is satisfied when the scope README/contract and verifier publish the Phase 60 command/status row, reject stale planned/future wording, keep broad `generated-outputs` in progress, and preserve all forbidden-claim guards.
</success_criteria>

<output>
After completion, create `.planning/phases/60-executable-arc-fitting-evidence/60-05-SUMMARY.md`.
</output>
