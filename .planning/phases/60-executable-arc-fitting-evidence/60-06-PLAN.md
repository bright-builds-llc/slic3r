---
phase: 60-executable-arc-fitting-evidence
plan: "06"
type: execute
wave: 6
depends_on: ["60-05"]
files_modified:
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/parity-matrix.md
  - docs/port/migration-guidance.md
autonomous: true
requirements: [ARCEV-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 60-2026-06-24T15-09-13
generated_at: 2026-06-24T15:37:11.000Z
must_haves:
  truths:
    - "Maintainer can inspect public port docs and see fork.prusaslicer.arc-fitting described as the narrow checked-in arc summary evidence slice."
    - "Public port docs keep broad generated-outputs in progress and keep fork.prusaslicer.gcode-output separate."
    - "Public port docs contain no stale Phase 60 planned/future current-state wording for arc-fitting."
  artifacts:
    - path: "docs/port/parity-matrix.md"
      provides: "Generated-output and fork arc-fitting status wording"
      contains: "fork.prusaslicer.arc-fitting"
    - path: "docs/port/migration-guidance.md"
      provides: "Maintainer migration boundary wording for arc-fitting fixtures/status"
      contains: "expected-arc-summary.tsv"
    - path: "docs/port/README.md"
      provides: "Public port overview with current arc-fitting evidence state"
      contains: "bazel run //packages/parity:prusaslicer_arc_fitting_parity"
    - path: "docs/port/package-map.md"
      provides: "Package ownership and Phase 57-60 history wording"
      contains: "packages/prusa-arc-fitting-scope"
  key_links:
    - from: "docs/port/parity-matrix.md"
      to: "packages/parity/status.tsv"
      via: "matching generated-outputs and fork.prusaslicer.arc-fitting wording"
      pattern: "narrow Prusa arc-fitting checked-in summary evidence slice"
    - from: "docs/port/package-map.md"
      to: "//packages/parity:prusaslicer_arc_fitting_parity"
      via: "package ownership text"
      pattern: "bazel run //packages/parity:prusaslicer_arc_fitting_parity"
---

<objective>
Update public port docs so they publish the exact narrow arc-fitting evidence slice while keeping broad generated outputs in progress.

Purpose: Complete the public port documentation portion of ARCEV-03 and close stale planned/future wording after Phase 60 command/status/docs publication.
Output: Updated parity matrix, migration guidance, port README, and package map for the narrow `fork.prusaslicer.arc-fitting` evidence slice.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
@.planning/phases/60-executable-arc-fitting-evidence/60-RESEARCH.md
@.planning/phases/60-executable-arc-fitting-evidence/60-05-SUMMARY.md
@packages/parity/status.tsv
@packages/parity/README.md
@packages/prusa-arc-fitting-scope/arc-fitting-scope.md
@docs/port/README.md
@docs/port/package-map.md
@docs/port/parity-matrix.md
@docs/port/migration-guidance.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update parity matrix and migration guidance for arc-fitting</name>
  <files>docs/port/parity-matrix.md, docs/port/migration-guidance.md</files>
  <read_first>
    - docs/port/parity-matrix.md
    - docs/port/migration-guidance.md
    - packages/parity/status.tsv
    - packages/parity/README.md
    - packages/prusa-arc-fitting-scope/arc-fitting-scope.md
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <action>In `docs/port/parity-matrix.md`, update the `Generated outputs` row so it mentions both existing `fork.prusaslicer.gcode-output` and new `fork.prusaslicer.arc-fitting` as narrow fork evidence slices while the row status cell remains exactly `in progress`. Add `fork.prusaslicer.arc-fitting` to the Fork Parity Interpretation section with this exact evidence description: `fork.prusaslicer.arc-fitting` is verified in `packages/parity/status.tsv` only for the narrow Prusa arc-fitting checked-in summary evidence slice through `bazel run //packages/parity:prusaslicer_arc_fitting_parity`, backed by `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/expected-arc-summary.tsv`, `slic3r_flavors::prusa_arc_fitting`, `prusa_arc_fitting_summary_lines`, the Phase 57 scope contract, Phase 58 fixture corpus, Phase 59 Rust parser/readiness boundary, and Phase 60 public command/status/docs. State that broad `generated-outputs` remains `in progress`. State that the arc-fitting slice does not prove byte-for-byte G-code parity, full generated-output parity, broad generated-output verification, full ArcWelder algorithm equivalence, tolerance or geometry parity, printability, firmware behavior, printer-runtime behavior, GUI behavior, support generation, wall seam behavior, STEP import, full 3MF import/export, binary G-code, thumbnails, post-processing, host upload, network/device behavior, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, sync automation, or non-Prusa fork behavior. In `docs/port/migration-guidance.md`, add `prusaslicer.arc-fitting` to the Fixture Update Protocol: fixture work starts from `packages/prusa-arc-fitting-scope`, Phase 58 created `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/`, `arc-fitting-observations.gcode`, `fixture-provenance.tsv`, and `expected-arc-summary.tsv`; Phase 59 owns `slic3r_flavors::prusa_arc_fitting` and `prusa_arc_fitting_summary_lines`; Phase 60 publishes `bazel run //packages/parity:prusaslicer_arc_fitting_parity` and `fork.prusaslicer.arc-fitting`. In the Future Fork Parity Status section, add the same narrow evidence slice and deferral wording. This implements D-10, D-11, and D-13 for public parity matrix and migration guidance.</action>
  <verify>
    <automated>mdformat --check docs/port/parity-matrix.md docs/port/migration-guidance.md</automated>
    <automated>bazel run //packages/parity:status</automated>
    <automated>bazel run //packages/parity:prusaslicer_arc_fitting_parity</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'narrow Prusa arc-fitting checked-in summary evidence slice|Phase 57 scope contract|Phase 58 fixture corpus|Phase 59 Rust parser/readiness boundary|Phase 60 public command/status/docs' docs/port/parity-matrix.md docs/port/migration-guidance.md` returns matches in both files.
    - `rg -n 'expected-arc-summary.tsv|prusa_arc_fitting_summary_lines|bazel run //packages/parity:prusaslicer_arc_fitting_parity|fork.prusaslicer.arc-fitting' docs/port/parity-matrix.md docs/port/migration-guidance.md` returns matches.
    - `rg -n '\\| Generated outputs \\|.*\\| .*in progress.* \\|' docs/port/parity-matrix.md` returns a match.
    - `rg -n 'fork.prusaslicer.gcode-output.*fork.prusaslicer.arc-fitting|fork.prusaslicer.arc-fitting.*fork.prusaslicer.gcode-output' docs/port/parity-matrix.md` returns a match.
    - `! rg -n 'future fork\\.prusaslicer\\.arc-fitting|Phase 60 planned `fork\\.prusaslicer\\.arc-fitting`' docs/port/parity-matrix.md docs/port/migration-guidance.md` exits 0.
  </acceptance_criteria>
  <done>Parity matrix and migration guidance publish the narrow arc-fitting evidence slice, preserve the separate G-code output slice, keep broad generated outputs in progress, and retain all deferred surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Update port README and package map ownership for arc-fitting</name>
  <files>docs/port/README.md, docs/port/package-map.md</files>
  <read_first>
    - docs/port/README.md
    - docs/port/package-map.md
    - docs/port/parity-matrix.md
    - docs/port/migration-guidance.md
    - packages/parity/status.tsv
    - packages/parity/README.md
    - packages/prusa-arc-fitting-scope/arc-fitting-scope.md
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <action>Update `docs/port/README.md` Current Parity Visibility State with a bullet that says `bazel run //packages/parity:prusaslicer_arc_fitting_parity` is the shared public command for the narrow Prusa arc-fitting checked-in summary evidence slice backed by the Phase 57 scope contract, Phase 58 fixture corpus, Phase 59 Rust parser/readiness boundary, and Phase 60 public command/status/docs. Add `prusaslicer.arc-fitting` to the fixture corpus list. Add a `## Current Prusa Arc-Fitting Evidence State` section after the Prusa G-code output section. That section must name `packages/prusa-arc-fitting-scope`, accepted source identity `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, source path `src/libslic3r/Geometry/ArcWelder.cpp`, fixture namespace `packages/parity-fixtures/forks/prusaslicer/prusaslicer.arc-fitting/`, expected artifact `expected-arc-summary.tsv`, Rust boundary `slic3r_flavors::prusa_arc_fitting`, helper `prusa_arc_fitting_summary_lines`, command `bazel run //packages/parity:prusaslicer_arc_fitting_parity`, status row `fork.prusaslicer.arc-fitting`, and evidence chain `Phase 57 scope contract, Phase 58 fixture corpus, Phase 59 Rust parser/readiness boundary, and Phase 60 public command/status/docs`. State that existing `fork.prusaslicer.gcode-output` remains separate and broad `generated-outputs` remains `in progress`. Include the D-03/D-11 deferral list: byte-for-byte G-code parity, full generated-output parity, broad generated-output verification, full ArcWelder algorithm equivalence, tolerance or geometry parity, printability, firmware behavior, printer-runtime behavior, GUI behavior, support generation, wall seam behavior, STEP import, full 3MF import/export, binary G-code, thumbnails, post-processing, host upload, network/device behavior, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, sync automation, and non-Prusa fork behavior. Update `docs/port/package-map.md` so `packages/slic3r-rust` owns the Phase 59 `slic3r_flavors::prusa_arc_fitting` parser/readiness boundary and Phase 60 summary adapter, `packages/parity` owns `bazel run //packages/parity:prusaslicer_arc_fitting_parity` and the `fork.prusaslicer.arc-fitting` status row, `packages/parity-fixtures` owns the Phase 58 Prusa arc-fitting fixture namespace and `expected-arc-summary.tsv`, and a new `packages/prusa-arc-fitting-scope` package row owns the Phase 57 scope contract and verifier. Add Phase 57, 58, 59, and 60 note bullets for the arc-fitting chain, using the same exact phase-chain wording. This implements D-10, D-11, and D-13 for public README/package ownership docs.</action>
  <verify>
    <automated>mdformat --check docs/port/README.md docs/port/package-map.md</automated>
    <automated>bazel run //packages/parity:prusaslicer_arc_fitting_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Current Prusa Arc-Fitting Evidence State|narrow Prusa arc-fitting checked-in summary evidence slice|Phase 57 scope contract, Phase 58 fixture corpus, Phase 59 Rust parser/readiness boundary, and Phase 60 public command/status/docs' docs/port/README.md` returns matches.
    - `rg -n 'packages/prusa-arc-fitting-scope|slic3r_flavors::prusa_arc_fitting|expected-arc-summary.tsv|bazel run //packages/parity:prusaslicer_arc_fitting_parity|fork.prusaslicer.arc-fitting' docs/port/package-map.md` returns matches.
    - `rg -n 'fork.prusaslicer.gcode-output.*separate|separate.*fork.prusaslicer.gcode-output|generated-outputs.*in progress|in progress.*generated-outputs' docs/port/README.md docs/port/package-map.md` returns matches.
    - `! rg -n 'future fork\\.prusaslicer\\.arc-fitting|Phase 60 planned `fork\\.prusaslicer\\.arc-fitting`|planned public evidence command' docs/port/README.md docs/port/package-map.md` exits 0.
  </acceptance_criteria>
  <done>Port README and package map publish arc-fitting current-state ownership, include the Phase 57-60 chain, keep G-code output separate, and keep broad generated-output status in progress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public port docs -> maintainer decisions | Port docs communicate what evidence is verified and what remains deferred. |
| status TSV -> port docs | Port docs must match `packages/parity/status.tsv` without promoting broad generated-output status. |
| package ownership docs -> future executors | Package map guides future changes to the correct local checked-in packages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-06-01 | Spoofing | public port evidence wording | mitigate | Use exact status token, public command, and Phase 57-60 arc-fitting chain in all touched docs. |
| T-60-06-02 | Tampering | broad generated-output status | mitigate | Require `Generated outputs` to remain `in progress` and keep `fork.prusaslicer.gcode-output` separate from `fork.prusaslicer.arc-fitting`. |
| T-60-06-03 | Repudiation | stale planned/future wording | mitigate | Add grep acceptance checks that reject specific stale Phase 60 planned/future arc-fitting wording in touched port docs. |
| T-60-06-04 | Information Disclosure | docs-only publication | mitigate | Docs introduce no secrets, credentials, private data, file discovery, network/device behavior, upstream source import, host upload, runtime discovery, sync behavior, release behavior, or printer-runtime exposure. |
</threat_model>

<verification>
Run after both tasks:

```bash
mdformat --check docs/port/README.md docs/port/package-map.md docs/port/parity-matrix.md docs/port/migration-guidance.md
bazel run //packages/parity:status
bazel run //packages/parity:prusaslicer_arc_fitting_parity
bazel run //packages/parity:prusaslicer_gcode_output_parity
bazel run //packages/parity-fixtures:verify_prusa_arc_fitting_fixture
bazel run //packages/prusa-arc-fitting-scope:verify
awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv
git diff --check -- docs/port/README.md docs/port/package-map.md docs/port/parity-matrix.md docs/port/migration-guidance.md
```
</verification>

<success_criteria>
ARCEV-03 public port docs are complete when the parity matrix, migration guidance, port README, and package map all describe `fork.prusaslicer.arc-fitting` as the narrow checked-in arc summary evidence slice, keep broad `generated-outputs` in progress, keep `fork.prusaslicer.gcode-output` separate, and contain no stale planned/future current-state wording.
</success_criteria>

<output>
After completion, create `.planning/phases/60-executable-arc-fitting-evidence/60-06-SUMMARY.md`.
</output>
