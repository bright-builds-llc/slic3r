---
phase: 60-executable-arc-fitting-evidence
plan: "03"
type: execute
wave: 3
depends_on: ["60-01", "60-02"]
files_modified:
  - packages/parity/status.tsv
  - packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh
  - packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh
  - packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
  - packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh
autonomous: true
requirements: [ARCEV-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 60-2026-06-24T15-09-13
generated_at: 2026-06-24T15:27:46.600Z
must_haves:
  truths:
    - "Maintainer can inspect exactly one fork.prusaslicer.arc-fitting verified row in packages/parity/status.tsv."
    - "Broad generated-outputs remains exactly one in progress row."
    - "Existing fork.prusaslicer.gcode-output status wording remains unchanged."
  artifacts:
    - path: "packages/parity/status.tsv"
      provides: "Published narrow arc-fitting status row"
      contains: "fork.prusaslicer.arc-fitting\tverified\t//packages/parity:prusaslicer_arc_fitting_parity"
    - path: "packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh"
      provides: "Fixture verifier status-row publication enforcement"
      contains: "ARC_FITTING_STATUS_ROW"
    - path: "packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh"
      provides: "Scope verifier status-row publication enforcement"
      contains: "ARC_FITTING_STATUS_ROW"
  key_links:
    - from: "packages/parity/status.tsv"
      to: "//packages/parity:prusaslicer_arc_fitting_parity"
      via: "status evidence column"
      pattern: "prusaslicer_arc_fitting_parity"
    - from: "packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh"
      to: "packages/parity/status.tsv"
      via: "exact ARC_FITTING_STATUS_ROW check"
      pattern: "fork\\.prusaslicer\\.arc-fitting"
---

<objective>
Publish the exact narrow `fork.prusaslicer.arc-fitting` status row and update fail-closed status verifiers.

Purpose: ARCEV-03 requires public status publication only after the command and mutation guards pass, while preserving broad `generated-outputs` and existing G-code output semantics.
Output: One new exact status row and verifier/test updates that reject row drift, missing rows, duplicate rows, broad generated-output promotion, and existing G-code row widening.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
@.planning/phases/60-executable-arc-fitting-evidence/60-RESEARCH.md
@.planning/milestones/v1.14-phases/56-executable-semantic-g-code-evidence/56-03-SUMMARY.md
@packages/parity/status.tsv
@packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh
@packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh
@packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
@packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh

<interfaces>
Exact status row to add and enforce:
```text
fork.prusaslicer.arc-fitting	verified	//packages/parity:prusaslicer_arc_fitting_parity	Shared fixture comparison proves the narrow Prusa arc-fitting checked-in summary evidence slice backed by the Phase 57 scope contract, Phase 58 fixture corpus, Phase 59 Rust parser/readiness boundary, and Phase 60 public parity command only; byte-for-byte G-code parity, full generated-output parity, broad generated-output verification, full ArcWelder algorithm equivalence, tolerance or geometry parity, printability, firmware behavior, printer-runtime behavior, GUI behavior, support generation, wall seam behavior, STEP import, full 3MF import/export, binary G-code, thumbnails, post-processing, host upload, network/device behavior, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, sync automation, and non-Prusa fork behavior remain deferred
```

Existing G-code row must remain exactly:
```text
fork.prusaslicer.gcode-output	verified	//packages/parity:prusaslicer_gcode_output_parity	Shared fixture comparison proves the narrow semantic Prusa G-code evidence slice backed by the Phase 53 closed semantic scope contract, Phase 54 semantic fixture summary, Phase 55 Rust semantic parser/readiness boundary, and Phase 56 public parity command only; byte-for-byte G-code parity, full generated-output parity, toolpath geometry, extrusion behavior, timing, support generation, wall seam behavior, arc fitting, STEP import, full 3MF import/export, printer-runtime behavior, firmware or printability, GUI export or viewer behavior, binary G-code, thumbnails, post-processing, host upload, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, and sync automation remain deferred
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish and enforce the exact arc-fitting status row</name>
  <files>packages/parity/status.tsv, packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh, packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh</files>
  <read_first>
    - packages/parity/status.tsv
    - packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh
    - packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
    - packages/parity/compare_prusaslicer_arc_fitting.sh
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <action>Add the exact `fork.prusaslicer.arc-fitting` row shown in the interfaces block to `packages/parity/status.tsv` after `fork.prusaslicer.gcode-output`. Do not edit the existing `fork.prusaslicer.gcode-output` row text. In both `verify_prusa_arc_fitting_fixture.sh` and `verify_prusa_arc_fitting_scope.sh`, add `readonly ARC_FITTING_STATUS_ROW=$'...'` using the exact row text above. Replace the current Phase 57/58 absence check (`arc_status_count != 0`) with: require exact row once, require first field count `fork.prusaslicer.arc-fitting` equals `1`, require exactly one `generated-outputs` row with status `in progress`, and require exactly one unchanged `fork.prusaslicer.gcode-output` row. Preserve all existing forbidden-claim checks, local-only behavior checks, and generated-output guards. This implements D-08, D-09, D-13, and the status subset of D-12.</action>
  <verify>
    <automated>bazel run //packages/parity:status</automated>
    <automated>bazel run //packages/parity:prusaslicer_arc_fitting_parity</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_arc_fitting_fixture</automated>
    <automated>bazel run //packages/prusa-arc-fitting-scope:verify</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F '\t' '$1=="fork.prusaslicer.arc-fitting" && $2=="verified" && $3=="//packages/parity:prusaslicer_arc_fitting_parity" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `awk -F '\t' '$1=="fork.prusaslicer.gcode-output" { print }' packages/parity/status.tsv` prints the exact G-code row from the interfaces block.
    - `rg -n 'ARC_FITTING_STATUS_ROW' packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh` returns matches in both files.
  </acceptance_criteria>
  <done>Status publication is exact, local verifiers require the new row, broad `generated-outputs` remains in progress, and the existing G-code output row is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add status-row mutation coverage for fixture and scope verifiers</name>
  <files>packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh, packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh</files>
  <read_first>
    - packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh
    - packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh
    - packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh
    - packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
    - packages/parity/status.tsv
    - .planning/phases/60-executable-arc-fitting-evidence/60-CONTEXT.md
  </read_first>
  <behavior>
    - RED: Missing status-row mutation cases are visible with `rg -n 'fork.prusaslicer.arc-fitting.*wrong|stale|duplicate'`.
    - GREEN: Fixture and scope verifier tests reject missing arc row, duplicate arc row, wrong evidence target, broad generated-output promotion, and existing G-code row drift.
  </behavior>
  <action>Update both arc-fitting verifier mutation test scripts so their valid status fixtures include exactly one `generated-outputs	in progress` row, exactly one unchanged `fork.prusaslicer.gcode-output` row, and exactly one exact `fork.prusaslicer.arc-fitting	verified	//packages/parity:prusaslicer_arc_fitting_parity` row with the row text from this plan. Add focused failure cases in both test files: remove the arc row and assert the diagnostic mentions `fork.prusaslicer.arc-fitting`; duplicate the arc row and assert the diagnostic mentions `fork.prusaslicer.arc-fitting`; change the evidence target to `//packages/parity:prusaslicer_gcode_output_parity` and assert the diagnostic mentions `//packages/parity:prusaslicer_arc_fitting_parity`; change `generated-outputs` status from `in progress` to `verified` and assert the diagnostic mentions `generated-outputs`; mutate the existing G-code row by replacing `narrow semantic Prusa G-code evidence slice` with `narrow semantic and arc-fitting Prusa G-code evidence slice` and assert the diagnostic mentions `fork.prusaslicer.gcode-output`. Keep each test case as an isolated temp fixture mutation and preserve existing mutation coverage.</action>
  <verify>
    <automated>bash -n packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh</automated>
    <automated>shfmt -l -d packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh</automated>
    <automated>bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_arc_fitting_fixture_test //packages/prusa-arc-fitting-scope:verify_prusa_arc_fitting_scope_test --test_output=errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'fork.prusaslicer.arc-fitting' packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh` returns matches in both files.
    - `rg -n 'prusaslicer_arc_fitting_parity' packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh` returns matches in both files.
    - `rg -n 'generated-outputs' packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh` returns matches in both files.
    - `bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_arc_fitting_fixture_test //packages/prusa-arc-fitting-scope:verify_prusa_arc_fitting_scope_test --test_output=errors` exits 0.
  </acceptance_criteria>
  <done>Fixture and scope verifier mutation suites fail closed on status row absence, duplication, wrong target, broad generated-output promotion, and G-code row widening.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status.tsv -> public parity status | Checked-in public status text becomes maintainer-facing evidence. |
| status.tsv -> verifier scripts | Verifiers enforce exact publication state. |
| test temp status fixtures -> verifier diagnostics | Mutation tests prove false or widened status claims fail closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03-01 | Spoofing | `fork.prusaslicer.arc-fitting` row | mitigate | Add exactly one row with exact evidence target and exact narrow evidence-chain wording. |
| T-60-03-02 | Repudiation | `generated-outputs` row | mitigate | Verifiers require exactly one `generated-outputs` row with status `in progress`. |
| T-60-03-03 | Tampering | existing G-code output row | mitigate | Verifiers require the unchanged exact `fork.prusaslicer.gcode-output` row and mutation tests reject widened wording. |
| T-60-03-04 | Information Disclosure | status publication | mitigate | Status row adds no secrets, credentials, private data, network/device behavior, upstream source import, runtime discovery, host upload, or printer-runtime exposure. |
</threat_model>

<verification>
Run the task verification commands, then run:

```bash
bazel test //packages/parity:prusaslicer_arc_fitting_parity_failure_test --test_output=errors
bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test --test_output=errors
git diff --check -- packages/parity/status.tsv packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh
```
</verification>

<success_criteria>
ARCEV-03 status publication is satisfied when exactly one narrow `fork.prusaslicer.arc-fitting` row is verified, broad `generated-outputs` remains `in progress`, and both fixture and scope verifier mutation suites reject status drift.
</success_criteria>

<output>
After completion, create `.planning/phases/60-executable-arc-fitting-evidence/60-03-SUMMARY.md`.
</output>
