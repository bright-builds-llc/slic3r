---
phase: 49-structural-g-code-scope-contract
plan: "02"
type: tdd
wave: 2
depends_on:
  - "49-01"
files_modified:
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
autonomous: true
requirements:
  - GCSCOPE-01
  - GCSCOPE-02
  - GCSCOPE-03
user_setup: []
must_haves:
  truths:
    - "Maintainer can run the scope verifier and it enforces the exact structural field set."
    - "Unsupported structural fields fail closed instead of being ignored."
    - "Forbidden broad generated-output, runtime, fork, import, and sync claim text fails closed in README and scope text."
    - "Verifier preserves exact inventory, category-map, narrow status, parity target, and broad generated-outputs in-progress checks."
  artifacts:
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      provides: "Fail-closed structural scope verifier"
      contains: "verify_structural_scope_contract"
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh"
      provides: "Mutation suite for structural field and overclaim drift"
      contains: "test_unsupported_structural_field_fails"
  key_links:
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      to: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      via: "exact section rows and row count"
      pattern: "STRUCTURAL_SCOPE_SECTION"
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      to: "packages/parity/status.tsv"
      via: "generated-outputs and fork status checks"
      pattern: "generated-outputs"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 49-2026-06-16T14-43-39
generated_at: 2026-06-16T15:09:12.734Z
---

<objective>
Make the Phase 49 structural G-code scope contract executable through fail-closed verifier and mutation coverage.

Purpose: Complete GCSCOPE-02 and enforce the GCSCOPE-01/GCSCOPE-03 contract created in Plan 49-01 without adding Phase 50 fixture expansion, Phase 51 Rust parser work, or Phase 52 public structural evidence/status publication.
Output: Updated Bash verifier and mutation suite with RED then GREEN evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-structural-g-code-scope-contract/49-CONTEXT.md
@.planning/phases/49-structural-g-code-scope-contract/49-RESEARCH.md
@.planning/phases/49-structural-g-code-scope-contract/49-01-PLAN.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/prusa-gcode-output-scope/gcode-output-scope.md
@packages/prusa-gcode-output-scope/README.md
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
@packages/prusa-gcode-output-scope/BUILD.bazel
@packages/fork-inventories/prusaslicer.tsv
@packages/fork-inventories/category-map.tsv
@packages/parity/status.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv

<interfaces>
Key Bash helpers already present in `verify_prusa_gcode_output_scope.sh`:

```bash
require_text "${file}" "${label}" "${pattern}"
reject_text "${file}" "${label}" "${pattern}"
require_section_table_row "${file}" "${label}" "${section}" "${field}" "${value}"
require_exact_tsv_row_once "${file}" "${label}" "${expected_row}"
require_first_field_count "${file}" "${label}" "${id}"
require_category_reference_count "${file}" "${label}" "${id}"
verify_status_published
verify_parity_target_published
reject_overclaiming_text
verify_readme
verify_scope_record
verify_source_row_details
verify_deferred_scope_terms
verify_inventory_inputs
```

Key test helpers already present in `verify_prusa_gcode_output_scope_test.sh`:

```bash
assert_contains "${file}" "${pattern}"
remove_line_containing "${file}" "${pattern}"
replace_first_line_containing "${file}" "${pattern}" "${replacement}"
replace_text "${file}" "${pattern}" "${replacement}"
write_valid_fixture "${dir}"
run_verifier "${dir}" "${stdout_file}" "${stderr_file}"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED mutation coverage for structural scope drift</name>
  <files>packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</files>
  <read_first>
    packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
    packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
    packages/prusa-gcode-output-scope/gcode-output-scope.md
    packages/prusa-gcode-output-scope/README.md
    .planning/phases/49-structural-g-code-scope-contract/49-CONTEXT.md
    .planning/phases/49-structural-g-code-scope-contract/49-RESEARCH.md
    packages/parity/status.tsv
  </read_first>
  <behavior>
    - Missing required structural field row must fail and mention `source_ref`.
    - Extra unsupported structural field row must fail and mention `expected exactly 16 structural field rows`.
    - Missing structural traceability row for broad `generated-outputs` status must fail and mention `Broad status row`.
    - Missing structural reviewer signoff row must fail and mention `Structural reviewer signoff`.
    - Scope text claim `Phase 49 structural evidence proves toolpath geometry.` must fail as a forbidden Prusa G-code scope claim.
    - Scope text claim `Phase 49 structural evidence proves full generated-output parity.` must fail as a forbidden Prusa G-code scope claim.
    - README text claim `Phase 49 verifies printer-runtime behavior.` must fail as a forbidden Prusa G-code scope claim.
  </behavior>
  <action>
    Update only `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` in this RED task.

    First keep `write_valid_fixture` verifier-compatible with the current GREEN baseline by retaining the existing Phase 45 README ownership sentence and existing Phase 45 README warning sentences. Do not replace those README lines with Phase 49 wording in this RED task; Task 2 updates `verify_readme` and the temp README fixture together after the RED structural mutation coverage exists. This keeps `test_complete_fixture_passes` passing under the current verifier so RED failures prove missing structural enforcement rather than an old README sentence mismatch.

    Then update the generated temp `gcode-output-scope.md` in `write_valid_fixture` so it contains the exact `## v1.13 Structural Evidence Scope` section and exact `## v1.13 Structural Traceability` section from Plan 49-01 Task 1 while preserving the existing current-verifier-required `## Scope Record` and `## Source Row Details` sections.

    Add a helper named `insert_line_before` if needed. Its signature must be `insert_line_before() { local file="$1"; local pattern="$2"; local inserted_line="$3"; ... }`, and it must write through a `.tmp` file before `mv`.

    Add these six test functions, each with explicit `# Arrange`, `# Act`, and `# Assert` comments:

    `test_missing_allowed_structural_field_fails`: create dir `${tmp_dir}/missing-allowed-structural-field`, call `write_valid_fixture`, remove the line containing `| source_ref |`, run the verifier, fail if it exits 0, assert stderr contains `^error:` and `source_ref`.

    `test_unsupported_structural_field_fails`: create dir `${tmp_dir}/unsupported-structural-field`, call `write_valid_fixture`, insert the exact row `| geometry_count | unsupported generated-output semantics | Unsupported field that must fail closed. |` before `## v1.13 Structural Traceability`, run the verifier, fail if it exits 0, assert stderr contains `^error:` and `expected exactly 16 structural field rows`.

    `test_missing_structural_traceability_fails`: create dir `${tmp_dir}/missing-structural-traceability`, call `write_valid_fixture`, remove the line containing `| Broad status row |`, run the verifier, fail if it exits 0, assert stderr contains `^error:` and `Broad status row`.

    `test_missing_structural_reviewer_signoff_fails`: create dir `${tmp_dir}/missing-structural-reviewer-signoff`, call `write_valid_fixture`, remove the line containing `| Structural reviewer signoff |`, run the verifier, fail with `missing structural reviewer signoff fixture passed` if it exits 0, assert stderr contains `^error:` and `Structural reviewer signoff`.

    `test_scope_structural_overclaim_fails`: create dir `${tmp_dir}/scope-structural-overclaim`, call `write_valid_fixture`, append the exact line `Phase 49 structural evidence proves toolpath geometry.` to `${dir}/gcode-output-scope.md`, run the verifier, fail if it exits 0, assert stderr contains `^error:` and `forbidden Prusa G-code scope claim`.

    `test_scope_full_generated_output_parity_overclaim_fails`: create dir `${tmp_dir}/scope-full-generated-output-parity-overclaim`, call `write_valid_fixture`, append the exact line `Phase 49 structural evidence proves full generated-output parity.` to `${dir}/gcode-output-scope.md`, run the verifier, fail with `scope full generated-output parity overclaim fixture passed` if it exits 0, assert stderr contains `^error:` and `forbidden Prusa G-code scope claim`.

    `test_readme_structural_overclaim_fails`: create dir `${tmp_dir}/readme-structural-overclaim`, call `write_valid_fixture`, append the exact line `Phase 49 verifies printer-runtime behavior.` to `${dir}/README.md`, run the verifier, fail if it exits 0, assert stderr contains `^error:` and `forbidden Prusa G-code scope claim`.

    Add these test function calls after `test_phase47_rust_summary_boundary_is_allowed`:
    `test_missing_allowed_structural_field_fails`
    `test_unsupported_structural_field_fails`
    `test_missing_structural_traceability_fails`
    `test_missing_structural_reviewer_signoff_fails`
    `test_scope_structural_overclaim_fails`
    `test_scope_full_generated_output_parity_overclaim_fails`
    `test_readme_structural_overclaim_fails`

    Run the test before changing the verifier. It must fail because the current verifier does not enforce the new structural section yet. Record the RED failure in the plan summary.
  </action>
  <verify>
    <automated>rg -n 'test_missing_allowed_structural_field_fails|test_unsupported_structural_field_fails|test_missing_structural_traceability_fails|test_missing_structural_reviewer_signoff_fails|test_scope_structural_overclaim_fails|test_scope_full_generated_output_parity_overclaim_fails|test_readme_structural_overclaim_fails|Phase 49 structural evidence proves full generated-output parity' packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>set +e; bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh >/tmp/phase49-structural-red.out 2>/tmp/phase49-structural-red.err; status="$?"; set -e; test "${status}" -ne 0; rg -n 'unsupported structural field fixture passed|missing allowed structural field fixture passed|missing structural traceability fixture passed|missing structural reviewer signoff fixture passed|scope structural overclaim fixture passed|scope full generated-output parity overclaim fixture passed|README structural overclaim fixture passed' /tmp/phase49-structural-red.err</automated>
  </verify>
  <acceptance_criteria>
    - `verify_prusa_gcode_output_scope_test.sh` contains `test_unsupported_structural_field_fails`.
    - `verify_prusa_gcode_output_scope_test.sh` contains `test_missing_structural_reviewer_signoff_fails`.
    - `verify_prusa_gcode_output_scope_test.sh` contains `test_scope_full_generated_output_parity_overclaim_fails`.
    - `verify_prusa_gcode_output_scope_test.sh` contains `| geometry_count | unsupported generated-output semantics | Unsupported field that must fail closed. |`.
    - `verify_prusa_gcode_output_scope_test.sh` contains `Phase 49 structural evidence proves full generated-output parity.`.
    - The RED temp `README.md` fixture still contains the current verifier-required Phase 45 ownership sentence until Task 2 updates `verify_readme`.
    - The RED verification command exits 0 only when the mutation suite itself exits nonzero before verifier implementation.
    - `/tmp/phase49-structural-red.err` contains one of the exact failure markers listed in the RED verify command.
  </acceptance_criteria>
  <done>Mutation coverage exists and proves the current verifier is insufficient before GREEN implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GREEN structural verifier enforcement</name>
  <files>packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh, packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</files>
  <read_first>
    packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
    packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
    packages/prusa-gcode-output-scope/gcode-output-scope.md
    packages/prusa-gcode-output-scope/README.md
    packages/prusa-gcode-output-scope/BUILD.bazel
    .planning/phases/49-structural-g-code-scope-contract/49-CONTEXT.md
    .planning/phases/49-structural-g-code-scope-contract/49-RESEARCH.md
    packages/fork-inventories/prusaslicer.tsv
    packages/fork-inventories/category-map.tsv
    packages/parity/status.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv
  </read_first>
  <behavior>
    - Valid repository files pass `bazel run //packages/prusa-gcode-output-scope:verify`.
    - Valid temp fixture from the mutation suite passes.
    - Missing or extra structural field rows fail closed.
    - Missing structural traceability rows fail closed.
    - Forbidden broad behavior, non-Prusa fork, and full/broad generated-output claim text fails closed in README and scope files.
    - `generated-outputs` must remain exactly one `in progress` status row.
  </behavior>
  <action>
    Update `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh` to enforce the Plan 49-01 contract.

    Add these constants near the existing readonly constants:

    `readonly STRUCTURAL_SCOPE_SECTION="## v1.13 Structural Evidence Scope"`
    `readonly STRUCTURAL_TRACEABILITY_SECTION="## v1.13 Structural Traceability"`
    `readonly STRUCTURAL_FIELD_ROW_COUNT="16"`

    Replace `reject_text`'s error message string `forbidden Phase 45 claim` with `forbidden Prusa G-code scope claim`, and update existing overclaim test assertions in `verify_prusa_gcode_output_scope_test.sh` to expect `forbidden Prusa G-code scope claim`.

    Add a helper named `require_section_table_exact_row` with signature `require_section_table_exact_row "${file}" "${label}" "${section}" "${row}"`. It must search only inside the named section, stop at the next `## ` heading, and call `error "${label}: missing required table row in ${section}: ${row}"` when absent.

    Add a helper named `require_section_table_body_row_count` with signature `require_section_table_body_row_count "${file}" "${label}" "${section}" "${expected_count}"`. It must count body rows inside the named section where the first column starts with lowercase letters, digits, or underscores: `^\| [a-z0-9_]+ \|`. It must stop at the next `## ` heading. It must call `error "${label}: ${section}: expected exactly ${expected_count} structural field rows, found ${count}"` when the count differs.

    Add `verify_structural_scope_contract`. It must first call `require_section_table_body_row_count "${scope_file}" "gcode-output-scope.md" "${STRUCTURAL_SCOPE_SECTION}" "${STRUCTURAL_FIELD_ROW_COUNT}"`, then call `require_section_table_exact_row` once for each exact row from Plan 49-01 Task 1's structural evidence table. Use the same row strings, including punctuation and backticks.

    Add `verify_structural_traceability`. It must call `require_section_table_row` for the exact rows under `${STRUCTURAL_TRACEABILITY_SECTION}`:
    - field `Inventory row`, value `` `prusaslicer.gcode-output` in `packages/fork-inventories/prusaslicer.tsv` ``
    - field `Category-map row`, value `` `gcode.shared` in `packages/fork-inventories/category-map.tsv` references `prusaslicer.gcode-output` exactly once ``
    - field `Accepted source identity`, value `` `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961` ``
    - field `Fixture namespace`, value `` `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/` ``
    - field `Current expected summary`, value `` `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv` ``
    - field `Fixture provenance`, value `` `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv` ``
    - field `Published narrow status row`, value `` `fork.prusaslicer.gcode-output` stays verified only for the narrow summary-only Prusa G-code evidence slice in `packages/parity/status.tsv` ``
    - field `Broad status row`, value `` `generated-outputs` stays `in progress` in `packages/parity/status.tsv` ``
    - field `Structural reviewer signoff`, value `Peter Ryszkiewicz, 2026-06-16 UTC`

    Add `verify_generated_outputs_in_progress`. It must use `awk -F '\t'` over `${status_file}` and require exactly one row where `$1 == "generated-outputs"` and `$2 == "in progress"`. It must call `error "packages/parity/status.tsv: generated-outputs must remain exactly one in progress row, found ${count}"` when the count differs.

    Update `verify_readme` to require the exact Phase 49 README sentences added in Plan 49-01 Task 2.

    Expand `reject_overclaiming_text` with these exact forbidden strings in addition to the existing Phase 45 strings:
    `Phase 49 structural evidence proves byte-for-byte G-code parity`
    `Phase 49 structural evidence proves full generated-output parity`
    `Phase 49 structural evidence proves broad generated-output parity`
    `Phase 49 structural evidence proves toolpath geometry`
    `Phase 49 structural evidence proves printability`
    `Phase 49 structural evidence proves printer-runtime behavior`
    `Phase 49 structural evidence proves support generation`
    `Phase 49 structural evidence proves wall seam behavior`
    `Phase 49 structural evidence proves arc fitting`
    `Phase 49 structural evidence proves GUI export/viewer behavior`
    `Phase 49 structural evidence proves release behavior`
    `Phase 49 structural evidence proves network/device behavior`
    `Phase 49 structural evidence proves non-Prusa fork behavior`
    `Phase 49 structural evidence proves Bambu Studio support`
    `Phase 49 structural evidence proves OrcaSlicer support`
    `Phase 49 structural evidence proves upstream source imports`
    `Phase 49 structural evidence proves sync automation`
    `Phase 49 verifies printer-runtime behavior`

    Update `write_valid_fixture` in `verify_prusa_gcode_output_scope_test.sh` with the exact Phase 49 README ownership and structural verification sentences at the same time as `verify_readme`, preserving `test_complete_fixture_passes` as the GREEN baseline after the README transition.

    Call `verify_structural_scope_contract`, `verify_structural_traceability`, and `verify_generated_outputs_in_progress` after `verify_source_row_details` and before `verify_deferred_scope_terms`.

    Run the full mutation suite and package verifier until GREEN. This task is the first full package-verifier checkpoint after Plan 49-01 changes README and scope contract wording, because this task updates verifier expectations to match that transition.
  </action>
  <verify>
    <automated>rg -n 'test_scope_full_generated_output_parity_overclaim_fails|test_missing_structural_reviewer_signoff_fails|Structural reviewer signoff|Peter Ryszkiewicz, 2026-06-16 UTC|Phase 49 structural evidence proves full generated-output parity|Phase 49 structural evidence proves broad generated-output parity|Phase 49 structural evidence proves non-Prusa fork behavior' packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bash -n packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
    <automated>bazel test --cache_test_results=no //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
    <automated>shfmt -d packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>shellcheck packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv</automated>
    <automated>git diff --check -- packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `verify_prusa_gcode_output_scope.sh` contains `readonly STRUCTURAL_FIELD_ROW_COUNT="16"`.
    - `verify_prusa_gcode_output_scope.sh` contains `verify_structural_scope_contract`.
    - `verify_prusa_gcode_output_scope.sh` contains `Structural reviewer signoff` and `Peter Ryszkiewicz, 2026-06-16 UTC` inside `verify_structural_traceability`.
    - `verify_prusa_gcode_output_scope.sh` contains `verify_generated_outputs_in_progress`.
    - `verify_prusa_gcode_output_scope_test.sh` contains `test_unsupported_structural_field_fails`.
    - `verify_prusa_gcode_output_scope_test.sh` contains `test_missing_structural_reviewer_signoff_fails`.
    - `verify_prusa_gcode_output_scope_test.sh` contains `test_scope_full_generated_output_parity_overclaim_fails`.
    - `verify_prusa_gcode_output_scope.sh` contains `Phase 49 structural evidence proves full generated-output parity`, `Phase 49 structural evidence proves broad generated-output parity`, and `Phase 49 structural evidence proves non-Prusa fork behavior`.
    - `bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` prints `ok: verify_prusa_gcode_output_scope_test`.
    - After the README/verifier transition, `bazel run //packages/prusa-gcode-output-scope:verify` prints `ok: Prusa G-code output scope verification passed`.
    - `shellcheck packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` exits 0.
  </acceptance_criteria>
  <done>The verifier enforces exact structural field rows, rejects unsupported structural fields and broad claim text, and preserves traceability/status guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository metadata to verifier | Markdown and TSV files are untrusted inputs to the Bash verifier. |
| Temp mutation fixture to verifier | Mutation tests construct isolated checkout roots that must not hide unsupported fields or overclaims. |
| Verifier result to maintainer | Maintainers rely on verifier pass/fail output as evidence that scope did not drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05 | Tampering | `gcode-output-scope.md` structural field table | mitigate | Enforce exact required rows plus exact body row count of 16 through `verify_structural_scope_contract`; mutation test `test_unsupported_structural_field_fails` proves extra rows fail. |
| T-49-06 | Spoofing | README and scope overclaim text | mitigate | Expand `reject_overclaiming_text` with Phase 49 structural overclaim strings and test both README and scope insertion paths. |
| T-49-07 | Repudiation | Traceability rows and status interpretation | mitigate | Require exact structural traceability rows plus exact `generated-outputs` in-progress row count and existing exact narrow `fork.prusaslicer.gcode-output` publication checks. |
| T-49-08 | Denial of Service | Bash verifier behavior | mitigate | Keep verifier local, deterministic, and file-based with `set -euo pipefail`; run `bash -n`, `shfmt -d`, `shellcheck`, local Bash tests, and Bazel sh_test. |
| T-49-09 | Elevation of Privilege | Verifier execution surface | accept | The verifier adds no network, credential, process-spawning beyond local shell/Bazel execution already present, or external service access; no ASVS L1 auth/session/access-control surface is introduced. |
</threat_model>

<verification>
Run these plan-level checks after both tasks:

```bash
rg -n 'test_scope_full_generated_output_parity_overclaim_fails|test_missing_structural_reviewer_signoff_fails|Structural reviewer signoff|Peter Ryszkiewicz, 2026-06-16 UTC|Phase 49 structural evidence proves full generated-output parity|Phase 49 structural evidence proves broad generated-output parity|Phase 49 structural evidence proves non-Prusa fork behavior' packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
bash -n packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
bash packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
bazel run //packages/prusa-gcode-output-scope:verify
bazel test --cache_test_results=no //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test
shfmt -d packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
shellcheck packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv
git diff --check -- packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
```
</verification>

<success_criteria>
- Structural scope verifier fails when an allowed field row is missing.
- Structural scope verifier fails when an unsupported structural field row is inserted.
- Structural scope verifier fails when broad behavior, full/broad generated-output parity, or non-Prusa fork behavior claim text is inserted in README or scope text.
- Structural scope verifier requires exact inventory/category/status traceability and exact `generated-outputs` in-progress status.
- Full Bash, Bazel, shfmt, shellcheck, status, and whitespace checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/49-structural-g-code-scope-contract/49-02-SUMMARY.md`
</output>
