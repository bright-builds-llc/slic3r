---
phase: 49-structural-g-code-scope-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/prusa-gcode-output-scope/gcode-output-scope.md
  - packages/prusa-gcode-output-scope/README.md
autonomous: true
requirements:
  - GCSCOPE-01
  - GCSCOPE-03
user_setup: []
must_haves:
  truths:
    - "Maintainer can inspect the allowed structural G-code evidence fields before fixture, Rust parser, or executable evidence work expands."
    - "Maintainer can trace the structural scope to the accepted prusaslicer.gcode-output inventory row, fixture namespace, expected summary, provenance, and narrow status row."
    - "Package README states the structural expansion is narrow and keeps broad generated-outputs in progress."
  artifacts:
    - path: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      provides: "Human-readable structural G-code scope contract"
      contains: "## v1.13 Structural Evidence Scope"
    - path: "packages/prusa-gcode-output-scope/README.md"
      provides: "Maintainer entrypoint and verification command for the expanded scope package"
      contains: "Phase 49 structural verification allows only command counts"
  key_links:
    - from: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      to: "packages/fork-inventories/prusaslicer.tsv"
      via: "Structural traceability table"
      pattern: "prusaslicer\\.gcode-output"
    - from: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      to: "packages/parity/status.tsv"
      via: "Structural traceability table"
      pattern: "generated-outputs.*in progress"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 49-2026-06-16T14-43-39
generated_at: 2026-06-16T15:09:12.734Z
---

<objective>
Create the reviewed structural Prusa G-code scope contract in the existing `packages/prusa-gcode-output-scope` package.

Purpose: Complete the inspectable contract surface for GCSCOPE-01 and the traceability surface for GCSCOPE-03 before verifier enforcement expands in Plan 49-02.
Output: Updated human-readable scope record and minimal package README wording.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-structural-g-code-scope-contract/49-CONTEXT.md
@.planning/phases/49-structural-g-code-scope-contract/49-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/prusa-gcode-output-scope/gcode-output-scope.md
@packages/prusa-gcode-output-scope/README.md
@packages/fork-inventories/prusaslicer.tsv
@packages/fork-inventories/category-map.tsv
@packages/parity/status.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv

Relevant Bright Builds pages read during planning:
- `standards/index.md`
- `standards/core/architecture.md`
- `standards/core/code-shape.md`
- `standards/core/verification.md`
- `standards/core/testing.md`

No project-local `.claude/skills/` or `.agents/skills/` directory exists in this checkout.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the closed structural evidence contract</name>
  <files>packages/prusa-gcode-output-scope/gcode-output-scope.md</files>
  <read_first>
    packages/prusa-gcode-output-scope/gcode-output-scope.md
    .planning/phases/49-structural-g-code-scope-contract/49-CONTEXT.md
    .planning/phases/49-structural-g-code-scope-contract/49-RESEARCH.md
    .planning/REQUIREMENTS.md
    packages/fork-inventories/prusaslicer.tsv
    packages/fork-inventories/category-map.tsv
    packages/parity/status.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv
  </read_first>
  <action>
    Extend the existing `packages/prusa-gcode-output-scope/gcode-output-scope.md` file per D-01 and D-02. Keep the existing Phase 45 scope record and `## Source Row Details` table intact. Insert two new sections between `## Source Row Details` and `## Boundary`.

    First add this section header and introductory paragraph:

    `## v1.13 Structural Evidence Scope`

    `This section is an additive structural contract for the existing narrow `prusaslicer.gcode-output` evidence chain. It allows only the fields listed below for Phase 50 structural fixture expectations and Phase 51 typed parsing. It does not promote broad `generated-outputs` status and does not prove byte-for-byte G-code parity, toolpath geometry, printability, printer-runtime behavior, support generation, wall seam behavior, arc fitting, GUI export/viewer behavior, release behavior, network/device behavior, non-Prusa fork behavior, upstream source imports, or sync automation.`

    Then add this exact Markdown table with these exact sixteen body rows, in this order, per D-04, D-05, and D-06:

    `| Structural Field | Category | Evidence Boundary |`
    `| --- | --- | --- |`
    `| source_ref | source identity | Accepted PrusaSlicer source identity only: `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`. |`
    `| inventory_source_paths | source identity | Inventory source paths only: `src/libslic3r/GCode.cpp;src/libslic3r/GCode.hpp`. |`
    `| fixture_source_literal | source identity | Source literal only: `tests/fff_print/test_gcodewriter.cpp#L20-L35`. |`
    `| fixture_id | fixture identity | Fixture identity only: `gcodewriter-set-speed.gcode`. |`
    `| fixture_path | fixture identity | Checked-in fixture path only: `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode`. |`
    `| command_count_total | command counts | Count of G-code command rows in the selected fixture only; no generated-output behavior claimed. |`
    `| command_count_g1 | command counts | Count of `G1` command rows in the selected fixture only; no toolpath geometry claimed. |`
    `| section_count_total | section counts | Count of structural sections in the selected summary only; no GUI, print, or runtime section behavior claimed. |`
    `| ordered_marker_1 | ordered markers | First ordered marker value from the selected fixture summary only. |`
    `| ordered_marker_2 | ordered markers | Second ordered marker value from the selected fixture summary only. |`
    `| ordered_marker_3 | ordered markers | Third ordered marker value from the selected fixture summary only. |`
    `| ordered_marker_4 | ordered markers | Fourth ordered marker value from the selected fixture summary only. |`
    `| movement_axis_present | movement/extrusion indicators | Boolean structural indicator for movement-axis text presence only; no toolpath geometry, travel, or printability claim. |`
    `| extrusion_axis_present | movement/extrusion indicators | Boolean structural indicator for extrusion-axis text presence only; no extrusion amount, material, or printability claim. |`
    `| temperature_marker_count | temperature/tool-change markers | Count of temperature marker commands in the selected fixture only; no printer-runtime behavior claimed. |`
    `| tool_change_marker_count | temperature/tool-change markers | Count of tool-change marker commands in the selected fixture only; no multi-tool runtime behavior claimed. |`

    Second add this section:

    `## v1.13 Structural Traceability`

    Add this exact Markdown table under it:

    `| Required Link | Exact Target |`
    `| --- | --- |`
    `| Inventory row | `prusaslicer.gcode-output` in `packages/fork-inventories/prusaslicer.tsv` |`
    `| Category-map row | `gcode.shared` in `packages/fork-inventories/category-map.tsv` references `prusaslicer.gcode-output` exactly once |`
    `| Accepted source identity | `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961` |`
    `| Fixture namespace | `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/` |`
    `| Current expected summary | `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv` |`
    `| Fixture provenance | `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv` |`
    `| Published narrow status row | `fork.prusaslicer.gcode-output` stays verified only for the narrow summary-only Prusa G-code evidence slice in `packages/parity/status.tsv` |`
    `| Broad status row | `generated-outputs` stays `in progress` in `packages/parity/status.tsv` |`

    Update the `## Boundary` paragraph so it states this exact boundary: `This record is a metadata-only scope gate plus structural evidence contract. It must not be treated as fixture expansion, Rust structural parser readiness, executable structural evidence, byte-for-byte G-code parity, broad generated-output parity, or generated-output status promotion.`
  </action>
  <verify>
    <automated>rg -n '^## v1\.13 Structural Evidence Scope$|^## v1\.13 Structural Traceability$' packages/prusa-gcode-output-scope/gcode-output-scope.md</automated>
    <automated>awk 'BEGIN { count=0; in_section=0 } $0=="## v1.13 Structural Evidence Scope" { in_section=1; next } in_section && /^## / { in_section=0 } in_section && /^\| [a-z0-9_]+ \|/ { count++ } END { exit count == 16 ? 0 : 1 }' packages/prusa-gcode-output-scope/gcode-output-scope.md</automated>
    <automated>rg -n '\| source_ref \| source identity \| Accepted PrusaSlicer source identity only: `prusaslicer:version_2\.9\.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`\. \|' packages/prusa-gcode-output-scope/gcode-output-scope.md</automated>
    <automated>rg -n '\| Broad status row \| `generated-outputs` stays `in progress` in `packages/parity/status.tsv` \|' packages/prusa-gcode-output-scope/gcode-output-scope.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/prusa-gcode-output-scope/gcode-output-scope.md` contains exactly one `## v1.13 Structural Evidence Scope` header.
    - The structural evidence table has exactly 16 body rows whose first column values are: `source_ref`, `inventory_source_paths`, `fixture_source_literal`, `fixture_id`, `fixture_path`, `command_count_total`, `command_count_g1`, `section_count_total`, `ordered_marker_1`, `ordered_marker_2`, `ordered_marker_3`, `ordered_marker_4`, `movement_axis_present`, `extrusion_axis_present`, `temperature_marker_count`, `tool_change_marker_count`.
    - `packages/prusa-gcode-output-scope/gcode-output-scope.md` contains the exact traceability row `| Broad status row | `generated-outputs` stays `in progress` in `packages/parity/status.tsv` |`.
  </acceptance_criteria>
  <done>The scope contract is inspectable, has a closed structural field table, and names the exact inventory, category-map, fixture, expected-summary, provenance, narrow status, and broad generated-output status links.</done>
</task>

<task type="auto">
  <name>Task 2: Update the package README without broad rewrites</name>
  <files>packages/prusa-gcode-output-scope/README.md</files>
  <read_first>
    packages/prusa-gcode-output-scope/README.md
    packages/prusa-gcode-output-scope/gcode-output-scope.md
    .planning/phases/49-structural-g-code-scope-contract/49-CONTEXT.md
    .planning/REQUIREMENTS.md
    packages/parity/status.tsv
  </read_first>
  <action>
    Update `packages/prusa-gcode-output-scope/README.md` minimally per D-03 and D-12. Do not rewrite unrelated sections.

    Replace the current ownership sentence with: ``packages/prusa-gcode-output-scope` owns the Phase 45 reviewed scope gate and the Phase 49 structural evidence scope contract for `prusaslicer.gcode-output`.`

    Keep the verification command exactly as `bazel run //packages/prusa-gcode-output-scope:verify`.

    Add these two exact sentences immediately after the verification command sentence:

    `Phase 49 structural verification allows only command counts, section counts, ordered markers, movement/extrusion indicators, temperature/tool-change markers, source identity, and fixture identity for the narrow Prusa G-code evidence chain.`

    `Phase 49 structural verification keeps broad generated-outputs in progress and does not prove byte-for-byte G-code parity, toolpath geometry, printability, printer-runtime behavior, support generation, wall seam behavior, arc fitting, GUI export/viewer behavior, release behavior, network/device behavior, Bambu Studio support, OrcaSlicer support, upstream source imports, or sync automation.`

    Update the single record bullet to: `- [`gcode-output-scope.md`](gcode-output-scope.md) records the Phase 45 maintainer scope gate and Phase 49 structural evidence field contract for `prusaslicer.gcode-output`.`

    Update the `## Boundary` paragraph to: `This package is a metadata-only scope gate and structural evidence contract. It records the reviewed source identity, existing fixture source, expected-summary path, Rust boundary path, evidence command, status token, closed structural field list, and deferred scope before later phases consume the contract.`
  </action>
  <verify>
    <automated>rg -n 'Phase 49 structural verification allows only command counts, section counts, ordered markers, movement/extrusion indicators, temperature/tool-change markers, source identity, and fixture identity' packages/prusa-gcode-output-scope/README.md</automated>
    <automated>rg -n 'Phase 49 structural verification keeps broad generated-outputs in progress and does not prove byte-for-byte G-code parity' packages/prusa-gcode-output-scope/README.md</automated>
    <automated>rg -n 'bazel run //packages/prusa-gcode-output-scope:verify' packages/prusa-gcode-output-scope/README.md</automated>
    <automated>git diff --check -- packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `packages/prusa-gcode-output-scope/README.md` contains the exact sentence beginning `Phase 49 structural verification allows only command counts`.
    - `packages/prusa-gcode-output-scope/README.md` contains `broad generated-outputs in progress`.
    - `packages/prusa-gcode-output-scope/README.md` still contains `bazel run //packages/prusa-gcode-output-scope:verify`.
    - `git diff --check -- packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md` exits 0.
  </acceptance_criteria>
  <done>The package README names the structural scope expansion, keeps the verifier command discoverable, and preserves the narrow no-overclaim boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer to scope contract | Maintainers rely on Markdown text as evidence scope authority before downstream fixture and parser work begins. |
| Scope contract to later phases | Phase 50 and Phase 51 will consume the field names and traceability text created here. |
| Scope contract to status interpretation | The contract references `packages/parity/status.tsv` and must not imply broad generated-output publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Spoofing | `packages/prusa-gcode-output-scope/README.md` | mitigate | Require exact README wording that says Phase 49 structural verification is narrow and does not prove byte-for-byte, geometry, printability, runtime, release, network/device, non-Prusa fork, upstream import, or sync behavior. |
| T-49-02 | Tampering | `packages/prusa-gcode-output-scope/gcode-output-scope.md` | mitigate | Create an explicit closed structural field table with the 16 allowed field names; Plan 49-02 enforces the exact rows and row count. |
| T-49-03 | Repudiation | `packages/prusa-gcode-output-scope/gcode-output-scope.md` | mitigate | Add exact traceability rows for `prusaslicer.gcode-output`, `gcode.shared`, fixture namespace, expected summary, provenance, `fork.prusaslicer.gcode-output`, and broad `generated-outputs` in-progress status. |
| T-49-04 | Information Disclosure | Scope and README text | accept | No secrets, credentials, private source, network data, or user data are introduced; all references are existing checked-in metadata paths. |
</threat_model>

<verification>
Run these plan-level checks after both tasks:

```bash
rg -n '^## v1\.13 Structural Evidence Scope$|^## v1\.13 Structural Traceability$' packages/prusa-gcode-output-scope/gcode-output-scope.md
awk 'BEGIN { count=0; in_section=0 } $0=="## v1.13 Structural Evidence Scope" { in_section=1; next } in_section && /^## / { in_section=0 } in_section && /^\| [a-z0-9_]+ \|/ { count++ } END { exit count == 16 ? 0 : 1 }' packages/prusa-gcode-output-scope/gcode-output-scope.md
rg -n 'Phase 49 structural verification allows only command counts' packages/prusa-gcode-output-scope/README.md
git diff --check -- packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md
```

The full package verifier is intentionally deferred to Plan 49-02 after the verifier is updated to require the Phase 49 README and scope contract wording.
</verification>

<success_criteria>
- `gcode-output-scope.md` contains the closed structural evidence table with exactly 16 allowed fields.
- `gcode-output-scope.md` contains exact traceability rows for inventory, category-map, fixture, expected summary, provenance, narrow status, and broad generated-output status.
- `README.md` names the Phase 49 structural verification command boundary without claiming broad generated-output parity.
- No Phase 50 structural fixture artifact, Phase 51 Rust structural parser, or Phase 52 public structural evidence/status work is created by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/49-structural-g-code-scope-contract/49-01-SUMMARY.md`
</output>
