---
phase: 56-executable-semantic-g-code-evidence
plan: "02"
type: execute
wave: 2
depends_on:
  - "56-01"
files_modified:
  - packages/parity/compare_prusaslicer_gcode_output_test.sh
autonomous: true
requirements:
  - GSEV-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 56-2026-06-21T16-41-17
generated_at: 2026-06-21T17:08:02.864Z
must_haves:
  truths:
    - Maintainer can see the public parity evidence fail closed on semantic movement class drift.
    - Maintainer can see the public parity evidence fail closed on coordinate-bound, extrusion-total, feedrate, fixture identity, and unsupported deferred-behavior drift.
    - Existing marker `line_4` and structural `command_count_g1` mutation guards still run.
  artifacts:
    - path: packages/parity/compare_prusaslicer_gcode_output_test.sh
      provides: semantic mutation guards for public Prusa G-code evidence
  key_links:
    - from: packages/parity/compare_prusaslicer_gcode_output_test.sh
      to: packages/parity/compare_prusaslicer_gcode_output.sh
      via: `run_comparator`
      pattern: run_comparator
    - from: packages/parity/compare_prusaslicer_gcode_output_test.sh
      to: expected-gcode-semantic-summary.tsv
      via: temp-copy semantic mutations
      pattern: expected_gcode_semantic_summary
---

<objective>
Add focused public semantic mutation guards for the exact drift classes required by GSEV-02.

Purpose: Prove the public parity command fails closed when semantic evidence drifts, while keeping marker and structural guards intact.
Output: Extended `compare_prusaslicer_gcode_output_test.sh` with one semantic mutation concern per case.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
@.planning/phases/56-executable-semantic-g-code-evidence/56-01-SUMMARY.md
@packages/parity/compare_prusaslicer_gcode_output_test.sh
@packages/parity/compare_prusaslicer_gcode_output.sh
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add semantic value drift mutation guards</name>
  <files>packages/parity/compare_prusaslicer_gcode_output_test.sh</files>
  <read_first>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-01-SUMMARY.md
    packages/parity/compare_prusaslicer_gcode_output_test.sh
    packages/parity/compare_prusaslicer_gcode_output.sh
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
  </read_first>
  <behavior>
    - Movement class mutation changes only a temp copy of `expected-gcode-semantic-summary.tsv` and fails with stderr containing `expected-gcode-semantic-summary.tsv` and `movement_class_counts`.
    - Coordinate-bound mutation changes only a temp copy and fails with stderr containing `expected-gcode-semantic-summary.tsv` and `coordinate_bounds`.
    - Extrusion-total mutation changes only a temp copy and fails with stderr containing `expected-gcode-semantic-summary.tsv` and `extrusion_total`.
    - Feedrate-observation mutation changes only a temp copy and fails with stderr containing `expected-gcode-semantic-summary.tsv` and `feedrate_observations`.
  </behavior>
  <action>
    Add `expected_gcode_semantic_summary="${fixture_dir}/expected-gcode-semantic-summary.tsv"` beside the marker and structural variables. Update `assert_file` calls to require it. Update `run_comparator` to accept `expected_artifact`, `expected_structural_artifact`, `expected_semantic_artifact`, `stdout_file`, and `stderr_file`, then pass the semantic expected path as both the Rust semantic input and expected semantic artifact to the eight-argument comparator created in Plan 56-01. Update the existing marker and structural mutation calls so they pass the unmodified semantic artifact.

    Add `mutate_semantic_value()` using `awk` with `FS = OFS = "\t"` and column 3 as the semantic field selector, replacing column 5 exactly once. Add four focused temp-copy cases using copied semantic artifacts and these replacements: `movement_class_counts` -> `travel:1;extrusion:0;coordinate_motion:0;feedrate_only:3`; `coordinate_bounds` -> `x:0..10;y:none;z:none`; `extrusion_total` -> `e_axis_observed:true;extrusion_total:1.0`; `feedrate_observations` -> `F99999.123;F1;F203.2;F203.200`. Each case must fail if the comparator exits 0 and must assert stderr contains `expected-gcode-semantic-summary.tsv` plus the mutated semantic field. Preserve the existing `line_4` marker guard and `command_count_g1` structural guard unchanged except for passing the new semantic argument.
  </action>
  <acceptance_criteria>
    - `rg -n "expected_gcode_semantic_summary|mutate_semantic_value" packages/parity/compare_prusaslicer_gcode_output_test.sh` finds the semantic fixture variable and helper.
    - `rg -n "movement_class_counts|coordinate_bounds|extrusion_total|feedrate_observations" packages/parity/compare_prusaslicer_gcode_output_test.sh` finds the four semantic value mutation cases.
    - `rg -n "mutate_line_4_marker_value|mutate_command_count_g1_value" packages/parity/compare_prusaslicer_gcode_output_test.sh` still finds the existing marker and structural mutation helpers.
    - `bazel test --cache_test_results=no //packages/parity:prusaslicer_gcode_output_parity_failure_test --test_output=errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n packages/parity/compare_prusaslicer_gcode_output_test.sh && shfmt -l -d packages/parity/compare_prusaslicer_gcode_output_test.sh && bazel test --cache_test_results=no //packages/parity:prusaslicer_gcode_output_parity_failure_test --test_output=errors</automated>
  </verify>
  <done>Public failure coverage rejects semantic value drift for movement classes, coordinate bounds, extrusion totals, and feedrate observations with field-specific diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add semantic identity and deferred-claim mutation guards</name>
  <files>packages/parity/compare_prusaslicer_gcode_output_test.sh</files>
  <read_first>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-CONTEXT.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-RESEARCH.md
    .planning/phases/56-executable-semantic-g-code-evidence/56-01-SUMMARY.md
    packages/parity/compare_prusaslicer_gcode_output_test.sh
    packages/parity/compare_prusaslicer_gcode_output.sh
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv
  </read_first>
  <behavior>
    - Fixture identity mutation changes only a temp semantic artifact and fails with stderr containing `expected-gcode-semantic-summary.tsv` and `fixture_id`.
    - Unsupported deferred-behavior claim mutation changes only a temp semantic artifact and fails with stderr containing `expected-gcode-semantic-summary.tsv`, `movement_class_counts`, and `full generated-output parity`.
    - The checked-in semantic fixture remains unchanged after the tests.
  </behavior>
  <action>
    Add `mutate_semantic_boundary()` using `awk` with column 3 as the semantic field selector and column 6 as the evidence-boundary replacement, changing exactly one row. Add a fixture identity case that copies `expected-gcode-semantic-summary.tsv`, changes `fixture_id` column 5 to `unreviewed-speed.gcode`, runs the comparator, and asserts stderr contains `expected-gcode-semantic-summary.tsv` and `fixture_id`.

    Add an unsupported deferred-behavior claim case that copies `expected-gcode-semantic-summary.tsv`, changes the `movement_class_counts` evidence boundary to `full generated-output parity verified`, runs the comparator, and asserts stderr contains `expected-gcode-semantic-summary.tsv`, `movement_class_counts`, and `full generated-output parity`. Add a final source-artifact guard that `grep -Fq` still finds `movement_class_counts` with `travel:0;extrusion:0;coordinate_motion:0;feedrate_only:4` and `fixture_id` with `gcodewriter-set-speed.gcode` in the checked-in semantic fixture.
  </action>
  <acceptance_criteria>
    - `rg -n "mutate_semantic_boundary|fixture_id|unreviewed-speed.gcode|full generated-output parity verified" packages/parity/compare_prusaslicer_gcode_output_test.sh` finds the new helper and cases.
    - `rg -n "expected-gcode-semantic-summary.tsv" packages/parity/compare_prusaslicer_gcode_output_test.sh` finds semantic diagnostic assertions in the mutation cases.
    - `grep -Fq $'movement_class_counts\tmovement classes\ttravel:0;extrusion:0;coordinate_motion:0;feedrate_only:4' packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv` exits 0.
    - `grep -Fq $'fixture_id\tfixture identity\tgcodewriter-set-speed.gcode' packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-semantic-summary.tsv` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n packages/parity/compare_prusaslicer_gcode_output_test.sh && shfmt -l -d packages/parity/compare_prusaslicer_gcode_output_test.sh && bazel test --cache_test_results=no //packages/parity:prusaslicer_gcode_output_parity_failure_test --test_output=errors</automated>
  </verify>
  <done>Public failure coverage rejects semantic fixture identity drift and unsupported deferred-behavior claims without mutating checked-in fixture artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness to temp artifact | Mutation tests copy checked-in artifacts into a temp directory before editing. |
| Temp artifact to public comparator | The public comparator receives mutated semantic artifacts as explicit local paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-02-01 | Tampering | semantic mutation fixtures | mitigate | Mutate temp copies only, one semantic concern per test, and assert nonzero comparator exit with field-specific diagnostics. |
| T-56-02-02 | Repudiation | public failure evidence | mitigate | Require stderr to mention `expected-gcode-semantic-summary.tsv` and the mutated semantic field so failures are attributable. |
| T-56-02-03 | Tampering | existing marker/structural guards | mitigate | Keep `line_4` and `command_count_g1` mutation guards present and passing after semantic wiring. |
| T-56-02-04 | Elevation of Privilege | deferred behavior creep | mitigate | Unsupported claim mutation must fail; no network, device, host upload, release, sync, or runtime execution is added. |
</threat_model>

<verification>
Run after both tasks:

```bash
bash -n packages/parity/compare_prusaslicer_gcode_output_test.sh
shfmt -l -d packages/parity/compare_prusaslicer_gcode_output_test.sh
shellcheck packages/parity/compare_prusaslicer_gcode_output_test.sh
bazel test --cache_test_results=no //packages/parity:prusaslicer_gcode_output_parity_failure_test --test_output=errors
bazel run //packages/parity:prusaslicer_gcode_output_parity
git diff --check -- packages/parity/compare_prusaslicer_gcode_output_test.sh
```

After creating the required phase summary, run closeout verification for the generated summary file. Do not run `mdformat` over phase `*-SUMMARY.md` files; repo-local `AGENTS.md` requires `summary-extract` and `git diff --check` instead so YAML metadata stays parseable.

```bash
node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/56-executable-semantic-g-code-evidence/56-02-SUMMARY.md
git diff --check -- .planning/phases/56-executable-semantic-g-code-evidence/56-02-SUMMARY.md
```
</verification>

<success_criteria>
GSEV-02 is satisfied when the public failure test rejects movement class, coordinate-bound, extrusion-total, feedrate-observation, fixture identity, and unsupported deferred-behavior semantic drift while marker and structural mutation guards continue to pass.
</success_criteria>

<output>
After completion, create `.planning/phases/56-executable-semantic-g-code-evidence/56-02-SUMMARY.md` with `requirements-completed: [GSEV-02]`.
</output>
