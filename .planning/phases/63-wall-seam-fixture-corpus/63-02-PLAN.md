---
phase: 63-wall-seam-fixture-corpus
plan: "02"
type: execute
wave: 2
depends_on:
  - "63-01"
files_modified:
  - packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh
  - packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh
  - packages/parity-fixtures/BUILD.bazel
  - packages/parity-fixtures/README.md
autonomous: true
requirements_addressed: [SEAMFIX-01, SEAMFIX-02, SEAMFIX-03]
requirements:
  - SEAMFIX-01
  - SEAMFIX-02
  - SEAMFIX-03
user_setup: []
must_haves:
  truths:
    - "Maintainer can run a package-local wall-seam fixture verifier through Bash and Bazel."
    - "Missing, duplicate, out-of-order, unsupported, wrong-source, wrong-fixture, checksum, stale-doc, status-boundary, and overclaim cases fail closed with named diagnostics."
    - "Maintainer can inspect package-level command docs for the wall-seam fixture verifier without public wall-seam parity/status/docs publication."
    - "Maintainer can confirm `generated-outputs` remains `in progress`, sibling `fork.prusaslicer.gcode-output` and `fork.prusaslicer.arc-fitting` rows remain exact, and `fork.prusaslicer.wall-seam` remains absent in Phase 63."
  artifacts:
    - path: "packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh"
      provides: "Fail-closed verifier for wall-seam fixture corpus"
      contains: "WALL_SEAM_REQUIRED_FIELDS"
    - path: "packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh"
      provides: "Mutation coverage for SEAMFIX-03 drift classes"
      contains: "test_out_of_order_wall_seam_row_fails"
    - path: "packages/parity-fixtures/BUILD.bazel"
      provides: "Bazel sh_binary and sh_test targets"
      contains: "verify_prusa_wall_seam_fixture_test"
    - path: "packages/parity-fixtures/README.md"
      provides: "Package-level Phase 63 fixture docs and verification command"
      contains: "verify_prusa_wall_seam_fixture"
  key_links:
    - from: "packages/parity-fixtures/BUILD.bazel"
      to: "packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh"
      via: "sh_binary srcs"
      pattern: "name = \"verify_prusa_wall_seam_fixture\""
    - from: "packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv"
      via: "default package-local fixture path"
      pattern: "expected-wall-seam-summary\\.tsv"
    - from: "packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh"
      to: "packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh"
      via: "temp fixture mutation harness"
      pattern: "run_verifier"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 63-2026-06-26T23-55-59
generated_at: 2026-06-27T00:18:43Z
---

<objective>
Add fail-closed wall-seam fixture verification, mutation coverage, and package-level command docs.

Purpose: This turns the checked-in Phase 63 wall-seam fixture namespace into enforceable evidence that rejects drift and overclaims before Phase 64 Rust parsing consumes it.
Output: Bash verifier, Bash mutation suite, Bazel verifier/test targets, and package README updates naming the Phase 63 fixture verifier command.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/63-wall-seam-fixture-corpus/63-CONTEXT.md
@.planning/phases/63-wall-seam-fixture-corpus/63-RESEARCH.md
@.planning/phases/63-wall-seam-fixture-corpus/63-01-SUMMARY.md
@.planning/phases/62-wall-seam-scope-contract/62-VERIFICATION.md
@packages/prusa-wall-seam-scope/wall-seam-scope.md
@packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
@packages/parity-fixtures/BUILD.bazel
@packages/parity-fixtures/README.md
@packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh
@packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/fixture-provenance.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/wall-seam-observations.gcode
@packages/parity/status.tsv

<interfaces>
Plan 63-01 must have created these exact target names and files before this plan runs:
```text
//packages/parity-fixtures:prusa_wall_seam_bundle
//packages/parity-fixtures:prusa_wall_seam_expected_wall_seam_summary
packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv
packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/fixture-provenance.tsv
packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/wall-seam-observations.gcode
```

Verifier constants to use:
```text
SOURCE_REF=prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961
FIXTURE_PATH=packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/wall-seam-observations.gcode
FIXTURE_ID=wall-seam-observations.gcode
SOURCE_PATH=src/libslic3r/GCode/SeamAligned.cpp
SOURCE_ANCHOR=SeamAligned.cpp#L16;SeamAligned.cpp#L115-L148;SeamAligned.cpp#L272-L313;SeamAligned.cpp#L463-L525
EXPECTED_SHA=9a6306f382e64365ec6e11952f360195bca37fa442f29c7c7f616e1705a6bdad
EXPECTED_SIZE=360
WALL_SEAM_SUMMARY_ROW_COUNT=13
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-closed verifier, mutation suite, and Bazel targets</name>
  <files>packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh, packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh, packages/parity-fixtures/BUILD.bazel</files>
  <read_first>
    - `packages/parity-fixtures/BUILD.bazel`
    - `packages/parity-fixtures/verify_prusa_arc_fitting_fixture.sh`
    - `packages/parity-fixtures/verify_prusa_arc_fitting_fixture_test.sh`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/fixture-provenance.tsv`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/wall-seam-observations.gcode`
    - `packages/parity/status.tsv`
    - Confirm these new files are absent before creating them: `packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh`, `packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh`.
  </read_first>
  <behavior>
    - Valid Phase 63 fixture corpus exits 0 and prints exactly `ok: Prusa wall-seam fixture verification passed`.
    - Missing wall-seam row, duplicate wall-seam row, out-of-order wall-seam row, and unsupported wall-seam field each exit nonzero and name `expected-wall-seam-summary.tsv` plus the failed field.
    - Wrong source ref, wrong fixture path, wrong fixture ID, wrong provenance row, and wrong fixture SHA each exit nonzero with diagnostics naming the affected artifact.
    - Unsupported broad claim text in namespace README, package README, provenance, expected summary, or verifier source exits nonzero.
    - Generated-output status promotion, sibling status-row widening, and premature `fork.prusaslicer.wall-seam` publication each exit nonzero and name `packages/parity/status.tsv`.
  </behavior>
  <action>Implement `verify_prusa_wall_seam_fixture.sh` per D-09 and D-10 using the existing arc-fitting verifier style. Use `#!/usr/bin/env bash`, `set -euo pipefail`, package-local defaults for zero-argument runs, and explicit-argument mode accepting these files in order: fixture README, fixture provenance, expected wall-seam summary, wall-seam observations G-code, parity status TSV, parity-fixtures README, and optional checkout root. Define exact constants: `SOURCE_REF=prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, `FIXTURE_PATH=packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/wall-seam-observations.gcode`, `FIXTURE_ID=wall-seam-observations.gcode`, `SOURCE_PATH=src/libslic3r/GCode/SeamAligned.cpp`, `SOURCE_ANCHOR=SeamAligned.cpp#L16;SeamAligned.cpp#L115-L148;SeamAligned.cpp#L272-L313;SeamAligned.cpp#L463-L525`, `EXPECTED_SHA=9a6306f382e64365ec6e11952f360195bca37fa442f29c7c7f616e1705a6bdad`, `EXPECTED_SIZE=360`, `EXPECTED_LINE_COUNT=7`, `WALL_SEAM_SUMMARY_HEADER=$'source_ref\tfixture_path\twall_seam_field\twall_seam_category\twall_seam_value\tevidence_boundary'`, `WALL_SEAM_SUMMARY_ROW_COUNT=13`, and `WALL_SEAM_REQUIRED_FIELDS="source_ref inventory_source_paths source_anchor fixture_id fixture_path seam_transition_observations layer_context_observations travel_context_observations coordinate_bounds extrusion_observations retraction_observations evidence_boundary"`.

The verifier must check file existence, ASCII/LF encoding for `.gcode` and TSV artifacts, exact G-code line count, exact byte count, exact SHA-256, exact fixture lines from Plan 63-01, exact provenance header and row from Plan 63-01, exact expected-summary header, exactly six columns for summary data rows, allowed wall-seam fields only, one row per required field, exact approved row order, source/fixture provenance alignment, exact field-specific values from Plan 63-01, namespace README text, package README text once Task 2 adds it, and no overclaiming text. Add status checks requiring exactly one `generated-outputs` row with status `in progress`, exactly one `fork.prusaslicer.gcode-output` row matching the current full row in `packages/parity/status.tsv`, exactly one `fork.prusaslicer.arc-fitting` row matching the current full row, and zero `fork.prusaslicer.wall-seam` rows. Add self-scan guards for verifier behavior terms using split literals where needed: `curl `, `git `, `clone`, `fetch`, `PrusaSlicer --`, `slic3r --`, `send-gcode`, and `host upload`.

Implement `verify_prusa_wall_seam_fixture_test.sh` per D-11 with isolated `mktemp` roots, `write_valid_fixture_copy`, `run_verifier`, `assert_contains`, and one focused test per required drift class: `test_valid_fixture_passes`, `test_missing_wall_seam_row_fails`, `test_duplicate_wall_seam_row_fails`, `test_out_of_order_wall_seam_row_fails`, `test_unsupported_wall_seam_field_fails`, `test_unsupported_broad_claim_text_fails`, `test_wrong_source_ref_fails`, `test_wrong_fixture_identity_fails`, `test_wrong_fixture_path_fails`, `test_stale_namespace_readme_reference_fails`, `test_stale_package_readme_reference_fails`, `test_provenance_mismatch_fails`, `test_fixture_checksum_drift_fails`, `test_generated_outputs_status_promotion_fails`, `test_gcode_output_status_widening_fails`, `test_arc_fitting_status_widening_fails`, `test_premature_wall_seam_status_publication_fails`, and `test_forbidden_verifier_behavior_fails`. Each test must include `# Arrange`, `# Act`, and `# Assert` sections.

Update `packages/parity-fixtures/BUILD.bazel` per D-12 to add `sh_binary(name = "verify_prusa_wall_seam_fixture", srcs = ["verify_prusa_wall_seam_fixture.sh"], data = [":prusa_wall_seam_bundle", "README.md", "//packages/parity:status.tsv"])` and `sh_test(name = "verify_prusa_wall_seam_fixture_test", srcs = ["verify_prusa_wall_seam_fixture_test.sh"], data = ["verify_prusa_wall_seam_fixture.sh", ":prusa_wall_seam_bundle", "README.md", "//packages/parity:status.tsv"])`. Add both scripts to `package_boundary`. Do not add `//packages/parity:prusaslicer_wall_seam_parity`, `packages/slic3r-rust`, `docs/port/*`, or a `fork.prusaslicer.wall-seam` status row.</action>
  <verify>
    <automated>bash -n packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh</automated>
    <automated>bash -n packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh</automated>
    <automated>bazel query //packages/parity-fixtures:verify_prusa_wall_seam_fixture</automated>
    <automated>bazel query //packages/parity-fixtures:verify_prusa_wall_seam_fixture_test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "WALL_SEAM_REQUIRED_FIELDS|require_wall_seam_allowed_fields|require_wall_seam_field_counts|require_wall_seam_exact_rows" packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh` returns matches.
    - `rg -n "test_missing_wall_seam_row_fails|test_duplicate_wall_seam_row_fails|test_out_of_order_wall_seam_row_fails|test_unsupported_wall_seam_field_fails|test_generated_outputs_status_promotion_fails|test_premature_wall_seam_status_publication_fails" packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh` returns matches.
    - `rg -n "verify_prusa_wall_seam_fixture|verify_prusa_wall_seam_fixture_test" packages/parity-fixtures/BUILD.bazel` returns matches.
    - `test "$(awk -F '\t' '$1 == "fork.prusaslicer.wall-seam" { count++ } END { print count + 0 }' packages/parity/status.tsv)" = "0"` passes.
  </acceptance_criteria>
  <done>The verifier, mutation suite, and Bazel targets are created and syntax/query verified without publishing public parity/status/Rust/doc surfaces; full execution that depends on package README text is performed in Task 2 after that README text exists.</done>
</task>

<task type="auto">
  <name>Task 2: Publish package-level fixture command docs and final boundary checks</name>
  <files>packages/parity-fixtures/README.md</files>
  <read_first>
    - `packages/parity-fixtures/README.md`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md`
    - `packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh`
    - `.planning/phases/63-wall-seam-fixture-corpus/63-CONTEXT.md`
  </read_first>
  <action>Update `packages/parity-fixtures/README.md` per D-13. Add a Phase 63 paragraph in the Fork Fixture Namespace and Current State sections with these concrete facts: namespace `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/`, fixture `wall-seam-observations.gcode`, provenance `fixture-provenance.tsv`, expected artifact `expected-wall-seam-summary.tsv`, bundle target `//packages/parity-fixtures:prusa_wall_seam_bundle`, verification command `bazel run //packages/parity-fixtures:verify_prusa_wall_seam_fixture`, source ref `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, and source path `src/libslic3r/GCode/SeamAligned.cpp`. State that Phase 64 owns `slic3r_flavors::prusa_wall_seam`, Phase 65 owns `bazel run //packages/parity:prusaslicer_wall_seam_parity` and `fork.prusaslicer.wall-seam`, and Phase 63 does not update `packages/parity/status.tsv`, public parity behavior, Rust crates, or `docs/port/*`. Preserve D-15 by keeping broad `generated-outputs` as `in progress` and preserving the existing `fork.prusaslicer.gcode-output` and `fork.prusaslicer.arc-fitting` row meanings. Do not use achieved-language claims for byte-for-byte G-code parity, broad generated-output verification, full wall-seam algorithm or geometry equivalence, seam visibility, printability, firmware behavior, printer-runtime behavior, GUI behavior, support generation, release behavior, host upload, network/device behavior, profile auto-update execution, Bambu Studio, OrcaSlicer, upstream import, or sync automation.</action>
  <verify>
    <automated>shellcheck packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_wall_seam_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_wall_seam_fixture_test</automated>
    <automated>git diff --check -- packages/parity-fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Phase 63 adds the Prusa wall-seam fixture namespace|verify_prusa_wall_seam_fixture|prusa_wall_seam_bundle|expected-wall-seam-summary.tsv" packages/parity-fixtures/README.md` returns matches.
    - `rg -n "Phase 64 owns.*prusa_wall_seam|Phase 65 owns.*prusaslicer_wall_seam_parity|Phase 63 does not update.*packages/parity/status.tsv" packages/parity-fixtures/README.md` returns matches.
    - `test "$(awk -F '\t' '$1 == "generated-outputs" { if ($2 == "in progress") count++ } END { print count + 0 }' packages/parity/status.tsv)" = "1"` passes.
    - `test "$(awk -F '\t' '$1 == "fork.prusaslicer.gcode-output" { count++ } END { print count + 0 }' packages/parity/status.tsv)" = "1"` passes.
    - `test "$(awk -F '\t' '$1 == "fork.prusaslicer.arc-fitting" { count++ } END { print count + 0 }' packages/parity/status.tsv)" = "1"` passes.
    - `test "$(awk -F '\t' '$1 == "fork.prusaslicer.wall-seam" { count++ } END { print count + 0 }' packages/parity/status.tsv)" = "0"` passes.
  </acceptance_criteria>
  <done>Package-level docs expose the Phase 63 corpus, bundle, and verifier command while preserving all Phase 64/65 and deferred-scope boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in TSV/G-code -> Bash verifier | Verifier treats repository artifacts as inputs that may be malformed or tampered with. |
| shell arguments -> filesystem reads | Explicit verifier mode accepts path arguments from tests and must not execute or expand fixture content. |
| package docs -> public claims | README wording can accidentally spoof achieved parity/status if not checked. |
| status TSV -> fixture verifier | Current public status rows are read as boundary evidence and must not be widened by this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-05 | Tampering | `verify_prusa_wall_seam_fixture.sh` | mitigate | Enforce exact SHA-256, byte count, line count, ASCII/LF, headers, row counts, allowed fields, ordered rows, source/fixture identity, exact provenance, and package docs text. |
| T-63-06 | Repudiation | mutation suite | mitigate | Add one focused mutation test per SEAMFIX-03 drift class with diagnostic assertions naming the artifact or field. |
| T-63-07 | Elevation of Privilege | shell verifier argument handling | mitigate | Quote variable expansions, avoid `eval`, never execute fixture content, and use local file reads plus fixed POSIX text tools only. |
| T-63-08 | Information Disclosure | verifier behavior | mitigate | Self-scan for network, Git, generator, slicer-runtime, send-gcode, and host-upload behavior terms; keep verification offline and local. |
| T-63-09 | Spoofing | package README/status boundary | mitigate | Require exact README boundary snippets and exact status row counts for `generated-outputs`, `fork.prusaslicer.gcode-output`, `fork.prusaslicer.arc-fitting`, and absent `fork.prusaslicer.wall-seam`. |
</threat_model>

<verification>
Run all task-level commands plus:

```bash
bazel run //packages/parity-fixtures:verify_prusa_wall_seam_fixture
bazel test //packages/parity-fixtures:verify_prusa_wall_seam_fixture_test
test "$(awk -F '\t' '$1 == "fork.prusaslicer.wall-seam" { count++ } END { print count + 0 }' packages/parity/status.tsv)" = "0"
git diff --check -- packages/parity-fixtures
```
</verification>

<success_criteria>
SEAMFIX-03 is complete, and SEAMFIX-01 plus SEAMFIX-02 remain protected, when the Phase 63 fixture corpus is documented, Bazel-owned, verified by `//packages/parity-fixtures:verify_prusa_wall_seam_fixture`, covered by mutation tests for all listed drift classes, and still has no public wall-seam status row, public parity target, Rust parser, or public port-doc publication.
</success_criteria>

<output>
After completion, create `.planning/phases/63-wall-seam-fixture-corpus/63-02-SUMMARY.md` with `requirements-completed: [SEAMFIX-01, SEAMFIX-02, SEAMFIX-03]`.
</output>
