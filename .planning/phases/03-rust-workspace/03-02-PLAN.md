---
phase: 03-rust-workspace
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/slic3r-rust/BUILD.bazel
  - packages/slic3r-rust/crates/slic3r_core/BUILD.bazel
  - packages/slic3r-rust/crates/slic3r_core/tests/smoke.rs
autonomous: true
requirements:
  - RUST-02
must_haves:
  truths:
    - Maintainers can run Rust tests from Bazel against the new package.
    - Maintainers can run Rust format and lint checks from a single package-local Bazel verify surface.
    - The first Rust crate has a smoke test that proves the exported library target links correctly.
  artifacts:
    - path: packages/slic3r-rust/crates/slic3r_core/tests/smoke.rs
      provides: Bazel-native crate smoke test
      contains: "// Arrange"
    - path: packages/slic3r-rust/crates/slic3r_core/BUILD.bazel
      provides: Rust test, clippy, and rustfmt targets for the crate
      contains: "rust_test("
    - path: packages/slic3r-rust/BUILD.bazel
      provides: Package-local verification suite
      contains: "test_suite("
  key_links:
    - from: packages/slic3r-rust/BUILD.bazel
      to: packages/slic3r-rust/crates/slic3r_core/BUILD.bazel
      via: `verify` suite membership
      pattern: 'tests = \['
    - from: packages/slic3r-rust/crates/slic3r_core/tests/smoke.rs
      to: packages/slic3r-rust/crates/slic3r_core/src/lib.rs
      via: public crate symbol import
      pattern: "use slic3r_core::"
---

<objective>
Add Bazel-native format, lint, and test verification for the Rust package.

Purpose: Satisfy RUST-02 with a package-local verification command instead of Cargo wrapper scripts.
Output: A smoke test, crate-level Rust verification targets, and `//packages/slic3r-rust:verify`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-rust-workspace/03-CONTEXT.md
@.planning/phases/03-rust-workspace/03-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@../coding-and-architecture-requirements/standards/core/testing.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@.planning/phases/03-rust-workspace/03-01-PLAN.md

<interfaces>
Expected outputs from `03-01`:

From `packages/slic3r-rust/BUILD.bazel`:
```bzl
filegroup(
    name = "workspace_root",
    srcs = ["Cargo.toml"],
)
```

From `packages/slic3r-rust/crates/slic3r_core/BUILD.bazel`:
```bzl
rust_library(
    name = "slic3r_core",
    srcs = ["src/lib.rs"],
    edition = "2024",
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the first Bazel-native Rust smoke test</name>
  <files>packages/slic3r-rust/crates/slic3r_core/BUILD.bazel, packages/slic3r-rust/crates/slic3r_core/tests/smoke.rs</files>
  <action>Add a crate smoke test under `tests/smoke.rs` that proves the public symbol introduced in `03-01` compiles and links through Bazel. Keep the test single-concern and structured with explicit Arrange/Act/Assert comments. Wire it with `rust_test` in the crate `BUILD.bazel`; do not replace this with a shell test or a Cargo-only test command.</action>
  <verify>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_core:slic3r_core_smoke_test</automated>
  </verify>
  <done>The crate has a passing Bazel-native smoke test target that exercises the first exported Rust surface.</done>
</task>

<task type="auto">
  <name>Task 2: Expose package-local fmt, clippy, and verify targets</name>
  <files>packages/slic3r-rust/BUILD.bazel, packages/slic3r-rust/crates/slic3r_core/BUILD.bazel</files>
  <action>Add `rust_clippy`, `rustfmt_test`, and a package-level `test_suite(name = "verify")` that aggregates format check, lint, and the smoke test per RUST-02. Keep the verification surface Bazel-native: no alias pointing at a `test_suite`, no `sh_test` wrappers around Cargo, and no whole-repo Rust verify surface in this phase. The discoverable package command should be `bazel test //packages/slic3r-rust:verify`.</action>
  <verify>
    <automated>bazel test //packages/slic3r-rust:verify</automated>
  </verify>
  <done>`bazel test //packages/slic3r-rust:verify` runs rustfmt check, clippy, and the crate smoke test as one package-local verification surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust sources → Bazel verification targets | Verification must exercise the real crate files, not wrapper scripts or dead aliases |
| Maintainer command line → verify label | The documented package verify label must actually execute format, lint, and test work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | R | `packages/slic3r-rust:verify` | mitigate | Implement `verify` as a real `test_suite` that directly owns the Rust checks rather than an alias that only looks runnable |
| T-03-02-02 | E | Rust verification glue | mitigate | Use `rust_test`, `rust_clippy`, and `rustfmt_test` directly; do not shell out to `cargo fmt`, `cargo clippy`, or `cargo test` |
</threat_model>

<verification>
Run `bazel test //packages/slic3r-rust/crates/slic3r_core:slic3r_core_smoke_test`.
Run `bazel test //packages/slic3r-rust:verify`.
</verification>

<success_criteria>
The Rust package exposes a single Bazel verification command that executes rustfmt check, clippy, and a passing smoke test against the first crate.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rust-workspace/03-rust-workspace-02-SUMMARY.md`
</output>
