---
phase: 03-rust-workspace
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - MODULE.bazel
  - packages/BUILD.bazel
  - packages/slic3r-rust/BUILD.bazel
  - packages/slic3r-rust/Cargo.toml
  - packages/slic3r-rust/crates/slic3r_core/BUILD.bazel
  - packages/slic3r-rust/crates/slic3r_core/Cargo.toml
  - packages/slic3r-rust/crates/slic3r_core/src/lib.rs
autonomous: true
requirements:
  - RUST-01
must_haves:
  truths:
    - Bazel resolves a pinned Rust toolchain for this repo on macOS.
    - The Rust package contains a real library crate instead of an empty workspace shell.
    - Maintainers can build the Rust package from the repo root without touching the legacy oracle package.
  artifacts:
    - path: MODULE.bazel
      provides: rules_rust registration and an exact Rust toolchain pin
      contains: 'bazel_dep(name = "rules_rust"'
    - path: packages/slic3r-rust/crates/slic3r_core/BUILD.bazel
      provides: First Bazel-native Rust library target
      contains: "rust_library("
    - path: packages/slic3r-rust/crates/slic3r_core/src/lib.rs
      provides: Crate root for the first Rust package member
      contains: "#![forbid(unsafe_code)]"
  key_links:
    - from: MODULE.bazel
      to: packages/slic3r-rust/crates/slic3r_core/BUILD.bazel
      via: rules_rust toolchain registration
      pattern: 'register_toolchains\("@rust_toolchains//:all"\)'
    - from: packages/BUILD.bazel
      to: packages/slic3r-rust/BUILD.bazel
      via: package-level build alias
      pattern: 'actual = "//packages/slic3r-rust:'
---

<objective>
Stand up the Bazel-owned Rust toolchain and first real crate under `packages/slic3r-rust`.

Purpose: Satisfy RUST-01 by replacing the empty Rust workspace shell with a buildable Bright Builds-aligned package structure.
Output: A pinned `rules_rust` toolchain, a build-addressable Rust package surface, and the first `slic3r_core` library crate.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-rust-workspace/03-CONTEXT.md
@.planning/phases/03-rust-workspace/03-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@../coding-and-architecture-requirements/standards/core/architecture.md
@../coding-and-architecture-requirements/standards/core/code-shape.md
@../coding-and-architecture-requirements/standards/core/verification.md
@../coding-and-architecture-requirements/standards/languages/rust.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
Current scaffold:

From `MODULE.bazel`:
```bzl
module(
    name = "slic3r",
    version = "0.0.0",
)
```

From `packages/BUILD.bazel`:
```bzl
alias(
    name = "rust_workspace",
    actual = "//packages/slic3r-rust:workspace_root",
)
```

From `packages/slic3r-rust/Cargo.toml`:
```toml
[workspace]
members = []
resolver = "3"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the Rust toolchain and Bazel package build surface</name>
  <files>MODULE.bazel, packages/BUILD.bazel, packages/slic3r-rust/BUILD.bazel</files>
  <action>Add `rules_rust` `0.69.0` and register a pinned Rust `1.94.1` toolchain with `rustfmt_version = "1.94.1"` per RUST-01. Replace the Rust package placeholder shape with a real Bazel build surface by keeping `workspace_root` for metadata and adding a buildable alias such as `//packages:rust_build` that resolves to the first Rust library target. Keep Bazel orchestration thin: do not add Cargo shell wrappers, do not add `crate_universe`, and do not introduce host-probing scripts or a checked-in Bazel launcher wrapper in this phase.</action>
  <verify>
    <automated>bazel build //packages:rust_build</automated>
  </verify>
  <done>`MODULE.bazel` pins `rules_rust` and Rust, and the repo exposes a Bazel-addressable Rust build surface from `//packages:rust_build`.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize the first Bright Builds-aligned library crate</name>
  <files>packages/slic3r-rust/Cargo.toml, packages/slic3r-rust/crates/slic3r_core/BUILD.bazel, packages/slic3r-rust/crates/slic3r_core/Cargo.toml, packages/slic3r-rust/crates/slic3r_core/src/lib.rs</files>
  <action>Create the first workspace member crate at `packages/slic3r-rust/crates/slic3r_core` per RUST-01 and the phase research. Configure the Cargo workspace with explicit members, workspace package metadata, and workspace rust lints, then make the crate a library-only core boundary with `#![forbid(unsafe_code)]`, edition `2024`, and no `mod.rs` tree. Add only the minimal public symbol and crate-level docs needed to prove the package compiles cleanly under Bazel; do not pre-design future CLI, launcher, or parity APIs in this phase.</action>
  <verify>
    <automated>bazel build //packages/slic3r-rust/...</automated>
  </verify>
  <done>The Rust workspace has a real `slic3r_core` member crate and Bazel builds it successfully without relying on empty placeholder targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → Bazel module graph | Untrusted local edits can alter downloaded rules/toolchain versions |
| Bazel package aliases → Rust crate targets | Repo-facing build labels must resolve to real Rust targets, not placeholder filegroups |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | `MODULE.bazel` | mitigate | Pin exact `rules_rust`, Rust, and rustfmt versions instead of floating defaults |
| T-03-01-02 | E | `packages/slic3r-rust/BUILD.bazel` | mitigate | Use native `rust_library` targets and package aliases; do not add shell wrappers that execute host Cargo commands |
</threat_model>

<verification>
Run `bazel build //packages:rust_build`.
Run `bazel build //packages/slic3r-rust/...`.
</verification>

<success_criteria>
The repo root can build a real Rust package through Bazel, the Rust toolchain is pinned in `MODULE.bazel`, and `packages/slic3r-rust` contains a concrete `slic3r_core` library crate with Bright Builds-aligned structure.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rust-workspace/03-rust-workspace-01-SUMMARY.md`
</output>
