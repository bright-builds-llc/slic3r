---
phase: 52-executable-structural-g-code-evidence
plan: "05"
type: execute
wave: 5
depends_on:
  - "52-04"
files_modified:
  - packages/parity/README.md
  - packages/parity-fixtures/README.md
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
  - packages/slic3r-rust/README.md
autonomous: true
requirements:
  - GCEV-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 52-2026-06-18T01-09-43
generated_at: 2026-06-18T01:27:32.342Z
must_haves:
  truths:
    - "Package docs describe `fork.prusaslicer.gcode-output` as the narrow structural Prusa G-code evidence slice."
    - "Package docs name the Phase 49 scope, Phase 50 structural sidecar, Phase 51 Rust structural parser/readiness, and Phase 52 public command chain."
    - "Package docs keep byte-for-byte G-code, broad generated-output, runtime, release, network/device, non-Prusa fork, upstream import, and sync surfaces deferred."
  artifacts:
    - path: "packages/parity/README.md"
      provides: "Public parity command and fork status row interpretation"
      contains: "narrow structural Prusa G-code evidence slice"
    - path: "packages/parity-fixtures/README.md"
      provides: "Fixture package ownership for the structural sidecar consumed by Phase 52"
      contains: "expected-gcode-structural-summary.tsv"
    - path: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md"
      provides: "Fixture-local structural publication boundary"
      contains: "Phase 52 public structural parity/status publication"
    - path: "packages/slic3r-rust/README.md"
      provides: "Rust summary binary structural mode documentation"
      contains: "--structural"
  key_links:
    - from: "packages/parity/README.md"
      to: "//packages/parity:prusaslicer_gcode_output_parity"
      via: "documented Bazel run command"
      pattern: "bazel run //packages/parity:prusaslicer_gcode_output_parity"
    - from: "packages/slic3r-rust/README.md"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs"
      via: "documented `--structural` mode"
      pattern: "--structural"
---

<objective>
Update package-level documentation for the public structural Prusa G-code evidence chain.

Purpose: GCEV-03 requires package docs to publish the exact narrow structural evidence slice without broadening generated-output or runtime claims.
Output: parity, fixture, fixture-local, and Rust workspace README wording aligned with Phase 52 structural evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
@.planning/phases/52-executable-structural-g-code-evidence/52-RESEARCH.md
@.planning/phases/52-executable-structural-g-code-evidence/52-04-SUMMARY.md
@packages/parity/README.md
@packages/parity-fixtures/README.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
@packages/slic3r-rust/README.md
@packages/parity/status.tsv
</context>

<decision_coverage>
- D-08: package docs move from summary-only wording to narrow structural evidence wording.
- D-10: docs describe the Phase 49 -> Phase 50 -> Phase 51 -> Phase 52 evidence chain.
- D-11: package docs keep all deferred generated-output/runtime/fork surfaces explicit.
- D-04: docs must not imply fresh G-code generation or runtime PrusaSlicer comparison.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Update parity and Rust package docs</name>
  <files>packages/parity/README.md, packages/slic3r-rust/README.md</files>
  <read_first>
    packages/parity/README.md
    packages/slic3r-rust/README.md
    packages/parity/status.tsv
    packages/parity/compare_prusaslicer_gcode_output.sh
    packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_gcode_output_summary.rs
    .planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
  </read_first>
  <action>
    In `packages/parity/README.md`, update the current-surface bullet and `fork.prusaslicer.gcode-output` section to say `bazel run //packages/parity:prusaslicer_gcode_output_parity` runs the shared fixture comparison for the narrow structural Prusa G-code evidence slice backed by `expected-gcode-summary.tsv` and `expected-gcode-structural-summary.tsv`. Name the Phase 46 fixture, Phase 50 structural sidecar, Phase 51 Rust structural parser/readiness boundary, and Phase 52 public parity command. Keep the status token `fork.prusaslicer.gcode-output` and command unchanged. In `packages/slic3r-rust/README.md`, update the G-code paragraph to document that `prusa_gcode_output_summary` supports existing summary mode and explicit `--structural expected-gcode-structural-summary.tsv` mode, both over caller-supplied checked-in TSV artifacts. State that it does not inspect Git, access the network, spawn PrusaSlicer, generate fresh G-code, upload, release, import upstream source, or sync.
  </action>
  <acceptance_criteria>
    `rg -n "narrow structural Prusa G-code evidence slice|expected-gcode-structural-summary.tsv|Phase 50 structural sidecar|Phase 51 Rust structural parser/readiness boundary|Phase 52 public parity command" packages/parity/README.md packages/slic3r-rust/README.md` finds required package-doc wording.
    `rg -n -- "--structural expected-gcode-structural-summary.tsv|does not inspect Git|generate fresh G-code" packages/slic3r-rust/README.md` finds the Rust adapter boundaries.
    `rg -n "summary-only Prusa G-code evidence slice" packages/parity/README.md packages/slic3r-rust/README.md` finds no matches.
  </acceptance_criteria>
  <verify>
    <automated>mdformat --check packages/parity/README.md packages/slic3r-rust/README.md</automated>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
  </verify>
  <done>Parity and Rust package docs publish the structural evidence chain while preserving the exact public command and no-runtime boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Update fixture package docs for Phase 52 consumption</name>
  <files>packages/parity-fixtures/README.md, packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md</files>
  <read_first>
    packages/parity-fixtures/README.md
    packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
    packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
    packages/parity/status.tsv
    .planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
  </read_first>
  <action>
    In `packages/parity-fixtures/README.md`, replace the old text that says the G-code row is a narrow summary-only evidence slice with structural evidence wording: Phase 46 owns fixture surface integrity, Phase 50 adds `expected-gcode-structural-summary.tsv`, Phase 51 consumes it through the Rust structural parser/readiness boundary, and Phase 52 verifies the narrow structural evidence slice through `bazel run //packages/parity:prusaslicer_gcode_output_parity` plus the exact `fork.prusaslicer.gcode-output` status row. In the fixture-local README, keep the artifact/provenance values unchanged and update the status boundary to say `Phase 52 public structural parity/status publication consumes the Phase 50 structural sidecar through the Phase 51 Rust structural parser/readiness boundary.` Preserve the existing statements that there is no live generation, upstream fetching/importing, binary G-code, thumbnails, post-processing, host upload, printer-runtime behavior, network/device behavior, Bambu Studio support, OrcaSlicer support, upstream source imports, or sync automation.
  </action>
  <acceptance_criteria>
    `rg -n "Phase 50 adds .*expected-gcode-structural-summary.tsv|Phase 51 consumes|Phase 52 verifies the narrow structural evidence slice|fork\\.prusaslicer\\.gcode-output" packages/parity-fixtures/README.md` finds fixture package wording.
    `rg -n "Phase 52 public structural parity/status publication consumes the Phase 50 structural sidecar through the Phase 51 Rust structural parser/readiness boundary" packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md` finds fixture-local status boundary.
    `rg -n "summary-only evidence slice" packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md` finds no stale public-slice wording except historical Phase 46 artifact descriptions.
  </acceptance_criteria>
  <verify>
    <automated>mdformat --check packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
  </verify>
  <done>Fixture package docs make the structural sidecar's Phase 52 consumption visible without claiming generated-output runtime behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package docs to maintainer expectations | README wording influences what maintainers believe the evidence command proves. |
| Rust package docs to command usage | Rust workspace docs describe how the summary binary should be invoked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05-01 | Information disclosure | package README wording | mitigate | Replace stale summary-only public-slice text with narrow structural wording and keep deferred surfaces explicit. |
| T-52-05-02 | Repudiation | Rust README command boundary | mitigate | Document both summary and `--structural` modes as checked-in TSV parsing only, with no runtime generation or upstream execution. |
| T-52-05-03 | Tampering | fixture package narrative | mitigate | Name the Phase 50 sidecar and Phase 51 parser/readiness boundary so structural evidence cannot be mistaken for shell-only fixture checks. |
</threat_model>

<verification>
Run task verification plus `bazel test --cache_test_results=no //packages/parity:prusaslicer_gcode_output_parity_failure_test` and `git diff --check`.
</verification>

<success_criteria>
- Package docs consistently say `fork.prusaslicer.gcode-output` is narrow structural evidence.
- Docs name the Phase 49, 50, 51, and 52 evidence chain.
- Docs do not claim byte-for-byte G-code, broad generated-output, runtime, release, network/device, non-Prusa fork, upstream import, or sync support.
</success_criteria>

<output>
After completion, create `.planning/phases/52-executable-structural-g-code-evidence/52-05-SUMMARY.md`.
</output>
