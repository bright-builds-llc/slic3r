---
phase: 52-executable-structural-g-code-evidence
plan: "06"
type: execute
wave: 6
depends_on:
  - "52-05"
files_modified:
  - docs/port/README.md
  - docs/port/parity-matrix.md
  - docs/port/migration-guidance.md
  - docs/port/package-map.md
autonomous: true
requirements:
  - GCEV-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 52-2026-06-18T01-09-43
generated_at: 2026-06-18T01:27:32.342Z
must_haves:
  truths:
    - "Port docs publish only the exact narrow structural `fork.prusaslicer.gcode-output` evidence slice."
    - "Port docs require the Phase 49 closed structural scope contract as the first link in the Phase 49 -> Phase 50 -> Phase 51 -> Phase 52 public evidence chain."
    - "The `Generated outputs` surface remains `in progress` in public docs."
    - "Port docs keep deferred generated-output, runtime, release, network/device, fork, upstream import, and sync surfaces explicit."
  artifacts:
    - path: "docs/port/README.md"
      provides: "Control-plane current state for structural G-code evidence"
      contains: "narrow structural Prusa G-code evidence slice"
    - path: "docs/port/parity-matrix.md"
      provides: "Public parity dashboard preserving `generated-outputs` in progress"
      contains: "Generated outputs"
    - path: "docs/port/migration-guidance.md"
      provides: "Maintainer fixture/status evolution guidance"
      contains: "expected-gcode-structural-summary.tsv"
    - path: "docs/port/package-map.md"
      provides: "Package ownership map for Phase 52 structural public evidence"
      contains: "Phase 49 closed structural scope contract"
  key_links:
    - from: "docs/port/parity-matrix.md"
      to: "packages/parity/status.tsv"
      via: "status wording for `fork.prusaslicer.gcode-output` and `generated-outputs`"
      pattern: "fork\\.prusaslicer\\.gcode-output"
    - from: "docs/port/package-map.md"
      to: "packages/parity/README.md"
      via: "package role summary for parity command ownership"
      pattern: "packages/parity"
---

<objective>
Update public port documentation for Phase 52 structural Prusa G-code evidence.

Purpose: GCEV-03 requires public docs to describe the exact narrow structural slice while keeping broad `generated-outputs` in progress and every deferred surface explicit.
Output: updated port control-plane README, parity matrix, migration guidance, and package map wording.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
@.planning/phases/52-executable-structural-g-code-evidence/52-RESEARCH.md
@.planning/phases/52-executable-structural-g-code-evidence/52-05-SUMMARY.md
@docs/port/README.md
@docs/port/parity-matrix.md
@docs/port/migration-guidance.md
@docs/port/package-map.md
@packages/parity/status.tsv
</context>

<decision_coverage>
- D-08: port docs move the narrow fork row from summary-only to structural evidence.
- D-09: port docs keep broad `generated-outputs` exactly `in progress`.
- D-10: port docs describe the Phase 49 scope, Phase 50 fixture, Phase 51 Rust parser/readiness, and Phase 52 public executable evidence chain.
- D-11: port docs keep byte-for-byte G-code parity, geometry/toolpath parity, printability, printer-runtime behavior, support generation, wall seam behavior, arc fitting, STEP import, full 3MF import/export, GUI behavior, binary G-code, thumbnails, post-processing, host upload, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, and sync automation explicit as deferred.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Update parity matrix and migration guidance</name>
  <files>docs/port/parity-matrix.md, docs/port/migration-guidance.md</files>
  <read_first>
    docs/port/parity-matrix.md
    docs/port/migration-guidance.md
    packages/parity/status.tsv
    packages/parity/README.md
    .planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
  </read_first>
  <action>
    In `docs/port/parity-matrix.md`, update the `Generated outputs` table note and the fork parity interpretation bullets so they say `fork.prusaslicer.gcode-output` is verified only for the narrow structural Prusa G-code evidence slice through `bazel run //packages/parity:prusaslicer_gcode_output_parity`. The note must include the sentence `Phase 49 closed structural scope contract defines the scope. Phase 50 adds the structural sidecar. Phase 51 proves typed structural parsing/readiness. Phase 52 proves executable structural evidence/status wiring.` Keep the `Generated outputs` status column exactly `` `in progress` ``. In `docs/port/migration-guidance.md`, update the Prusa G-code guidance and future fork status bullets to name the Phase 49 closed structural scope contract, `expected-gcode-summary.tsv`, `expected-gcode-structural-summary.tsv`, `prusa_gcode_output_summary_lines`, `prusa_gcode_output_structural_summary_lines`, and the public parity command. Preserve the deferred surface list from D-11.
  </action>
  <acceptance_criteria>
    `rg -n "Phase 49 closed structural scope contract defines the scope\\. Phase 50 adds the structural sidecar\\. Phase 51 proves typed structural parsing/readiness\\. Phase 52 proves executable structural evidence/status wiring\\." docs/port/parity-matrix.md` finds the exact sentence.
    `rg -n "narrow structural Prusa G-code evidence slice|Phase 49 closed structural scope contract|expected-gcode-structural-summary.tsv|prusa_gcode_output_structural_summary_lines" docs/port/parity-matrix.md docs/port/migration-guidance.md` finds required structural wording.
    `rg -n "Phase 49 closed structural scope contract" docs/port/parity-matrix.md docs/port/migration-guidance.md` finds the required Phase 49 contract wording.
    `rg -n "\\| Generated outputs \\|.*\\| .*in progress.* \\|" docs/port/parity-matrix.md` confirms broad status remains in progress.
    `rg -n "summary-only Prusa G-code evidence slice" docs/port/parity-matrix.md docs/port/migration-guidance.md` finds no matches.
  </acceptance_criteria>
  <verify>
    <automated>mdformat --check docs/port/parity-matrix.md docs/port/migration-guidance.md</automated>
    <automated>bazel run //packages/parity:status</automated>
  </verify>
  <done>The parity matrix and migration guidance publish structural evidence while preserving the broad generated-output boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Update port README and package map ownership</name>
  <files>docs/port/README.md, docs/port/package-map.md</files>
  <read_first>
    docs/port/README.md
    docs/port/package-map.md
    docs/port/parity-matrix.md
    docs/port/migration-guidance.md
    packages/parity/README.md
    packages/slic3r-rust/README.md
    .planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
  </read_first>
  <action>
    In `docs/port/README.md`, update the current parity visibility section to describe `bazel run //packages/parity:prusaslicer_gcode_output_parity` as the shared parity command for the narrow structural Prusa G-code evidence slice backed by the Phase 49 closed structural scope contract, Phase 50 structural fixture, Phase 51 Rust structural parser/readiness, and Phase 52 public executable evidence. In `docs/port/package-map.md`, update the `packages/parity`, `packages/parity-fixtures`, `packages/prusa-gcode-output-scope`, and `packages/slic3r-rust` role/history text so Phase 49 owns the closed structural scope contract, Phase 52 owns public structural evidence, Phase 50 owns `expected-gcode-structural-summary.tsv`, Phase 51 owns Rust structural parser/readiness, and the broad `generated-outputs` status remains in progress. Preserve the existing deferred list from D-11; do not claim byte-for-byte G-code parity, broad generated-output parity, runtime support, GUI behavior, release support, Bambu Studio, OrcaSlicer, upstream source imports, or sync automation.
  </action>
  <acceptance_criteria>
    `rg -n "narrow structural Prusa G-code evidence slice|Phase 49 closed structural scope contract|Phase 49 owns the closed structural scope contract|Phase 52 owns public structural evidence|expected-gcode-structural-summary.tsv|broad generated-outputs status remains in progress" docs/port/README.md docs/port/package-map.md` finds required public docs wording.
    `rg -n "Phase 49 closed structural scope contract" docs/port/README.md docs/port/package-map.md` finds the required Phase 49 contract wording.
    `rg -n "byte-for-byte G-code parity|full generated-output parity|printer-runtime behavior|Bambu Studio|OrcaSlicer|upstream source imports|sync automation" docs/port/README.md docs/port/package-map.md` finds deferred-surface wording.
    `rg -n "summary-only Prusa G-code evidence slice" docs/port/README.md docs/port/package-map.md` finds no matches.
  </acceptance_criteria>
  <verify>
    <automated>mdformat --check docs/port/README.md docs/port/package-map.md</automated>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
  </verify>
  <done>Port README and package map identify Phase 52 structural evidence ownership without broadening the public parity claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs to maintainer decisions | Port docs guide maintainer interpretation of parity status and migration readiness. |
| Status source to docs | Docs must reflect `packages/parity/status.tsv` without expanding its claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06-01 | Information disclosure | `docs/port/parity-matrix.md` | mitigate | Keep `Generated outputs` status `in progress` and document the fork row as narrow structural evidence only. |
| T-52-06-02 | Repudiation | migration guidance | mitigate | Name the exact command, status token, summary artifact, structural artifact, and Rust helpers so the evidence chain is auditable. |
| T-52-06-03 | Tampering | package map ownership | mitigate | Tie Phase 49 ownership to the closed structural scope contract and Phase 52 ownership to public structural evidence only, while preserving deferred surfaces. |
</threat_model>

<verification>
Run both task verification sets plus `bazel test --cache_test_results=no //packages/parity:prusaslicer_gcode_output_parity_failure_test`, `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture`, and `git diff --check`.
</verification>

<success_criteria>
- Port docs no longer call the public `fork.prusaslicer.gcode-output` row summary-only.
- Port docs name Phase 49 closed structural scope contract, Phase 50 structural sidecar, Phase 51 Rust structural parser/readiness, and Phase 52 public command/status.
- Broad `generated-outputs` remains `in progress`; every D-11 deferred surface remains explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/52-executable-structural-g-code-evidence/52-06-SUMMARY.md`.
</output>
