---
phase: 52-executable-structural-g-code-evidence
plan: "04"
type: execute
wave: 4
depends_on:
  - "52-03"
files_modified:
  - packages/prusa-gcode-output-scope/gcode-output-scope.md
  - packages/prusa-gcode-output-scope/README.md
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
  - packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
autonomous: true
requirements:
  - GCEV-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 52-2026-06-18T01-09-43
generated_at: 2026-06-18T01:27:32.342Z
must_haves:
  truths:
    - "Maintainer can inspect the scope package and see the narrow status row described as structural evidence, not summary-only evidence."
    - "The scope verifier rejects stale summary-only status publication wording and still enforces broad `generated-outputs` as `in progress`."
    - "The scope package continues to forbid byte-for-byte, geometry, printability, runtime, release, network/device, non-Prusa fork, upstream import, and sync claims."
  artifacts:
    - path: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      provides: "Structural traceability row aligned with Phase 52 publication"
      contains: "narrow structural Prusa G-code evidence slice"
    - path: "packages/prusa-gcode-output-scope/README.md"
      provides: "Scope package boundary wording for Phase 52 consumption"
      contains: "Phase 52 public evidence consumes this contract"
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      provides: "Fail-closed verifier for structural status wording and scope traceability"
      contains: "narrow structural Prusa G-code evidence slice"
    - path: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh"
      provides: "Mutation fixtures for structural status and traceability wording"
      contains: "narrow structural Prusa G-code evidence slice"
  key_links:
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      to: "packages/parity/status.tsv"
      via: "exact structural `GCODE_OUTPUT_STATUS_ROW`"
      pattern: "GCODE_OUTPUT_STATUS_ROW"
    - from: "packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh"
      to: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      via: "`verify_structural_traceability`"
      pattern: "Published narrow status row"
---

<objective>
Update the Prusa G-code scope package so Phase 52 structural publication remains traceable and fail-closed.

Purpose: GCEV-03 requires status/docs to publish the exact narrow structural evidence slice without leaving stale summary-only traceability in the scope verifier.
Output: scope package wording plus verifier/test updates that enforce the structural status row and broad-generated-output restraint.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
@.planning/phases/52-executable-structural-g-code-evidence/52-RESEARCH.md
@.planning/phases/52-executable-structural-g-code-evidence/52-03-SUMMARY.md
@packages/prusa-gcode-output-scope/gcode-output-scope.md
@packages/prusa-gcode-output-scope/README.md
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
@packages/parity/status.tsv
</context>

<decision_coverage>
- D-08: scope package wording must move the narrow fork row from summary-only to structural evidence.
- D-09: verifier must continue enforcing exactly one broad `generated-outputs` in-progress row.
- D-10: docs must describe the Phase 49 scope, Phase 50 fixture, Phase 51 Rust parser/readiness, and Phase 52 public evidence chain.
- D-11: deferred surfaces remain explicit and forbidden from positive claims.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Update scope package traceability wording</name>
  <files>packages/prusa-gcode-output-scope/gcode-output-scope.md, packages/prusa-gcode-output-scope/README.md</files>
  <read_first>
    packages/prusa-gcode-output-scope/gcode-output-scope.md
    packages/prusa-gcode-output-scope/README.md
    packages/parity/status.tsv
    .planning/phases/52-executable-structural-g-code-evidence/52-CONTEXT.md
  </read_first>
  <action>
    In `gcode-output-scope.md`, replace the opening description `narrow summary-only` wording with `narrow Prusa G-code evidence contract that starts with summary markers and is extended by v1.13 structural evidence`. In the `## v1.13 Structural Traceability` table, replace the `Published narrow status row` target with exactly: `` `fork.prusaslicer.gcode-output` stays verified only for the narrow structural Prusa G-code evidence slice in `packages/parity/status.tsv`, backed by the Phase 46 fixture, Phase 50 structural summary, Phase 51 Rust structural parser/readiness boundary, and Phase 52 public parity command ``. In the final boundary paragraph, state that the scope record is consumed by Phase 50 fixture expansion, Phase 51 Rust structural parser/readiness, and Phase 52 public structural evidence, while still not proving byte-for-byte G-code parity, broad generated-output parity, generated-output status promotion, runtime behavior, release behavior, network/device behavior, non-Prusa fork behavior, upstream imports, or sync automation. In `README.md`, add the exact sentence `Phase 52 public evidence consumes this contract for the narrow structural Prusa G-code evidence slice while keeping broad generated-outputs in progress.`
  </action>
  <acceptance_criteria>
    `rg -n "narrow structural Prusa G-code evidence slice|Phase 52 public evidence consumes this contract|Phase 51 Rust structural parser/readiness boundary|Phase 52 public parity command" packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md` finds all required phrases.
    `rg -n "narrow summary-only Prusa G-code evidence slice" packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md` finds no matches.
    `rg -n "byte-for-byte G-code parity|toolpath geometry|printer-runtime behavior|Bambu Studio support|OrcaSlicer support|upstream source imports|sync automation" packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md` finds deferred/forbidden-scope text.
  </acceptance_criteria>
  <verify>
    <automated>mdformat --check packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md</automated>
    <automated>git diff --check -- packages/prusa-gcode-output-scope/gcode-output-scope.md packages/prusa-gcode-output-scope/README.md</automated>
  </verify>
  <done>The scope package docs trace the narrow status row to structural evidence and keep the broad scope boundaries explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile scope verifier with structural publication</name>
  <files>packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh, packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</files>
  <read_first>
    packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
    packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
    packages/prusa-gcode-output-scope/gcode-output-scope.md
    packages/prusa-gcode-output-scope/README.md
    packages/parity/status.tsv
  </read_first>
  <action>
    Update `GCODE_OUTPUT_STATUS_ROW` in both scope verifier scripts to the exact structural row from Plan 52-03. Update `verify_readme` to require `Phase 52 public evidence consumes this contract for the narrow structural Prusa G-code evidence slice while keeping broad generated-outputs in progress.` Update `verify_structural_traceability` to require the exact `Published narrow status row` text from Task 1. Update the valid temp fixtures and wrong-row mutations in `verify_prusa_gcode_output_scope_test.sh` to match the structural wording. Keep the generated-output guard requiring exactly one `generated-outputs` row with status `in progress`, keep all Phase 49 overclaim rejections, and keep the exact 16-row structural field table enforcement.
  </action>
  <acceptance_criteria>
    `rg -n "narrow structural Prusa G-code evidence slice|Phase 52 public evidence consumes this contract|generated-outputs.*in progress" packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` finds required verifier text.
    `rg -n "narrow summary-only Prusa G-code evidence slice" packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh` finds no matches.
    `bazel run //packages/prusa-gcode-output-scope:verify` prints `ok: Prusa G-code output scope verification passed`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh</automated>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
    <automated>bazel test --cache_test_results=no //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
  </verify>
  <done>The scope verifier accepts only the structural status/traceability wording and still fails closed on broad status promotion or forbidden scope claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scope docs to verifier contract | Markdown scope records are treated as evidence-chain source of truth by local Bash verification. |
| Status TSV to scope verifier | Scope verification reads public status rows to prevent broad overclaiming. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04-01 | Repudiation | scope traceability row | mitigate | Require exact structural status wording naming Phase 46, Phase 50, Phase 51, and Phase 52 links. |
| T-52-04-02 | Information disclosure | scope and README wording | mitigate | Preserve explicit deferred-surface text and existing forbidden-claim scans. |
| T-52-04-03 | Tampering | generated-output status guard | mitigate | Keep verifier check that `generated-outputs` appears exactly once and remains `in progress`. |
</threat_model>

<verification>
Run all task verification commands plus `bazel run //packages/parity:prusaslicer_gcode_output_parity`, `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture`, and `git diff --check`.
</verification>

<success_criteria>
- Scope docs no longer describe the published narrow fork row as summary-only.
- Scope verifier and mutation suite pass with structural status wording.
- Broad `generated-outputs` remains exactly `in progress`.
</success_criteria>

<output>
After completion, create `.planning/phases/52-executable-structural-g-code-evidence/52-04-SUMMARY.md`.
</output>
