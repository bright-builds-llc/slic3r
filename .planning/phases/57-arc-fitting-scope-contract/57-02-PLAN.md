---
phase: 57-arc-fitting-scope-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - "57-01"
files_modified:
  - packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
autonomous: true
requirements:
  - ARCSCOPE-02
  - ARCSCOPE-03
must_haves:
  truths:
    - "Maintainer can run the arc-fitting scope verifier and see scope drift fail closed."
    - "The verifier rejects unsupported arc fields, missing or duplicate rows, traceability drift, generated-output status promotion, G-code output status drift, premature arc status publication, runtime/printability claims, and missing deferred-scope wording."
    - "The verifier performs only local file reads and text checks."
  artifacts:
    - path: "packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh"
      provides: "Fail-closed arc-fitting scope verifier"
      contains: "ok: Prusa arc-fitting scope verification passed"
    - path: "packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh"
      provides: "Exact generated-output and G-code row preservation checks"
      contains: "fork.prusaslicer.gcode-output"
    - path: "packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh"
      provides: "Closed arc field table checks"
      contains: "arc_command_counts"
  key_links:
    - from: "packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh"
      to: "packages/prusa-arc-fitting-scope/arc-fitting-scope.md"
      via: "Scope table checks"
      pattern: "Approved Arc Evidence Fields"
    - from: "packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh"
      to: "packages/parity/status.tsv"
      via: "Status preservation checks"
      pattern: "generated-outputs"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 57-2026-06-23T18-45-58
generated_at: 2026-06-23T19:00:00Z
---

<objective>
Implement the fail-closed verifier for the Phase 57 arc-fitting scope contract.

Purpose: Make the contract executable so unsupported arc fields, missing traceability, broad claims, and status drift cannot pass silently.
Output: `packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-arc-fitting-scope-contract/57-CONTEXT.md
@.planning/phases/57-arc-fitting-scope-contract/57-RESEARCH.md
@.planning/phases/57-arc-fitting-scope-contract/57-01-SUMMARY.md
@packages/prusa-arc-fitting-scope/README.md
@packages/prusa-arc-fitting-scope/arc-fitting-scope.md
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
@packages/fork-inventories/prusaslicer.tsv
@packages/fork-inventories/category-map.tsv
@packages/parity/status.tsv
</context>

<threat_model>
This verifier must not run external tools, network calls, Git commands, source imports, or runtime generation. It should read only the files passed as arguments or package-local defaults.

Primary risk: false-positive verification from loose text matching. Mitigation: use exact rows, exact row counts, first-field duplicate checks, category-map reference counts, and explicit forbidden-claim rejection.
</threat_model>

<tasks>
<task id="1" name="Create verifier entrypoint and file argument handling">
<read_first>
- packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
- packages/prusa-arc-fitting-scope/BUILD.bazel
- packages/prusa-arc-fitting-scope/README.md
- packages/prusa-arc-fitting-scope/arc-fitting-scope.md
</read_first>
<action>
Create executable `packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh` with:
- shebang `#!/usr/bin/env bash`
- `set -euo pipefail`
- `error()` helper that prints `error: ...` to stderr and exits 1
- package-local default paths for `README.md`, `arc-fitting-scope.md`, `packages/fork-inventories/prusaslicer.tsv`, `packages/fork-inventories/category-map.tsv`, and `packages/parity/status.tsv`
- support for exactly 0 args, 5 args (`README scope inventory category-map status`), or 6 args (same plus checkout root)
- usage error text `usage: verify_prusa_arc_fitting_scope.sh [README arc-fitting-scope inventory category-map parity-status [checkout-root]]`
- final success output `ok: Prusa arc-fitting scope verification passed`
</action>
<acceptance_criteria>
- `packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh` starts with `#!/usr/bin/env bash`.
- `packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh` contains `set -euo pipefail`.
- `packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh` contains `usage: verify_prusa_arc_fitting_scope.sh`.
- `packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh` contains `ok: Prusa arc-fitting scope verification passed`.
</acceptance_criteria>
</task>

<task id="2" name="Add exact scope and field table checks">
<read_first>
- packages/prusa-arc-fitting-scope/arc-fitting-scope.md
- packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
</read_first>
<action>
Add verifier helpers equivalent to the existing G-code scope verifier:
- `require_file`
- `require_text`
- `reject_text`
- `require_section_table_row`
- `require_section_table_exact_row`
- `require_section_table_body_row_count`

Add `verify_scope_record` to require the scope table rows named in Plan 01 task 3.

Add `verify_arc_field_contract` to require exactly 12 body rows in `## Approved Arc Evidence Fields` and exact field rows for:
`source_ref`, `inventory_source_paths`, `source_anchor`, `fixture_id`, `fixture_path`, `arc_command_counts`, `arc_direction_counts`, `center_offset_observations`, `coordinate_bounds`, `extrusion_observations`, `feedrate_observations`, and `evidence_boundary`.
</action>
<acceptance_criteria>
- Verifier contains function `require_section_table_body_row_count`.
- Verifier contains function `verify_scope_record`.
- Verifier contains function `verify_arc_field_contract`.
- Verifier contains string `expected exactly 12 arc evidence field rows`.
- Verifier contains all 12 approved field names.
</acceptance_criteria>
</task>

<task id="3" name="Add traceability and status preservation checks">
<read_first>
- packages/fork-inventories/prusaslicer.tsv
- packages/fork-inventories/category-map.tsv
- packages/parity/status.tsv
- packages/prusa-arc-fitting-scope/arc-fitting-scope.md
- packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
</read_first>
<action>
Add exact constants and checks:
- `ACCEPTED_SOURCE_IDENTITY="prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961"`
- `ARC_FITTING_INVENTORY_ROW` exactly matching the current `prusaslicer.arc-fitting` row in `packages/fork-inventories/prusaslicer.tsv`
- `ARC_CATEGORY_MAP_ID="arc.shared"`
- `GCODE_OUTPUT_STATUS_ROW` exactly matching the current `fork.prusaslicer.gcode-output` row in `packages/parity/status.tsv`

Add functions:
- `require_exact_tsv_row_once`
- `require_first_field_count`
- `require_category_reference_count`
- `verify_inventory_row`
- `verify_status_boundaries`

`verify_status_boundaries` must require exactly one `generated-outputs` row with second field `in progress`, exactly one exact `fork.prusaslicer.gcode-output` row, and zero `fork.prusaslicer.arc-fitting` rows in `packages/parity/status.tsv` during Phase 57.
</action>
<acceptance_criteria>
- Verifier contains `prusaslicer.arc-fitting`.
- Verifier contains `arc.shared`.
- Verifier contains `fork.prusaslicer.gcode-output`.
- Verifier contains `fork.prusaslicer.arc-fitting`.
- Verifier contains `expected generated-outputs to remain in progress`.
- Verifier contains `no verified fork.prusaslicer.arc-fitting status row may be published in Phase 57`.
</acceptance_criteria>
</task>

<task id="4" name="Add forbidden claim and deferred-scope checks">
<read_first>
- packages/prusa-arc-fitting-scope/README.md
- packages/prusa-arc-fitting-scope/arc-fitting-scope.md
- packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
</read_first>
<action>
Add `reject_overclaiming_text` and `verify_deferred_scope_terms`.

Reject exact or regex claim patterns where Phase 57 arc-fitting scope `proves`, `verifies`, `validates`, `confirms`, `claims`, `establishes`, `demonstrates`, or `certifies` any of:
- byte-for-byte G-code parity
- broad generated-output parity
- broad generated-output verification
- full ArcWelder algorithm equivalence
- tolerance parity
- geometry parity
- printability
- printer-runtime behavior
- firmware behavior
- support generation
- wall seam behavior
- GUI behavior
- release behavior
- network/device behavior
- non-Prusa fork behavior
- Bambu Studio support
- OrcaSlicer support
- upstream source imports
- sync automation

Require deferred terms to appear in `arc-fitting-scope.md`: `Byte-for-byte G-code parity`, `broad generated-output verification`, `full ArcWelder algorithm equivalence`, `tolerance or geometry parity`, `printability`, `printer-runtime behavior`, `support generation`, `wall seam behavior`, `GUI behavior`, `release behavior`, `network/device behavior`, `non-Prusa fork behavior`, `upstream source imports`, and `sync automation`.
</action>
<acceptance_criteria>
- Verifier contains function `reject_overclaiming_text`.
- Verifier contains function `verify_deferred_scope_terms`.
- Verifier contains `full ArcWelder algorithm equivalence`.
- Verifier contains `tolerance or geometry parity`.
- Verifier contains `Bambu Studio support`.
- Verifier contains `OrcaSlicer support`.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run:
```bash
chmod +x packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
bash -n packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
bash packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
bazel run //packages/prusa-arc-fitting-scope:verify
git diff --check -- packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
```
</verification>

<success_criteria>
- The verifier passes on the checked-in valid scope.
- The verifier checks the scope record, closed field table, inventory/category traceability, status preservation, and forbidden/deferred scope language.
- The verifier does not perform network, Git, source import, generator, or runtime behavior.
</success_criteria>
