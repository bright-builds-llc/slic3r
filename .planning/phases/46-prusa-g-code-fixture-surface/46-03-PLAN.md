---
phase: 46-prusa-g-code-fixture-surface
plan: "03"
type: execute
wave: 3
depends_on: ["46-02"]
files_modified:
  - packages/parity-fixtures/README.md
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/migration-guidance.md
  - docs/port/parity-matrix.md
autonomous: true
requirements: [PGFIX-01, PGFIX-02]
requirements_addressed: [PGFIX-01, PGFIX-02]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 46-2026-06-13T16-58-19
generated_at: 2026-06-13T17:19:25Z
must_haves:
  truths:
    - "Maintainer can find the Phase 46 G-code fixture namespace and verifier from `packages/parity-fixtures/README.md`."
    - "Maintainer can find the fixture surface from `docs/port/README.md`, package ownership docs, migration guidance, and parity matrix without reading phase planning files."
    - "Maintainer can confirm docs state Rust summary parsing remains Phase 47-owned and executable parity/status publication remains Phase 48-owned."
    - "Maintainer can run final verification and confirm no `slic3r_flavors::prusa_gcode_output`, no `//packages/parity:prusaslicer_gcode_output_parity`, and no `fork.prusaslicer.gcode-output` status row exists."
  artifacts:
    - path: "packages/parity-fixtures/README.md"
      provides: "Fixture package route to G-code fixture namespace, bundle, and verifier"
      contains: "verify_prusa_gcode_output_fixture"
    - path: "docs/port/README.md"
      provides: "Current Phase 46 fixture-surface state"
      contains: "Current Prusa G-code Output Fixture Surface State"
    - path: "docs/port/package-map.md"
      provides: "Package ownership note for Phase 46 fixture files"
      contains: "prusa_gcode_output_bundle"
    - path: "docs/port/migration-guidance.md"
      provides: "Fixture update route and status gating language"
      contains: "expected-gcode-summary.tsv"
    - path: "docs/port/parity-matrix.md"
      provides: "Non-overclaiming parity interpretation for the fixture surface"
      contains: "generated-outputs"
  key_links:
    - from: "packages/parity-fixtures/README.md"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/"
      via: "namespace path"
      pattern: "prusaslicer\\.gcode-output"
    - from: "docs/port/migration-guidance.md"
      to: "packages/prusa-gcode-output-scope/gcode-output-scope.md"
      via: "reviewed scope route"
      pattern: "packages/prusa-gcode-output-scope"
    - from: "docs/port/parity-matrix.md"
      to: "packages/parity/status.tsv"
      via: "status boundary"
      pattern: "fork\\.prusaslicer\\.gcode-output"
---

<objective>
Publish the Phase 46 fixture surface through package and port documentation and
run final cross-surface verification.

Purpose: Maintainers need to find the fixture namespace, verifier, update
route, and status boundary without inferring Rust summary readiness, executable
parity, status publication, or broad generated-output parity.

Output: Updated fixture package README and port docs, plus final verification
covering fixture artifacts, Phase 45 verifier reconciliation, docs formatting,
and absence of Phase 47/48 artifacts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-prusa-g-code-fixture-surface/46-CONTEXT.md
@.planning/phases/46-prusa-g-code-fixture-surface/46-RESEARCH.md
@.planning/phases/46-prusa-g-code-fixture-surface/46-01-SUMMARY.md
@.planning/phases/46-prusa-g-code-fixture-surface/46-02-SUMMARY.md
@.planning/phases/45-prusa-g-code-output-scope-gate/45-VERIFICATION.md
@.planning/milestones/v1.11-phases/42-prusa-project-file-fixture-surface/42-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@packages/parity-fixtures/README.md
@packages/parity-fixtures/BUILD.bazel
@packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md
@packages/prusa-gcode-output-scope/gcode-output-scope.md
@docs/port/README.md
@docs/port/package-map.md
@docs/port/migration-guidance.md
@docs/port/parity-matrix.md
@packages/parity/status.tsv

Bright Builds sources informing this plan: local `AGENTS.md`,
`AGENTS.bright-builds.md`, `standards-overrides.md`, and the pinned
architecture, code-shape, verification, and testing standards cited in
`46-RESEARCH.md` at commit `05f8d7a6c9c2e157ec4f922a05273e72dab97676`.
</context>

<decision_coverage>
| Decision | Plan | Task | Coverage | Notes |
| --- | --- | --- | --- | --- |
| D-16 | 46-03 | 1, 2 | Full | Updates `packages/parity-fixtures/README.md` and relevant `docs/port/` surfaces with fixture namespace, verifier, Rust summary boundary, executable parity boundary, and status boundary. |
| D-17 | 46-03 | 1, 2 | Full | Docs continue to defer byte-for-byte G-code parity, broad generated-output parity, geometry, extrusion, timing, support, seam, arc, STEP, runtime, GUI, binary G-code, thumbnails, post-processing, host upload, network/device, profile auto-update, release, Bambu Studio, OrcaSlicer, upstream imports, and sync automation. |
| D-14 | 46-03 | 3 | Full | Final audit proves no Phase 47 Rust implementation, no Phase 48 parity target, and no status row exist. |
| D-15 | 46-03 | 3 | Full | Final verifier set remains local and hermetic. |
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Document fixture package route and verifier</name>
  <files>packages/parity-fixtures/README.md</files>
  <read_first>
    - `packages/parity-fixtures/README.md`
    - `packages/parity-fixtures/BUILD.bazel`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/fixture-provenance.tsv`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-summary.tsv`
    - `packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh`
    - `.planning/phases/46-prusa-g-code-fixture-surface/46-CONTEXT.md` decisions D-16 and D-17
  </read_first>
  <action>
    Update `packages/parity-fixtures/README.md` near the existing Prusa
    project-file fixture section and current-state list.

    Add a paragraph with these exact strings:

```text
Phase 46 adds the Prusa G-code output fixture namespace at `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/`.
It contains the checked-in `gcodewriter-set-speed.gcode` fixture, `fixture-provenance.tsv`, and `expected-gcode-summary.tsv` for the fixture surface only.
The fixture bundle target is `//packages/parity-fixtures:prusa_gcode_output_bundle`, and maintainers can verify the fixture surface with `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture`.
The selected fixture is derived from source-controlled `GCodeWriter::set_speed` expected-output literals at `tests/fff_print/test_gcodewriter.cpp#L20-L35` under `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961` because the accepted upstream tree has no checked-in `.gcode` blob.
Rust summary parsing remains Phase 47-owned, and executable parity plus the `fork.prusaslicer.gcode-output` status row remain Phase 48-owned.
```

    Add a boundary sentence with these exact strings:

```text
The G-code fixture namespace does not prove byte-for-byte G-code parity, full generated-output parity, toolpath geometry, extrusion, timing, support generation, wall seam behavior, arc fitting, STEP import, full 3MF import/export, printer-runtime behavior, firmware or printability behavior, GUI export or viewer behavior, binary G-code, thumbnails, post-processing, host upload, network/device integration, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, or sync automation.
```

    Update the current-state list with:

```text
Phase 46 adds the Prusa G-code output fixture namespace under `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/`, exported as `//packages/parity-fixtures:prusa_gcode_output_bundle` and checked by `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture`.
```
  </action>
  <acceptance_criteria>
    - `rg -n 'Phase 46 adds the Prusa G-code output fixture namespace at `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/`' packages/parity-fixtures/README.md` succeeds.
    - `rg -n 'bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture' packages/parity-fixtures/README.md` succeeds.
    - `rg -n 'accepted upstream tree has no checked-in `.gcode` blob' packages/parity-fixtures/README.md` succeeds.
    - `rg -n 'Rust summary parsing remains Phase 47-owned, and executable parity plus the `fork.prusaslicer.gcode-output` status row remain Phase 48-owned' packages/parity-fixtures/README.md` succeeds.
    - `rg -n 'does not prove byte-for-byte G-code parity, full generated-output parity' packages/parity-fixtures/README.md` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'prusaslicer\.gcode-output|verify_prusa_gcode_output_fixture|prusa_gcode_output_bundle|fork\.prusaslicer\.gcode-output' packages/parity-fixtures/README.md</automated>
    <automated>mdformat --check packages/parity-fixtures/README.md</automated>
  </verify>
  <done>`packages/parity-fixtures/README.md` exposes the fixture namespace, bundle, verifier, source-literal derivation, and Phase 47/48 boundaries without broad parity claims.</done>
</task>

<task type="auto">
  <name>Task 2: Update port docs with Phase 46 fixture handoff and status boundary</name>
  <files>docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <read_first>
    - `docs/port/README.md`
    - `docs/port/package-map.md`
    - `docs/port/migration-guidance.md`
    - `docs/port/parity-matrix.md`
    - `packages/parity-fixtures/README.md`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md`
    - `packages/prusa-gcode-output-scope/gcode-output-scope.md`
    - `packages/parity/status.tsv`
    - `.planning/phases/46-prusa-g-code-fixture-surface/46-CONTEXT.md` decisions D-16 and D-17
    - `.planning/phases/45-prusa-g-code-output-scope-gate/45-VERIFICATION.md`
  </read_first>
  <action>
    In `docs/port/README.md`, add a section titled exactly:

```markdown
## Current Prusa G-code Output Fixture Surface State
```

    Include these exact strings:

```text
Phase 46 adds the fixture namespace `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/` for `prusaslicer.gcode-output`.
The namespace contains `gcodewriter-set-speed.gcode`, `fixture-provenance.tsv`, and `expected-gcode-summary.tsv`.
Run `bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture` to verify the fixture surface.
The fixture source is derived from source-controlled `GCodeWriter::set_speed` expected-output literals at `tests/fff_print/test_gcodewriter.cpp#L20-L35` under `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`.
Rust summary parsing remains Phase 47-owned; `bazel run //packages/parity:prusaslicer_gcode_output_parity` and `fork.prusaslicer.gcode-output` remain Phase 48-owned and absent in Phase 46.
```

    Update the previous Phase 45 G-code output scope section so it no longer
    says no fixture bytes or expected summary artifacts exist without context.
    Replace that sentence with:

```text
Phase 46 now owns the fixture bytes and `expected-gcode-summary.tsv`; Phase 45 remains the reviewed scope gate that selected the path.
```

    In `docs/port/package-map.md`, update the `packages/parity-fixtures` row or
    notes to mention the G-code fixture namespace, `expected-gcode-summary.tsv`,
    `//packages/parity-fixtures:prusa_gcode_output_bundle`, and
    `//packages/parity-fixtures:verify_prusa_gcode_output_fixture`. Do not add
    `//packages/parity:prusaslicer_gcode_output_parity` as an owned command.

    In `docs/port/migration-guidance.md`, update the existing
    `prusaslicer.gcode-output` fixture bullet so it says Phase 46 has created
    `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/`,
    `gcodewriter-set-speed.gcode`, and `expected-gcode-summary.tsv`, and that
    updates require reviewed intake changes to `packages/fork-vendors/forks.tsv`,
    `packages/fork-inventories/prusaslicer.tsv`, and
    `packages/prusa-gcode-output-scope/gcode-output-scope.md`.

    In `docs/port/migration-guidance.md`, preserve the exact status boundary:

```text
`fork.prusaslicer.gcode-output` is not verified or published in Phase 46.
The reserved Phase 48 evidence command is `bazel run //packages/parity:prusaslicer_gcode_output_parity`, and the row may be published only after executable summary-only evidence passes.
```

    In `docs/port/parity-matrix.md`, update the G-code scope bullet to state
    Phase 46 fixture artifacts now exist and are verified by the fixture
    verifier, while broad `generated-outputs` remains `in progress` and
    `fork.prusaslicer.gcode-output` remains absent from `packages/parity/status.tsv`.

    All four docs must keep these deferrals visible:
    byte-for-byte G-code parity, full generated-output parity, toolpath
    geometry, extrusion, timing, support generation, wall seam behavior, arc
    fitting, STEP import, full 3MF import/export, printer-runtime behavior,
    firmware or printability behavior, GUI export or viewer behavior, binary
    G-code, thumbnails, post-processing, host upload, network/device
    integration, profile auto-update execution, fork release builds, Bambu
    Studio, OrcaSlicer, upstream source imports, and sync automation.
  </action>
  <acceptance_criteria>
    - `rg -n '## Current Prusa G-code Output Fixture Surface State' docs/port/README.md` succeeds.
    - `rg -n 'bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture' docs/port/README.md docs/port/package-map.md` succeeds.
    - `rg -n 'Phase 46 now owns the fixture bytes and `expected-gcode-summary.tsv`; Phase 45 remains the reviewed scope gate that selected the path\.' docs/port/README.md` succeeds.
    - `rg -n 'prusa_gcode_output_bundle' docs/port/package-map.md` succeeds.
    - `rg -n '`fork\.prusaslicer\.gcode-output` is not verified or published in Phase 46' docs/port/migration-guidance.md` succeeds.
    - `rg -n 'broad `generated-outputs` remains `in progress`' docs/port/parity-matrix.md` succeeds.
    - `awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv` succeeds.
    - `! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'prusaslicer\.gcode-output|expected-gcode-summary\.tsv|verify_prusa_gcode_output_fixture|fork\.prusaslicer\.gcode-output|generated-outputs' docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md</automated>
    <automated>awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv</automated>
    <automated>! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv</automated>
    <automated>mdformat --check docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md</automated>
  </verify>
  <done>Port docs expose the Phase 46 fixture surface and verifier while preserving Phase 47/48 ownership and non-overclaiming status boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Run final fixture, docs, and absence verification</name>
  <files>packages/parity-fixtures/README.md, docs/port/README.md, docs/port/package-map.md, docs/port/migration-guidance.md, docs/port/parity-matrix.md</files>
  <read_first>
    - `packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh`
    - `packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh`
    - `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh`
    - `packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh`
    - `packages/parity-fixtures/README.md`
    - `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md`
    - `docs/port/README.md`
    - `docs/port/package-map.md`
    - `docs/port/migration-guidance.md`
    - `docs/port/parity-matrix.md`
    - `packages/parity/status.tsv`
    - `packages/parity/BUILD.bazel`
  </read_first>
  <action>
    Run the final Phase 46 validation set after docs are updated. Fix only
    Phase 46-owned docs, verifier, or Bazel wiring if a check fails.

    Required commands:

```bash
bazel query //packages/parity-fixtures:prusa_gcode_output_bundle
bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture
bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test
bash packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh
bazel run //packages/prusa-gcode-output-scope:verify
bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test
shfmt -d packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
shellcheck packages/parity-fixtures/verify_prusa_gcode_output_fixture.sh packages/parity-fixtures/verify_prusa_gcode_output_fixture_test.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope_test.sh
mdformat --check packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md
! bazel query //packages/parity:prusaslicer_gcode_output_parity
! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv
! rg -n 'pub mod prusa_gcode_output|prusa_gcode_output_summary|parse_prusa_gcode_output_summary' packages/slic3r-rust
git diff --check
```

    Do not run `mdformat` on any phase `*-SUMMARY.md` file per repo-local
    `AGENTS.md` guidance.
  </action>
  <acceptance_criteria>
    - All commands listed in the action pass.
    - `rg -n 'verify_prusa_gcode_output_fixture' packages/parity-fixtures/README.md docs/port/README.md docs/port/package-map.md` succeeds.
    - `rg -n 'not verified or published in Phase 46' docs/port/migration-guidance.md` succeeds.
    - `! bazel query //packages/parity:prusaslicer_gcode_output_parity` succeeds.
    - `! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv` succeeds.
    - `! rg -n 'pub mod prusa_gcode_output|prusa_gcode_output_summary|parse_prusa_gcode_output_summary' packages/slic3r-rust` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_gcode_output_fixture</automated>
    <automated>bazel test //packages/parity-fixtures:verify_prusa_gcode_output_fixture_test</automated>
    <automated>bazel run //packages/prusa-gcode-output-scope:verify</automated>
    <automated>bazel test //packages/prusa-gcode-output-scope:verify_prusa_gcode_output_scope_test</automated>
    <automated>mdformat --check packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/README.md docs/port/README.md docs/port/package-map.md docs/port/migration-guidance.md docs/port/parity-matrix.md</automated>
    <automated>! bazel query //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>! rg -n '^fork\.prusaslicer\.gcode-output\t' packages/parity/status.tsv</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Final verification confirms Phase 46 fixture artifacts, verifier behavior, docs, and later-phase absence boundaries are coherent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| docs -> maintainer interpretation | Docs can overstate fixture evidence as Rust summary, executable parity, status publication, or generated-output parity. |
| fixture docs -> parity status | Docs name `fork.prusaslicer.gcode-output` but the status row must remain absent until Phase 48. |
| verifier/docs agreement | Package docs and verifier must route to the same fixture namespace and command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-46-03-01 | Spoofing | `packages/parity-fixtures/README.md` | mitigate | Name exact namespace, bundle target, verifier command, source-literal derivation, and Phase 47/48 ownership. |
| T-46-03-02 | Repudiation | `docs/port/README.md` | mitigate | Add a current Phase 46 section and replace stale Phase 45 absence wording with scope-gate handoff wording. |
| T-46-03-03 | Tampering | `docs/port/migration-guidance.md` | mitigate | Preserve exact statement that `fork.prusaslicer.gcode-output` is not verified or published in Phase 46. |
| T-46-03-04 | Tampering | `packages/parity/status.tsv` | mitigate | Final audit rejects `fork.prusaslicer.gcode-output` rows and confirms `generated-outputs` remains `in progress`. |
| T-46-03-05 | Information Disclosure | generated-output/runtime claims | mitigate | Docs repeat exclusions for post-processing, host upload, network/device, profile auto-update, upstream imports, and sync automation. |
| T-46-03-06 | Elevation of Privilege | executable evidence boundary | mitigate | Final audit rejects `//packages/parity:prusaslicer_gcode_output_parity` and Rust `prusa_gcode_output` implementation markers. |
</threat_model>

<verification>
Run the full final validation set from Task 3, including fixture verifier,
mutation tests, Phase 45 scope verifier, Markdown/shell checks, and the
Rust/parity/status absence audit.
</verification>

<success_criteria>
PGFIX-01 and PGFIX-02 are publicly discoverable and verifiable: maintainers can
inspect the fixture namespace, run the fixture verifier, read docs that explain
the exact boundary, and confirm Phase 47/48 artifacts remain absent.
</success_criteria>

<output>
After completion, create `.planning/phases/46-prusa-g-code-fixture-surface/46-03-SUMMARY.md`
</output>
