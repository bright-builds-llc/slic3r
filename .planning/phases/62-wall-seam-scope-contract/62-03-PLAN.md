---
phase: 62-wall-seam-scope-contract
plan: "03"
type: execute
wave: 3
depends_on:
  - "62-02"
files_modified:
  - packages/prusa-wall-seam-scope/BUILD.bazel
  - packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh
autonomous: true
requirements_addressed: [SEAMSCOPE-01, SEAMSCOPE-02, SEAMSCOPE-03]
requirements:
  - SEAMSCOPE-01
  - SEAMSCOPE-02
  - SEAMSCOPE-03
must_haves:
  truths:
    - "Maintainer can run Bash and Bazel verification for the wall-seam scope package."
    - "Mutation tests prove unsupported seam fields, missing fields, traceability drift, generated-output promotion, existing status-row drift, premature wall-seam status publication, forbidden claims, and missing deferred-scope wording fail closed."
    - "The phase remains limited to scope and verifier artifacts."
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 62-2026-06-26T23-04-21
generated_at: 2026-06-26T23:18:44.232Z
---

<objective>
Add the mutation suite and final Bazel verification wiring for the Phase 62 scope package.

Purpose: Prove the wall-seam scope verifier fails closed for the required drift and overclaim classes, then expose the suite through Bazel.
Output: `verify_prusa_wall_seam_scope_test.sh`, updated `BUILD.bazel`, and passing Bash/Bazel verification.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/62-wall-seam-scope-contract/62-CONTEXT.md
@.planning/phases/62-wall-seam-scope-contract/62-RESEARCH.md
@.planning/phases/62-wall-seam-scope-contract/62-01-SUMMARY.md
@.planning/phases/62-wall-seam-scope-contract/62-02-SUMMARY.md
@packages/prusa-wall-seam-scope/BUILD.bazel
@packages/prusa-wall-seam-scope/README.md
@packages/prusa-wall-seam-scope/wall-seam-scope.md
@packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
@packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh
</context>

<threat_model>
The mutation suite creates temporary local copies only. It must not mutate repo sources outside its temp directory, run network/Git/upstream import commands, or depend on external services.

Primary risk: tests prove file existence but not fail-closed behavior. Mitigation: each mutation must assert the verifier exits nonzero and stderr contains a specific diagnostic fragment.
</threat_model>

<tasks>
<task id="1" name="Write mutation test harness">
<read_first>
- packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh
- packages/prusa-wall-seam-scope/README.md
- packages/prusa-wall-seam-scope/wall-seam-scope.md
- packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
</read_first>
<action>
Create executable `packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh` with:
- shebang `#!/usr/bin/env bash`
- `set -euo pipefail`
- verifier path resolution that uses Bazel `TEST_SRCDIR`/`TEST_WORKSPACE` when present and package-local script otherwise
- temp directory created by `mktemp -d "${TMPDIR:-/tmp}/verify-prusa-wall-seam-scope-test.XXXXXX"`
- `trap 'rm -rf "${tmp_dir}"' EXIT`
- helper functions `fail`, `assert_contains`, `remove_line_containing`, `replace_first_line_containing`, `replace_text`, `insert_line_before`, `write_valid_fixture`, and `run_verifier`
- final output `ok: verify_prusa_wall_seam_scope_test`

`write_valid_fixture` must create a self-contained temp fixture with valid copies of README, wall-seam scope, inventory TSV, category-map TSV, and parity status TSV.
</action>
<acceptance_criteria>
- Test file contains `verify-prusa-wall-seam-scope-test`.
- Test file contains `write_valid_fixture`.
- Test file contains `run_verifier`.
- Test file contains `ok: verify_prusa_wall_seam_scope_test`.
- Test file is executable.
</acceptance_criteria>
</task>

<task id="2" name="Add required mutation cases">
<read_first>
- packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
- packages/prusa-wall-seam-scope/wall-seam-scope.md
- packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope_test.sh
</read_first>
<action>
Add mutation test functions and invoke all of them at the bottom:
- `test_valid_fixture_passes`
- `test_missing_required_scope_row_fails`
- `test_missing_wall_seam_field_fails`
- `test_unsupported_wall_seam_field_fails`
- `test_duplicate_wall_seam_field_fails`
- `test_inventory_row_drift_fails`
- `test_duplicate_inventory_row_fails`
- `test_missing_seam_category_reference_fails`
- `test_generated_outputs_promotion_fails`
- `test_gcode_output_status_drift_fails`
- `test_arc_fitting_status_drift_fails`
- `test_premature_wall_seam_status_publication_fails`
- `test_missing_deferred_scope_term_fails`
- `test_runtime_overclaim_fails`
- `test_printability_overclaim_fails`
- `test_seam_visibility_overclaim_fails`
- `test_algorithm_equivalence_overclaim_fails`

Each negative test must:
1. call `write_valid_fixture`
2. mutate only files under that temp fixture
3. run the verifier
4. fail if the verifier exits 0
5. assert stderr contains `^error:` and a specific diagnostic substring from the verifier
</action>
<acceptance_criteria>
- Test file contains `test_valid_fixture_passes`.
- Test file contains `test_unsupported_wall_seam_field_fails`.
- Test file contains `test_generated_outputs_promotion_fails`.
- Test file contains `test_arc_fitting_status_drift_fails`.
- Test file contains `test_premature_wall_seam_status_publication_fails`.
- Test file contains `test_seam_visibility_overclaim_fails`.
- Test file invokes `test_missing_deferred_scope_term_fails`.
</acceptance_criteria>
</task>

<task id="3" name="Wire Bazel test target and package boundary">
<read_first>
- packages/prusa-wall-seam-scope/BUILD.bazel
- packages/prusa-arc-fitting-scope/BUILD.bazel
</read_first>
<action>
Update `packages/prusa-wall-seam-scope/BUILD.bazel` to include:
```python
sh_test(
    name = "verify_prusa_wall_seam_scope_test",
    srcs = ["verify_prusa_wall_seam_scope_test.sh"],
    data = ["verify_prusa_wall_seam_scope.sh"],
)
```

Update `filegroup(name = "package_boundary", ...)` so it includes:
- `README.md`
- `wall-seam-scope.md`
- `verify_prusa_wall_seam_scope.sh`
- `verify_prusa_wall_seam_scope_test.sh`
</action>
<acceptance_criteria>
- `packages/prusa-wall-seam-scope/BUILD.bazel` contains `sh_test(`.
- `packages/prusa-wall-seam-scope/BUILD.bazel` contains `name = "verify_prusa_wall_seam_scope_test"`.
- `packages/prusa-wall-seam-scope/BUILD.bazel` contains `verify_prusa_wall_seam_scope_test.sh`.
- `packages/prusa-wall-seam-scope/BUILD.bazel` package boundary contains `verify_prusa_wall_seam_scope_test.sh`.
</acceptance_criteria>
</task>

<task id="4" name="Run final validation and record exact commands">
<read_first>
- packages/prusa-wall-seam-scope/BUILD.bazel
- packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
- packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh
</read_first>
<action>
Run final validation commands and fix any failures:
```bash
bash -n packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
bash -n packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh
bash packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
bash packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh
bazel run //packages/prusa-wall-seam-scope:verify
bazel test //packages/prusa-wall-seam-scope:verify_prusa_wall_seam_scope_test
bazel run //packages/fork-inventories:verify
bazel run //packages/prusa-gcode-output-scope:verify
bazel run //packages/prusa-arc-fitting-scope:verify
git diff --check -- packages/prusa-wall-seam-scope .planning/phases/62-wall-seam-scope-contract
```
Record the commands and pass/fail results in the plan SUMMARY.md.
</action>
<acceptance_criteria>
- `bash packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh` exits 0.
- `bash packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh` exits 0.
- `bazel run //packages/prusa-wall-seam-scope:verify` exits 0.
- `bazel test //packages/prusa-wall-seam-scope:verify_prusa_wall_seam_scope_test` exits 0.
- `bazel run //packages/fork-inventories:verify` exits 0.
- `bazel run //packages/prusa-gcode-output-scope:verify` exits 0.
- `bazel run //packages/prusa-arc-fitting-scope:verify` exits 0.
- `git diff --check -- packages/prusa-wall-seam-scope .planning/phases/62-wall-seam-scope-contract` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run the same command list from task 4. Do not mark this plan complete until all listed commands pass or a clear environment blocker is recorded.
</verification>

<success_criteria>
- Bash and Bazel validation pass for the new scope package.
- Mutation tests prove the required fail-closed behaviors.
- Phase 62 has all artifacts needed for phase-level verification.
</success_criteria>
