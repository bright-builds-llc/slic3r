---
phase: 62-wall-seam-scope-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - "62-01"
files_modified:
  - packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
autonomous: true
requirements_addressed: [SEAMSCOPE-02, SEAMSCOPE-03]
requirements:
  - SEAMSCOPE-02
  - SEAMSCOPE-03
must_haves:
  truths:
    - "Maintainer can run the wall-seam scope verifier and see scope drift fail closed."
    - "The verifier rejects unsupported seam fields, missing or duplicate rows, traceability drift, generated-output status promotion, G-code output status drift, arc-fitting status drift, premature wall-seam status publication, runtime/printability claims, and missing deferred-scope wording."
    - "The verifier performs only local file reads and text checks."
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 62-2026-06-26T23-04-21
generated_at: 2026-06-26T23:18:44.232Z
---

<objective>
Implement the fail-closed verifier for the Phase 62 wall-seam scope contract.

Purpose: Make the contract executable so unsupported seam fields, missing traceability, broad claims, existing status drift, and premature wall-seam publication cannot pass silently.
Output: `packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/62-wall-seam-scope-contract/62-CONTEXT.md
@.planning/phases/62-wall-seam-scope-contract/62-RESEARCH.md
@.planning/phases/62-wall-seam-scope-contract/62-01-SUMMARY.md
@packages/prusa-wall-seam-scope/README.md
@packages/prusa-wall-seam-scope/wall-seam-scope.md
@packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
@packages/prusa-gcode-output-scope/verify_prusa_gcode_output_scope.sh
@packages/fork-inventories/prusaslicer.tsv
@packages/fork-inventories/category-map.tsv
@packages/parity/status.tsv
</context>

<threat_model>
This verifier must not run external tools, network calls, Git commands, source imports, or runtime generation. It should read only the files passed as arguments or package-local defaults.

Primary risk: false-positive verification from loose text matching. Mitigation: use exact rows, exact row counts, first-field duplicate checks, category-map reference counts, zero-row premature status checks, and explicit forbidden-claim rejection.
</threat_model>

<tasks>
<task id="1" name="Create verifier entrypoint and file argument handling">
<read_first>
- packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
- packages/prusa-wall-seam-scope/BUILD.bazel
- packages/prusa-wall-seam-scope/README.md
- packages/prusa-wall-seam-scope/wall-seam-scope.md
</read_first>
<action>
Create executable `packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh` with:
- shebang `#!/usr/bin/env bash`
- `set -euo pipefail`
- `error()` helper that prints `error: ...` to stderr and exits 1
- package-local default paths for `README.md`, `wall-seam-scope.md`, `packages/fork-inventories/prusaslicer.tsv`, `packages/fork-inventories/category-map.tsv`, and `packages/parity/status.tsv`
- support for exactly 0 args, 5 args (`README scope inventory category-map status`), or 6 args (same plus checkout root)
- usage error text `usage: verify_prusa_wall_seam_scope.sh [README wall-seam-scope inventory category-map parity-status [checkout-root]]`
- final success output `ok: Prusa wall-seam scope verification passed`
</action>
<acceptance_criteria>
- `packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh` starts with `#!/usr/bin/env bash`.
- `packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh` contains `set -euo pipefail`.
- `packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh` contains `usage: verify_prusa_wall_seam_scope.sh`.
- `packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh` contains `ok: Prusa wall-seam scope verification passed`.
</acceptance_criteria>
</task>

<task id="2" name="Add exact scope and field table checks">
<read_first>
- packages/prusa-wall-seam-scope/wall-seam-scope.md
- packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
</read_first>
<action>
Add verifier helpers:
- `require_file`
- `require_text`
- `reject_text`
- `require_section_table_row`
- `require_section_table_exact_row`
- `require_section_table_body_row_count`

Add `verify_scope_record` to require the scope table rows named in Plan 01 task 3.

Add `verify_wall_seam_field_contract` to require exactly 12 body rows in `## Approved Wall-Seam Evidence Fields` and exact field rows for:
`source_ref`, `inventory_source_paths`, `source_anchor`, `fixture_id`, `fixture_path`, `seam_transition_observations`, `layer_context_observations`, `travel_context_observations`, `coordinate_bounds`, `extrusion_observations`, `retraction_observations`, and `evidence_boundary`.
</action>
<acceptance_criteria>
- Verifier contains function `require_section_table_body_row_count`.
- Verifier contains function `verify_scope_record`.
- Verifier contains function `verify_wall_seam_field_contract`.
- Verifier contains string `expected exactly 12 wall-seam evidence field rows`.
- Verifier contains all 12 approved field names.
</acceptance_criteria>
</task>

<task id="3" name="Add traceability and status preservation checks">
<read_first>
- packages/fork-inventories/prusaslicer.tsv
- packages/fork-inventories/category-map.tsv
- packages/parity/status.tsv
- packages/prusa-wall-seam-scope/wall-seam-scope.md
- packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
</read_first>
<action>
Add exact constants and checks:
- `ACCEPTED_SOURCE_IDENTITY="prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961"`
- `WALL_SEAM_INVENTORY_ROW` exactly matching the current `prusaslicer.wall-seam` row in `packages/fork-inventories/prusaslicer.tsv`
- `SEAM_CATEGORY_MAP_ID="seam.shared"`
- `GCODE_OUTPUT_STATUS_ROW` exactly matching the current `fork.prusaslicer.gcode-output` row in `packages/parity/status.tsv`
- `ARC_FITTING_STATUS_ROW` exactly matching the current `fork.prusaslicer.arc-fitting` row in `packages/parity/status.tsv`

Add functions:
- `require_exact_tsv_row_once`
- `require_first_field_count`
- `require_category_reference_count`
- `verify_inventory_row`
- `verify_status_boundaries`

`verify_status_boundaries` must require exactly one `generated-outputs` row with second field `in progress`, exactly one exact `fork.prusaslicer.gcode-output` row, exactly one exact `fork.prusaslicer.arc-fitting` row, and zero `fork.prusaslicer.wall-seam` rows in `packages/parity/status.tsv` during Phase 62.
</action>
<acceptance_criteria>
- Verifier contains `prusaslicer.wall-seam`.
- Verifier contains `seam.shared`.
- Verifier contains `fork.prusaslicer.gcode-output`.
- Verifier contains `fork.prusaslicer.arc-fitting`.
- Verifier contains `fork.prusaslicer.wall-seam`.
- Verifier contains `no verified fork.prusaslicer.wall-seam status row may be published in Phase 62`.
</acceptance_criteria>
</task>

<task id="4" name="Add forbidden claim and deferred-scope checks">
<read_first>
- packages/prusa-wall-seam-scope/README.md
- packages/prusa-wall-seam-scope/wall-seam-scope.md
- packages/prusa-arc-fitting-scope/verify_prusa_arc_fitting_scope.sh
</read_first>
<action>
Add `reject_overclaiming_text` and `verify_deferred_scope_terms`.

Reject exact or regex claim patterns where Phase 62 wall-seam scope `proves`, `verifies`, `validates`, `confirms`, `claims`, `establishes`, `demonstrates`, or `certifies` any of:
- byte-for-byte G-code parity
- broad generated-output parity
- broad generated-output verification
- full wall-seam algorithm equivalence
- wall-seam geometry equivalence
- seam visibility
- printability
- printer-runtime behavior
- firmware behavior
- support generation
- GUI behavior
- release behavior
- network/device behavior
- non-Prusa fork behavior
- Bambu Studio support
- OrcaSlicer support
- upstream source imports
- sync automation

Require deferred terms to appear in `wall-seam-scope.md`: `Byte-for-byte G-code parity`, `broad generated-output verification`, `full wall-seam algorithm or geometry equivalence`, `seam visibility`, `printability`, `printer-runtime behavior`, `support generation`, `GUI behavior`, `release behavior`, `network/device behavior`, `non-Prusa fork behavior`, `upstream source imports`, and `sync automation`.
</action>
<acceptance_criteria>
- Verifier contains function `reject_overclaiming_text`.
- Verifier contains function `verify_deferred_scope_terms`.
- Verifier contains `full wall-seam algorithm equivalence`.
- Verifier contains `wall-seam geometry equivalence`.
- Verifier contains `seam visibility`.
- Verifier contains `Bambu Studio support`.
- Verifier contains `OrcaSlicer support`.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run:
```bash
chmod +x packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
bash -n packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
bash packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
bazel run //packages/prusa-wall-seam-scope:verify
git diff --check -- packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
```
</verification>

<success_criteria>
- The verifier passes on the checked-in valid scope.
- The verifier checks the scope record, closed field table, inventory/category traceability, status preservation, premature wall-seam status publication, and forbidden/deferred scope language.
- The verifier does not perform network, Git, source import, generator, or runtime behavior.
</success_criteria>
