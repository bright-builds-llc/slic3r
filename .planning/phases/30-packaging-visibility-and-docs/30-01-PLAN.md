---
phase: 30-packaging-visibility-and-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/parity/status.tsv
  - packages/parity/README.md
  - packages/parity-fixtures/README.md
  - packages/parity-fixtures/linux-packaged-launcher/README.md
  - packages/parity-fixtures/windows-packaged-launcher/README.md
  - docs/port/parity-matrix.md
  - docs/port/contract-inventory.md
  - docs/port/migration-guidance.md
  - docs/port/linux-launcher-slice.md
  - docs/port/windows-launcher-slice.md
  - docs/port/entrypoint-architecture.md
  - docs/port/package-map.md
  - docs/port/README.md
  - packages/launcher/README.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PVIS-01
  - PVIS-02
  - PVIS-03
user_setup: []
must_haves:
  truths:
    - "Maintainer can run bazel run //packages/parity:status and see linux.packaged-launcher plus windows.packaged-launcher as verified rows."
    - "Maintainer can trace linux.packaged-launcher to //packages/parity:linux_packaged_launcher_parity and windows.packaged-launcher to //packages/parity:windows_packaged_launcher_parity."
    - "Migration, launcher, package, parity, and fixture docs describe scoped Linux and Windows package-shaped launcher evidence without claiming signing, installers, AppImage, MSI, DMG, GUI packaging, release archives, native/cross-compiled release binaries, broad bundled dependency layout, downstream fork work, or release channels."
    - "The v1.7 traceability tables map all 12 active requirements to exactly one phase: LPKG-01..03 Phase 27, WPKG-01..03 Phase 28, PKGE-01..03 Phase 29, PVIS-01..03 Phase 30."
    - "Phase 30 summary guidance claims only PVIS-01, PVIS-02, and PVIS-03 in requirements-completed."
  artifacts:
    - path: "packages/parity/status.tsv"
      provides: "Checked-in parity status source with Linux and Windows packaged launcher rows"
      contains: "linux.packaged-launcher"
    - path: "packages/parity/status.tsv"
      provides: "Checked-in parity status source with Windows packaged launcher row"
      contains: "windows.packaged-launcher"
    - path: "packages/parity/README.md"
      provides: "Parity command discoverability without stale status-deferred wording"
      contains: "bazel run //packages/parity:linux_packaged_launcher_parity"
    - path: "docs/port/parity-matrix.md"
      provides: "High-level scoped packaging-visible validation state"
      contains: "linux.packaged-launcher"
    - path: "docs/port/contract-inventory.md"
      provides: "Detailed contract inventory for scoped Linux and Windows packaged launcher evidence"
      contains: "//packages/parity:windows_packaged_launcher_parity"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap traceability table with exactly one phase per v1.7 requirement"
      contains: "PVIS-03 | Phase 30"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement traceability table with exactly one phase per v1.7 requirement"
      contains: "PVIS-03 | Phase 30"
  key_links:
    - from: "packages/parity/status.tsv"
      to: "//packages/parity:linux_packaged_launcher_parity"
      via: "evidence column for linux.packaged-launcher"
      pattern: "^linux\\.packaged-launcher\\tverified\\t//packages/parity:linux_packaged_launcher_parity\\t"
    - from: "packages/parity/status.tsv"
      to: "//packages/parity:windows_packaged_launcher_parity"
      via: "evidence column for windows.packaged-launcher"
      pattern: "^windows\\.packaged-launcher\\tverified\\t//packages/parity:windows_packaged_launcher_parity\\t"
    - from: "docs/port/parity-matrix.md"
      to: "packages/parity/status.tsv"
      via: "status row references"
      pattern: "linux\\.packaged-launcher|windows\\.packaged-launcher"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "matching v1.7 traceability tables"
      pattern: "LPKG-01.*Phase 27|PVIS-03.*Phase 30"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 30-2026-05-23T13-19-23
generated_at: 2026-05-23T13:39:45Z
---

<objective>
Publish Phase 29 Linux and Windows packaged launcher evidence through checked-in status and existing documentation surfaces.

Purpose: Close v1.7 by making the verified scoped packaged launcher state visible without expanding runtime behavior or overclaiming release-grade packaging.
Output: Updated status rows, narrowed docs, traceability verification, and a Phase 30 summary that claims only PVIS-01, PVIS-02, and PVIS-03.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-packaging-visibility-and-docs/30-CONTEXT.md
@.planning/phases/30-packaging-visibility-and-docs/30-RESEARCH.md
@.planning/phases/29-cross-platform-packaging-evidence/29-01-SUMMARY.md
@packages/parity/status.tsv
@packages/parity/BUILD.bazel
@packages/parity/README.md
@docs/port/parity-matrix.md
@docs/port/contract-inventory.md
@docs/port/migration-guidance.md
@docs/port/linux-launcher-slice.md
@docs/port/windows-launcher-slice.md
@docs/port/entrypoint-architecture.md
@docs/port/package-map.md
@docs/port/README.md
@packages/launcher/README.md
@packages/parity-fixtures/README.md
@packages/parity-fixtures/linux-packaged-launcher/README.md
@packages/parity-fixtures/windows-packaged-launcher/README.md

Bright Builds context used for this plan: local AGENTS guidance, AGENTS.bright-builds.md, standards-overrides.md, and the canonical Verification standard. Local repo guidance forbids running mdformat on phase *-SUMMARY.md files and requires the exact requirements-completed summary key.

<interfaces>
Status TSV schema from packages/parity/status.tsv:
```text
surface<TAB>status<TAB>evidence<TAB>notes
```

Authoritative Phase 29 Bazel parity labels from packages/parity/BUILD.bazel:
```text
//packages/parity:linux_packaged_launcher_parity
//packages/parity:windows_packaged_launcher_parity
```

Required v1.7 traceability map:
```text
LPKG-01, LPKG-02, LPKG-03 -> Phase 27
WPKG-01, WPKG-02, WPKG-03 -> Phase 28
PKGE-01, PKGE-02, PKGE-03 -> Phase 29
PVIS-01, PVIS-02, PVIS-03 -> Phase 30
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish Linux and Windows packaged launcher status rows</name>
  <read_first>
    - packages/parity/status.tsv
    - packages/parity/parity_status.sh
    - packages/parity/BUILD.bazel
    - packages/parity/README.md
    - packages/parity-fixtures/README.md
    - packages/parity-fixtures/linux-packaged-launcher/README.md
    - packages/parity-fixtures/windows-packaged-launcher/README.md
    - .planning/phases/30-packaging-visibility-and-docs/30-CONTEXT.md
    - .planning/phases/30-packaging-visibility-and-docs/30-RESEARCH.md
  </read_first>
  <files>packages/parity/status.tsv, packages/parity/README.md, packages/parity-fixtures/README.md, packages/parity-fixtures/linux-packaged-launcher/README.md, packages/parity-fixtures/windows-packaged-launcher/README.md</files>
  <action>Add exactly two checked-in status rows per D-01 and D-02, without changing packages/parity/parity_status.sh per D-03. The rows must use tab separators and these exact surface/evidence values: linux.packaged-launcher -> verified -> //packages/parity:linux_packaged_launcher_parity; windows.packaged-launcher -> verified -> //packages/parity:windows_packaged_launcher_parity. Keep the existing launcher-packaging row macOS-scoped per D-04. Replace the packages/parity/README.md sentence "Linux and Windows packaged launcher parity now has runnable shared evidence; status publication remains deferred to the packaging visibility phase." with wording that says Linux and Windows packaged launcher parity now has runnable shared evidence and checked-in status rows. Update packages/parity-fixtures/README.md to list Phase 29 Linux and Windows packaged launcher fixture bundles. In both platform fixture README files, add the matching bazel run //packages/parity:*_packaged_launcher_parity command and keep the Deferred lists scoped to release/package formats only. Do not add new parity targets, new comparator code, or a new documentation hub.</action>
  <verify>
    <automated>awk -F '\t' 'BEGIN { found = 0 } $1 == "linux.packaged-launcher" && $2 == "verified" && $3 == "//packages/parity:linux_packaged_launcher_parity" { found++ } $1 == "windows.packaged-launcher" && $2 == "verified" && $3 == "//packages/parity:windows_packaged_launcher_parity" { found++ } END { exit(found == 2 ? 0 : 1) }' packages/parity/status.tsv</automated>
    <automated>bazel run //packages/parity:status</automated>
  </verify>
  <acceptance_criteria>
    - packages/parity/status.tsv contains one linux.packaged-launcher row and one windows.packaged-launcher row, both status verified, both pointing to the exact Phase 29 evidence targets.
    - packages/parity/status.tsv keeps launcher-packaging scoped to //packages/parity:macos_packaged_launcher_parity and does not replace it with an aggregate all-platform row.
    - packages/parity/README.md no longer says Linux or Windows packaged launcher status publication is deferred.
    - Fixture docs name the Linux and Windows packaged launcher parity commands and do not claim AppImage, MSI, DMG, signing, installers, GUI packaging, release archives, native/cross-compiled release binaries, broad dependency bundling, downstream fork work, or release channels.
  </acceptance_criteria>
  <done>PVIS-01 is materially represented in the checked-in parity status data, and fixture/parity package docs point readers to the same evidence labels.</done>
</task>

<task type="auto">
  <name>Task 2: Replace stale deferred docs with scoped verified packaging evidence</name>
  <read_first>
    - docs/port/parity-matrix.md
    - docs/port/contract-inventory.md
    - docs/port/migration-guidance.md
    - docs/port/linux-launcher-slice.md
    - docs/port/windows-launcher-slice.md
    - docs/port/entrypoint-architecture.md
    - docs/port/package-map.md
    - docs/port/README.md
    - packages/launcher/README.md
    - packages/parity/status.tsv
    - packages/parity/README.md
  </read_first>
  <files>docs/port/parity-matrix.md, docs/port/contract-inventory.md, docs/port/migration-guidance.md, docs/port/linux-launcher-slice.md, docs/port/windows-launcher-slice.md, docs/port/entrypoint-architecture.md, docs/port/package-map.md, docs/port/README.md, packages/launcher/README.md</files>
  <action>Run the scoped publication pass required by D-05 through D-08. In docs/port/parity-matrix.md, update the Linux runtime, Windows runtime, Launcher path, and Packaging-visible behavior notes so they no longer say Linux or Windows packaged launcher parity/evidence is deferred; the Packaging-visible behavior row must mention linux.packaged-launcher, windows.packaged-launcher, //packages/parity:linux_packaged_launcher_parity, and //packages/parity:windows_packaged_launcher_parity. In docs/port/contract-inventory.md, update the launcher and packaging-visible rows so Linux and Windows scoped package-shaped launcher trees are verified through the Phase 29 parity targets while AppImage, distro packages, MSI, DMG, installers, signing, GUI packaging, release archives, native/cross-compiled release binaries, broad bundled dependency layout, and release channels remain deferred. In docs/port/migration-guidance.md, replace the Scope Now vs Deferred bullets that still treat Linux and Windows packaging-visible parity as future work with current scoped support for package-shaped launcher trees plus deferred release-grade package formats. In docs/port/linux-launcher-slice.md and docs/port/windows-launcher-slice.md, add the matching bazel run //packages/parity:*_packaged_launcher_parity commands under Supported Now and update Verification Status to say Phase 29 shared packaged launcher evidence exists. In docs/port/entrypoint-architecture.md, update the Phase 27 and Phase 28 sections so they no longer say shared packaged parity evidence remains later work, and add a narrow Phase 29 note if that is the clearest way to name both evidence commands. In docs/port/package-map.md, update package roles and phase notes so packages/parity and packages/parity-fixtures include Linux and Windows scoped packaged launcher evidence and fixtures; add Phase 28/29/30 notes if missing. In docs/port/README.md and packages/launcher/README.md, add the current Linux and Windows packaged launcher evidence/status facts without creating a new cross-platform docs hub. Use the phrases "scoped packaged launcher tree" or "package-shaped launcher tree" when describing verified Linux/Windows support, and keep every broader package/release surface explicitly deferred.</action>
  <verify>
    <automated>if rg -n "status publication remains deferred|Shared (Linux|Windows|cross-platform|packaged).*parity evidence remains (later work|Phase 29 scope)|Linux packaging parity remains deferred|Windows packaging-visible behavior remains deferred|Windows implementation deferred to a later milestone|Windows parity is outside the current milestone" docs/port packages/launcher/README.md packages/parity/README.md packages/parity-fixtures; then exit 1; fi</automated>
    <automated>rg -n "linux\.packaged-launcher|windows\.packaged-launcher|//packages/parity:linux_packaged_launcher_parity|//packages/parity:windows_packaged_launcher_parity" docs/port/parity-matrix.md docs/port/contract-inventory.md docs/port/linux-launcher-slice.md docs/port/windows-launcher-slice.md docs/port/package-map.md docs/port/README.md packages/launcher/README.md</automated>
  </verify>
  <acceptance_criteria>
    - Docs describe verified Linux and Windows support only as scoped/package-shaped launcher tree evidence for the already verified Rust-backed help/version/config/export/transform slice.
    - Docs no longer contain stale statements that Linux or Windows packaged launcher shared evidence/status publication remains future work.
    - Docs still explicitly defer signing, installers, AppImage, MSI, DMG, GUI packaging, release archives, native/cross-compiled release binaries, broad bundled dependency layout, downstream fork work, and release channels wherever broader support could be inferred.
    - No new documentation hub or broad restructure is created.
  </acceptance_criteria>
  <done>PVIS-02 is materially represented in existing migration, launcher, package, parity, and fixture documentation surfaces without broader packaging claims.</done>
</task>

<task type="auto">
  <name>Task 3: Verify v1.7 traceability and closeout metadata guidance</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/30-packaging-visibility-and-docs/30-CONTEXT.md
    - .planning/phases/30-packaging-visibility-and-docs/30-RESEARCH.md
    - AGENTS.md
  </read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>Implement D-09 through D-12. Inspect .planning/ROADMAP.md and .planning/REQUIREMENTS.md and edit only if the active v1.7 traceability tables deviate from this exact mapping: LPKG-01, LPKG-02, LPKG-03 -> Phase 27; WPKG-01, WPKG-02, WPKG-03 -> Phase 28; PKGE-01, PKGE-02, PKGE-03 -> Phase 29; PVIS-01, PVIS-02, PVIS-03 -> Phase 30. Do not rewrite Phase 27-29 summary bodies. Do not run mdformat on phase *-SUMMARY.md files. When the execution summary is created, its YAML frontmatter must use the exact hyphenated key requirements-completed with only PVIS-01, PVIS-02, and PVIS-03.</action>
  <verify>
    <automated>bazel run //packages/parity:linux_packaged_launcher_parity</automated>
    <automated>bazel run //packages/parity:windows_packaged_launcher_parity</automated>
    <automated>bazel run //packages/parity:status</automated>
    <automated>node - <<'NODE'
const fs = require('fs');
const files = {
  roadmap: fs.readFileSync('.planning/ROADMAP.md', 'utf8'),
  requirements: fs.readFileSync('.planning/REQUIREMENTS.md', 'utf8'),
};
const expected = [
  ['LPKG-01', 'Phase 27'],
  ['LPKG-02', 'Phase 27'],
  ['LPKG-03', 'Phase 27'],
  ['WPKG-01', 'Phase 28'],
  ['WPKG-02', 'Phase 28'],
  ['WPKG-03', 'Phase 28'],
  ['PKGE-01', 'Phase 29'],
  ['PKGE-02', 'Phase 29'],
  ['PKGE-03', 'Phase 29'],
  ['PVIS-01', 'Phase 30'],
  ['PVIS-02', 'Phase 30'],
  ['PVIS-03', 'Phase 30'],
];
for (const [fileName, content] of Object.entries(files)) {
  for (const [requirement, phase] of expected) {
    const escaped = requirement.replace('-', '\\-');
    const pattern = new RegExp(`\\| ${escaped} \\| ${phase} \\|`, 'g');
    const count = (content.match(pattern) || []).length;
    if (count !== 1) {
      throw new Error(`${fileName}: expected exactly one ${requirement} -> ${phase} table mapping, found ${count}`);
    }
  }
}
console.log('v1.7 traceability maps 12/12 requirements exactly once in ROADMAP and REQUIREMENTS');
NODE</automated>
    <automated>node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter get .planning/phases/30-packaging-visibility-and-docs/30-CONTEXT.md</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ROADMAP.md maps all 12 v1.7 requirements to exactly one phase using the required mapping.
    - .planning/REQUIREMENTS.md maps all 12 v1.7 requirements to exactly one phase using the required mapping.
    - Phase 27-29 summary bodies are not rewritten.
    - Phase 30 summary frontmatter guidance is followed exactly: requirements-completed contains PVIS-01, PVIS-02, and PVIS-03 only.
    - git diff --check passes.
  </acceptance_criteria>
  <done>PVIS-03 is verified, Phase 30 closeout metadata is constrained to PVIS IDs, and status/docs verification has passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Maintainer -> Bazel status command | Maintainer executes checked-in status data and expects it to describe real evidence labels. |
| Docs/status data -> human reader | Documentation and TSV rows can mislead maintainers if they overclaim packaging support. |
| Planning metadata -> audit tooling | ROADMAP, REQUIREMENTS, and SUMMARY frontmatter feed traceability and milestone audit checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-30-01 | Tampering | packages/parity/status.tsv | mitigate | Add only the two explicit platform rows and verify exact surface/status/evidence triples with awk plus bazel run //packages/parity:status. |
| T-30-02 | Repudiation | .planning/ROADMAP.md and .planning/REQUIREMENTS.md | mitigate | Run the node traceability check proving every v1.7 requirement maps to exactly one phase in both files. |
| T-30-03 | Information Disclosure | docs/port and package README files | accept | This phase adds public migration documentation only and introduces no secrets, credentials, or external service data. |
| T-30-04 | Denial of Service | Bazel parity verification commands | accept | Existing Phase 29 evidence commands are reused; no new live status execution or broader comparator work is introduced. |
| T-30-05 | Spoofing | Docs references to evidence labels | mitigate | Docs must use exact Bazel labels from packages/parity/BUILD.bazel and status.tsv; verification greps for those labels in the updated surfaces. |
</threat_model>

<verification>
Overall verification for the executor:

1. `bazel run //packages/parity:status`
2. `bazel run //packages/parity:linux_packaged_launcher_parity`
3. `bazel run //packages/parity:windows_packaged_launcher_parity`
4. The node traceability check in Task 3 prints `v1.7 traceability maps 12/12 requirements exactly once in ROADMAP and REQUIREMENTS`.
5. `git diff --check`
6. After creating `.planning/phases/30-packaging-visibility-and-docs/30-01-SUMMARY.md`, run:

```bash
node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/30-packaging-visibility-and-docs/30-01-SUMMARY.md --fields requirements_completed --pick requirements_completed
```

The extracted summary value must contain PVIS-01, PVIS-02, and PVIS-03 only. Do not run mdformat on phase *-SUMMARY.md files.
</verification>

<success_criteria>
- `packages/parity/status.tsv` publishes `linux.packaged-launcher` and `windows.packaged-launcher` verified rows with exact Phase 29 evidence targets.
- Existing docs no longer underclaim Linux/Windows scoped packaged launcher evidence as deferred.
- Existing docs still defer signing, installers, AppImage, MSI, DMG, GUI packaging, release archives, native/cross-compiled release binaries, broad bundled dependency layout, downstream fork work, and release channels.
- `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` map all 12 v1.7 requirements to exactly one phase.
- Phase 30 summary frontmatter uses `requirements-completed` with only `PVIS-01`, `PVIS-02`, and `PVIS-03`.
</success_criteria>

<output>
After completion, create `.planning/phases/30-packaging-visibility-and-docs/30-01-SUMMARY.md` with frontmatter including:

```yaml
requirements-completed:
  - PVIS-01
  - PVIS-02
  - PVIS-03
```

Do not include LPKG, WPKG, or PKGE IDs in the Phase 30 summary.
</output>
