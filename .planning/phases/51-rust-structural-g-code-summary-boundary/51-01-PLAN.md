---
phase: 51-rust-structural-g-code-summary-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
  - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
autonomous: true
requirements:
  - GCRUST-01
  - GCRUST-02
requirements_addressed:
  - GCRUST-01
  - GCRUST-02
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 51-2026-06-17T22-55-52
generated_at: 2026-06-17T23:14:34Z
must_haves:
  truths:
    - "Developer can parse the Phase 50 structural summary TSV through pure Rust without reading files at runtime."
    - "Developer can inspect row-preserving typed structural rows and a typed facts projection."
    - "Cargo and Bazel tests reject invalid headers, wrong columns, missing rows, duplicate rows, out-of-order rows, unsupported fields, unsupported claim text, wrong source refs, and wrong fixture identities."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      provides: "Dependency-free structural summary parser, typed rows, typed facts projection, and structural parse errors"
      contains: "parse_prusa_gcode_output_structural_summary"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs"
      provides: "Public re-exports for the structural parser boundary"
      contains: "PrusaGcodeOutputStructuralSummary"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs"
      provides: "Cargo and Bazel parser acceptance/rejection coverage"
      contains: "EXPECTED_GCODE_STRUCTURAL_SUMMARY"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel"
      provides: "Bazel compile_data link for the structural TSV"
      contains: "prusa_gcode_output_expected_gcode_structural_summary"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv"
      via: "include_str! plus Bazel compile_data"
      pattern: "EXPECTED_GCODE_STRUCTURAL_SUMMARY"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      to: "Phase 50 structural TSV contract"
      via: "exact six-column header and ordered sixteen-row expected list"
      pattern: "EXPECTED_STRUCTURAL_ROWS"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel"
      to: "//packages/parity-fixtures:prusa_gcode_output_expected_gcode_structural_summary"
      via: "prusa_gcode_output_test compile_data"
      pattern: "prusa_gcode_output_expected_gcode_structural_summary"
---

<objective>
Create the pure typed Rust parser boundary for the Phase 50 Prusa structural G-code sidecar.

Purpose: GCRUST-01 and GCRUST-02 need a dependency-free Rust boundary that preserves the exact row contract before Phase 52 adds public structural evidence.
Output: structural parser types/functions, public re-exports, focused rejection tests, and Bazel compile_data wiring for the structural TSV.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/51-rust-structural-g-code-summary-boundary/51-CONTEXT.md
@.planning/phases/51-rust-structural-g-code-summary-boundary/51-RESEARCH.md
@.planning/phases/50-structural-g-code-fixture-expansion/50-01-SUMMARY.md
@.planning/phases/50-structural-g-code-fixture-expansion/50-02-SUMMARY.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel

<interfaces>
From packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs:
```rust
pub fn parse_prusa_gcode_output_summary(input: &str) -> PrusaGcodeOutputParseResult;
pub fn prusa_gcode_output_summary_lines(
    input: &str,
) -> Result<Vec<String>, PrusaGcodeOutputParseError>;

pub const fn prusa_gcode_output_metadata() -> PrusaGcodeOutputMetadata;
```

From packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs:
```rust
pub(crate) const PRUSA_GCODE_OUTPUT_SOURCE_REF: VendorSourceRef =
    VendorSourceRef::prusa_slicer_version_2_9_5();
pub(crate) const PRUSA_GCODE_OUTPUT_FIXTURE_PATH: &str =
    "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode";
```

From packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel:
```python
rust_test(
    name = "prusa_gcode_output_test",
    srcs = ["tests/prusa_gcode_output.rs"],
    compile_data = [
        "src/prusa_gcode_output.rs",
        "//packages/parity-fixtures:prusa_gcode_output_expected_gcode_summary",
    ],
)
```
</interfaces>
</context>

<decision_coverage>
D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, and D-14 are covered in this plan. D-12 and D-13 are covered in 51-02.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the structural parser contract</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs, packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs</files>
  <read_first>
    - .planning/phases/51-rust-structural-g-code-summary-boundary/51-CONTEXT.md
    - .planning/phases/51-rust-structural-g-code-summary-boundary/51-RESEARCH.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  </read_first>
  <behavior>
    - Parsing the checked-in structural TSV returns exactly 16 typed rows in the TSV order.
    - The parser accepts only the exact header `source_ref\tfixture_path\tstructural_field\tstructural_category\tstructural_value\tevidence_boundary`.
    - Each row keeps typed source ref, fixture path, structural field, structural category, structural value, and exact evidence-boundary text.
    - The facts projection is derived only after row validation succeeds and exposes counts, booleans, ordered markers, source identity, and fixture identity as typed Rust values.
  </behavior>
  <action>
    Extend `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` per D-01 through D-08 with no new crate, module, dependency, filesystem access, Git access, network access, process execution, release behavior, sync behavior, or status publication.

    Add the exact constant `PRUSA_GCODE_OUTPUT_EXPECTED_STRUCTURAL_SUMMARY_PATH` with value `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv`. Add a structural parser function named `parse_prusa_gcode_output_structural_summary(input: &str) -> PrusaGcodeOutputStructuralParseResult`.

    Add public structural types with these names: `PrusaGcodeOutputStructuralSummary`, `PrusaGcodeOutputStructuralSummaryRow`, `PrusaGcodeOutputStructuralFacts`, `PrusaGcodeOutputStructuralField`, `PrusaGcodeOutputStructuralCategory`, `PrusaGcodeOutputStructuralValue`, `PrusaGcodeOutputEvidenceBoundary`, `PrusaGcodeOutputStructuralParseError`, and `PrusaGcodeOutputStructuralParseResult`.

    The field enum must contain exactly these variants and `as_str()` values in order: `SourceRef` -> `source_ref`, `InventorySourcePaths` -> `inventory_source_paths`, `FixtureSourceLiteral` -> `fixture_source_literal`, `FixtureId` -> `fixture_id`, `FixturePath` -> `fixture_path`, `CommandCountTotal` -> `command_count_total`, `CommandCountG1` -> `command_count_g1`, `SectionCountTotal` -> `section_count_total`, `OrderedMarker1` -> `ordered_marker_1`, `OrderedMarker2` -> `ordered_marker_2`, `OrderedMarker3` -> `ordered_marker_3`, `OrderedMarker4` -> `ordered_marker_4`, `MovementAxisPresent` -> `movement_axis_present`, `ExtrusionAxisPresent` -> `extrusion_axis_present`, `TemperatureMarkerCount` -> `temperature_marker_count`, and `ToolChangeMarkerCount` -> `tool_change_marker_count`.

    The category enum must accept only these exact values: `source identity`, `fixture identity`, `command counts`, `section counts`, `ordered markers`, `movement/extrusion indicators`, and `temperature/tool-change markers`.

    The structural value parser must type counts as `u32`, movement/extrusion indicators as `bool`, ordered marker values as the existing `PrusaGcodeOutputMarkerValue` variants, source refs as `VendorSourceRef`, and fixture/source identities as exact known string constants. Reject value drift with an `UnexpectedStructuralValue { line_number, structural_field, value }` error. Reject wrong source refs with `UnexpectedSourceRef`, wrong fixture path column values with `UnexpectedFixturePath`, unsupported fields with `UnsupportedStructuralField`, unsupported categories with `UnsupportedStructuralCategory`, and unsupported boundary text with `UnexpectedEvidenceBoundary`.

    The expected row list must contain exactly the 16 Phase 50 structural fields from D-06 in order. Duplicate, missing, extra, and out-of-order rows must fail by structural field key. Evidence-boundary text must be matched exactly for every expected row per D-08; do not implement forbidden-word-only scanning.

    Add `impl PrusaGcodeOutputStructuralSummary { pub fn facts(&self) -> PrusaGcodeOutputStructuralFacts { ... } }`. The facts value must include `source_ref`, `inventory_source_paths`, `fixture_source_literal`, `fixture_id`, `fixture_path`, `command_count_total`, `command_count_g1`, `section_count_total`, `ordered_markers: [PrusaGcodeOutputMarkerValue; 4]`, `movement_axis_present`, `extrusion_axis_present`, `temperature_marker_count`, and `tool_change_marker_count`.

    Update `src/lib.rs` to re-export the new public structural types and `parse_prusa_gcode_output_structural_summary`. Keep public names narrow per D-14; do not use names containing `verified`, `runtime`, `release`, `executable`, `printer`, `network`, `sync`, `gcode_parity`, or `generated_output_parity`.
  </action>
  <verify>
    <automated>cargo +1.94.1 test -p slic3r_flavors --test prusa_gcode_output --manifest-path packages/slic3r-rust/Cargo.toml</automated>
    <automated>cargo +1.94.1 fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "parse_prusa_gcode_output_structural_summary|PrusaGcodeOutputStructuralSummary|PrusaGcodeOutputStructuralFacts|EXPECTED_STRUCTURAL_ROWS|PRUSA_GCODE_OUTPUT_EXPECTED_STRUCTURAL_SUMMARY_PATH" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` finds all listed strings.
    - `rg -n "PrusaGcodeOutputStructuralSummary|parse_prusa_gcode_output_structural_summary" packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs` finds public re-exports.
    - `rg -n "std::fs|std::process|Command::|git|network|packages/parity/status.tsv|packages/parity/BUILD.bazel" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` does not find new runtime side-effect code added by this task.
    - `cargo +1.94.1 test -p slic3r_flavors --test prusa_gcode_output --manifest-path packages/slic3r-rust/Cargo.toml` exits 0.
  </acceptance_criteria>
  <done>The Rust module exposes a pure, dependency-free structural parser and typed facts projection for the exact Phase 50 TSV contract, and existing G-code parser tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add structural parser rejection coverage and Bazel data wiring</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs, packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel</files>
  <read_first>
    - .planning/phases/51-rust-structural-g-code-summary-boundary/51-CONTEXT.md
    - .planning/phases/51-rust-structural-g-code-summary-boundary/51-RESEARCH.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
    - packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs
    - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
    - packages/parity-fixtures/BUILD.bazel
  </read_first>
  <behavior>
    - Acceptance test proves the structural TSV parses to the exact row count, first/last fields, typed counts, booleans, markers, source ref, fixture id, and fixture path.
    - Rejection tests cover every GCRUST-02 class in Cargo and Bazel: invalid header, wrong column count, missing row, duplicate row, out-of-order row, unsupported structural field, unsupported claim text, wrong source ref, and wrong fixture identity.
    - Bazel has compile-time access to the structural TSV through the Phase 50 fixture alias.
  </behavior>
  <action>
    Update `packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs` per D-09 and D-10. Add `EXPECTED_GCODE_STRUCTURAL_SUMMARY` using `include_str!("../../../../parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv")`. Add focused one-concern tests with Arrange/Act/Assert comments and helper functions that mutate only the structural TSV input.

    Add these exact test functions or equivalent names with the same behaviors: `parses_expected_gcode_structural_summary_rows_and_facts`, `rejects_structural_invalid_header`, `rejects_structural_wrong_column_count`, `rejects_structural_missing_row`, `rejects_structural_duplicate_row`, `rejects_structural_out_of_order_rows`, `rejects_unsupported_structural_field`, `rejects_structural_unsupported_boundary_claim`, `rejects_structural_unexpected_source_ref`, `rejects_structural_unexpected_fixture_path`, and `rejects_structural_unexpected_fixture_id`.

    The acceptance test must assert row count `16`, field order from `SourceRef` through `ToolChangeMarkerCount`, `command_count_total == 4`, `command_count_g1 == 4`, `section_count_total == 1`, ordered markers `[FeedrateFixedPoint, FeedrateInteger, FeedrateOneDecimal, FeedrateThreeDecimalRounded]`, `movement_axis_present == false`, `extrusion_axis_present == false`, `temperature_marker_count == 0`, `tool_change_marker_count == 0`, fixture id `gcodewriter-set-speed.gcode`, and fixture path `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode`.

    Update `packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` so `prusa_gcode_output_test` `compile_data` includes `//packages/parity-fixtures:prusa_gcode_output_expected_gcode_structural_summary` next to the existing summary TSV alias. Do not create a new Bazel test target and do not edit `packages/parity/BUILD.bazel` or `packages/parity/status.tsv`; D-11 keeps public structural command/status work out of Phase 51.
  </action>
  <verify>
    <automated>cargo +1.94.1 test -p slic3r_flavors --test prusa_gcode_output --manifest-path packages/slic3r-rust/Cargo.toml</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "EXPECTED_GCODE_STRUCTURAL_SUMMARY|parses_expected_gcode_structural_summary_rows_and_facts|rejects_structural_invalid_header|rejects_structural_wrong_column_count|rejects_structural_missing_row|rejects_structural_duplicate_row|rejects_structural_out_of_order_rows|rejects_unsupported_structural_field|rejects_structural_unsupported_boundary_claim|rejects_structural_unexpected_source_ref|rejects_structural_unexpected_fixture_path|rejects_structural_unexpected_fixture_id" packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs` finds all required test names or exact behavior labels.
    - `rg -n "prusa_gcode_output_expected_gcode_structural_summary" packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` finds the structural TSV compile_data alias.
    - `test -z "$(git diff --name-only -- packages/parity packages/parity/status.tsv)"` exits 0.
    - `cargo +1.94.1 test -p slic3r_flavors --test prusa_gcode_output --manifest-path packages/slic3r-rust/Cargo.toml` exits 0.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test` exits 0.
  </acceptance_criteria>
  <done>Cargo and Bazel both prove the structural parser accepts only the exact Phase 50 structural TSV contract and rejects every GCRUST-02 malformed-input class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller supplied string -> Rust parser | Untrusted TSV text enters the pure structural parser. |
| structural TSV rows -> typed facts projection | Parsed rows become inspectable typed facts only after full row validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01-01 | Tampering | `parse_prusa_gcode_output_structural_summary` | mitigate | Validate exact header, six columns, non-empty required values, exact 16 structural fields, duplicate rows, missing rows, extra rows, and row order before returning `PrusaGcodeOutputStructuralSummary`. |
| T-51-01-02 | Spoofing | source and fixture parsing | mitigate | Reject any source ref other than `VendorSourceRef::prusa_slicer_version_2_9_5()`, any fixture path other than the checked-in G-code fixture path, and any `fixture_id` value other than `gcodewriter-set-speed.gcode`. |
| T-51-01-03 | Repudiation | evidence-boundary text | mitigate | Treat `evidence_boundary` as exact contract data and return `UnexpectedEvidenceBoundary` for unsupported claim text such as `full generated-output parity verified`. |
| T-51-01-04 | Elevation of Scope | parser implementation | mitigate | Keep the parser pure data-in/data-out: no `std::fs`, Git, network, process execution, release behavior, sync behavior, `packages/parity` edits, or status publication. |
| T-51-01-05 | Information Disclosure | structural parser output | accept | The parser returns only checked-in fixture metadata and structural counts with no secrets, credentials, user data, or runtime environment data. |
</threat_model>

<verification>
Run these commands serially:

```bash
cargo +1.94.1 test -p slic3r_flavors --test prusa_gcode_output --manifest-path packages/slic3r-rust/Cargo.toml
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test
bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check
cargo +1.94.1 fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check
git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/tests/prusa_gcode_output.rs packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
test -z "$(git diff --name-only -- packages/parity packages/parity/status.tsv)"
```
</verification>

<success_criteria>
GCRUST-01 and GCRUST-02 are complete when `parse_prusa_gcode_output_structural_summary` parses the checked-in structural TSV into typed rows/facts, Cargo and Bazel rejection tests cover every required malformed-input class, no new dependency is added, and no public parity command or status row is created.
</success_criteria>

<output>
After completion, create `.planning/phases/51-rust-structural-g-code-summary-boundary/51-01-SUMMARY.md` with `requirements-completed: [GCRUST-01, GCRUST-02]`.
</output>
