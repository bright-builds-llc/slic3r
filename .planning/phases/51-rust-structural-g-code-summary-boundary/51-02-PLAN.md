---
phase: 51-rust-structural-g-code-summary-boundary
plan: "02"
type: execute
wave: 2
depends_on:
  - "51-01"
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
  - packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
  - packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
autonomous: true
requirements:
  - GCRUST-03
requirements_addressed:
  - GCRUST-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 51-2026-06-17T22-55-52
generated_at: 2026-06-17T23:14:34Z
must_haves:
  truths:
    - "Developer can inspect structural Prusa G-code readiness as pure Rust metadata."
    - "The registry row remains a `FutureCandidate` with `ParitySurface::generated_outputs()` rather than status verification."
    - "No filesystem discovery, Git, network, process execution, release behavior, sync behavior, public parity command, or status publication is introduced."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      provides: "Pure structural readiness metadata constants and typed function"
      contains: "prusa_gcode_output_structural_readiness"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs"
      provides: "Public re-export for structural readiness metadata"
      contains: "PrusaGcodeOutputStructuralReadiness"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      provides: "Narrow registry wording for structural readiness without status publication"
      contains: "structural summary readiness is parser metadata only"
    - path: "packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs"
      provides: "Registry tests for structural readiness and no-overclaim semantics"
      contains: "prusa_gcode_output_structural_readiness"
  key_links:
    - from: "packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs"
      to: "packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs"
      via: "G-code metadata constants used by the existing capability row"
      pattern: "PRUSA_GCODE_OUTPUT_PROVENANCE"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs"
      to: "prusa_gcode_output_structural_readiness()"
      via: "metadata assertions for source, fixture, structural summary path, FutureCandidate, generated_outputs, and reserved future status token"
      pattern: "prusa_gcode_output_structural_readiness"
    - from: "packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs"
      to: "packages/parity/status.tsv"
      via: "absence guard in verification command, not runtime file reads"
      pattern: "generated_outputs"
---

<objective>
Expose structural Prusa G-code parser readiness through pure Rust metadata and existing registry tests.

Purpose: GCRUST-03 needs developers to inspect structural summary readiness without filesystem discovery, Git, network, process execution, release behavior, sync behavior, or premature status publication.
Output: typed structural readiness metadata, narrow registry wording, public re-exports, and registry tests preserving `FutureCandidate`, `generated_outputs`, and reserved status token semantics.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/51-rust-structural-g-code-summary-boundary/51-CONTEXT.md
@.planning/phases/51-rust-structural-g-code-summary-boundary/51-RESEARCH.md
@.planning/phases/51-rust-structural-g-code-summary-boundary/51-01-SUMMARY.md
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
@packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
@packages/parity/status.tsv

<interfaces>
From 51-01 output in packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs:
```rust
pub fn parse_prusa_gcode_output_structural_summary(
    input: &str,
) -> PrusaGcodeOutputStructuralParseResult;
pub(crate) const PRUSA_GCODE_OUTPUT_EXPECTED_STRUCTURAL_SUMMARY_PATH: &str =
    "packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv";
```

From packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs:
```rust
pub struct FlavorCapability {
    pub flavor_id: FlavorId,
    pub capability_id: &'static str,
    pub feature_surface: &'static str,
    pub feature_category: &'static str,
    pub origin: FeatureOrigin,
    pub parity_dependencies: &'static [ParitySurface],
    pub checklist_status: ChecklistStatus,
    pub provenance: &'static [FlavorProvenance],
    pub caution_flags: &'static [&'static str],
    pub future_parity_notes: &'static str,
}
```
</interfaces>
</context>

<decision_coverage>
D-12, D-13, and D-14 are covered in this plan. D-01 through D-11 are already covered by 51-01 and must not be weakened here.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure structural readiness metadata</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs, packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs</files>
  <read_first>
    - .planning/phases/51-rust-structural-g-code-summary-boundary/51-CONTEXT.md
    - .planning/phases/51-rust-structural-g-code-summary-boundary/51-RESEARCH.md
    - .planning/phases/51-rust-structural-g-code-summary-boundary/51-01-SUMMARY.md
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
    - packages/parity/status.tsv
  </read_first>
  <behavior>
    - A caller can get structural readiness metadata without reading the structural TSV from disk.
    - Metadata names the inventory id, source ref, source paths, fixture path, expected structural summary path, parser boundary, generated-output parity dependency, FutureCandidate checklist status, and reserved future status token.
    - Metadata does not claim public evidence, status verification, runtime behavior, release behavior, sync behavior, or generated-output completion.
  </behavior>
  <action>
    Extend `packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` per D-12 through D-14 with pure typed metadata only.

    Add `pub struct PrusaGcodeOutputStructuralReadiness` with these exact fields: `inventory_id: &'static str`, `source_ref: VendorSourceRef`, `inventory_source_paths: &'static [&'static str]`, `fixture_path: &'static str`, `expected_structural_summary_path: &'static str`, `parser_boundary: &'static str`, `parity_dependency: ParitySurface`, `checklist_status: ChecklistStatus`, and `reserved_future_status_token: &'static str`.

    Add `static PRUSA_GCODE_OUTPUT_INVENTORY_SOURCE_PATHS: [&str; 2] = ["src/libslic3r/GCode.cpp", "src/libslic3r/GCode.hpp"];`. Add `pub const fn prusa_gcode_output_structural_readiness() -> PrusaGcodeOutputStructuralReadiness` returning these exact values: inventory id `prusaslicer.gcode-output`, source ref `PRUSA_GCODE_OUTPUT_SOURCE_REF`, source paths `&PRUSA_GCODE_OUTPUT_INVENTORY_SOURCE_PATHS`, fixture path `PRUSA_GCODE_OUTPUT_FIXTURE_PATH`, expected structural summary path `PRUSA_GCODE_OUTPUT_EXPECTED_STRUCTURAL_SUMMARY_PATH`, parser boundary `slic3r_flavors::prusa_gcode_output::parse_prusa_gcode_output_structural_summary`, parity dependency `ParitySurface::generated_outputs()`, checklist status `ChecklistStatus::FutureCandidate`, and reserved future status token `PRUSA_GCODE_OUTPUT_RESERVED_STATUS_TOKEN`.

    If `PrusaGcodeOutputMetadata` was not extended in 51-01, add `expected_structural_summary_path: &'static str` to it and return `PRUSA_GCODE_OUTPUT_EXPECTED_STRUCTURAL_SUMMARY_PATH` from `prusa_gcode_output_metadata()`. This is full metadata, not a status publication.

    Update `src/lib.rs` to re-export `PrusaGcodeOutputStructuralReadiness` and `prusa_gcode_output_structural_readiness`. Do not add a binary, public parity command, filesystem discovery helper, Git helper, network helper, process helper, release helper, sync helper, or status-publication helper.
  </action>
  <verify>
    <automated>cargo +1.94.1 test -p slic3r_flavors --test flavor_registry --manifest-path packages/slic3r-rust/Cargo.toml</automated>
    <automated>cargo +1.94.1 test -p slic3r_flavors --test prusa_gcode_output --manifest-path packages/slic3r-rust/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "PrusaGcodeOutputStructuralReadiness|prusa_gcode_output_structural_readiness|PRUSA_GCODE_OUTPUT_INVENTORY_SOURCE_PATHS|expected_structural_summary_path" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` finds all listed strings.
    - `rg -n "PrusaGcodeOutputStructuralReadiness|prusa_gcode_output_structural_readiness" packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs` finds public re-exports.
    - `rg -n "std::fs|std::process|Command::|git|network|packages/parity/status.tsv|packages/parity/BUILD.bazel" packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs` does not find new runtime side-effect code added by this task.
    - `test -z "$(git diff --name-only -- packages/parity packages/parity/status.tsv packages/parity/BUILD.bazel)"` exits 0.
  </acceptance_criteria>
  <done>Structural readiness is inspectable through pure typed Rust metadata while preserving `FutureCandidate`, `generated_outputs`, and reserved future status token semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove registry readiness without status overclaiming</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs, packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs</files>
  <read_first>
    - .planning/phases/51-rust-structural-g-code-summary-boundary/51-CONTEXT.md
    - .planning/phases/51-rust-structural-g-code-summary-boundary/51-RESEARCH.md
    - .planning/phases/51-rust-structural-g-code-summary-boundary/51-01-SUMMARY.md
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs
    - packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
    - packages/parity/status.tsv
  </read_first>
  <behavior>
    - Registry G-code capability remains `FeatureOrigin::SharedDownstream`, `ChecklistStatus::FutureCandidate`, and dependent on `ParitySurface::generated_outputs()`.
    - Registry wording names structural summary readiness as parser metadata only and leaves public structural evidence/status publication to Phase 52.
    - Tests fail if readiness metadata points at the wrong structural summary path, source ref, fixture path, status token, checklist status, or parity dependency.
  </behavior>
  <action>
    Update the `prusaslicer.gcode-output` capability in `packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs` without changing `FlavorCapability` shape unless Task 1 made a compile-required metadata change. Keep `capability_id`, `feature_surface`, `feature_category`, provenance, parity dependency, checklist status, and caution flags unchanged. Replace only `future_parity_notes` for the G-code row with this exact string: `Source-observed G-code output planning row; structural summary readiness is parser metadata only, and public structural evidence plus status publication remain Phase 52 work before generated-output behavior is claimed.`

    Update `packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` to import `prusa_gcode_output_structural_readiness`. Add a test named `prusa_gcode_output_structural_readiness_exposes_parser_metadata_without_status_publication` or an equivalent behavior label. Assert exact values: inventory id `prusaslicer.gcode-output`, source ref `VendorSourceRef::prusa_slicer_version_2_9_5()`, inventory source paths `["src/libslic3r/GCode.cpp", "src/libslic3r/GCode.hpp"]`, fixture path `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/gcodewriter-set-speed.gcode`, expected structural summary path `packages/parity-fixtures/forks/prusaslicer/prusaslicer.gcode-output/expected-gcode-structural-summary.tsv`, parser boundary `slic3r_flavors::prusa_gcode_output::parse_prusa_gcode_output_structural_summary`, parity dependency `ParitySurface::generated_outputs()`, checklist status `ChecklistStatus::FutureCandidate`, and reserved future status token `fork.prusaslicer.gcode-output`.

    Extend the existing `prusa_gcode_output_registry_row_traces_to_source_and_generated_output_dependency` or add a focused adjacent test so the G-code capability notes equal the exact replacement string, `caution_flags` remains empty, and the registry row still appears in `capabilities_by_checklist_status(ChecklistStatus::FutureCandidate)`.

    Extend `runtime_claim_words_do_not_become_public_helper_names` so `public_helper_names` includes `prusa_gcode_output_structural_readiness` and `metadata_values` includes `expected_structural_summary_path`, `parser_boundary`, and `reserved_future_status_token` from the readiness value. Do not add banned helper names containing `verified`, `runtime`, `release`, `executable`, `printer`, `network`, `sync`, `gcode_parity`, or `generated_output_parity`.
  </action>
  <verify>
    <automated>cargo +1.94.1 test -p slic3r_flavors --test flavor_registry --manifest-path packages/slic3r-rust/Cargo.toml</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test</automated>
    <automated>bazel build //packages/slic3r-rust/crates/slic3r_flavors:clippy</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "structural summary readiness is parser metadata only|public structural evidence plus status publication remain Phase 52 work" packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` finds the exact registry wording and tests.
    - `rg -n "prusa_gcode_output_structural_readiness_exposes_parser_metadata_without_status_publication|prusa_gcode_output_structural_readiness|expected-gcode-structural-summary.tsv|ChecklistStatus::FutureCandidate|ParitySurface::generated_outputs" packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs` finds the readiness test coverage.
    - `awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `awk -F '\t' '$1=="fork.prusaslicer.gcode-output" && $2=="verified" && $3=="//packages/parity:prusaslicer_gcode_output_parity" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `test -z "$(git diff --name-only -- packages/parity packages/parity/status.tsv packages/parity/BUILD.bazel)"` exits 0.
    - `cargo +1.94.1 test -p slic3r_flavors --test flavor_registry --manifest-path packages/slic3r-rust/Cargo.toml` exits 0.
    - `bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test` exits 0.
  </acceptance_criteria>
  <done>The registry and readiness tests prove structural parser readiness is inspectable as metadata while broad generated-output status remains in progress and no Phase 52 command/status publication is introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust metadata -> developer inspection | Static readiness metadata may be interpreted as evidence status if wording or status fields overclaim. |
| registry capability -> parity status consumers | The capability row uses generated-output dependency metadata but must not mutate public parity status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02-01 | Repudiation | `future_parity_notes` | mitigate | Use exact wording that says structural summary readiness is parser metadata only and public structural evidence plus status publication remain Phase 52 work before generated-output behavior is claimed. |
| T-51-02-02 | Tampering | structural readiness metadata | mitigate | Test exact source ref, source paths, fixture path, expected structural summary path, parser boundary, parity dependency, checklist status, and reserved future status token. |
| T-51-02-03 | Elevation of Scope | registry and metadata functions | mitigate | Keep metadata pure: no filesystem discovery, Git, network, process execution, release behavior, sync behavior, public parity command, status mutation, or `packages/parity` edits. |
| T-51-02-04 | Spoofing | registry provenance | mitigate | Preserve existing `PRUSA_GCODE_OUTPUT_PROVENANCE` source ref and source paths, and assert registry capability provenance matches `prusa_gcode_output_metadata()`. |
| T-51-02-05 | Information Disclosure | readiness metadata | accept | Metadata contains only checked-in source/fixture paths and public status tokens; it exposes no secrets, credentials, user data, or environment data. |
</threat_model>

<verification>
Run these commands serially:

```bash
cargo +1.94.1 test -p slic3r_flavors --test prusa_gcode_output --manifest-path packages/slic3r-rust/Cargo.toml
cargo +1.94.1 test -p slic3r_flavors --test flavor_registry --manifest-path packages/slic3r-rust/Cargo.toml
bazel test //packages/slic3r-rust/crates/slic3r_flavors:prusa_gcode_output_test
bazel test //packages/slic3r-rust/crates/slic3r_flavors:flavor_registry_test
bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check
bazel build //packages/slic3r-rust/crates/slic3r_flavors:clippy
cargo +1.94.1 fmt --manifest-path packages/slic3r-rust/Cargo.toml --all -- --check
cargo +1.94.1 clippy -p slic3r_flavors --all-targets --manifest-path packages/slic3r-rust/Cargo.toml -- -D warnings
awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { found=1 } END { exit found ? 0 : 1 }' packages/parity/status.tsv
test -z "$(git diff --name-only -- packages/parity packages/parity/status.tsv packages/parity/BUILD.bazel)"
git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/prusa_gcode_output.rs packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs packages/slic3r-rust/crates/slic3r_flavors/src/registry.rs packages/slic3r-rust/crates/slic3r_flavors/tests/flavor_registry.rs
```
</verification>

<success_criteria>
GCRUST-03 is complete when structural parser readiness is exposed through pure typed Rust metadata and registry tests while `ChecklistStatus::FutureCandidate`, `ParitySurface::generated_outputs()`, `reserved_future_status_token`, `generated-outputs` in-progress status, and the existing narrow `fork.prusaslicer.gcode-output` status row remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/51-rust-structural-g-code-summary-boundary/51-02-SUMMARY.md` with `requirements-completed: [GCRUST-03]`.
</output>
